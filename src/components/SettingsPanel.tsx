// M08.G red-phase stub — replaced by the real focused settings surface
// (tier + budget controls) in the green-phase impl commit.
export function SettingsPanel(): JSX.Element {
  return <section />;
}
