// M08.D1 stub — red phase. The interactive Skill node is implemented
// in the impl commit.
import type { NodeProps } from '@xyflow/react';

export function BuilderSkillNode(_props: NodeProps): JSX.Element {
  return <div />;
}
