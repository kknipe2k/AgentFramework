// M08.D1 stub — red phase. The interactive Hook node is implemented
// in the impl commit.
import type { NodeProps } from '@xyflow/react';

export function BuilderHookNode(_props: NodeProps): JSX.Element {
  return <div />;
}
