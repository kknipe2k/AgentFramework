// M08.D1 stub — red phase. The interactive HITL node is implemented
// in the impl commit.
import type { NodeProps } from '@xyflow/react';

export function BuilderHitlNode(_props: NodeProps): JSX.Element {
  return <div />;
}
