// M08.F2 — the Tester's scoped graph pane. RED-PHASE STUB; the green
// phase implements the scoped React-Flow rendering.
export function TesterGraphPane(): JSX.Element {
  return <div className="tester-graph-pane" data-testid="tester-graph-pane" />;
}
