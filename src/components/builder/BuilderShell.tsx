// M08.C red-phase stub — green ships the three-panel Builder grid
// (Palette / empty Canvas region / empty Inspector region).
export function BuilderShell(): JSX.Element {
  return <div data-testid="builder-shell" />;
}
