// M06.E stub — green phase implements the MCP Servers panel (list +
// status + Add/Test/Remove). Red phase: returns null so behavior tests
// fail right-reason ("unable to find" — the surface is not yet
// implemented).
export function MCPServerSettings(): JSX.Element | null {
  return null;
}
