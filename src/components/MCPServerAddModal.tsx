// M06.E stub — green phase implements the Add-server form + tier-eval
// display. Red phase: returns null so behavior tests fail right-reason
// ("unable to find" — the surface is not yet implemented).
interface MCPServerAddModalProps {
  onClose: () => void;
}

export function MCPServerAddModal(_props: MCPServerAddModalProps): JSX.Element | null {
  return null;
}
