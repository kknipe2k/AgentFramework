// M08.D1 stub — red phase. The shared plain-English capability
// disclosure is implemented in the impl commit.

export interface CapabilityDisclosureProps {
  capabilities: string[];
  emptyMessage: string;
  'data-testid'?: string;
}

export function CapabilityDisclosure(_props: CapabilityDisclosureProps): JSX.Element | null {
  return null;
}
