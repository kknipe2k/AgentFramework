{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://schemas.aria-runtime.dev/event.v1.json",
  "title": "AgentEvent",
  "description": "Canonical AgentEvent union emitted by the runtime. Source-of-truth for renderer TypeScript types (generated via `cargo xtask regenerate-types`). Mirrors `runtime_core::event::AgentEvent` (crates/runtime-core/src/event.rs). Variants span spec sections §2 (session/agent/tool/skill/stream/decision/token), §3a (plan/task), §3b (mode), §4a (verify+rails), §4b (gap), §6a (HITL), §8.security (capability), §2a (budget). Variants the v0.1 runtime does not yet emit are still defined here so future milestones extend the union additively.",
  "$defs": {
    "ToolSource": {
      "type": "string",
      "enum": ["builtin", "mcp", "generated"],
      "description": "Origin of a tool invocation. Spec §0b + §2."
    },
    "ApprovedBy": {
      "type": "string",
      "enum": ["user", "auto"],
      "title": "ApprovedBy",
      "description": "Source of the plan approval. `user` = HITL approval seam resolved by the renderer; `auto` = framework JSON declared `approval_required: false`."
    },
    "HookCategoryRef": {
      "type": "string",
      "enum": ["verify", "lint", "build", "test", "custom"],
      "title": "HookCategoryRef",
      "description": "Hook category — spec §4a. Mirrors common.v1.json HookCategory; embedded in verify_started events."
    },
    "OnFailureRef": {
      "type": "string",
      "enum": ["block", "warn", "rollback"],
      "title": "OnFailureRef",
      "description": "Hook on_failure policy — spec §4a. Mirrors common.v1.json HookOnFailure; embedded in verify_failed events."
    },
    "RailPolicy": {
      "type": "string",
      "enum": ["hard", "soft"],
      "title": "RailPolicy",
      "description": "Rail policy — spec §4a. Hard rails block; soft rails warn."
    },
    "CapabilityKindRef": {
      "type": "string",
      "title": "CapabilityKindRef",
      "description": "Capability-kind discriminator embedded in capability_violation + capability_grant events. Spec §8.security L1 — mirrors capability.v1.json CapabilityKind (5 values, locked). The mirror exists because typify does not support cross-schema $ref to enums; per-schema $defs duplication is the established M04.D pattern (HookCategoryRef, OnFailureRef, RailPolicy, HitlTriggerRef, GapSeverityRef).",
      "enum": ["read", "write", "exec", "network", "process_spawn"]
    },
    "RequestedAction": {
      "type": "string",
      "title": "RequestedAction",
      "description": "Plain-English description of what the agent attempted; surfaced in the HITL prompt + capability-violation modal so the user understands the rejection. Per gotcha #43 (typify-friendly extraction): validated inline strings inside oneOf variants are extracted to $defs with a title.",
      "minLength": 1
    },
    "DeclaredScope": {
      "type": "string",
      "title": "DeclaredScope",
      "description": "Plain-English description of the scope the agent's grants cover. Renderer pairs with `requested_action` to surface the mismatch (e.g., grants cover `src/**` glob; request targeted `~/.ssh/keys`). Per gotcha #43 (typify-friendly extraction).",
      "minLength": 1
    },
    "GrantedResource": {
      "type": "string",
      "title": "GrantedResource",
      "description": "Resource name the grant covers — tool name, file glob, network hostname, or sub-agent id depending on `capability_kind`. Per gotcha #43 (typify-friendly extraction).",
      "minLength": 1
    },
    "HitlTriggerRef": {
      "type": "string",
      "title": "HitlTriggerRef",
      "description": "HITL trigger discriminator embedded in HITL + notifier events. Spec §6a — mirrors hitl.v1.json HitlTrigger (9 values, locked). The mirror exists because typify does not support cross-schema $ref to enums; per-schema $defs duplication is the established M04.D pattern (HookCategoryRef, OnFailureRef, RailPolicy).",
      "enum": [
        "on_gap",
        "on_risky_tool",
        "on_dont_touch_edit",
        "on_failure_threshold",
        "on_capability_violation",
        "on_budget_threshold",
        "on_plan_approval",
        "per_task",
        "per_epic"
      ]
    },
    "HitlUiVariantRef": {
      "type": "string",
      "title": "HitlUiVariantRef",
      "description": "HITL UI variant discriminator embedded in `hitl_requested` events. Spec §6a — mirrors hitl.v1.json HitlUiVariant.",
      "enum": ["panel", "modal", "toast"]
    },
    "GapSeverity": {
      "type": "string",
      "title": "GapSeverity",
      "description": "Severity matrix per spec §4b. `critical` blocks the session (loader-detected tool gaps; agent_missing); `important` blocks the owning agent but lets the session continue (request_capability tool gaps); `advisory` warns and continues (skill gaps from any source); `requested` is the M05 marker for request_capability-emitted gaps so the renderer can distinguish loader vs meta-tool origin in the same severity tier.",
      "enum": ["critical", "important", "advisory", "requested"]
    },
    "GapSource": {
      "type": "string",
      "title": "GapSource",
      "description": "How this gap was discovered. `loader` = the framework_loader walked the framework JSON at session start and the reference did not resolve. `request_capability` = a running agent asked for the capability mid-session via the meta-tool. Drives renderer display + HITL prompt copy.",
      "enum": ["loader", "request_capability"]
    },
    "SuggestedAction": {
      "type": "string",
      "title": "SuggestedAction",
      "description": "Plain-English next-step text the renderer surfaces in the GapPanel + HITL prompt. Free-form; emitter composes per severity (e.g. \"Install tool 'fetch_prs' and click Resume\" for critical; \"Skill 'rag' continues without — install async or dismiss\" for advisory). minLength 1 — emitters MUST supply a non-empty action.",
      "minLength": 1
    },
    "TierRef": {
      "type": "string",
      "title": "TierRef",
      "description": "User tier discriminator — spec §8.security L4 + §0d release scope (M05 Stage D). Two-tier system in v0.1: `novice` (curated allowlist — read + HTTPS-only network) is the first-run default; `promoted` (full capability surface; L1 still narrows) is the user-toggled tier. Full tier is post-v0.1. The mirror exists because typify does not support cross-schema $ref to enums; per-schema $defs duplication is the established M04.D pattern (HookCategoryRef, OnFailureRef, RailPolicy, HitlTriggerRef, CapabilityKindRef).",
      "enum": ["novice", "promoted"]
    },
    "TierForbiddenAction": {
      "type": "string",
      "title": "TierForbiddenAction",
      "description": "Plain-English description of what the agent attempted; surfaced in the tier-violation modal so the user understands the rejection. Pairs with `capability_kind` (e.g., \"attempted to write to src/lib.rs while in Novice tier\"). Per gotcha #43 (typify-friendly extraction).",
      "minLength": 1
    },
    "TierTransitionReason": {
      "type": "string",
      "title": "TierTransitionReason",
      "description": "Plain-English reason for the tier transition. For promotions: user clicked confirm in the Settings panel. For demotions: user clicked demote OR a downgrade-on-uncertainty affordance fired. Per gotcha #43 (typify-friendly extraction).",
      "minLength": 1
    },
    "McpServerNameRef": {
      "type": "string",
      "title": "McpServerNameRef",
      "description": "MCP server identifier embedded in M06.C lifecycle events (mcp_installed / mcp_uninstalled / mcp_auth_granted). Mirrors mcp.v1.json#/$defs/McpServerName (DNS-label pattern, 1–64 chars). The mirror exists because typify does not support cross-schema $ref to validated string newtypes; per-schema $defs duplication is the established M04.D pattern (HookCategoryRef, OnFailureRef, RailPolicy, HitlTriggerRef, CapabilityKindRef, TierRef).",
      "minLength": 1,
      "maxLength": 64,
      "pattern": "^[a-z0-9][a-z0-9-]*$"
    },
    "McpTransportKind": {
      "type": "string",
      "title": "McpTransportKind",
      "description": "Transport discriminant — mirrors the McpTransport oneOf type tag from mcp.v1.json. Embedded inline rather than via cross-schema $ref because typify cross-schema enum refs require per-schema $defs duplication (pattern: HookCategoryRef, OnFailureRef, RailPolicy, HitlTriggerRef, CapabilityKindRef).",
      "enum": ["stdio", "http"]
    },
    "NarrowedFromGrantDescription": {
      "type": "string",
      "title": "NarrowedFromGrantDescription",
      "description": "One short, human-readable description of a parent capability that the L2a narrowing seam considered when computing a child's narrowed grants. Format is emitter-defined (e.g., `read:src:glob:src/**`); renderer surfaces verbatim. Per gotcha #43 (typify-friendly extraction): inline-validated strings inside oneOf variants must be extracted to $defs with a title or typify panics.",
      "minLength": 1
    }
  },
  "oneOf": [
    {
      "type": "object",
      "title": "SessionStart",
      "additionalProperties": false,
      "required": ["type", "session_id", "framework", "model"],
      "properties": {
        "type": { "const": "session_start" },
        "session_id": { "type": "string" },
        "framework": { "type": "string" },
        "model": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "SessionEnd",
      "additionalProperties": false,
      "required": ["type", "session_id", "duration_ms", "end_reason"],
      "properties": {
        "type": { "const": "session_end" },
        "session_id": { "type": "string" },
        "duration_ms": { "type": "integer", "minimum": 0 },
        "end_reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "AgentSpawned",
      "additionalProperties": false,
      "required": ["type", "agent_id", "agent_name", "session_id"],
      "properties": {
        "type": { "const": "agent_spawned" },
        "agent_id": { "type": "string" },
        "agent_name": { "type": "string" },
        "parent_id": { "type": ["string", "null"] },
        "session_id": { "type": "string" },
        "narrowed_from": {
          "type": "array",
          "items": { "$ref": "#/$defs/NarrowedFromGrantDescription" },
          "description": "Optional — when this spawn passed L2a narrowing (M06 Stage A wire-up of the M05.B `narrow` primitive at the framework-loader walk site), carries one short human-readable description per proposed (pre-narrow) child capability so the renderer's AgentNode debug inspector can surface 'spawned with narrowed scope' attribution per §8.security L2a. Absent for top-level agents (no parent grants to narrow against) or for spawns that did not flow through the narrowing seam (e.g., the M02 smoke session that pre-dates the wire-up). Present (even as an empty array) signals 'this spawn was gated by L2a'. Per the M04.D mirror-not-cross-schema-ref pattern (CapabilityKindRef etc.) — Vec<String> over Vec<CapabilityDeclaration> avoids cross-schema $ref friction in the json-schema-to-typescript pipeline; the underlying CapabilityDeclaration data is serialized to its `kind:resource:scope:side_effect_class` string form at emit time."
        }
      }
    },
    {
      "type": "object",
      "title": "AgentComplete",
      "additionalProperties": false,
      "required": ["type", "agent_id", "result"],
      "properties": {
        "type": { "const": "agent_complete" },
        "agent_id": { "type": "string" },
        "result": { "type": "string" },
        "tokens_total": { "type": ["integer", "null"], "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "AgentError",
      "additionalProperties": false,
      "required": ["type", "agent_id", "error"],
      "properties": {
        "type": { "const": "agent_error" },
        "agent_id": { "type": "string" },
        "error": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "ToolInvoked",
      "additionalProperties": false,
      "required": ["type", "agent_id", "tool_name", "source", "input"],
      "properties": {
        "type": { "const": "tool_invoked" },
        "agent_id": { "type": "string" },
        "tool_name": { "type": "string" },
        "source": { "$ref": "#/$defs/ToolSource" },
        "server": { "type": ["string", "null"] },
        "input": {}
      }
    },
    {
      "type": "object",
      "title": "ToolResult",
      "additionalProperties": false,
      "required": ["type", "agent_id", "tool_name", "output", "duration_ms"],
      "properties": {
        "type": { "const": "tool_result" },
        "agent_id": { "type": "string" },
        "tool_name": { "type": "string" },
        "output": {},
        "duration_ms": { "type": "integer", "minimum": 0 },
        "tokens_in": { "type": ["integer", "null"], "minimum": 0 },
        "tokens_out": { "type": ["integer", "null"], "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "ToolError",
      "additionalProperties": false,
      "required": ["type", "agent_id", "tool_name", "error"],
      "properties": {
        "type": { "const": "tool_error" },
        "agent_id": { "type": "string" },
        "tool_name": { "type": "string" },
        "error": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "SkillLoaded",
      "additionalProperties": false,
      "required": ["type", "agent_id", "skill_name"],
      "properties": {
        "type": { "const": "skill_loaded" },
        "agent_id": { "type": "string" },
        "skill_name": { "type": "string" },
        "mode": { "type": ["string", "null"] }
      }
    },
    {
      "type": "object",
      "title": "PlanCreated",
      "additionalProperties": false,
      "required": ["type", "plan_id", "title", "task_count", "approval_required"],
      "properties": {
        "type": { "const": "plan_created" },
        "plan_id": { "type": "string" },
        "title": { "type": "string" },
        "task_count": { "type": "integer", "minimum": 0 },
        "approval_required": { "type": "boolean" }
      }
    },
    {
      "type": "object",
      "title": "PlanApprovalRequested",
      "additionalProperties": false,
      "required": ["type", "plan_id"],
      "properties": {
        "type": { "const": "plan_approval_requested" },
        "plan_id": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "PlanApproved",
      "additionalProperties": false,
      "required": ["type", "plan_id", "approved_by"],
      "properties": {
        "type": { "const": "plan_approved" },
        "plan_id": { "type": "string" },
        "approved_by": { "$ref": "#/$defs/ApprovedBy" }
      }
    },
    {
      "type": "object",
      "title": "PlanRevised",
      "additionalProperties": false,
      "required": ["type", "plan_id", "revision_reason"],
      "properties": {
        "type": { "const": "plan_revised" },
        "plan_id": { "type": "string" },
        "revision_reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "PlanAborted",
      "additionalProperties": false,
      "required": ["type", "plan_id", "reason"],
      "properties": {
        "type": { "const": "plan_aborted" },
        "plan_id": { "type": "string" },
        "reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "PlanComplete",
      "additionalProperties": false,
      "required": ["type", "plan_id", "duration_ms"],
      "properties": {
        "type": { "const": "plan_complete" },
        "plan_id": { "type": "string" },
        "duration_ms": { "type": "integer", "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "TaskStarted",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "agent_id"],
      "properties": {
        "type": { "const": "task_started" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "agent_id": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "TaskCompleted",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "duration_ms"],
      "properties": {
        "type": { "const": "task_completed" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "duration_ms": { "type": "integer", "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "TaskFailed",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "error", "failure_count"],
      "properties": {
        "type": { "const": "task_failed" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "error": { "type": "string" },
        "failure_count": { "type": "integer", "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "TaskSkipped",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "reason"],
      "properties": {
        "type": { "const": "task_skipped" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "TaskRolledBack",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "snapshot_id"],
      "properties": {
        "type": { "const": "task_rolled_back" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "snapshot_id": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "TaskEscalated",
      "additionalProperties": false,
      "required": ["type", "plan_id", "task_id", "failure_count", "max_failures"],
      "properties": {
        "type": { "const": "task_escalated" },
        "plan_id": { "type": "string" },
        "task_id": { "type": "string" },
        "failure_count": { "type": "integer", "minimum": 0 },
        "max_failures": { "type": "integer", "minimum": 1 }
      }
    },
    {
      "type": "object",
      "title": "ModeChanged",
      "additionalProperties": false,
      "required": ["type", "from", "to", "reason"],
      "properties": {
        "type": { "const": "mode_changed" },
        "from": { "type": "string" },
        "to": { "type": "string" },
        "reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "VerifyStarted",
      "additionalProperties": false,
      "required": ["type", "hook_id", "category", "firing_point"],
      "properties": {
        "type": { "const": "verify_started" },
        "hook_id": { "type": "string" },
        "category": { "$ref": "#/$defs/HookCategoryRef" },
        "firing_point": { "type": "string" },
        "level": { "type": ["string", "null"] }
      }
    },
    {
      "type": "object",
      "title": "VerifyPassed",
      "additionalProperties": false,
      "required": ["type", "hook_id", "duration_ms"],
      "properties": {
        "type": { "const": "verify_passed" },
        "hook_id": { "type": "string" },
        "duration_ms": { "type": "integer", "minimum": 0 },
        "output_preview": { "type": ["string", "null"] }
      }
    },
    {
      "type": "object",
      "title": "VerifyFailed",
      "additionalProperties": false,
      "required": ["type", "hook_id", "duration_ms", "error", "on_failure"],
      "properties": {
        "type": { "const": "verify_failed" },
        "hook_id": { "type": "string" },
        "duration_ms": { "type": "integer", "minimum": 0 },
        "error": { "type": "string" },
        "on_failure": { "$ref": "#/$defs/OnFailureRef" }
      }
    },
    {
      "type": "object",
      "title": "RailTriggered",
      "additionalProperties": false,
      "required": ["type", "rail_id", "policy", "firing_point", "message"],
      "properties": {
        "type": { "const": "rail_triggered" },
        "rail_id": { "type": "string" },
        "policy": { "$ref": "#/$defs/RailPolicy" },
        "firing_point": { "type": "string" },
        "message": { "type": "string" },
        "agent_id": { "type": ["string", "null"] }
      }
    },
    {
      "type": "object",
      "title": "SkillMissing",
      "description": "Spec §4b Layer 1 (loader) or Layer 2 (request_capability) gap event for an unresolved skill reference. M05.A added `suggested_action` + `requested_via`; severity tightened from free-string to GapSeverity enum. Defaults to severity=advisory (skill gaps are recoverable per spec §4b severity matrix — load continues without).",
      "additionalProperties": false,
      "required": ["type", "agent_id", "skill_name", "severity", "suggested_action", "requested_via"],
      "properties": {
        "type": { "const": "skill_missing" },
        "agent_id": { "type": "string" },
        "skill_name": { "type": "string" },
        "severity": { "$ref": "#/$defs/GapSeverity" },
        "suggested_action": { "$ref": "#/$defs/SuggestedAction" },
        "requested_via": { "$ref": "#/$defs/GapSource" }
      }
    },
    {
      "type": "object",
      "title": "ToolMissing",
      "description": "Spec §4b Layer 1 (loader) or Layer 2 (request_capability) gap event for an unresolved tool reference. M05.A added `suggested_action` + `requested_via`; severity tightened from free-string to GapSeverity enum. Tool gaps are blocking per spec §4b severity matrix — the owning agent cannot proceed.",
      "additionalProperties": false,
      "required": ["type", "agent_id", "tool_name", "severity", "suggested_action", "requested_via"],
      "properties": {
        "type": { "const": "tool_missing" },
        "agent_id": { "type": "string" },
        "tool_name": { "type": "string" },
        "severity": { "$ref": "#/$defs/GapSeverity" },
        "suggested_action": { "$ref": "#/$defs/SuggestedAction" },
        "requested_via": { "$ref": "#/$defs/GapSource" }
      }
    },
    {
      "type": "object",
      "title": "McpMissing",
      "description": "Spec §4b + §5: gap event for an unresolved MCP server reference. v0.1 emits this only from Layer 2 request_capability (the v0.1 framework JSON has no MCP-server declaration field — server lifecycle is M06). M05.A authoring decision: variant + payload land now so the schema is forward-compatible with M06's loader-time emission.",
      "additionalProperties": false,
      "required": ["type", "agent_id", "server_name", "severity", "suggested_action", "requested_via"],
      "properties": {
        "type": { "const": "mcp_missing" },
        "agent_id": { "type": "string" },
        "server_name": { "type": "string" },
        "severity": { "$ref": "#/$defs/GapSeverity" },
        "suggested_action": { "$ref": "#/$defs/SuggestedAction" },
        "requested_via": { "$ref": "#/$defs/GapSource" }
      }
    },
    {
      "type": "object",
      "title": "McpInstalled",
      "description": "Spec §5 + §13.5: M06.C McpClient::add_server succeeded. Records server name + transport discriminant + presence-of-auth so the renderer + audit consumer can show installation history without exposing secrets. Emitted alongside the audit `mcp_installed` line.",
      "additionalProperties": false,
      "required": ["type", "name", "transport_kind", "has_auth"],
      "properties": {
        "type": { "const": "mcp_installed" },
        "name": { "$ref": "#/$defs/McpServerNameRef" },
        "transport_kind": { "$ref": "#/$defs/McpTransportKind" },
        "has_auth": {
          "type": "boolean",
          "description": "True iff a per-server auth secret was stored alongside the install. Indicates the renderer should show the credential indicator on the MCPNode (Stage E)."
        }
      }
    },
    {
      "type": "object",
      "title": "McpUninstalled",
      "description": "Spec §5 + §13.5: M06.C McpClient::remove_server succeeded. Records server name only — transport / auth-state are stripped on removal so the audit trail reflects the post-state.",
      "additionalProperties": false,
      "required": ["type", "name"],
      "properties": {
        "type": { "const": "mcp_uninstalled" },
        "name": { "$ref": "#/$defs/McpServerNameRef" }
      }
    },
    {
      "type": "object",
      "title": "McpAuthGranted",
      "description": "Spec §5 + §13.5 + §8.security L5: M06.C secret stored for an MCP server's auth_secret_ref. The secret value is NEVER logged — only the server name + the keychain-key reference. Emitted alongside the audit `mcp_auth_granted` line.",
      "additionalProperties": false,
      "required": ["type", "name"],
      "properties": {
        "type": { "const": "mcp_auth_granted" },
        "name": { "$ref": "#/$defs/McpServerNameRef" }
      }
    },
    {
      "type": "object",
      "title": "AgentMissing",
      "description": "Spec §4b Layer 1 (loader) — `framework.agents[].spawns[]` references an agent id absent from `framework.agents[]`. Per spec §4b severity matrix this is a load-time block: the framework fails to load (severity=critical). Also emittable from Layer 2 request_capability when an agent asks for a sub-agent it cannot spawn.",
      "additionalProperties": false,
      "required": ["type", "agent_id", "missing_agent_id", "severity", "suggested_action", "requested_via"],
      "properties": {
        "type": { "const": "agent_missing" },
        "agent_id": { "type": "string" },
        "missing_agent_id": { "type": "string" },
        "severity": { "$ref": "#/$defs/GapSeverity" },
        "suggested_action": { "$ref": "#/$defs/SuggestedAction" },
        "requested_via": { "$ref": "#/$defs/GapSource" }
      }
    },
    {
      "type": "object",
      "title": "GapResolved",
      "additionalProperties": false,
      "required": ["type", "agent_id", "capability", "kind"],
      "properties": {
        "type": { "const": "gap_resolved" },
        "agent_id": { "type": "string" },
        "capability": { "type": "string" },
        "kind": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "HitlRequested",
      "description": "M04.E §6a — emitted when an enabled HITL trigger evaluates. Carries the correlation id (prompt_id) the renderer round-trips back via respond_hitl, the UI variant the renderer should render, the user-facing question, and the available choices. The original M03 minimal shape (prompt + hitl_kind) is replaced; no live producers existed pre-M04.E (audit-verified).",
      "additionalProperties": false,
      "required": ["type", "prompt_id", "trigger", "question", "options", "ui_variant", "timeout_at_unix_ms"],
      "properties": {
        "type": { "const": "hitl_requested" },
        "prompt_id": {
          "type": "string",
          "description": "Correlation id (UUID) tying this request to the eventual hitl_resolved / hitl_timeout. The renderer passes this back via respond_hitl(prompt_id, choice)."
        },
        "trigger": { "$ref": "#/$defs/HitlTriggerRef" },
        "agent_id": {
          "type": ["string", "null"],
          "description": "Originating agent, when applicable (per_task / per_epic are plan-level, not agent-level)."
        },
        "question": {
          "type": "string",
          "description": "User-facing question. Free-text per spec §6a; framework JSON authors compose."
        },
        "options": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Expected choice tokens. Empty array means free-text response. Built-in options for on_failure_threshold: [retry, skip, abort]."
        },
        "ui_variant": { "$ref": "#/$defs/HitlUiVariantRef" },
        "timeout_at_unix_ms": {
          "type": "integer",
          "minimum": 0,
          "description": "Wall-clock deadline (unix milliseconds). Renderer may display countdown; seam fires hitl_timeout when crossed."
        }
      }
    },
    {
      "type": "object",
      "title": "HitlResolved",
      "description": "M04.E §6a — emitted when the user responds to a HITL prompt. Carries the same prompt_id as the originating hitl_requested + the chosen token (from options[]) + how long the user took.",
      "additionalProperties": false,
      "required": ["type", "prompt_id", "choice", "duration_ms"],
      "properties": {
        "type": { "const": "hitl_resolved" },
        "prompt_id": { "type": "string" },
        "choice": {
          "type": "string",
          "description": "Selected token; one of the originating options[], or a free-text response when options[] was empty."
        },
        "duration_ms": { "type": "integer", "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "HitlTimeout",
      "description": "M04.E §6a — emitted when a HITL prompt's timeout_at_unix_ms elapses without a hitl_resolved. The default_action_on_timeout from the framework JSON (e.g. `abort`) drives downstream FSM transitions.",
      "additionalProperties": false,
      "required": ["type", "prompt_id", "trigger", "default_action"],
      "properties": {
        "type": { "const": "hitl_timeout" },
        "prompt_id": { "type": "string" },
        "trigger": { "$ref": "#/$defs/HitlTriggerRef" },
        "default_action": {
          "type": "string",
          "description": "Free-text action token from framework JSON (e.g. `abort`). SDK consumers route this to plan / task FSM transitions."
        }
      }
    },
    {
      "type": "object",
      "title": "NotifierDispatched",
      "description": "M04.E §6a — emitted when a notifier (terminal_bell / desktop / sound / plugin) successfully fires for a HITL request. Notifier failures emit notifier_failed instead.",
      "additionalProperties": false,
      "required": ["type", "notifier_type", "trigger", "success"],
      "properties": {
        "type": { "const": "notifier_dispatched" },
        "notifier_type": {
          "type": "string",
          "description": "Type of notifier that fired (terminal_bell / desktop / sound / plugin)."
        },
        "trigger": { "$ref": "#/$defs/HitlTriggerRef" },
        "success": { "type": "boolean" }
      }
    },
    {
      "type": "object",
      "title": "NotifierFailed",
      "description": "M04.E §6a — emitted when a notifier raises an error (e.g. desktop notification permission denied). Notifier failures are NON-FATAL: the HITL seam still resolves on user response or timeout regardless of which notifiers fired.",
      "additionalProperties": false,
      "required": ["type", "notifier_type", "trigger", "error"],
      "properties": {
        "type": { "const": "notifier_failed" },
        "notifier_type": { "type": "string" },
        "trigger": { "$ref": "#/$defs/HitlTriggerRef" },
        "error": {
          "type": "string",
          "description": "Human-readable error text. Renderer surfaces in inspector or toast."
        }
      }
    },
    {
      "type": "object",
      "title": "CapabilityViolation",
      "additionalProperties": false,
      "required": ["type", "agent_id", "capability_kind", "requested_action", "declared_scope"],
      "properties": {
        "type": { "const": "capability_violation" },
        "agent_id": {
          "type": "string",
          "description": "Agent whose dispatch the §8.security L2a enforcer rejected."
        },
        "capability_kind": {
          "$ref": "#/$defs/CapabilityKindRef",
          "description": "Coarse category of access the attempted dispatch needed."
        },
        "requested_action": { "$ref": "#/$defs/RequestedAction" },
        "declared_scope": { "$ref": "#/$defs/DeclaredScope" }
      }
    },
    {
      "type": "object",
      "title": "CapabilityGrant",
      "additionalProperties": false,
      "required": ["type", "granted_to", "capability_kind", "resource"],
      "properties": {
        "type": { "const": "capability_grant" },
        "parent_agent_id": {
          "type": "string",
          "description": "Optional — agent whose grants this grant narrows from. Absent for framework-loader-issued root grants; present whenever an agent spawns a sub-agent and narrows a subset of its grants to the child (§8.security L2a narrowing)."
        },
        "granted_to": {
          "type": "string",
          "description": "Agent receiving the grant."
        },
        "capability_kind": {
          "$ref": "#/$defs/CapabilityKindRef"
        },
        "resource": { "$ref": "#/$defs/GrantedResource" },
        "narrowed_from": {
          "type": "string",
          "description": "Optional — plain-English description of the parent's broader scope. Renderer uses this in the inspector to show 'narrowed from ... to ...' attribution; absent for root grants."
        }
      }
    },
    {
      "type": "object",
      "title": "BudgetWarn",
      "additionalProperties": false,
      "required": ["type", "spent_usd", "cap_usd", "percent"],
      "properties": {
        "type": { "const": "budget_warn" },
        "spent_usd": { "type": "number" },
        "cap_usd": { "type": "number" },
        "percent": { "type": "integer", "minimum": 0 }
      }
    },
    {
      "type": "object",
      "title": "BudgetDownshift",
      "additionalProperties": false,
      "required": ["type", "from_model", "to_model", "reason"],
      "properties": {
        "type": { "const": "budget_downshift" },
        "from_model": { "type": "string" },
        "to_model": { "type": "string" },
        "reason": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "BudgetSuspended",
      "additionalProperties": false,
      "required": ["type", "spent_usd", "cap_usd"],
      "properties": {
        "type": { "const": "budget_suspended" },
        "spent_usd": { "type": "number" },
        "cap_usd": { "type": "number" }
      }
    },
    {
      "type": "object",
      "title": "BudgetExceeded",
      "additionalProperties": false,
      "required": ["type", "spent_usd", "cap_usd"],
      "properties": {
        "type": { "const": "budget_exceeded" },
        "spent_usd": { "type": "number" },
        "cap_usd": { "type": "number" }
      }
    },
    {
      "type": "object",
      "title": "StreamText",
      "additionalProperties": false,
      "required": ["type", "agent_id", "text"],
      "properties": {
        "type": { "const": "stream_text" },
        "agent_id": { "type": "string" },
        "text": { "type": "string" }
      }
    },
    {
      "type": "object",
      "title": "DecisionRecord",
      "additionalProperties": false,
      "required": ["type", "agent_id", "decision", "rationale"],
      "properties": {
        "type": { "const": "decision_record" },
        "agent_id": { "type": "string" },
        "decision": { "type": "string" },
        "rationale": { "type": "string" },
        "tool_used": { "type": ["string", "null"] }
      }
    },
    {
      "type": "object",
      "title": "TierViolation",
      "description": "M05 Stage D §8.security L4 — emitted when the L4 tier gate rejects a capability request before the L1 enforcer runs. Distinct from `capability_violation` (which fires on L1 rejection). Renderer routes to a tier-violation modal in the Settings panel: \"Your current tier (Novice) does not permit this action; promote to Promoted or modify the request.\" Carries the tier that rejected + the requested kind + a plain-English action.",
      "additionalProperties": false,
      "required": ["type", "agent_id", "tier", "capability_kind", "attempted_action"],
      "properties": {
        "type": { "const": "tier_violation" },
        "agent_id": {
          "type": "string",
          "description": "Agent whose dispatch the §8.security L4 evaluator rejected."
        },
        "tier": { "$ref": "#/$defs/TierRef" },
        "capability_kind": {
          "$ref": "#/$defs/CapabilityKindRef",
          "description": "The coarse kind the tier's allowlist excludes."
        },
        "attempted_action": { "$ref": "#/$defs/TierForbiddenAction" }
      }
    },
    {
      "type": "object",
      "title": "TierTransition",
      "description": "M05 Stage D §8.security L4 — emitted on a successful tier change. `previous` is the tier before the change; `current` is the tier after. Includes a plain-English `reason`. Promotion is renderer-confirmed (Settings panel modal calls `request_tier_transition`); demotion is direct. Renderer applies the new tier to its current-tier state and surfaces a toast.",
      "additionalProperties": false,
      "required": ["type", "previous", "current", "reason"],
      "properties": {
        "type": { "const": "tier_transition" },
        "previous": { "$ref": "#/$defs/TierRef" },
        "current": { "$ref": "#/$defs/TierRef" },
        "reason": { "$ref": "#/$defs/TierTransitionReason" }
      }
    },
    {
      "type": "object",
      "title": "TokenUsage",
      "additionalProperties": false,
      "required": ["type", "input", "output", "model", "cost_usd"],
      "properties": {
        "type": { "const": "token_usage" },
        "input": { "type": "integer", "minimum": 0 },
        "output": { "type": "integer", "minimum": 0 },
        "model": { "type": "string" },
        "cost_usd": { "type": "number" }
      }
    }
  ]
}
