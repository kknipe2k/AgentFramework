# M08.F1 — Stage Retrospective

> **Stage:** M08 Stage `F1` — Tester backend (isolated session + the M07.V Dec-6 discharge) — see `docs/build-prompts/M08-workbench.md`
> **Parent milestone:** M08 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-21
> **Session end:** 2026-05-21
> **Branch:** `claude/m08-workbench` (parent-milestone feature branch)
> **Starting commit:** `89c8d1b` (M08.E retrospective)
> **Ending commit:** `b0a74e3` (follow-up coverage commit; retro commit pending approval)
> **Estimated effort (per prompt):** 9–12 hours
> **Actual elapsed:** ~10 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded and the §4 Hard Rules were observed (do-not-commit-without-approval, don't-push, no skipped gates, CODEOWNERS plan-first, no `unsafe` outside the sandbox)
- [x] All "Read first" files were read in order (CLAUDE.md, STAGE-PROMPT-PROTOCOL, M08 phase doc Stage A/F1, M07.V + M08.E retros, spec Phase 9, ADRs 0011/0014/0007, gotchas, coverage-policy, retro template)
- [x] The deliverable + test plan were stated and surfaced for red-phase approval before any green implementation
- [x] User confirmed the red phase ("approved proceed") before green began
- [x] The branch was correct (`claude/m08-workbench`, M08.E impl present)
- [x] No uncommitted changes carried over (`git status` clean apart from pre-existing `.claude/` untracked junk)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Pre-red analysis | The phase doc's `<construction_reachability_check>` places `connect_test_session_mcp` in `crates/runtime-main/src/builder/tester.rs`, but `runtime-mcp` depends on `runtime-main` — `runtime-main` cannot reference `runtime_mcp::McpDispatcher` without a Cargo cycle. The §5a connect handler had to move to the shell (`commands.rs`). | The phase doc should have checked the crate dependency direction before assigning the wire's constructor file; F1.3.6 + the wire-#3 `constructor` should name `src-tauri/src/commands.rs`. | 3 |
| Green compile | The pre-red deep-dive analyzer reported `AgentEvent` field types from `generated/event.rs`; the actual `runtime_core::event::AgentEvent` is a **hand-rolled** enum in `event.rs`. `CapabilityKindRef` (hand-rolled) has no `Display` → `capability_kind.to_string()` failed to compile. | N/A — analyzer error, not a prompt gap. Fixed with an explicit `capability_kind_label` match. | 2 |
| Green gate (clippy) | `connect_test_session_mcp`'s original `emit: &mut dyn FnMut(AgentEvent)` made the async future `!Send` (`clippy::future_not_send`). | N/A — caught at the clippy gate; redesigned the handler to **return** `Vec<AgentEvent>` (cleaner, and removed a `collection_is_never_read` test smell). | 2 |
| Green gate (deny) | The new `runtime-mcp` dev-dependency on `src-tauri` was authored without a `version` field → `cargo deny` `error[wildcard]`. | N/A — one-line fix (`version = "0.1.0"`); mirrors the existing `src-tauri` `runtime-mcp` normal-dep entry. | 2 |
| Green coverage | `builder/tester.rs` measured 87.22% line after the impl commit — the artifact-integrity pre-flight path was reachable but unexercised by the red-phase contract tests (tool-free frameworks, no imported artifacts). | N/A — anticipated; closed with a protocol-sanctioned net-new additive follow-up commit (`b0a74e3`), lifting `tester.rs` to 93.52% / package 96.57%. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M08 phase doc F1 `<construction_reachability_check>` wire #3 + F1.3.6 vs the Cargo dependency graph | The phase doc places `connect_test_session_mcp` in `tester.rs`; the crate graph forbids it (cycle). | Placed the §5a connect handler in `src-tauri/src/commands.rs` (the shell sees both crates and is the MCP composition root). Discharge of 🟡 #3 is still a genuine production caller. ADR-0019 records the placement + rationale. | M08 phase doc F1 — wire #3 `constructor` + F1.3.6 should name `commands.rs`. |
| M08 phase doc F1 `<architecture_check>` ("expect both `run_test_session_with` and `run_test_session`") vs the `run_smoke_session` precedent | Whether a non-command `run_test_session` wrapper belongs in `tester.rs`. | Followed the established precedent: the `#[tauri::command] test_framework` **is** the production wrapper (as `run_smoke_session` is — there is no non-command `run_smoke_session`). `tester.rs` ships only the `run_test_session_with` seam. A `runtime-main` `run_test_session` could not spawn the drone (`DroneLifecycle` is shell-side) — it would be vestigial. | M08 phase doc F1 `<architecture_check>` / F1.1 deliverable 1. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After the red-phase commit (`fbca187`) | The 26-test red plan + the two structural deviations (connect-handler placement; no `run_test_session`), with the Cargo-cycle rationale and a Stage-V note. | Yes — the user could accept, or amend the phase doc. | Yes — before any green implementation, so a different decision would have cost nothing. |
| Stage end (this surface) | The full green bundle: gate results, the inverted reachability map, ADR-0019, the assembled-regression result. | Yes | Yes |

---

## [LIVE] Protocol drift events

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| `runtime_core::event::AgentEvent` is a hand-rolled enum (`event.rs`), not the typify-generated `generated/event.rs` one. The generated parallel artifact exists but is not what `use runtime_core::event::AgentEvent` resolves to. | A read-only deep-dive reported field types from the generated file; `skills_lock_integration.rs` constructing `ArtifactHashMismatch` with plain `String`s was the contradiction that exposed it. | When a deep-dive reports a type, verify the `use` path resolves to the file it claims — a hand-rolled curated-union next to a generated parallel artifact is a real pattern in this codebase (the `BudgetWarn` / `McpRequestBlocked` precedent). |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation? | 5 | §5 seam archetype, §6 gate ordering, §10 Hard Rule 8, §11 ADR triggers all applied directly. |
| 2 | Did the "Read first" list orient correctly? | 4 | Good — but the phase doc's own `<construction_reachability_check>` carried a placement that the crate graph forbade (see Ambiguity). |
| 3 | Did Claude state deliverable + test plan before code? | 5 | Surfaced for red-phase approval before any green code. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Green gates: 1 compile fix, ~3 mechanical clippy fixes, 1 deny fix, 1 rustdoc fix — all single-iteration, none a rabbit hole. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | Red committed locally then surfaced for approval; green committed after "approved proceed"; nothing pushed. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Escalated the two structural deviations at red-approval (forced, but the user should know); proceeded on technical/API-shape decisions per §12. |
| 7 | Was the human-direction load reasonable? | 5 | One red-phase approval, one final approval — the §12 model. |
| 8 | Were the surfaces actionable? | 5 | The red surface gave the Cargo-cycle rationale + a Stage-V note so the deviation is reviewable, not a surprise. |
| **Process axis total** | | **39/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case, one-thing functions, `*_with` seam archetype, comments explain *why*. |
| 2 | Are tests behavior tests, not tautology tests? | 5 | The assembled regression drives a real drone + concrete `McpDispatcher` (gotcha #66); the tampered-artifact test exercises the discharge end-to-end. |
| 3 | Are public APIs documented? | 5 | Every `pub` type/fn has a doc comment; `# Errors` on the `Result` returns; rustdoc gate green. |
| 4 | Are deliverables exactly what was promised? | 4 | All F1.4 buckets delivered; `run_test_session` intentionally not shipped (precedent — see Ambiguity), connect handler relocated (Cargo cycle). Both surfaced + ADR-recorded. |
| 5 | Would a stranger understand the code? | 5 | The module docs state the isolation model, the seam-vs-wrapper split, and the 🟡 #2 discharge inline. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No `any`-equiv, no ad-hoc `#[allow]`, no magic numbers, no silent error drops. |
| 7 | Is coverage measured, ≥ gates? | 4 | `runtime-main` package 96.57% line (≥95); workspace 91.90% (≥80). `builder/tester.rs` itself is 93.52% — below the aspirational per-module 95; the residual is test-scaffold stub methods (`TurnStub::count_tokens`/`list_models`) the run loop never calls + the rare `SdkError`-infra error map. No coverage exclusion added. |
| 8 | Did CI green hold cross-platform? | 4 | All local gates green; the assembled regression uses the cross-platform `smoke_signal_persistence.rs` harness archetype. CI cross-OS prediction: green (no `cfg`-gated platform branches added — gotcha #74 N/A). |
| **Product axis total** | | **37/40** | |

### Axis 3: Pattern — does this generalize?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all template sections useful? | 5 | `<construction_reachability_check>` is the load-bearing A→F1 mechanism; `<architecture_check>` caught the no-new-engine claim. |
| 2 | Were any sections needed but missing? | 4 | A "crate-dependency-direction check" slot would have caught the connect-handler placement at phase-doc-authoring time. |
| 3 | Do milestone gotchas generalize? | 5 | The hand-rolled-vs-generated `AgentEvent` surprise is worth a gotcha (see Decisions). |
| 4 | Did the time-box match reality? | 5 | ~10h vs the 9–12h estimate. |
| 5 | Implicit protocol moments? | 4 | The "net-new additive follow-up test for a reachable-but-unexercised production path" is a recurring pattern (coverage of a discharge wire) worth naming. |
| 6 | Re-runnable by a fresh session? | 4 | Yes, given the two phase-doc corrections (placement + no-`run_test_session`) are folded in. |
| 7 | Anything to change before re-use? | 5 | Fix the F1 `<construction_reachability_check>` wire #3 to name `commands.rs`. |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `fbca187` committed locally then surfaced for red approval; green `a3e79d0` + follow-up `b0a74e3` after "approved proceed"; nothing pushed.
- [x] **G2: no Severity-5 friction** — max severity 3 (the Cargo-cycle placement, caught pre-red — no rework).
- [x] **G3: no protocol drift unaddressed** — drift table empty.
- [x] **G4: the stage completed** — `b0a74e3` on `claude/m08-workbench`; all F1.4 acceptance criteria met (26 tests green; the three M07.V Dec-6 wires discharged; ADR-0019 filed; the two-commit strict-TDD invariant held).
- [x] **G5: every axis row ≥3** — lowest row is 4.
- [x] **G6: CI-parity** — local gates run with the exact `CLAUDE.md` §6 commands: `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo test --workspace --doc`, `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps`, `cargo audit`, `cargo deny check`, `cargo llvm-cov --workspace --ignore-filename-regex "src.main\.rs|generated" --fail-under-lines 80`, `cargo llvm-cov --package runtime-main --ignore-filename-regex "src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs" --fail-under-lines 95`. No divergent flags; `cargo llvm-cov clean --workspace` run before the coverage gates (gotcha #81).

### Soft gates

- [x] **S1:** Process 39/40 ≥ 30
- [x] **S2:** Product 37/40 ≥ 32
- [x] **S3:** Pattern 32/35 ≥ 25
- [x] **S4:** Time-box — ~10h vs 9–12h estimate, ratio ~1.0
- [x] **S5:** ≤3 Severity-3 friction — actual: 1

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage F2 with the M08 phase-doc corrections in the Decisions section applied.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.90%
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs`, `generated`

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 96.57%
- **Exclusions added this stage:** None added this stage — `builder/tester.rs` is pure / seam / `tempfile`-tested; the OS-touching wrapper (`test_framework`, drone spawn, teardown) lives in `src-tauri/src/commands.rs`, not in `tester.rs`. No four-mirror sync needed.
- **Exclusion list as of stage end:** `src.main\.rs`, `generated`, `src.providers.anthropic\.rs`, `src.drone_ipc.connection\.rs`, `src.sandbox_ipc.connection\.rs`, `src.import.fetch\.rs`

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/builder/tester.rs` | new (M08.F1) | 93.52% | new | New module. Residual missed lines are test-scaffold stub methods the run loop never invokes + the rare `SdkError`-infra error map; the package gate (96.57% ≥ 95) holds. |
| `crates/runtime-main/src/hitl/seam.rs` | 99.57% (M04) | 99.57% | → | — |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns match `CLAUDE.md` §6 / `codecov.yml`. No coverage-policy change this stage (no new exclusion, no threshold change) — only the §B baseline for the `builder` module's new `tester.rs` is appended at the M08 closeout per the phase doc's `<coverage_policy_reconciliation>`.

---

## [END] Decisions for the next stage

### `CLAUDE.md` updates

- [ ] Add a gotcha: `runtime_core::event::AgentEvent` is a **hand-rolled** curated union (`crates/runtime-core/src/event.rs`), NOT the typify-generated `generated/event.rs` enum — `use runtime_core::event::AgentEvent` resolves to the hand-rolled one. Its `CapabilityKindRef` has no `Display`. Verify the `use` path before relying on a generated type's derives.

### Milestone document updates (`M08-workbench.md`)

- [ ] Stage F1 `<construction_reachability_check>` wire #3 + F1.3.6: name `src-tauri/src/commands.rs` as `connect_test_session_mcp`'s file (the Cargo dependency graph forbids `tester.rs` — `runtime-mcp` depends on `runtime-main`). ADR-0019 records this.
- [ ] Stage F1 `<architecture_check>` / F1.1 deliverable 1: drop the "expect both `run_test_session_with` and `run_test_session`" claim — the `#[tauri::command] test_framework` is the production wrapper, per the `run_smoke_session` precedent.
- [ ] Stage F2 `<wire_signature_audit>`: the shipped `test_framework` signature is `(app: AppHandle, framework_doc: Framework, task: String) -> Result<TestOutcome, CmdError>`; the `TestOutcome` serde shape is `{ passed, capability_failures: [{agent_id, needed, reason}], token_spend: {input, output, total}, timing, vdr, trace: [AgentEvent] }`.

### Protocol additions

- [ ] Consider a phase-doc-authoring check: a wire's `constructor` file must respect the crate dependency direction (a `runtime-main` file cannot reference a `runtime-mcp` type).

### Open issues to file

- [ ] None — `builder/tester.rs` at 93.52% is acceptable (package 96.57%); the residual is test scaffolding, not production code.

---

## [END] Inverted construction-reachability map

Stage A authored all three M07.V Dec-6 wires `inputs_reachable="false"`. F1 inverts each to `inputs_reachable="true"` with a concrete production call site:

| Wire | M07.V | Production caller (file:line) | Reachable via |
|---|---|---|---|
| agent-with-tools production driver (`ProviderEvent::ToolUse` → concrete `McpDispatcher`) | 🟡 #5 | `crates/runtime-main/src/builder/tester.rs:328` (`AgentSdk::with_capability_wiring`) → `:333` (`sdk.run_agent`) | `run_test_session_with`, exercised by `tester_isolated_session.rs` with a tool-bearing framework |
| `skills_lock::verify` on the artifact-load path | 🟡 #2 | `crates/runtime-main/src/builder/tester.rs:181` (`crate::skills_lock::verify` in `load_verified_artifact`) | `run_test_session_with` → `verify_framework_artifacts` → `load_verified_artifact`, exercised by `run_test_session_with_a_tampered_imported_artifact_refuses_the_run` |
| `McpDispatcher::on_server_connected` connect-handler | 🟡 #3 | `src-tauri/src/commands.rs:1525` (`dispatcher.on_server_connected` in `connect_test_session_mcp`) | `test_framework` → `connect_test_session_mcp` — **shell, not `tester.rs`** (Cargo cycle; ADR-0019) |

**Smoke-session-infra reuse (no new session engine):** `run_test_session_with` reuses `AgentSdk::with_capability_wiring` → optional `with_mcp_dispatch` → `run_agent` — the exact M07.D2 construction. The only novel surfaces are the throwaway-DB resolution (`throwaway_test_db_path`), the test-defaults `HitlSeam`, the integrity pre-flight, and the three Dec-6 production-caller wires.

**Isolation (ADR-0019):** throwaway `std::env::temp_dir()` DB path (never `app_local_data_dir`); `HitlSeam::test_defaults()` auto-resolves every prompt so the run never blocks; teardown reaps the drone then deletes the throwaway DB. The assembled regression confirms: signals persist under the test session id, `token_usage > 0` (folded into `TestOutcome.token_spend` AND a `token_usage` DB row), and a pre-existing user DB is byte-untouched.

**ADR-0019:** filed, status `Proposed` (→ `Accepted` in the M08 PR before merge).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M08 Stage `F1`, authored per `CLAUDE.md` §19. User review and approval pending.

**Claude:** I have completed M08 Stage F1 per the stage prompt. The Tester backend ships as the isolated-session model (ADR-0019), the three M07.V Dec-6 wires are discharged with concrete production call sites, and all gates are green (26 tests; `runtime-main` 96.57% line; workspace 91.90%; clippy/fmt/doc/audit/deny clean). The strict v1.8 two-commit invariant held — `git diff fbca187..a3e79d0 -- '**/tests/**'` is empty and the in-source `#[cfg(test)]` blocks are byte-identical red→impl; the `b0a74e3` follow-up is a single net-new additive coverage test. I have NOT pushed; I am surfacing this retrospective + the diff stat + the commit set for user approval.

**Surfaced at:** 2026-05-21
