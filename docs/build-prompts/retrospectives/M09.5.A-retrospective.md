# M09.5.A — Stage Retrospective

> **Stage:** M09.5 Stage `A` — shell perimeter (CSP, store-exposure seam, path confinement, mutants-nightly) — see `docs/build-prompts/M09.5-perimeter-hardening.md`
> **Parent milestone:** M09.5 (perimeter-hardening fix cluster, between M09 and M10) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-10 (build-machine session, restarted mid-stage)
> **Session end:** 2026-06-10
> **Branch:** `claude/m09.5-perimeter-hardening`
> **Starting commit:** `d412a3a` (phase doc only; this session restarted after a prior session dropped — state inventory found a clean tree, no local commits)
> **Ending commit:** `d334ee1` (uncommitted-to-origin — awaiting approval; push happens once at G)
> **Estimated effort (per prompt):** ~1–2 hrs (TD-050) + ~2–3 hrs (TD-051) + ~0.5 hr (TD-056)
> **Actual elapsed:** one session

---

## Pre-flight

- [x] CLAUDE.md loaded; §4 Hard Rules restated implicitly via the state-inventory-first discipline
- [x] Read-first files read: phase doc Stage A (A.1–A.8), gotchas #56/#74/#81/#92, tauri.conf.json, App.tsx, main.rs, commands.rs, wdio.conf.ts, fuzz-nightly.yml, the review §2 C1/C2/C7, tech-debt TD-050/051/056, error.v1.json, ipc.ts
- [x] State inventory run FIRST per the restart prompt: branch correct, no local commits, clean tree, no retro started → resume = "start A.7 from the top (ground_at_red)"
- [x] Deliverable + test plan stated before code
- [x] Branch correct
- [x] `git status` clean at entry (the dropped prior session left nothing local)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| red e2e run | The harness onPrepare runs `tsc --noEmit`, which the red vitest file (missing `testMode` module) broke — blocking the build of the red e2e. Moved the vitest red file aside to run the e2e red, restored after. | The phase doc could note that the tauri-build step gates on `tsc`, so a red TS unit test blocks the e2e harness build; sequence the red runs accordingly. | 2 |
| impl (init script) | `tauri::plugin::Builder::new` needs its runtime type param annotated (`E0283`); fixed with `::<tauri::Wry>`. | A.2 wire-audit could pin that the plugin Builder is generic over the runtime. | 1 |
| impl (`import.meta.env`) | First use of `import.meta.env` in the codebase — untyped (`TS2339`); added `src/vite-env.d.ts` with the vite/client reference. | — (reasonable to discover) | 1 |
| green e2e | The 3 path-refusal cases failed with `WebDriverError: [object Object]` — returning the raw Tauri error object trips WebView2's execute/sync structured-clone serializer. The impl was correct; the TEST masked it. Flattened to `errorType`/`errorMessage` primitives. | A.4/A.7 could warn that `browser.execute` cannot return a Tauri error object — flatten to primitives. This also retroactively means the load/import RED cases failed for the wrong reason (serialization, not the assertion). | 3 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Milestone Locks ("no schema change") vs A.3 ("typed `path_not_permitted` `CmdError`") | A.3 requires a new `CmdError` variant, which is a `schemas/error.v1.json` change — seemingly forbidden by the milestone-level "no schema change" lock | The lock is qualified "(nothing here touches **artifact** shapes)"; `error.v1.json` is the IPC error **envelope**, not an artifact schema. Added the additive `PathNotPermitted` variant (minor, in-place v1 per §14), regenerated types. Flagged for maintainer confirmation re: whether §11 wants an ADR for an additive error-envelope variant. | Phase doc could state explicitly that the error envelope is exempt from the artifact-schema lock + whether the additive variant needs an ADR. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Session start | State-inventory report + chosen resume point (start A.7 from the top) per the restart prompt | Yes | Yes — first output, before any work |

Note: this session proceeded through red → impl → green autonomously (maintainer not watching live per operating mode); the binding checkpoint is this final approval surface. The strict-TDD red→impl two-commit history is preserved as the audit evidence.

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §5 strict-TDD (impl must not touch test paths) | The impl-affecting confinement breaks pre-existing dependent specs (Inspector/ImportPanel/ARIA/builder_inspector), which needed adaptation. Risk: bundling those into the impl commit would break the red→impl test-path-diff-EMPTY invariant. | Yes | Split: impl commit = production only (red→impl test diff verified EMPTY); a separate labelled `test(M09.5.A): adapt …` commit holds all dependent-spec adaptations + net-new helper coverage. Gates verified at final tree state. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Windows lexical `..` collapse | `root/ghost/..` is canonicalized by Win32 (it resolves into the root even though `ghost` doesn't exist), so the helper's `ParentDir`-in-remainder rejection arm is effectively unreachable on Windows. Dropped a test whose expectation was wrong for Windows semantics. | The residual-`..` rejection is defensive code for platforms/paths where `..` survives into the non-existing remainder; on Windows the OS collapses it first (and the residual-dotdot test resolves via the outside-roots path, not the ParentDir arm). |
| cargo-mutants generated only 2 mutants for `path_confine.rs`, both unviable | Under `-D warnings`, the whole-function-body replacement (`Ok(Default::default())`) leaves params unused → fails to compile → unviable. No viable mutant survived (0 missed). | Mutation provides little additional signal for small std-only pure helpers under strict lints; the 11 unit tests + assembled e2e are the real evidence. Stated honestly rather than claimed as a strong mutation pass. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test | `tests/e2e-tauri/perimeter_paths.e2e.ts` — the adversarial cases ARE the discovery (traversal refused, CSP blocks inline script, seam exposes stores), run against the BUILT binary | None — the full 19-spec e2e-tauri suite + 60 Playwright specs stay green under the new CSP + seam |

### Maintainer IRL (2026-06-10) — PASS

All five steps passed; 19/19 full e2e-tauri suite. The CSP probe, the traversal refusals (canonicalized `\\?\` paths in the `path_not_permitted` messages), and the shell-resolved seam were observed working; bare-launch `window.__graphStore === undefined` confirmed. Three findings from the CSP console noise:

- **Dead Google-Fonts `@import`** (`src/styles.css`) pinged Google on every launch of a no-telemetry product and (post-CSP) spammed violations — the bundled woff2 has served the fonts since M08.8. **Fixed in-cluster** (removed; `connect-src 'self'` added on principle).
- **`connect-src` lacked `'self'`** — self-origin connects were blocked. **Fixed in-cluster.**
- **MCP children orphan on hard kill** — observed; recorded as confirming evidence for the **M10 MCP-shutdown item** (per-call timeouts/shutdown, already routed to M10 authoring in the milestone Scope). **Routed, not fixed here** (zero-propagation).

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | CLAUDE.md sufficient orientation? | 5 | State-inventory-first resume worked exactly as the restart prompt directed |
| 2 | "Read first" list correct? | 5 | Every cited file was load-bearing |
| 3 | Deliverable + test plan before code? | 5 | Yes |
| 4 | Self-correct in ≤3 rounds? | 4 | The e2e serialization bug took one extra cycle (red masked it); caught + fixed within budget |
| 5 | Do-not-commit-until-approved held? | 5 | No push; awaiting approval. Commits are local-only per the build-machine workflow |
| 6 | Escalate/proceed balance right? | 4 | Surfaced the schema-lock ambiguity (in retro); proceeded on the obvious reading with a flag |
| 7 | Human-direction load reasonable? | 5 | Zero mid-stage intervention needed |
| 8 | Surfaces actionable? | 5 | State inventory + this surface |
| **Process axis total** | | **38/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | Pure helper, path-agnostic archetype, doc comments cite TD-051/052 |
| 2 | Behavior tests not tautologies? | 5 | The hostile cases ARE the acceptance; assembled e2e observes refusal in the built app |
| 3 | Public APIs documented + compile-checked? | 5 | `confine` + `ConfineError` + the dialog commands documented; doc build green with strict flags |
| 4 | Deliverables exactly as promised? | 5 | CSP + seam + confinement + mutants-nightly, all four; plus the implied `pick_artifact_file` for the import flow |
| 5 | Stranger-readable? | 5 | — |
| 6 | §9 anti-patterns absent? | 5 | — |
| 7 | Coverage measured ≥80 / ≥95? | 5 | runtime-main 96.23% lines (≥95 gate); vitest 93.28%; path_confine.rs itself 74% with the unreachable-on-Windows defensive arms noted |
| 8 | CI green across the matrix? | 4 | Verified locally on Windows (both e2e suites + full Rust + frontend gates); CI matrix runs on push |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Template sections useful? | 5 | — |
| 2 | Sections missing? | 4 | A "shell-resolved test-mode seam" pattern is now established (env → js_init_script → window flag → gated renderer); worth a gotcha |
| 3 | Gotchas local or general? | 4 | The `browser.execute` cannot-return-Tauri-error-object trap generalizes — candidate gotcha |
| 4 | Time-box matched? | 5 | Within estimate |
| 5 | Implicit protocol moments? | 4 | The impl-vs-test commit split for contract-adapting pre-existing specs is now demonstrated |
| 6 | Re-runnable by a fresh session? | 5 | — |
| 7 | Format changes needed? | 5 | — |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved** — no push; local commits only, awaiting this surface's approval before push at G
- [x] **G2: no Severity-5 friction** — max severity 3 (the e2e serialization fix)
- [x] **G3: no unaddressed protocol drift** — the strict-TDD split was self-corrected (impl=production only, verified red→impl test diff EMPTY)
- [x] **G4: stage completed** — commits `8e18440`→`d334ee1`; all A.4 acceptance observed in the built app
- [x] **G5: all rows ≥3** — lowest is 4
- [x] **G6: CI-parity** — gates run with the byte-identical §6 commands: `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace`, `cargo test --workspace --doc`, `RUSTDOCFLAGS=… cargo doc`, `cargo audit`, `cargo deny check`, the §6 `cargo llvm-cov` invocations (clean-first per gotcha #81), `npx prettier --check`, `npx eslint .`, `npx tsc --noEmit`, `npx vitest run --coverage`, `npm run test:e2e`, `npm run test:e2e:tauri`. No CI-divergent flags. cargo-mutants ran `--in-place` locally per gotcha #92 (Windows); CI uses scratch-dir (no `--in-place`).

### Soft gates

- [x] **S1:** Process 38/40 ≥30
- [x] **S2:** Product 39/40 ≥32
- [x] **S3:** Pattern 32/35 ≥25
- [x] **S4:** within time-box
- [x] **S5:** ≤3 Severity-3 (one)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Minor CLAUDE.md/gotcha candidates: the shell-resolved test-mode seam pattern, and the `browser.execute` cannot-return-a-Tauri-error-object trap.

---

## [END] Coverage holdouts

No new coverage exclusions. `path_confine.rs` is inside the existing `runtime-main` ≥95 gate (package total 96.23% lines). The file itself sits at 74% because the `ParentDir`-in-remainder rejection (lines ~94–96, ~122–125) and the filesystem-root-reached guard (line ~106) are defensive arms unreachable on Windows due to OS lexical `..` collapse — they are exercised conceptually by `confine_rejects_residual_dotdot_in_nonexistent_remainder` (which resolves via the outside-roots path on Windows) and the `#[cfg(unix)]` symlink test on Linux CI. No `--ignore-filename-regex` change; no `docs/coverage-policy.md` §C entry needed (the gate is on the package, which passes).

---

## [END] Decisions for the next stage (M09.5.B)

1. **The `browser.execute` Tauri-error-serialization trap** — any e2e-tauri spec asserting a command REJECTION must flatten the caught error to primitives (`errorType`/`errorMessage`) before returning; returning the raw error object trips WebView2's structured-clone serializer (`WebDriverError: [object Object]`). Candidate `docs/gotchas.md` entry.
2. **The shell-resolved test-mode seam** is now the archetype for any future production-gated test affordance: env (`AGENT_RUNTIME_E2E`) → `js_init_script` plugin sets a `window` flag → the renderer gates on it. Reuse rather than a bare `import.meta.env.DEV`.
3. **Confirm the additive `PathNotPermitted` error-envelope variant needs no ADR** (or file one) — the maintainer's call per §11. The variant is additive, in-place v1, types regenerated; `codecov.yml`/§6 unchanged.
4. **Stage B (TD-052)** is the same canonicalize-then-check pattern applied to the built-in `Read`/`Write` tools — the `path_confine` resolve logic (nearest-existing-ancestor + resolve-then-check symlink policy) is a reference for B's resolver, though B operates inside the L1 enforcer (ADR-0028 boundary), not the shell.
