# M08.5.5.D.fix — Stage Retrospective

> **Stage:** M08.5.5 Stage `D.fix` — re-verification + reconciliation (docs-only) — see `docs/build-prompts/M08.5.5-mcp-resilience.md`
> **Parent milestone:** M08.5.5 (work-stage-class fix cycle per ADR-0008; no closeout, no gap-analysis entry — resolution flows into M08.6's gap-analysis Carry-forward per `CLAUDE.md` §20)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-24 (after C.fix retro `b9d5a8d` on `claude/m08.5.5-mcp-resilience`)
> **Session end:** 2026-05-25 (surface time; D.fix drafts iterated when B2.fix was inserted in-cycle on 2026-05-25)
> **Branch:** `claude/m08.5.5-mcp-resilience`
> **Starting commit:** `b9d5a8d` (M08.5.5 Stage C.fix retrospective)
> **Ending commit:** `uncommitted — awaiting approval` (this retrospective is committed as part of the stage commit; the IRL paste-back lands as an amend before push). **Intermediate commits on the branch post-D.fix-drafting + pre-D.fix-commit: B2.fix red `677fdf0` + impl `51c4de5` (the in-cycle B2 insertion, pushed 2026-05-25).**
> **Estimated effort (per prompt):** 2–4 hours
> **Actual elapsed:** ~2.5 h initial drafting + ~1 h post-B2.fix re-iteration on D.fix drafts (CHANGELOG, summary, Resolution section, this retro); pending the IRL re-run + paste-back + amend

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response.
- [x] All "Read first" files in the stage prompt were read in the order listed: CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, `docs/M08-irl-findings.md` (existing M08.5 Resolution section as the structural template), `docs/M08-irl-test-plan.md` Scenarios E4 + E5, `docs/m08.5-irl-re-verify-handoff.md` (Option C routing decision + M08.6 carry-forward list), `docs/m08.5-e-fix-amend-block.md` (surfacing-paste-back template), `docs/build-prompts/M08.5.5-mcp-resilience.md` Stage D.fix section D.1–D.4, `docs/build-prompts/retrospectives/M08.5-summary.md` (template for M08.5.5-summary.md), `docs/build-prompts/retrospectives/M08.5.E.fix-retrospective.md` (template + amend pattern), `RETROSPECTIVE-TEMPLATE.md`, `SUMMARY-TEMPLATE.md`. Plus the prior M08.5.5 stage retros (A / B.fix / C.fix) for contract-fidelity evidence collection. After B2.fix's in-cycle insertion on 2026-05-25, the read-list was extended to include `crates/runtime-mcp/src/transport/stdio.rs` (current source, post-fix), `crates/runtime-mcp/tests/mcp_npx_cmd_quoting.rs` (B2.fix's red-phase test), the amended `docs/adr/0023-windows-npm-cli-cmd-wrapper.md` (Multi-arg invocations sub-section), and the B2.fix commit messages on `677fdf0` + `51c4de5` (B2.fix has no separate per-stage retro file per the in-cycle-insertion pattern — its retrospective material is folded into this D.fix retro's stage-trail row + Friction event + Decisions sections + into `M08.5.5-summary.md` Stage trail).
- [x] The deliverable was stated upfront (5 artifacts: Resolution section append with paste-back placeholders, M08.5.5-summary.md cycle aggregate, M08.5.5.D.fix-retrospective.md per template, CHANGELOG `[Unreleased]` cycle entries, CLAUDE.md §6 gate-list reconciliation for the gitleaks gate) + TaskCreate-tracked as 9 discrete deliverables.
- [x] User confirmed the deliverable scope + the 🔴 #7 routing change via AskUserQuestion at session entry — the maintainer chose Option 1 (route 🔴 #7 to M08.6 carry-forward, since C.fix's red-phase decision dropped Sub-fix 2 to prevent user-DB corruption).
- [x] Branch was correct per the stage prompt (`claude/m08.5.5-mcp-resilience`); pre-flight checks confirmed: A.fix + B.fix + C.fix impl commits present in `git log main..HEAD`; the M08.5.5 cycle's three stage retros all present.
- [x] No uncommitted changes carried over from a prior session (`git status` showed only the two pre-existing untracked `.claude/` files + `src-tauri/target/` — same as every prior M08.5.5 stage).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Stage entry — phase-doc vs C.fix retro reconciliation | The Stage D.fix prompt + the phase doc § D.3 + § D.6 commit message all claim 🔴 #7 closed by "migration 004 reconcile" — but the C.fix retrospective documents that Sub-fix 2 was DROPPED at C.fix red-phase per maintainer decision (phase-doc-semantic-inversion would have corrupted user `session.sqlite` files). The Stage D.fix prompt did not absorb the C.fix scope-correction outcome. Surfaced as the first AskUserQuestion of the session. Resolved by routing 🔴 #7 to M08.6 carry-forward + revising the Resolution section / summary / CHANGELOG / commit message to match what actually shipped (not what the phase doc pre-states). | The stage prompt's `<phase_doc_inventory_audit>` should be supplemented with a `<prior_stage_decisions_audit>` block that confirms each prior stage's actual scope (per its retro) matches the phase doc's claims for the current stage's re-verify scope. Without it, a re-verify stage can ship claiming closures that didn't actually happen. | 4 |
| Post-D.fix-drafting / pre-D.fix-commit (2026-05-25) — in-cycle B2.fix insertion mid-D.fix | After the D.fix bundle was drafted + surfaced for the IRL paste-back (2026-05-24), the user manually IRL-re-verified the post-C.fix binary on 2026-05-25 and discovered that B.fix's `cmd.exe /C` wrapper had a SECOND defect in the wrapper itself: cmd.exe's `/?` rule 2 strip-first-and-last applies to the inner command line, mangling the resolved program name (`npx.cmd"` with stray literal `"`). User filed a new B2.fix stage directive (red-phase test + one-line `format!` outer-quote fix + ADR-0023 amendment per explicit override of CLAUDE.md §11 immutability); B2.fix landed in two commits `677fdf0` (red) + `51c4de5` (impl) and was pushed. The D.fix drafts (CHANGELOG, M08.5.5-summary.md, Resolution section, this retro) were then iterated to absorb the new 5-stage cycle shape (A → B → B2 → C → D). | The phase doc is authored at cycle entry. A re-verify stage's IRL paste-back can discover a NEW defect inside an earlier stage's fix itself (the fix-itself-has-a-defect class) — when it does, the cycle's stage count grows + the re-verify stage's drafts need iteration. The stage prompt could codify "the re-verify stage's drafts are iterated, not frozen, until the IRL paste-back is complete AND any newly-discovered defect classes are surfaced + closed." Pairs with the B2.fix Decision below: assembled-app regression must exercise the FULL composition path (real subprocess via `Command::output()`), not just the pre-spawn `build_command` shape — B.fix's tests asserted on the wrapper's resolved program but did NOT exercise the post-wrapper cmd.exe parsing layer where rule 2's defect lives. | 4 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `docs/build-prompts/M08.5.5-mcp-resilience.md` § D.6 commit message (claims 3 closures) vs `docs/build-prompts/retrospectives/M08.5.5.C.fix-retrospective.md` lines 32, 44, 100, 203 (proves Sub-fix 2 was dropped + 🔴 #7 carries forward) | Whether the Stage D.fix Resolution section / summary / commit message should claim 🔴 #7 closed (per phase doc) or carry-forward (per C.fix actual outcome). | Surfaced via `AskUserQuestion` at session entry → maintainer chose carry-forward (Option 1). The Resolution section / summary / CHANGELOG / commit message all authored to match the actual shipped code. | The phase doc § D.3 / D.6 should be amended in a follow-up commit OR (better) the structural fix is the protocol addition above (`<prior_stage_decisions_audit>` block). |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Session entry (after reading prior stage retros) | AskUserQuestion on 🔴 #7 routing: three options (carry-forward / re-scope new sub-stage / leave unmentioned) with concrete trade-offs + a recommendation. | Yes — maintainer chose recommended option (Option 1, carry-forward) immediately. | Right — surfaced BEFORE drafting any of the five deliverables, so they could all be authored from the corrected scope. Saved authoring + rewriting effort. |
| Stage end | Per the `<approval_surface>` items below (filled at surface time). | (filled at surface time) | (filled at surface time) |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The phase doc's pre-stated D.6 commit message was stale wrt C.fix's actual outcome. The Stage D.fix prompt (authored before C.fix shipped) assumed all three 🔴 would close, but C.fix's pre-red phase-doc inspection caught the schema-direction inversion + maintainer dropped Sub-fix 2. The re-verify stage's pre-stated commit message no longer matched reality. | The phase doc is authored at cycle entry; per-stage scope corrections (like C.fix's Sub-fix 2 drop) propagate forward and can invalidate the re-verify stage's pre-stated claims. The stage prompt's `<phase_doc_inventory_audit>` catches file-presence claims but not "this prior stage's outcome differs from the phase doc's prediction" claims. | Adds weight to the M08.5.5-summary.md's recommendation for a `<phase_doc_claims_verification>` step (carried from C.fix Decisions) AND a complementary `<prior_stage_decisions_audit>` block for re-verify stages. The cycle hit two distinct miss classes: (1) phase-doc-vs-codebase inversion at C.fix; (2) phase-doc-vs-prior-stage-outcome divergence at D.fix. Both deserve protocol attention at M08.6. |
| The contract-fidelity-pass-as-V-substitute generalizes cleanly to this cycle's pattern. The cycle has TWO closed 🔴 (#6 + M06.5 #1) — each with documented right-reason-RED on pre-fix `main` (the test-only red commits `ca1db1f` + `feea6e3` + `677fdf0` for B2) + GREEN on impl (`230e1c3` + `94c2bc7` + `51c4de5` for B2) + the `git diff <red>..<impl> -- '<test-paths>'` EMPTY invariant proven in each stage's retro / surface. The dropped 🔴 #7 has its own structural guard: the C.fix retro records the maintainer decision + the carry-forward routing. **The V-substitute is robust to mid-cycle scope corrections** — each surviving 🔴 still has the same shape of evidence; the dropped one has a different but equally explicit decision trail. **B2.fix extends the V-substitute's robustness one more step: the cycle gained a NEW stage mid-D.fix and the new stage's red-commit-impl-commit pair carries the same shape of evidence as A/B/C; the D.fix retro absorbs the new stage's retrospective material directly (no separate retro file per the in-cycle-insertion pattern).** | The M08.5.E.fix precedent established the V-substitute for a no-Stage-V D.fix-class cycle; M08.5.5.D.fix proves the substitute generalizes to (a) a cycle where a planned closure is dropped mid-cycle, AND (b) a cycle where a new stage is inserted mid-cycle. The pattern is durable across both directions of mid-cycle scope-change. | Forward implication: every IRL fix-cycle's re-verify stage runs the same shape of contract-fidelity pass, AND carries forward any dropped sub-fix's decision trail explicitly, AND folds any in-cycle inserted stage's retrospective material into the re-verify stage's retro. The M08.5.5 summary records this. |
| B.fix's wrapper had its own defect that all of B.fix's tests passed — the assembled regression test in `crates/runtime-mcp/tests/mcp_add_with_path_args.rs` was structurally too SHALLOW. It used `transport.connect()` which wraps cmd.exe's exit via the rmcp child-process handshake — that path inherits cmd.exe's stderr to the parent and surfaces a generic "child exited before handshake" error, NOT the SPECIFIC "filename, directory name, or volume label syntax is incorrect" string from cmd.exe's parse-layer. The B.fix test asserted on the wrapper's PROGRAM (cmd.exe correctly resolved) + the bypass of the OS-layer BatBadBut error, but did NOT exercise the post-spawn cmd.exe parsing layer. B2.fix's red-phase test uses `tokio::process::Command::output()` directly (which captures stderr) — and that IS the missing layer of coverage. | The M08.5.5 cycle's existing pattern bit ("assembled-app regression is mandatory for code stages per CLAUDE.md §6 v1.8") was nominally satisfied by B.fix's `connect()`-based assembled test — but the test was structurally too SHALLOW to catch the parse-layer defect. The pattern bit needs sharpening: assembled regression must exercise the FULL composition path the running app uses, capturing the full process tree's stderr/stdout signals, not just the immediate Rust-spawn-layer surface. | Strengthens the M08.5.5 summary's existing Decision (fix-itself-has-a-defect class gotcha candidate). The bit applies forward to any cycle where the fix wraps an OS subprocess: real-spawn via `Command::output()` (or equivalent that captures the full child's stderr) is the canonical assembled-app shape. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 Hard Rules stated at the top; §6 gate ordering; §8 commit flow + do-not-commit + cross-machine state requirement; §12 default-to-executing + escalation discipline; §18 §6-gate-list-update sanctioning; §19 retrospective protocol; §20 gap-analysis routing all load-bearing. Read once at session start; never had to re-read. |
| 2 | Did the stage prompt's "Read first" list correctly orient Claude? | 4 | All listed files were load-bearing — including the three M08.5.5 prior stage retros (without C.fix's retro this stage would have shipped claiming a 🔴 #7 closure that didn't happen, which IS the load-bearing gap the Friction event documents). Gap: the prompt did not explicitly direct reading the prior-stage retros (it lists templates + amend block + handoff doc, but the prior retros are the source-of-truth for "what actually shipped"). |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated in the first response after orientation, as a 5-artifact TaskCreate plan. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Zero self-correction rounds — the one design-class issue (🔴 #7 routing) surfaced as an AskUserQuestion before any drafting, not as a fix-after-the-fact. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` run before this retrospective; the stage commit is drafted, all artifacts assembled, surface pending. Amend gated on user paste-back; push gated on approval. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Escalated the user-domain choice (🔴 #7 routing) via `AskUserQuestion` at the right early moment (BEFORE drafting any of the deliverables, so they could all be authored from the corrected scope). Proceeded autonomously on every technical decision (Resolution section structure matching M08.5's, summary template adherence, CHANGELOG entry conventions matching the existing `[Unreleased]` block, CLAUDE.md §6 secret-scan-gate subsection placement). |
| 7 | Was the human-direction load reasonable? | 5 | One `AskUserQuestion` at session entry (🔴 #7 routing) + one final outcome surface + the user-runs-IRL-on-Windows paste-back surface. No mid-stage approval clutter. |
| 8 | Were the surfaces actionable? | 5 | The single mid-stage question had three explicit options with trade-offs; the final surface (this) carries all `<approval_surface>` items the stage prompt enumerated PLUS the cross-machine state + the contract-fidelity pass evidence + the strict-docs-only invariant proof. |
| **Process axis total** | | **39/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | No code changed; all artifacts are markdown. The Resolution section matches the existing `docs/M08-irl-findings.md` voice (per-🔴 bullets, file:line citations, the M08.5 section's structural template). The CHANGELOG entries match the existing `[Unreleased]` conventions (one `### Changed` heading per stage, bulleted scope description, cross-references to ADRs + retros). The CLAUDE.md §6 edit is a §18-sanctioned gate-list update (new "Secret-scan gate" subsection between "Schema gates" and "Pre-commit hook") — no protocol semantic change. The summary matches the M08.5-summary.md template. The retrospective matches the M08.5.E.fix template + RETROSPECTIVE-TEMPLATE.md fully. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | n/a | D.fix adds no tests — it verifies existing ones. The contract-fidelity pass over B.fix + C.fix's regression tests confirms each is a behavior test (asserts user-observable outcomes — no infinite-loop OS error on the Windows MCP spawn, no stray `mcp.sqlite` after first post-cycle launch — not implementation structure). |
| 3 | Are public APIs documented with compile-checked examples (§6)? | n/a | No public API surface changed. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | Exactly the D.4 deliverables: appended Resolution section (prior lines byte-untouched; verified by `git diff` over the file at amend time); M08.5.5-summary.md per the SUMMARY-TEMPLATE.md / M08.5-summary.md shape; M08.5.5.D.fix-retrospective.md per RETROSPECTIVE-TEMPLATE.md; CHANGELOG `[Unreleased]` cycle entries; CLAUDE.md §6 gate-list reconciliation. The 🔴 #7 scope-correction was a corrected-scope acceptance, not creep or cut — it matches the actual cycle's shipped code. |
| 5 | Would a stranger understand the doc without the spec? | 5 | The Resolution section names each closed 🔴's impl commit SHA + regression test name + file:line root cause + the contract-fidelity invariant; the M08.5.5-summary.md's Stage trail names every red + impl commit SHA; the CHANGELOG re-summarizes each stage's scope. The dropped Sub-fix 2 is honestly recorded with the inversion details + the carry-forward routing. A fresh reader can reconstruct what shipped (and what didn't) from any one of the three artifacts. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No marketing language; comments / prose explain *why* (the schema-direction inversion's user-DB-corruption risk, the cmd.exe wrap's BatBadBut rationale, the path-agnostic archetype's third use). No magic numbers; no stringly-typed APIs. The `<paste verbatim>` placeholder convention is the cycle's user-runs-IRL-on-Windows discipline made explicit (per the M08.5.E.fix precedent). |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured? | n/a | D.fix changes no Rust source. Coverage gates are structurally invariant to a docs-only stage; the C.fix end-of-stage measurements (runtime-main 96.65%) carry through `94c2bc7` to the D.fix commit unchanged. |
| 8 | Did CI green hold across the matrix? | ⏳ | **Cannot be self-verified for the cross-OS `cargo llvm-cov` per-package gates** — D.fix's diff is markdown + CLAUDE.md (no Rust, no `Cargo.toml`, no coverage-config). Coverage results are provably identical to the C.fix end-of-stage measurements which carry through to D.fix unchanged. CI re-runs the full Rust suite on PR regardless. Honestly flagged in the surface; same as the M08.5 cycle's established pattern. |
| **Product axis total** | | **30/30 on scored rows** | Row 8 pending CI; rows 2/3/7 n/a. |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all prompt sections useful? | 4 | `<execution_steps>` (the 12-step shape — stage_0 / draft_resolution / draft_summary_and_retrospective / draft_changelog / reconcile_claude_md / surface_for_user_runs / user_pastes / amend / verify_gates / commit / surface_for_final / fill_retrospective — was correct for a docs-only re-verify stage); `<acceptance_criteria>` (the D.4 checklist drove the artifact list); `<scope_locks>` (kept me out of code-change territory); `<gotchas>` (CLAUDE.md §18 + §20 + the strict-docs-only invariant warning + #81 llvm-cov clean — all load-bearing in retro context); `<execution_warnings>` (the verbatim-from-user discipline + the docs-only-amend invariant were the correct framing); `<retrospective_requirements>` (the special-log directives drove this retro's structure); `<approval_surface>` (drove the surface). Gap: same as Axis-1 #2 — the prompt didn't explicitly direct reading the prior-stage retros, which IS the source-of-truth for the corrected-scope check. |
| 2 | Were any sections needed but missing? | 3 | An explicit `<prior_stage_decisions_audit>` block — recognizing that a re-verify stage's pre-stated claims (per phase doc) can diverge from what actually shipped (per prior stage retros) — would have surfaced the 🔴 #7 routing question without requiring it to be caught at session entry. Not blocking; the AskUserQuestion pattern handled it cleanly. Adds weight to the M08.5.5 summary's protocol-decision recommendation. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize? | 5 | The phase-doc-vs-prior-stage-outcome divergence class generalizes to every IRL fix-cycle's re-verify stage. The contract-fidelity-pass-as-V-substitute generalizes to every IRL fix-cycle's E.fix / D.fix re-verify (cycle's E vs D nomenclature aside). The surfacing-paste-back protocol generalizes to every cycle whose closeout needs real-app observations from a build machine. All three are documented in M08.5.5-summary.md as protocol-decision candidates for M08.6 phase-doc authoring. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | ~2.5 h drafting + (pending amend + push) vs 2–4 h estimate (~0.6–1.25× on the drafting half; full estimate validation pending the IRL paste-back round). Within 2× cleanly on the drafting portion. |
| 5 | Were there implicit protocol moments? | 4 | The "user runs manual IRL on Windows; agent records verbatim" convention (from M08.5.E.fix) is the cycle's foundational re-verify discipline. Made explicit via the `<paste verbatim>` placeholder pattern. The `STAGE-PROMPT-PROTOCOL.md` could codify it as a `<manual_repro_handling>` slot — same candidate as M08.5.E.fix's Pattern #2 carry-forward. |
| 6 | Could a different fresh session reproduce this? | 5 | The Resolution section, summary, CHANGELOG, CLAUDE.md edit, and retrospective are all template-driven (M08.5 Resolution-section structure, SUMMARY-TEMPLATE.md, RETROSPECTIVE-TEMPLATE.md, `[Unreleased]` ### conventions, CLAUDE.md §6 subsection-placement pattern). The 9-task TaskCreate plan is the orientation. The AskUserQuestion on 🔴 #7 routing would re-surface for a fresh session (and resolve the same way — Option 1 is the safe + correct routing). |
| 7 | If we copy this prompt format, does anything need to change? | 4 | Two recommendations (above): explicit prior-stage-retros read direction in `<read_first>`, and the candidate `<prior_stage_decisions_audit>` block. Otherwise the prompt format is sound. |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [ ] **G1: do-not-commit-until-approved rule held** — *evidence:* no `git commit` run before this retrospective; the stage commit is drafted, all artifacts assembled, push gated on approval. **(Filled at amend + surface time.)**
- [x] **G2: no Severity-5 friction events** — *evidence:* max severity 4 (TWO Severity-4 events: the phase-doc-vs-prior-stage-outcome divergence on 🔴 #7 surfaced + resolved at session entry via `AskUserQuestion`; AND the in-cycle B2.fix insertion when the user's IRL re-verify on 2026-05-25 discovered the B.fix-wrapper-has-a-defect class — resolved by inserting + executing the new stage + iterating the D.fix drafts. Both surfaced explicitly + resolved within the stage's protocol envelope; neither blocks the cycle close).
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift table empty.
- [ ] **G4: the stage actually completed** — *evidence:* (filled at amend + surface time. The contract-fidelity pass ran across the two surviving 🔴 — #6 + M06.5 #1 — with documented red commit + impl commit SHAs + the `git diff <red>..<impl> -- '<test-paths>'` EMPTY invariant per stage's retro; the dropped 🔴 #7 has its own decision-trail evidence in the C.fix retrospective + the maintainer's D.fix routing decision. The Resolution section is appended with `<paste verbatim>` placeholders for the user's E4 + E5 IRL re-runs; CHANGELOG `[Unreleased]` carries 4 new `### Changed` entries (A.fix + B.fix + C.fix + D.fix); CLAUDE.md §6 secret-scan-gate subsection added; M08.5.5-summary.md written with the corrected verdict + carry-forward routing; this retrospective is written.)
- [x] **G5: scores ≥3 in every individual row** — *evidence:* lowest is 3 (Pattern row 2, missing `<prior_stage_decisions_audit>` block — recognized as candidate addition, not blocker). Every other scored row at 4 or 5.
- [ ] **G6: CI-parity confirmation** — see below. **(Filled at gates run time.)**

**G6 detail.** Local gate execution this session, with the exact command:

| Gate | Command | Result | CI-parity |
|---|---|---|---|
| cargo fmt | `cargo fmt --all -- --check` | ✓ exit 0 | exact CI command |
| cargo clippy | `cargo clippy --workspace --all-targets -- -D warnings` | ✓ exit 0 (`Finished dev profile in 28.89s`) | exact CI command |
| cargo test | `cargo test --workspace` | ✓ all-green (every workspace test passing; 2 pre-existing ignored — same as C.fix baseline) | exact CI command |
| cargo test --doc | `cargo test --workspace --doc` | ✓ exit 0 (3 passed in runtime_main; 1 in xtask; 2 pre-existing ignored) | exact CI command |
| cargo doc | `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps` | ✓ exit 0 (`Finished dev profile in 37.63s`) | exact CI command (POSIX env-var syntax; the harness Bash tool is POSIX) |
| cargo audit | `cargo audit` | ✓ exit 0 (17 allowed warnings — pre-existing, same as C.fix baseline) | exact CI command |
| cargo deny | `cargo deny check` | ✓ "advisories ok, bans ok, licenses ok, sources ok" (one stale-allow advisory warning — pre-existing, same as C.fix baseline) | exact CI command |
| cargo llvm-cov clean | `cargo llvm-cov clean --workspace` | ✓ (gotcha #81 — done before any coverage-gate run; no coverage gates run this stage per the divergence-citation below) | exact CI command |
| Prettier | `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'` | ✓ "All matched files use Prettier code style!" | exact CI command |
| ESLint | `npx eslint .` | ✓ exit 0 | exact CI command |
| tsc | `npx tsc --noEmit` | ✓ exit 0 | exact CI command |
| Vitest | `npm run test` (`vitest run --coverage`) | ✓ 50 files / 576 tests passed; renderer aggregate coverage unchanged from C.fix baseline | exact CI command |
| npm audit | `npm audit --audit-level=high` | ✓ exit 0 (8 moderate pre-existing — same as A/B/C-fix baseline; no deps added) | exact CI command |
| gitleaks (lefthook) | `gitleaks git --staged --no-banner` | (will run automatically on `git commit` via the lefthook pre-commit hook installed in A.fix) | mirrors `.github/workflows/ci.yml::gitleaks` invocation; the action wraps `gitleaks detect --redact -v` over full history, the lefthook hook runs on `--staged` which is the appropriate local subset |

**Divergence (cited per CLAUDE.md §6 "cite any divergence").**

- The **5× `cargo llvm-cov` per-package gates** (workspace ≥80; runtime-drone ≥95; runtime-main ≥95; runtime-sandbox ≥95; runtime-mcp ≥95) are **not re-run locally** this stage. The D.fix diff contains **zero** Rust source files, zero `Cargo.toml` / `Cargo.lock`, zero coverage-config files (`ci.yml` `--ignore-filename-regex` untouched; `codecov.yml` untouched) — only `.md` files and the CLAUDE.md §6 gate-list reconciliation (markdown). Coverage results are therefore provably identical to the C.fix end-of-stage measurements (runtime-main 96.65%, the new stray_db_cleanup.rs at 96.15%) which carry through `94c2bc7` to the D.fix commit unchanged. CI re-runs the full Rust suite + the 5 coverage gates on the PR regardless. This is the established cycle pattern (the M08.5.E.fix precedent for docs-only re-verify stages).
- The **`e2e-tauri-driver` job's regression tests** are **not run locally** this stage — the build machine in this orchestration session has no `tauri-driver` / `msedgedriver` accessible to the harness. **CI is the verifier** for this stage's E2E acceptance. Inherent to a docs-only re-verify on a harness-less coding session — same M08.5.E.fix precedent.
- The **manual UI repro re-run** of `docs/M08-irl-test-plan.md` Scenarios E4 + E5: **the user runs these on the Windows build machine after seeing the surface**, then pastes verbatim observations back. The Resolution section carries `<paste verbatim>` placeholders that the user fills in after seeing the surface, and the agent amends the commit with the verbatim text before push. The do-not-push-until-approved hard rule + the strict-docs-only-amend invariant (`git diff --stat HEAD` shows ONLY `docs/M08-irl-findings.md` modified) are the safety gates; the cycle's user-runs-IRL-on-Windows discipline is honored per the M08.5.E.fix precedent.

### Soft gates

- [x] **S1:** Process axis total ≥30/40 — *actual:* 39
- [x] **S2:** Product axis total ≥32/40 — *actual:* 30/30 on scored rows; row 8 pending CI; rows 2/3/7 n/a. Treated as met on the scored surface.
- [x] **S3:** Pattern axis total ≥25/35 — *actual:* 30
- [ ] **S4:** Time-box within 2× — *actual:* ~2.5 h drafting vs 2–4 h estimate (~0.6–1.25×). Within 2× cleanly on the drafting portion. **(Final ratio filled at amend + push time.)**
- [x] **S5:** ≤3 Severity-3 friction events — *actual:* 0 (one Severity-4 event documented above; no Severity-3 events)

### Outcome

- [x] **Pattern is sound.** All hard gates pass or are pending the standard amend-then-commit-then-surface-then-push sequence (G1/G4/G6 pending the procedural close). All soft gates pass. D.fix is structurally a verification-and-reconciliation stage; its acceptance is the artifacts (Resolution, CHANGELOG, CLAUDE.md gate-list, retro, summary) + the contract-fidelity pass result + the surfacing-paste-back amend + the surface — all of which landed cleanly. The cycle's pattern held across all four stages; M08 demo path is unblocked for the MCP-Windows portion; M08.6 absorbs the carry-forward (🔴 #4 + 🔴 #5 + 🔴 #7 + 11 🟡 + 🟢).

---

## [END] Coverage holdouts

No coverage gate touched. The D.fix diff is `docs/M08-irl-findings.md`
(append-Resolution-section), `CHANGELOG.md` (`[Unreleased]` entries
for A / B / C / D), `CLAUDE.md` (§6 Secret-scan gate subsection added),
`docs/build-prompts/retrospectives/M08.5.5.D.fix-retrospective.md`
(new), `docs/build-prompts/retrospectives/M08.5.5-summary.md` (new).
Zero Rust source, zero `src/` source, zero `Cargo.toml` / `Cargo.lock`,
zero coverage-config. Workspace and per-package coverage baselines are
unchanged from C.fix's authoritative measurement (runtime-main 96.65%,
stray_db_cleanup.rs 96.15%) carried through `94c2bc7` to this stage
commit. Vitest renderer aggregate also unchanged (no JS/TS changes).

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` are unchanged by this stage. The CLAUDE.md §6 commands are the source-of-truth mirrors and were verified against `.github/workflows/ci.yml` at C.fix; no change since. `codecov.yml` unchanged. The CLAUDE.md §6 edit adds a NEW gate subsection ("Secret-scan gate" between "Schema gates" and "Pre-commit hook") naming the existing gitleaks job + lefthook hook — a documentation-of-an-existing-gate change per CLAUDE.md §18, NOT a workflow change.

---

## [END] Decisions for the next stage (or next parent milestone)

### `CLAUDE.md` updates

- [x] **§6 Secret-scan gate subsection added** in this stage's commit. Documents the existing `gitleaks` CI job + the `lefthook.yml` pre-commit hook per CLAUDE.md §18 gate-list update sanction. Cross-references the install commands per OS + the per-CI-job rationale.
- [ ] **Candidate gotcha (§15)** — fix-itself-has-a-defect on a composed OS subprocess. "When a fix wraps a subprocess invocation (cmd.exe, shell, etc.) in an OS-level shim, the assembled regression must exercise the FULL composition path end-to-end (real spawn via `Command::output()` or equivalent that captures the full child's stderr), not just the pre-spawn `build_command` shape via `transport.connect()` — `connect()`-based assembled tests inherit the child's stderr to the parent and surface a generic 'child exited before handshake' error, hiding the OS-subprocess-layer error signature that the actual user-visible defect produces. M08.5.5.B2.fix's red-phase test is the canonical shape: build via the seam (`StdioTransport::build_command`), spawn via `tokio::process::Command::output()`, capture stderr, assert on the specific OS-error signature." (Source: M08.5.5.B2.fix Surprise #2 + the fix-itself-has-a-defect class.) Reinforces the summary's existing parallel candidate.
- [ ] **Candidate gotcha (§15)** — cmd.exe `/?` rule 2 strip-first-and-last requires outer quoting on multi-arg invocations. "When invoking a `.cmd`/`.bat` shim via `cmd.exe /C <command line>` with multiple arguments (and thus multiple quoted segments in the inner command line), wrap the inner command line in an OUTER pair of quotes: `cmd.raw_arg(format!(\"/C \\\"{inner}\\\"\"));`. cmd.exe's `/?`-documented rule 2 (the 'old behavior' fallback when rule 1's 'exactly two quote characters' condition fails) strips the first and last quote characters of the rest of the command line; without the outer quotes, the inner's first/last quotes get stripped, mangling the resolved program name. The symptom is `The filename, directory name, or volume label syntax is incorrect.` — same string class as pre-B.fix BatBadBut. Reference: Microsoft cmd /? quote-handling rules. Fixed in M08.5.5.B2.fix; ADR-0023 carries the full rationale with the in-place amendment per explicit user override of §11." (Source: M08.5.5.B2.fix.)
- [ ] **Candidate gotcha (§15)** — phase-doc-vs-prior-stage-outcome divergence in re-verify stages. "A re-verify stage's pre-stated claims (per phase doc) can diverge from what actually shipped (per prior stage retros) when a mid-cycle stage corrects scope (drops a sub-fix, scopes down a deliverable). The stage prompt's `<phase_doc_inventory_audit>` catches file-presence claims but not 'this prior stage's outcome differs from the phase doc's prediction' claims. **Mitigation:** every re-verify stage's session entry reads the prior stage retros' [END] Outcome sections + cross-checks each pre-stated closure against the actual shipped scope; AskUserQuestion if material divergence." Pairs with the C.fix-era candidate gotcha on phase-doc-semantic-inversion — same structural class, different surfacing moment.

### `STAGE-PROMPT-PROTOCOL.md` updates

- [ ] **`<prior_stage_decisions_audit>` block for re-verify stages.** Codify reading prior-stage retros' [END] Outcome sections as a stage-entry step (between `<read_first>` and `<execution_steps>`). The block's deliverable is a brief diff between phase-doc pre-stated claims and actual prior-stage outcomes; if material discrepancies exist, AskUserQuestion to either correct the phase doc OR route the dropped sub-fix forward. Candidate carries to M08.6 protocol revision alongside the M08.5.5.C.fix candidate `<phase_doc_claims_verification>` block (same root cause — phase docs aren't auto-updated as cycles progress).
- [ ] **`<manual_repro_handling>` slot** — carried forward from M08.5.E.fix. Candidate remains open at M08.6 protocol revision.
- [ ] **In-cycle stage-insertion protocol** — codify the M08.5.5.B2.fix-class pattern. "When the IRL paste-back at a re-verify stage discovers a NEW defect class (the fix-itself-has-a-defect class, or any other not-anticipated-by-the-phase-doc defect class), the re-verify stage is paused mid-flight; a new stage is inserted via a fresh user directive carrying its own red-then-impl scope; the re-verify stage's drafts are then iterated to absorb the new stage. The new stage has no separate phase-doc section + no separate per-stage retro file (it's an in-cycle insertion); its retrospective material is folded into the re-verify stage's retro's Friction events + Surprise events + Decisions sections, and into the cycle summary's Stage trail row." (Source: this stage's experience with B2.fix. The pattern is durable across cycles where IRL re-verify catches a new defect class.)

### Milestone-prompt updates (M08.6+)

- [ ] M08.6 phase doc effort estimates should distinguish "small-surface strict-TDD code stage" (halve M08.5-class rule-of-thumb) from "large-surface infrastructure stage with cross-stack integration" (keep M08.5-class rule-of-thumb) — the M08.5.5 cycle's pattern.
- [ ] M08.6 Stage A intake absorbs: 🔴 #4 (tier UI/backend desync) + 🔴 #5 (Canvas state not persisted) + **🔴 #7 (`mcp_servers` schema divergence)** + 11 🟡 + 🟢 elevation. The 🔴 #7 addition is the M08.5.5 cycle's contribution to M08.6's intake — the dropped Sub-fix 2 from C.fix red-phase routes here per maintainer decision.
- [ ] M08.6 phase-doc-authoring sessions must grep-verify every concrete claim (schema content, migration order, API signature, file path) against the actual codebase BEFORE the phase doc lands (the M08.5.5.C.fix-era + M08.5.5.D.fix-era recurring miss class).
- [ ] M08.6's gap-analysis Carry-forward section absorbs M08.5.5's resolution per CLAUDE.md §20 (this cycle adds no gap-analysis entry).

### Open issues to file

- [ ] **`StrayDbCleaned` audit-entry-kind** carry-forward (M08.5.5.C.fix Decisions). Tagged as nice-to-have, not blocking.

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff + the artifacts. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M08.5.5 Stage `D.fix`, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per `CLAUDE.md` §20 this work-stage-class cycle adds **no** `docs/gap-analysis.md` entry — the resolution flows into M08.6's gap-analysis Carry-forward.

**Claude:** I have completed M08.5.5 Stage D.fix per the stage prompt + the in-cycle insertion of M08.5.5.B2.fix + the IRL paste-back amend. The cross-stage contract-fidelity pass evidence is collected from the prior stage retros + the in-cycle B2.fix commit pair (B.fix `ca1db1f`→`230e1c3` for 🔴 #6 layer 1 spawn-layer BatBadBut bypass; B2.fix `677fdf0`→`51c4de5` for 🔴 #6 layer 2 parse-layer cmd.exe quote-strip; C.fix `feea6e3`→`94c2bc7` for M06.5 🔴-1; the dropped 🔴 #7 has its own decision-trail evidence in the C.fix retro + this stage's session-entry AskUserQuestion routing). The user's IRL paste-back lands as a strict-docs-only amend on the D.fix commit: Scenarios E4 + E5 verbatim observations replace the `<paste verbatim>` placeholders; the cleanup-module naming prose corrected (`.stray-mcp.sqlite.bak` canonical with `.bak.<unix-ms>` idempotent fallback, NOT `.bak.<UTC-timestamp>` always); two new 🟡 adjacent findings surfaced during the IRL re-verify (MCP-runtime cryptic-error-surfacing UX + MCP Add modal Command-vs-Args split UX) route to M08.6 Stage A intake (joining the 11 🟡 from M08.5 IRL + 🔴 #7 = 13 🟡 + 2 🔴 total). `CHANGELOG.md` `[Unreleased]` carries `### Changed` entries for A.fix + B.fix + **B2.fix** + C.fix + D.fix; `CLAUDE.md` §6 carries the new Secret-scan gate subsection (§18 gate-list update); `M08.5.5-summary.md` records the five-stage roll-up with verdict "Scoped MCP-Windows cluster closed in two layers; 🔴 #7 routed forward; pattern held with two notable Severity-4 frictions" and the carry-forward routing (🔴 #4 + 🔴 #5 + 🔴 #7 + 13 🟡 + 🟢 → M08.6 Stage A intake). The amend is strict-docs-only (verified with `git diff --stat HEAD` before commit --amend). The `e2e-tauri-driver` headline acceptance is pending the post-push CI run (the M08.5 cycle's established CI-as-verifier precedent). I am surfacing this final amended retrospective + the diff + the artifacts for push approval. **I will not push until you approve.**

**Surfaced at:** 2026-05-25 (this surface, post-IRL-paste-back amend)
