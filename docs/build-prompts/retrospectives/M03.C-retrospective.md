<!--
M03 Stage C retrospective. Per CLAUDE.md §19 + RETROSPECTIVE-TEMPLATE.md.
First milestone authored on the v1.2 XML stage-prompt protocol.
-->

# M03.C — Stage Retrospective

> **Stage:** M03 Stage `C` — Remaining 8 node types + animated edges + color encoding; see `docs/build-prompts/M03-live-graph.md` §C
> **Parent milestone:** M03 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-05 ~18:25 UTC
> **Session end:** 2026-05-05 ~18:55 UTC
> **Branch:** `claude/m03-live-graph` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `fc6e9e1` (M03 Stage B — React Flow + Zustand foundation + 3 basic nodes)
> **Ending commit:** `uncommitted — awaiting approval`
> **Estimated effort (per prompt):** 5.5 hours
> **Actual elapsed:** ~0.5 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded via system prompt) and the §4 Hard Rules were stated at the top of the first response (no commit without approval; no push to main; no skipping gates; no telemetry; no hand-rolled types from schemas; cargo deny clean; no unsafe outside runtime-sandbox; capability/drone/sandbox/provider plan-first; .aria/archive untouched; v0.1 scope locked).
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M03-live-graph.md §C.1–C.4, agent-runtime-spec.md §3, MVP §M3, gotchas.md #21/#25/#26/#27).
- [x] All `read_reference` files were read with their `purpose` understood (AgentNode.tsx archetype to mirror; graphStore.ts shape being extended; GraphCanvas nodeTypes map being extended; styles.css color-encoding pattern being extended; AgentNode.test.tsx 5-tests-per-component shape; graphStore.test.ts synthetic-state pattern reference).
- [x] `read_prior_stages` retrospectives consulted (M03.A + M03.B [END] Decisions sections). Specific decisions applied: (1) per M03.B Decisions, kept `nodeTypes` map at module level (now 11 entries) — Stage B trap re-applies; (2) per M03.B Decisions, used **CSS-driven animation via `Edge.animated: true` + `react-flow__edge--animated` class** — no custom edge components; (3) per M03.A + M03.B Decisions, used **schema snake_case** field names throughout (`event.framework`, `event.model`, `event.source`, `event.server`, `event.tool_name`, `event.duration_ms`); (4) per M03.B Decisions, kept the **AgentNode/ToolNode/SkillNode CSS structure** as the archetype — additive `.X-node` + `.X-node--{state}` rules without structural refactor; (5) per M03.B Decisions, leveraged the **exhaustiveness check** as the schema-drift forcing function — promoted `session_start` to a wired case while keeping the `_exhaustive: never` default branch.
- [x] The deliverable was stated in 1–3 sentences and the test plan in 5 bullets BEFORE writing code (in the surface message after orientation reads).
- [x] User did not need to confirm deliverable + test plan separately (per `CLAUDE.md` §12 "operating mode" — outcome approval, not step approval).
- [x] The branch was correct: `claude/m03-live-graph` per the milestone prompt.
- [x] No new uncommitted changes carried over from a prior session — `git status` showed only the 3 carry-forward `.log` files (gitignored, pre-existing).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| `verify_gates` round 1 (prettier) | 9 newly-authored / edited files tripped prettier formatting (graphStore.ts, VerifyNode.tsx, graphStore.test.ts, 6 of the 8 new node-test files). Auto-fixed via `prettier --write`. Same trap class M03.B logged. | M03.B retro Decisions noted "prettier --write is the standard first-step on a multi-file authoring stage"; M03.C reproduced the same trap because the lesson didn't graduate to a per-stage gotcha or `CLAUDE.md` mention. Worth promoting to `docs/gotchas.md` at the M03 closeout (`<gotchas_graduation>`). | 1 |
| `verify_gates` round 1 (act warnings) | Same React Flow + happy-dom `act(...)` warning surface as Stage B — `MarkerDefinitions`, `EdgeRendererComponent`, `Pane`, `ZoomPane`, `FlowRendererComponent` all emit. Tests still pass + gates green. M03.B already flagged this as a candidate for `docs/gotchas.md` #32 graduation. Stage C inherits the same surface; no new mitigation needed. | Same root cause M03.B retro flagged; the Stage F closeout should graduate this trap to `docs/gotchas.md` #32. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| C.3 sample body for `applyEvent('tool_invoked')` says "extend Stage B's existing handler"; spec §3 Behavior says "agent → MCPNode → ToolNode for MCP-hosted tools" | Whether the existing Stage B behavior on a builtin source should be preserved verbatim (just add `animated: true` to its existing edge) OR rewritten to share more code with the new MCP branch | Preserved Stage B's non-MCP behavior verbatim; added the MCP branch as a sibling early-return inside the same case. Both branches set `animated: true` on their tool-call edges. Stage B's existing test (`tool_invoked_adds_ToolNode_and_edge_from_agent`) continues to pass without modification, locking the contract. | M03 prompt §C.3 sample body could lead with "preserve the Stage B path verbatim; sibling-add the MCP path as the first conditional" to reduce design ambiguity. |
| Whether GapNode + HITLNode should have source handles (downstream flow) | Spec §3 Behavior says "GapNode appears immediately on tool_missing (suspends)"; HITL "blocks the graph visually". Both seemed to be terminal states with no downstream flow, but the prompt's C.4 §"renders source/target Handles per node type" listed PlanNode/TaskNode having both. | GapNode + HITLNode ship with **target only** (no source) — graph flow lands in them; resolution comes via UI side-channel, not graph edges. FrameworkNode ships with **source only** (graph root, no upstream). PlanNode/TaskNode/VerifyNode/HookNode/MCPNode have both. The HITL/Gap-target-only decision is the natural reading of "blocks the graph visually" and lines up with M5 / M4 wiring (gap_resolved fires as an event, not as a graph edge mutation). | Document the per-node-type handle layout in `docs/build-prompts/M03-live-graph.md` §C.3 explicitly so M4 / M5 know which side spawns the inbound edge. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| `surface` (end of stage) | This retrospective + diff stat + gate results + draft commit message + explicit "I will not commit until you approve." | Yes — single-bundle approval per CLAUDE.md §8 | Yes — at stage end, after all gates green |

(No mid-stage surfaces. The two ambiguity events above were technical decisions per `CLAUDE.md` §12 "own technical decisions" — preserving Stage B verbatim + per-node handle layout. Both logged here for the closeout's `<gotchas_graduation>` audit rather than escalated mid-stage.)

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §4 #1 do-not-commit-without-approval | Held throughout — no `git commit` invoked at any point. | n/a (no drift) | Surface-only at session end. |
| §4 #5 no hand-rolled types from schemas | The new MCPNodeData / GapNodeData / FrameworkNodeData / etc. interfaces are renderer-only data shapes (not regenerated from `schemas/event.v1.json`); the *event* consumption side continues to use the schema-generated `AgentEvent` discriminated union directly. The renderer-state-shape types are correctly hand-authored — no schema in `schemas/` describes them, since they're not artifacts the runtime emits over IPC. | n/a (no drift) | Schema-as-source-of-truth invariant respected. |
| §4 #10 no scope creep | Resisted the temptation to wire Gap/HITL/Plan/Task/Verify/Hook events to the store even though the components were now in place. The schema doesn't yet declare the events those components consume; M4 / M5 own that work. The synthetic-state testing pattern is the locked alternative for now. | n/a (no drift) | The `<gotchas>` traps "resist scope creep into M4 territory" + "synthetic-state pattern locked here" worked as intended. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Stage C ran in ~30 minutes vs. the 5.5h estimate (ratio 0.09×) | Three factors compounded: (1) the milestone prompt's §C.3 was strikingly detailed — sample interfaces, sample applyEvent body, sample component file, sample CSS palette — which collapsed the design phase to "follow the prompt"; (2) Stage B locked the foundational patterns (nodeTypes outside component, Zustand selectors, Handle/Position imports, ARIA pattern, data-testid) so all 8 new components were near-mechanical mirrors of AgentNode; (3) the 8 component test files were near-identical 5-test shells. The CSS extension was the longest single edit. | Stage estimates calibrated to "novice authoring" overshoot when the prompt is well-detailed and the archetype is locked. M03's calibration row in `docs/build-prompts/M03-live-graph.md` "Estimation calibration" notes "M01: ratio 0.3×; M02: ratio 0.7×; M03 25–31h conservative". Stage C's 0.09× extends this trend. Future stage estimates should account for "if archetype is locked, mechanical replication is fast"; the gap-analysis Carry-forward section is the right place to capture this. |
| The `M03.B Decisions for the next stage` section anticipated **every** Stage C technical decision: nodeTypes-stays-module-level, CSS-driven-animation-not-custom-edge, AgentNode-CSS-pattern-reusable, exhaustiveness-check-as-forcing-function, MCP/Framework-as-the-only-wired-events. There were no surprise-design moments mid-stage. | The post-M02 `[END] Decisions for the next stage` discipline is functioning exactly as intended. M02 retros also did this well, but M03.B's were unusually load-bearing — every one of them was actionable and applied. | Reinforces the protocol: per `CLAUDE.md` §19 "Stage B+ must read prior stage retrospectives' Decisions sections before any code"; this stage was Decision-driven from the first edit. Worth highlighting at the M03 closeout summary. |
| `vitest --coverage` measured `src/components/GraphCanvas.tsx` at 100% line / 33.33% function — same surface as Stage B, unchanged | No regression: the inline arrow handlers (`(_, node) => selectNode(node.id)` and `() => selectNode(null)`) are still uncovered as functions, but `selectNode` itself is fully covered via direct invocation in graphStore tests. Stage D's inspector-panel work will exercise the click → inspector flow end-to-end and pick these up. | No protocol change. Stage D's work naturally closes this gap. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rules §4 stated up front; PR/commit workflow §8 + retrospective protocol §19 + gap-analysis §20 + schemas-as-source-of-truth §14 + style.md §9 all referenced explicitly. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 5 | The 6-file list + 6 reference files + 2 prior-stage retrospectives gave full context. M03.B Decisions section was load-bearing — every decision was applied. The §C.3 sample bodies kept the snake_case schema discipline intact (M03.A + M03.B retros both flagged camelCase prompt-vs-schema drift; Stage C's prompt avoids the trap). |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated explicitly in the surface message before any test or production-code edit. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Round 1 (verify_gates): prettier --write fixed 9 files; eslint + tsc clean on first try; vitest green on first try with all 88 tests passing. Round 2 not needed. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` invoked. Surface-only at session end. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Technical decisions (preserve Stage B's non-MCP path verbatim, per-node-type handle layout, GapNode-no-alert-role, HITLNode-modifier-driven-by-resolved-flag, edge-target-match for tool_result animated-flag clearing) made autonomously per §12 "own technical decisions". Both ambiguity events logged for retro rather than mid-stage escalation. |
| 7 | Was the human-direction load reasonable? | 5 | Zero mid-stage interrupts. Single bundle for approval at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | Diff stat + per-gate results + retrospective + draft commit message in one bundle. |
| **Process axis total** | | **40** | **/40** |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | All 8 component files use named exports with explicit `JSX.Element` return types matching Stage B's archetype. graphStore extensions use named pure helper functions (`mcpPosition`); no abbreviations; no magic numbers (existing `AGENT_X_STRIDE` named constant retained, MCP layout uses `60 + mcpCount * 80` with a comment naming the y=70 placement rationale). No `any`, no `@ts-ignore`, no ad-hoc `// eslint-disable`. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | All 7 new graphStore tests assert observable state shape after event dispatch. The MCP-reuse test (`tool_invoked_with_source_mcp_reuses_MCPNode_across_tools`) catches a class of bug — the lazy-spawn path could naively spawn MCPNode + agent→MCP edge per call — by asserting only one MCP node + one agent→MCP edge after two MCP tool calls. The animated-edge tests assert the state-machine contract: invoke → animated=true; result → animated=false. The 40 component tests assert rendered DOM structure + a11y attrs + handle counts; the GapNode + HITLNode + FrameworkNode tests have specialized assertions for their spec §3 specializations. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | All 8 new data interfaces have JSDoc naming spec § + when M4+ wires the events. `applyEvent` extension docs name the new wirings. No compile-checked examples because the surface is renderer-internal (consumed only by `App.tsx` + `GraphCanvas.tsx`). |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | C.2/C.3 deliverable matched verbatim: 8 new components + 8 new test files + graphStore extensions (8 interfaces, 11-variant union, 2 wired event handlers, animated edge lifecycle) + GraphCanvas nodeTypes 3→11 + styles.css palette + 8 node-style blocks + edge keyframes + CHANGELOG entry. The synthetic-state testing pattern locked here per the prompt's `<gotchas>` direction; no Gap/HITL/Plan/Task/Verify/Hook event wiring (deferred to M4/M5 per scope locks). |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | Each new data interface JSDoc names the spec section + the M4/M5 wiring deferral; the graphStore `applyEvent` MCP branch comments name the spec §3 routing rule; the `tool_result` edge-update comment explains why matching by target resolves both agent→tool and MCP→tool shapes; CSS comments cite spec §3 Visual Design palette. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No hidden AI usage (footer in commit message); no magic numbers (MCP layout uses commented values); no `any`/`@ts-ignore`; no silent error drops; no premature abstraction (the synthetic-state pattern is the test-side discipline, not a framework). The 8 new components share an archetype but each has its own rendered DOM — no inheritance / HOC / generic-component temptation surfaced. |
| 7 | Is coverage ≥80% / ≥95% on safety primitives — measured, not estimated? | 5 | Workspace TS: 95.76% line (≥80%); graphStore.ts: 97.4% line (≥95%, **improved** from 96.37% Stage B baseline); all 11 node components: 100% line; App.tsx: 93.47% (preserved); ipc.ts: 53.12% (preserved — uncovered M02 path). Branches 93.24% globally (≥80%). |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local Windows full suite green (frontend gates only — Stage C is TS-only, so all Rust crates remain at Stage A baselines). CI run pending PR open. Confidence high — no Rust changes; the new TS surface is pure renderer code with no platform-specific paths. |
| **Product axis total** | | **38** | **/40** |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` (XML protocol) sections useful, or were any dead weight? | 5 | All XML slots actively used. The `<gotchas>` slot's "scope creep into M4 territory" + "synthetic-state pattern locked" + "exhaustive applyEvent" + "nodeTypes module-level" + "ARIA per WAI APG" + "color palette in :root" were each load-bearing — the implementation followed each rule from the start because the prompt named them. The `<execution_warnings>` slot's "no schema bump in Stage C" + "no custom edge components" + "no plan-state wiring" kept the scope locks active throughout. |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | One: a `<handle_layout>` or per-node-type clarification slot would help — the per-component handle decision (target-only for Gap/HITL, source-only for Framework, both for the rest) was a technical decision logged in Ambiguity. v1.3 protocol consideration. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15 / `docs/gotchas.md`? | 4 | The 6 stage `<gotchas>` traps were largely M03.C-specific (synthetic-state pattern, exhaustive switch, nodeTypes module-level, ARIA per WAI APG, color palette in :root). The synthetic-state testing pattern + the React Flow + happy-dom act() warning surface (carried from M03.B) should graduate to `docs/gotchas.md` at the M03 closeout (`<gotchas_graduation>`) — both are forward-applicable to any future stage that touches the renderer graph. |
| 4 | Did the time-box estimate match reality (within 2×)? | 4 | Estimated 5.5h, actual ~0.5h. Ratio 0.09× — far under estimate, well within S4 (which requires within 2× — i.e., 0.5×–2×). The 0.09× ratio is below the S4 lower bound (0.5×); flagging as a soft-gate observation, not failure (S4's intent is to catch under-estimates that mask scope drops; here scope was not dropped — coverage holdouts confirm full deliverable). The unusually low ratio reflects the locked archetype + detailed prompt + Decisions inheritance. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | Two: (1) the per-node-type handle layout decision (target-only vs source-only vs both) wasn't explicit in the prompt — see Ambiguity event 2; (2) the prettier-write-as-first-step pattern from M03.B retro Decisions wasn't carried into Stage C explicitly, so the same trap fired again (Friction event 1). Both should graduate to `docs/gotchas.md` at the M03 closeout. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | Yes. The XML slots + `<read_first>` + `<read_reference>` give full orientation; the deliverable is a strict reference to C.3 Detailed Changes (sample bodies for each interface + applyEvent + sample component); gates are objective; retrospective is template-driven. The two ambiguity events would re-occur on a fresh run, but both are technical-decision class (not user-domain) and the resolution path is clear from the spec + Stage B archetype. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | One pre-Stage-D nudge: extend `docs/gotchas.md` (at the M03 closeout) with the synthetic-state testing pattern + the prettier-write-first reminder + the per-node handle-layout convention. These three would close the implicit-protocol-moments observed in Stages B + C. |
| **Pattern axis total** | | **30** | **/35** |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log` shows latest commit is `fc6e9e1` (M03 Stage B — React Flow + Zustand foundation + 3 basic nodes) — no Stage C commits before user approval. Working tree dirty awaiting approval.
- [x] **G2: no Severity-5 friction events** — *evidence:* friction-events table maxes at Sev 2 (React Flow + happy-dom act() warnings — same surface Stage B logged). Sev-1 prettier auto-fix was a 30-second self-correction.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift table records all three checked Hard Rules holding (no commit, no hand-rolled-types-from-schemas, no scope creep). No drift.
- [x] **G4: stage actually completed** — *evidence:* C.2 "Files to Change" + C.3 "Detailed Changes" applied; C.4 acceptance criteria met; full gate suite green; 88 frontend tests pass (7 new graphStore + 40 new component + 41 preserved); coverage 95.76% global / 97.4% graphStore (improved from 96.37%) / 100% on all 11 node components / 93.47% App / preserved on ipc.ts; 8 new components + 8 new test files; nodeTypes map grown 3→11; styles.css palette + 8 node styles + edge keyframes shipped.
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest individual row is 4 (multiple); no row below 3.

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 30
- [⚠] **S4:** Time-box within 2× of actual — *actual:* estimated 5.5h, took ~0.5h, ratio 0.09× (below the 0.5×–2× band). Surfacing as a soft-gate observation, not failure: scope was delivered in full per the coverage holdouts + acceptance criteria; the unusually fast actual reflects the locked archetype + detailed prompt + load-bearing M03.B Decisions inheritance. Pattern: M01 0.3×, M02 0.7×, M03.A 0.7×, M03.B 0.27×, M03.C 0.09× — estimates trending high vs. actuals as the build matures. Worth a calibration note at the M03 closeout summary.
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; 4 of 5 soft gates pass; S4 surfaces as observation not failure (full scope delivered; estimate-actual gap is calibration drift, not scope drop). Proceed to Stage D with minor closeout-time `docs/gotchas.md` graduations per Axis 3 notes (synthetic-state pattern + prettier-write-first + React Flow + happy-dom act() — graduate at Stage F closeout `<gotchas_graduation>`).
- [ ] Pattern is sound but rough.
- [ ] Pattern works but has friction.
- [ ] Pattern is not yet ready.

---

## [END] Coverage holdouts

Per the M02-summary "Coverage holdouts" carry-forward + Stage A/B retro pattern.

### Workspace gate (≥80% line)

- **Coverage actual this stage (Rust):** unchanged from Stage A/B baselines (Stage C is TS-only; no Rust crates touched). Stage A measured workspace 93.99%, runtime-drone 96.84%, runtime-main 99.37%. Re-running cargo llvm-cov is wasted CI cycles for a TS-only stage; full coverage delta will run in CI on PR push.
- **Coverage actual this stage (Frontend):** 95.76% line (target ≥80%); 93.24% branches; 94.28% functions; 95.76% statements.
- **Exclusions added this stage:** None added this stage.
- **Exclusion list as of stage end (workspace Rust):** `src.main\.rs|generated` (per `.github/workflows/ci.yml` workspace gate; unchanged since M02).
- **Exclusion list as of stage end (frontend `vitest.config.ts`):** `src/**/*.d.ts`, `src/main.tsx`, `src/types/**` (generated TypeScript bindings; unchanged since Stage A).

### Per-package gates (≥95% line on safety primitives)

- **Crate `runtime-drone`:** unchanged; baseline preserved (96.84% Stage A).
- **Crate `runtime-main`:** unchanged; baseline preserved (99.37% Stage A).
- **Frontend primitive `src/lib/graphStore.ts`** (treated as safety primitive — single source of graph state): **97.40% line** (improved from 96.37% Stage B baseline), 92.47% branch, 100% functions. The improvement reflects the new wired branches (session_start FrameworkNode, MCP lazy-spawn, animated-edge lifecycle) being exercised by the 7 new tests. Uncovered lines (433–434, 499–501) are the `_exhaustive: never` fallback branch (intentionally unreachable at runtime — guards against future schema additions). Per CLAUDE.md §5: ≥95% with documented exclusions met + improved.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline (Stage B) | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-drone/*` (all five M01-tracked modules) | preserved | unchanged (TS-only stage) | → | n/a |
| `crates/runtime-main/*` (all six M02-tracked modules) | preserved | unchanged (TS-only stage) | → | n/a |
| `src/lib/graphStore.ts` | 96.37% | 97.40% | ↑ | Stage C wires new branches; tests cover them |
| `src/components/GraphCanvas.tsx` | 100% lines / 33.33% functions | 100% lines / 33.33% functions | → | inline event-handler arrow functions on `<ReactFlow>` props (same Stage B surface; Stage D's inspector flow exercises these) |
| `src/components/nodes/AgentNode.tsx` | 100% | 100% | → | n/a |
| `src/components/nodes/ToolNode.tsx` | 100% | 100% | → | n/a |
| `src/components/nodes/SkillNode.tsx` | 100% | 100% | → | n/a |
| `src/components/nodes/MCPNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/GapNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/HITLNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/PlanNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/TaskNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/VerifyNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/HookNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/FrameworkNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/App.tsx` | 93.47% | 93.47% | → | preserved (same uncovered branch as Stage B/M02) |

All M02 + Stage A + Stage B per-module baselines preserved. Stage C improves graphStore.ts +1.03 percentage points and adds 8 new 100%-covered components.

### CI workflow drift check

- [x] No `--ignore-filename-regex` change required. Stage C is purely additive to the renderer surface; the existing exclusions cover the same set of files.

---

## [END] Decisions for the next stage

Stage D inherits a working live graph with all 11 spec §3 node types renderable, animated edges on tool calls, MCP routing on `tool_invoked.source='mcp'`, FrameworkNode root on `session_start`, and the synthetic-state testing pattern locked for the six event-less components. Per the prompt's special_log items:

### `CLAUDE.md` updates

- [ ] No `CLAUDE.md` content changes recommended at this stage. Three candidates for `docs/gotchas.md` graduation at the M03 closeout (`<gotchas_graduation>`): (a) the synthetic-state testing pattern for event-less components; (b) the prettier-write-as-first-step authoring discipline (recurred in M03.B + M03.C); (c) the React Flow + happy-dom `act(...)` warning surface (carried from M03.B).

### `TEMPLATE.md` (XML stage-prompt protocol) updates

- [ ] No template changes recommended at this stage. Consider for v1.3 (post-M03): a `<handle_layout>` slot or per-node-type clarification convention for stages that ship multiple component variants of the same node-class shape. Stage C's per-component handle decision (target-only for Gap/HITL, source-only for Framework, both for the rest) was a Stage-C ambiguity that a slot would have collapsed.

### Milestone document updates (M03-live-graph.md)

- [ ] **§D.3 should preserve all Stage B + Stage C component imports + tests.** Stage D adds the inspector panel + token-spend node-weight scaling + zoom/pan controls but should not modify any of the 11 node components or the graphStore's existing wired cases. Locking this in the Stage D prompt would prevent inadvertent component-shape churn.
- [ ] **§D.3 inspector-panel layout decision.** The prompt's special_log asked which layout (right-rail vs bottom-drawer). **Recommendation: right-rail.** Reasoning: (a) the graph canvas is at `height: 70vh` (per styles.css), so vertical real-estate is at a premium for graph itself; (b) most desktop screens are wider than tall, so a right-rail at e.g. `width: 320px` lets the canvas keep using full vertical space; (c) the spec §3 Behavior text "Click any node for full VDR trace, input/output, timing" implies a panel that doesn't compete with the graph's vertical layout. Bottom-drawer would force the canvas to shrink vertically when expanded, disrupting the live-rendering experience.
- [ ] **§D.3 token-spend node-weight scaling — palette compatibility.** The CSS palette ships with `:root` custom properties for color encoding. Stage D's node-weight scaling (per spec §3 Visual Design "Token spend shown as node weight — larger spend = visually larger node") should drive `transform: scale(...)` or `padding` increase rather than color or border modifications, so the existing `--node-active/complete/error/gap/hitl` palette holds without per-node-type overrides. Verified against the Stage C styles.css structure: each node-type's `.X-node` class has fixed `padding` + `min-width`; Stage D can multiplicatively adjust both via inline style or new `--X-node--heavy` modifier classes.

### Stage D inheritances (load-bearing)

- **Synthetic-state testing pattern is durable.** Stage C's component tests for the six event-less components (Gap, HITL, Plan, Task, Verify, Hook) pass populated state directly into `<NodeComponent>` rather than dispatching events through the store. This pattern is locked here so M4 (plan/task/verify/hook event wiring) and M5 (gap/HITL event wiring) extend without renderer-test churn. **Confirmed durable** — no refactor needed before M4. Stage D should follow the same pattern for any inspector-panel snapshot tests (pass synthetic store state, assert on rendered panel).
- **The 11-node-type CSS color palette holds up against Stage D's token-spend node-weight scaling.** The `:root` custom properties (`--node-active/complete/error/gap/hitl`) drive border-color across all 11 node types. Stage D's node-weight (size, not color) does not need per-node-type color overrides. **Confirmed compatible** — the palette is orthogonal to node-weight.
- **No refactor needed for reuse vs duplication of base styles.** The 8 new node components share an archetype but each has its own rendered DOM (different fields, different specializations like GapNode `data-kind` and HITLNode `role="alert"`). Extracting a shared `<NodeShell>` HOC would force every component to thread props through, hurting clarity. **Confirmed: keep per-component duplication of the small archetype shape.** Stage D does not need to refactor this.
- **`graphStore.applyEvent` is the seam Stage D extends with token-spend tracking.** The `token_usage` event is currently a no-op; Stage D should promote it to a wired case that updates a `tokenSpend` field on AgentNodeData (or attach to a parallel state slice). The exhaustiveness check is the forcing function — adding a wired case is a 1-line `case` label promotion, not a switch refactor.
- **Stage F closeout's `<gotchas_graduation>` should graduate three traps to `docs/gotchas.md`:** (1) synthetic-state testing pattern for event-less node components; (2) prettier-write-as-first-step on multi-file authoring stages; (3) React Flow + happy-dom `act(...)` warning surface (already flagged in M03.B). All three recurred in 2+ stages.

### Protocol additions

- [ ] None applicable at this stage. The S4 calibration drift (estimates trending high vs. actuals across stages) is a Stage F summary-level observation, not a per-stage protocol change.

### Open issues to file

- [ ] None. All carry-forwards captured here, in the M02 + Stage A + Stage B gap-analysis Carry-forwards, and in the M03 closeout's gotchas-graduation slot (Stage F).

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff stat + draft commit message. If discrepancies surface, push back here and Claude revises.

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M03 Stage `C`, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m03-live-graph`; the PR drafts and pushes only at the end of the final stage (Stage F).

**Claude:** I have completed M03 Stage `C` per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until Stage F.

**Surfaced at:** 2026-05-05 ~18:55 UTC.
