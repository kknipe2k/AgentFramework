<!--
M03 Stage A retrospective. Per CLAUDE.md §19 + RETROSPECTIVE-TEMPLATE.md.
First milestone authored on the v1.2 XML stage-prompt protocol.
-->

# M03.A — Stage Retrospective

> **Stage:** M03 Stage `A` — Build hygiene + carry-forward closures + new deps; see `docs/build-prompts/M03-live-graph.md` §A
> **Parent milestone:** M03 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-05 ~15:30 UTC
> **Session end:** 2026-05-05 ~17:15 UTC
> **Branch:** `claude/m03-live-graph` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `4492c59` (M03 prompt-authoring PR #46 merged)
> **Ending commit:** `uncommitted — awaiting approval`
> **Estimated effort (per prompt):** 2.5 hours
> **Actual elapsed:** ~1.75 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded via system prompt) and the §4 Hard Rules were stated at the top of the first response (no commit without approval; no push to main; no skipping gates; no telemetry; no hand-rolled types from schemas; cargo deny clean; no unsafe outside runtime-sandbox; capability/drone/sandbox/provider plan-first; .aria/archive untouched; v0.1 scope locked).
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M03-live-graph.md sections, spec §0–§0d/§3/§13.5, MVP §M3, style.md, gotchas.md, RETROSPECTIVE-TEMPLATE.md).
- [x] All `read_reference` files were read with their `purpose` understood (commands.rs `*_with` archetype; drone_ipc_loopback.rs `current_exe()` archetype; xtask/src/main.rs typify pipeline to extend; src/types/agent_event.ts hand-mirrored shape; runtime-core::event::AgentEvent canonical Rust source-of-truth).
- [x] All `read_prior_milestones` artifacts were consulted (M02 gap-analysis Carry-forward + M02-summary "Decisions to apply before the next parent milestone").
- [x] The deliverable was stated in 1–3 sentences and the test plan in 5 bullets BEFORE writing code (in the surface message after WEBCHECK).
- [x] User did not need to confirm deliverable + test plan separately (per §12 "operating mode" — outcome approval, not step approval).
- [x] The branch was correct: `claude/m03-live-graph` per the milestone prompt.
- [x] No uncommitted changes carried over from a prior session — `git status` showed only the pre-existing `Cargo.lock` modification + 3 untracked `.log` files (not Stage A artifacts; `.gitignore`'d).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| `verify_gates` round 1 (clippy) | `clippy::map_unwrap_or` lint on `schema_path.file_name().map(...).unwrap_or_else(...)` — caught one round, fixed via `map_or_else`. Pedantic+nursery clippy first-pass tax (per gotchas.md #21). | gotchas.md #21 already captures this; the trap fired exactly once on the new function and was a 30-second self-correction. | 1 |
| `verify_gates` round 2 (Playwright timeout) | `page.goto('/')` timed out at 30s. Initial cause: Vite 7 binds the dev server to IPv6 `[::1]:1420` only when `host: 'localhost'` (the implicit default) — Playwright's bundled chromium connects via IPv4, gets ECONNREFUSED. | The M03.A prompt's WEBCHECK header flagged Vite 5→7 as the bump target but did NOT call out the IPv6/IPv4 binding behavior change. Worth adding to gotchas.md. | 3 |
| `verify_gates` round 2 (Playwright timeout, second pass) | After `host: '127.0.0.1'` fix, `page.goto('/')` still timed out — root cause: cold `node_modules/.vite/deps` cache + concurrent workers + Vite 7's Rolldown-scout warmup. First-test cost ~53s vs 30s default. | The bump-Vite-to-7 carry-forward implied "minor risk control bump" but in practice Vite 7's optimizer cold-start is meaningfully slower than Vite 5's on Windows. Worth one line in the M03.A prompt. | 3 |
| `verify_gates` round 2 (TS exhaustiveness) | After regenerating `src/types/agent_event.ts` with the full 36-variant union (vs M02's 10-variant hand-mirrored subset), `src/components/EventList.tsx::renderSummary` became non-exhaustive — `tsc --noEmit` flagged "Function lacks ending return statement". | The prompt anticipates EventList being deleted in Stage B (Pre-existing legacy file inventory entry). The exhaustiveness break is a 1-line `default:` branch fix; not really friction once you understand the schema-broadening implication, but worth a short heads-up that broadening the union surfaces dormant non-exhaustive switches in M02 components. | 2 |
| `verify_gates` round 1 (xtask compile) | First compile of the new test module failed with `unresolved import tempfile` — xtask had no `tempfile` dev-dep. Added 1 line to `Cargo.toml`. | The A.4 default test plan mentions seam unit tests but the deliverable list (A.2 "Files to Change") didn't anticipate the dev-dep addition. Two-line guidance: "if seam tests need scratch dirs, add `tempfile = { workspace = true }` to xtask's `[dev-dependencies]`". | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M03 prompt §A.3 ("10 v0.1 variants matching `runtime_core::event::AgentEvent`") vs the actual `crates/runtime-core/src/event.rs` (~36 variants shipped by M02) | The prompt's variant enumeration uses field names that don't match the shipped Rust enum (e.g., prompt says `tool_id`/`skill_id`/`confidence`; Rust has `tool_name`/`skill_name`/`tool_used`). The hand-mirrored TS at `src/types/agent_event.ts` uses the Rust shipped names. | Followed Rust shipped enum (the canonical source-of-truth per `CLAUDE.md` §14). Schema mirrors all ~36 variants of the actual enum so the TS output covers the full surface. The hand-mirrored 10-variant subset's field names match the Rust enum (the prompt's enumeration was a stale draft) — preserved parity on the M02 subset; new variants added beyond it. | The M03 prompt's §A.3 enumeration of variants should be updated to match the actual Rust enum, OR replaced with a one-line "schema mirrors `runtime_core::event::AgentEvent` verbatim". |
| `<execution_warnings>` says "Vite 8 ships Rolldown (released March 2026, fresh)" | WEBCHECK confirmed Vite 8.0.10 is GA earlier than the prompt anticipated (release notes show Vite 8 stable since Q1 2026). The "fresh" framing suggests "very recently shipped"; in reality it's been shipped for some weeks. | Scope-lock holds regardless: M03 stays on Vite 7 per the execution warning; the warning's literal date is stale but its outcome ("don't bump to 8 in M03") is the operative scope-lock. | The M03 prompt's `<execution_warnings>` could re-phrase: "Vite 8 + Rolldown is GA; M03 stays on Vite 7 for risk control. M04+ evaluates Vite 8." (drops the "released March 2026, fresh" date that has no shelf life). |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| `surface` (end of stage) | This retrospective + diff stat + gate results + draft commit message + explicit "I will not commit until you approve." | Yes — single-bundle approval per CLAUDE.md §8 | Yes — at stage end, after all gates green |

(No mid-stage surfaces. The Vite 7 IPv6 / Playwright timeout / `EventList` exhaustiveness friction was self-correctable within the 3-iteration budget — not user-domain decisions.)

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §4 #1 do-not-commit-without-approval | Held throughout — no `git commit` invoked at any point. | n/a (no drift) | Surface-only at session end. |
| §4 #5 no hand-rolled types from schemas | Stage A's deliverable closes this for the TS side. The new schema covers all variants of the shipped `AgentEvent` Rust enum so future renderer code consumes generated types. | n/a (no drift) | Schema-as-source-of-truth invariant strengthened. |
| §4 #10 no scope creep | Resisted the temptation to start on Stage B's React Flow work even after `@xyflow/react` + `zustand` deps installed. The new deps are dormant in `node_modules` until Stage B. The renderer change in `EventList.tsx` is a 1-line `default:` branch — strict build-hygiene compliance, not a re-architecture. | n/a (no drift) | The `<gotchas>` trap "resist scope creep into graph rendering" worked as intended. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Vite 7 binds the dev server to IPv6 only when `host` is unset / `false` | Vite 5 bound to IPv4 by default with the same config. Behavior change in Vite 6+ that the WEBCHECK URL list didn't surface. | gotchas.md should grow a "Vite 7 dual-stack binding" entry; M03 prompt's WEBCHECK should add `https://vite.dev/config/server-options.html#server-host` to verify the host default behavior. |
| Cold dep-optimization on Vite 7 takes ~53s on Windows | Vite 5's optimizer was usually <10s on the same code surface; Vite 7's Rolldown-scout warmup is materially slower (especially on Windows for Tauri/React deps). | Frontend stages should budget extra time for first cold E2E run; CI should preserve `.vite` cache across job steps. The 60s per-test Playwright timeout I bumped is the safety net. |
| `npm audit` reports 6 moderate vulns in `vitest 2.x → @vitest/mocker → vite ≤6.4.1` chain | These don't affect the top-level `vite ^7.1.0` we now use directly. The vulns ride on vitest's own pinned vite. Bumping vitest to 4.x would close them but is breaking. | Out of Stage A scope (vitest bump). Documented for M04+ Frontend tooling cleanup. CI's `npm audit --audit-level=high` correctly filters them out. |
| The schema source-of-truth produces a 36-variant TS union vs M02's 10-variant hand-mirrored subset | The M02 hand-mirror covered only what the smoke session emitted; the schema covers the full Rust enum. Beneficial — the renderer can now pattern-match on every variant the runtime can emit. But it surfaces dormant non-exhaustive switches (e.g., `EventList::renderSummary`) that M02 quietly tolerated. | Future renderer code should embrace the full union; non-exhaustiveness becomes a tsc-strict signal that catches missing handling at compile time. The 1-line `default: return ''` fix in `EventList.tsx` is appropriate because EventList is replaced in Stage B. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rules §4 stated up front; PR/commit workflow §8 + retrospective protocol §19 + gap-analysis §20 all referenced explicitly. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | The 8-file list + 5 reference files + 2 prior-milestone artifacts gave full context. One miss: `STAGE-PROMPT-PROTOCOL.md` content is dense — 670 lines — and reading it cover-to-cover at session start is heavy. Could be sectioned ("read §1–§6 for slot reference; skim §10–§13 for authoring rules"). |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated in the same turn as WEBCHECK results, before any production code edit. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | Within the 3-iteration `verify_gates` budget. Round 1: clippy + xtask tempfile dep + `tsc` exhaustiveness — fixed in one batch. Round 2: Vite 7 IPv6 binding — fixed via host: '127.0.0.1'. Round 3: Vite 7 dep-optimizer cold start — fixed via per-test timeout bump + workers=1. Three discrete iterations on Playwright; would have benefited from upfront WEBCHECK on Vite 7 IPv6/IPv4 behavior. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` invoked. Surface-only at session end. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Technical decisions (schema variant set, `host: '127.0.0.1'`, vite optimizeDeps, per-test timeout bump) made autonomously per §12 "own technical decisions". Ambiguity events logged for retro rather than mid-stage escalation. |
| 7 | Was the human-direction load reasonable? | 5 | Zero mid-stage interrupts. Single bundle for approval at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | Diff stat + per-gate results + retrospective + draft commit message in one bundle. |
| **Process axis total** | | **38** | **/40** |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | `regenerate_typescript_types_with` follows the `*_with` archetype; `run_npx_json_schema_to_typescript` is named for what it does, not how. No magic numbers, no `any`, no `#[allow]` ad-hoc. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | 4 unit tests (header-banner write; check-mode drift detection; runner-error propagation; byte-stable repeat-runs) each assert observable behavior the production code must satisfy. Case 4 in `check_drift.rs` exercises the full subprocess + file-mutation + restoration flow. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | `regenerate_typescript_types_with` has full `# Errors` section + behavioral docs; production wrapper documented. No compile-checked example because the function is xtask-internal (not a public API). |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | A.3 deliverable matched verbatim: schema, xtask extension, drone test retrofit, dep bumps, file deletions, ignore-list cleanups, vitest --coverage default, CHANGELOG. The Vite IPv6 fix + Playwright timeout bump + `EventList.tsx` exhaustiveness branch are forced consequences of the bump, not expansions. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | The schema header banner names the source-of-truth + drift-check command; the seam's doc names the test/production wiring; the `vite.config.ts` IPv4 comment explains the Node/DNS rationale. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No hidden AI usage (footer signed off in commit); no magic numbers (90_000 timeout has a comment); no `any`/`@ts-ignore`; no silent error drops; no premature abstraction. |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | 5 | Workspace 93.99% (≥80%); runtime-drone 96.84% (≥95%, baselines preserved); runtime-main 99.37% (≥95%, baselines preserved); frontend src/ 89.14% (≥80%, now active default per `vitest run --coverage`). |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local Windows full suite green. CI run pending PR open. Confidence high — the changes are local, no platform-specific code paths added; the drone integration tests retrofit was the cross-platform move (and was M02's archetype). |
| **Product axis total** | | **38** | **/40** |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | All XML slots actively used. The `<execution_warnings>` slot was load-bearing (kept Vite 7 / keyring 3.6 scope locks visible). The `<read_reference>` slot's `purpose` attributes made the 5 reference files immediately actionable. |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | One: a "v1.2 Frontend gate context" addendum or pointer would help — the prompt assumes the agent knows the Vite/Playwright cold-start cost, which v1.2 protocol didn't standardize. Suggest adding a `<runtime_environment>` optional slot for stages that exercise Playwright/Vite/network. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | The four `<gotchas>` traps were all M03.A-specific. But the surprise events (Vite 7 IPv6, dep-optimizer cold-start) generalize to any future Frontend stage and should graduate to `docs/gotchas.md` at the M03 closeout per the v1.2 `<gotchas_graduation>` rule. |
| 4 | Did the time-box estimate match reality (within 2×)? | 4 | Estimated 2.5h, actual ~1.75h. Ratio 0.7× — under estimate, well within S4. Lighter than M02's friction-heavy stages because the deliverable shape was unambiguous and the only unknowns were the fast-moving tooling (caught by WEBCHECK). |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | The Vite 7 IPv6/IPv4 behavior was implicit. The dep-optimizer cold-start budget was implicit. Both should be in `docs/gotchas.md` after Stage F closeout graduates them. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | Yes. The XML slots + `<read_first>` + `<read_reference>` give full orientation; the deliverable is a strict reference to A.3 Detailed Changes; gates are objective; retrospective is template-driven. The Vite 7 IPv6 friction would re-occur on a fresh run, but the WEBCHECK + 3-iteration budget would resolve it. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Two pre-Stage-B nudges: (1) embed the Vite 7 IPv6 + cold-start gotchas into `docs/gotchas.md` so Stage B onward doesn't re-discover; (2) consider a `<runtime_environment>` slot in v1.3 for stages that touch dev-server / Playwright / network surfaces. |
| **Pattern axis total** | | **30** | **/35** |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log` shows latest commit is `4492c59` (Merge PR #46) — no Stage A commits before user approval. Working tree dirty awaiting approval.
- [x] **G2: no Severity-5 friction events** — *evidence:* friction-events table maxes at Sev 3 (Vite 7 binding + cold-start). Both self-corrected within the 3-iteration budget.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift table is empty (no Hard Rule near-misses).
- [x] **G4: stage actually completed** — *evidence:* all A.2 "Files to Change" + A.3 "Detailed Changes" applied; A.4 acceptance criteria met; full gate suite green; codegen drift check zero diff; `npm run test` defaults to coverage with 80%+; legacy `counter.{js,test.js}` deleted; ignore-list cleaned; drone integration tests retrofitted to `current_exe()`.
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest individual row is 4 (multiple); no row below 3.

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 38
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 30
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 2.5h, took ~1.75h, ratio 0.7×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 2

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage B with minor `CLAUDE.md` / gotchas.md updates per Axis 3 notes (Vite 7 IPv6 + cold-start gotchas — graduate at Stage F closeout).
- [ ] Pattern is sound but rough.
- [ ] Pattern works but has friction.
- [ ] Pattern is not yet ready.

---

## [END] Coverage holdouts

Per the M02-summary "Coverage holdouts" carry-forward addition.

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 93.99% (lines, 2861 / 3033 covered)
- **Exclusions added this stage:** None added this stage.
- **Exclusion list as of stage end:** `src.main\.rs|generated` (per `.github/workflows/ci.yml` workspace gate; unchanged since M02).

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
  - **Coverage actual this stage:** 96.84% (1044 / 1077)
  - **Exclusions added this stage:** None added this stage.
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs` (unchanged since M02).

- **Crate:** `runtime-main`
  - **Coverage actual this stage:** 99.37% (638 / 642)
  - **Exclusions added this stage:** None added this stage.
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.key_store\.rs` (unchanged since M02.E).

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline (M02) | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-drone/src/snapshot.rs` | 100.00% | 100.00% | → | n/a |
| `crates/runtime-drone/src/db.rs` | 99.30% | 99.30% | → | n/a |
| `crates/runtime-drone/src/heartbeat.rs` | 98.88% | 98.88% | → | n/a |
| `crates/runtime-drone/src/command_handler.rs` | 98.13% | 98.13% | → | n/a |
| `crates/runtime-drone/src/ipc.rs` | 86.89% | 86.89% | → | n/a |
| `crates/runtime-main/src/providers/anthropic_sse.rs` | 98.33% | 98.33% | → | n/a |
| `crates/runtime-main/src/sdk/agent_sdk.rs` | 99.41% | 99.41% | → | n/a |
| `crates/runtime-main/src/sdk/event_pipeline.rs` | 100.00% | 100.00% | → | n/a |
| `crates/runtime-main/src/sdk/decision_extractor.rs` | 100.00% | 100.00% | → | n/a |
| `crates/runtime-main/src/drone_ipc/client.rs` | 100.00% | 100.00% | → | n/a |
| `crates/runtime-main/src/providers/mod.rs` | 100.00% | 100.00% | → | n/a |

All M02 per-module baselines preserved. Stage A added no new safety-primitive surface (xtask is dev-only tooling, not gated at ≥95%).

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. Verified by inspecting `.github/workflows/ci.yml` lines 339-389 — no Stage A drift.

---

## [END] Decisions for the next stage

Stage B inherits a working build hygiene base + ready React Flow + Zustand deps. Specific applicable decisions:

### `CLAUDE.md` updates

- [ ] No `CLAUDE.md` content changes recommended at this stage. The Vite 7 IPv6 / cold-start gotchas should be added to `docs/gotchas.md` at the M03 closeout (`<gotchas_graduation>`) — they're not Stage B prerequisites.

### `TEMPLATE.md` updates

- [ ] No template changes recommended at this stage. Consider for v1.3 (post-M03): a `<runtime_environment>` optional slot for stages that exercise Playwright / Vite / network so the cold-start budget is explicit.

### Milestone document updates (M03-live-graph.md)

- [ ] Stage A's prompt §A.3 enumeration of "10 v0.1 variants" is now stale (the schema covers ~36 variants matching the Rust enum). The Stage B / C / D / E / F prompts inherit this — they should refer to "the full `runtime_core::event::AgentEvent` surface as exposed in `src/types/agent_event.ts`" rather than re-enumerating subsets. Worth a small Phase doc edit before Stage B opens.

### Stage B inheritances (load-bearing)

- **Zustand store shape Stage B will inherit.** The schema-driven `src/types/agent_event.ts` is now the canonical event union. Stage B's `src/lib/graphStore.ts` should `import type { AgentEvent } from '../types/agent_event'` and base its `applyEvent(event: AgentEvent)` reducer on the discriminated union — no re-mirroring, no per-variant interface in `graphStore.ts`. Stage B's tests should exercise the seam against the full 36-variant union via `Pick<AgentEvent, { type: 'agent_spawned' | ... }>` narrowing patterns rather than asserting against a hand-built subset.
- **TS codegen drift check ran cleanly on first try** after the seam was wired. `cargo xtask regenerate-types --check` produces zero diff on PR-merged state. Stage B work that adds new schemas (e.g., `error.v1.json`) should follow the same pattern: register in `regenerate_types`'s `ts_targets` array; CI's drift check picks it up automatically.
- **`current_exe()` drone test retrofit revealed no other cross-platform path issues.** Both Unix (`integration.rs`) and Windows (`integration_windows.rs`) work under both `cargo test` and `cargo llvm-cov` target dirs. No further retrofit needed.
- **Vite 5→7 bump introduced two unexpected dev-server behaviors that Stage B inherits but shouldn't re-discover:**
  - `vite.config.ts` now pins `host: '127.0.0.1'` (IPv4-only loopback) — keep.
  - `vite.config.ts` now pre-bundles deps via `optimizeDeps.include` — Stage B should add `@xyflow/react` + `zustand` to that list for fast first-load.
  - `playwright.config.ts` now uses `workers: 1` + `timeout: 60_000` + `webServer.timeout: 90_000` + `baseURL: 'http://127.0.0.1:1420'` — keep. If Stage B's Playwright tests grow significantly (Stage F adds tauri-driver + WebdriverIO), the workers=1 setting may need revisiting; for now it absorbs the optimizer cold-start.

### Protocol additions

- [ ] None applicable at this stage.

### Open issues to file

- [ ] None. All carry-forwards captured here, in the M02 gap-analysis Carry-forward, and in the M03 closeout's gotchas-graduation slot (Stage F).

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff stat + draft commit message. If discrepancies surface, push back here and Claude revises.

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M03 Stage `A`, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m03-live-graph`; the PR drafts and pushes only at the end of the final stage (Stage F).

**Claude:** I have completed M03 Stage `A` per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until Stage F.

**Surfaced at:** 2026-05-05 17:15 UTC.
