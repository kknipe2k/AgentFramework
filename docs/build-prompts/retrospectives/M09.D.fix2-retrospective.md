# M09.D.fix — Iteration 2 — Stage Retrospective

> **Stage:** M09 Stage `D.fix` iteration 2 — see `docs/build-prompts/M09-workbench-vertical-slice.md` (Stage M09.D.fix — Iteration 2)
> **Parent milestone:** M09 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m09-prep`
> **Starting commit:** `d77dab6` (after the merge that pulled `b233b97` — the corrected D.fix2 phase-doc section)
> **Ending commit:** `ef851be` (red `f90ed80` → fix `c76befd` [mutation-blocking] → doc fixup `acd9a72` → Expand `784d367` → results disclosure `95e9867` → settings `78f3fe1` → ledger `bea6136` → doc-link fix `ef851be`; execution-status flip pending the maintainer re-IRL)
> **ADR-0008:** iteration 2 — the last. The iteration-1 misdiagnosis was caught **pre-impl** via a Hard-Rule-8 escalation, so the iteration cap was not burned on a non-fix.

---

## Pre-flight

- [x] §4 Hard Rules honored (rule 8 — surfaced the capability-enforcement change as a plan before coding; rule 11 — the flip waits on the re-IRL; no commit before approval; no push).
- [x] Read-first: the corrected D.fix2 section; both dispatcher builders + the bare `CapabilityEnforcer::new()`; `dispatch.rs` `check()` + `mcp_tool_capability`; `enforcer.rs` L4-first + default-Novice + `grant`; `capability_map.rs:148` (the Pure-vs-Irreversible contrast); the MockTransport/MockConnResolver test harness.
- [x] Red approved before implementation ("Iteration-2 red approved. Implement the four distinct commits + the execution-status ledger correction…").

---

## [LIVE] The escalation that re-scoped the root cause (the load-bearing event)

The iteration-1 re-IRL FAILed at Novice though the maintainer was Promoted. The phase doc (D.fix.9 / the first D.fix2 draft) diagnosed "the new seam drops the tracked tier." Grounding the run path **disproved** that — the tier threads correctly to the run-session enforcer. The real denial: `try_mcp_dispatch` checks the **`McpDispatcher`'s OWN** enforcer, which **both** `build_test_mcp_dispatcher` and production `build_mcp_dispatcher` built as a bare `CapabilityEnforcer::new()` (default-Novice, no grants), never framework-/tier-wired. It denies on **L4** (Novice, any user tier) and, even tier-fixed, on **L1** (`mcp_tool_capability` is Exec/Irreversible; the `tools_called` grant is Exec/Pure; `subsumes` needs exact `side_effect_class` equality). **Per Hard Rule 8 / §12 ("Ask first" on capability-enforcement) I stopped and surfaced the corrected diagnosis + plan instead of implementing the misdiagnosed fix on the last iteration** — the maintainer approved Option A (re-scope) + the production-path check. The corrected phase doc + this fix followed. **This is the protocol working as designed: the misdiagnosis was caught before it burned the iteration cap.**

## [LIVE] Friction events

| When | What | Severity |
|---|---|---|
| verify_gates / clippy | `doc_markdown` flagged `MockTransport` in the red fixture doc (red ran fmt/tsc/vitest, not clippy `--all-targets`). Separate labelled fixup `acd9a72`. | 1 |
| verify_gates / doc | The `build_session_mcp_enforcer` docstring used an intra-doc link `[CapabilityEnforcer::new]` that rustdoc can't resolve (type not module-imported in commands.rs). Fixed to a plain code span (`ef851be`). | 1 |
| mutation_gate | cargo-mutants generated only 6 whole-function body-replacement mutants for the enforcer/dispatcher builders — **all unviable** (the functions return `CapabilityEnforcer` / `Arc<dyn …>` / `Framework`, which `Default::default()` can't construct in the scratch build; the functions have no `&&`/`==`/`!` operators to mutate). 0 viable ⇒ 0 survivors, but the gate exercised nothing. The semantics are pinned by the 3-case unit + the real-dispatcher regression instead (a cargo-mutants limitation for constructor/builder fns — gotcha candidate). | 2 |

## [LIVE] Surprise events

| What | Implication |
|---|---|
| The production `build_mcp_dispatcher` shares the identical bare-enforcer gap (`commands.rs:282`) — confirmed at the maintainer's request. | Real (non-Tester) MCP runs would hit the same L4/L1 denial; latent in v0.1 only because the sole caller (`run_smoke_session`) runs a no-tools smoke. Both dispatchers are fixed; the smoke passes an empty framework + Novice (no grants — behaviorally identical to the prior bare enforcer). |
| The mutation gate couldn't produce a viable mutant for a clean builder fn. | The BLOCKING-mutation expectation assumes mutatable logic (operators / branches). Constructor-style fns need behavioral tests as the pin; the gate is structurally inapplicable. Recorded for the maintainer's judgment. |

## [LIVE] Protocol drift events

None. Red (`f90ed80`) committed only after approval. The four feature impl commits do **not** touch `tests/` (`git diff f90ed80..78f3fe1 -- 'tests/'` EMPTY); the impl commit `c76befd` does not touch the in-source red block (0 matches). The two mechanical test fixups (the `MockTransport` backtick; the existing `build_mcp_dispatcher` call-site arg updates) ride in separate / production-driven commits. The flip is **not** committed on green (rule 11).

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | CLAUDE.md orientation? | 5 | Rule 8 + §12 drove the escalation that caught the misdiagnosis. |
| 2 | Read-first oriented? | 5 | The enforcer + dispatch + capability_map reads landed the two-layer (L4+L1) root cause. |
| 3 | Deliverable + plan first? | 5 | Plan surfaced (Hard Rule 8) + approved before code. |
| 4 | Self-correct ≤3 rounds? | 5 | Two mechanical doc rounds, each first-try. |
| 5 | Do-not-commit held? | 5 | Red after approval; flip held; no push. |
| 6 | Escalate/proceed split? | 5 | The defining call — escalated the capability-enforcement change + the misdiagnosis rather than implementing it (Hard Rule 8 / §12). |
| 7 | Human-direction load? | 5 | One escalation + one red approval. |
| 8 | Surfaces actionable? | 5 | The escalation surface drove a precise Option-A re-scope with a corrected spec. |
| **Process total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | §9 style + naming? | 5 | `build_session_mcp_enforcer` named for what it is; *why* comments cite the L4/L1 layers + the authored-only boundary. |
| 2 | Behavior tests? | 5 | Promoted⇒allowed / Novice⇒TierForbidden / unauthored⇒Denied + the real-dispatcher Invoked/Blocked. |
| 3 | Public APIs documented? | 5 | The helper + both dispatchers carry doc comments. |
| 4 | Exactly promised? | 5 | The enforcer fix (both dispatchers) + 3 UI v2 + the ledger correction; no dispatch-logic/transport/schema change. |
| 5 | Stranger-understandable? | 5 | The red header + the docstrings narrate the two-layer denial + the authored-only boundary. |
| 6 | §9 anti-patterns absent? | 5 | No ad-hoc allows; the doc lints fixed properly (backtick / plain span), not suppressed. |
| 7 | Coverage ≥ gates? | 5 | workspace 91.88%; runtime-main 96.32%; runtime-mcp 96.00%; vitest 93.4%. |
| 8 | CI green across matrix? | 4 | All Rust + frontend gates + both e2e legs green on Windows (real-app 18/18 spec files); Linux/macOS in CI. |
| **Product total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | Template sections useful? | 5 | The wire-audit + the Hard-Rule-8 read-first drove the escalation. |
| 2 | Sections missing? | 4 | A "two enforcers — run-session vs MCP-dispatcher" note in the phase doc would have pre-empted both the iter-1 fix and the iter-2 first-draft misdiagnosis. |
| 3 | Gotchas generalize? | 4 | (a) Two separate enforcers gate built-in vs MCP dispatch — wire BOTH. (b) cargo-mutants yields 0 viable mutants for constructor/builder fns (no operators; non-Default returns) — behavioral tests are the pin. |
| 4 | Time-box within 2×? | 4 | Larger than estimated (the escalation + two-layer grounding), but the escalation saved the iteration. |
| 5 | Implicit protocol moments? | 5 | The red/4-impl/3-fixup commit shape + the EMPTY tests/ + untouched-red-block proofs. |
| 6 | Re-runnable fresh? | 5 | The unit + real-dispatcher regression are key-independent, in CI. |
| 7 | Copyable forward? | 5 | The MCP dispatcher is now framework-/tier-wired in both paths — M10+ inherits a working enforced-MCP slice. |
| 8 | Assembled test earned its keep? | 5 | The real-dispatcher regression runs through the actual `check()` path (not a stub) — the layer the iteration-1 stub test bypassed. |
| **Pattern total** | | **37/40** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved** — red after approval; the plan surfaced (Hard Rule 8) + approved; flip held; no push.
- [x] **G2: no Severity-5 friction** — max 2.
- [x] **G3: no unaddressed protocol drift** — `tests/` EMPTY red→impl; red block untouched by impl.
- [x] **G4: stage completed** — impl on `claude/m09-prep`; all machine gates + both e2e legs green; **the maintainer real-app re-IRL PASSED (2026-06-09, Promoted)** — a canvas-authored agent called the MCP `read_text_file` tool → pulled real data → the built-in `Write` landed a real `out/result.txt` in scope; an ungranted Write was denied. The execution-status slice row is flipped (`grounded-by-real-app, live session`).
- [x] **G5: every row ≥3** — lowest is 4.
- [x] **G6: CI-parity**:
  - `cargo fmt --check` ✓ · `clippy --workspace --all-targets -D warnings` ✓ · `cargo test --workspace` ✓ · `cargo doc -D missing_docs/broken_intra_doc_links` ✓ · `cargo audit` ✓ · `cargo deny check` ✓
  - `llvm-cov` workspace 91.88% (≥80) ✓ · runtime-main 96.32% (≥95) ✓ · runtime-mcp 96.00% (≥95) ✓ (clean runs, gotcha #81)
  - `prettier --check` ✓ · `eslint .` ✓ · `tsc` ✓ · `npm run test` 671/671, 93.4% ✓ · `npm audit --audit-level=high` 0 high/critical ✓
  - `npm run test:e2e` (Playwright) → **60/60** (incl. the new results-disclosure test) ✓
  - `npm run test:e2e:tauri` (rebuilt binary) → **18/18 spec files** (incl. the new `tester_expand_frame.e2e.ts`; `vertical_slice.e2e.ts` 2/2) ✓
  - **Mutation gate (BLOCKING, enforcer/dispatcher diff):** `cargo mutants -p agent-runtime --in-diff` → 6 mutants, **6 unviable, 0 viable, 0 missed** (whole-fn body-replacements on constructor-style fns; no operators to mutate). No survivors; the semantics are pinned by the 3-case unit + the real-dispatcher regression. Reported transparently — the gate is structurally inapplicable to these builder fns.

### Soft gates

- [x] **S1:** Process ≥30/40 — 40
- [x] **S2:** Product ≥32/40 — 39
- [x] **S3:** Pattern ≥25/40 — 37
- [x] **S4:** Time-box within 2× — borderline (the escalation + grounding); justified.
- [x] **S5:** ≤3 Severity-3 events — 0

### Outcome

- [x] **Sound.** All gates pass; **the maintainer re-IRL PASSED (2026-06-09, Promoted)** — G4 closed. The execution-status slice row is flipped (citing `vertical_slice.e2e.ts` + `mcp_tool_injection_execution.rs` + the `build_session_mcp_enforcer` unit + the 2026-06-09 IRL date). **M09.D is closed** (across D + D.fix iter1 + iter2) → M09.V → M09.G. No iteration 3 needed.

---

## [END] Coverage holdouts

No `--ignore-filename-regex` change. The new logic landed in `src-tauri/src/commands.rs` (`build_session_mcp_enforcer` covered by the 3-case unit + the real-dispatcher regression; the OS-API dispatcher wrappers under the `src-tauri/**` tauri-shell 50% patch gate). No new exclusion; no §C entry.

---

## [END] Decisions for the next stage (M09.V → M09.G)

- [ ] **Gotchas to graduate (closeout):** (a) **two enforcers** — built-in dispatch uses the run-session enforcer; MCP dispatch uses the `McpDispatcher`'s own — wire BOTH for tier+grants. (b) **cargo-mutants yields 0 viable mutants for constructor/builder fns** (non-Default returns, no operators) — behavioral tests are the pin; don't read "0 viable" as "tested."
- [ ] **Execution-status flip + the ledger re-assertion (M09.G / on re-IRL).** The MCP-dispatch row was corrected (`bea6136`); the canvas-authored-slice row flips on the re-IRL.
- [ ] **Tech-debt (M10):** the verdict-on-gap-suspend truthfulness gap (out of D.fix scope).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M09 Stage `D.fix` iteration 2 through the green checkpoint. The iteration-1 misdiagnosis was caught **before** implementation via a Hard-Rule-8 escalation; the corrected root cause is the MCP dispatcher's own bare enforcer (in **both** `build_test_mcp_dispatcher` and production `build_mcp_dispatcher`), fixed by `build_session_mcp_enforcer` (tier + per-authored-tool grants, authored-only boundary). The red was approved before commit; the four feature commits touch no `tests/` path and the impl does not touch the in-source red block; the mechanical fixups are separate. All Rust + frontend gates + both e2e legs are green (real-app 18/18). The BLOCKING mutation gate found 0 viable mutants (constructor-style fns); the behavior is pinned by the 3-case unit + the real-dispatcher regression — reported transparently. The **execution-status flip is NOT committed** — per rule 11 it awaits the maintainer real-app re-IRL (a real `out/result.txt` at Promoted, in the grown-frame Tester, denied out-of-scope). I will not commit the flip, push, or open a PR until directed. **ADR-0008: if the re-IRL still fails, I escalate — no iteration 3.**

**Surfaced at:** 2026-06-09 (local). **Maintainer re-IRL + execution-status flip:** pending.
