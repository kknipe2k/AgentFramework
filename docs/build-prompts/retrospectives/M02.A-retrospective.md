<!--
Per-session retrospective. Filled DURING the session at [LIVE] markers,
finalized at [END] markers. User reviews; does not fill.
-->

# M02.A — Stage Retrospective

> **Stage:** M02 Stage A — see `docs/build-prompts/M02-event-pipeline.md`
> **Parent milestone:** M02 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (per `CLAUDE.md` §19)
> **Session start:** 2026-05-03 20:23 UTC
> **Session end:** 2026-05-03 22:18 UTC
> **Branch:** `claude/m02-event-pipeline`
> **Starting commit:** `77fc097`
> **Ending commit:** _uncommitted — awaiting approval_
> **Estimated effort (per prompt):** ~2–3 h (M01 0.3× recalibration applied)
> **Actual elapsed:** ~1.9 h (well under upper estimate)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded) and §4 Hard Rules were stated at top of first response
- [x] All "Read first" files in the milestone prompt were read in order: M02-event-pipeline.md A.1–A.4, gap-analysis.md M01 entry + carry-forward, M01-summary.md decisions section, RETROSPECTIVE-TEMPLATE.md, CLAUDE.md, existing crates (runtime-core, runtime-drone), .gitattributes, .gitignore, .github/workflows/ci.yml
- [x] Deliverable + test plan stated before code (this section + scope-clarification on `GracefulShutdown` surfaced before implementation)
- [x] Branch correct: `claude/m02-event-pipeline` per stage prompt
- [x] `git status` clean at start

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Step 0 / orientation | A.3 Option A integration_windows.rs spec calls for `DroneCommand::GracefulShutdown` to cause drone process exit, but M01 wired only OS signals into the shutdown future — `GracefulShutdown` returned the command_handler loop but left the process alive. Required surfacing a small drone-behavior change (oneshot from command_handler→shutdown_source select) to make the prompt's test pass. | The prompt could have noted explicitly: "M02.A extends drone shutdown to fire on IPC `GracefulShutdown` (small wire change, in scope for the Windows test)." Avoided the on-the-fly scoping note. | 2 |
| Verify gates / clippy round 1 | Three clippy-pedantic violations on first lint pass: `doc_markdown` (HeartbeatStatus without backticks), `derive_partial_eq_without_eq` on `Signal` (impossible due to `serde_json::Value`), `needless_pass_by_value` on `check_round_trip(s: Signal)`. Recurring pedantic-on-new-types friction (M01.B saw analogous typify pedantic noise). One self-correction round; all three fixed in <5 min. | The stage prompt could include a checklist: "Before clippy, scan new types for: (a) `serde_json::Value` → add `#[allow(clippy::derive_partial_eq_without_eq)]`; (b) doc paragraphs naming types in plain prose → use backticks; (c) test helpers consuming non-trivial `Clone` types → take by reference." | 2 |
| Verify gates / clippy round 2 | Two more clippy items on the integration_windows test: `too_many_lines` (118 > 100) and `manual_assert` (`if x { panic!() }` → `assert!`). Pure-test-file noise, neither indicates a code-quality issue. One self-correction round (~3 min). | The stage prompt's "test files >100 lines" pattern could pre-flag the `#[allow(clippy::too_many_lines)]` need for integration tests with a single linear flow. | 2 |
| Verify gates / coverage round 1 | First drone-coverage run showed regressions vs M01.C baseline: heartbeat.rs 97.83% (baseline 98.59%), db.rs 98.63% (baseline 98.82%). Root cause: new test had a `match … _ => panic!()` arm (1 unreachable line) and `init_mcp_servers` used four separate `conn.execute()` calls (4 unreachable `?` error paths). Refactored: collapsed to single `execute_batch()`, dropped the panic arm in the heartbeat test. Result: every module at or above M01.C baseline. | The `*_with`/`_inner` doc could include a "test idioms that minimize coverage cost" subsection: prefer `execute_batch` for multi-statement DDL; drop unreachable test panics when an existing test already covers the variant check. | 3 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M02-event-pipeline.md A.3 Option A vs M01 drone implementation | A.3 says `GracefulShutdown` causes drone exit; M01 drone exits only on OS signals. | Implemented IPC-driven shutdown via oneshot from command_handler to run_inner's combined shutdown source (treated as in-scope for the Stage A Windows test). | Future stage prompts that imply a behavior change should call it out explicitly in §A.1 "Problem Statement". |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-implementation | Scope clarification on `GracefulShutdown` semantics for the Windows integration test (small drone-behavior extension required). Surfaced the decision + chosen path before writing code. | Yes — single sentence, clear path forward | Yes — surfaced before any code |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Adding the Windows integration test improved `ipc.rs` coverage from 84.70% (M01.C baseline) to 86.89% — a 2.19pp gain — in addition to closing the §0d Windows-only release-scope gap. | The Windows accept_loop's named-pipe code path was previously exercised only by the `ipc::tests::server_*` unit tests on a self-loop. The integration test exercises the same paths through a real subprocess + named pipe, hitting code regions that the in-process unit tests miss (e.g., the `ServerOptions::first_pipe_instance` path during subsequent client connects). | Cross-platform integration tests have a measurable coverage payoff beyond their primary functional purpose. Apply the same pattern to M02 Stage C/D when the AnthropicProvider's SSE loop ships — an HTTP/2 wiremock-driven integration test would similarly lift coverage on the new `cfg(provider)` paths. |
| `cargo deny` flagged `RUSTSEC-2024-0429` as "advisory was not encountered" — i.e., the glib unsoundness ignore in deny.toml is now stale because the underlying crate is no longer a transitive dep. | Tauri 2.x dependency tree shifted between M01 (where the advisory was actively flagged) and M02.A (where it isn't). Same exit-0 behavior, but the warning is informative. | Future cleanup work could prune the `deny.toml` advisory ignore list when advisories drop off the dep graph. Track but don't act this stage — out of A.2 scope. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 Hard Rules stated at session open; §5/§7/§8/§16 referenced in flow; no orientation gaps surfaced |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | Read order well-defined; M01 retros + gap-analysis carried forward correctly. One minor: A.3 Option A's `GracefulShutdown`-as-shutdown semantics were implicit, not explicit — caused the Sev-2 surface |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated at session start with the GracefulShutdown scope clarification; no code before plan |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Three independent self-correction passes (clippy r1, clippy r2, coverage r1); each within 1 round |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made; surfacing-only |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | One scope surface (GracefulShutdown extension); routine TDD/lint/coverage handled autonomously per §12 |
| 7 | Was the human-direction load reasonable? | 5 | Zero mid-stage user input required after initial prompt |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | The scope clarification was a single sentence with a clear chosen path |
| **Process axis total** | | **39 / 40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 + `docs/style.md`? | 5 | snake_case Rust + UPPER_SNAKE_CASE consts; types CamelCase; one `#[allow]` with justification (`derive_partial_eq_without_eq` on `Signal`); functions ≤50 lines except integration test (justified `#[allow(clippy::too_many_lines)]`) |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | mcp_servers tests assert SQL CHECK invariants (not just "the table exists"); HeartbeatStatus roundtrip exercises the full SQLite store → parse → equal path; integration_windows test is end-to-end behavior; signal round-trips assert serde stability of the wire format |
| 3 | Are public APIs documented w/ compile-checked examples? | 5 | New public types (`HeartbeatStatus`, `Signal`, `ContextType`, etc.) all carry `///` docs; `HeartbeatStatus` has a doctest verifying Display + FromStr round-trip; doctest passed |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 4 | All A.2 files delivered. One scope-extension: drone now exits on IPC GracefulShutdown (necessary for the A.3 Option A test). Documented in this retro and in the commit message |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | `init_mcp_servers` carries a paragraph comment explaining the schema rationale + secrets handling; signal.rs module docs cite spec §2b; HeartbeatStatus docs cite spec §1d; integration_windows test docs explain the relationship to `integration.rs` |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers (timeouts named in test bodies); no stringly-typed APIs (HeartbeatStatus replaced `String`); no `// TODO` without context; no marketing language; one `#[allow(...)]` per file with rationale comment |
| 7 | Is coverage ≥80% / ≥95% on safety primitive (measured)? | 5 | Workspace 96.17% (gate 80%); drone safety primitive 96.84% (gate 95%); per-module: snapshot 100%, db 99.30% (↑ from 98.82%), heartbeat 98.88% (↑ from 98.59%), command_handler 98.13% (↑ from 97.94%), ipc 86.89% (↑ from 84.70%) — all at or above M01.C baseline |
| 8 | Did CI green hold across Linux/macOS/Windows × stable+MSRV (predicted)? | 5 | All gates green locally on Windows; integration_windows runs in 5s; Codecov-uploads added; CI predict: green |
| **Product axis total** | | **39 / 40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 4 | All sections useful; the [LIVE] friction table caught the GracefulShutdown surface and the clippy/coverage rounds in real time |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 3 | A "Coverage holdouts" subsection (per M01-summary.md decision) would have been a natural place for the M01.C/M02.A baseline tracking. Not blocking; nice-to-have |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | Two new patterns generalize: (a) test code that adds `panic!()` arms costs coverage — prefer single-pattern destructuring or single-statement asserts; (b) `serde_json::Value`-bearing enums need `#[allow(clippy::derive_partial_eq_without_eq)]` with rationale |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 2–3 h; actual ~1.9 h. Within range; M01-summary's 0.3× recalibration held |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | The "test idioms that minimize coverage cost" guidance (use execute_batch for multi-statement DDL; avoid unreachable test panics) is a real reusable pattern that's worth promoting |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | The A.3 spec is concrete, the test set is enumerated, the gates are listed. The one ambiguity (GracefulShutdown-as-shutdown) is now documented in this retro for Stage B import |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Two carry-forward suggestions to the M02 stage prompt template: (1) explicit pre-flag for behavior changes implied by tests; (2) a "Coverage cost-aware test idioms" line under STEP 1 |
| **Pattern axis total** | | **29 / 35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — _evidence:_ `git log` shows starting commit `77fc097` unchanged; no commits made; surfacing-only state at session end
- [x] **G2: no Severity-5 friction** — _evidence:_ friction-events table above; max severity is 3 (coverage round 1); resolved within one self-correction
- [x] **G3: no unaddressed protocol drift** — _evidence:_ protocol-drift table is empty
- [x] **G4: stage actually completed** — _evidence:_ A.2 files all touched; A.4 tests all green; all 11 gates pass; `cargo test --package runtime-drone --test integration_windows` passes in 5s; coverage at/above M01.C baselines per-module
- [x] **G5: ≥3 in every axis row** — _evidence:_ minimum row score is 3 (Pattern row 2 "missing TEMPLATE.md sections"); no row below 3

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — _actual:_ 39
- [x] **S2:** Product axis total ≥32 / 40 — _actual:_ 39
- [x] **S3:** Pattern axis total ≥25 / 35 — _actual:_ 29
- [x] **S4:** Time-box within 2× — _actual:_ 1.9 h vs 2–3 h estimate (0.6×–0.95×)
- [x] **S5:** ≤3 Severity-3 friction — _actual:_ 1 (coverage round 1)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage B with the minor TEMPLATE.md / per-stage-prompt suggestions in §Decisions below.
- [ ] Sound but rough
- [ ] Friction-heavy
- [ ] Not yet ready

---

## [END] Decisions for the next stage (Stage B)

### `CLAUDE.md` updates

- [ ] §15 (Common gotchas / `docs/gotchas.md`): add gotcha "Test code that adds unreachable `panic!()` arms costs coverage; prefer single-statement asserts or destructuring without an else-panic. The `init_mcp_servers` `execute_batch` consolidation in M02.A is the archetype." (low priority; not blocking)
- [ ] §15: add gotcha "Enums embedding `serde_json::Value` cannot derive `Eq` (Value contains `f64`); add `#[allow(clippy::derive_partial_eq_without_eq)]` with a one-line rationale. Pattern: `Signal` in `runtime-core/src/signal.rs`."

### `TEMPLATE.md` updates

- [ ] Consider adding a "Coverage holdouts" subsection per the M01-summary recommendation; M02.A would have used it for the per-module baseline ticks.

### Milestone document updates (M02-event-pipeline.md)

- [ ] Stage A §A.1 Problem Statement could be expanded with one sentence: "Stage A also extends `command_handler::run` to drive drone-process exit on IPC `GracefulShutdown` via a `oneshot` to `run_inner`'s combined shutdown source — required by the Windows integration test." (Avoid future fresh-session surfacing the same scoping question.)

### Stage B specifics

- [ ] **Scaffolds Stage B will import from runtime-core:** `signal::Signal`, `signal::ContextType`, `signal::{PreSignalId, ParentSignalId, RetryOfSignalId}`, `HeartbeatStatus`. The `LLMProvider` trait + `ProviderEvent` enum in Stage B's `crates/runtime-main/src/providers/` will sit alongside these in the type surface; no changes to runtime-core needed for B.
- [ ] **`*_with` pattern doc landed correctly?** Yes — `docs/style.md` "Function design / `*_with` / `*_inner` test-seam pattern" subsection cites the M01.C archetype with the executable example. Stage B's `AnthropicProvider::stream_with` SSE-source variant should follow this pattern verbatim per `M01-summary.md` Decision #4.
- [ ] **Coverage delta gate ran cleanly first try?** Cleanly on the local `cargo llvm-cov` invocation (workspace 96.17%, drone 96.84%). The new Codecov delta-gate (`codecov.yml`) takes effect on first M02 PR upload — verify on the M02 PR push that the gate reports correctly. If Codecov rejects the upload format on first PR, revisit the per-crate flag setup in `.github/workflows/ci.yml`.

### Open issues to file

- [ ] None — all decisions are template/doc suggestions, not blocking.

---

## [END] User-review notes

> The user reviews this retrospective alongside the PR. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- [Empty until user reviews]

---

## [END] Sign-off

**Claude:** I have completed M02 Stage A per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted (per CLAUDE.md §20, push waits for Stage F).

**Surfaced at:** 2026-05-03 22:18 UTC.
