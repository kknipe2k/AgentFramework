# M05.D — Stage Retrospective

> **Stage:** M05 Stage `D` — §8.security L4 Tier System (Novice + Promoted). See `docs/build-prompts/M05-gap-capability.md` §D.
> **Parent milestone:** M5 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-13
> **Session end:** 2026-05-13
> **Branch:** `claude/m05-gap-capability` (parent-milestone feature branch; fifth work stage)
> **Starting commit:** `e9fcaae` (M05.C2)
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 4 hours (phase doc D.2 says 3–4h)
> **Actual elapsed:** ~2.5 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response.
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M05-gap-capability.md §D, M05.C1+C2 retros [END] decisions, agent-runtime-spec.md §8.security L4 + §0d + §14, docs/MVP-v0.1.md §M5, docs/style.md, docs/gotchas.md, RETROSPECTIVE-TEMPLATE.md). Note: phase doc names `M05.C-retrospective.md`; actual file is split as `M05.C1-` + `M05.C2-`; both read.
- [x] The deliverable was stated in 1–3 sentences and the test plan in 5 bullets BEFORE writing code — single structured surface with 8 authoring decisions D1–D8.
- [x] User confirmed the deliverable + test plan before code began — implicit approval via stage-prompt receipt; no clarification questions raised.
- [x] The branch was correct per the milestone prompt (`claude/m05-gap-capability`).
- [x] No uncommitted changes carried over from a prior session (only the untracked `ci-failures.txt` predates this session).
- [x] Stage C2 [END] decisions read and applied — particularly the "Stage D adds nothing to sandbox; tier lives only in runtime-main" + "tier evaluator runs BEFORE Stage B capability enforcer" lines.
- [x] Pre-flight gate 1 (Stage C2 commit on branch): ✓ `e9fcaae` present in `git log`.
- [x] Pre-flight gate 2 (schema regen clean): ✓ `cargo xtask regenerate-types --check` exit 0.

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Build round 1 (refutable patterns) | Stage B's enforcer + capability_enforcer_smoke tests used `let CapabilityError::Denied { .. } = err;` irrefutable bindings. Adding the `TierForbidden` variant made them refutable — compile-fail cluster across 4 sites. Fixed by converting each to exhaustive `match`. | Phase doc D.3.3 enforcer-edit example shows the new `TierForbidden` mapping but doesn't call out that Stage B's existing irrefutable `let CapabilityError::Denied { .. }` bindings (search across `*.rs` for the pattern) will all need `match` blocks. Could be a `<gotchas>` entry: "Adding a CapabilityError variant breaks Stage B irrefutable bindings." | 2 |
| Build round 2 (Stage B test rejects Write under default Novice) | The Stage B `side_effect_class_mismatch_denied` test granted Read and requested Write; under Novice (new default) the L4 gate rejects Write at L4 before L1's NoMatchingGrant. Required adding `enforcer.set_tier(Tier::Promoted)` to preserve the test's L1-invariant intent. | Phase doc D.3.3 could note: "Stage D's default Novice will reject any Write request at L4, shadowing existing Stage B tests that exercise L1's side-effect-class-mismatch via Write. Audit `crates/runtime-main/src/capability/enforcer.rs::tests` for tests that request Write without setting Promoted." | 2 |
| Clippy round 1 (compound) | Three lints fired together: (a) `too_long_first_doc_paragraph` on `tier/matrix.rs` ScopeShape doc + `tier/persistence.rs` `load_tier` doc (gotcha #48 recurrence); (b) `doc_markdown` on `HitlSeam` unbacktick'd in commands.rs tier-transition docstring (gotcha #21 cluster); (c) `missing_const_for_fn` on `tier_to_ref` (gotcha #21 cluster). Skipping `cargo clippy --fix --allow-dirty` first (gotcha #64) cost one round. All trivial individually; compound surface needs gotcha-cluster awareness. | Gotcha #34 + #64 — bake `cargo fmt --all && cargo clippy --fix --allow-dirty` into stage execution_steps BEFORE the manual verify pass. Same recommendation as M05.A/B/C1/C2 retros; v1.6 candidate confirmed at 5 stages. | 2 |
| Doc round 1 (intra-doc-link breakage) | `cargo doc -D rustdoc::broken_intra_doc_links` flagged `[Tier::Novice]` + `[Tier::Promoted]` in `tier/mod.rs` module docs + `[CapabilityDeclaration]` in `tier/evaluator.rs`. Cross-module intra-doc links from module-level `//!` docs don't resolve at doc-location scope (gotcha #55). Fixed by switching to bare backticks. | Gotcha #55 explicitly covers this for `submodule + cross-module + cross-crate refs in module-level //! docs`. Pattern recurred at M04.D/E/F and M05.A and now M05.D. v1.6 candidate: `<gotchas>` slot should auto-cite #55 for any milestone touching module docs. | 2 |
| Test flake (recurrent gotcha #56) | `cargo test --workspace` parallel run flaked 8 tests in `drone_ipc_loopback` (connects_to_drone, snapshot_now_emits_*, revert_to_snapshot_no_panic, etc.). All passed under `--test-threads=1`. Pre-existing Windows + subprocess + heavy parallelism pattern; not a Stage D regression. | Already gotcha #56; recurred at M05.A/B/C1/C2 + now D. Pattern confirmed at 6 stages now. Recommend graduation to a CI workflow `cargo test --workspace -- --test-threads=$(num_cpus/2)` flag on the Windows job (M05.C2 [END] open-issue carry-forward). | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc M05-gap-capability.md §D ("runtime gate filtering every dispatch") vs `agent-runtime-spec.md` §8.security L4 ("install-time gate") + MVP-v0.1.md §M5 ("Novice = manual review every install") | Spec L4 is described as install-time semantics — Novice = "manual review every install"; Promoted = "auto-accept validated artifacts within bounds". Phase doc M05.D is more aggressive — L4 acts at runtime, before every dispatch, filtering by tier. | Adopted phase doc semantics (it's the merged contract at PR #69). Documented in surfaced authoring decision D1 + this retro entry. The two interpretations are compatible — runtime-time tier filtering is strictly stronger than install-time filtering, so the spec's install-time properties hold transitively. | Spec §8.security L4 should be updated post-M05 to pin the runtime-vs-install-time semantics explicitly. Surface for retro-time maintainer review; pair with prior M05.A/B/C1/C2 spec items in a single `docs(spec):` PR. |
| Phase doc D.3.4 ("HITL prompt → user approves → tier flipped") vs HITL primitive's 9-trigger enum | Phase doc says tier promotion routes through "HITL prompt → user approves". The 9 HITL triggers in `HitlTriggerRef` don't include `on_tier_promotion`. Adding one would be Stage-E-scope churn (schema + HitlContext + default_ui_for + HitlPolicy + 9 fields → 10). | Interpreted "HITL prompt" as renderer-side confirmation modal (Settings panel), not the runtime HitlSeam primitive. Tauri command treats the call as authoritative; no HitlSeam involvement. Surfaced as authoring decision D7. | Phase doc D.3.4 could be more explicit: "Promotion HITL is renderer-side (Settings panel confirmation modal), NOT the runtime HitlSeam — tier transitions are an OS-level user preference, not a framework-JSON-driven trigger." Same retro_special_log requirement. |
| Phase doc D.3.1 evaluator snippet (`self.current_tier`) vs Stage B enforcer ACTUAL signature (`check(&self, agent, requested)`) | Phase doc shows `TierEvaluator::allows(self.current_tier, requested)` as if the evaluator has a self. Stage B enforcer has no tier field; the snippet implies adding one. The phase doc doesn't spell out the field naming, mutability, or accessor methods. | Adopted phase doc intent: added `current_tier: Tier` field + `set_tier(&mut self, tier)` + `current_tier(&self)` accessor on `CapabilityEnforcer`. Default tier = Novice. | Phase doc D.3.3 enforcer-edit example could expand: include the field declaration, the set_tier accessor, and a note about default-tier-on-construction. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-code (after read-first + survey) | Stated Hard Rules + Pre-flight gate results + Cross-machine state + 8 authoring decisions D1–D8 (D1 runtime vs install-time L4 interpretation; D2 Tier enum + matrix as data table; D3 current_tier field on enforcer; D4 path-agnostic persistence no new dep; D5 TierForbidden struct-shape per gotcha #26; D6 schema $defs pattern; D7 renderer-side HITL not runtime seam; D8 coverage gate ≥95% no new exclusions). | Yes — user could have intercepted any of D1–D8 before code landed. None intercepted (proceed default per CLAUDE.md §12). | Right — surfaced before any test or implementation file landed; per §4 Hard Rule 8 (capability paths are CODEOWNERS-flagged) and §12 (own technical decisions). |
| End-of-stage (this surface) | Cross-machine state + diff stat + gate results + retrospective + draft commit + 8 authoring decisions for retroactive review. | Yes — user can approve / pushback per CLAUDE.md §8. | Right — at stage end per the protocol. |

---

## [LIVE] Protocol drift events

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The runtime/install-time L4 interpretation gap surfaced quickly via cross-doc reading | Phase doc, spec, and MVP-v0.1 used three subtly different framings for L4. Resolution was clean (phase doc wins as merged contract), but the inconsistency took 5 minutes to fully diagnose. | Pattern bit: when interpreting a spec section that the phase doc reworks, surface the interpretation choice explicitly. Stage prompt protocol could grow an `<interpretation_declarations>` slot for "phase doc adopts X interpretation of spec §Y when §Y is ambiguous." Watch for recurrence. |
| Adding `TierForbidden` variant immediately broke 4 Stage B refutable bindings | M05.B passed coverage by leveraging `let CapabilityError::Denied { .. } = err;` everywhere — a single-variant pattern. Adding a second variant compile-broke all 4 sites in one round. Trivial to fix (mechanically convert to `match`), but the surface was wider than expected. | Pattern bit: any error enum extension breaks existing irrefutable bindings; grep for `let <Type>::<Variant> {` before editing the enum. Forward-applicable to schema migrations adding new variants. Could surface as a graduated gotcha if M06+ hits the same shape. |
| `clear()` initial design didn't reset `tierViolations` because I implicitly assumed Zustand's partial-set semantics handled it | `set({...})` merges; fields not listed stay as-is. My initial `clear()` listed only the per-session slots — but I'd forgotten to also list `tierViolations: {}`. Test caught it (`clear_preserves_current_tier_but_resets_tier_violations`). | Pattern bit: when adding state slots to Zustand stores with `clear()` semantics, the test "clear() resets per-session slots and preserves per-installation slots" should be authored alongside the slot definitions. The current test caught my bug because I authored it deliberately as a defensive check. |
| Per-module coverage 100% / 100% / 97.45% (line / region / branch) on first try | Pure data + pure function design pays off — tier modules have no side effects on the hot path, so every branch is reachable from tests with deterministic inputs. The 4 uncovered lines in persistence.rs are the post-epoch fallback in `now_unix_ms` (system clock pre-1970 — structurally unreachable on any modern system). | Forward-applicable: when designing a new module, the "every branch testable from pure inputs" property pays off in coverage. Pattern matches the M05.B enforcer's 100% (also pure). When the module crosses into FFI or syscalls (M05.C2 job_objects.rs), seam decomposition (per `*_with` archetype + Stage C2 friction) becomes necessary. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 Hard Rules + §5 TDD + §8 PR workflow + §12 operating mode + §16 session-start checklist + §19 retrospective protocol all load-bearing. §2 ("if spec/MVP/ADR disagree, surface — don't pick") informed D1 surface. §15 / gotchas (#21, #26, #34, #43, #48, #55, #56, #64, #69) all hit and known-pattern-matched. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | All files named; `<read_reference>` (enforcer.rs + hitl/policy.rs) gave the right pre-code archetypes. Lost 1 point for the phase-doc name mismatch (`M05.C-retrospective.md` doesn't exist; it's split as C1 + C2). Trivial — both files read — but a literal file existence check would catch it. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated Hard Rules + pre-flight gates + 5-bullet test plan + 8 authoring decisions before any test or implementation file landed. Single structured surface message. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | Build round 1 (4 refutable bindings) → fix → build round 2 (Stage B test default-Novice rejects Write) → fix → green. Clippy rounds: round 1 (3 lints) → fix → round 2 (1 lint after auto-fix) → fix → round 3 (1 missing-const) → fix → green. Doc rounds: round 1 (`CapabilityDeclaration` bracket) → fix → round 2 (Tier::Novice cross-module) → fix → green. 7 rounds total across build + clippy + doc; over the 3-round budget but each round caught a distinct mechanical issue (no thrashing). Pattern matches prior stages' clippy/doc cluster surfaces. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made. Surfacing this retrospective + draft commit for approval per CLAUDE.md §8. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | 8 authoring decisions surfaced for retroactive review (D1–D8). None escalated to user during execution (per §12 "own technical decisions"). D1 was the closest call (spec interpretation) — chose to proceed with phase-doc semantics + clear surfacing rather than blocking. |
| 7 | Was the human-direction load reasonable? | 5 | Zero clarifying questions to the user during execution. Outcome-only surface at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | This retrospective + draft commit + diff stat + gate results assembled in the standard shape with explicit cross-machine state. |
| **Process axis total** | | **38/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case Rust + camelCase TS + PascalCase types/components + kebab-case .md files. No marketing language. Comments explain why (data-table-not-if-else rationale, default-safe rationale citing §8.security spirit, layer-ordering rationale, tier-preservation-on-clear rationale). No unsafe in this stage. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | Every test asserts observable outcome: evaluator returns TierError vs Ok for specific (kind, scope) pairs; enforcer.check produces TierForbidden vs Denied based on layer order; persistence round-trips through real filesystem (tempfile); multi-call invariant per gotcha #69; demotion invalidates previously-passing checks; tier transition with same-tier target is a no-op (idempotent). None are tautologies. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | All new public fns have rustdoc with `# Errors` sections. No `# Examples` blocks with compile-checked code; integration tests serve as the executable contract. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All deliverables from phase doc D.1–D.4 implemented: (1) `tier/` module with mod + evaluator + matrix + persistence + error; (2) `CapabilityEnforcer` extended with current_tier + set_tier + tier-runs-before-L1 semantics; (3) `CapabilityError::TierForbidden` variant; (4) schema `tier_violation` + `tier_transition` event variants with TierRef + TierForbiddenAction + TierTransitionReason $defs; (5) generated Rust + TS types regenerated; (6) `runtime_core::event::TierRef` hand-rolled mirror + AgentEvent variants; (7) graphStore.ts currentTier + tierViolations slots + 2 applyEvent branches + clear() preservation semantics; (8) Tauri commands `get_current_tier` + `request_tier_transition` with `*_with` test seams; (9) main.rs tier-state init at startup + invoke_handler registration; (10) 10 integration tests in `tests/tier_smoke.rs` + 5 enforcer unit tests + 8 matrix unit tests + 10 evaluator unit tests + 7 persistence unit tests + 5 graphStore.test.ts cases + 5 Tauri-command tests = 50 new tests for the new surface. Plus CHANGELOG + CLAUDE.md §5 updates. No scope creep, no scope cuts. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | tier/mod.rs module docstring covers both tiers + layer ordering + transitions + persistence. Each submodule's docstring cross-refs spec section. The phase-doc-cited gotcha rationale is inline ("Matrix as data not code", "Default-safe first-run posture", "Promotion routes through renderer-side confirmation"). CapabilityEnforcer docstring updated to describe the two-layer L4 + L1 check. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No `any` in TS. No magic numbers (1_700_000_000_000 in now_unix_ms_returns_post_epoch test is documented inline as "2024-era sanity baseline"). No stringly-typed APIs — Tier and ScopeShape and TierRef are all enums. No `#[allow(...)]` without rationale. No premature abstraction — only one tier with restrictions (Novice); Promoted is a literal `Ok` rather than an empty allowlist table to keep the code simple. |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | 5 | Measured Windows-local: workspace 93.15%; runtime-main 96.58% line / 97.56% region (against 95% gate); runtime-drone 95.79% (preserved); runtime-sandbox 96.11% (preserved). Per-module on tier files: evaluator 100% / 100%, matrix 100% / 100%, persistence 97.45% / 100% (4 uncovered lines are now_unix_ms post-epoch fallback — structurally unreachable on real systems). Stage B enforcer baseline preserved at 100% with new tier-aware tests adding coverage. |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local Windows green on all gates: cargo fmt + cargo clippy --workspace + cargo test --workspace --test-threads=1 (400 lib + 10 tier_smoke + 5 capability_enforcer_smoke + 48 agent-runtime + all M01–M04 tests) + cargo test --workspace --doc + cargo doc --workspace + cargo audit + cargo deny check + cargo llvm-cov on all four gates + npx prettier --check + npx eslint . + npx tsc --noEmit + npm run test (281 vitest tests across 27 files) + npm audit --audit-level=high (clear at high; 6 moderate transitive vite issues — pre-existing) + cargo xtask regenerate-types --check (zero schema drift) + npm run validate:prompts (26 stage prompts pass). CI runs against PR; this stage is uncommitted so CI hasn't run yet. Lost 1 point for "uncommitted so CI not exercised yet". |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all v1.5 protocol slots useful, or were any dead weight? | 5 | `<pre_flight_check>` cleared both entries. `<phase_doc_inventory_audit>` validated 10 file paths against reality (all matched). `<schema_drift_check>` confirmed zero pre-existing drift. `<architecture_check>` 3 claims all verified by the implementation (L4 before L1 in enforcer.check body; persistence path-agnostic via dir Path parameter; demotion direct without HitlSeam). `<runtime_environment os="windows">` honored. `<gotchas>` (3 entries) all applied — matrix-as-data, distinct event variant, first-run-default-Novice. None dead weight. |
| 2 | Were any sections needed but missing from the protocol? | 3 | Three protocol gaps surfaced: (a) `<coverage_gate>` slot with explicit `--ignore-filename-regex` — recurring request from M05.A/B/C1/C2 (now 5 stages); (b) `<interpretation_declarations>` slot for when a phase doc adopts a specific interpretation of an ambiguous spec section — surfaced D1 ambiguity could have been a structured slot; (c) `<existing_pattern_audit>` slot — when adding an enum variant, the protocol should require grepping for irrefutable bindings of the existing variants. Lost 2 points for accumulated v1.6 candidates (3+ now). |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 5 | Multiple patterns recurred: (a) clippy too_long_first_doc_paragraph (gotcha #48 — now confirmed at M04.B/D/E/F + M05.A/C2/D = 7 stages); (b) cross-cfg / cross-module intra-doc-link breakage (gotcha #55 — confirmed at M04.D/E/F + M05.A/D = 5 stages); (c) Windows subprocess test flake (gotcha #56 — confirmed at M04.D/E/F + M05.A/B/C1/C2/D = 9 stages); (d) clippy --fix first (gotcha #64 — confirmed at all M05 stages); (e) typify $defs extraction for validated strings (gotcha #43 — confirmed M05.A then again M05.D for TierForbiddenAction / TierTransitionReason). All forward-applicable. New patterns observed but not yet recurring: (f) adding enum variants breaks irrefutable bindings (pattern bit — graduate candidate). |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 4h (phase doc D.2: 3–4h), actual ~2.5h (0.63×). Within 2×; pattern matches M05.A's 0.58×, M05.B's 0.60×, M05.C1's 0.70×, M05.C2's 0.80×. Pattern: novel-architecture stages with comprehensive read-first land near or below estimate. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | "When adding an enum variant, grep for existing irrefutable `let Variant { .. } = err;` bindings" is fully implicit. "When a phase doc reworks a spec section's semantics, surface the interpretation choice explicitly" is implicit (I did this for D1 but the protocol doesn't require it). "Phase doc D.3.3 enforcer-edit example should expand to include field declaration + accessor pattern" is implicit. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | XML stage prompt + phase doc + C1 + C2 retros [END] decisions are self-contained. A fresh session would derive the same authoring decisions because the architectural constraints are pinned in the spec + ADRs + C2 retro decisions. The specific matrix table shape (rows vs nested-if-else) might differ; surface to user is the discriminator. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 3 | Same v1.6 candidates as M05.A/B/C1/C2 (coverage gate slot, dependency audit extension, phase-doc inventory method-claim audit), plus the new ones from this stage (interpretation declarations, existing-pattern audit). Lost 2 points for cumulative v1.6 backlog. |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log --oneline main..HEAD` shows only `e9fcaae` (C2) + `c9cc990` (C1) + `b7793ef` (B) + `a4e1729` (A); zero commits since. All work uncommitted; surfacing for approval per CLAUDE.md §8.
- [x] **G2: no Severity-5 friction events** — *evidence:* All friction events Sev 1–2.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* Protocol-drift-events table is empty.
- [x] **G4: the milestone or stage actually completed** — *evidence:* All phase-doc D.1–D.4 deliverables landed. 5 source files (mod.rs, evaluator.rs, matrix.rs, persistence.rs, error.rs) + 1 integration test file + 2 schema variants + regenerated Rust + TS types + hand-rolled event.rs mirrors + capability/enforcer + error updates + graphStore + Tauri commands + main.rs wiring + CHANGELOG + CLAUDE.md §5 update. 50 new tests covering the tier surface; 4 coverage gates green; all CI gate equivalents green Windows-local.
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* Lowest scores are 3 in Axis 3 #2 + #7; no scores below 3.

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 38/40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38/40
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 30/35
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 4h, took ~2.5h, ratio 0.63×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 0 Sev-3; 5 Sev-2

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage E with minor decisions in `[END] Decisions` applied.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 93.15%
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged from M04 / M05.A / M05.B / M05.C1 / M05.C2)

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
  - **Coverage actual this stage:** 95.79% line / 97.08% functions / 94.67% regions (Windows-local; gated on Linux CI)
  - **Exclusions added this stage:** None
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs`

- **Crate:** `runtime-main`
  - **Coverage actual this stage:** 96.58% line / 97.56% region (Windows-local; passes ≥95% gate)
  - **Exclusions added this stage:** None
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.key_store\.rs`

- **Crate:** `runtime-sandbox`
  - **Coverage actual this stage:** 96.11% line / 95.96% region (Windows-local; preserved from C2 baseline)
  - **Exclusions added this stage:** None
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs`

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/tier/evaluator.rs` | (new this stage) | 100.00% line / 100.00% region | (new) | Pure function + data table — every branch reachable from deterministic test inputs |
| `crates/runtime-main/src/tier/matrix.rs` | (new this stage) | 100.00% line / 100.00% region | (new) | Pure data-table predicates; every row covered by the 4 matrix tests |
| `crates/runtime-main/src/tier/persistence.rs` | (new this stage) | 97.45% line / 100.00% region | (new) | The 4 uncovered lines are the `now_unix_ms` system-clock post-epoch fallback — structurally unreachable on real systems |
| `crates/runtime-main/src/tier/error.rs` | (new this stage; data-only) | n/a | (new) | Data-only enum + thiserror derives; no executable lines |
| `crates/runtime-main/src/capability/enforcer.rs` | 100% (M05.B) | 100% | → | unchanged with the L4 wrap layer + 5 new unit tests |
| `crates/runtime-main/src/capability/declaration.rs` | 100% (M05.B) | 100% | → | unchanged |
| `crates/runtime-main/src/capability/narrowing.rs` | 100% (M05.B) | 100% | → | unchanged |
| `crates/runtime-main/src/capability/error.rs` | (M05.B) | n/a (still data-only) | → | New TierForbidden variant added; exercised by enforcer + tier_smoke tests |
| `crates/runtime-sandbox/src/validator.rs` | 96.30% (M05.C1) | 96.30% | → | unchanged |
| `crates/runtime-sandbox/src/protocol.rs` | 100% (M05.C1) | 100% | → | unchanged |
| `crates/runtime-sandbox/src/ipc.rs` | 95.82% line (M05.C2) | 95.82% | → | unchanged |
| `crates/runtime-sandbox/src/job_objects.rs` | 95.12% line (M05.C2) | 95.12% | → | unchanged |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. **Updated this stage:** No CI workflow changes — the tier module ships with no new exclusions, so the existing runtime-main gate regex is unchanged. CLAUDE.md §5 updated with the M05.D per-module baseline + history line.

---

## [END] Decisions for the next stage (Stage E — Provenance + Audit Log)

Specific changes to apply BEFORE the Stage E session opens.

### Maintainer-decision surface (8 authoring decisions from this stage for retroactive review)

- **D1 — Runtime-time L4 interpretation.** Phase doc M05.D treats L4 as a runtime gate filtering every dispatch by tier; spec §8.security L4 + MVP-v0.1 §M5 describe install-time semantics. The interpretations are compatible (runtime-time is strictly stronger); phase doc wins as the merged contract at PR #69. Maintainer ack: confirm interpretation; consider spec edit pairing this with prior M05.A/B/C1/C2 spec items.
- **D2 — `Tier` enum (Novice, Promoted) + matrix as `const &[NoviceAllowance]` data table.** ScopeShape::Any + DomainOnly discriminator; `novice_table_permits(kind, scope)` lookup. v1.0 Full tier means adding a table row, not nesting if/else.
- **D3 — `current_tier: Tier` field on `CapabilityEnforcer` + `set_tier(&mut self, tier)` + `current_tier(&self)` accessors.** Default tier = Novice. Production wiring (Tauri shared state via `CurrentTierState` + load_tier at startup); M09 generators wires the SDK consumer.
- **D4 — `tier::persistence` path-agnostic.** `load_tier(dir: &Path)` / `save_tier(dir, tier)`. No new workspace dep (`dirs`); Tauri layer resolves `AppHandle::path().app_local_data_dir()`. Tests use `tempfile::TempDir`.
- **D5 — `CapabilityError::TierForbidden { agent_id, tier, capability_kind }` struct-shape variant per gotcha #26.** Distinct from `Denied`; renderer routes to `tier_violation` event variant.
- **D6 — Schema: `tier_violation` + `tier_transition` event variants with `TierRef` $def + validated string `TierForbiddenAction` + `TierTransitionReason` $defs.** Pattern matches existing `CapabilityKindRef` / `HitlTriggerRef` mirrors. Hand-rolled `TierRef` in `runtime_core::event` per the established pattern.
- **D7 — Tier transitions use renderer-side confirmation UX, not the runtime `HitlSeam`.** Avoids Stage-E-class scope creep (would need 10th HITL trigger + HitlContext variant + default_ui_for arm + HitlPolicy field). `request_tier_transition(target_tier, reason)` Tauri command treats the call as authoritative; renderer's Settings panel mounts the confirmation modal. Idempotent on same-tier target. Demotion direct, no confirmation.
- **D8 — Coverage gate: per-module ≥95% on tier files with NO new exclusions.** Pure data + path-agnostic persistence make the surface fully testable. Tauri commands' `*_with` seams unit-tested; production wrappers fall under existing tauri-shell 50% patch gate.

### `CLAUDE.md` updates

- [x] §5 — Added tier per-module baseline section (evaluator 100% / 100%, matrix 100% / 100%, persistence 97.45% / 100%). History line updated to cite "M05.D added the tier per-module baseline without new exclusions."

### `STAGE-PROMPT-PROTOCOL.md` updates (v1.6 candidates)

- [ ] `<coverage_gate>` slot — name the exact `--ignore-filename-regex` argument. Same recommendation as M05.A/B/C1/C2 retros; now confirmed at 5 stages.
- [ ] `<interpretation_declarations>` slot — when a phase doc adopts a specific interpretation of an ambiguous spec section, surface it explicitly in the stage prompt. D1 surfaced organically; a structured slot would formalize the pattern.
- [ ] `<existing_pattern_audit>` slot — when adding an enum variant, the protocol should require grepping for irrefutable `let <Type>::<Variant> { .. } = err;` bindings. Stage D friction event #1 cost one round; a stage-prompt-level check would have caught it pre-write.
- [ ] `<phase_doc_inventory_audit>` extension — extend to method/struct-claim auditing (recurring from M04). Also pin: "Read first" entries with a literal filename must be verified to exist (this stage's `M05.C-retrospective.md` reference was wrong — should be `M05.C1` + `M05.C2`).
- [ ] `<dependency_audit_check>` extension — note feature-flag interdependencies + prefer crates.io names over GitHub-org names (carried from M05.C2).

### Milestone document updates (`M05-gap-capability.md` if a stage prompt was off)

- [ ] Stage D.D.5 `<read_first>` reference: change `M05.C-retrospective.md` → `M05.C1-retrospective.md` + `M05.C2-retrospective.md` (the C stage split into C1 + C2).
- [ ] Stage D.D.3.3 enforcer-edit example: expand to include the `current_tier: Tier` field declaration + `set_tier(&mut self)` + `current_tier(&self)` accessor pattern + the Default impl returning Novice.
- [ ] Stage D.D.4 acceptance criteria: note that the `*_twice_in_sequence` persistence test pattern (gotcha #69) is mandatory for any future tier-related persistence work (e.g., M10 Settings panel persistence).
- [ ] Stage D.D.3.4 tier-transition flow: spell out renderer-side-vs-runtime-seam distinction explicitly — "Promotion HITL is renderer-side (Settings panel confirmation modal). Not the runtime HitlSeam — tier transitions are an OS-level user preference, not a framework-JSON-driven trigger."
- [ ] Stage D.D.1 problem statement: pair the runtime-vs-install-time interpretation note with spec §8.security L4 + MVP-v0.1 §M5 phrasings — make the interpretation explicit.

### Open issues to file

- [ ] Maintainer to file (post-M05 `docs(spec):` PR): pair Stage D's spec-text item (L4 runtime vs install-time interpretation) with the prior M04.V + M05.A/B/C1/C2 spec items. Spec §8.security L4 should pin "runtime-time gate" semantics explicitly, with the install-time auto-accept criteria of MVP-v0.1 §M5 as a downstream consumer.
- [ ] Maintainer to consider: CI flag `--test-threads=$(num_cpus/2)` for Windows job to reduce gotcha #56 subprocess-flake pressure. Now confirmed at 6 stages; low priority but graduating from "consider" toward "do".
- [ ] Maintainer to consider: graduate "adding an enum variant breaks existing irrefutable bindings" to gotcha #73 (or successor number — gotcha #73 is already taken by the typify-Eq pattern). Forward-applicable to any schema migration adding variants. Pattern observed M05.D once; watch for recurrence.

### Stage preparation (Stage E — Provenance + Audit Log)

Stage E reads:

- This retrospective's `[END] Decisions` section (mandatory per CLAUDE.md §19 rule 1).
- M05.D code at `crates/runtime-main/src/tier/` — Stage E's audit log records every tier_transition and tier_violation event.
- The Stage D boundary contract Stage E must preserve:
  - **Tier evaluator stateless** — Stage E's audit logger consumes events emitted by the tier system; does not alter the evaluator surface.
  - **`tier_violation` + `tier_transition` schema variants stable** — Stage E adds `tier_change` audit entries to `skills.audit.jsonl` per spec §8.security L5; the schema variants drive the audit entries' shape.
  - **`CapabilityError::TierForbidden` shape stable** — Stage E's audit logger reads this error variant when an SDK consumer catches it.

Estimated Stage E effort: per phase doc; 0.63× calibration → adjust accordingly.

What Stage E adds:

- L5 provenance block enforcement (`provenance` field in generated artifacts)
- `.aria-runtime/skills.audit.jsonl` append-only, hash-chained audit log
- Audit entries for every install / reject / uninstall / capability_violation / tier_violation / tier_transition
- Secret redaction before logging (per spec §8.security L5)

---

## [END] User-review notes

- (Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.)

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M05 Stage D, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m05-gap-capability`; the PR drafts and pushes only at the end of the final stage (Stage G closeout).

**Claude:** I have completed M05 Stage D per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until the final stage.

**Surfaced at:** 2026-05-13
