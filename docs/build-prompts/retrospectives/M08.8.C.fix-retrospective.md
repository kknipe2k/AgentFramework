# M08.8.C.fix — Stage Retrospective

> **Stage:** M08.8 Stage `C.fix` — close the tier UI's DISPLAY half (#19 desync seed + #20 feedback toast) — see `docs/build-prompts/M08.8-design-workbench.md` (Stage C.fix)
> **Parent milestone:** M08.8 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-05
> **Session end:** 2026-06-05
> **Branch:** `claude/m08.8-design-workbench`
> **Starting commit:** `b1226fd` (after C's docs landed — TD-047 logged)
> **Ending commit:** red + impl + this retro — uncommitted, awaiting approval
> **Estimated effort (per prompt):** ~1–2 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules honored (Hard Rule 1 — surfacing before any commit; Hard Rule 11 — the grounded-claims rule is the whole point of this fix)
- [x] All Stage C.fix "Read first" files read (the C.fix prompt + parent C; the C retro's "#19 fixed at the backend" claim; M08.6-irl #19/#20; App.tsx mount seed; ipc.ts; graphStore.ts slots; commands.rs get_current_tier + the idempotent no-op; SettingsPanel.tsx; toastStore.ts; the e2e-tauri reload pattern)
- [x] Deliverable + test plan stated before code
- [x] Branch correct (`claude/m08.8-design-workbench`); `git status` clean of tracked changes at start
- [x] Frontend-only confirmed: no backend / enforcer / SettingsPanel change; the idempotent no-op (commands.rs:717–720) left untouched

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Impl (first vitest run) | The mount seed wrote `undefined` into `currentTier` for every existing App test (their mock resolves `undefined` for `get_current_tier`), crashing AppShell's `titleCase(tier)` — 9 failures + 3 errors. | The `<wire_signature_audit>` pinned `get_current_tier → Tier` (never null), so the happy path was correct; the test-mock's `undefined` exposed a real robustness gap. Fixed in production with a defensive TierRef guard (mirrors `listInstalledArtifacts`' `Array.isArray` coercion), not by editing the existing tests. | 2 |
| Lint | The new e2e file imported chai's `expect` but asserts via `waitUntil` (no `expect`) — eslint no-unused-vars. Removed the import. | A test-file mechanical cleanup; folded into the red commit (test files), so the red→impl test diff stays EMPTY. | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| C retro ("#19 fixed at the backend") vs the running app | Whether #19 was actually closed by C | It was not — C wired enforcement (backend) but the display layer (the renderer seed) was never touched. The grounded-claims rule (Hard Rule 11): enforcement-observed ≠ display-fixed. This fix closes the display half. | Already the premise of the C.fix prompt; logged here per `<special_log>`. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Red phase | The 5 vitest reds + 1 e2e reload test, right-reason evidence (`getCurrentTier`/`setCurrentTier` not a function; store stays 'novice'; no toast) | Yes | Right — before impl |
| Close | The full bundle (gates + the empty red→impl test diff + the re-IRL walkthrough) | Yes | At surface-and-wait per Hard Rule 1 |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §5 strict-TDD byte-identical test block | The mount-seed robustness gap surfaced during impl needed a production guard, NOT a test edit | Yes — fixed in `src/App.tsx`; existing tests untouched; new test files final in the red commit | `git diff <red>..<impl> -- '**/tests/**'` is EMPTY by construction |
| Hard Rule 11 (grounded claims) | The very bug being fixed: C claimed "#19 fixed at the backend" with no display-layer evidence | N/A (prior stage) | This fix's close is the maintainer re-IRL WITH a restart — the only evidence that licenses "#19 resolved" |

---

## [END] Scoring (3 axes per PROCESS-VALIDATION.md)

- **Prompt fidelity:** 5 — frontend-only honored (getCurrentTier wrapper + setCurrentTier action + App-mount seed + listener toast); no backend/enforcer/SettingsPanel change; the idempotent no-op left correct.
- **TDD discipline:** 5 — red first (5 unit + 1 assembled e2e), all right-reason; impl untouching tests; defensive guard added in production not tests.
- **Self-sufficiency:** 5 — the one friction (undefined seed) was diagnosed and fixed from the agent side without escalation.

## [END] Threshold gates

- Behavior-over-tautology: ✅ — the e2e reload test is the assembled-app regression (a reloaded renderer over the same Promoted backend must read "Demote"); on `main` pre-fix it reads "Promote" (the #19 desync).
- Mutation gate: advisory — frontend-only; no enforcer/Rust execution-wiring change (per the C.fix prompt).
- CI-parity: ✅ — prettier/eslint/tsc/vitest (629 passed, 93.47% lines) + npm audit (exit 0) all run with CI-identical flags.

## [END] Outcome

**Sound** (pending the maintainer re-IRL with restart, which is this stage's close gate and also completes parent C — flips the tier execution-status "observed in app" + marks TD-036 resolved).

## [END] Decisions for the next milestone

- The App-mount seed pattern (read backend → guard → seed store) now has two instances (`invokeHasApiKey`, `getCurrentTier`); the budget-cap display has the SAME latent desync (`globalBudgetCap` is process-memory only, M04 — `set_global_budget` has no startup read). The original "Stage D — budget" is superseded by ADR-0031, so the persisted-cap mirror lands at **M11/M13**, not a Stage D: when first-run UX persists the cap, mirror this seed.
- `<special_log>`: C's "#19 fixed at the backend" missed the display layer (Hard Rule 11 — enforcement-observed ≠ display-fixed); the re-IRL with a restart closes C's display half + completes C (execution-status + TD-036); frontend-only → mutation advisory; ADR-0030 stays Proposed (flips at the M08.8 closeout PR); TD-047 (tier-limited Tester signal) is OUT of scope → M10/M13 (ex-Stage F, superseded by ADR-0031).
