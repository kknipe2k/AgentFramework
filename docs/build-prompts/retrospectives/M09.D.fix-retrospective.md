# M09.D.fix — Stage Retrospective

> **Stage:** M09 Stage `D.fix` — see `docs/build-prompts/M09-workbench-vertical-slice.md` (Stage M09.D.fix)
> **Parent milestone:** M09 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m09-prep`
> **Starting commit:** `c610859` (after the merge that pulled `a07dad3` — the D.fix phase-doc section)
> **Ending commit:** `79f6fc2` (red `1927644` → fix `15d4add` [mutation-blocking] → Tester resize `d65bb38` → settings disclosure `aecec03` → mechanical budget-test fixup `79f6fc2`; execution-status flip pending the maintainer re-IRL)
> **Estimated effort (per prompt):** ~2 hours
> **Actual elapsed:** ~2.4 hours

---

## Pre-flight (claimed before session start)

- [x] §4 Hard Rules honored (no commit before approval; no push; no gate skips; rule 11 — the flip needs the maintainer re-IRL).
- [x] All "Read first" files read (D.fix.1–D.fix.6; the M09.C run-path: `test_agent_config` @tester.rs:348 — the tool-list builder; `run_test_session_inner` @523; `try_mcp_dispatch` @agent_sdk.rs:884; the MCP dispatch source `dispatch.rs:141/161`; `McpTool`/`ToolDef` shapes; `capability_live_tool.rs` E-02 stub pattern; TesterModal + Modal + SettingsPanel + DESIGN.md principle 3 / Modals rule).
- [x] Deliverable + test plan stated before any test file.
- [x] User confirmed the red phase before implementation ("M09.D.fix red approved — implement the three distinct commits…").
- [x] Branch correct.

---

## [LIVE] Friction events

| When | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| implement / commit 1 | After moving production to a new `test_framework_with_tools` seam, `test_framework_with` (the delegating wrapper) became dead code (`-D dead-code`: no production caller — its only callers are `#[cfg(test)]`, which don't count in the non-test build). Collapsed the two seams into one — gave `test_framework_with` the `mcp_tool_defs` param directly — and updated its 3 existing in-source test call sites with `Vec::new()` (mechanical). | A note that adding a parallel `*_with_tools` seam beside an existing `*_with` makes the old one dead in a binary crate; extend the existing seam instead. | 1 |
| implement / commit 3 | Making the budget section default-collapsed broke 5 pre-existing budget vitest tests + 1 Playwright test (they drive `budget-cap-input`, now unmounted until disclosed). Updated them to click the disclosure toggle first — a separate labelled `test(M09.D.fix)` fixup (`79f6fc2`) so the impl commit's `tests/**` diff stays EMPTY. | The disclosure red could note "existing budget tests will need an expand-first step — plan the mechanical fixup commit." | 2 |

---

## [LIVE] Ambiguity / discovery events

| When | What | How resolved |
|---|---|---|
| ground_at_red / the root cause | The phase doc named ONE condition (the tool DEFINITION never injected). Reading the run path confirmed it (`test_agent_config` builds the list from `builtin_tool_defs(&allowed)` only — MCP names dropped) AND surfaced a **SECOND, undocumented condition**: a canvas-authored framework sets **no `mcp_aliases`** (grep-confirmed across `src/`), and `framework_mcp_servers` derived the servers-to-connect **only** from `mcp_aliases` — so the authored server **never connects**, the resolver stays empty, and dispatch + the injected def are **inert**. The M09.5 pattern: the assembled regression (which threads defs + a stub dispatch directly) does NOT exercise the connect path, so it could not catch this — only reading the real `test_framework`→`framework_mcp_servers`→`on_server_connected` chain did. | Fixed BOTH in the execution-wiring commit (commit 1): inject the def (condition 1) AND derive the connect-servers from the agents' canonical `allowed_tools` names (condition 2). Added an additive in-source unit test pinning `framework_mcp_servers`. Surfaced the second condition prominently so the maintainer re-IRL validates the full real path (rule 11 — not shipping a known-inert injection). |

---

## [LIVE] Protocol drift events

None. Red (`1927644`) committed only after explicit approval. The three impl commits (`15d4add`, `d65bb38`, `aecec03`) touch **no `tests/`** path — `git diff 1927644..aecec03 -- 'tests/'` is EMPTY, and the three red-test files are byte-identical `1927644..HEAD`. The mechanical existing-test updates (budget expand-first; the 3 `test_framework_with` call sites) ride in separate labelled commits / are in-source (`src-tauri/src`, not `tests/`). The execution-status flip is **not** committed on CI-green (rule 11) — it awaits the maintainer re-IRL.

---

## [LIVE] Surprise events

| What was surprising | Why | Implication |
|---|---|---|
| The IRL bug was TWO conditions, not one — and the phase-doc diagnosis (and its "dispatch was wired" Background claim) named only the first. | The author reasonably inferred from M09.C that the dispatcher was wired; but "the dispatcher is constructed" ≠ "the server is connected" — `connect_test_session_mcp` connects `framework_mcp_servers(...)`, which was empty for a canvas framework. structure ≠ behavior, one layer deeper (rule 11). | Reinforces the v1.8 assembled-mandate + the M09.5 lesson: the assembled test must drive the REAL composition, and where it cannot (here: the connect path needs a real registry), the maintainer re-IRL is the irreplaceable close. The injection-only fix would have shipped green-and-still-broken. |

---

## [LIVE] Stage 0 real-app discovery routing

| Disposition | Evidence / regression | Adjacent findings (triaged in-place) |
|---|---|---|
| **assembled regression + additive unit** | `crates/runtime-main/tests/mcp_tool_injection_execution.rs` (the tool reaches the model's list + the end-to-end dispatch→Write lands the file + the causality guard) + the in-source `framework_mcp_servers_derives_servers_from_canvas_authored_allowed_tools` unit | The verdict-reads-PASS-on-gap-suspend truthfulness gap the IRL also showed is **out of scope** (scope-lock) → noted for tech-debt / M10; verdict logic untouched. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | CLAUDE.md orientation? | 5 | The run-path read landed both conditions. |
| 2 | Read-first oriented? | 5 | E-02 stub pattern + the dispatch chain were load-bearing. |
| 3 | Deliverable + plan first? | 5 | Stated before code. |
| 4 | Self-correct ≤3 rounds? | 5 | Two mechanical rounds (dead-code seam collapse; budget-test expand-first), each first-try. |
| 5 | Do-not-commit held? | 5 | Red after approval; flip held for re-IRL; no push. |
| 6 | Escalate/proceed split? | 4 | Owned the second-condition fix (technical-completeness, not a user fork) rather than round-tripping — surfaced transparently. Borderline; logged for the maintainer's call. |
| 7 | Human-direction load? | 5 | One approval at red. |
| 8 | Surfaces actionable? | 5 | The red surface drove a one-line approval. |
| **Process total** | | **39/40** | |

### Axis 2: Product

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | §9 style + naming? | 5 | `run_test_session_with_tools` / `build_session_mcp_tool_defs` named for what they are; comments explain *why* (the IRL conditions). |
| 2 | Behavior tests? | 5 | Assembled side-effect (file on disk + captured model tool list) + the causality guard + the connect-derivation unit. |
| 3 | Public APIs documented? | 5 | The new seam + helper carry doc comments + `# Errors`. |
| 4 | Exactly promised (+ the needed second condition)? | 5 | Injection + connect-derivation; no new transport / dispatch change / enforcer-or-tier change / schema change. |
| 5 | Stranger-understandable? | 5 | The regression header narrates the IRL, the two conditions, and why no prior test caught them. |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no ad-hoc allows (the dead-code seam was removed, not `#[allow]`-ed); best-effort list_tools degrade is logged. |
| 7 | Coverage ≥ gates? | 5 | runtime-main 95.03% (tester.rs 95.52%); runtime-mcp 96.00%; workspace 91.88%; vitest 93.45%. |
| 8 | CI green across matrix? | 4 | All Rust + frontend gates + both e2e legs green on Windows (real-app 17/17 spec files); Linux/macOS in CI. |
| **Product total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence |
|---|---|---|---|
| 1 | Template sections useful? | 5 | The falsifiable-hypothesis framing led straight to reading the run path, which exposed the second condition. |
| 2 | Sections missing? | 4 | A "verify the connect path, not just the inject path" prompt nudge would have pre-named the second condition. |
| 3 | Gotchas generalize? | 4 | "A parallel `*_with_tools` seam makes the existing `*_with` dead in a binary crate — extend, don't parallel" is reusable. |
| 4 | Time-box within 2×? | 5 | ~2.4h vs ~2h. |
| 5 | Implicit protocol moments? | 5 | The red/3-impl/2-fixup commit shape + the EMPTY tests/ diff proof were explicit. |
| 6 | Re-runnable by a fresh session? | 5 | The assembled regression + the unit are key-independent, run in CI. |
| 7 | Copyable forward? | 5 | A canvas-authored MCP tool now connects + is injected + dispatches — M10+ inherits a working MCP slice. |
| 8 | Assembled test earned its keep? | 4 | It proved the injection end-to-end; the SECOND condition it could NOT reach (connect path) is why the maintainer re-IRL remains the authoritative close — the honest limit, logged. |
| **Pattern total** | | **37/40** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved** — red `1927644` after approval; impl + fixups per the same instruction; the flip held for the re-IRL. No push.
- [x] **G2: no Severity-5 friction** — max 2.
- [x] **G3: no unaddressed protocol drift** — `git diff 1927644..aecec03 -- 'tests/'` EMPTY; the three red-test files byte-identical red→HEAD.
- [~] **G4: stage completed — superseded by iteration 2.** The iter-1 re-IRL **closed the original injection 🔴** (the MCP tool was injected, called, dispatch reached) but **exposed a deeper regression**: the MCP dispatcher's own `CapabilityEnforcer` was never framework-/tier-wired, denying MCP Exec at Novice though the user was Promoted. No flip at iter-1; reopened as **M09.D.fix iteration 2** (`build_session_mcp_enforcer`). The slice closed at the **iter-2 re-IRL (2026-06-09, Promoted)** — see `M09.D.fix2-retrospective.md`.
- [x] **G5: every row ≥3** — lowest individual is 4.
- [x] **G6: CI-parity** — commands match CI; no divergent flags:
  - `cargo fmt --all --check` ✓ · `cargo clippy --workspace --all-targets -- -D warnings` ✓
  - `cargo test --workspace` ✓ (0 failed) · `cargo doc -D missing_docs` ✓ · `cargo audit` ✓ · `cargo deny check` ✓ (advisories/bans/licenses/sources ok)
  - `cargo llvm-cov -p runtime-main … --fail-under-lines 95` → 95.03% ✓ · `-p runtime-mcp --features test-helpers … 95` → 96.00% ✓ · `--workspace … 80` → 91.88% ✓ (clean run, gotcha #81)
  - `npx prettier --check` ✓ · `npx eslint .` ✓ · `npx tsc --noEmit` ✓ · `npm run test` → 670/670, 93.45% ✓ · `npm audit --audit-level=high` → 0 high/critical ✓
  - `npm run test:e2e` (Playwright) → 59/59 ✓ · `npm run test:e2e:tauri` (rebuilt binary) → **17/17 spec files**, incl. the new `tester_resize.e2e.ts` + `vertical_slice.e2e.ts` (2/2) ✓
  - **Mutation gate (BLOCKING, injection diff):** `cargo mutants -p runtime-main --in-diff <tester.rs injection>` → first run **2 missed** (`&&`→`||` :368, `==`→`!=` :367 — the "advertise every connected tool" hole); added the authored-only additive test (`1a8e0af`); re-run → **9 mutants: 4 caught, 5 unviable, 0 missed** ✓ (blocking gate green)

### Soft gates

- [x] **S1:** Process ≥30/40 — 39
- [x] **S2:** Product ≥32/40 — 39
- [x] **S3:** Pattern ≥25/40 — 37
- [x] **S4:** Time-box within 2× — ~2.4h vs ~2h
- [x] **S5:** ≤3 Severity-3 events — 0

### Outcome

- [~] **Sound work, but the iter-1 re-IRL surfaced a deeper gap → iteration 2.** All soft gates passed and the injection 🔴 closed, but the re-IRL exposed the MCP dispatcher's bare enforcer (denying MCP Exec at Novice though the user was Promoted). Reopened as **M09.D.fix iteration 2**, which wired `build_session_mcp_enforcer` and closed the slice at the **2026-06-09 re-IRL**. The execution-status flip + the MCP-dispatch row correction landed there (not at iter-1).

---

## [END] Coverage holdouts

No `--ignore-filename-regex` change. The injection landed in `runtime-main/src/builder/tester.rs` (95.52% lines, inside the ≥95 gate) + `src-tauri/src/commands.rs` (`framework_mcp_servers`/`test_framework_with` covered by the in-source unit + the existing tester_backend tests; `build_session_mcp_tool_defs` is a thin `list_tools` wrapper under the `src-tauri/**` tauri-shell 50% patch gate, the `mcp_list_server_tools` precedent). No new exclusion; no §C entry required.

---

## [END] Decisions for the next stage (M09.V → M09.G)

- [ ] **Execution-status reconcile (M09.G).** The ledger's "MCP dispatch executes" claim was not true through the assembled *model* path (the model was never offered the tool). On the re-IRL green, reconcile that row to what is proven and flip the canvas-authored-slice row (cite both evals + the re-IRL date).
- [ ] **Tech-debt (M10).** The verdict reads PASS on a gap-suspend (the model asking for a missing capability) — the truthfulness gap the IRL also showed; out of D.fix scope, log it.
- [ ] **Gotcha candidate.** A parallel `*_with_tools` seam beside an existing `*_with` makes the old one dead in a binary crate — extend the existing seam.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M09 Stage `D.fix` through the green checkpoint. The red was approved before commit; the three impl commits touch no `tests/` path (`git diff 1927644..aecec03 -- 'tests/'` EMPTY; the three red-test files byte-identical red→HEAD); the mechanical existing-test updates are separate labelled / in-source. The assembled regression proves the canvas-authored MCP tool reaches the model's tool list and drives a real Write from real MCP data; I also found and fixed a **second, undocumented condition** (the authored server never connected, because `framework_mcp_servers` read only `mcp_aliases`, which the canvas never sets) — without it the injection would have been inert, so the re-IRL would still have failed. All Rust + frontend gates + both e2e legs are green (real-app 17/17 spec files). The **execution-status flip is NOT committed** — per rule 11 it awaits the maintainer real-app re-IRL in the resizable Tester (a real file written within scope from real MCP data, denied out-of-scope). I will not commit the flip, push, or open a PR until directed.

**Surfaced at:** 2026-06-09 (local). **Maintainer re-IRL + execution-status flip:** pending. **Mutation gate (blocking):** result filled at the surface.
