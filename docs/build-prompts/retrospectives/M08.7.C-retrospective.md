# M08.7.C — Stage Retrospective

> **Stage:** M08.7 Stage `C` (rung 3 — skill load / `LoadSkill`) — see `docs/build-prompts/M08.7-execution-engine.md`
> **Parent milestone:** M08.7 (Execution Engine) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-31
> **Session end:** 2026-05-31
> **Branch:** `claude/m08.7-execution-engine`
> **Starting commit:** `192b385`
> **Ending commits:** `9750f3f` (red + ADR-0027 Proposed) → `1f701c0` (impl) → `7e4c038` (const-fn test follow-up) → `15ff803` (assembled denial follow-up) → `2fd55a2` (retro) → `<live-eval + TD-037 + retro-update>` (rung-3 completion after the maintainer's close-gate correction) — pushed only on approval
> **Estimated effort (per prompt):** 4–6 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in scope (esp. §4 rule 11 grounded-claims, §2 surface-contradictions, §8 commit workflow, §11 ADR triggers, §14 schemas-as-source)
- [x] All "Read first" files read (CLAUDE.md, cluster-pattern.md, M08.7 phase doc Background/Stage A/Stage C C.1–C.6, M08.7.B retro, spec §0b/§4b, harness-review Item 2, ADR-0000 template + ADR-0022, retro template) + the 7 read_reference source files
- [x] Deliverable + test plan stated before code; C.3.0 ground-at-red findings surfaced before tests (the first checkpoint)
- [x] User confirmed (ground-at-red injection decision approved via AskUserQuestion — tool-result; red approved)
- [x] Branch correct (`claude/m08.7-execution-engine`, rungs 1+2 present)
- [x] `git status` clean except pre-existing untracked `.claude/*`, `rung2/`

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| ground_at_red | The phase doc's central premise — "read the resolved skill body [from the Framework]; the loaded Framework is in scope at `run_test_session_with`" — is **factually incomplete**: `FrameworkSkillsItem` is reference-only (`{name, path?, registry_id?, source}`, no body), and per ADR-0022 the resolved body lives in a **separate** `LoadedFramework.companions[]`, which the run path (`run_test_session_with(&Framework)` → `CapabilityWiring.framework: Arc<Framework>` → `drive_stream`) **drops** before the Tester runs. ground-at-red caught it (its purpose) and reshaped the wire (thread a `resolved_skills` map into `CapabilityWiring`). | C.3.0 could state "the resolved body is in `LoadedFramework.companions`, NOT on `Framework`; the run path carries only `Framework`, so rung 3 must thread the body in." The construction_reachability_check's "loaded Framework is in scope" was the right backstop but its phrasing implied the body was on `Framework`. | 3 |
| ground_at_red | Spec §0b ("**Tool result returns the skill body**") **contradicts** the phase-doc ADR-0027 sketch ("system_prompt-section append; reject tool-result"). The sketch's rejection rationale ("a tool result vanishes after the turn") is factually wrong here — `run_agent` re-sends accumulated `config.messages` every turn. Surfaced per CLAUDE.md §2 (do not pick); maintainer selected the spec-compliant tool-result model. | The phase-doc ADR-0027 sketch should have been checked against spec §0b before authoring; the C.3.3 sketch asserted a system_prompt model the spec contradicts. The adr_trigger correctly anticipated "surface the contradiction." | 2 |
| green / clippy | `clippy::missing_const_for_fn` flagged the test stub's `SkillScriptStub::new` — a lint that **could not surface during red** (the test file did not compile until the production handler + seam existed, E0432). The fix is a test-file edit, which the strict-TDD impl-commit invariant forbids; landed as a separate labelled `style(M08.7.C)` follow-up (v1.8 convention). | A note that lints on test code depending on not-yet-existing production symbols cannot surface in red and land as a mechanical follow-up. Generalizes to every strict-TDD stage whose red test file references new production symbols. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc ("read the resolved body from the Framework") vs `generated/framework.rs` (`FrameworkSkillsItem` reference-only) vs ADR-0022 (body in `companions`) vs `tester.rs` (`run_test_session_with(&Framework)` — no companions) | Whether the resolved skill body is reachable from `drive_stream` | Threaded a `resolved_skills: BTreeMap<String,String>` onto `CapabilityWiring` (additive `with_resolved_skills` builder) + a `run_test_session_with_skills` seam. The body is resolved once at load (ADR-0022); this threads it in — no re-resolution. | Resolved in-cluster; recorded in ADR-0027 + this retro. Phase doc for rungs 4/5 should state which struct carries a primitive's data into the run path. |
| Spec §0b (tool-result) vs phase-doc ADR-0027 sketch (system_prompt) | Where the skill body lands + how it persists | Surfaced as a labelled decision (AskUserQuestion); maintainer chose tool-result (spec). ADR-0027 records system_prompt/three-tier as the forward path. | Resolved in-cluster; ADR-0027 Decision + Alternatives. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After ground_at_red | The 3 findings (reachability premise incomplete; load policy tool-invoked confirmed; injection-model spec↔doc contradiction) + cross-machine state + the green plan + **one maintainer-domain decision** (injection model: tool-result vs system_prompt, via AskUserQuestion with side-by-side previews) | Yes — maintainer selected tool-result (the recommended option) | Yes — before any test written, per ground_at_red's "surface before writing tests" |
| After red commit | Right-reason red (E0432 unresolved imports — the missing handler + seam), the 7 pinned scenarios, ADR-0027 Proposed summary, the green plan, the strict-TDD commitment | Yes — maintainer approved ("proceed to red") | Yes |

---

## [LIVE] Protocol drift events

None observed. Do-not-commit-without-approval held: the ground-at-red decision was approved (AskUserQuestion) before tests; red committed only after "approved"; impl committed under the red approval (proceed-to-green); no push (awaiting final approval). Strict-TDD two-commit held — `git diff 9750f3f..1f701c0 -- 'crates/runtime-main/tests/'` is **EMPTY**; the const-fn lint fix + the additive denial test landed as **separate labelled follow-up commits** (v1.8 convention), not folded into impl. No schema change; sandbox untouched (Hard Rule 8 / scope lock). Spec-vs-doc contradiction surfaced, not picked (§2).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The resolved skill body is **not reachable from the run loop** — `LoadedFramework.companions` is dropped before `run_test_session_with(&Framework)`, so the Tester (and by extension `drive_stream`) never sees skill bodies. | ADR-0022 resolves bodies into `companions`, but no consumer threaded `companions` into the run path; the run path was `Framework`-only since M08. | The M08.7 thesis confirmed a **third** time (after rung-1 grant-loading + rung-2 tier-wiring): scaffolding built around an execution core that was never fully wired — here, resolved data that never reached the executor. Each rung surfaces a "resolved/built but not threaded" gap. |
| The real-app **Builder-Tester** skill IRL is **not v0.1-runnable** — surfaced at the close gate. The Tauri `test_framework` command receives a bare `Framework` (no companions); the renderer holds companions only for save, and the v0.1 canvas **authors no skill bodies at all** (`ipc.ts:504` — "M09's Generators will"). So there is no skill body in renderer state to thread. | I had named the Builder-Tester real-app IRL as the close gate in the prior final surface — but the production `companions → resolved_skills` join can't be fed in v0.1. The maintainer caught it and directed: wire it, or STOP and surface if walled deeper than expected. It is walled deep (canvas authors no bodies — M09). | Wiring only the backend join would be a dead path (renderer always sends `[]`) — the exact paint-not-execute anti-pattern (rule 11). Re-tiered to the canvas-skill-body rung (TD-037, zero-propagation like rung-2's Finding B / TD-036); rung-3's behavior close rests on the `#[ignore]`d live eval (`skill_load_live.rs`, real Anthropic) instead of the Builder UI. |
| 13 of 18 mutants were **unviable**. | The cluster's value-returning fns (`load_skill_tool_def → ToolDef`, `load_skill → LoadedSkill`, the dispatch returning `DispatchedTool`) have non-`Default` return types, so cargo-mutants' body-replacement-with-`Default` mutants don't compile; the `json!`-macro error arms aren't mutated (macro bodies). The 5 viable mutants (the `allowed_skills` membership gate + the `==`/closure logic) were all **caught**. | A handler built from value constructors + macro formatting yields few viable mutants; the gate's signal concentrates on the gate/branch logic (which is exactly what bit). Consistent with M08.7.B's "few-viable-mutant" note. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test (the assembled integration test IS the discovery) + the real-app IRL **required as the close gate** | `crates/runtime-main/tests/skill_load_execution.rs` — drives the REAL `run_test_session_with_skills → run_agent → drive_stream` loop (only the provider stubbed); asserts `SkillLoaded(shout)` + LoadSkill advertised + the skill body present in the turn-2 `AgentConfig` (structural injection-into-context) + additive composition + the assembled denial path | Reachability finding → fixed in-cluster (`CapabilityWiring.resolved_skills` + the `run_test_session_with_skills` seam — threading already-resolved data, ADR-0022 honored). Injection-model contradiction → ADR-0027 (the governing decision). The dispatch denial arms → covered by the additive assembled denial test (rule 11 — assembled path run). |

This is a backend stage with an **assembled Rust integration** surface. The cluster's CI close is the **assembled test observed** (7 tests green against the real loop, the skill body observed in the turn-2 config). Per rule 11 the **behavioral** close — a real Anthropic model loading a "reply in ALL CAPS" skill and replying in all caps — is the **IRL gate, maintainer-run**, and is **NOT exercised by CI** (the `ScriptedToolStub` is not the model). Stated honestly: CI proves injection-into-context; the IRL proves behavior-change.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4 rule 11 + §2 (surface contradictions) + §5 + §6 + §8 + §11 directly drove the work; §2 was load-bearing for the injection-model surface |
| 2 | "Read first" list oriented correctly? | 5 | spec §0b + ADR-0022 + the generated framework model were exactly what ground-at-red needed to find the reachability + injection-model issues |
| 3 | Deliverable + test plan before code? | 5 | C.3.0 findings + the green plan + the BDD-to-test mapping surfaced before any test; the injection decision was a labelled fork |
| 4 | Self-correct in ≤3 rounds? | 5 | Gates green effectively first-pass; one mechanical clippy const-fn (separate follow-up); no self-correction loop |
| 5 | Do-not-commit-until-approved held? | 5 | ground decision → red → (proceed-to-green) impl; no push |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the genuine spec↔doc injection-model fork (AskUserQuestion); proceeded on the reachability wire + API design (the `resolved_skills` threading, the seam) as §12 technical calls |
| 7 | Human-direction load reasonable? | 5 | 2 checkpoints (ground-at-red decision, red) + the pending final — appropriate; the one AskUserQuestion was a real spec contradiction |
| 8 | Surfaces actionable? | 5 | Each surface carried findings + a recommendation; the fork carried side-by-side previews + a recommended option |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case; the `with_*` additive-builder convention; the `*_with_skills` seam mirrors `*_with_tier`; doc-commented; the `#[allow(too_many_arguments)]` follows codebase precedent |
| 2 | Behavior tests not tautology (§5)? | 5 | Assertions on the skill body present in the real turn-2 `AgentConfig` + no-SkillLoaded-on-denial + no-forbidden-body-injected, never an event alone; mutation gate 0-missed confirms the tests bite |
| 3 | Public APIs documented + compile-checked? | 5 | `load_skill`, `LoadedSkill`, `LoadSkillError`, `load_skill_tool_def`, `with_resolved_skills`, `run_test_session_with_skills` fully doc'd (incl. `# Errors`); `cargo doc -D missing_docs` green |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | The handler + the drive_stream branch + the threading wire + the seam + the advertisement + ADR-0027; the additive denial test is rule-11 coverage of an execution-wiring branch, not creep; sandbox untouched |
| 5 | Stranger-understandable? | 5 | ADR-0027 + the test-file module doc explain the tool-result model, the reachability threading, and the CI-structural-vs-IRL-behavioral split |
| 6 | §9 anti-patterns absent? | 5 | No unsafe, no stringly-typed beyond the JSON tool I/O (the wire format), no silent drops; the one `#[allow]` is documented + precedented |
| 7 | Coverage measured? | 5 | runtime-main 95.23% line ≥95 (measured, exit 0); `load_skill.rs` 100% |
| 8 | CI green across the matrix? | 3 | Predicted green; only Windows-local run performed (CI Linux/macOS pending the PR run) — stated, not assumed |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | ground_at_red + close_gate + strict-TDD + adr_triggers all earned their keep; adr_trigger #2 framing drove the injection-model surface |
| 2 | Sections missing? | 4 | The phase doc's "read the resolved body" premise (companions vs Framework) could be a ground-at-red sub-step: "confirm which struct carries the primitive's data into the run path before assuming reachability" |
| 3 | Gotchas local or generalize? | 4 | "a strict-TDD red test file referencing new production symbols cannot surface clippy lints until green; the lint fix is a labelled follow-up" generalizes to every strict-TDD code stage |
| 4 | Time-box matched? | 5 | Within the 4–6 h range; the greenfield handler + the reachability wire as predicted |
| 5 | Implicit protocol moments? | 4 | The "phase-doc premise references a struct that doesn't carry the data; ground-at-red reshapes the wire" pattern (second instance after M08.7.B's `hitl_seam=None`) is worth codifying for rungs 4/5 |
| 6 | Re-runnable by a fresh session? | 5 | The ground findings + ADR-0027 + the cited surfaces + the decision make it reproducible |
| 7 | Anything to change before copying format? | 4 | Add to ground_at_red: "trace the primitive's data from its loader-resolution site to the run-loop call site — confirm it is threaded, not dropped" |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — ground decision (AskUserQuestion) → red after "approved" → impl under proceed-to-green; **no push**. `git log` shows `9750f3f`, `1f701c0`, `7e4c038`, `<denial>` ahead of main.
- [x] **G2: no Severity-5 friction** — max severity 3.
- [x] **G3: no unaddressed protocol drift** — none observed.
- [x] **G4: stage completed** — impl `1f701c0`; all gates pass; 7 assembled+unit tests green against the real loop.
- [x] **G5: every row ≥3** — lowest is 3 (CI-matrix row: Windows-local only, CI pending; honest per rule 11).
- [x] **G6: CI-parity** — gate commands run verbatim per §6 (no CI-divergent flags). Mutation gate via `cargo mutants --in-place --in-diff` (gotcha #92 Windows workaround); 18 mutants, 5 caught, 13 unviable, **0 missed**.

### Soft gates

- [x] **S1:** Process 40/40 ≥30
- [x] **S2:** Product 38/40 ≥32
- [x] **S3:** Pattern 31/35 ≥25
- [x] **S4:** Time-box within 2×
- [x] **S5:** ≤3 Severity-3 — one (the reachability finding); zero higher

### Outcome

- [x] **Pattern is sound.** All hard + soft gates pass. **BUT** per the cluster-gate (`cluster-pattern.md` §1 step 4) the cluster does **not** close on tests-green. The **CI assembled test is observed** (the skill body present in the turn-2 `AgentConfig`); the **behavioral IRL gate** (real Anthropic replies in ALL CAPS) is **maintainer-run and REQUIRED** to close — stated, not claimed as done by CI.

---

## [END] Coverage holdouts

### Per-package gate (runtime-main ≥95%)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 95.23% line (TOTAL), exit 0
- **Exclusions added this stage:** None — `load_skill.rs` is 100% (pure, unit + assembled tested); the `agent_sdk.rs` `dispatch_load_skill` denial arms are exercised by the additive assembled denial test. No new OS-call holdout.
- **Exclusion list as of stage end:** unchanged — `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs`

### CI workflow drift check

- [x] No coverage-config change this stage (no exclusion/threshold change) → the four mirrors (`codecov.yml`, CLAUDE.md §5/§6, `docs/coverage-policy.md`) are untouched and consistent. No `docs/coverage-policy.md` §C entry required.

---

## [END] Decisions for the next stage

### Milestone document updates (M08.7 phase doc — rungs 4/5 authoring)
- [ ] Add to ground_at_red: "trace the primitive's data from its loader-resolution site to the run-loop call site — confirm it is **threaded**, not dropped. M08.7.C found skill bodies resolved into `LoadedFramework.companions` but never threaded past `run_test_session_with(&Framework)`."
- [ ] Rung 4 (`request_capability`) shares the `RequestCapabilityInvocation` parse-from-`ToolUse` pattern; note that the drive_stream interception branch mirrors the rung-3 LoadSkill branch placement (after built-in + MCP, before `pipeline.next_event`).

### Protocol additions
- [ ] cluster-pattern / STAGE-PROMPT-PROTOCOL: note that a strict-TDD red test file referencing not-yet-existing production symbols **cannot surface clippy lints until green**; the mechanical lint fix lands as a labelled follow-up (the M08.7.C `style(M08.7.C)` const-fn precedent), keeping the impl-commit red→impl test diff EMPTY.

### Open issues filed
- None new. (The production wiring of `companions → resolved_skills` in the Tauri-shell Tester wrapper is the documented join in ADR-0027 / the seam doc — a production-path follow-on tracked with the M08.7b live-graph + TD-034 surfacing work, not a new TD.)

---

## [END] Special log (per `<retrospective_requirements>`)

- **C.3.0 ground findings.** (1) **Resolved-body reachability:** `FrameworkSkillsItem` is reference-only; ADR-0022 resolves skill bodies into `LoadedFramework.companions`, NOT `Framework.skills`; the run path carries only `Framework`, so the body was unreachable from `drive_stream`. Fixed by threading a `resolved_skills` map onto `CapabilityWiring` (already-resolved — no re-resolution). (2) **Load policy:** tool-invoked confirmed (spec §0b — LoadSkill auto-injected; input `{skill_name, reason}`; gate = `allowed_skills`); adr_trigger #2 (auto-load contradiction) did NOT fire. (3) **Injection target:** the spec↔phase-doc contradiction (below).
- **ADR-0027 decision.** Tool-result injection (spec §0b): the LoadSkill `tool_result` returns the skill body, persisting in `config.messages` across turns (the rung-1 feedback contract). Resolved-body threaded via `CapabilityWiring.resolved_skills`. Composition additive. Forward path: the three-tier (stable/context/volatile) system-prompt assembly (harness-review Item 2) — loaded skills migrate to the stable tier when that lands; a future ADR supersedes. The maintainer selected tool-result over the phase-doc system_prompt sketch (whose "vanishes across turns" rationale is factually wrong here).
- **CI-structural vs IRL-behavioral split (rule 11).** The CI assembled test proves **injection-into-context** — the skill body is present in the turn-2 `AgentConfig` the loop re-sent (observed on the real config, not from the `SkillLoaded` event). It does **NOT** prove behavior-change: the `ScriptedToolStub` is not the model. The **behavioral** assertion — a real Anthropic model loading the "ALL CAPS" skill and replying in all caps — is the **IRL gate** (maintainer-run, required to close).
- **Mutation-gate result.** `cargo mutants --in-place --in-diff` on the cluster diff: 18 mutants, **5 caught, 13 unviable, 0 missed**. The viable mutants were the `allowed_skills` membership gate + the `==`/closure logic (all caught); the unviable ones are value-constructor body replacements (`ToolDef`/`LoadedSkill`/`DispatchedTool` are not `Default`) + `json!`-macro arms (not mutated). No surviving viable mutant on the cluster's execution-wiring logic → gate passes.
- **The behavior close + the re-tier (the maintainer's close-gate correction).** The prior final surface named the **Builder-Tester real-app IRL** as the close gate. That was wrong: the production `companions → resolved_skills` join cannot be fed in v0.1 — `test_framework` gets a bare `Framework` (no companions) and the v0.1 canvas authors no skill bodies (`ipc.ts:504`). Wiring only the backend join would be a dead path (renderer sends `[]`) — paint-not-execute (rule 11). Re-tiered to TD-037 (canvas-skill-body rung; M09 Generators + the IPC wire + an `e2e-tauri` regression). **Rung-3's behavior close is the `#[ignore]`d live eval** `skill_load_live.rs` (real `AnthropicProvider`, gated on `ANTHROPIC_API_KEY`): a real model loads the "ALL CAPS" skill via `LoadSkill` and the reply is all-caps — the encoded behavior-change eval, run through the real `run_test_session_with_skills → run_agent → drive_stream → load_skill` path (just not the Builder UI).
- **What was NOT exercised.** (a) **The live eval has not been run by me** — it needs `ANTHROPIC_API_KEY` (CLAUDE.md §10; I have no key in-session). It is **maintainer-run**; its assertion is encoded but the all-caps behavior is **unverified until the maintainer runs it**. (b) The in-Builder skill IRL (re-tiered — TD-037). (c) In-app agent-output surfacing (TD-034). (d) CI Linux/macOS (Windows-local only, PR-pending). Per rule 11: CI proves injection-into-context; the live eval (maintainer-run) is the behavior gate — I do NOT claim the behavior observed.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M08.7.C (rung 3 — skill load) per the stage prompt. The `LoadSkill` handler reads the already-resolved skill body (ADR-0022 — no re-resolution) under the `allowed_skills` gate, emits `SkillLoaded`, and injects the body as the LoadSkill `tool_result` (ADR-0027 — spec §0b tool-result model, maintainer-selected over the phase-doc system_prompt sketch). ground-at-red found the resolved body was not reachable from the run loop (`companions` dropped before the Tester); fixed in-cluster by threading a `resolved_skills` map onto `CapabilityWiring` + a `run_test_session_with_skills` seam. All machine gates pass (Windows-local, CI-parity commands): clippy 0, workspace test 0, doc 0, rustdoc 0, audit (17 pre-existing allowed warnings), deny ok, runtime-main coverage 95.23% ≥95. Mutation gate: 18 mutants, 5 caught, 13 unviable, **0 missed**.

**Per rule 11 / the cluster-gate: the cluster does NOT close on tests-green.** The CI assembled test proves injection-into-context (the skill body in the turn-2 `AgentConfig`). The **behavioral gate** is the `#[ignore]`d live eval (`skill_load_live.rs`, real Anthropic) — **maintainer-run** (`ANTHROPIC_API_KEY` required; I have no key in-session, so the all-caps behavior is **encoded but unverified by me**). The Builder-Tester real-app IRL I named in the prior surface is **NOT v0.1-runnable** (the canvas authors no skill bodies — `ipc.ts:504`) and is **re-tiered to TD-037** (zero-propagation), per your "STOP and surface if walled deeper than expected; we'll re-tier" directive. Strict-TDD held — `git diff 9750f3f..1f701c0 -- crates/runtime-main/tests/` is EMPTY; the const-fn lint fix + the additive assembled denial + the live eval are separate labelled follow-ups. ADR-0027 filed Proposed (flips Accepted in the M08.7 PR). I have committed (red → impl → const-fn → denial → retro → live-eval/TD-037/retro-update) but **will not push until you approve**.

**Surfaced at:** 2026-05-31
