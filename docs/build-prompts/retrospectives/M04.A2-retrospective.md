# M04.A2 — Stage Retrospective

> **Stage:** M04 Stage A2 — see `docs/build-prompts/M04-plan-verify-hitl-budget.md` §A2
> **Parent milestone:** M04 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-08
> **Session end:** 2026-05-08
> **Branch:** `claude/m04-plan-verify-hitl-budget`
> **Starting commit:** `c5fe035`
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 4.5 hours
> **Actual elapsed:** ~3 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md → STAGE-PROMPT-PROTOCOL.md → M04-plan-verify-hitl-budget.md A2 sections → spec/gotchas/M04.A1 retrospective). Spec §1c, §1d, §2b, §2c.3, §13.5 read in chunks because the file exceeds the Read tool's 25k-token limit.
- [x] The deliverable was stated in 1–3 sentences and the test plan in 3–5 bullets BEFORE writing code (Hard Rules + deliverable + 5-bullet test plan, then surfaced scope mismatch before any code)
- [x] User confirmed the deliverable + test plan before code began — explicitly via two `AskUserQuestion`-mediated decisions (revised A2 scope; full CmdError migration)
- [x] The branch was correct (`claude/m04-plan-verify-hitl-budget`)
- [x] No uncommitted changes carried over (`git status` was clean at session start)
- [⚠] Pre-flight `Test-Path env:ANTHROPIC_API_KEY` failed (UNSET on this session). Documented in surface text as a soft fail; wiremock tests use a synthetic `sk-ant-test` placeholder, live `anthropic_smoke.rs` skipped via `#[ignore]`. Not blocking.

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Phase doc read (pre-implementation) | Phase doc A2.1 #2 + A2.3 referenced integration points that do not exist in the codebase: (a) no `WriteSignal` IPC command in `runtime_core::DroneCommand`; (b) no `prompt_template.rs` / centralized system-prompt builder; (c) no `event_translation.rs` (translation lives in `event_pipeline.rs`); (d) no `src-tauri/src/lib.rs` (Tauri builder lives in `main.rs`). Phase doc was authored before A1 and didn't fully reflect the codebase shape. Surfaced to user with concrete file evidence; user approved scope reduction (option 1 of 3). | The M04 phase doc inventory rows (`docs/build-prompts/M04-plan-verify-hitl-budget.md` §A2.2) should be revised pre-Stage-A3 to match codebase reality: drop references to fictional files, name the actual integration points, and split the deferred items (WriteSignal IPC + structured-emitter prompt template) into a new sub-stage entry. | 4 |
| Phase 1 (cmd_error_ext + tests) | Generated `runtime_core::generated::error::CmdError` is shape-different from the M02 hand-rolled version: tuple variants over `ErrorMessage` newtype (with `minLength: 1` validation) vs struct variants over `String` with `thiserror::Error` derive. The ergonomic migration required: helper constructors, `Display` + `std::error::Error` impls in a separate module (orphan rule satisfied because both types are local to `runtime-core`), `From<KeyStoreError>` placement in `runtime-main/key_store.rs` (KeyStoreError is local to `runtime-main`; CmdError is foreign). Surfaced to user as a decision; option 1 (full migration) approved. | Stage A1 retro decision called out the generated/error.rs path correction; A2 prompt could have linked to a "wire-format-equal-but-Rust-shape-different migration playbook" — the orphan-rule placement choice and the empty-string fallback for `minLength: 1` validation are reusable patterns that would shave 15 minutes off any future schema-derived enum migration. | 3 |
| Verify gates round 1 (clippy) | 12 clippy errors after first build: doc-markdown backticks (CmdError, SQLite, MAX_RETRIES, GracefulShutdown), doc-lazy-continuation in multi-line bullet docs, redundant_clone in spawn closure, manual_let_else for the spawn-failure / connect-failure tests, match_wild_err_arm + match_same_arms in the reconnect test, unused_async on `spawn_drone` helper, missing_const_for_fn on Windows-side cleanup_socket. All resolved in 2 rounds. | The CLAUDE.md §6 Rust gates list could note "run `cargo fmt --all` then `cargo clippy --workspace --all-targets -- -D warnings`" as a standard sequence — fmt-first lets the next clippy pass surface only real lints, not formatting collisions. (Already implicitly the pattern; making it explicit reduces a 1-cycle round of trivial fixes for new contributors.) | 2 |
| Verify gates round 2 (test) | `drone_reconnect_events.rs::old_events_subscription_does_not_observe_post_reconnect_drone` panicked at `futures-util::stream::unfold:108`: "Unfold must not be polled after it returned `Poll::Ready(None)`". Root cause: my drain loop polled until None then I polled the stream again in the post-restart check. Resolved by tracking termination state and skipping the second poll when drain already saw None. | A small graduation candidate for `docs/gotchas.md`: "Streams built on `futures::stream::unfold` panic on poll-after-Ready(None). When draining via `.next()` until None, do not poll again — track termination explicitly." Forward-applicable to any test that exercises drone IPC events() + reconnect. | 2 |
| Verify gates round 2 (test, Windows-specific) | `cargo test` on Windows hit "Access is denied (os error 5)" trying to remove `target\debug\runtime-drone.exe` because a prior test run had a still-running drone subprocess holding the file lock. Resolved by `Stop-Process` via PowerShell. The test fixtures DO use `kill_on_drop(true)` but Windows file locks linger briefly after process exit. | A docs/gotchas.md candidate: "Windows file lock on subprocess binaries can persist for a few seconds after process exit even with `kill_on_drop(true)`. If `cargo test` reports `Access is denied (os error 5)` on the drone binary, run `Stop-Process -Name runtime-drone -Force` and retry." Forward-applicable to any future test that spawns runtime-drone as a subprocess. | 2 |
| Verify gates round 3 (fmt drift) | Final `cargo fmt --all -- --check` returned exit 1 because my Edit added a multi-line `match` expression where rustfmt prefers the let-binding-with-trailing-`{}` shape. Resolved with `cargo fmt --all`; second `--check` returned exit 0. | (Already covered by the round-1 graduation candidate above — running `cargo fmt --all` BEFORE the final `--check` catches this class of drift.) | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `docs/build-prompts/M04-plan-verify-hitl-budget.md` §A2.2 inventory + §A2.3 detailed changes vs codebase reality | Phase doc references `vdr` in `runtime-main` (it's in `runtime-drone`); references `WriteSignal` IPC command (doesn't exist); references `event_translation.rs` (doesn't exist); references `prompt_template.rs` (doesn't exist); references `src-tauri/src/lib.rs` (only `main.rs`). | Surfaced to user with concrete file evidence (file listings via `find`, grep results, snippet reads). User approved scope reduction. Updated CHANGELOG entry to document the deferred items + their target landing stage. | M04 Phase doc §A2.2 + §A2.3 should be revised before Stage A3 opens — alternatively, fold the deferred items into Stage B's plan/verify primitive surface (which naturally introduces the missing integration points). |
| Phase doc §A2.3 says "replace `pub use runtime_core::error::CmdError`" but M04.A1 retro decision says use `runtime_core::generated::error::CmdError` | The hand-curated `runtime_core::error` module holds `RuntimeError` (a different type); generated `CmdError` lives at `runtime_core::generated::error::CmdError`. | Followed M04.A1 retro decision (cited explicitly). Added top-level `pub use generated::error::{CmdError, ErrorMessage};` in `runtime-core/src/lib.rs` so commands.rs imports as `use runtime_core::CmdError` — a third path that's both stable AND short. | Phase doc §A2.3 should be updated to match the actual import path. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-implementation (after read_first + initial code-reading phase) | The four phase-doc-vs-reality mismatches above + the `CmdError` shape-difference. Asked two `AskUserQuestion` questions: (1) scope reduction options (3 choices); (2) `CmdError` migration approach (2 choices). | Yes — both questions were concrete decision points, both got direct answers. The first prevented ~12+ hours of work on integration points that don't exist; the second locked in the migration approach before any callsite refactor. | Right — before any source file modification. The user's previous direction was to surface immediately if the integration didn't fit; this matched. |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The phase doc inventory was as inaccurate as it was. Three full primitives (`WriteSignal`, prompt-template builder, `event_translation.rs`) named in the phase doc literally don't exist in the codebase, and the retroactive read of M04.A1 gave the same warning for `error.rs`. | Phase docs are authored before stages run, so drift accumulates fast in early-milestone work. M04 was authored across 3 chunks (PR #50) before A1 even merged. | Phase doc inventory rows should be **verified against `git ls-files`** at stage-prompt authoring time, not at stage-execution time. Add to STAGE-PROMPT-PROTOCOL.md as a graduation candidate: "the phase doc author should run `git ls-files crates/ src/ src-tauri/` and confirm every inventory row has a real path or is explicitly marked NEW." |
| Generated `CmdError` tuple-variant shape vs hand-rolled struct-variant shape: serde wire format identical, Rust callsite shape completely different. ~17 callsites required rewriting from `CmdError::Provider { message: m }` to `CmdError::provider(m)`. | The schema's `oneOf` shape compiled to typify's tuple variant via `#[serde(content = "message")]` — wire-equal but Rust-different. typify is doing the right thing; the surprise is that the migration was bigger than the wire format suggested. | The `<schema_drift_check>` v1.3 tag could include a "shape-migration check" sub-task — when a generated type replaces a hand-rolled type, count callsites and surface the migration estimate before authoring. Forward-applicable to M04 Stage B (plan/task schemas) and beyond. |
| `cargo test --workspace` finished `runtime-main` tests with the drone subprocess still holding the binary file lock on Windows; subsequent rebuild failed. | `tokio::process::Command::kill_on_drop(true)` SIGKILLs the child but Windows reaps file handles asynchronously. | Already covered above as a gotcha graduation candidate. Forward-applicable to any future Windows test that spawns a long-lived subprocess. |
| `futures::stream::unfold` panics on poll-after-Ready(None). | The doc says "must not be polled after," but the panic is sharp — not a graceful "returns None forever" behavior. | Already covered above as a gotcha graduation candidate. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 Hard Rules stated; §12 "escalate scope, own technical decisions" drove both surfaces; §10 don't-touch zones held; §5 TDD discipline + `*_with` archetype guided drone_lifecycle.rs. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 3 | Most reads were applicable. The phase-doc inventory inaccuracies cost ~30 minutes of "what does this codebase actually look like" investigation. -2 because the cost was real (a Severity-4 friction event traces back to the inventory rows). |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated Hard Rules + deliverable + 5-bullet test plan + scope-mismatch surface in single response after read-first; first code action was after user approval of revised scope. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | Cross-stack iteration on clippy errors took 2 rounds (12 → 0 errors); drone_reconnect_events test required 1 retry for the unfold-panic; Windows file-lock required 1 PowerShell intervention. Stayed within the 3-round budget. -1 because the second clippy round shouldn't have been needed if I'd run `cargo fmt --all` before the first clippy invocation per the M01 graduation gotcha. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made; this retrospective is being surfaced for approval. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Phase-doc-vs-reality scope mismatch escalated immediately (correctly per §12 "Any spec ambiguity or contradiction"). CmdError migration approach escalated (genuinely user-domain — full migration vs drift-test was a scope/risk choice). Technical decisions (orphan rule placement, empty-string fallback for ErrorMessage validation, helper constructor design) owned per §12 "own technical-best-practice decisions." |
| 7 | Was the human-direction load reasonable? | 5 | Two `AskUserQuestion` invocations both with clear options + recommendations. No mid-execution clarifications needed beyond those two. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | Both surfaces were concrete (file paths, line counts, decision-tree options). User answered both unambiguously. |
| **Process axis total** | | **37/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case Rust + camelCase TS; functions ≤50 lines; helper constructors over `Self::Variant(ErrorMessage::try_from(...).unwrap())` boilerplate; `*_with` testable seams on every command + drone_lifecycle. Comments only where the WHY is non-obvious (orphan rule placement, kill_on_drop failsafe, drain-then-don't-poll-again). |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | Wiremock tests assert wire-format invariants (header values, response field names, error-status mapping). drone_reconnect_events asserts the v0.1 behavior lock through real subprocess kill + restart, not a unit-level mock. cmd_error_ext tests assert Display parity with M02 messages byte-for-byte (would fail if a message body changed). Wire-shape tests assert the JSON envelope by deserialization, not by reading the typify-emitted source. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | New public APIs (`DroneLifecycle::spawn`, `spawn_with`, `shutdown`, `compute_ipc_addr`, `CmdError::provider/drone/key_store/internal/message`, `ErrorMessage::as_str`) all have rustdoc with `# Errors` sections. No new doc tests added (the rustdoc bodies don't have runnable examples — pure description). -1 because a `cmd_error_ext::CmdError::provider` doc test would be a useful compile-checked anchor. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All 6 tractable items shipped per the user-approved scope reduction. Two deferred items (vdr WriteSignal wiring + structured-emitter decision extractor) explicitly documented in CHANGELOG + this retro's "Decisions for next stage" section. No scope creep — did not implement plan/verify primitives ahead of Stage B. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 4 | drone_lifecycle.rs has thorough module docs explaining the cross-platform IPC addressing + kill_on_drop failsafe + the seam-vs-wrapper split. cmd_error_ext.rs explains the schema-vs-Rust-ergonomics separation of concerns. -1 because the M04 Stage A2 reduced-scope rationale lives in the CHANGELOG + this retro, not inline; a future contributor reading the code wouldn't know why VDR projector wiring is missing. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers (CONNECT_MAX_RETRIES, CONNECT_BASE_BACKOFF, SHUTDOWN_TIMEOUT, GRACEFUL_TIMEOUT_MS named); no stringly-typed APIs; no `any` (TS uses the generated `CmdError` discriminator); no ad-hoc `#[allow]` (one in cmd_error_ext.rs is `#[must_use]`, not allow); no implementation-detail tests; no generic names. |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | 5 | Measured: workspace 91.17% line; runtime-drone 95.86% line (≥95%); runtime-main 98.09% line (≥95%); ipc.ts 100%; wiremock count_tokens 4/4 cases. |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | All gates green locally on Windows 11 (build agent OS per `<runtime_environment>`). Linux/macOS CI happens on PR push; -1 because not yet observed. The drone_reconnect_events.rs test is `#![cfg(any(unix, windows))]` and exercises real subprocess spawn — high cross-platform-risk surface. |
| **Product axis total** | | **37/40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 4 | `<schema_drift_check>` directly load-bearing. `<runtime_environment os="windows">` correctly pinned the platform-specific gotchas (named pipe + Test-Path issue). `<gotchas>` traps were 5/6 directly applicable. `<dependency_audit_check>` was a no-op for this stage (deps already had the listed features) but cheap to verify. -1 because the `<execution_warnings>` tag's "drone subprocess spawn at Tauri setup is the highest-risk surface" framing was useful but the *bigger* risk surface ended up being the phase-doc-vs-reality drift, which no tag flagged. |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 3 | A `<phase_doc_inventory_audit>` tag (or a sub-tag of `<pre_flight_check>`) would have caught the WriteSignal / prompt_template / event_translation references before code work began. Forward-applicable to every milestone where the phase doc is authored before stage execution. -2 because the missing tag cost a real Severity-4 friction event. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | Three forward-applicable gotchas surface from this stage: (a) `futures::stream::unfold` panic on poll-after-None; (b) Windows file-lock persistence after subprocess SIGKILL even with `kill_on_drop(true)`; (c) wire-equal but Rust-shape-different schema migrations require a callsite-count estimate before authoring. All three apply to M04 Stage B+ and onward. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 4.5h, actual ~3h. Ratio 0.67×. The user-approved scope reduction kept actual under estimate. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 3 | (a) The pre-implementation surface-and-confirm for scope mismatch wasn't named in any prompt section; I followed CLAUDE.md §12 "surface as flag" because the phase doc was structurally inaccurate. (b) The `cargo fmt --all` BEFORE final fmt-check is implicit (M01 graduation gotcha) but didn't make it into the §6 quality-gate ordering. (c) The orphan-rule placement decision for `From<KeyStoreError> for CmdError` (in `runtime-main/key_store.rs` because KeyStoreError is local) is implicit Rust knowledge, but the same rule comes up every time a type migrates between crates. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 4 | Mostly yes. The deliverable (after scope reduction) is concrete + the gates are mechanical. The phase-doc-vs-reality discovery + scope-reduction surface would recur (~20 minutes of investigation per fresh session). The CmdError migration approach is deterministic given the M04.A1 retro decision. -1 because the surface-and-confirm step is judgment-based — different sessions might surface different scope splits. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 3 | Yes — three changes needed before M04 Stage B opens: (a) Phase doc §A2.2 inventory rows revised to match codebase reality OR Stage A3 prompt added for the deferred items; (b) Phase doc Stage B onward should add explicit `git ls-files` verification of every inventory row at authoring time; (c) Add the three forward-applicable gotchas to docs/gotchas.md. |
| **Pattern axis total** | | **26/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log --oneline -1` returns `c5fe035 chore(workspace): M04 Stage A1 — build hygiene + xtask codegen extensions`; no new commits this session.
- [x] **G2: no Severity-5 friction events** — *evidence:* highest severity is 4 (phase-doc-vs-reality scope mismatch); resolved via user-approved scope reduction.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* table is empty (none observed).
- [x] **G4: stage actually completed** — *evidence:* All 6 user-approved tractable items shipped: drone_lifecycle.rs (new + 8 unit tests), commands.rs `pub use CmdError` + `tauri::State<Arc<DroneClient>>` parameters + 17 callsite migrations, cmd_error_ext.rs (new + 13 unit tests), key_store.rs `From<KeyStoreError> for CmdError` + 2 unit tests, anthropic.rs real `count_tokens` + anthropic_wiremock.rs 4 new tests, drone_reconnect_events.rs (new integration test, 2 cases passing), ipc.ts unwrapCmdError refactor + ipc.test.ts 9 new tests, spec §1d note disposition (locked v0.1 behavior), CHANGELOG entry. All gates green.
- [x] **G5: scores ≥3 in every individual row** — *evidence:* lowest individual rows are Pattern Q2 (3) "missing TEMPLATE.md sections" and Pattern Q5 (3) "implicit protocol moments" and Pattern Q7 (3) "prompt-format changes needed first". All ≥3.

### Soft gates

- [x] **S1:** Process axis total ≥30/40 — *actual:* 37
- [x] **S2:** Product axis total ≥32/40 — *actual:* 37
- [x] **S3:** Pattern axis total ≥25/35 — *actual:* 26
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 4.5h, took ~3h, ratio 0.67×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 1 (the `CmdError` shape-migration surface — the others are Severity 1 or 2)

### Outcome

- [x] **Pattern is sound but rough.** All hard gates pass; all soft gates pass numerically, BUT the Pattern axis is 26/35 (within S3 floor of 25 but lower than M04.A1's 28). The lower Pattern score reflects the phase-doc-vs-reality friction event — a real protocol gap that should be addressed in `STAGE-PROMPT-PROTOCOL.md` before Stage B opens. Apply minor `CLAUDE.md` / phase-doc updates per "Decisions for the next stage."

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.17% line (6566 regions, 92.12% region; 569 functions, 89.63% function)
- **Exclusions added this stage:** None added.
- **Exclusion list as of stage end:** `src.main\.rs|generated`

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
  - Coverage actual this stage: 95.86% line
  - Exclusions added this stage: None.
  - Exclusion list as of stage end: `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs`
- **Crate:** `runtime-main`
  - Coverage actual this stage: 98.09% line
  - Exclusions added this stage: None.
  - Exclusion list as of stage end: `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.key_store\.rs`

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/key_store.rs` | (excluded from gate) | 50.00% region; 48.44% line | new measurement | Excluded from `runtime-main` 95% gate per M02.E + M02 `tauri-shell` 50% codecov.yml. Two new tests for `From<KeyStoreError> for CmdError` raised the testable-surface coverage but the Tauri-shell wrapper bodies remain structurally untestable on CI without a platform keychain. |
| `crates/runtime-main/src/providers/anthropic.rs` | (excluded from gate) | 98.45% region; 96.35% line | ↑ | Real-network wrapper + new wire-format types `CountTokensRequest` + `CountTokensResponse` are exercised by 4 new wiremock tests. The structurally-untestable `reqwest::Client` construction stays at the same coverage baseline. |
| `crates/runtime-main/src/sdk/decision_extractor.rs` | 100.00% (M03.E) | 100.00% | → | No production-code change this stage (deferred). |
| `crates/runtime-main/src/sdk/event_pipeline.rs` | 100.00% (M03.E) | 100.00% | → | No production-code change this stage (vdr projector wiring deferred). |
| `crates/runtime-main/src/sdk/agent_sdk.rs` | (per M03.E baseline) | 95.88% region; 99.41% line | ~ | No production-code change this stage. |
| `crates/runtime-main/src/drone_ipc/client.rs` | 96.75% (M04.A1) | (per stage measurement) | → | No production-code change this stage. |
| `crates/runtime-core/src/cmd_error_ext.rs` | (new file) | 100.00% line (13 new unit tests) | ↑ from baseline | New module; full test coverage of helper constructors, Display parity, message accessor, wire-format invariants, std::error::Error wiring. |
| `src-tauri/src/drone_lifecycle.rs` | (new file) | 67.06% region; 68.51% line | ↑ from baseline | New module. Testable seam (`spawn_with`) covered by 8 unit tests; production wrapper (`spawn`) + tokio::process::Command construction paths fall under the `tauri-shell` 50% patch gate per `codecov.yml`. |
| `src-tauri/src/commands.rs` | (per M03.E baseline) | 68.59% region; 67.03% line | ~ | Refactored shape (tuple-variant CmdError) + new `tauri::State<Arc<DroneClient>>` parameters; production wrappers fall under the `tauri-shell` 50% gate. The `*_with` testable seams remain at high coverage. |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. Confirmed by inspecting `.github/workflows/ci.yml` lines 352-389. **Note:** CLAUDE.md §6 documentation already includes `key_store.rs` in the runtime-main exclusion regex (verified at `CLAUDE.md` lines 121-129 — this was the M04.A1 carry-forward decision, already applied). No drift; no documentation change needed this stage.

---

## [END] Decisions for the next stage

Stage B inherits the following:

### Special-log decisions per the prompt's `<special_log>` directive

- **Long-lived events() reconnect outcome:** v0.1 behavior locked via test. Subscriptions do NOT survive reconnect; renderers must resubscribe. Spec §1d ⚠️ note replaced with the lock; integration test at `crates/runtime-main/tests/drone_reconnect_events.rs` is the test reference. **No further action required from Stage B.**
- **Structured-emitter delimiter format:** N/A — deferred. The structured emitter (with `<<DECISION>>...<<END>>` delimited blocks) requires a prompt-template builder which doesn't exist. Stage B's plan/verify primitive surface introduces the natural place for this builder; the delimiter format from the M03.5 spec text remains valid as a target.
- **count_tokens response field name:** Verified via web fetch against `https://platform.claude.com/docs/en/api/messages-count-tokens` (the docs.anthropic.com URL redirects to platform.claude.com as of 2026-05-08). Field name is `input_tokens`; confirmed matches spec §2c.3 wording. **No spec follow-up needed.**
- **Drone subprocess startup latency on cold-start:** Implemented with 5-attempt exponential backoff (200/400/800/1600/3200ms = 6.2s cumulative). On a warm host, first connect typically succeeds within the first attempt. Cold-start hasn't been benchmarked under load — `Stage F may need a loading state` warning from the prompt is forward-applicable but not blocking for Stage B.
- **Cross-stack glue points verbatim-quoted from upstream:**
  - `count_tokens` request shape (POST `/v1/messages/count_tokens` + headers + body) — quoted from `https://platform.claude.com/docs/en/api/messages-count-tokens`.
  - Tauri 2.x `.setup(|app| { app.manage(...); Ok(()) })` pattern + `tauri::State<'_, T>` access — quoted from `https://v2.tauri.app/develop/state-management/`.
  - `tokio::process::Command::kill_on_drop(true)` semantics + `Child::start_kill` fallback — quoted from `https://docs.rs/tokio/latest/tokio/process/struct.Command.html`.

### `CLAUDE.md` updates

- [ ] Optional: Update §6 Rust quality-gate ordering to make the "fmt-then-clippy" sequence explicit (currently implicit via the M01 graduation gotcha). Low priority; cosmetic.
- [ ] No mandatory CLAUDE.md updates this stage.

### `STAGE-PROMPT-PROTOCOL.md` updates

- [ ] **Add a `<phase_doc_inventory_audit>` tag (or a sub-tag of `<pre_flight_check>`)** that requires the phase doc author to verify every file listed in §A.2 inventory rows exists via `git ls-files` at authoring time, OR is explicitly marked NEW. Forward-applicable to every milestone where phase docs are authored before stage execution. Stage B authoring should incorporate this.

### `TEMPLATE.md` updates

- [ ] **Add a `<schema_migration_callsite_audit>` sub-tag** for stages that replace a hand-rolled type with a generated type (or vice versa). The audit counts callsites that need refactoring and surfaces the estimate before authoring. Forward-applicable to M04 Stage B (plan/task schemas land in `runtime_core::generated::{plan,task}` and replace TBD hand-rolled types).

### Milestone document updates (`M04-plan-verify-hitl-budget.md`)

- [ ] **§A2.2 inventory rows** — drop references to `crates/runtime-main/src/sdk/event_translation.rs` (doesn't exist; translation lives in `event_pipeline.rs`); drop references to `prompt_template.rs` (doesn't exist; introduced naturally by Stage B's structured-emitter scope or carry forward); change `src-tauri/src/lib.rs` to `src-tauri/src/main.rs`.
- [ ] **§A2.3 detailed changes** — update the `pub use runtime_core::error::CmdError` line to `pub use runtime_core::CmdError` (the actual import path; runtime-core re-exports at the crate root).
- [ ] **§A2.5 CLI prompt** — the prompt's `<read_reference>` tag includes `crates/runtime-main/src/vdr.rs` but that file doesn't exist (vdr is in `runtime-drone`). Update or drop the row.
- [ ] **Add a Stage A3 (or fold into Stage B's prompt header):** the deferred items — VDR projector wiring at WriteSignal + structured-emitter decision extractor — need a target landing stage. Recommended: Stage B introduces the `WriteSignal` IPC command variant (since plan/verify primitives drive signal emission anyway) and ships the prompt-template module alongside.

### Protocol additions (graduation candidates for `docs/gotchas.md` at M04 closeout)

- [ ] **Stream-unfold poll-after-None:** "Streams built on `futures::stream::unfold` panic on poll-after-Ready(None). When draining via `.next()` until None, do not poll again — track termination explicitly." Forward-applicable to drone IPC events() tests + any future stream-based subscription test.
- [ ] **Windows file-lock persistence:** "Windows file lock on subprocess binaries can persist for a few seconds after process exit even with `kill_on_drop(true)`. If `cargo test` reports `Access is denied (os error 5)` on a runtime-* binary, run `Stop-Process -Name <bin> -Force` and retry." Forward-applicable to any test that spawns runtime-drone or future runtime-* subprocesses.
- [ ] **Generated-type ergonomics:** "When a typify-generated type replaces a hand-rolled type with a different Rust shape (e.g., struct variant → tuple variant over newtype), add inherent helper constructors + Display + std::error::Error impls in a separate module within the same crate (orphan rule satisfied). Place `From<ForeignType>` impls in the crate that owns the source type. The wire format stays the same; the migration is mechanical but ~17-callsite-class for a 5-variant enum."

### Open issues to file

- [ ] None — all decisions are inline or queued for Stage B / Stage G.

---

## [END] User-review notes

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M04 Stage A2, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m04-plan-verify-hitl-budget`; the PR drafts and pushes only at the end of Stage G.

**Claude:** I have completed M04 Stage A2 per the stage prompt (with user-approved scope reduction surfaced before any code). The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval.

**Surfaced at:** 2026-05-08
