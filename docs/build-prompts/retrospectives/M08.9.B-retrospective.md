# M08.9.B — Stage Retrospective

> **Stage:** M08.9 Stage `B` — Run drill-down (verdict → tool payload → raw) — see `docs/build-prompts/M08.9-honest-tester.md`
> **Parent milestone:** M08.9 (honest Tester) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-06
> **Session end:** 2026-06-06
> **Branch:** `claude/m08.9-honest-tester` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `85bc01a` (A retro)
> **Ending commit:** `3f148ae` (impl) on `b10eb6f` (red) — local, unpushed, awaiting approval
> **Estimated effort (per prompt):** ~1–1.5 hours (small — disclosure primitives existed)
> **Actual elapsed:** ~1 hour

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded; §4 Hard Rules in force
- [x] All "Read first" files read (phase doc Stage B + Stage A; TesterModal.tsx result layout + trace replay; ipc.ts TestOutcome + the generated `agent_event.ts` AgentEvent union; the Output-rail payload surface in InspectorPanel.tsx + ValidationCard.tsx; DESIGN.md principles 1+3; the existing TesterModal/InspectorPanel/ValidationCard vitests + tester_verdict.e2e.ts for the key-guard pattern)
- [x] Deliverable + test plan stated before code
- [x] Branch correct (`claude/m08.9-honest-tester`)
- [x] No carried-over uncommitted tracked changes (only pre-existing untracked IRL dirs: `budget-irl/`, `gap-irl/`, `rung2/`, `.claude/*`)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| impl / tsc verify | The red test file did not typecheck: `noUncheckedIndexedAccess` makes `steps[0]` / array-destructured `step` possibly-undefined, and `expect(step.kind).toBe('tool')` does not narrow | (general gotcha) — under strict TS, narrow the discriminated step via `const step = steps[0]; if (step?.kind === 'tool')` rather than `const [step] = steps; expect(step.kind)…` | 2 |
| impl / tsc verify | Production `foldTrace` hit two `noUncheckedIndexedAccess`/predicate errors: `trace[j]` possibly-undefined; the `(e): e is AgentEvent` predicate is not assignable to the array's `ToolInvoked\|ToolResult\|ToolError\|null` element type | (general) — guard `candidate !== undefined`; the raw-event filter predicate must be the precise member union, not `AgentEvent` | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `<scope_locks>` "build no new disclosure primitive" vs `<retrospective_requirements><special_log>` "which primitives … needed a lift-to-shared" | Whether extracting `formatPayload` + the Show-raw disclosure into shared modules counts as "building a new primitive" | Read as: *inventing a new disclosure UX* is forbidden; *extracting an existing one for reuse* is the documented expectation (the special_log explicitly anticipates a lift-to-shared, mirroring the M08.D1 CapabilityDisclosure extraction + A's InspectorPanel parameterization). Lifted `formatPayload`→`src/lib/formatPayload.ts` and the Show-raw→`src/components/RawDisclosure.tsx`; ValidationCard delegates to the latter with its exact testids/classes preserved. | Already the de-facto reading; noted for closeout. |
| `CLAUDE.md` §4.1/§8 (do-not-commit) vs `<tdd_discipline strict>` (red→impl diff EMPTY) | Same tension A logged — two local commits must exist to diff red..impl | Two commits **local-only and unpushed**; nothing reaches the remote / a PR without approval | de-facto protocol (carried from A) |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Stage end | The full approval bundle (cross-machine state, red→impl EMPTY-diff evidence, gate results, drill-down walkthrough, design-review note, "will not commit/push until approved") | Yes | Yes — after both phases complete + all CI-parity frontend gates green |

---

## [LIVE] Protocol drift events

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The InspectorPanel "Output-rail payload component" was not a component — it was a module-private `formatPayload` function + inline `<pre>` markup | The phase doc/`<wire_signature_audit>` framed it as a component to "pin its name + props"; the actual reusable unit was a pure formatter | The lift-to-shared was a function extraction, not a component reuse. The store-coupled InspectorPanel can't read `outcome.trace` (it reads graph nodes), so only the formatter is shareable — recorded honestly in the special-log. |
| ValidationCard is an `role="alert"` err-card | Reusing the literal `<ValidationCard>` for every tool row would mislabel OK tool calls as alerts | Lifted the Show-raw disclosure out (RawDisclosure) instead of reusing the whole card; ValidationCard now delegates to it, DOM/testids byte-identical → its existing tests pass untouched. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **deferred to Stage V** (assembled-execution pass) + maintainer IRL | `tests/e2e-tauri/tester_drilldown.e2e.ts` (key-guarded real-app drill) + `TesterModal.drilldown.test.tsx` (the drill-down mounts INSIDE the assembled modal under the verdict — construction reachability) + `TraceDrilldown.test.tsx` (foldTrace + row behavior) | None. The real drill is key-dependent (a real tool call in the trace needs a live model to emit a `ToolUse`); CI skips it like `tester_verdict.e2e.ts` / `smoke.e2e.ts`. The maintainer IRL is the authoritative close. `test:e2e:tauri` was **NOT run in this build session** (no `tauri-driver`/`msedgedriver` + built app available here) — honestly disclosed. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §5/§6 gate ordering + the strict-TDD two-commit flow applied directly |
| 2 | "Read first" list correct? | 4 | Every pin load-bearing; the one gap — the "Output-rail payload component" is a function, not a component (logged under Surprise) |
| 3 | Deliverable + test plan before code? | 5 | Stated; red phase encoded it (foldTrace + rows + wiring + e2e) |
| 4 | Self-correct in ≤3 rounds? | 5 | One tsc round (test + prod narrowing), fixed first try; the test-narrowing fix folded into the red commit so the red→impl test diff stays empty |
| 5 | Do-not-commit-until-approved held? | 5 | Local commits only; unpushed; no PR |
| 6 | Escalate right things / proceed right things? | 5 | No escalation; the lift-to-shared + the "link to explainer" concretization owned per §12 |
| 7 | Human-direction load reasonable? | 5 | Zero mid-flight input needed |
| 8 | Surfaces actionable? | 5 | Single approval bundle |
| **Process axis total** | | **39/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | camelCase TS / kebab artifacts; doc-why comments; no marketing |
| 2 | Behavior tests, not tautology (§5)? | 5 | Tests assert observable disclosure: collapsed→expand reveals input/result; Show-raw reveals raw event JSON; tier/capability rows distinct + link to explainer; the wiring test drives the assembled modal |
| 3 | Public APIs documented? | 5 | `TraceStep`, `foldTrace`, `TraceDrilldown`, `RawDisclosure`, `formatPayload` all doc'd |
| 4 | Exactly promised — no creep/cut? | 5 | Pure disclosure; no run change, no backend/schema change; reused existing primitives |
| 5 | Stranger-readable? | 5 | Docs name the reuse + the tier≠defect framing inline |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no `any`, no ad-hoc allow/ts-ignore |
| 7 | Coverage ≥80 on `src/` — measured? | 5 | `npm run test` global lines **93.54%**, exit 0; `TraceDrilldown.tsx` 97.67% |
| 8 | CI green across OS×toolchain? | 3 | Local frontend gates green; `test:e2e:tauri` not runnable in this session (driver/built-app absent) — CI + maintainer IRL close it |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | wire_signature_audit + construction_reachability_check both paid off (the wiring test is the reachability proof) |
| 2 | Sections needed but missing? | 5 | None |
| 3 | Gotchas local or generalize? | 4 | The `noUncheckedIndexedAccess` narrowing trip is general (worth a gotcha if recurring); the "Output-rail component is a function" pin-mismatch is M08.8.A-specific |
| 4 | Time-box matched (within 2×)? | 5 | ~1h vs ~1–1.5h estimate |
| 5 | Implicit protocol moments? | 4 | The "no new disclosure primitive vs lift-to-shared" reading is implicit — the special_log anticipating it is what disambiguated |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the pins make it mechanical |
| 7 | Format needs change before reuse? | 5 | No |
| **Pattern axis total** | | **33/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — commits `b10eb6f` (red) / `3f148ae` (impl) are local + unpushed; no PR; surfacing for approval now.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — none.
- [x] **G4: stage completed** — `3f148ae`; acceptance criteria met (trace→rows; expand→payload via the shared formatter; Show-raw via the shared disclosure; tier/capability rows distinct + link to explainer; drill-down mounts under the verdict in the assembled modal). The real-app IRL is the maintainer's pending close per `<real_app_irl>`.
- [x] **G5: every axis row ≥3** — lowest is 3 (Product #8, e2e-tauri not run locally).
- [x] **G6: CI-parity** — frontend gates run with the exact `CLAUDE.md` §6 commands (see below). No divergent flags.

**CI-parity evidence (exact commands):**
- `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'` (green) ; `npx eslint .` (green) ; `npx tsc --noEmit` (green)
- `npm run test` → **61 files, 646 passed, 0 failed**; global lines **93.54%** (≥80 src floor; vitest exit 0)
- `npm audit --audit-level=high` (exit 0; 2 pre-existing moderate, below the high gate)
- **Strict-TDD invariant:** `git diff b10eb6f..HEAD -- 'tests/'` is **EMPTY** (impl did not touch the test files; the test-narrowing fix was folded into the red commit by amend before impl).
- **NOT run this session:** `npm run test:e2e:tauri` — requires the built Tauri app + `tauri-driver`/`msedgedriver`, unavailable here. The maintainer IRL + the CI `e2e-tauri-driver` job are the close (the drill assertion is key-dependent and skips on CI without an Anthropic key, like `smoke.e2e.ts`). Rust gates were not re-run — B is renderer-only (no Rust diff).

### Soft gates

- [x] **S1:** Process 39/40 ≥ 30
- [x] **S2:** Product 38/40 ≥ 32
- [x] **S3:** Pattern 33/35 ≥ 25
- [x] **S4:** ~1h vs ~1–1.5h (ratio <2×)
- [x] **S5:** 0 Severity-3 friction (max 2)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage V (five-pass real-app verifier).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line, `src/` vitest)

- **Coverage actual this stage:** global lines **93.54%** (exit 0); `TraceDrilldown.tsx` 97.67%, `TesterModal.tsx` 94%. The two shared extractions (`formatPayload.ts`, `RawDisclosure.tsx`) are exercised by the new TraceDrilldown tests + the existing InspectorPanel/ValidationCard tests.
- **Exclusions added this stage:** None (renderer-only; no Rust coverage change).

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage; B is renderer-only — the four Rust-coverage mirrors are untouched. No `codecov.yml`/`docs/coverage-policy.md` change needed beyond the closeout §C M08.9 entry (Stage G).

---

## [END] Decisions for the next stage (V — verifier)

### `CLAUDE.md` updates
- [ ] None required.

### Milestone document updates
- [ ] None — the Stage V prompt is accurate; it verifies A (verdict) + B (drill-down) against the real app.

### Protocol additions
- [ ] Consider (closeout call): a one-line gotcha on `noUncheckedIndexedAccess` discriminated-union narrowing in vitest (`const x = arr[0]; if (x?.kind === …)`), if it recurs.

### Open issues to file
- [ ] None new. B is pure disclosure; it adds no execution behavior and no schema change.

### For the V verifier specifically
- The drill assertion is key-dependent — the 5th assembled-execution pass should drive the real app with an Anthropic key (a Read-using framework yields a clean PASS run with a drillable tool call), or honestly mark the pass INCOMPLETE/HYPOTHESIS (rule 11) if no key is reachable. The key-independent surface (`trace-drilldown` absent before a run) IS runnable on CI.

---

## [END] Sign-off

**Claude:** I have completed M08.9 Stage `B` per the stage prompt. The Tester run is now **drillable**: `TesterModal` renders `outcome.trace` as a step list under the verdict (`TraceDrilldown`), each tool call expanding to its input/result and a Show-raw disclosure over the raw event; tier-blocked and capability-violation rows are distinct and link to their explainer. It is **pure disclosure** — no run change, no backend, no schema change. Reuse was achieved by two **behavior-preserving lift-to-shared extractions**: `formatPayload` (the M08.8.A Output-rail payload formatter, which was a module-private function, not a component) → `src/lib/formatPayload.ts`, reused by InspectorPanel + TraceDrilldown; and the Show-raw disclosure → `src/components/RawDisclosure.tsx`, which `ValidationCard` now delegates to (DOM/testids preserved → its tests pass untouched) and TraceDrilldown reuses for each tool row. The **drill-down real-app IRL** is the maintainer's pending close. Commits `b10eb6f` (red) + `3f148ae` (impl) are local and unpushed; the red→impl test diff is **EMPTY**; I will NOT push or open a PR until approved.

**Surfaced at:** 2026-06-06
