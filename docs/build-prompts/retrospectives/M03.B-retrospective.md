<!--
M03 Stage B retrospective. Per CLAUDE.md §19 + RETROSPECTIVE-TEMPLATE.md.
First milestone authored on the v1.2 XML stage-prompt protocol.
-->

# M03.B — Stage Retrospective

> **Stage:** M03 Stage `B` — React Flow + Zustand foundation + 3 basic node types; see `docs/build-prompts/M03-live-graph.md` §B
> **Parent milestone:** M03 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-05 ~17:30 UTC
> **Session end:** 2026-05-05 ~18:00 UTC
> **Branch:** `claude/m03-live-graph` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `bb8202e` (M03 Stage A — build hygiene + carry-forward + new deps)
> **Ending commit:** `uncommitted — awaiting approval`
> **Estimated effort (per prompt):** 5.5 hours
> **Actual elapsed:** ~1.5 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded via system prompt) and the §4 Hard Rules were stated at the top of the first response (no commit without approval; no push to main; no skipping gates; no telemetry; no hand-rolled types from schemas; cargo deny clean; no unsafe outside runtime-sandbox; capability/drone/sandbox/provider plan-first; .aria/archive untouched; v0.1 scope locked).
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M03-live-graph.md §B.1–B.4, agent-runtime-spec.md §3, MVP §M3, style.md, gotchas.md #21/#25/#26/#27).
- [x] All `read_reference` files were read with their `purpose` understood (`src/types/agent_event.ts` schema-driven discriminated union; `src/lib/eventReducer.ts` legacy useReducer pattern being deleted; `tests/unit/components.test.tsx` RTL+vi.mock idiom; `src/App.tsx` state-machine pattern to preserve; `src/lib/ipc.ts` typed wrappers preserved as-is).
- [x] `read_prior_stages` retrospective consulted: `docs/build-prompts/retrospectives/M03.A-retrospective.md` [END] Decisions for the next stage section. Specific decisions applied: (1) schema is source-of-truth — `src/lib/graphStore.ts` imports `AgentEvent` from `src/types/agent_event.ts` and uses snake_case field names (`event.agent_id`, `event.agent_name`, `event.parent_id`, `event.tool_name`, `event.skill_name`, `event.duration_ms`); (2) `optimizeDeps.include` extended with `@xyflow/react` + `zustand` (Stage A pre-bundles Tauri + React; Stage B adds the new graph deps); (3) Stage A's prompt §A.3 "10 variants" stale enumeration is replaced by full schema coverage — graphStore exhausts the 36-variant union with `_exhaustive: never`.
- [x] The deliverable was stated in 1–3 sentences and the test plan in 5 bullets BEFORE writing code (in the surface message after orientation reads).
- [x] User did not need to confirm deliverable + test plan separately (per `CLAUDE.md` §12 "operating mode" — outcome approval, not step approval).
- [x] The branch was correct: `claude/m03-live-graph` per the milestone prompt.
- [x] No new uncommitted changes carried over from a prior session — `git status` showed only the 3 carry-forward `.log` files (gitignored, pre-existing).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| `verify_gates` round 1 (prettier) | 8 newly-authored files (`graphStore.ts`, `styles.css`, three node tests, two refactored tests, `ToolNode.tsx`) tripped prettier formatting checks. Auto-fixed via `prettier --write`. | The Stage A retro (or `CLAUDE.md`) could explicitly note that `prettier --write` is the standard first-step on a multi-file authoring stage rather than relying on per-author muscle memory. Trivial 30-second pause once recognized. | 1 |
| `verify_gates` round 1 (eslint) | `@typescript-eslint/no-unnecessary-type-assertion` flagged two `(n.data as ToolNodeData).agentId` / `(n.data as SkillNodeData).agentId` assertions in `graphStore.ts` helpers. Discriminated-union narrowing already produced the right type after `n.type === 'tool'` / `'skill'`; assertions were redundant. | The prompt's B.3 sample body had the assertions; they reflected pre-narrowing instinct rather than v12 + TS-strict reality. Worth one line in the retrospective Decisions for Stage C: "trust the React Flow `Node<Data, Type>` discriminator after a `.type === 'X'` filter — assertions are dead weight". | 1 |
| `verify_gates` round 1 (act warnings) | `App.test.tsx` triggered `Warning: An update to GraphCanvas inside a test was not wrapped in act(...)` from React Flow's internal state updates during the `registeredHandler({payload})` synchronous dispatches. Wrapping the for-loop in `act(() => { ... })` silenced the warnings for the dispatch path; downstream React Flow internal effects (`MarkerDefinitions`) still emit warnings during the second test (`surfaces_command_error_via_error_paragraph`) where no events are dispatched. The downstream warnings are React Flow + happy-dom layout-effect noise; tests pass and gates stay green. | `docs/gotchas.md` #27 covers RTL DOM-ref staleness; React Flow + happy-dom act() noise is its own animal. Worth a line either in #27 or as a new gotcha (#32) explaining: React Flow inside a happy-dom Vitest harness emits unavoidable act() warnings for internal layout effects (zoom-pane, edge marker definitions) — not actionable signal. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M03 prompt §B.3 sample body uses camelCase (`event.agentId`, `event.agentName`, `event.parentId`) vs. the actual generated `src/types/agent_event.ts` schema (snake_case: `event.agent_id`, `event.agent_name`, `event.parent_id`) | Field names in the prompt's draft don't match the regenerated schema | Followed the schema (canonical source-of-truth per CLAUDE.md §14; Stage A retro carry-forward). Used `event.agent_id`, `event.agent_name`, `event.parent_id`, `event.tool_name`, `event.skill_name`, `event.duration_ms` throughout `graphStore.ts`. The prompt's draft pre-dated Stage A's schema regeneration. | M03 prompt §B.3 sample code body should be revised to match the snake_case schema before Stage C drafts the remaining 8 node types — same drift root cause Stage A retro flagged. |
| M03 prompt §B.3 lists `stream_thinking` as a no-op case in the `applyEvent` switch | Schema (`src/types/agent_event.ts`) has no `stream_thinking` variant; only `stream_text` exists | Dropped the `stream_thinking` case. The TS exhaustiveness check (`_exhaustive: never`) is the safety net — if a future schema adds `stream_thinking`, the compiler errors and Stage X+ updates the switch. | Same edit recommended on §B.3 sample code. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| `surface` (end of stage) | This retrospective + diff stat + gate results + draft commit message + explicit "I will not commit until you approve." Plus the deviation note (schema snake_case used throughout instead of the prompt's draft camelCase, and `stream_thinking` dropped because the schema doesn't define it). | Yes — single-bundle approval per CLAUDE.md §8. The deviation note is a forwarding flag for the M03 closeout (`<gotchas_graduation>`) so the Phase doc B.3 sample body gets revised before Stage C drafts the next round of node types. | Yes — at stage end, after all gates green. |

(No mid-stage surfaces. The prettier + eslint round-1 friction was self-correctable within the 3-iteration budget — not user-domain decisions. The schema-vs-prompt snake_case ambiguity was a clear technical decision per CLAUDE.md §12 "own technical decisions" — schema wins.)

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §4 #1 do-not-commit-without-approval | Held throughout — no `git commit` invoked at any point. | n/a (no drift) | Surface-only at session end. |
| §4 #5 no hand-rolled types from schemas | The graphStore's `applyEvent` consumes the schema-generated `AgentEvent` discriminated union directly. No re-mirroring; field names are snake_case per the schema. Stage A retro Decisions explicitly carried this forward. | n/a (no drift) | Schema-as-source-of-truth invariant strengthened. |
| §4 #10 no scope creep | Resisted spawning `MCPNode`/`GapNode`/`HITLNode` etc. even though `@xyflow/react` was already installed and the styling pattern was clear. Stage C's job. | n/a (no drift) | The `<gotchas>` trap worked as intended — focusing on the foundation + 3 nodes. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The schema's snake_case naming surfaced an immediate prompt drift in §B.3's sample code body (camelCase) — same root cause Stage A retro flagged for §A.3's "10 v0.1 variants" stale enumeration | The prompt was authored before Stage A regenerated the schema; the example body then drifted from the canonical TS surface. Two consecutive stages catching this means the Phase-doc-vs-generated-types drift is a structural risk for any milestone that lands a schema in Stage X and consumes it in Stage X+1 | The M03 prompt's §B.3 sample body should be revised before Stage C drafts the remaining node types; Stage C's `<read_reference>` should explicitly point at `src/types/agent_event.ts` as the canonical field-name source (it does already, so the discipline only needs to extend to "trust the generated TS over the prompt's sample body"). The closeout's `<gotchas_graduation>` is the right forcing function. |
| React Flow + happy-dom emits ~12 `act(...)` warnings per App.test.tsx run, even when the test doesn't dispatch state updates (`MarkerDefinitions`, `EdgeRendererComponent`, internal Zoom/Pane effects) | React Flow uses `useLayoutEffect` for size measurements; happy-dom doesn't fully simulate the layout pipeline so React schedules updates that fall outside RTL's `act()` boundary | Stage C onward must accept this as background noise. New tests SHOULD wrap synchronous Zustand dispatches in `act()` (Stage B's pattern in App.test.tsx); they should NOT chase the layout-effect warnings. Worth promoting to a new `docs/gotchas.md` entry (#32) at the M03 closeout. |
| `vitest --coverage` measures `src/components/GraphCanvas.tsx` functions at 33.33% (1/3 covered) even though line coverage is 100% | The `(_, node) => selectNode(node.id)` and `() => selectNode(null)` event handlers are inline arrow functions on `<ReactFlow>`'s `onNodeClick` / `onPaneClick` props; v8 coverage counts them as separate functions. Unit tests don't simulate React Flow node-click events (which would require panning + dragging in happy-dom); the `selectNode` action itself IS covered via direct invocation in `graphStore.test.ts::selectNode_sets_selectedNodeId`. | Decision: leave function coverage at 33% for GraphCanvas; the inline handlers are pure delegates to `selectNode` (which is covered). Stage D's inspector-panel work will exercise these paths via component-tests of the click → inspector-pane flow; revisit then. The global threshold is line ≥80%, which holds at 93.47%. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rules §4 stated up front; PR/commit workflow §8 + retrospective protocol §19 + gap-analysis §20 + schemas-as-source-of-truth §14 all referenced explicitly. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | The 7-file list + 5 reference files + 1 prior-stage retrospective gave full context. The prompt's draft B.3 sample body had snake_case-vs-camelCase drift (caught early via the Stage A retro Decisions; documented as Ambiguity event) — minor friction, no rework. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated explicitly in the surface message before any test or production-code edit. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Round 1 (verify_gates): prettier --write fixed 8 files; eslint flagged 2 unnecessary type assertions (fixed); React Flow act() warnings handled via act() wrap on synchronous dispatch (downstream layout-effect warnings accepted as background noise). Round 2 not needed. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` invoked. Surface-only at session end. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Technical decisions (snake_case schema deviation from prompt's draft body, dropping `stream_thinking` from the no-op switch, accepting React Flow function-coverage at 33% for inline event delegates, act() wrap pattern, naive horizontal-stagger layout) made autonomously per §12 "own technical decisions". The schema deviation is logged for the M03 closeout `<gotchas_graduation>` rather than escalated mid-stage. |
| 7 | Was the human-direction load reasonable? | 5 | Zero mid-stage interrupts. Single bundle for approval at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | Diff stat + per-gate results + retrospective + draft commit message in one bundle. |
| **Process axis total** | | **39** | **/40** |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | `applyEvent`, `clear`, `selectNode` are named for what they do; helper functions (`nextAgentPosition`, `toolPosition`, `skillPosition`, `withAgentStatus`) are pure functions named for their role; no magic numbers (`AGENT_X_STRIDE = 220` named constant). No `any`, no `@ts-ignore`, no ad-hoc `// eslint-disable`. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | All 13 graphStore tests assert observable behavior (state shape after dispatch, idempotence, edge-target wiring, exhaustive-no-op contract). Node component tests assert rendered DOM structure + a11y attrs + Handle presence. The `every_other_AgentEvent_variant_is_a_safe_no_op` test is the load-bearing exhaustive-contract assertion — it would catch a future protocol regression where adding a new wired variant accidentally drops state for unrelated variants. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | `applyEvent` JSDoc names the idempotence + exhaustiveness contract; `NodeStatus` + the three Data interfaces are documented inline. No compile-checked example because the surface is renderer-internal (consumed only by `App.tsx` + `GraphCanvas.tsx`). |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | B.2/B.3 deliverable matched verbatim: graphStore + 3 node components + GraphCanvas + App refactor + styles + EventList/eventReducer/eventReducer.test deletion. No extra components. The `_exhaustive: never` switch IS comprehensive over all 36 variants but each non-handled variant is an explicit-no-op case label, not a handler — Stage C/D/M4+ light up the cases as the events get nodal representation. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | The graphStore JSDoc says what `applyEvent` does + why it's exhaustive; CSS comments cite spec §3 Visual Design; GraphCanvas comments name the React Flow v12 nodeTypes-stability-rule and the Zustand selector-form rule. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No hidden AI usage (footer in commit message); no magic numbers; no `any`/`@ts-ignore`; no silent error drops; no premature abstraction (the position helpers are tiny pure functions, not a layout-engine seam). |
| 7 | Is coverage ≥80% / ≥95% on safety primitives — measured, not estimated? | 5 | Workspace TS: 93.47% line (≥80%); graphStore.ts (treated as primitive): 96.37% line (≥95%); all node components + GraphCanvas: 100% line; App.tsx: 93.47% line. Branches 90.09% globally (≥80%). |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local Windows full suite green (cargo workspace tests + frontend gates). CI run pending PR open. Confidence high — Stage B is TS-only; Rust crates untouched so all M02 baselines mechanically preserved. The new TS surface is pure renderer code with no platform-specific paths. |
| **Product axis total** | | **38** | **/40** |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | All XML slots actively used. The `<gotchas>` slot's "React Flow nodeTypes outside component" + "Zustand selector form" traps were load-bearing — the implementation followed both rules from the start because the prompt named them. The `<execution_warnings>` slot's "don't bump @xyflow/react or zustand mid-stage" kept Stage A's pinned deps respected. |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | One: a `<schema_drift_check>` optional slot for stages that consume types regenerated in a prior stage. The "schema field naming snake_case vs prompt's draft camelCase" drift would have been caught at write_failing_tests time if the prompt had a literal "trust the generated TS, NOT this prompt's sample bodies, for field names" line. v1.3 protocol consideration. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15 / `docs/gotchas.md`? | 4 | The 5 stage `<gotchas>` traps were largely M03.B-specific (React Flow v12 nodeTypes, Zustand selector form, scope-creep guard, exhaustiveness-check, RTL DOM-ref staleness). The act() warnings from React Flow + happy-dom should graduate to `docs/gotchas.md` at the M03 closeout (`<gotchas_graduation>`) — they're not Stage B-specific; any future stage that adds React Flow components inside Vitest will hit them. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 5.5h, actual ~1.5h. Ratio 0.27× — well under estimate, well within S4. The estimate accounted for React Flow + Zustand learning curve; in practice the prompt was specific enough (and Stage A's deps were pre-installed) that the foundation work was straightforward. Pattern from M01: detailed Phase doc + pre-installed deps make stages run faster than the estimate. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | Two: (1) prefer Zustand `getState()` calls over selector hooks for synchronous test dispatching where you don't want React subscriptions — the test-side pattern wasn't obvious from the prompt; (2) React Flow + happy-dom act() warning behavior (already noted as a candidate gotchas.md graduation). |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | Yes. The XML slots + `<read_first>` + `<read_reference>` give full orientation; the deliverable is a strict reference to B.3 Detailed Changes; gates are objective; retrospective is template-driven. The schema-vs-prompt drift would re-occur on a fresh run, but the Stage A retro Decisions item (carry-forward) makes the resolution path explicit. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Two pre-Stage-C nudges: (1) revise §B.3's sample code body to use snake_case + drop `stream_thinking` so Stage C's draft of MCP/Gap/HITL/Plan/Task/Verify/Hook/Framework Node bodies start from a correct baseline; (2) consider promoting the "trust generated TS, not prompt-sample bodies" rule to a generic protocol-level guidance for any stage that consumes recently-regenerated types. |
| **Pattern axis total** | | **31** | **/35** |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log` shows latest commit is `bb8202e` (M03 Stage A — build hygiene + carry-forward + new deps) — no Stage B commits before user approval. Working tree dirty awaiting approval.
- [x] **G2: no Severity-5 friction events** — *evidence:* friction-events table maxes at Sev 2 (React Flow + happy-dom act() warnings). Both Sev-1 events were 30-second self-corrections.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift table records all three checked Hard Rules holding (no commit, no hand-rolled types, no scope creep). No drift.
- [x] **G4: stage actually completed** — *evidence:* B.2 "Files to Change" + B.3 "Detailed Changes" applied; B.4 acceptance criteria met; full gate suite green; 41 frontend tests pass; coverage ≥80% global with graphStore ≥95%; legacy `eventReducer.ts` + `EventList.tsx` + `eventReducer.test.ts` deleted; App.tsx refactored to Zustand store; styles.css extended per spec §3.
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest individual row is 4 (multiple); no row below 3.

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 39
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 31
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 5.5h, took ~1.5h, ratio 0.27×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage C with minor Phase doc + gotchas.md updates per Axis 3 notes (snake_case schema discipline reaffirmed for Stage C; React Flow + happy-dom act() warning candidate for `docs/gotchas.md` #32 graduation at Stage F closeout).
- [ ] Pattern is sound but rough.
- [ ] Pattern works but has friction.
- [ ] Pattern is not yet ready.

---

## [END] Coverage holdouts

Per the M02-summary "Coverage holdouts" carry-forward + Stage A retro pattern.

### Workspace gate (≥80% line)

- **Coverage actual this stage (Rust):** unchanged from Stage A baseline (Stage B is TS-only; no Rust crates touched). Stage A measured workspace 93.99%, runtime-drone 96.84%, runtime-main 99.37%. Re-running cargo llvm-cov is wasted CI cycles for a TS-only stage; full coverage delta will run in CI on PR push.
- **Coverage actual this stage (Frontend):** 93.47% line (target ≥80%); 90.09% branches; 92.3% functions; 93.47% statements.
- **Exclusions added this stage:** None added this stage.
- **Exclusion list as of stage end (workspace Rust):** `src.main\.rs|generated` (per `.github/workflows/ci.yml` workspace gate; unchanged since M02).
- **Exclusion list as of stage end (frontend `vitest.config.ts`):** `src/**/*.d.ts`, `src/main.tsx`, `src/types/**` (generated TypeScript bindings; unchanged since Stage A).

### Per-package gates (≥95% line on safety primitives)

- **Crate `runtime-drone`:** unchanged; baseline preserved (96.84% Stage A).
- **Crate `runtime-main`:** unchanged; baseline preserved (99.37% Stage A).
- **Frontend primitive `src/lib/graphStore.ts`** (treated as safety primitive — single source of graph state): **96.37% line**, 88.73% branch, 100% functions. Uncovered lines (227–228, 295–297) are the `_exhaustive: never` fallback branch (intentionally unreachable at runtime — guards against future schema additions) and one defensive parent-not-found path in `skillPosition`. Per CLAUDE.md §5: ≥95% with documented exclusions met.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline (Stage A) | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-drone/*` (all five M01-tracked modules) | preserved | unchanged (TS-only stage) | → | n/a |
| `crates/runtime-main/*` (all six M02-tracked modules) | preserved | unchanged (TS-only stage) | → | n/a |
| `src/lib/graphStore.ts` (NEW) | n/a | 96.37% | new | new safety primitive — first measurement |
| `src/components/GraphCanvas.tsx` (NEW) | n/a | 100% lines / 33.33% functions | new | inline event-handler arrow functions on `<ReactFlow>` props; covered behaviorally via `selectNode` action tests in graphStore.test.ts. Stage D's inspector-panel work will exercise the click → store flow end-to-end. |
| `src/components/nodes/AgentNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/ToolNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/components/nodes/SkillNode.tsx` (NEW) | n/a | 100% | new | n/a |
| `src/App.tsx` | M02 baseline | 93.47% | preserved | Two uncovered lines (36–38) are the renderer-side `setError(unwrapCmdError(e))` branch in `handleSetKey` — covered by error-path test but the unhandled-promise branch isn't reached because the test resolves OK. Same pattern as M02; preserved. |

All M02 + Stage A per-module baselines preserved. Stage B's net change is +1 safety primitive (graphStore.ts at 96.37%) + 4 new 100%-covered components.

### CI workflow drift check

- [x] No `--ignore-filename-regex` change required. Stage B is purely additive to the renderer surface; the existing exclusions cover the same set of files.

---

## [END] Decisions for the next stage

Stage C inherits a working React Flow + Zustand foundation + 3 basic node types + per-spec §3 visual baseline. Per the prompt's special_log items:

### `CLAUDE.md` updates

- [ ] No `CLAUDE.md` content changes recommended at this stage. The React Flow + happy-dom act() warning surface should be added to `docs/gotchas.md` as #32 at the M03 closeout (`<gotchas_graduation>`) — not a Stage C prerequisite.

### `TEMPLATE.md` updates

- [ ] No template changes recommended at this stage. Consider for v1.3 (post-M03): a `<schema_drift_check>` optional slot for stages that consume types regenerated in a prior stage; Stage A → Stage B's "snake_case schema vs. camelCase prompt-sample" was the second consecutive stage to surface this drift pattern (Stage A's was the spec's "10 v0.1 variants" stale enumeration vs. the actual 36-variant Rust enum).

### Milestone document updates (M03-live-graph.md)

- [ ] **§B.3 sample code body should be revised** before this milestone PR opens. The current sample uses camelCase (`event.agentId`, `event.parentId`, etc.) and references a non-existent `stream_thinking` variant. The actually-shipped graphStore.ts uses snake_case throughout (matching the schema-generated TS) and treats only `stream_text` + `decision_record` + `session_start` (no `stream_thinking`) as the documented no-op trio. Same drift root cause Stage A retro flagged for §A.3.
- [ ] **§C.3 sample code bodies (the 8 remaining node types) should pre-emptively use snake_case** for any AgentEvent fields they consume — same lesson, applied forward.

### Stage C inheritances (load-bearing)

- **`nodeTypes` map in `GraphCanvas.tsx`** is the seam Stage C extends. Stage C adds 8 new entries (mcp/gap/hitl/plan/task/verify/hook/framework) to the same module-level constant. Continuing to define `nodeTypes` outside the component remains load-bearing for streaming UX.
- **Edge-animation pattern Stage C will use:** **CSS-driven, not custom edge components.** Stage B's edge `data.kind` field already discriminates between `agent-spawn` / `tool-call` / `skill-load`. Stage C should add `animated: true` on `tool_invoked`-created edges (and flip to `animated: false` on `tool_result`) to leverage `@xyflow/react`'s built-in `react-flow__edge--animated` class. Pure CSS handles the dash-flow visual. Custom edge components are reserved for M4+ if a need surfaces (per spec §3 token-spend visualization).
- **Background + Controls toolbar feels right for spec §3 visual design.** The `<Controls />` component (zoom/pan/fit-view) is unobtrusive; `<Background />`'s default dot pattern reads as the "dark canvas" the spec calls for. Stage C should keep both. Stage D may add `<MiniMap />` if user-testing surfaces a navigation need at high node-count.
- **AgentNode / ToolNode / SkillNode CSS structure CAN be reused for the remaining 8 node types** with minor variant additions: GapNode + HITLNode get prominence styling (pulsing border + higher z-index + amber/white accents per spec §3 Color Encoding); MCPNode gets a tool-count badge (Stage C extends `ToolNodeData` with `source: 'mcp'` discriminator); PlanNode + TaskNode + VerifyNode + HookNode + FrameworkNode all share the AgentNode shape with their own border-color assignments. No CSS structural refactor needed; just additive `.X-node` + `.X-node--{state}` rules.
- **Zustand discriminated-union exhaustiveness check held under Stage C's likely additions.** The `_exhaustive: never` switch's existing 30 no-op cases mean Stage C can promote any of them to wired cases (e.g., `session_start` → spawn FrameworkNode; `tool_invoked` with `source: 'mcp'` → lazily spawn MCPNode parent group; `hitl_requested` → spawn HITLNode) without triggering the exhaustiveness alarm. No store refactor expected.

### Protocol additions

- [ ] None applicable at this stage.

### Open issues to file

- [ ] None. All carry-forwards captured here, in the M02 + Stage A gap-analysis Carry-forwards, and in the M03 closeout's gotchas-graduation slot (Stage F).

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff stat + draft commit message. If discrepancies surface, push back here and Claude revises.

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M03 Stage `B`, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m03-live-graph`; the PR drafts and pushes only at the end of the final stage (Stage F).

**Claude:** I have completed M03 Stage `B` per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until Stage F.

**Surfaced at:** 2026-05-05 ~18:00 UTC.
