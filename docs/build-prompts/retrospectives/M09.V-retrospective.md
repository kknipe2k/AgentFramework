# M09.V — Verifier Retrospective

**Date:** 2026-06-09
**Verifier session ID:** session_<not-exposed-in-this-context>
**Milestone phase doc:** docs/build-prompts/M09-workbench-vertical-slice.md
**Iteration:** 1
**Prior V iteration retros:** N/A
**Cross-machine state at start:**
  git log --oneline main..HEAD: HEAD at `73d2f3a docs(M09.D): flip the execution-status slice row — re-IRL passed (2026-06-09)`; the full A→B→C→D→D.fix(iter1)→D.fix(iter2) chain present, each code stage a strict two-commit `test(...)` → `feat/fix(...)` pair.
  ls docs/build-prompts/retrospectives/M09.*-retrospective.md: M09.A, M09.B, M09.C, M09.D, M09.D.fix, M09.D.fix2 (present — deliberately NOT read; bias guard).

## Per-pass summary

| Pass | Time | Findings | Notable |
|---|---|---|---|
| Inventory | ~10 min | 0🔴 / 0🟡 / 0🟢 | All four surfaces present + shape-match A.3/B.3/C.3/D.3 (`nextAgentRef` + "+ New agent" item; `FileAccessEditor` + `builderAgent.capabilities`; `mcp_list_server_tools` + `source:'mcp'` palette + `pushAgentTool`; `vertical_slice.e2e.ts`). |
| Wire | ~20 min | 0🔴 / 0🟡 / 0🟢 | The authoring→serialize→enforce trace holds at every step, incl. the two D.fix repairs (model-facing tool-def injection; the dispatcher's own framework-/tier-wired enforcer). No break. |
| Behavior | ~20 min | 0🔴 / 0🟡 / 2🟢 | Ran the assembled evals myself — the on-disk file lands from real MCP data; out-of-scope/tier denials leave no file. Two 🟢 on assertion-strength + a deferred truthfulness gap. |
| Multi-call invariants | ~10 min | 0🔴 / 0🟡 / 0🟢 | `updateNode` round-trips `file_access` (ADR-0020 re-derive); `nextAgentRef`+`addNode` id-guard never collides; `pushAgentTool` appends (idempotent on both lists). |
| Assembled execution (5th) | ~15 min | — | DROVE THE REAL TAURI APP — 5/5 assertions green across the 4 M09 specs on the real WebView2 binary. |

## Findings

#### 🟢 #1 — Behavior: `vertical_slice.e2e.ts` run-assertion is deliberately weak

**Spec claim:** D.4 BDD — the assembled e2e asserts "a real file is written within scope … and an out-of-scope write is denied."
**Observed:** `vertical_slice.e2e.ts` test 2 (`the_authored_framework_runs_through_the_assembled_tester`) asserts only `verdict.getText().trim().length > 0` — it passes on PASS, FAIL, or TIER-LIMITED alike. In my real-app run (key loaded via `.env.local`, hermetic-Novice seed) the verdict came back **TIER-LIMITED**, and the test passed. The on-disk file effect is NOT asserted by any `tests/e2e-tauri/` regression.
**Expected (per spec):** an on-disk-effect assertion in the assembled regression.
**Trace / harness:** real `tauri-driver` run + read of the spec.
**Disposition:** Not a defect. The on-disk effect at Promoted is intentionally maintainer-IRL only (the e2e harness `seedNoviceTier()`s for hermeticity, so a Write is L4 tier-limited and never lands; CI has no live model/MCP server). The on-disk behavior IS regression-guarded — at the assembled-Rust granularity by `mcp_tool_injection_execution.rs` (file lands with MCP-sourced content + causality guard + authored-only) and the `test_framework_with` tier-contrast tests (no file on denial). Same over-weak-Tester-e2e-assertion class as TD-049. **Action:** log to `docs/tech-debt.md` (observability/test-assertion completeness) — consider strengthening the run-test to assert verdict ∈ {PASS, TIER_LIMITED} once a Promoted-capable hermetic path exists.

#### 🟢 #2 — Behavior: a gap-suspended run still folds to verdict `Pass`

**Spec claim:** M09 ships "suspends cleanly" (E-04); the D.fix iter1 note flagged that a `request_capability` gap-suspend read as a green PASS.
**Observed:** `fold_outcome` (tester.rs:184) has no suspend arm — a clean gap-suspend (no `CapabilityViolation`/`TierViolation`) yields `verdict: Pass`. `TestVerdict::TierLimited` (TD-047, resolved) only covers L4 tier blocks, not gap-suspends.
**Expected (per spec):** a suspended run should not read as a clean PASS.
**Trace / harness:** code read of `fold_outcome` + the `TestVerdict` enum.
**Disposition:** Explicitly OUT of M09 scope — the M09.D.fix `<scope_locks>` says "the verdict-shows-PASS-on-gap-suspend truthfulness gap is OUT of scope here (tech-debt / M10) — note it; do not change verdict logic." Correctly deferred. **Action:** no TD ledger entry currently exists for it (TD-047 is the adjacent, resolved tier-limit case); log to `docs/tech-debt.md` and route to M10 (the gap-resolve→resume slice) so the deferral is tracked, not just named in the phase doc.

## Verification axes scoring

| Axis | Score (1–5) | Rationale |
|---|---|---|
| **Coverage adequacy** | 5 | All four V.2 surfaces checked across all five passes; the 5th pass drove the real binary; the on-disk effect (the one surface the real-app run couldn't reach hermetically) was grounded by running the assembled Rust eval directly. |
| **Finding signal-to-noise** | 5 | No false positives; both 🟢s trace to a concrete file:line and are correctly classified as in-design / out-of-scope, not defects. |
| **Fresh-context discipline** | 5 | Clear-and-paste held; the six M09 retros + summary + gap-analysis were not read; `<read_first>` honored. |

Sum 15 / avg 5 — above the soft signal.

## Standing-rule compliance (v1.8 — M06.V Decisions 6 + 7)

- **Decision 6 — delivered+tested / driver-absent / accepted-ADR carry-forward → 🟡 + enumeration:** Not triggered — the one undriven-by-me path (the Promoted on-disk write) is covered by the maintainer IRL (2026-06-09) + the assembled Rust eval, not absent. No `<wire_trace_vs_adr_reconcile>` / `<scope_change>` blocks in this milestone.
- **Decision 7 — `--features integration` reference-MCP-server smoke:** N/M — no live MCP server in this environment; the MCP dispatch path was exercised through the assembled `MockTransport`/`StubMcpRead` dispatcher evals (Promoted⇒Invoked / Novice⇒Blocked) which I ran green.

## Outcome (one-line marker)

**Sound** — all five passes produced clean signal; no 🔴, no 🟡; two 🟢 (one in-design, one correctly-deferred). Merge recommendation: **proceed to closeout**.

## `[END] Decisions for D.fix or next milestone`

- Decision 1: 🟢 finding #1 → log to `docs/tech-debt.md` (test-assertion completeness, TD-049 class); optionally strengthen `vertical_slice.e2e.ts` test 2 to assert `verdict ∈ {PASS, TIER_LIMITED}` rather than non-empty text.
- Decision 2: 🟢 finding #2 → log to `docs/tech-debt.md` (Tester verdict truthfulness — gap-suspend reads PASS) and route to M10 (gap resolve→resume); the deferral is named in the M09.D.fix phase doc but not yet ledgered.
- Decision 3: no V-protocol refinement needed — the five-pass + real-app-drive flow worked; the harness (`.env.local` key + `seedNoviceTier`) made the real run reach `test_framework` and return a live verdict, which is a meaningful real-app observation that the composed authored→serialize→run path executes.

## Cross-machine state at end

**Cross-machine state at end:**
  git log --oneline main..HEAD: unchanged from start (HEAD `73d2f3a`); this retro is pending (not committed — awaiting maintainer approval per CLAUDE.md §8).
  ls docs/build-prompts/retrospectives/M09.*-retrospective.md: M09.A, M09.B, M09.C, M09.D, M09.D.fix, M09.D.fix2, **M09.V (this file)**.
