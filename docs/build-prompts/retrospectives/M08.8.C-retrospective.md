# M08.8.C — Stage Retrospective

> **Stage:** M08.8 Stage `C` — Tier in the run loop (TD-036) + tier-UI truth — see `docs/build-prompts/M08.8-design-workbench.md`
> **Parent milestone:** M08.8 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-04
> **Session end:** 2026-06-04
> **Branch:** `claude/m08.8-design-workbench`
> **Starting commit:** `3fd68d4` (after fast-forward to origin — the alpha re-letter)
> **Ending commit:** red `476eb45` + impl `c15c6a8` + docs (this retro / ADR-0030 / CHANGELOG / TD-036) — uncommitted docs awaiting approval
> **Estimated effort (per prompt):** ~2–3 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules honored throughout (esp. Hard Rule 8 — surfaced the capability-enforcement plan before coding)
- [x] All Stage C "Read first" files read (phase doc Stage C, TD-036/TD-034, tester.rs, enforcer.rs, commands.rs, SettingsPanel.tsx, ADR-0019, cluster-pattern §5, ADR-0021)
- [x] Deliverable + test plan stated and the ADR plan surfaced before code; user approved both forks (smoke-exclusion; ADR-0030)
- [x] Branch correct (`claude/m08.8-design-workbench`); fast-forwarded to origin first (resolved a stale-phase-doc contradiction)
- [x] `git status` clean of tracked changes at start

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Orientation | The pasted prompt was labelled `M08.8.C` (tier) but my local phase doc had C=Budget / E=Tier — a hard contradiction. Surfaced it; the user revealed origin was ahead (alpha re-letter `2912638`) and I fast-forwarded. | The §8 phase-doc-edit pre-flight already covers this; the stale local doc was the cause, not the prompt. Working as designed — I surfaced instead of picking. | 3 |
| Impl (green run) | The Novice assembled test's `!outcome.passed` assertion was false against the documented `fold_outcome` contract (only L1 `CapabilityViolation` folds; L4 `TierViolation` does not). Discovered when the test ran green in impl. | The red phase should have checked the fold contract before asserting `!passed`. Corrected in the red commit (amended, unpushed) to assert the true contract; disclosed in the surface. | 3 |
| Mutation gate | cargo-mutants produced only 2 mutants on the wire, both unviable (`TestOutcome: !Default`) — a vacuous pass. Added a hand-mutant (ignore the threaded tier → pass Novice) to prove the wire is behaviorally pinned (Promoted test fails). | The mutation gate on a thin delegation needs a manual wrong-value mutant; cargo-mutants doesn't model call-arg substitution. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Pasted prompt (`id=M08.8.C` = tier) vs local phase doc (C=Budget, E=Tier) | Which stage / which work | Fast-forwarded to origin (`3fd68d4`); the alpha re-letter made C=Tier — the prompt was correct. | Local-machine staleness; the §8 pre-flight + the user's cross-machine paste resolved it. No doc fix (orch owns closeout reconciliation of the C.2/C.4 "smoke" naming). |
| Phase doc C.2/C.4 name "test_framework **+ run_smoke_session**" | Whether to wire smoke | Surfaced the grounded reality (smoke has no enforcer, no tools); user chose Tester-only; documented in ADR-0030. | Phase doc C.2/C.4 — flagged to orch for closeout reconciliation; not edited mid-stage (false-premise text). |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Orientation | The C-vs-E stage contradiction | Yes — user supplied the missing cross-machine state (origin ahead) | Right — before any code |
| Pre-coding | The full plan + the ADR-0030 plan + the smoke-exclusion fork (Hard Rule 8) | Yes — two clear forks with recommendations | Right — before code, per Hard Rule 8 |
| Red phase | The 3 red artifacts + right-reason evidence (E0061 + label mismatch) | Yes | Right — red committed, surfaced for red-phase approval per strict v1.8 |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §2 (surface contradictions, don't pick) | The C/E stage mismatch could have been silently "picked" | Yes — surfaced, did not pick | The exact failure mode §8 phase-doc pre-flight guards against (cf. M04 PR #53) |
| Strict-TDD byte-identical test block | A pre-existing test (clean-run) + a false Novice assertion needed test-file changes during impl | Yes — amended the unpushed red commit for the assertion fix; the clean-run arg is the single disclosed mechanical co-change | Behavioral tier tests are byte-identical red→impl (verified by scoped diff) |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The tier seam already existed (`run_test_session_with_tier`) | I expected to touch `tester.rs`; the wire was purely in the Tauri command | The "painted, not wired" diagnosis (TD-036) was exact — the gap was one delegation call |
| `fold_outcome` does not fail on `TierViolation` | A tier-denied run reports `passed=true` | Defensible (the framework is well-authored; the user's tier is low) — but worth noting; it sharpened the Promoted (passed=false) vs Novice (passed=true) contrast |
| Both cargo-mutants candidates unviable | `TestOutcome: !Default` | Thin-delegation wires need a hand-mutant to get real mutation signal |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Deferred to the maintainer IRL + assembled regression | `tests/e2e-tauri/tier_enforcement.e2e.ts` (UI-truth: topbar chip + truthful label) + the assembled `commands.rs::tester_backend` tier tests (the enforcement contrast) | `fold_outcome` does not fold `TierViolation` into a test failure — noted in the test + retro, not a defect (defensible contract); smoke-exclusion documented in ADR-0030. No propagation to M08.9. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | Hard Rule 8 + §2 contradiction-surfacing both fired correctly |
| 2 | "Read first" list oriented correctly? | 5 | Every pinned `file:line` matched reality |
| 3 | Deliverable + test plan before code? | 5 | Plan + ADR plan surfaced and approved pre-code |
| 4 | Self-correct in ≤3 rounds? | 5 | The Novice-assertion + mutation-gate issues resolved in one pass each |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed only after explicit "red commit first" authorization; docs uncommitted pending approval |
| 6 | Escalated the right things? | 5 | Surfaced the stage contradiction + the smoke/ADR forks; proceeded autonomously on the wire |
| 7 | Human-direction load reasonable? | 4 | Two clarifications (stage identity; forks) — both genuinely user-domain |
| 8 | Surfaces actionable? | 5 | Forks had recommendations; red surface had right-reason evidence |
| **Process axis total** | | **39/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | Mirrors existing seams (`run_test_session_with_tier`, `#[allow]` reason) |
| 2 | Behavior tests, not tautology? | 5 | Asserts the violation KIND contrast + the file-on-disk side effect (rule 11 / §4) |
| 3 | Public APIs documented? | 5 | `test_framework_with` / `test_framework` doc-comments updated; ADR-0030 |
| 4 | Exactly promised — no creep/cut? | 5 | Tester wired; smoke excluded with rationale; nothing else touched |
| 5 | Stranger-readable? | 5 | The doc-comments + ADR-0030 explain the L4/L1 gate ordering |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers / stringly types / silent drops |
| 7 | Coverage measured? | 5 | workspace 91.88% ≥80; runtime-main 96.27% lines ≥95; vitest 93.51% |
| 8 | CI-green prediction? | 4 | All gates run locally green; the real-app `e2e-tauri` close is the maintainer IRL |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | — |
| 2 | Sections needed but missing? | 4 | The "hand-mutant for a thin-delegation wire" pattern could be a gotcha |
| 3 | Gotchas local or generalize? | 4 | The unviable-mutant-on-delegation → hand-mutant pattern generalizes |
| 4 | Time-box matched? | 5 | Within estimate |
| 5 | Implicit protocol moments? | 4 | The "false red assertion discovered at green" → amend-red flow is worth codifying |
| 6 | Re-runnable by a fresh session? | 5 | The wire is deterministic; the pinned seams are exact |
| 7 | Prompt format need changes? | 5 | No |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red committed only after the explicit "red commit first" authorization; the impl was committed after "proceed to the impl commit"; the docs (ADR/CHANGELOG/retro/TD) are uncommitted pending final approval; nothing pushed (origin clean at `3fd68d4`).
- [x] **G2: no Severity-5 friction** — max severity 3.
- [x] **G3: no unaddressed protocol drift** — both drift events self-corrected.
- [x] **G4: stage completed** — red `476eb45` + impl `c15c6a8`; all assembled + frontend gates green; the real-app IRL is the authoritative close (rule 11), pending the maintainer watch.
- [x] **G5: every row ≥3** — lowest is 4.
- [x] **G6: CI-parity** — gates run with the exact §6 commands (no divergent flags): `cargo fmt --all`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test -p agent-runtime`, `RUSTDOCFLAGS=... cargo doc`, `cargo audit`, `cargo deny check`, `cargo llvm-cov --workspace ... --fail-under-lines 80`, `cargo llvm-cov --package runtime-main ... --fail-under-lines 95`, `npx prettier --check`, `npx eslint .`, `npx tsc --noEmit`, `npm run test`. No `--skip` / `--test-threads` / `--features` / `--target` divergence.

### Soft gates

- [x] **S1:** Process 39/40 ≥30
- [x] **S2:** Product 39/40 ≥32
- [x] **S3:** Pattern 32/35 ≥25
- [x] **S4:** Time-box within 2×
- [x] **S5:** ≤3 Severity-3 events — 2 Severity-3

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. The real-app IRL is the remaining authoritative close (rule 11) — not a gate failure, the by-design human-watch step.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.88%
- **Exclusions added this stage:** None added.
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged).

### Per-package gates (≥95% line)

- **Crate:** runtime-main — **Coverage actual:** 96.27% lines (unchanged — no runtime-main code touched this stage; the seam `run_test_session_with_tier` already existed). **Exclusions added:** None.

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `src-tauri/src/commands.rs` | (tauri-shell patch-gate 50%) | 67.62% lines | → | New `test_framework` tier read is an OS-touching command wrapper (untestable without AppHandle — the tauri-shell holdout); `test_framework_with` tier-threading IS covered by the 3 assembled tests. Above the 50% patch gate. |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage; the §6 commands match `.github/workflows/ci.yml`.

---

## [END] Decisions for the next stage

### `CLAUDE.md` updates

- [ ] (Optional) Consider a gotcha: a thin-delegation wire produces only unviable cargo-mutants (e.g., `Ok(Default::default())` when the return type is `!Default`); use a hand-mutant (wrong value into the call) to get real mutation signal.

### Milestone document updates

- [ ] Orchestrator to reconcile the phase doc C.2/C.4 "smoke" naming at closeout (smoke intentionally excluded per ADR-0030 — false-premise text, not edited mid-stage).

### Open issues to file

- [ ] (Optional) Evaluate whether `fold_outcome` should fold `TierViolation` into a test failure or surface it distinctly in the Tester UI (currently a tier-denied run reports `passed=true`). Defensible today; a product-surface question for the Tester results UI (Stage F / M09).

### Execution-status ledger

- [ ] On the maintainer IRL (a Promoted run enforces Promoted; an out-of-scope Write shows the scope-gate `CapabilityViolation` in the app), flip the execution-status "observed in app" status for the tier surface and update row 1's "real-app scope-watch deferred (TD-036)" note. NOT flipped now (rule 11 — not yet observed in the running app).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M08.8 Stage `C`, per `CLAUDE.md` §19.

**Claude:** I completed M08.8.C per the stage prompt: the Tester run loop now enforces the user's tracked tier (TD-036), the tier label is truthful (#20), and #19's root (UI-tier ≠ enforced-tier) is fixed at the backend. The BLOCKING mutation gate passed (cargo-mutants 2 unviable; a wrong-tier hand-mutant is caught by the Promoted test). ADR-0030 (Proposed) refines ADR-0019; smoke is intentionally excluded (documented). Strict v1.8 two-commit TDD held — behavioral tier tests byte-identical red→impl, the one mechanical clean-run arg disclosed. I have NOT pushed; the docs commit is pending approval. The real-app IRL (a Promoted run enforces Promoted) is the authoritative close per rule 11.

**Surfaced at:** 2026-06-04
