# M03.5.B — Stage Retrospective

> **Stage:** M03.5 Stage `B` — see `docs/build-prompts/M03.5-pre-m04-prep.md`
> **Parent milestone:** M03.5 (pre-M04 prep, doc/protocol-only mini-milestone). Final stage.
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-07 ~mid-day (continuation after Stage A approval)
> **Session end:** 2026-05-07 ~mid-day (single short session)
> **Branch:** `claude/m03.5-pre-m04-prep` (parent-milestone feature branch; Stage A's commit `6e36a3e` already on the branch)
> **Starting commit:** `6e36a3e`
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 3.5 hours
> **Actual elapsed:** ~30 minutes (well under the conservative 3.5h estimate; doc/protocol-only stage with deterministic edits to a single file plus two small companion edits)

---

## Pre-flight (claimed before session start)

These were satisfied before code was written:

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were honored (no commit-without-approval; no main push; no telemetry; etc.)
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M03.5-pre-m04-prep.md Stage B sections, docs/gotchas.md, RETROSPECTIVE-TEMPLATE.md)
- [x] Read-prior-stages was honored — Stage A's retrospective `[END] Decisions for the next stage` section was read first and the two carry-forward inputs (`<fan_out_grep>` value-consistency expansion + `<runtime_environment>` PowerShell-via-bash note) were applied directly into the v1.3 tag descriptions
- [x] The deliverable was stated in 1–3 sentences before writing code (5 new tag sections + 3 anti-patterns + v1.3 changelog entry + CHANGELOG note + README row update)
- [x] Test plan section was N/A per `<test_plan_required>false</test_plan_required>` — verification commands ran post-implementation per B.4
- [x] The branch was correct (`claude/m03.5-pre-m04-prep`)
- [x] No code-relevant uncommitted changes carried over (`git status` showed only the untracked Stage A retrospective, which Stage B will fold into its own commit — see protocol-drift events below)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Verification step (post-implement) | Stage A's B.4 verification regex `^### .pre_flight_check` (and its 4 siblings for the other v1.3 tag headings) was authored with one wildcard char between `### ` and `pre_flight_check`, but the actual heading format `### \`<pre_flight_check>\`` has TWO chars there (backtick + `<`). The patterns as written never match. Confirmed by running both the buggy regex (0 matches) and a corrected regex (1 match) for each of the 5 headings. | The phase doc B.4 patterns should be `^### \`<pre_flight_check>\`` (literal heading match) instead of `^### .pre_flight_check`. This is a Stage A authoring artifact frozen in commit `6e36a3e`; per §10 not retroactively edited. | 2 |
| Verification step (post-implement) | Final B.4 check pattern `first milestone authored on the v1\.3` returned 0 matches under ripgrep (case-sensitive default) but 1 match with `-i`. PowerShell Select-String is case-insensitive by default, so the phase doc's literal command would have passed. The string in README.md actually starts with capital `F`. Cost ~30 seconds + one re-run. | The phase doc could have used the case-as-written pattern `First milestone authored on the v1\.3` for ripgrep portability, OR the README text could match the lowercase pattern. Stage A authoring artifact again. | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc B.3 instructions for `scripts/validate-stage-prompts.py` extension vs. actual repository state | The phase doc B.3 instructs: "If the validator script is structured such that adding the five new tag names to the recognized-tags list is a one-line change per tag (and doesn't require new validation logic), do it in this stage. If the script is more complex (e.g., requires schema-aware AST traversal), defer to a v1.4 follow-up so v1.3 ships clean." But `scripts/validate-stage-prompts.py` does not exist in the repository — never authored. The Phase doc treats the validator as if it existed; it doesn't. The execution_warning's "DO add a TODO comment in scripts/validate-stage-prompts.py" assumes the file exists. | Defer the validator extension to v1.4 entirely (not just the new tags) — the script needs to be authored from scratch, well over the <30-line additive threshold per B.3's decision criterion. The TODO-comment fallback is also moot (no file to write into). Documented in this retro as a Decision for M04 / v1.4. | The phase doc B.3 + STAGE-PROMPT-PROTOCOL.md §11 ("A validation script lives at `scripts/validate-stage-prompts.py`") both treat the validator as existing infrastructure when it's actually still a TODO. Surface for M04 prompt author: validator script authoring is a v1.4 deliverable that should be flagged on M04's roadmap row. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| End of stage (this surface) | Diff stat + 10 verification results + retrospective + M03.5-summary.md + draft commit message + draft PR description + explicit "I will not commit until you approve" | Yes — single approval gate covering both stage commit + push approval + PR-draft review (PR creation deferred to explicit user ask) | Right per CLAUDE.md §8 — surface-then-approve flow. Stage B is the final stage of M03.5, so this surface also gates the milestone PR. |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §19 Retrospective Protocol — Stage A's retrospective should have been committed with the Stage A commit | `git status` revealed `docs/build-prompts/retrospectives/M03.5.A-retrospective.md` is untracked even though Stage A's commit message references it. Stage A landed (commit `6e36a3e`) without the retrospective file in the commit. | Yes — folding the Stage A retrospective file into Stage B's commit so the milestone branch is internally consistent at PR time. The Stage A commit is still valid on its own (the retrospective is the per-stage protocol artifact, not load-bearing for the doc-PR contents); fixing it via Stage B avoids rewriting history. | Surface candidate for `CLAUDE.md` §19 / `RETROSPECTIVE-TEMPLATE.md` — the per-stage workflow should explicitly include the retrospective `.md` file in the staged-files list. Equivalent enforcement is naturally available via `<pre_flight_check>` v1.3: a check `git diff --name-only HEAD~1 HEAD must include retrospectives/M[NN].<X>-retrospective.md` would catch this at the next stage's pre-flight. M04's first stage should pin this. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Both Stage A friction inputs (`<fan_out_grep>` value-consistency case + `<runtime_environment>` PowerShell-via-bash case) folded directly into the v1.3 tag descriptions without changing tag semantics or worked-example structure | The Stage A retrospective's Decisions section was specific enough that the carry-forward changes were one-paragraph extensions to the existing tag descriptions rather than design changes. The retrospective protocol's "Decisions for the next stage" surface served as a structured input channel — exactly its purpose, but rare to see it work this cleanly in a single round. | The retro Decisions section is high-leverage when the upstream stage produces specific, applicable findings. Validates the per-stage retrospective as a forward-information mechanism, not just a backward-assessment artifact. |
| The phase doc's two B.4 regex bugs (one match-too-narrow, one case-sensitivity) were caught by the verification step, but pointed at Stage A's authoring rather than Stage B's implementation | Stage A's verification step ran 22 patterns, all of which passed at Stage A time. Stage B's verification ran 10 patterns, 6 of which were Stage A-authored phase-doc text and 2 of which surfaced typos. The typos were latent — they would have surfaced whenever the patterns first ran. | Phase doc verification commands are themselves authored content and should themselves be tested at authoring time. Stage A-authored stage-B verification commands are a class of latent-defect surface that didn't receive the same scrutiny as the deliverable text. v1.4 candidate: a dry-run check that the verification regexes themselves match exactly the headings/text the deliverable is supposed to produce. |
| The validator script's nonexistence — Stage B's optional extension was structurally impossible because the file isn't there | The phase doc and STAGE-PROMPT-PROTOCOL.md §11 both reference `scripts/validate-stage-prompts.py` as if it exists. The v1.2 changelog says "v1.2 ships lean. Structural checks are errors..." — describing intended validator behavior. But the validator was never authored; it's still a v1.2 carry-forward to v1.4. | Spec drift between protocol-document narrative and repo state. Worth flagging in M03.5 carry-forward to M04: the "validator self-test" gate that v1.3 promises depends on a file that doesn't exist. M04's prompt author should treat validator authoring as a v1.4 deliverable, not v1.3, and explicitly call this out in the M04 prompt's Pre-M04 Carry-forward section. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rules honored; §8 commit workflow + §19 retrospective + §20 append-only ledger preservation applied without ambiguity |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 5 | The 5 read-first files + 3 read-reference files covered the full surface; the read_prior_stages carry-forward from M03.5.A retrospective was the high-leverage input |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Deliverable stated in opening message; test_plan_required=false honored — verification per B.4 ran post-implement |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | One verification iteration; the two regex-issue surfaces were Stage A-authored phase-doc bugs (not Stage B implementation issues), resolved by running corrected regex on the same intent |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` invoked; surface flow about to fire |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | The validator-script-doesn't-exist ambiguity was resolved internally (defer to v1.4) without requiring a user surface — the decision criterion was unambiguous (script missing → can't extend → defer). The Stage A retrospective untracked-file drift was fixed in-stage rather than escalated. |
| 7 | Was the human-direction load reasonable? | 5 | Zero user questions during the stage; single approval gate at end |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | Final surface bundles diff stat + 10 verifications + retrospective + summary + draft commit + draft PR per the standard approval bundle in B.5 `<approval_surface>` |
| **Process axis total** | | **40/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | Doc/protocol-only edits; tag descriptions match the v1.2 archetype style (description paragraph + Schema line + Validator behavior line + worked example); changelog entries match the v1.2 entry's numbered-list shape |
| 2 | Are tests behavior tests, not tautology tests (§5)? | N/A | Doc/protocol-only stage; no tests added. Verification is structural (Grep / pattern-match) per B.4 |
| 3 | Are public APIs documented with compile-checked examples (§6)? | N/A | No new public APIs; v1.3 tags are XML schema additions documented inline |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All 5 new tag sections + 3 new anti-patterns + v1.3 changelog entry + §10 hardening rule + §11 errors/warnings bullets + §7 cross-reference sentence + CHANGELOG.md `[Unreleased]` Added subsection + README.md M03.5 + M04 row updates landed per B.3. Validator extension explicitly deferred to v1.4 (script doesn't exist; documented as a Decision below). |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | Each new tag has a self-contained description + use-case + worked example; the §13 anti-patterns name the failure shape and the discriminator vs. the right tag; the v1.3 changelog cross-references which prior friction event each tag addresses |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers, no AI usage hints, no premature abstraction; all inserts are explanatory protocol text with named M01–M03 friction sources |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | N/A | Doc/protocol-only stage — no source code touched, no coverage delta |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | N/A (proxy: 5) | Doc/protocol-only stage — no CI build delta. Local proxy that did run: prettier on `**/*.{ts,tsx,js,jsx,json}` clean. Markdown unchanged in count of headings/lists. |
| **Product axis total** | | **25/25 across applicable rows** | (rows 2/3/7/8 N/A on doc/protocol-only stage; remaining 5/8 rows = 25/25) |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | The Phase doc applied the v1.2 template cleanly; strict reference-first sections (`<deliverable ref=`, `<acceptance_criteria ref=`, `<scope_locks ref=`, `<commit_message ref=`) all resolved correctly to Phase doc sections |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | The phase doc B.4 verification commands (Stage A-authored regexes) were latent-defect surface — the regex itself wasn't validated at authoring time. v1.4 candidate: a dry-run check that pattern strings in B.4 match the deliverable text they target. Surface for `TEMPLATE.md` per Stage A's similar finding. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 5 | The Stage A friction (PowerShell-via-bash) and Stage B friction (regex bug + case sensitivity) are protocol-level, not milestone-local; they're being absorbed into v1.3 tag design (`<runtime_environment>`) and v1.4 follow-ups (verification-pattern dry-run) rather than into `docs/gotchas.md`. Correct disposition. |
| 4 | Did the time-box estimate match reality (within 2×)? | 4 | Estimated 3.5h; actual ~30 min. ~7× under-estimate, same pattern as Stage A. The conservative phase-doc estimation calibration ("~3–5h actual") is anchored to full-milestone heuristics; doc/protocol-only mini-milestone stages are closer to 0.5h actual each. Same finding as Stage A; both stages converge on this. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | Two: (a) Phase doc B.4 verification regexes should themselves be sanity-checked at authoring time (Stage A latent typo); (b) per-stage retrospective files should be in the staged-files list at commit time (Stage A's was untracked). Both addressable via v1.3 `<pre_flight_check>` once authored into M04 stage prompts. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | Yes — strict reference-first + verbatim-prose discipline + 10 deterministic Grep checks make this prompt highly reproducible. The two latent regex bugs would surface the same way; their resolution is identical. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | M04 prompt should: (a) include the retrospective file in pre-staged commit list; (b) sanity-check phase-doc verification regexes at authoring time; (c) surface validator-script authoring as v1.4 deliverable on the M04 roadmap row. All three are addressable via v1.3 tag adoption + the M04 prompt's pre-flight section. |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git status` shows 4 modified + 2 untracked (M03.5.A retro carry-over + M03.5.B retro + M03.5-summary); no `git commit` invoked
- [x] **G2: no Severity-5 friction events** — *evidence:* friction-events table has 1 row at severity 2 + 1 at severity 1; nothing higher
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* one drift event (Stage A retrospective untracked) addressed by folding into Stage B's commit
- [x] **G4: the milestone or stage actually completed** — *evidence:* all 10 acceptance criteria from B.4 verified (5 with Stage A regex-typo workaround + 5 clean); validator extension explicitly deferred to v1.4 with documented rationale; uncommitted-awaiting-approval per the surface flow
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest individual score is 4 (Axis 3 rows 2, 4, 5, 7); no rows below 3

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 40/40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 25/25 across 5 applicable rows (3 rows N/A on doc/protocol-only stage). Effective 100%; pass.
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 31/35
- [ ] **S4:** Time-box within 2× of actual — *actual:* estimated 3.5h, took ~0.5h, ratio ~0.14× (massively under-estimated; doc/protocol-only stages should estimate at PR #48 ~1h baseline). Same finding as Stage A; consistent.
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 0 severity-3 events; 1 severity-2 + 1 severity-1 event

### Outcome

- [x] **Pattern is sound but rough.** All hard gates pass; 1 soft gate (S4 time-box) failed in the under-estimate direction. Same pattern as Stage A. Address with a `TEMPLATE.md` / `docs/build-prompts/README.md` note that doc/protocol-only mini-milestones (M03.5-style) should estimate at PR #48 baseline (~1h per stage), not at parent-milestone scale. No drama; under-estimate didn't cause friction.

Outcome routing: **proceed to milestone PR push (after explicit approval) and M04 prompt authoring on a separate branch.** TEMPLATE.md / README.md update for doc-only mini-milestone estimation can land alongside M04 authoring or as a separate small follow-up; not blocking.

---

## [END] Coverage holdouts

N/A — documentation/protocol-only stage; no source code touched; no coverage gates run.

---

## [END] Decisions for the next parent milestone (M04 — Plan + Verify + HITL + Budget)

Specific changes to apply BEFORE M04 stage prompts are authored, OR specific findings M04's prompt author should fold into the v1.3 tag adoption:

### v1.3 tag adoption guidance for M04

- [ ] **High-value v1.3 tags for M04** — given M04's cross-stack risk surface (plan/task SQLite DDL + verify pipeline + HITL flow + budget enforcement integration with the Anthropic count_tokens endpoint):
  - `<pre_flight_check>` — every M04 stage. Specifically include: branch correct; prior stage retro file is in staged-files of prior commit (catches the M03.5.A drift); ANTHROPIC_API_KEY present for stages that touch budget enforcement; `cargo xtask regenerate-types --check` clean (catches schema drift before code).
  - `<schema_drift_check>` — M04 stages that touch `schemas/*.v1.json` (likely all of A, B, plan-introducing, verify-introducing). The hand-extension pattern from M03.D event.rs is the recurring M03 finding this tag exists to prevent.
  - `<dependency_audit_check>` — M04 stages that add new crates or npm deps. Per gotcha #29 + #39, every dep addition needs feature-flag + transitive-audit verification before code lands.
- [ ] **Lower-value v1.3 tags for M04** — use selectively where applicable:
  - `<fan_out_grep>` — useful when M04 renames a type or changes a wire-format field name (rare for a green-field plan/verify milestone, but the budget-integration stage may rename `count_tokens` consumers). Author per-stage; not blanket.
  - `<runtime_environment>` — useful for stages with platform-specific verification commands. Author per-stage.

### M04 carry-forward (M03.5 → M04 absorption)

- [ ] **Validator script (`scripts/validate-stage-prompts.py`) is a v1.4 deliverable, not v1.3.** STAGE-PROMPT-PROTOCOL.md §11 references the script as if it exists; it doesn't. v1.3's "validator self-test" gate is structurally a v1.4 commitment. M04's roadmap row (or M04 closeout's gap-analysis entry equivalent if M04 is the next code-shipping milestone) should call this out. Recommendation: open a tracking issue at M04 prompt-authoring time.
- [ ] **Phase doc verification regex sanity check.** Stage A authored two B.4 regexes that were latent typos (one match-too-narrow, one case-sensitivity drift between PowerShell and ripgrep). Future phase docs should run their own verification regex against expected matches at authoring time. Candidate slot: the milestone-author-side check before phase doc commits.
- [ ] **Stage A retrospective untracked drift** — the Stage A commit `6e36a3e` did not include `docs/build-prompts/retrospectives/M03.5.A-retrospective.md` even though the commit message references it. Stage B folds the file into its own commit. Future stages should explicitly stage the retrospective file as part of the per-stage commit; the v1.3 `<pre_flight_check>` tag's first M04 use case can include `git status` showing zero untracked retrospective files.

### `CLAUDE.md` updates

- [ ] None recommended at CLAUDE.md level. The findings here are protocol-level (v1.4 follow-up) or template-level (below).

### `TEMPLATE.md` updates

- [ ] Note in `docs/build-prompts/TEMPLATE.md` (Estimation calibration section): doc/protocol-only mini-milestones (M03.5-style) should estimate at the PR #48 ~1h-per-stage baseline, not the parent-milestone heuristic. Consistent finding across both M03.5 stages.
- [ ] Note in `docs/build-prompts/TEMPLATE.md` (X.4 Tests / Verification section): when phase doc verification commands include regex patterns, the patterns should be sanity-checked against the expected deliverable text at phase doc authoring time. Latent-defect surface caught in M03.5.B.

### Milestone document updates (M03.5-pre-m04-prep.md, frozen)

- [ ] None — both Stage A and Stage B executed cleanly against the phase doc as written. The two B.4 regex typos in Stage B's verification commands are Stage A authoring artifacts, frozen in commit `6e36a3e`; per CLAUDE.md §10 don't-touch-zone, the phase doc is not retroactively edited. The findings are documented in this retro and absorbed into M04 carry-forward.

### Protocol additions (v1.4 candidates)

- [ ] **Validator script authoring** — `scripts/validate-stage-prompts.py` to enable the structural checks STAGE-PROMPT-PROTOCOL.md §11 describes. Both v1.2 and v1.3 protocol docs reference the script as if it exists; closing this gap converts §11 from intent to enforcement.
- [ ] **Verification-regex dry-run** — at phase doc authoring time, run each B.4 / X.4 regex against the deliverable text it targets to catch typos before they ship. Could be a small `xtask phase-doc-check` or similar.
- [ ] **Pre-flight retrospective-file staging check** — first natural use of `<pre_flight_check>` in M04: `git status` includes `retrospectives/M[NN].<X-1>-retrospective.md` in tracked files (catches the Stage A drift pattern).

### Open issues to file

- [ ] None at M03.5 milestone-PR push time. Findings absorbed into the M04 prompt-authoring carry-forward list above. M04's prompt author handles the absorption when authoring the M04 phase doc.

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff stat + draft commit + draft PR. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- (Empty until user reviews.)

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M03.5 Stage B (final stage of the M03.5 mini-milestone), authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone branch (`claude/m03.5-pre-m04-prep`); after explicit approval Claude pushes the branch and surfaces the PR draft for review (PR creation waits for explicit user go-ahead).

**Claude:** I have completed M03.5 Stage B per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message + the parent-milestone summary + the draft PR description for user approval. After approval I will commit, push the milestone branch, and re-surface the PR draft for explicit go-ahead before opening the PR.

**Surfaced at:** 2026-05-07 (single-session)
