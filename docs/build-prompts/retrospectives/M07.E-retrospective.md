# M07.E — Stage Retrospective

> **Stage:** M07 Stage `E` — Builder Import panel renderer + ADR-0015
> enriched `import_artifact` IPC return for the §M7 review primitive.
> See `docs/build-prompts/M07-registry-import.md` §E.
> **Parent milestone:** M07 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-19
> **Session end:** 2026-05-19
> **Branch:** `claude/m07-registry-import` (parent-milestone feature branch)
> **Starting commit:** `06ac7c0` (M07.D2 retro precision amendment; HEAD before this stage)
> **Ending commits:** red/test `42186c1` (amended 3×); impl/green `f0a7721`
> **Estimated effort (per prompt):** 5–6 h
> **Actual elapsed:** ~6 h (≈1.0×, in band)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in force (no push without approval; strict-TDD two-commit; no Co-Authored-By; no commit on `main`; ADR + Hard Rule 8 plan-first on §5-shell + CODEOWNERS paths).
- [x] All `<read_first>` files read in order: CLAUDE.md; STAGE-PROMPT-PROTOCOL v1.8 (the `<wire_signature_audit>` + `<phase_doc_inventory_audit shape="...">` slots — exactly the right tools for this stage); M07 phase doc Background + Key constraints + Stage E E.1–E.4; `src-tauri/src/commands.rs` for the SHIPPED `import_artifact` signature (commands.rs:1144) + `ImportOutcome` struct (commands.rs:1016); `src/lib/graphStore.ts` for the existing state-slot pattern; the M06.E `MCPServerSettings` / `MCPServerAddModal` panel idiom; M07.C / D1 / D2 retros `[END]`; gotcha #23 (Playwright cannot drive Tauri window); `RETROSPECTIVE-TEMPLATE.md`.
- [x] `<pre_flight_check>`: HEAD == `claude/m07-registry-import`, M07.C `849fd0c` + M07.D1 `f5ef25a` + M07.D2 `ab18302` impl commits present. ADR-0015 next-number verified (existing ADRs 0001–0014).
- [x] M07.C/D1/D2 `[END]` Decisions read and applied: `tier_gate` is consumed by Stage E (E.3.4 — confirmed: the renderer applies L4 review, the backend installs+locks); `Installed` already carries `report: L3Report` (M07.C); MCP-config import path is dependency-inverted (M07.C — confirmed not relevant to E; renderer doesn't touch the MCP wire); the gotcha #56 Windows-local subprocess flake on `drone_reconnect_events` + the cargo llvm-cov --workspace stall are the documented carry-forward class (M07.D2).
- [x] Deliverable + test plan stated before code (red surface).
- [x] `git status` clean except untracked `.claude/scheduled_tasks.lock` (harness artifact).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Orientation / `<wire_signature_audit>` | The v1.8 audit immediately caught **six** drift points between the phase-doc E.3 pseudocode and the shipped Stage C/B wire (params, return shape, fictional `import_phase` event, no `share_provenance` on the bridge, absent M05 "CapabilityDisclosure" React component, file-picker out of scope). **Working exactly as designed** — this is the v1.8 audit's whole purpose — but the magnitude (6 drifts, including that the spec'd review screen has no backing data in the shipped wire) crossed into a product-surface scope fork requiring user escalation. Not friction; the audit caught what the protocol exists to catch. | n/a — the slot did its job. | 1 |
| Plan-first / scope fork | The Stage-E "no new backend" assumption was falsified. Investigated lock-reachability per user redirection — `schemas/skills-lock.v1.json` is a closed 6-field integrity ledger with no `capabilities`, AND no Tauri command exposes the lock; the browser-extension pattern is falsified for this codebase. A `get_import_review(lock_key)` read command can't work (review data not persisted keyed by lock_key — would need re-fetch + re-validate + re-run L3). The minimal touch is to additively enrich the existing `Installed`→`ImportOutcome` return with data the pipeline already computes and discards. Surfaced as a Hard Rule 8 plan-first decision before red; user approved Option 1 with two binding conditions (verify hand-mirrored, regression must drive real-import). **ADR-0015 filed** alongside as the plan-first artifact. | The phase-doc Background + the C retro's "the renderer reads the tier + L3 report" carry-forward intent (M07.C line 182) imply the wire should already carry the review data — that intent is unmet by the shipped `ImportOutcome`. A phase-doc note "Stage C return shape discards `TierReview`; Stage E either accepts that or enriches" would have surfaced this at C-authoring time, not E-execution time. | 2 |
| `cargo test` first green run | The red-phase `skill_with_caps_bytes()` fixture had `requires_secrets: ["OPENAI_API_KEY"]` at skill top-level; skill schema rejects unknown fields → 2 tests failed at run time (not the asserted contract; the fixture). `requires_secrets` is a **framework-schema** field (§15d), not a skill-schema field. Red amended in place (M07.C precedent). | Phase-doc fixture authoring should distinguish: skill-schema fields vs framework-schema fields. The schema-validate boundary is per-kind. | 2 |
| Gate: `cargo fmt --check` | rustfmt collapsed the multi-line `import::export_with_provenance(&mut fw, ...)` call in the integration test → impl-commit gate would fail. Amended red again to pre-format the test file (rustfmt-clean tests at red-commit time, the M07.D2 pattern). | A pre-red-commit step "run `cargo fmt --all` + `cargo clippy --all-targets` on test files before committing red" would prevent this class of green-time amendment. Three milestones running this gap. | 2 |
| Gate: `tsc --noEmit` | 5 `TS18048: 'rec' is possibly 'undefined'` errors on graphStore.test.ts + 1 on ImportPanel.test.tsx (TS strict-noUncheckedIndexedAccess on Record indexing). Plus `prettier --check` flagged ImportPanel.test.tsx. All mechanical test-file fixes (`rec.foo` → `rec?.foo`). Amended red a third time. | Same as above — a pre-red-commit `npx tsc --noEmit` + `npx prettier --check` on test files would catch this. | 2 |
| Gate: `eslint` | `'unknown' overrides all other types in this union type` (`unknown \| null` → `unknown`) on graphStore.ts + ipc.ts; `react/no-unescaped-entities` on ImportPanel.tsx (apostrophe). 3 lints. Folded into impl commit (production-file fixes). | The `unknown \| null` redundancy isn't obvious from the spec intent — `unknown` covers `null`. Worth a §15 gotcha. | 1 |
| Gate: `cargo llvm-cov --workspace` + `cargo test -p agent-runtime --bins` (full) | Documented gotcha #56 / TD-005 Windows-local class — the workspace llvm-cov stalled past 13 min (M07.D2 retro precedent: "non-measurable on this box; CI Linux measures"); `drone_reconnect_events` ×2 failed multi-threaded but pass with `--test-threads=1`. The runtime-main per-package gate (the safety-primitive gate, ≥95) ran clean to **95.56% line — PASS** with the `--test-threads=1` mitigation cited per CLAUDE.md §6 CI-divergence rule (gotcha #56 already documented; the CI-workflow graduation remains the open structural close). | Not a Stage E regression; the documented carry-forward class. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase-doc E.3.2/E.3.5 pseudocode vs `src-tauri/src/commands.rs::ImportOutcome` (shipped) | Whether the renderer can render the §M7 review screen against the shipped wire | The audit pinned the actual SHIPPED wire; the scope fork was surfaced and the user approved enriching the return (ADR-0015). | M07 phase doc E.3 (orchestration-session-owned per §8; not edited mid-flight — M07.A–D1 grandfathering precedent). |
| Phase-doc E.3.4 "reuse the M05 capability-disclosure plain-English component" vs `src/` | Whether such a component exists | It doesn't — M05.F only built the `CapabilityViolationRecord` store record + the per-AgentNode `CapabilityBadge` glyph; no component consumes `capabilityViolations` as plain-English. Renderer faithfully renders the artifact's declared capabilities verbatim (the disclosure is the artifact's declaration; the surface is per-line list, not a built translation engine). | M07 phase doc E.3.4 imprecision (orchestration-session-owned). |
| Phase-doc E.3.3 `<FilePicker>` (`@tauri-apps/api` dialog) vs `package.json` | Whether the dialog plugin is installed | It is NOT — `@tauri-apps/plugin-dialog` is not in `package.json` (only `api` + `plugin-notification`). Adding it = new npm + Rust dependency + capability registration → out of "no new backend" scope. File-picker deferred to Stage V / gap-analysis carry-forward; the `importArtifact` wrapper already accepts `'file'` sources; the panel UI lands when the picker does. | M07 phase doc E.3.3 imprecision (orchestration-session-owned). |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-red | The 6-drift wire-signature audit reconciliation + scope-fork question (`AskUserQuestion`) — Option 1 (impoverished render) vs Option 2 (expand scope) vs Option 3 (pause and fix phase doc) | Yes — user redirected to investigate lock-reachability first (Option 2-adjacent: narrow additive read-command if possible). | Right — surfaced BEFORE writing any pseudocode or test, exactly when the v1.8 audit triggers. |
| Re-direction follow-up | The lock-reachability determination + plan + escalation (`AskUserQuestion`) — three options for the resulting backend touch | Yes — user approved Option 1 (enrich return + ADR) with two binding conditions (verify hand-mirrored; regression must drive real-import → rendered disclosure, no mocking ImportOutcome). | Right — surfaced after the investigation completed, before any test or ADR. |
| Red surface (amended `249d7a7`) | Cross-machine state; strict-TDD red committed local + nothing pushed; wire-signature audit reconciliation; right-reason red verified across all four surfaces; condition-1 PASS evidence; condition-2 anchor encoded | Yes — user approved decisively. | Right — strict-TDD `surface_for_red_approval`. |

---

## [LIVE] Protocol drift events

None observed. The three red-commit amendments (fixture defect → fmt → tsc/prettier on test files) were all done **locally** before push; the strict-TDD invariant `git diff <red>..<impl> -- '**/tests/**'` is EMPTY between the final amended red (`42186c1`) and the impl (`f0a7721`). The src-tauri `#[cfg(test)] mod tests` block is byte-identical red→impl (the binary-crate strict-TDD variant per CLAUDE.md v1.8 + the M06.5.A.fix precedent — `git diff 42186c1..f0a7721 -- src-tauri/src/commands.rs | grep import_outcome` returns nothing, confirming the test additions were not modified by impl). No `--no-verify`; the runtime-main llvm-cov command is byte-identical to CLAUDE.md §6 (with the `--test-threads=1` flag cited inline per the §6 CI-divergence rule — gotcha #56 already documented; the CI-workflow graduation remains the open structural close, carried forward). The workspace llvm-cov + agent-runtime --bins full suite local stalls are the documented gotcha #56 / TD-005 Windows-non-measurable class (CI Linux measures; not an E regression). No Hard Rule approached.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The phase doc's "reuse the M05 capability-disclosure plain-English component" referenced a component that does not exist as React surface (only as a store record + per-AgentNode glyph). | The phase-doc author assumed the M05.F retro's "capability-violation modal" was a discrete reusable React component, but M05.F left it as a store-recorded slot consumed by ad-hoc inspector text — never extracted as a `<CapabilityDisclosure>` component. | A phase-doc-authoring discipline: when "reuse component X" appears, the v1.8 inventory audit must verify the component actually exists as renderer surface (not just as a store record). This is the M06.E lesson generalizing further. |
| `skills.lock` carries NO `capabilities` field and NO Tauri command reads it; the browser-extension "render from the manifest" pattern is fundamentally falsified for this codebase. | The phase-doc author treated the lock as a permission manifest. The schema is a closed integrity/provenance ledger; capabilities live in the artifact JSON (which `install_with` does not persist anywhere renderer-reachable). | The lock-reachability determination should be a v1.8 audit slot for ANY renderer stage that surfaces artifact metadata: "is the data on the bridge? If not, is the source persisted anywhere renderer-reachable? If not, the backend touch is unavoidable." This generalizes the wire audit to data-flow audits. |
| 3 red-commit amendments needed for mechanical test-file fixes (fixture defect + rustfmt + tsc strict + prettier). | The amended red got the contract right immediately; what changed across amendments was mechanical (schema-field correctness, format, strict-undefined guards). The protocol's "pre-red mechanical sweep" is missing. | **§15 candidate**: a pre-red-commit hook running `cargo fmt --all`, `cargo clippy --workspace --all-targets`, `npx prettier --check tests/`, `npx tsc --noEmit` against the test files to catch mechanical issues before red is committed. Three milestones running (M07.B/C/D all touched similar amendments). |
| `cargo clippy --fix` + `cargo fmt --all` both apply rustfmt-style line-collapsing on test files, but neither is automatically run during test authoring. | rustfmt is run-on-save in editors but tests are authored manually here. | A `.pre-commit` integration or a stage-prompt step "run fmt+clippy autofix on test files before red commit" would close this gap. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4 + §6 + §7 + §8 + §11 (ADR) + §12 (escalate scope, own technical) + §14 (schema check) all load-bearing and correctly applied |
| 2 | "Read first" oriented correctly? | 5 | The v1.8 audit slots + the M07.C/D1/D2 retros + the M06.E renderer pattern were exactly the context needed; right-sized read-list |
| 3 | Deliverable + test plan before code per §16? | 5 | Wire reconciliation + scope-fork determination + plan + ADR all surfaced before any red |
| 4 | Self-correct in ≤3 rounds per §7? | 4 | Red-commit amendments: 3 (fixture → fmt → tsc/prettier). Each was a single mechanical fix; total iterations in the loop were within budget but the protocol exposed a known gap (no pre-red mechanical sweep) |
| 5 | Do-not-commit-until-approved held? | 5 | Plan-first surfaced + approved; red surfaced + approved; impl + amendments local; nothing pushed; final push gated on this surface |
| 6 | Escalated right things, proceeded on right things? | 5 | The 6-drift wire audit + lock-reachability determination correctly escalated as scope/product-surface forks (§12); the `unknown\|null` ESLint redundancy + apostrophe escape + per-package coverage exclusion-vs-not call were owned as technical decisions (§12 split correctly applied) |
| 7 | Human-direction load reasonable? | 4 | Three approval surfaces (pre-red plan, scope fork, red approval); the user-imposed two-condition verification protocol was a clean additional gate. Slightly above ideal load but each interaction added structural value |
| 8 | Surfaces actionable? | 5 | Each surface had a clear decision (with recommendation), evidence, and a continuation path; user decided in one step |
| **Process axis total** | | **38/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case Rust, camelCase TS, `data-testid` per established panel convention, no comments-as-what, why-not-what comments |
| 2 | Behaviour tests, not tautology (§5)? | 5 | The Rust integration suite drives the REAL `import_artifact_with` pipeline with wiremock fetch + file source + real `capability_summary` + real `share_provenance` round-trip + real `install_with` → assertions on the enriched `Installed`. The src-tauri in-source test pins the serde JSON shape. The renderer fixture consumes that exact Rust-proven shape (condition-2 anti-false-green linkage held — no fabricated mock). Vitest + Playwright assert observable contracts (testid presence, text content, click → store transitions) |
| 3 | Public APIs documented w/ compile-checked examples? | 5 | All new public items documented; `cargo doc -D missing_docs` green; doctest 1/1 |
| 4 | Deliverables exactly promised — no creep/cuts? | 5 | URL/kind form + tier-gate review modal (disclosure + L3 + trust line + secrets) + Install/Reject + hash-mismatch Reinstall/Remove prompt, additive backend enrichment per ADR-0015; file-picker + Reinstall-source-round-trip explicitly deferred (carry-forwards). Nothing added beyond the spec'd primitive |
| 5 | Stranger-understandable without the spec? | 5 | ImportPanel.tsx module doc states what it does + why each phase exists; ADR-0015 explains the why; CHANGELOG documents the wire shape and the carry-forwards |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no stringly types (kind is `ImportArtifactKind` enum), no ad-hoc `#[allow(...)]`, no silent error drops (`unwrapCmdError` consumed) |
| 7 | Coverage ≥80 / ≥95 — measured? | 4 | runtime-main **95.56% line ≥ 95% — PASS** (CI-parity command + `--test-threads=1` gotcha #56 cited; gotcha #81 `cargo llvm-cov clean` first). The aggregate dipped from M07.C's 96.65% → 95.56% because `import_artifact_with` now exercises additional production lines (the seq capability_summary + let-then-construct sequence + share_provenance carry); still ≥95. Workspace ≥80 + agent-runtime --bins full suite = the documented gotcha #56 / TD-005 Windows-non-measurable class (CI Linux measures); runtime-main aggregate is the safety-primitive gate that matters here |
| 8 | CI green across OS × toolchain? | 4 | All local gates green: fmt / clippy `-D warnings` / runtime-main test suite / runtime-main llvm-cov ≥95 / doc tests / cargo doc `-D missing_docs` / cargo audit + deny / npm prettier + eslint + tsc + audit + vitest 31/343. The `drone_reconnect_events` ×2 + workspace llvm-cov + full agent-runtime --bins are the documented gotcha #56 Windows-local class (CI Linux measures); CI prediction strong but not yet observed on CI for this commit |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | `<wire_signature_audit>` + `<phase_doc_inventory_audit shape=...>` were exactly the right slots; the v1.8 promotion paid off here as designed |
| 2 | Sections needed but missing? | 4 | A pre-red "mechanical-sweep" step (`cargo fmt --all` + `cargo clippy --fix` + `npx prettier --check tests/` + `npx tsc --noEmit`) on test files would have eliminated all 3 red-commit amendments. Three milestones running with this same shape of amendment — overdue for a protocol item or a `.pre-commit` hook |
| 3 | Gotchas local or generalize to §15? | 4 | One §15 candidate (the `unknown\|null` ESLint redundancy — TS subtle; mechanical fix `\| null` removed when `unknown` is the union type). The "lock-reachability audit before claiming 'no new backend'" is a sibling to the v1.8 wire audit — generalizable to any renderer stage surfacing artifact metadata |
| 4 | Time-box matched (within 2×)? | 5 | ~6 h vs 5–6 h estimate (≈1.0×, in band) |
| 5 | Implicit protocol moments? | 4 | The "3 red-commit amendments for mechanical test-file issues" is a recurring implicit protocol gap. Should be explicit |
| 6 | Re-runnable by a fresh session → comparable? | 5 | Yes — the wire-signature audit + the lock-reachability determination + the ADR + the conditions are deterministic from the shipped code + the read-first list |
| 7 | Copy format for remaining milestones — change anything? | 4 | Add a pre-red mechanical-sweep step + the lock-reachability audit slot |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — plan-first + red surfaced and approved; impl local only; final push gated on THIS approval surface.
- [x] **G2: no Severity-5 friction** — max severity 2 (the documented gotcha #56 class + the 3-amend mechanical cycle).
- [x] **G3: no unaddressed protocol drift** — none; the 3 red-commit amendments were the protocol working as designed (local-only; strict-TDD invariant preserved across the final red → impl boundary).
- [x] **G4: stage completed** — Builder Import panel + tier-gate review modal (capability disclosure + L3 + trust line + §15d) + Reinstall/Remove prompt + the ADR-0015 enriched return; renderer ≥80 (vitest 343/343); runtime-main ≥95 (95.56%).
- [x] **G5: every axis row ≥3** — lowest row is 4.
- [x] **G6: CI-parity** — `runtime-main` llvm-cov was the EXACT CLAUDE.md §6 regex + `--fail-under-lines 95`, with the `--test-threads=1` divergence cited inline per §6's CI-divergence rule (gotcha #56 already documented; CI-workflow graduation = TD-005 open). `cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`, `cargo test --workspace --doc`, `RUSTDOCFLAGS=-D missing_docs cargo doc --workspace --no-deps`, `cargo audit`, `cargo deny check`, `npx prettier --check`, `npx eslint .`, `npx tsc --noEmit`, `npm run test`, `npm audit --audit-level=high` all CI-parity, exit-0. The workspace llvm-cov + agent-runtime --bins full suite are the documented gotcha #56 / TD-005 Windows-non-measurable class (CI Linux measures; not used to claim green).

### Soft gates

- [x] **S1:** Process ≥30/40 — 38
- [x] **S2:** Product ≥32/40 — 38
- [x] **S3:** Pattern ≥25/35 — 31
- [x] **S4:** Time-box within 2× — ~6 h vs 5–6 h (≈1.0×)
- [x] **S5:** ≤3 Severity-3 friction — 0 (max severity 2)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage V with the Decisions applied (esp. the pre-red mechanical-sweep proposal + the lock-reachability audit slot).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** documented gotcha #56 / TD-005 Windows-non-measurable class (workspace llvm-cov stalled on this box past 13 min; CI Linux measures). Per the M07.D2 precedent, this is honored via the per-crate authoritative gates.
- **Exclusions added this stage:** None. Regex unchanged: `src.main\.rs|generated`.

### Per-package gate — runtime-main (≥95% line)

- **Coverage actual this stage:** **95.56% line — PASS** (CI-parity command, exit 0, gotcha #81 `cargo llvm-cov clean --workspace` first, `--test-threads=1` gotcha #56 mitigation cited).
- **Exclusions added this stage:** None. The new lines in `crates/runtime-main/src/import/mod.rs` (capability_summary now exercised in `import_artifact_with`'s main path, the let-then-construct sequence carrying the enriched fields, `L3Report` Serialize) sit within the passing 95.56% aggregate. The new `Installed` fields are exercised by the 3 new integration tests in `tests/import_pipeline_integration.rs`. No new --ignore-filename-regex term needed.
- **Regex as of stage end (byte-identical to CLAUDE.md §6 + `docs/coverage-policy.md` §A):** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs` — unchanged.
- **Four-mirror sync done this commit:** None needed — no exclusion or threshold change this stage.

### Per-package gate — runtime-drone / runtime-sandbox / runtime-mcp (≥95% line)

- Not touched this stage. Not re-measured locally (gotcha #56 Windows-non-measurable for runtime-mcp `--features test-helpers` lifecycle subprocess fixtures). CI Linux measures.

### Renderer gate (≥80% line, vitest)

- **Coverage actual this stage:** PASS (vitest exit 0; the configured 80% line/branch/function/statement thresholds met across `src/**`; ImportPanel.tsx covered by the new 6-test ImportPanel.test.tsx + ipc.ts/graphStore.ts updates covered by ipc.test.ts/graphStore.test.ts additions).

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `import/mod.rs` | 94.77% (M07.C) | within the 95.56% aggregate | aggregate-level | seam already exercised; new `Installed` fields covered by the new tests |
| `commands.rs` | binary-stub (covered by codecov tauri-shell 50% patch gate) | unchanged class | → | `ImportOutcome` widening covered by the in-source `import_outcome_serializes_*` tests |
| all other prior runtime-main modules | per M07.D2 | unchanged | → | no prior module touched |

### CI workflow drift check

- [x] `.github/workflows/ci.yml` runtime-main `--ignore-filename-regex` — unchanged this stage (no exclusion added). The M07.C `|src.import.fetch\.rs` term added in M07.C is already reflected; no Stage G `<coverage_policy_reconciliation>` item this stage.

---

## [END] Decisions for the next stage

### special_log (required by the stage prompt)

- **`import_artifact` actual params pinned (from shipped code, NOT assumed):** `source_kind: String`, `location: String`, `artifact_kind: String` (three flat Rust args; Tauri's automatic snake_case → camelCase conversion makes the JS-side wrapper invoke `'import_artifact', { sourceKind, location, artifactKind }`). PINNED from `src-tauri/src/commands.rs:1144` at the start of the stage; verified against the existing `respond_uncertainty` etc. wrappers for the camelCase convention.
- **Store-slot shape pinned (from shipped code):** `src/state/` does not exist; the store is `src/lib/graphStore.ts`. The pattern is plain `set((state) => ({ ...state, ...}))` (no immer), `Record<string, T>` keyed slots, and an explicit per-slot `clear()` preservation list. The new `imports: Record<string, ImportRecord>` slot follows that convention exactly; preserved across `clear()` (install/integrity state, parallels `currentMcpServers` / `currentTier`). The phase-doc E.3.2 pseudocode's slot shape matched in spirit.
- **The panel matches the SHIPPED wire post-ADR-0015:** wrapper sends `{ sourceKind, location, artifactKind }` (camelCase, 3 flat fields — NOT the phase-doc-assumed `{ src, kind }`); returns `ImportOutcome { lock_key, review_required, requires_secrets, capabilities, l3_report:{report_id,passed,reasons}, share_provenance|null }` (snake_case, additively enriched per ADR-0015). The Rust-proven JSON shape is anchored by `src-tauri/src/commands.rs::import_outcome_serializes_*` in-source tests; the renderer mirror in `src/lib/ipc.ts::ImportOutcome` and the mock fixture in the vitest + Playwright tests consume that exact shape.
- **M05 disclosure-component reuse confirmed:** the phase-doc reference was imprecise — M05.F left `capabilityViolations` as a store record + `CapabilityBadge` glyph; no standalone `<CapabilityDisclosure>` React component exists. The §M7 review modal renders the artifact's declared capabilities as a per-line `<ul>` (the disclosure IS the artifact's declaration; no plain-English translation engine rebuilt). This honors the intent (don't rebuild a translation engine) by faithfully rendering the source.
- **Playwright + vitest results:** vitest 31 files / 343 tests pass (incl. the 6 new ImportPanel tests + 6 new graphStore imports tests + 2 new ipc importArtifact tests). Playwright E2E is the gotcha #23-class store-injection contract (`window.__graphStore`-driven; not invoke-mocked because module mocking @tauri-apps/api ESM in Playwright is not supported — Vitest covers the invoke linkage). The Rust real-pipeline integration tests (`import_pipeline_integration.rs` 21/21 incl. the 3 new ADR-0015 tests) + the src-tauri in-source `import_outcome_serializes_*` tests (2/2) jointly form the condition-2 anti-false-green anchor.
- **Strict v1.8 two-commit invariant proven:** `git diff 42186c1..f0a7721 -- '**/tests/**'` is EMPTY (verified); `git diff 42186c1..f0a7721 -- src-tauri/src/commands.rs | grep -E '^\+.*import_outcome|^-.*import_outcome'` returns nothing (the in-source test block is byte-identical red→impl per the binary-crate variant + M06.5.A.fix precedent). The amended red incorporates 3 mechanical test-file fixes (fixture, rustfmt, tsc/prettier); impl is production-only. No Co-Authored-By; DCO `-s`; session-URL footer.

### Carry-forward to Stage V (or G if no V)

- **File-picker deferral:** native local-file picker via `@tauri-apps/plugin-dialog` needs a new npm + Rust dep + capability registration; out of this stage's no-new-backend scope. The `importArtifact` wrapper already accepts `'file'` sources; the panel UI lands when the picker does. Stage V verification: confirm the deferred file-picker path is documented in the gap-analysis entry the closeout writes.
- **Reinstall source round-trip:** the `Installed` struct does not currently round-trip the original `ImportSource` (URL or file path) through to the renderer, so the Reinstall button can't auto re-fetch. The panel surface emits the affordance; closing the loop is a Stage V follow-up (would need adding `source` to `Installed` + `ImportOutcome` and the panel calling `importArtifact` with it again).
- **Grandfathered phase-doc defect (Stage E):** the Stage-E phase-doc E.3 pseudocode was authored against an assumed `ImportOutcome` shape (phases, L3Report, ShareProvenance, native dialog) that the shipped Stage C wire does not provide. The v1.8 `<wire_signature_audit>` caught 6 drift points. Recorded in CHANGELOG.md + this retro + the impl-commit body. The cumulative M07 `docs/gap-analysis.md` entry lands at Stage G closeout (append-only / closeout-owned per §20). Phase doc itself NOT edited mid-flight per §8 (the M07.A–D1 grandfathering precedent).
- **Lock-reachability finding (generalizable):** the browser-extension "render from the lock/manifest" pattern is falsified for this codebase — the lock is a closed integrity ledger, no command reads it. For any future renderer stage surfacing artifact metadata: the v1.8 audit should include a data-flow check ("is the data on the bridge? if not, is the source persisted renderer-reachably? if not, the backend touch is unavoidable") sibling to the wire audit.

### `CLAUDE.md` / protocol updates (candidates — apply if substantive)

- [ ] **§15 gotcha candidate (graduate — RECURRED across M07.B/C/D and now E):** a pre-red-commit mechanical-sweep step (`cargo fmt --all` + `cargo clippy --workspace --all-targets` + `npx prettier --check` + `npx tsc --noEmit` against test files) catches the fixture/format/tsc-strict issues that have driven 1–3 red-commit amendments per stage. Three milestones running. Either codify as a stage-prompt `<pre_red_mechanical_sweep>` step OR add to `lefthook.yml` as a pre-commit gate.
- [ ] **§15 gotcha candidate (new):** `unknown | null` is a redundant union under ESLint's `@typescript-eslint/no-redundant-type-constituents` — `unknown` already covers `null`. Use `unknown` alone (commented "covers null") OR `T | null` when intent is "either typed T or null".
- [ ] **STAGE-PROMPT-PROTOCOL note:** generalize the v1.8 `<wire_signature_audit>` to a `<data_flow_audit>` for renderer stages — assert that the source data is renderer-reachable through the shipped bridge / persisted-and-read-back, not just that the IPC command exists. The M07.E lock-reachability falsification is the prototype case.

### Milestone document updates

- [ ] M07 phase-doc E.3.2 (params drift — `{ src, kind }` vs the shipped `source_kind/location/artifact_kind`), E.3.3 (`<FilePicker>` requires a non-installed plugin), E.3.4 ("reuse the M05 capability-disclosure plain-English component" — the component doesn't exist as React surface), E.3.5 (`import_phase` event is fictional; ShareProvenanceBadge has no data on the shipped bridge) are imprecise. **Not edited mid-flight** (§8 phase-doc-edit pre-flight + M07.A–D1 grandfathering precedent — orchestration-session-owned). Recorded in CHANGELOG.md + this retro + the impl-commit body. Stage V reads this `[END]` first; the cumulative M07 gap-analysis entry at Stage G closeout transcribes the carry-forwards.

### Open issues to file

- [ ] None blocking. The file-picker + Reinstall source-round-trip are carry-forwards (Stage V or follow-up milestone), not blockers.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M07 Stage E per `CLAUDE.md` §19. User review pending. This stage's commits land on `claude/m07-registry-import`; the PR drafts/pushes only at the end of the final M07 stage.

**Claude:** M07 Stage E complete per the stage prompt. The v1.8 `<wire_signature_audit>` caught 6 drift points before any pseudocode, falsifying the "no new backend" assumption against the shipped Stage C wire. After user-directed lock-reachability investigation (browser-extension pattern falsified — `skills.lock` is a closed integrity ledger, no command exposes it), the minimal touch was determined to be additively enriching the existing `Installed`→`ImportOutcome` return with data the pipeline already computes and discards. ADR-0015 (Proposed) authored as the plan-first artifact (Hard Rule 8); both binding conditions verified (hand-mirrored bridge struct PASS, real-import regression encoded). Red `42186c1` (amended 3× for mechanical fixture/fmt/tsc-strict fixes; contract unchanged across amendments — user-approved red contract preserved) → impl/green `f0a7721` (zero `**/tests/**` diff vs `42186c1` — strict v1.8 two-commit invariant proven; src-tauri in-source test block byte-identical red→impl per the binary-crate variant). All gates green; runtime-main llvm-cov 95.56% ≥ 95 (with `--test-threads=1` gotcha #56 mitigation cited per CLAUDE.md §6 CI-divergence rule). Vitest 31 files / 343 tests; the cross-language anti-false-green anchor is the runtime-main real-pipeline integration suite (3 new tests driving real `import_artifact_with`) + the src-tauri `import_outcome_serializes_*` in-source tests (2) pinning the serde JSON shape the renderer fixture consumes. File-picker + Reinstall source-round-trip are explicit Stage V / gap-analysis carry-forwards (out of this stage's no-new-backend scope). I have NOT pushed; surfacing for final approval.

**Surfaced at:** 2026-05-19
