# M08.9.D.fix — Stage Retrospective

> **Stage:** M08.9 Stage `D.fix` — close V 🔴 #1: make the committed Tester e2e regressions actually execute their substantive assertions — see `docs/build-prompts/M08.9-honest-tester.md` (Stage D.fix)
> **Parent milestone:** M08.9 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-06
> **Session end:** 2026-06-06
> **Branch:** `claude/m08.9-honest-tester`
> **Starting commit:** `97b2a42` (after the M08.9.V verifier retro landed — 🔴 #1 routed to D.fix)
> **Ending commit:** `61f1b11` (the D.fix impl commit; this retro + the D.fix phase-doc back-fill `8bda2bb` follow)
> **Estimated effort (per V Decision 1):** ~20 min
> **Actual elapsed:** ~1 short session (build + real-app run dominates wall-clock)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules honored (Hard Rule 1 — surface before any commit; Hard Rule 11 — the grounded-claims rule is the whole point: D.fix may NOT claim "regression green" until the assembled suite is observed exiting 0)
- [x] Read first: the M08.9.V retro 🔴 #1 (the finding + the proven fix path), `tests/e2e-tauri/tester_verdict.e2e.ts` + `tester_drilldown.e2e.ts` (the two crashing specs), their shared `openBuilderTester` helper, and the `closeTester()` store action V's throwaway spec proved working
- [x] Deliverable + test plan stated before code: add a per-case `afterEach` teardown to both describes; re-run `test:e2e:tauri` with a key and OBSERVE both substantive cases pass + suite exit 0
- [x] Branch correct (`claude/m08.9-honest-tester`); `git status` clean of tracked changes at start
- [x] Test-harness-only confirmed before editing: no product/renderer/store change — the fix is in the spec files only

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| — | None observed. The fix path was pre-proven by V's throwaway spec; the teardown applied cleanly to both describes and the real-app run was green on the first attempt (12/12, exit 0). | — | — |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| — | None observed. V handed off an exact root cause (modal scrim intercepts the view-switch click because no teardown closes the modal between shared-session `it`s) and an exact, already-proven remedy. | — | — |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Close | The full bundle: the 26-line two-file teardown diff (0 deletions, assertions untouched), the green `test:e2e:tauri` evidence (12/12, exit 0, with a key), and the explicit "test-harness only — no product change" claim grounded by the observed run | Yes | At surface-and-wait per Hard Rule 1 |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| Hard Rule 11 (grounded claims) | The claim D.fix exists to license — "the committed regression actually runs green" — is exactly the claim Hard Rule 11 forbids asserting from structure. It was NOT asserted until the assembled `test:e2e:tauri` run was observed exiting 0 with the two substantive cases passing. | N/A (held, not drifted) | The green run IS the evidence; the commit body cites the observed 12/12 / exit 0, not the presence of the `afterEach`. |
| §5 strict-TDD byte-identical test block | This stage edits ONLY test files (the harness IS the defect), so the red→impl "test diff is EMPTY" invariant is inverted: the change lives entirely in `tests/**`. | N/A | Documented below under Pattern; this is the test-harness-defect variant of the v1.8 mandate. |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The committed merge-blocking regressions had **never executed their substantive assertions** before V — they crash on a key-present machine (modal-scrim intercept) and skip on CI (no `ANTHROPIC_API_KEY`). | A spec can land "green" through B because the crash only fires when the real model key is present AND two cases share a session; CI's keyless skip masked it, and B never ran the real app with a key. | This is precisely the bug class Stage V (assembled-execution 5th pass) exists to catch — authored-but-never-run e2e. Reinforces: authoring an e2e regression ≠ running it; only the assembled run with a key proves the assertion executes. Candidate gotcha (V Decision 3): a spec whose helper opens a modal must close it in `afterEach`. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **Replaced by the assembled run** — the mechanical regression IS the discovery: the fix's only acceptance is the real-app `test:e2e:tauri` suite executing the substantive A + B cases and exiting 0. | `tests/e2e-tauri/tester_verdict.e2e.ts` + `tests/e2e-tauri/tester_drilldown.e2e.ts` (full suite 12/12, exit 0, with a key). V's 5th pass already drove the product correct (TIER-LIMITED not PASS; Read drill-down to input/result/raw) via a throwaway spec; D.fix makes the *committed* specs reproduce that. | None — the only finding was V 🔴 #1 itself, addressed here. No new adjacent surface appeared during the real-app run. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rule 11 + the assembled-app-regression mandate (v1.8) framed the whole stage: don't claim green until observed. |
| 2 | Did the "Read first" list correctly orient Claude? | 5 | V's 🔴 #1 carried the exact root cause + the proven fix; no further discovery needed. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Teardown-both-describes + observe-suite-exit-0 stated up front. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | Zero rounds — green on first real-app run. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | Surfaced the bundle; committed only on approval (`61f1b11`). |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Nothing to escalate; a pre-proven test-harness fix is squarely "proceed." |
| 7 | Was the human-direction load reasonable? | 5 | Single approval. |
| 8 | Were the surfaces actionable? | 5 | Diff + green evidence + explicit test-only scope. |
| **Process axis total** | | **40/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | The `afterEach` mirrors the existing `openBuilderTester` idiom; a `why`-comment names V 🔴 #1 and the scrim-intercept invariant. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | The specs assert observed product behavior on the real app (TIER-LIMITED, not PASS; drill to input/result/raw); D.fix only makes those assertions reachable. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | n/a | No public API touched; test-harness only. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | 0 deletions; 26 insertions across two files; no product/store/renderer change; assertions intact. |
| 5 | Would a stranger understand it without the spec? | 5 | The comment explains the shared-session-leaves-modal-open root cause inline. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers (the `5_000` timeout is named via the option key), no silent drops, no ad-hoc ignores. |
| 7 | Is coverage ≥80% / 100% on safety primitives? | n/a | No source change; coverage gates unmoved (test-harness-only diff). |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | The `e2e-tauri-driver` job is Windows-only and the substantive cases are `ANTHROPIC_API_KEY`-skip-guarded, so on CI they skip (documented ADR-0021 / §10 posture — maintainer IRL is the compensating control). The executing-assertion evidence is the key-present local run; the fix's value is precisely that a key-present run no longer crashes. |
| **Product axis total** | | **33/35 applicable** (≈4.7 avg) | three rows n/a (no source/API surface) |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or dead weight? | 4 | The coverage-holdout block is dead weight for a test-harness-only D.fix; kept as "n/a — no source change" rather than omitted. |
| 2 | Were any sections needed but missing? | 5 | The template covers the test-harness-defect case adequately via the protocol-drift + Stage-0 rows. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize? | 4 | The modal-scrim-intercept-without-`afterEach` trap generalizes to every e2e spec whose helper opens a modal (V Decision 3) — candidate `docs/gotchas.md` entry, not local. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | ~20 min est.; the edit was trivial, the real-app build/run dominated — within 2×. |
| 5 | Were there implicit protocol moments that should be written down? | 4 | The "test-harness-only D.fix inverts the strict-TDD empty-test-diff invariant" point deserves a one-line note in the protocol (the change lives entirely in `tests/**`). |
| 6 | Could a different fresh session reproduce comparable results? | 5 | Yes — V's finding is fully specified and the fix path was pre-proven. |
| 7 | If we copy this format, does anything need to change first? | 5 | No. |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved held** — no commit before the surface; D.fix landed at `61f1b11` post-approval.
- [x] **G2: no Severity-5 friction events** — friction table is "None observed."
- [x] **G3: no protocol drift left unaddressed** — both rows are "held / N/A," not drift; Hard Rule 11 enforced (no green claim until the run was observed).
- [x] **G4: the stage actually completed** — `61f1b11` on `claude/m08.9-honest-tester`; the committed suite executes the substantive A + B cases and exits 0 with a key (V 🔴 #1 closed by observation, not by the presence of the `afterEach`).
- [x] **G5: scores ≥3 in every applicable row** — no applicable row below 4.
- [x] **G6: CI-parity** — `npm run test:e2e:tauri` is the exact merge-blocking `e2e-tauri-driver` invocation; no divergent flags. The only environment delta is the local `ANTHROPIC_API_KEY` (the documented ADR-0021 / §10 skip-guard posture), which makes the substantive cases *execute and pass* locally where CI *skips* them — that is the compensating-control design, not a flag divergence.

### Soft gates

- [x] **S1:** Process axis 40/40 ≥ 30
- [x] **S2:** Product axis 33/35 applicable ≥ 32-equivalent (three rows n/a)
- [x] **S3:** Pattern axis 32/35 ≥ 25
- [x] **S4:** ~20 min est. vs ~1 short session — within 2×
- [x] **S5:** 0 Severity-3 friction events ≤ 3

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. The single 🔴 from V is closed by observed real-app execution; the fix is test-harness-only with the product behavior already grounded by V. Proceed to closeout (Stage G).

---

## [END] Coverage holdouts

n/a — test-harness-only diff (`tests/e2e-tauri/*.e2e.ts`, +26/-0). No source file changed; no `cargo llvm-cov` / `vitest --coverage` gate moved; no exclusion added or altered. The `.github/workflows/ci.yml` `--ignore-filename-regex` lists are unchanged by this stage.

---

## [END] Decisions for the next stage (closeout — Stage G)

### `CLAUDE.md` updates

- [ ] None required by this stage. (The assembled-app-regression mandate and Hard Rule 11 already cover the lesson.)

### `TEMPLATE.md` updates

- [ ] None.

### Milestone document updates

- [x] The D.fix stage section was back-filled into `docs/build-prompts/M08.9-honest-tester.md` (`8bda2bb`) so the phase doc records the V 🔴 #1 → D.fix close.

### Protocol additions

- [ ] (Optional, V Decision 3) Candidate `docs/gotchas.md` entry: an e2e-tauri spec whose helper opens a modal MUST close it in `afterEach` and wait for `[data-testid="..."-modal]` to disappear — shared-session `it`s otherwise leave the modal scrim intercepting the next case's clicks. Surfaced twice now (V's diagnosis + this fix). Orchestrator's call at closeout.
- [ ] (Optional) One-line note: a test-harness-only D.fix inverts the strict-TDD empty-test-diff invariant — the entire change lives in `tests/**` by design.

### Open issues to file

- [ ] Closeout (per V Decision 2): record TD-047 as RESOLVED **observed in the running app** — now licensed, because the committed merge-blocking gate executes the assertion (12/12, exit 0) rather than skipping/crashing. Cite `tester.rs:fold_outcome` arm + the M08.9.V real-app observation (TIER-LIMITED, not PASS) + this D.fix green run.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M08.9 Stage `D.fix`, authored per `CLAUDE.md` §19. The D.fix impl commit `61f1b11` already landed (V 🔴 #1 was routed for fix); this retro is the process record and is surfaced for approval before commit. The milestone PR drafts at closeout (Stage G).

**Claude:** I completed M08.9 Stage D.fix — V 🔴 #1 is closed by an observed real-app run (`test:e2e:tauri` 12/12, exit 0, with a key), not by the structure of the added teardown. The fix is test-harness-only (afterEach → closeTester on both describes, 0 deletions, assertions intact). The committed e2e regressions were authored-but-never-run before V — latent crash on a key-present machine, skipped on CI — and now execute their substantive assertions. I will not commit this retrospective until you approve.

**Surfaced at:** 2026-06-06
