# M08.5.5.A.fix — Stage Retrospective

> **Stage:** M08.5.5 Stage `A.fix` — harness + safety hardening (no 🔴 fix; ADRs 0024 + 0025) — see `docs/build-prompts/M08.5.5-mcp-resilience.md`
> **Parent milestone:** M08.5.5 (work-stage-class fix cycle per ADR-0008 D.fix precedent — no closeout, no gap-analysis entry, resolution flows into M08.6's gap-analysis Carry-forward per CLAUDE.md §20)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-24 ~04:30 UTC
> **Session end:** 2026-05-24 ~15:15 UTC
> **Branch:** `claude/m08.5.5-mcp-resilience`
> **Starting commit:** `bf39270` (origin/main HEAD after fast-forward at session start)
> **Red commit:** `f603524` — `test(M08.5.5.A): red-phase env-var precedence tests for read_api_key (ADR-0025)`
> **Impl commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 6–9 hours
> **Actual elapsed:** ~10 hours (slightly over; see Friction events)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded and the §4 Hard Rules were stated at the top of the first response
- [x] All "Read first" files in the stage prompt were read in order
- [x] The deliverable was stated in 1–3 sentences and the test plan in 3–5 bullets BEFORE writing code
- [x] User confirmed via AskUserQuestion before code began (Stage 0 directions + gitleaks install path + .env.local handling)
- [x] The branch was correct per the stage prompt (`claude/m08.5.5-mcp-resilience`)
- [x] No uncommitted changes from a prior session (working tree was clean at the start; only untracked `.claude/scheduled_tasks.lock` + `src-tauri/target/` from local dev)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Pre-flight | `wdio.conf.ts` gap-1 (workspace path stale) was already on `main` via `f48ba98`. Phase doc + ADRs assumed it was pending. | Phase doc should note: "verify current state of wdio.conf.ts on main; the workspace-path fix may have already merged." | 2 |
| Red-phase | Workspace `forbid(unsafe_code)` is non-overridable. rustc 1.84+ marks `std::env::set_var` / `remove_var` `unsafe`. The phase doc's verbatim test samples used direct `set_var` without `unsafe` blocks. I had to add `temp-env 0.3` dev-dependency. | Phase doc should note: "rustc 1.84+ requires `unsafe` for env-var mutation AND workspace lint forbids it; use `temp-env` (callback API, internal Mutex)." | 3 |
| Impl phase | `executeScript` arg-marshaling of WebdriverIO chainable elements doesn't deserialize to DOM nodes. Test failed with `targetElement.getBoundingClientRect is not a function`. | Phase doc should note: "use `document.querySelector` inside `browser.executeScript` rather than passing wdio chainable args." | 3 |
| Impl phase | Phase doc recommended `cargo build --release -p agent-runtime -p runtime-drone -p runtime-sandbox` in onPrepare. The agent-runtime crate built that way produces a **dev-mode** binary that loads the renderer from `tauri.conf.json::devUrl` (`http://localhost:1420`), unreachable at e2e-run time. Tests all failed with `localhost refused to connect`. CI uses `npx tauri build --no-bundle` for agent-runtime + separate `cargo build` for the siblings. | Phase doc should mirror the CI step exactly: `npx tauri build --no-bundle` for the Tauri app + `cargo build --release -p runtime-drone -p runtime-sandbox --bins` for siblings. The compile-mode distinction is not obvious and bit Stage 0. | 4 |
| Impl phase | Windows `spawnSync` on `npx.cmd` exited with `null` (signal-terminated) — Node `spawn` does not resolve `.cmd` extensions without `shell: true`. | Phase doc could note: "Windows wdio onPrepare invocations of npm/npx CLIs need `shell: true` because `spawn` does not handle batch-file extensions." | 3 |
| Impl phase | `await droppedNode.isDisplayed()` raced React Flow's render-tick commit (PASS on some runs, FAIL on others — same code). | Phase doc could note: "for React Flow renders, always poll visibility with `waitForDisplayed`, never sync `isDisplayed`." | 2 |
| Impl phase | `SetupPanel` wait in builder_drag is brittle when the OS keychain holds a key from a prior smoke-test #2 — SetupPanel hides and the wait times out. | Phase doc could note: "tests that don't depend on setup should wait for ViewSwitch (always mounted), not SetupPanel (conditional on hasKey)." | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc § A.3.1 vs `.github/workflows/ci.yml::e2e-tauri-driver` | onPrepare cargo build vs CI's `npx tauri build` — they're not equivalent because the Tauri compile-mode env vars differ. | Followed CI (the authoritative implementation) and updated wdio.conf.ts with an inline rationale citing both. | Phase doc § A.3.1 (post-stage update — but this stage's commit closes it for M08.5.5 forward). |
| Phase doc § A.3.2 test samples | Verbatim test samples used `std::env::set_var` without `unsafe` blocks; will not compile on rustc 1.84+ under `forbid(unsafe_code)`. | Adopted `temp-env` callback API; documented inline at the test block + in ADR-0025's notes path. | Phase doc should note the build-machine call: "tests may need `temp-env` or `serial_test`; pick whichever is already in the workspace or has minimal dep impact." |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-flight (after gitleaks/lefthook/.env.local checks failed) | AskUserQuestion: 3 questions on (a) gitleaks install path, (b) `.env.local` handling, (c) Stage 0 step 1 variant. | Yes — user answered all three; their answers unblocked immediate progress. | Right — surfaced before any code work, exactly the §12 escalation pattern for genuinely-user-domain choices. |
| Post-red-phase | Red commit surface: diff stat, test-run failure proof, falsifiable hypothesis, draft commit message. User edited the message (caught a grammar break + unbalanced paren) and approved. | Yes — user revised one paragraph then approved. | Right — surfaced before commit (per CLAUDE.md §8 do-not-commit rule). |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed. The do-not-commit rule held (only the red commit is on the branch, against an explicitly-approved commit message). Did not push (no push between stages per §8). Did not modify `.aria/` or `archive/aria-shell/`. Did not add telemetry. The one new dependency (`temp-env`) passed `cargo deny check` and is dev-only (no ADR trigger per §11).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The `ANTHROPIC_API_KEY` env var inherited from the parent shell was visible to the running app at e2e test time, causing smoke #3–#5 to switch from skipping to actually running and PASSING with real API responses. | The Stage 0 baseline showed tests 3–6 always skipped; with the ADR-0025 env-var-override active, the inherited shell env became load-bearing for IRL test execution. Positive surprise — ADR-0025 works IRL exactly as designed. | Phase doc author was conservative ("require `.env.local`"); the env var inherits naturally from any shell where the dev has it set. Mention this in `docs/build-machine-tauri-driver-setup.md` Phase 4.1 as "or: just have ANTHROPIC_API_KEY set in your shell." |
| Smoke #6 (`reload reconstructs the graph from persisted signals`) **failed** with `[data-testid^="agent-node-"] still not displayed after 15000ms` after `browser.reloadSession()`. | This test had never RUN on `main` IRL (always skipped without API key). The env-var override exposed a latent persistence bug for the first time. | This is M08-IRL 🔴 #5 ("Builder Canvas state not persisted across app restart"), which the stage prompt's `<scope_locks>` explicitly route to M08.6 Stage A intake. **No new finding; existing decision applies.** Adjacent-bug triage: confirmed M08.6 routing. |
| `gitleaks git --staged` does NOT flag synthetic / docs-example API key strings (AWS `AKIAIOSFODNN7EXAMPLE`, synthetic `ghp_…`, synthetic `sk-ant-…`) — they're either allow-listed or below the entropy threshold. RSA private key blocks DO trip the gate. | I expected the synthetic patterns to fire the gate; instead, the gate is precision-oriented (catches real-shaped secrets, not test-pattern strings). | The gate is structurally correct (it won't false-flag every doc-example secret). Verification via RSA private key was sufficient to prove exit code 1 on a deliberate leak. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard rules at top, retro template at bottom; everything I needed. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | Mostly, but a few cross-cutting things (CI workflow specifics) needed independent grep. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | First non-tool-call response was the deliverable + 5-bullet test plan, before any edits. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | Each issue resolved in 1-2 rounds; never hit the 3-round ceiling. The build-mode issue (cargo vs tauri-build) was 1 round to diagnose + 1 round to fix the spawnSync invocation. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | Red commit landed only after explicit user approval; impl commit not yet landed (awaiting this surface's approval). |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Escalated 3 genuinely-user-domain choices at pre-flight (gitleaks install path, .env.local handling, Stage 0 variant) and the red-phase commit message. Proceeded autonomously on every technical decision (temp-env adoption, executeScript pattern, isDisplayed→waitForDisplayed, npx via shell:true). |
| 7 | Was the human-direction load reasonable? | 5 | Two user touchpoints across ~10 hours: one AskUserQuestion at pre-flight, one red-phase surface with a grammar edit. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | User caught a real issue in the red commit message (unbalanced paren + grammar). Pre-flight options were concrete with recommendations. |
| **Process axis total** | | **38 / 40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case Rust, camelCase TS, kebab-case .md. No marketing language. Doc comments explain *why* (workspace lint, BatBadBut escaping rationale, etc.). |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | Three env-var tests cover env-overrides-keychain (the contract), empty-env-fallthrough (boundary), unset-env-fallthrough (boundary). Each fails for a *specific* assertion, not a setup error. |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | `read_api_key` docstring describes precedence + ADR reference; no doctest because the function reads from real env/keychain. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All 7 deliverables landed: wdio.conf.ts (.env.local + onPrepare), read_api_key env-var override, builder_drag JS dispatch, lefthook gitleaks, CI gitleaks job, ADRs 0024+0025 Accepted, 5 new gotchas (#83-#87), setup guide phase 0.5/3.5/4 updates. No 🔴 fix (per scope lock). |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | wdio.conf.ts comments explain Tauri compile-mode, ePages-de upstream SHA citation, and the dispatch-sequence rationale. builder_drag header explains the test mechanism choice + upstream verbatim citation. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers, no stringly-typed APIs, no `any`, no ad-hoc `#[allow(...)]`/`// @ts-ignore`, no generic names, no silent error drops, no premature abstraction. |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | 5 | runtime-drone 95.73% ✓, runtime-main 96.66% ✓, runtime-sandbox 96.11% ✓, runtime-mcp 95.83% ✓, workspace 92.15% ✓. All measured via `cargo llvm-cov clean` + the canonical commands from CLAUDE.md §6. |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local on Windows: all canonical gates green. CI not yet run on this commit (per CLAUDE.md §8 no-push-between-stages). After user approval + push, CI will validate. The gitleaks job is new; verified locally on a seed RSA-private-key fixture (exit 1). |
| **Product axis total** | | **38 / 40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | All slots earned their keep this stage (`<stage_0_real_app_discovery>` directly produced the test #6 = 🔴 #5 adjacent-bug discovery; `<tdd_discipline strict="false">` with the sub-discipline carve-out worked exactly as designed for the read_api_key change). |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 3 | None for the schema; but the phase doc's verbatim Rust test samples + onPrepare cargo-build recommendation needed adjustment for build-machine reality. A `<verify_before_running_sample>` slot could prompt the build-machine to validate phase-doc Rust samples against current toolchain before adopting them verbatim. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | The 5 new gotchas (#83-#87) are infrastructure-level; they generalize to any future tauri-driver test work. Already added to `docs/gotchas.md`. |
| 4 | Did the time-box estimate match reality (within 2×)? | 4 | Estimated 6-9 hours; actual ~10 hours. Within 1.5× — the cargo-vs-tauri-build discovery + the spawnSync shell:true diagnosis added ~2 hours that the phase doc didn't anticipate. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 3 | The Tauri-build-mode distinction (cargo build -p agent-runtime ≠ tauri build) is implicit knowledge — the phase doc and ADRs do not flag it. This bit hard at Stage 0. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 4 | Yes, modulo the discoveries this session encoded into wdio.conf.ts comments + gotchas #83-#87 + the retro. A fresh session re-running today would have the encoded knowledge. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | The phase-doc-verbatim-Rust-samples vs build-machine-toolchain reality drift (`forbid(unsafe_code)` not flagged) should land as a CLAUDE.md note or a stage-prompt `<verify_phase_doc_samples_compile>` precondition. |
| **Pattern axis total** | | **27 / 35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* red commit `f603524` landed only after explicit user approval at the red-phase surface; impl commit not yet created (awaiting this surface's approval).
- [x] **G2: no Severity-5 friction events** — *evidence:* highest was Sev 4 (cargo vs tauri build mode), self-corrected in 1 diagnosis round.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift-events table is "None observed."
- [x] **G4: stage actually completed** — *evidence:* all 7 deliverables shipped (proof in surface), strict-TDD diff invariant verified (`git diff f603524 -- crates/runtime-main/src/key_store.rs` shows only function body + doc), gitleaks gate verified locally (exit 1 on seed RSA key).
- [x] **G5: scores ≥3 in every individual row** — *evidence:* lowest score is 3 (Axis 3 rows 2, 5: protocol-coverage gaps documented).
- [x] **G6: CI-parity confirmation** — *evidence:* every gate ran the exact CLAUDE.md §6 canonical command:
  - `cargo fmt --all -- --check` ✓
  - `cargo clippy --workspace --all-targets -- -D warnings` ✓
  - `cargo test --workspace` ✓ (all suites pass)
  - `cargo test --workspace --doc` ✓
  - `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps` ✓
  - `cargo audit` ✓ (17 allowed warnings, no advisories)
  - `cargo deny check` ✓
  - `cargo llvm-cov clean --workspace` between gates per gotcha #81; per-package gates ran with the exact `--ignore-filename-regex` from CLAUDE.md §6
  - `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'` ✓
  - `npx eslint .` ✓
  - `npx tsc --noEmit` ✓
  - `npm run test` ✓ (vitest)
  - `npm audit --audit-level=high` ✓ (8 moderate, below threshold)
  - `npm run test:e2e:tauri` — **7 passing, 1 failing locally** (smoke #6 reload = 🔴 #5, out of scope per `<scope_locks>` routes to M08.6). CI will skip smoke 3-6 cleanly (no `ANTHROPIC_TEST_KEY` secret in CI env for this repo per the user's stated CI policy), so the local failure does not surface on CI. **CI-local divergence: documented above + in retro.**

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 38 / 40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38 / 40
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 27 / 35
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 6-9 hours, took ~10 hours, ratio ~1.1-1.7×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 4 (temp-env adoption, executeScript marshaling, npx spawnSync, builder_drag SetupPanel wait). Borderline — 4 is one over the threshold but each was a quick fix (≤30 min). Worth flagging because each was a phase-doc-vs-reality drift the protocol could surface earlier.

### Outcome

- [x] **Pattern is sound but rough.** All hard gates pass; 1 soft gate (S5) over by one event. Address in Decisions for next stage (the protocol-iteration recommendations below).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 92.15%
- **Exclusions added this stage:** None added.
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged from prior stage).

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
- **Coverage actual this stage:** 95.73%
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs` (unchanged).

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 96.66%
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs` (unchanged).

- **Crate:** `runtime-sandbox`
- **Coverage actual this stage:** 96.11%
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.seccomp\.rs|src.landlock\.rs` (unchanged).

- **Crate:** `runtime-mcp`
- **Coverage actual this stage:** 95.83%
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.transport.stdio\.rs|src.transport.http\.rs|src.client.auth_keyring\.rs|src.client.lifecycle\.rs` (unchanged).

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/key_store.rs` | (M02 baseline) | tests added; coverage unchanged for the function body lines but lower-bound is N/A — env-var branch is exercised by the 3 new tests. | ↑ | n/a — strict improvement |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. **No coverage policy / `codecov.yml` / `docs/coverage-policy.md` changes this stage** — the env-var override does not need an exclusion (it's in the gated set + adequately covered by the 3 new tests).

---

## [END] Decisions for the next stage (or next parent milestone if this is the final stage)

### `CLAUDE.md` updates

- [ ] Append gotcha #88 (or whichever is next when this lands): "Phase-doc verbatim Rust test samples must be validated against current workspace lints before adoption — `forbid(unsafe_code)` is non-overridable and rustc 1.84+ marks env-var mutation `unsafe`." (This stage's gotchas #83-#87 are infra-level; the lint-vs-sample issue is a META-gotcha worth recording.)
- [ ] Consider a §6 note: "for any wdio onPrepare hook spawning npm/npx CLIs on Windows, use `shell: true` per the spawnSync `null` exit-code symptom." (Pairs with gotcha #56 / #74 family.)

### `TEMPLATE.md` updates

- [ ] None — the template is sound. The phase doc itself needed an authoring-time gate.

### Milestone document updates (this milestone's `M08.5.5-mcp-resilience.md` if a stage prompt was off)

- [ ] § A.3.1 `wdio.conf.ts::onPrepare()` block: replace the `cargo build --release -p agent-runtime -p runtime-drone -p runtime-sandbox` recommendation with the CI-mirrored sequence: `npx tauri build --no-bundle` for the agent-runtime crate, then `cargo build --release -p runtime-drone -p runtime-sandbox --bins` for the siblings. The cargo-vs-tauri-build distinction is the Tauri compile-mode env-var setup that determines whether the binary uses `frontendDist` or `devUrl`. (Note: this stage's commit closes the issue forward; this Decision is for any future phase-doc author who might reuse the pattern.)
- [ ] § A.3.2 Rust test samples: cite the `temp-env` callback API + `forbid(unsafe_code)` rationale rather than the direct `std::env::set_var` form. (Same: closed forward by this stage.)

### Protocol additions

- [ ] None this stage. The Stage 0 real-app discovery protocol (revision 2) worked exactly as designed — it surfaced the 🔴 #5 manifestation in smoke #6 cleanly + routed per existing decision.

### Open issues to file

- [ ] None this stage. 🔴 #5 (smoke #6 reload reconstruct) is already routed to M08.6 Stage A intake per the prompt's `<scope_locks>` and `docs/m08.5-irl-re-verify-handoff.md`'s routing decision.

---

## [END] User-review notes

> The user reviews this retrospective alongside the PR. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M08.5.5 Stage `A.fix`, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-cycle-as-PR pattern, this stage's commits land on the cycle feature branch; the PR drafts and opens only at the end of the final stage (D.fix re-verify).

**Claude:** I have completed M08.5.5 Stage `A.fix` per the stage prompt. The retrospective above reflects my honest self-assessment. The red commit (`f603524`) was approved + landed; the impl commit is staged but **uncommitted, awaiting user approval**. The PR remains undrafted until D.fix.

**Surfaced at:** 2026-05-24 ~15:15 UTC
