# M08.8.B.fix2 — Stage Retrospective

> **Stage:** M08.8 Stage `B.fix2` — close TD-044 (reload reconstructs the graph from persisted signals) — see the `<work_stage_prompt id="M08.8.B.fix2">`
> **Parent milestone:** M08.8 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m08.8-design-workbench`
> **Prior stage:** M08.8.B.fix (Light Instrument restyle; logged TD-044 🟢, reclassified 🟡 here)
> **Type:** diagnose-first bug fix — renderer mount + new backend command + signal→event translator rewrite; no schema/enforcer/drone-protocol change

---

## Pre-flight (claimed before session start)

- [x] `CLAUDE.md` loaded; §4 Hard Rules held throughout (no commit/push until approval; nothing bypassed; `.env.local` not modified; test gating not changed).
- [x] Read-first per the prompt: `CLAUDE.md` §4/§5/§6, `STAGE-PROMPT-PROTOCOL.md` close_gate, `cluster-pattern.md`, the failing `smoke.e2e.ts` leg, TD-044, the projectors, `graphStore.ts`, `commands.rs` replay/recover paths, `M03-live-graph.md`.
- [x] Diagnose-first: surfaced the diagnosed root cause for approval BEFORE implementing (twice — the initial localStorage finding, then the deeper translator finding when the first fix proved insufficient).

---

## [LIVE] Friction events

| When (step) | What happened | Severity (1–5) |
|---|---|---|
| post-implement, first real run | My approved (layer-a) fix passed all gates but the real reload leg STILL failed. The initial diagnosis (localStorage wipe) was **necessary but not sufficient** — a second, deeper bug (the translator) was masked. Caught only by running the assembled app. | 3 |
| green-phase clippy | `needless_pass_by_value` on the in-source `signal(payload: Value)` test helper; switched to explicit `Map` insert. | 1 |
| green-phase workspace test (one run) | `recovery_lifecycle_round_trip_via_drone_ipc` failed once under parallel `cargo test -p runtime-main` load; passed 2/2 run alone and clean in `cargo test --workspace`. Known Windows subprocess flake (gotcha #56 / TD-005), unrelated to replay. | 1 |

The Severity-3 event is the load-bearing lesson: **a green gate suite (incl. unit + integration replay tests) was structurally blind to the bug because the fixtures fabricated the wrong signal shape** — exactly the §5 tautology-test failure mode. Only the real-app run (CLAUDE.md rule 11) exposed it. The diagnose-first protocol's "trace ALL FOUR layers" gotcha explicitly warned of layer (b) "signals read but reducer doesn't fire"; I initially locked on layer (a) and had to re-open.

---

## [LIVE] Ambiguity events

| Source in conflict | What was unclear | How resolved |
|---|---|---|
| strict-TDD `git diff <red>..<impl> -- '**/tests/**'` EMPTY vs the real fix | The fix required rewriting `crates/runtime-main/tests/sdk_replay.rs` (under `tests/`), whose fixtures encoded the fabricated shape — impossible under a literal "no test files touched." | Two-commit TDD: a **test commit** rewriting `sdk_replay.rs` to real serialized-`AgentEvent` payloads (proven RED against the old translator: 3/4 fail), then the **impl commit** touching zero `tests/` files. The invariant holds with `<red>` = the test commit. Orch pre-approved the test-layer change. |
| in-source vs `tests/` test placement | `replay.rs` + `commands.rs` in-source `#[cfg(test)]` tests also encoded the fabricated shape. | In-source tests live in production `.rs` files (NOT under `**/tests/**`), so they ride the impl commit without breaking the invariant — consistent with the M06.5.A.fix binary-crate precedent. |

---

## [LIVE] Surface / protocol-drift / surprise events

- **Surprise (the core finding):** `payload_json` IS a fully serialized `AgentEvent` (`#[serde(tag="type")]`). The hand-rolled translator reinvented the deserialization by hand against invented field names and was pinned by fabricated fixtures — so it produced ZERO events against real data while staying green. The fix is a one-line `serde_json::from_value::<AgentEvent>` that deletes ~160 lines of buggy reinvention. Logged as the sweep TD-045 (🟡).
- **Surprise (ruled out):** `app_local_data_dir`/`session.sqlite` DOES survive `reloadSession` (77 signals before & after — verified by probe); only renderer `localStorage` is wiped. This narrowed the fix to "backend supplies the session id," not "persist the DB differently."
- **Protocol drift:** none. Hard Rule 1 held. Two-commit strict-TDD invariant holds by construction. CI-parity: all gate commands byte-identical to §6; `ANTHROPIC_API_KEY` supplied per-run via shell prefix from the existing `ANTHROPIC_TEST_KEY` (no `.env.local`/wdio/gating change).

---

## [END] Stage B.fix2 special log (per `<retrospective_requirements>`)

- **Which of the 4 trace layers the root cause lived in:** BOTH (a) and (b).
  - **(a) signals not read on reload** — `localStorage.lastSessionId` wiped by `reloadSession` (fresh WebView2 profile) → replay never fired. Fixed by backend `replay_latest_session` (reads the latest session WITH signals from the persisted log) + App.tsx fallback + await-listener-before-replay.
  - **(b) signals read but reducer doesn't fire** — `replay.rs::translate_agent` read `payload_json.event` (a field that never existed; the discriminator is `payload_json.type`) → 0 events. Fixed by direct serde deserialization. **This layer was the dominant cause** and would have failed the test even with (a) fixed; (a) alone left it red.
  - (c) store update / (d) React Flow render were sound (proven: `agent-node-*` testid + `applyEvent` idempotence; the live smoke leg renders identically).
- **5/5 flake-stability:** held — 5/5 consecutive green on the reload leg, no variance (each run 7–11 s wall). No `waitUntil`/sleep/timeout bump was used (the prompt's banned bandaids); the path is now correct, not slow.
- **Regression check:** full `test:e2e:tauri` **8/8** spec files green (the other 7 specs unaffected by the App.tsx mount reorder + replay-latest fallback); vitest 624/624; Playwright 59/59; workspace tests green.

---

## [END] Self-assessment (3 axes per `PROCESS-VALIDATION.md`)

- **Process fidelity:** diagnose-first honored (surfaced before implementing, twice); grounded-claims honored (led with the contradiction when the first fix proved insufficient, rather than declaring done). The one process miss: my **initial** diagnosis over-narrowed to layer (a) despite the gotcha's "trace ALL FOUR" warning — corrected by the assembled-app run, which is exactly the rule-11 safety net working.
- **Product correctness:** root-cause fix at both layers; verified on the assembled running app (5/5 + 8/8), not inferred from green units. The translator fix also repairs `replay_session` (the localStorage path) and every other event variant, not just the test's agent nodes.
- **Pattern/discipline:** strict-TDD two-commit invariant preserved; fabricated fixtures replaced with real serialized-event round-trips (§5); coverage held (workspace 91.92% ≥ 80; runtime-main 96.27% ≥ 95; `replay.rs` 97.27%); new debt logged honestly (TD-045 🟡).

**Outcome:** Sound. The fix is at the root cause, observed in the assembled app, with the strict-TDD invariant and all §6 gates green.

---

## [END] Decisions for the next stage

- **`replay.rs` is now serde-round-trip — do not re-hand-roll it.** Any new `AgentEvent` variant replays for free; no translator edit needed.
- **TD-045 sweep is open** — the drone projectors and `ipc.ts` hand-mirrors are candidate sites for the same fabricated-fixture class; address on any future replay/recovery/audit touch.
- **The reload→reconstruct contract now lives in the backend** (`replay_latest_session` reads the latest persisted session). Multi-session (post-v0.1) should revisit "latest session" semantics; v0.1 single-session is correct.
- **Parking-lot — protocol sharpening (not a change this stage):** the in-source `#[cfg(test)]` tests in `src/sdk/replay.rs` were rewritten in the **impl** commit (C2). This is *literally* compliant with the strict-TDD `git diff <red>..<impl> -- '**/tests/**'`-empty invariant (in-source tests are not under `**/tests/**`), but it is a small structural gap: an impl commit silently rewrote tests. Recommend the protocol either (a) require a **3-commit** structure when in-source tests must change (red `tests/` commit → in-source-test commit → impl commit), or (b) require an explicit `TEST-REWRITE-IN-IMPL: <files>` marker in the impl commit body that surfaces the deviation for review. Logged for the next protocol-iteration pass; no protocol change here.
