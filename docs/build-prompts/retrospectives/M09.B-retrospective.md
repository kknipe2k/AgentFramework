# M09.B — Stage Retrospective

> **Stage:** M09 Stage `B` — see `docs/build-prompts/M09-workbench-vertical-slice.md` (Stage M09.B)
> **Parent milestone:** M09 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-07 19:20 local
> **Session end:** 2026-06-07 19:55 local
> **Branch:** `claude/m09-prep` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `edb366d`
> **Ending commit:** `17355c4` (red `66add9d` → impl `4f93a5e` → prettier-wrap `be71df9` → fixture `17355c4`)
> **Estimated effort (per prompt):** ~1.5 hours
> **Actual elapsed:** ~0.6 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md auto-loaded; §4 Hard Rules stated at the top of the first response.
- [x] All "Read first" files read in order (M09 phase doc Stage B B.1–B.6; common.v1.json `#/$defs/Capabilities` + agent.v1.json; builderStore.ts `builderAgent`/`updateNode`; NodeConfigPanel.tsx `findAgent`/fields/`AllowedList`; capability_live_tool.rs E-02; + the M09.A retrospective per §19 rule 1).
- [x] Deliverable (3 sentences) + test plan (3 bullets) stated before any test file was written.
- [x] User confirmed the red phase before implementation began ("M09.B red approved — implement…").
- [x] Branch correct (`claude/m09-prep`).
- [x] `git status` clean at start (only the 3 untracked red-test files added during the red phase).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| verify_gates / full vitest | The new `FileAccessEditor` reads `agent.capabilities.file_access`; the pre-existing `NodeConfigPanel.test.tsx` fixture `frameworkWithAgent()` omitted `capabilities` (already schema-invalid per agent.v1.json:9), so 5 existing render tests crashed once the panel rendered the editor. Fixed by adding the full minimal-valid `Capabilities` to that fixture — a SEPARATE existing test file (not this stage's red tests), committed separately (`17355c4`) so the red→impl diff stayed EMPTY. | The construction-reachability note could flag "existing NodeConfigPanel render-test fixtures omit `capabilities`; rendering the new editor will require updating them" so the regression is anticipated, not discovered at the gate. | 2 |
| verify_gates / prettier | The red test file's `toHaveBeenCalledWith` line exceeded printWidth; `prettier --check` flagged it. Mechanical wrap in a separate labelled commit (`be71df9`) — the M09.A tsc-cast-fixup precedent — so the red→impl test diff stayed EMPTY. | The strict-TDD red phase could note "run `prettier --check` on new test files before the red commit" (companion to M09.A's `tsc --noEmit` nudge). | 1 |

---

## [LIVE] Ambiguity events

None observed. The phase doc B.3 pinned the exact `capabilities` shape and the editor wiring; the only judgment call (fix the stale fixture vs. add defensive `?.`) resolved cleanly to the fixture, since `capabilities` is schema-required and defensive code would mask the contract.

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After red tests confirmed failing | Red-phase bundle (cross-machine state, 6 failing unit tests + right-reason evidence, the e2e, pinned wires, scope-lock confirmation) + "will not commit until you approve" | Yes — the user approved + directed implement in one reply | Yes — at the red checkpoint |
| After green gates | This final surface (green evidence + retrospective) | (pending user review) | At the green checkpoint |

---

## [LIVE] Protocol drift events

None observed. Red committed only after explicit approval; impl commit does not touch test files (`git diff 66add9d..4f93a5e -- '**/tests/**'` is EMPTY); the two mechanical test-file changes (prettier wrap + stale-fixture fix) are separate labelled commits.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The impl was ~15 src lines + a self-contained `FileAccessEditor` sub-component; the only "work" was the regression to the existing fixture. | The phase doc thesis ("the substrate already executes; the authoring gap is UI-thin") held again — `updateNode` already merged the `{capabilities}` patch with zero store change, exactly as M09.A predicted. | Confirms the vertical-slice scoping for C/D: each authoring affordance is a thin editor over an already-executing substrate. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **replaced by an assembled test** — the mechanical regression IS the discovery | `tests/e2e-tauri/builder_file_access.e2e.ts` — create agent → select (real `onNodeClick`) → grant a Write glob + a Read glob through the real File-access editor → `capabilities.file_access` reads back `{read:["data/**"],write:["out/**"]}` off the live `window.__builderStore`; the lists render the globs. | None. The *enforced* write is intentionally NOT exercised here — that is M09.D's observation (CLAUDE.md §4 rule 11). |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | CLAUDE.md sufficient orientation? | 5 | Read-first list + wire-audit landed the change precisely. |
| 2 | Did the "Read first" list orient correctly? | 5 | Every pinned line (Capabilities @39, updateNode @522, AllowedList @32–79, E-02) was load-bearing. |
| 3 | Deliverable + test plan before code per §16? | 5 | Stated before any test file. |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | One round (the stale fixture) — fixed immediately, correctly diagnosed (fixture, not defensive code). |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed only after explicit approval; no push. |
| 6 | Escalate/proceed split right? | 5 | No escalation needed; declaration-only scope held. |
| 7 | Human-direction load reasonable? | 5 | One approval at the red checkpoint. |
| 8 | Surfaces actionable? | 5 | Red surface drove a one-line approval. |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | `FileAccessEditor` named for what it is; comment explains *why* (declaration-only, the ADR-0032 widening). |
| 2 | Behavior tests, not tautology? | 5 | Asserts observable outcomes — the editor's `updateNode` patch, the rendered glob lists, the real-app store read. The preserve-other-fields test pins the immutable-spread invariant. |
| 3 | Public APIs documented? | 5 | `builderAgent` + `FileAccessEditor` carry doc comments with the schema-required + rule-11 rationale. |
| 4 | Exactly what was promised? | 5 | builderAgent capabilities + the Read/Write file_access editor; no enforcer/tier/schema touch; network/shell/spawn_agents UI correctly deferred. |
| 5 | Stranger-understandable? | 5 | Comments name the M09.B intent, E-02, and the declaration-only boundary. |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no stringly-typed surprises, no ad-hoc allows, no defensive `?.` masking the schema contract. |
| 7 | Coverage measured ≥80%? | 5 | Workspace vitest 93.59% lines (660/660 pass); builderStore.ts 96.37%; the editor exercised by 4 render/add/remove tests. |
| 8 | CI green across the matrix? | 4 | Frontend gates + renderer Playwright (59) + real-app `tauri-driver` (14/14 spec files; `builder_file_access.e2e.ts` isolated re-run 1/1) green on Windows; Linux/macOS legs run in CI on push (renderer e2e OS-portable; tauri-driver Linux+Windows). |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | Wire-audit + construction-reachability were directly load-bearing. |
| 2 | Sections needed but missing? | 4 | A "rendering a new field will break existing fixtures that omit now-required schema fields" nudge would have pre-empted friction #1. |
| 3 | Gotchas local or generalize? | 4 | "Adding a UI field that reads a schema-required slice breaks stale test fixtures that predate the requirement" is a general TS-stage trap — candidate gotcha (pairs with M09.A's prettier/tsc-the-red-tests note). |
| 4 | Time-box within 2×? | 5 | ~0.6h vs ~1.5h. |
| 5 | Implicit protocol moments? | 5 | The red/impl/prettier/fixture four-commit shape was explicit and clean. |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the e2e is key-independent and runs in CI. |
| 7 | Copyable to remaining stages? | 5 | C/D inherit the same NodeConfigPanel/updateNode editor seam. |
| **Pattern axis total** | | **33/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `66add9d` committed only after "M09.B red approved…"; impl `4f93a5e` + the two mechanical follow-ups per the same instruction. No push.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — table empty.
- [x] **G4: stage completed** — impl `4f93a5e` on `claude/m09-prep`; B.4 acceptance met (file_access authored on the real app); no execution-status flip (correct — that's M09.D).
- [x] **G5: every row ≥3** — lowest individual score is 4.
- [x] **G6: CI-parity** — commands run match CI:
  - `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'` ✓
  - `npx eslint .` ✓
  - `npx tsc --noEmit` ✓
  - `npm run test` (= `vitest run --coverage`) → 660/660, 93.59% lines ✓
  - `npm audit --audit-level=high` → 0 high/critical (2 moderate, below the gate) ✓
  - `npm run test:e2e` (Playwright renderer) → 59 passed ✓
  - `npm run test:e2e:tauri` (wdio + tauri-driver) → 14/14 spec files passing, incl. `builder_file_access.e2e.ts` (isolated re-run against a freshly-built binary: 1/1) ✓
  - No CI-divergent flags used.

### Soft gates

- [x] **S1:** Process ≥30/40 — actual 40
- [x] **S2:** Product ≥32/40 — actual 39
- [x] **S3:** Pattern ≥25/35 — actual 33
- [x] **S4:** Time-box within 2× — ~0.6h vs ~1.5h
- [x] **S5:** ≤3 Severity-3 events — 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to M09.C with the minor decision below.

---

## [END] Coverage holdouts

n/a — renderer/TS-only stage; no Rust crate gate touched, no `--ignore-filename-regex` change. Workspace vitest line coverage 93.59% (≥80% gate); builderStore.ts 96.37%. No exclusions added.

---

## [END] Decisions for the next stage (or next parent milestone if this is the final stage)

### `CLAUDE.md` updates

- [ ] None required this stage.

### `TEMPLATE.md` updates

- [ ] None.

### Milestone document updates

- [ ] M09.C inherits the green M09.B seams: `builderAgent` now mints a valid `Capabilities`, and `tools_called` is empty — M09.C's "attach an MCP tool mirrors into `capabilities.tools_called`" wiring lands on this grant via the same `updateNode({capabilities})` patch the File-access editor uses.

### Protocol additions

- [ ] Candidate `docs/gotchas.md` entry: rendering a new UI field that reads a now-required schema slice (here `capabilities.file_access`) breaks pre-existing test fixtures that predate the requirement — update stale fixtures in the same impl pass (separate labelled commit to preserve the red→impl invariant). Low priority; pairs with M09.A's "prettier/tsc the red tests" note.

### Open issues to file

- [ ] None.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M09 Stage `B`, per `CLAUDE.md` §19. The stage's commits land on the `claude/m09-prep` feature branch; the milestone PR drafts only at the end of the final stage (M09.D).

**Claude:** I have completed M09 Stage `B`. The red phase was approved before commit; the impl commit does not touch the test files (`git diff 66add9d..4f93a5e -- '**/tests/**'` is EMPTY); the prettier-wrap + stale-fixture changes are separate labelled commits. Frontend gates + renderer Playwright are green; the real-app `tauri-driver` leg is finalized in G6 above. No execution-status flip (correct for M09.B — the *enforced* write is M09.D's observation, CLAUDE.md §4 rule 11). I will not push or open a PR until directed.

**Surfaced at:** 2026-06-07 19:55 local
