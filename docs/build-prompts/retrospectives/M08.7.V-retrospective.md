# M08.7.V — Verifier Retrospective

**Date:** 2026-06-02
**Verifier session ID:** session_<current> (fresh clear-and-paste session — bias guard)
**Milestone phase doc:** docs/build-prompts/M08.7-execution-engine.md
**Iteration:** 1
**Prior V iteration retros:** N/A
**Scope:** rungs 1–5 (Stages A–E) only. Rungs 6–N (sub-agents, plan, hooks, MCP, ARIA) moved to M08.9 — out of scope; their absence is expected, not an inventory gap.

**Cross-machine state at start:**
```
git log --oneline main..HEAD: 45 commits (f7f9b7e..34bb21b)
ls docs/build-prompts/retrospectives/M08.7.*-retrospective.md:
  M08.7.A-retrospective.md  M08.7.B-retrospective.md  M08.7.C-retrospective.md
  M08.7.D-retrospective.md  M08.7.E-retrospective.md  M08.7.X-retrospective.md
  (NOT read — bias guard, ADR-0008)
```

---

## Per-pass summary

| Pass | Findings | Notable |
|---|---|---|
| Inventory | 0🔴 / 1🟡 / 0🟢 | All 5 rung source modules + all 5 assembled test files + 3 live-smoke files present and shape-matching. **But** the milestone's named deliverable "ADR-0026 — built-in tool execution contract" was never filed; the 0026 number resolves to an unrelated `plan-loop-vdr-carry-forward-routing` ADR (🟡 #1). |
| Wire | 0🔴 / 0🟡 / 0🟢 | Every rung-3/4/5 execution claim traces to a **production** call site in `drive_stream` (agent_sdk.rs:619/642/681) — not a `#[cfg(test)]` harness. Rung-5 cost is real (`CostBreakdown`→`estimate_cost`→`record_spend`), not hardcoded. |
| Behavior | 0🔴 / 0🟡 / 0🟢 | Every rung asserts the **side effect**, never the event alone: file-on-disk (1,2), tool_result-in-turn-2-config + final-text-quote (1,3), suspend = exactly-one-provider-turn (4), run-halt = counted turns (5). No M08.6.7 #32-class event-only trap. |
| Multi-call invariants | 0🔴 / 0🟡 / 1🟢 | Rung 3 composition (both skill bodies in the *last* config) ✓; rung 5 idempotence (warn fires exactly once across 3 in-band turns) ✓. Rung 1 has **no** explicit two-reads-feed-back test (🟢 #2 — low signal; the multi-turn re-stream is proven by rung 1's single-read→quote path + rung 3/5 repeated dispatch). |
| **Assembled-execution (5th)** | 0🔴 / 0🟡 / 0🟢 | **RAN** all 5 rung assembled test files (40 tests) + the full `runtime-main` crate (468 lib + all integration binaries, **0 failures**). Each side-effect assertion observed passing by execution. Live (real-Anthropic) smokes `#[ignore]`d + no key — **not run** (the maintainer's per-stage IRL gate, out of V's reach). |

---

## Findings

#### 🟡 #1 — Inventory: ADR-0026 "built-in tool execution contract" deliverable absent; number reused

**Contract:** The phase doc names "Two ADRs. **ADR-0026 — The built-in tool execution contract** (Stage A's archetype frozen…)" as a milestone deliverable (§"What this milestone produces" item 6; Implementation-Workflow steps 8–9; Verification Checklist line 1540 "ADR-0026 (built-in execution contract)"). The V `<read_first>` points at `docs/adr/0026-builtin-tool-execution-contract.md`. CLAUDE.md §11 mandates an ADR for any change that adds a §0a Capability-Matrix primitive — in-process built-in tool execution (Read/Write through `CapabilityEnforcer::check`) is one.
**Observed:** No such ADR exists. `docs/adr/0026-*` resolves to `0026-plan-loop-vdr-carry-forward-routing.md` (Status: Accepted; about routing two M08.V carry-forwards — an unrelated concern). The built-in-execution-contract ADR was never filed; the 0026 number was repurposed. The dangling reference propagates: ADR-0027's "Related" section cites "ADR-0026 (the built-in tool execution contract — the feedback contract this mirrors)", and the phase doc's closeout step says "ADR-0026 + ADR-0027 flip Proposed→Accepted" — but ADR-0026 (plan-loop) is *already* Accepted and is a different document, so that instruction is itself stale.
**Expected:** Either a filed ADR documenting the Stage-A archetype (file_access-scope-is-the-boundary; in-process-vs-sandbox split with Bash deferred; the multi-turn `feedback.dispatched` contract every later rung mirrors), or an explicit decision recording where that contract lives instead.
**Impact:** Does NOT block execution-is-real (rung 1 ships, is IRL-observed, tests green; the contract is *de facto* recorded in the phase-doc Stage A section + code + ADR-0027's description). It is a contract-traceability gap that actively misleads any reader chasing "ADR-0026 (built-in execution contract)".
**Action (zero-propagation, fix-in-cluster at closeout — NOT carry-to-M09):** At closeout, either (a) file the built-in-tool-execution-contract ADR under the next free number and correct the phase-doc + ADR-0027 cross-references to it, or (b) record a one-line scope-out waiver-ADR stating the contract lives in the phase-doc Stage A section + ADR-0027 and why a dedicated ADR is unnecessary. Either way, fix the stale "ADR-0026 flips Proposed→Accepted" closeout instruction (ADR-0026 is already Accepted-and-different; only ADR-0027 is Proposed).

#### 🟢 #2 — Multi-call: rung 1 has no explicit "two reads in one session both feed back" test

**Contract:** V `multi_call_invariants` pass: "rung 1 — two reads in one session both feed back."
**Observed:** `builtin_tool_execution.rs` has no test issuing two Reads in one session. The multi-turn re-stream feedback IS proven once (`read_feeds_file_contents_back_and_agent_quotes_them` — turn-2 config carries the result, final text quotes it), and the repeated-dispatch path is independently exercised by rung 3's `two_load_skills_compose_additively_in_context` and rung 5's 3-turn budget tests.
**Impact:** Low signal — the underlying mechanism (accumulating `config.messages`, re-sent every turn) is the same one those tests exercise.
**Action:** Log to `docs/tech-debt.md` (test-completeness). Optional additive test in a future stage; no fix required to merge.

#### 🟢 #3 — Behavior: rung 4 "recoverable" BDD leg proven indirectly, not by snapshot-rebuild

**Contract:** D.4.5 BDD: "the suspended state is recoverable" (phase doc D.4.1: "the gap event + session state persisted — recoverable per §1b").
**Observed:** `gap_detection_execution.rs` asserts the load-bearing legs directly (suspend = `configs.len()==1`, no further turn; gap event in trace with `requested_via=request_capability`; no `ToolInvoked` for `request_capability`). The gap event is persisted to the drone signal chain via `self.emit` (the recoverability substrate), but no assembled test asserts a rebuild-from-snapshot resumes the suspended session.
**Impact:** Minor — "suspends cleanly, no crash, no continue" (the v0.1 grounded acceptance) IS proven; rebuild-resume is a recovery-lifecycle concern already covered by `recovery_lifecycle.rs`/`plan_recovery.rs` for the general mechanism.
**Action:** Log to `docs/tech-debt.md` (test-completeness). No fix required to merge.

---

## Verification axes scoring

| Axis | Score (1–5) | Rationale |
|---|---|---|
| **Coverage adequacy** | 5 | All five V.2-scope rungs checked across all five passes; the 5th pass RAN every assembled rung + the full crate. The only un-run surface (real-Anthropic smokes) is keyless-by-design and explicitly the maintainer's IRL gate, reported honestly. |
| **Finding signal-to-noise** | 5 | The single 🟡 is traceable to a named deliverable + a quotable CLAUDE.md §11 rule + dangling cross-references; the two 🟢 are honest test-completeness notes, not false positives. No 🔴 manufactured. |
| **Fresh-context discipline** | 5 | Clear-and-paste session; the six `M08.7.{A–E,X}-retrospective.md` files + the (nonexistent) summary + post-M08.6 gap-analysis were NOT read. Reconciled only against spec / ADRs / phase-doc body / code. |

Sum = 15 (avg 5.0). No soft signal.

## Standing-rule compliance (v1.8)

- **Decision 6** — delivered+tested / driver-absent / accepted-ADR-root → 🟡 + enumeration: **Not triggered** — every rung 1–5 primitive has a confirmed *production* driver (the wire pass verified call sites at agent_sdk.rs:619/642/681). The "no production caller" condition that defined the M08.6 escape does not recur. No `<wire_trace_vs_adr_reconcile>`/`<scope_change>` block applied.
- **Decision 7** — `--features integration` smoke: **N/A for the real-Anthropic smokes** — `skill_load_live.rs` / `gap_detection_live.rs` / `budget_live.rs` are `#[ignore]`d and require `ANTHROPIC_API_KEY`; **no key in this session → not run** (NOT recorded as 0/0-pass; these are the maintainer's per-stage IRL gates). The stub-driven assembled tests (the CI gate) ran: **40/40 pass + full crate 468 lib + integration, 0 failures.**

## Outcome

**Sound** — all five passes produced clean signal; no 🔴. Merge recommendation: **proceed to closeout**, with 🟡 #1 resolved in-cluster at closeout (zero-propagation).

**Grounded-by-execution vs hypothesis (rule 11 — V's own verdict bound at its granularity):**
- Rungs 1, 2, 3, 4, 5 — **grounded-by-execution at the assembled-stub granularity.** I RAN each rung's `run_test_session_with*` assembled test (cargo test, observed pass) and the full `runtime-main` suite (0 failures); each side-effect assertion (file on disk, turn-count halt, suspend, skill body in config) is observed, not inferred.
- The **real-Anthropic behavioral leg** (rung 3 "reply in ALL CAPS"; rung 4 a real model raising a gap; rung 5 a real run hard-stopping under a tiny cap) — **NOT run by V** (no key). Per the phase doc these are the maintainer's per-stage IRL closes (recorded in the per-stage retros, which V did not read). V's verdict for those legs is therefore: **grounded by the maintainer's IRL per the phase-doc close record, not re-verified here.** Stated, not smoothed.

## [END] Decisions for closeout

- Decision 1: 🟡 #1 → at closeout, file the built-in-tool-execution-contract ADR under the next free number (and fix ADR-0027 "Related" + the phase-doc references), OR file a one-line scope-out waiver-ADR; AND correct the stale closeout instruction "ADR-0026 flips Proposed→Accepted" (ADR-0026 is already Accepted-and-different; only ADR-0027 is Proposed). Addresses finding #1.
- Decision 2: 🟢 #2 → log to `docs/tech-debt.md` under "test-completeness": rung-1 lacks an explicit two-reads-in-one-session feedback test (mechanism proven elsewhere).
- Decision 3: 🟢 #3 → log to `docs/tech-debt.md` under "test-completeness": rung-4 "recoverable" proven indirectly (event persisted), not by an assembled snapshot-rebuild assertion.
- Decision 4 (V protocol): none — the five-pass v1.9 shape worked; the 5th pass caught nothing escaped because nothing escaped (the wires are real). No template change recommended.

**Cross-machine state at end:**
```
git log --oneline main..HEAD: 45 commits (f7f9b7e..34bb21b) + this V retro (pending commit)
ls docs/build-prompts/retrospectives/M08.7.*-retrospective.md:
  M08.7.A..E + M08.7.X + M08.7.V-retrospective.md (this file)
```
