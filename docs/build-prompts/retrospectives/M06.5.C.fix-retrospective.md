# M06.5.C.fix — Stage Retrospective

> **Stage:** M06.5 Stage `C.fix` — see `docs/build-prompts/M06.5-irl-fix.md`
> **Parent milestone:** M06.5 IRL fix cycle (M07 Stage A gating remediation; work-stage-class D.fix per ADR-0008 — no closeout, no gap-analysis entry; verification + docs stage, no `<tdd_discipline>`)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-17
> **Session end:** 2026-05-18
> **Branch:** `claude/m06.5-irl-fix`
> **Starting commit:** `5fbf1dd` (ADR-0013 — branch tip; A.fix `7fc3277` + B.fix `9653718` + follow-up `f1129e4` all present)
> **Ending commit:** `uncommitted — awaiting final approval` (Resolution append + CHANGELOG + summary + this retro staged)
> **Estimated effort (per prompt):** 1–2 hours
> **Actual elapsed:** ~1 hour

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in force (do-not-commit/push without approval; no Co-Authored-By; don't act on a false premise)
- [x] Read-first files read in order: CLAUDE.md, STAGE-PROMPT-PROTOCOL (project memory), `docs/M06-irl-findings.md` (both 🔴 + 🟡-1..4 + disposition), `docs/M06-irl-test-plan.md` (the corrected 🔴-1 Scenario B / 🔴-2 Scenario A6+C-7 repros), `docs/build-prompts/M06.5-irl-fix.md` (Background/Key constraints/C.1–C.4), `M06.5.A.fix-retrospective.md`, `M06.5.B.fix-retrospective.md`, gotchas #23/#81 (+ #56/#22 from B.fix retro), RETROSPECTIVE-TEMPLATE
- [x] Deliverable + test plan stated before work
- [x] Branch correct: `claude/m06.5-irl-fix`; working tree clean at start (only untracked, not-ours `.claude/scheduled_tasks.lock`)

### Pre-flight checks (prompt-mandated)

- [x] branch == `claude/m06.5-irl-fix`
- [x] `git log` shows **both** the M06.5.A.fix impl `7fc3277` and the M06.5.B.fix impl `9653718` (+ follow-up `f1129e4`, ADR-0013 `5fbf1dd`)
- [x] `docs/M06-irl-findings.md` and `docs/M06-irl-test-plan.md` present on the branch

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| verify_gates / first workspace llvm-cov run | Reflexively added `--features runtime-mcp/test-helpers` to the *workspace* llvm-cov gate (it belongs only on the `cargo test` gate + the runtime-mcp *package* gate per CI). Caught immediately against the CI workflow (line 363-365 has no `--features`); re-ran the byte-exact CI command (91.57%, vs the inflated 92.47%). One extra ~6-min measurement. | The `<gates milestone="M06"/>` reference resolves to CLAUDE.md §6; the workspace llvm-cov command there carries no `--features`. Self-caught via the CI-parity hard rule — no prompt change needed; logged as the discipline working. | 2 |
| manual_repro step | The phase doc C.3 anticipates gotcha #23 ("the UI step is a documented manual observation") but does not state that the agent **cannot perform the manual repro at all** — it is a user-machine GUI session. Required surfacing a decision (run-it-yourself vs C.3 path-1 fallback). | C.3 / the C.5 prompt could state explicitly: "the manual repro is a user-machine GUI step the agent cannot drive (gotcha #23); the agent runs the automated A/B regression tests as the durable verification of record and surfaces the manual UI step as a single optional user confirmation." Recorded for M06.6. | 2 |
| post-gates / external "Resume M06.5.B.fix" instruction | A misdirected instruction arrived asking to resume + commit **B.fix** (already fully committed: `9653718`/`f1129e4`/`5fbf1dd`) and to read a background-task ID (`bi36x7bmt`) **not from this session**. Acting on it would have produced an empty/duplicate commit on a stale premise. | Not a phase-doc gap — this is the cross-session false-premise hazard CLAUDE.md §8/§19-rule-7 exists for. Handled correctly: surfaced the contradiction with the authoritative build-machine git state, did **not** act, did **not** fabricate a background-task read. | 3 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M06.5-irl-fix.md C.3/C.4 ("re-run the corrected repros … against the built app … record before/after") vs gotcha #23 (a Tauri 2.x window cannot be driven/observed from the agent side) | Whether the agent performs the manual repro or it is inherently a user GUI step | Surfaced via `AskUserQuestion` (run-it-yourself vs C.3 path-1 fallback, recommendation noted). User first chose "I'll run the manual repro now" (runbook provided), then on reconsideration directed the **C.3 path-1 fallback**: green A.fix+B.fix assembled-app regression tests as verification of record; manual UI step initially framed agent-blocked per gotcha #23 with the findings-doc "before" verbatim. A third maintainer reconciliation then refined the disposition: 🔴-1/🔴-2 **code-resolved + interim automated verification of record; real-app IRL re-confirmation deferred-and-tracked to the post-M07 IRL pass** (between-milestone IRL model) — deferred, not closed. All three were user-domain decisions, correctly escalated not picked. | Phase-doc detail — recorded for M06.6 (Cycle 2); C.3 already half-anticipated this (DB assertions = objective evidence, "also covered automatically by the A/B regression tests") |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After all gates green | The manual-repro decision (gotcha #23 constraint) via `AskUserQuestion`, with the C.3 path-1 fallback as an explicit option + the green automated A/B tests as the durable objective evidence | Yes — user chose, then refined to path-1 | Yes — after the agent-side work (gates) was done, exactly the §12 "do agent-side what's possible, escalate only the genuinely user-domain GUI step" posture |
| On the external "Resume B.fix" instruction | The full contradiction table (instruction-assumes vs actual build-machine state) + `git log --oneline main..HEAD` + the clean working tree + two concrete paths forward | Yes — user resolved it (directed the C.3 path-1 fallback, confirming the stale-resume was an out-of-date orchestration view) | Yes — immediately, before any commit; refused to act on the false premise per §8 |

---

## [LIVE] Protocol drift events

None. The harness "Co-Authored-By" default remains correctly overridden (session-URL + DCO footer only — to be applied at the C.fix commit on approval). The stale "Resume B.fix" instruction was a *potential* drift inducement (commit on a false premise / fabricate a tool read) — **not acted on**; surfaced per CLAUDE.md §8/§12/§19-rule-7. The reflexive `--features` on the workspace llvm-cov gate was self-caught by the CI-parity hard rule before it reached any surface.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| A stale orchestration instruction can arrive mid-stage citing a background-task ID from a different session and asking to re-commit already-landed work | The cross-session-blindness failure mode is usually framed as an *orchestration session reading stale origin*; here it manifested as a stale *instruction into the build session* | The build-machine git state must be the authoritative reconciliation surface for **inbound** instructions too, not only outbound surfaces. The §19-rule-7 "surface includes cross-machine state by default" already covers this — the guard worked. Recorded for M06.6. |

---

## [END] Special-log (prompt-mandated)

- **🔴-1 manual repro — before/after (verbatim).**
  **Before** (verbatim, `docs/M06-irl-findings.md` §🔴-1): *"Three DB
  files in `%LOCALAPPDATA%\dev.aria-runtime.app\`: the row landed in
  `mcp.sqlite` (4 KB, stale 12:49); the **live drone DB is
  `session.sqlite`** (3 MB, actively written, `mcp_servers` empty
  there); plus a 0-byte stray `mcp_servers.sqlite`. Three divergent
  path resolutions; the system reads `session.sqlite` → server
  invisible and (downstream) unusable."* and *"audit shows
  `mcp_installed` → **MCP Servers panel still says "No MCP servers
  installed."**"*
  **After:** 🔴-1 is **code-resolved** (`7fc3277`; ADR-0012). The
  **interim verification of record** is the three green assembled-path
  regression tests in `src-tauri/src/session_db.rs`
  (`registry_path_equals_drone_session_db_path`,
  `add_server_then_list_round_trips_through_the_same_store`,
  `no_stray_mcp_sqlite_path_literal_constructed`) — they prove both
  call sites resolve the one `session.sqlite` and the add→list
  round-trip is visible cross-connection (the assertion Stage-V
  structurally lacked). The **real-app IRL re-confirmation is
  deferred-and-tracked to the post-M07 IRL pass** per the
  between-milestone IRL model (maintainer-directed 2026-05-18); gotcha
  #23 (a Tauri 2.x WebView2 window cannot be driven/observed from the
  agent side) is *why* it cannot run in-stage — the card is **deferred,
  not closed**, until that pass re-confirms the observable behavior.
- **🔴-2 manual repro — before/after (verbatim).**
  **Before** (verbatim, `docs/M06-irl-findings.md` §🔴-2):
  *"`session.sqlite`: `signals = 0`, `token_usage = 0` — while
  `heartbeats = 15155` and `snapshots = 14` in the *same* DB. The
  drone is alive and persisting (heartbeats/snapshots) to the correct
  file, but the **agent signal stream lands nothing**. Signals exist
  in none of the three DBs."*
  **After:** 🔴-2 is **code-resolved** (`9653718`; ADR-0013). Interim
  verification of record: the three green assembled
  real-drone-subprocess tests in
  `crates/runtime-main/tests/smoke_signal_persistence.rs`
  (`smoke_session_persists_signals_to_live_drone_db`,
  `smoke_session_signal_count_matches_emitted_event_count`,
  `transient_signal_write_failure_does_not_abort_run`) + the
  `signal_kind_maps_each_coarse_category` unit test prove the assembled
  smoke path persists signals under the run's (now shared, ADR-0013)
  session id. The **real-app IRL re-confirmation is
  deferred-and-tracked to the post-M07 IRL pass** (same disposition as
  🔴-1; gotcha #23 is why it cannot run in-stage) — **deferred, not
  closed**. **`token_usage` remains `0`** by the distinct open finding
  (no production `token_usage` writer) — carried to the post-M07 IRL
  pass, **not** a regression.
- **A.fix + B.fix regression tests green in the canonical CI suite.**
  Run this session via the exact CI command
  `cargo test --workspace --features runtime-mcp/test-helpers` (the
  `cargo-test` job step in `.github/workflows/ci.yml`): all six
  regression tests + the `signal_kind` unit test `... ok`, **0 failed**
  workspace-wide. They exercise the assembled composition (real drone
  subprocess / the path both `src-tauri` call sites resolve), not the
  isolated components Stage-V verified — the precise gotcha #66 gap.
- **Resolution section APPENDED (prior lines untouched).**
  `git diff --numstat docs/M06-irl-findings.md` → **`153  0`** (153
  insertions, 0 deletions) — the prior Verdict / 🔴 / 🟡 / 🟢 / Passed
  / Disposition lines are byte-identical; the `## Resolution (M06.5 fix
  cycle)` section is purely additive (append-only audit-trail
  discipline, though the file is not the `docs/gap-analysis.md`
  append-only ledger).
- **M07.A carry-forward (explicit).** 🔴-1 + 🔴-2 are
  **code-resolved** (`7fc3277` / `9653718`; ADR-0012 / ADR-0013) with
  the automated assembled-composition regression tests as the
  **interim verification of record** → **M07 Stage A is unblocked** (on
  the code fix + interim automated verification). The **real-app IRL
  re-confirmation is deferred-and-tracked to the post-M07 IRL pass**
  per the between-milestone IRL model (maintainer-directed 2026-05-18)
  — gotcha #23 is why it cannot run in-stage; the cards are
  **deferred, not closed**. **🟡-1..4 carry to M07.A unchanged** (HITL
  `ui_variant`; `npx` Windows `.cmd` shim; stale Test banner; budget
  settings not state-wired). The **post-M07 real-app IRL carry-forward
  list** (in the findings Resolution section) tracks: 🔴-1 + 🔴-2
  real-app re-confirmation; the **NEW distinct finding `token_usage =
  0`** (no production `token_usage` writer; sole `INSERT` is
  `#[cfg(test)]` in `runtime-drone/vdr.rs`); and 🟡-1..4. Per CLAUDE.md
  §20 this fix cycle adds **no `docs/gap-analysis.md` entry**; the
  code-resolution flows into M07's gap-analysis Carry-forward.
- **Cycle-2 (M06.6) pattern — RECORDED, not acted on.** The
  assembled-app-regression mandate (each fix's regression test must
  exercise the assembled running-app path, not the isolated component
  that already passes Stage-V) is the empirical input for M06.6 to
  graduate into a permanent verification stage. This cycle changed
  **no** protocol artifact (`STAGE-PROMPT-PROTOCOL.md`, `CLAUDE.md`,
  validator) — scope lock honored. Recorded in `M06.5-summary.md` +
  the findings Resolution section.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §6 (canonical gate order + CI-parity), §8 (false-premise guard), §12 (escalate user-domain), §20 (no gap-analysis entry) all directly governed the session |
| 2 | "Read first" list oriented correctly? | 5 | Findings 🔴/🟡 + test-plan repros + A.fix/B.fix retro Decisions were exactly the inputs needed |
| 3 | Deliverable + test plan before work per §16? | 5 | Stated; tasks tracked |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | The reflexive `--features` divergence self-caught in 1 round against the CI workflow |
| 5 | Do-not-commit-until-approved held? | 5 | Nothing committed; refused to act on the stale "Resume B.fix" commit instruction; surfacing for approval |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the gotcha-#23 manual-repro decision (user-domain GUI) + the false-premise instruction; proceeded autonomously on gates + docs |
| 7 | Human-direction load reasonable? | 4 | Two user interactions on the manual-repro question (initial choice then a refinement to path-1) + the stale-instruction reconciliation; reasonable given the gotcha-#23 constraint |
| 8 | Surfaces actionable? | 5 | The contradiction table + cross-machine state made the stale instruction trivially resolvable; the AskUserQuestion gave a clear recommended fallback |
| **Process axis total** | | **39/40** | |

### Axis 2: Product (docs artifacts — no production code this stage)

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style? | 5 | Resolution/CHANGELOG/summary: why-not-what, file:line citations, no marketing |
| 2 | Verification not tautology (§5)? | 5 | Verification of record is the assembled-app regression tests (real drone subprocess / both-call-sites-resolve), explicitly NOT the isolated Stage-V components |
| 3 | Public surface documented? | 5 | n/a code; the Resolution section + ADR-0013 citation + summary fully document the cycle close |
| 4 | Exactly promised, no creep/cuts? | 5 | Only Resolution append + CHANGELOG finalize (incl. ADR-0013) + summary + retro; no 🟡 touched, no protocol artifact changed, no production code |
| 5 | Stranger-understandable? | 5 | Resolution cites SHAs, test names, the CI job, the before-verbatim, the token_usage carry rationale |
| 6 | §9 anti-patterns absent? | 5 | Append-only honored (153/0); no rewrite of prior disposition |
| 7 | Coverage measured? | 5 | No new code; full v1.6 suite green — workspace 91.57% ≥80, runtime-drone 95.86%, runtime-main 96.90% (agent_sdk.rs 95.38%), runtime-sandbox 96.11%, runtime-mcp 96.50%, all ≥95; unchanged from B.fix tip as expected |
| 8 | CI green across the matrix? | 4 | Predicted green (docs-only delta; markdown excluded from prettier; Rust/frontend/E2E re-run green this session); CI confirms on push |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | — |
| 2 | Sections missing? | 4 | A "manual repro is a user-machine GUI step the agent cannot drive (gotcha #23) — run automated A/B as verification of record + one optional user confirmation" note in the C.5 prompt would have pre-empted the surface |
| 3 | Gotchas generalize? | 5 | "inbound stale orchestration instruction → reconcile against authoritative build-machine git state, do not act/fabricate" generalizes; #23-manual-repro→path-1-fallback generalizes to every future IRL fix-cycle C stage |
| 4 | Time-box matched (within 2×)? | 5 | ~1h vs 1–2h |
| 5 | Implicit protocol moments? | 4 | The C.3 path-1 fallback being the *default* (not a fallback) for any Tauri-window manual repro was implicit; now explicit here + in the summary's M06.6 inputs |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the path-1 decision + the stale-instruction handling are documented |
| 7 | Anything to change before reuse? | 4 | Future IRL fix-cycle C-stage prompts should state the gotcha-#23 path-1-as-default explicitly |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — nothing committed; the stale "Resume B.fix" commit instruction was **refused** on the false premise; C.fix changes staged, surfacing for approval; **nothing pushed**
- [x] **G2: no Severity-5 friction** — max severity 3 (the stale instruction, surfaced + correctly not acted on)
- [x] **G3: no unaddressed protocol drift** — none; the potential drift inducement (commit on false premise) was surfaced and refused
- [x] **G4: stage completed** — full v1.6 canonical suite green incl. the A.fix + B.fix assembled-app regression tests; Resolution section appended (153/0, prior lines untouched); CHANGELOG finalized incl. ADR-0013; `M06.5-summary.md` + this retro written; M07.A carry-forward stated
- [x] **G5: scores ≥3 every row** — lowest row is 4 across all three axes
- [x] **G6: CI-parity** — gates run with the exact `.github/workflows/ci.yml` commands. `cargo fmt --all -- --check` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo test --workspace --features runtime-mcp/test-helpers` · `RUSTDOCFLAGS="-D missing_docs -D rustdoc::broken_intra_doc_links" cargo doc --workspace --no-deps` · `cargo audit --deny warnings --ignore <17 workspace-policy advisories>` · `cargo deny check` · the five `cargo llvm-cov` gates byte-identical to CI lines 363/371/390/422/448. **Cited divergence (per CLAUDE.md §6, identical to B.fix retro G4):** before each llvm-cov measurement, `cargo llvm-cov clean --workspace` (gotcha #81 — stale merged `.profraw`) + `CARGO_TARGET_DIR=target/llvm-cov-target cargo build --bin runtime-drone` (gotcha #56/#22 — the subprocess-harness tests `recovery_lifecycle`/`smoke_signal_persistence`/`drone_ipc_loopback`/drone `integration.rs` need the non-instrumented drone binary present in the llvm-cov target dir on Windows-local). These are **prerequisite setup steps, not divergent measurement flags** — the `cargo llvm-cov … --fail-under-lines` commands are byte-identical to CI's; CI Linux runs them unmodified and is the authoritative gate. Already documented in `docs/gotchas.md` #56/#81 and the B.fix retro; no new CI workflow change needed. One self-caught reflexive divergence (`--features` on the *workspace* gate) was corrected to the exact CI command before it reached any surface.

### Soft gates

- [x] **S1:** Process ≥30/40 — 39
- [x] **S2:** Product ≥32/40 — 39
- [x] **S3:** Pattern ≥25/35 — 32
- [x] **S4:** Time-box within 2× — ~1h vs 1–2h
- [x] **S5:** ≤3 Severity-3 friction — 1 (the stale instruction)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. C.fix established the durable automated assembled-composition regression tests as the **interim verification of record** for the code-resolved 🔴-1/🔴-2 (`7fc3277`/`9653718`; ADR-0012/0013), with the **real-app IRL re-confirmation deferred-and-tracked to the post-M07 IRL pass** per the between-milestone IRL model (maintainer-directed; gotcha #23 is why it cannot run in-stage — deferred, not closed), reconciled the findings doc (append-only), finalized the CHANGELOG (incl. the previously-uncited ADR-0013), and wrote the fix-cycle summary. **M07 Stage A is unblocked** on the code fix + interim automated verification.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.57% workspace line (gate exit 0; ≥80). No production code changed in C.fix — identical to the B.fix tip (B.fix retro recorded 91.71% region; line 91.57%).
- **Exclusions added this stage:** None (docs-only stage).
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged).

### Per-package gates (≥95% line on safety primitives)

- runtime-drone **95.86%** ≥95 (exit 0); runtime-main **96.90%** ≥95 (exit 0; `agent_sdk.rs` 95.38% — exact match to B.fix); runtime-sandbox **96.11%** ≥95 (exit 0); runtime-mcp **96.50%** ≥95 (exit 0, `--features test-helpers`). Exclusion lists byte-identical to CI (lines 371/390/422/448) — unchanged this stage.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| (all) | B.fix tip | unchanged | → | docs-only stage; no production code touched — every module identical to the B.fix-tip measurement |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns run this session match `.github/workflows/ci.yml` (gate steps lines 363/371/390/422/448) exactly. No workflow change needed; the gotcha #56/#81 prerequisite is a setup step, not a regex/flag change.

---

## [END] Decisions for the next stage (M07 Stage A — the cycle's consumer)

### `CLAUDE.md` updates

- [ ] None this stage (scope lock: no protocol edits in this fix cycle; recorded as M06.6 Cycle-2 input via `M06.5-summary.md`).

### Milestone document updates

- [ ] (M06.6 Cycle-2 input, recorded not acted on) Future IRL fix-cycle **C-stage** prompts should encode the **between-milestone IRL model** explicitly: the manual repro of a Tauri-window finding is a user-machine GUI step the agent cannot drive in-stage (gotcha #23); the in-cycle disposition is **code-resolved + automated assembled-composition tests = interim verification of record**, and the **real-app IRL re-confirmation is deferred-and-tracked to the next between-milestone IRL pass** (here: post-M07) — the 🔴 cards are *deferred, not closed*, and appear on the post-milestone IRL carry-forward list. This replaces the "C.3 path-1 fallback / optional user confirmation" framing with a structured deferral.

### Protocol additions

- [ ] (M06.6) Encode the inbound-stale-instruction guard explicitly: an instruction that contradicts the authoritative build-machine git state is reconciled and surfaced, never acted on or backfilled with a fabricated tool read (this stage's positive case; pairs with §8's outbound phase-doc-edit pre-flight and §19-rule-7).

### M07 Stage A — known carry-forward to encode

- [ ] 🟡-1..4 **+ the new `token_usage = 0`** distinct finding carry to M07.A `<read_prior_milestones>` (mirroring M06-IRL → M07.A). The `token_usage` finding's root: no production `token_usage` writer; sole `INSERT` is `#[cfg(test)]` in `runtime-drone/vdr.rs`; `handle_write_signal` runs only VDR + plan projectors.
- [ ] M07's gap-analysis **Carry-forward** records 🔴-1/🔴-2 resolution (this cycle adds no gap-analysis entry, §20).

### Open issues to file

- [ ] None (the `token_usage` finding is tracked in the findings Resolution section + summary, carrying to M07.A — not a separate issue).

---

## [END] User-review notes

> The user reviews this retrospective alongside the M06.5 PR (code/docs diff + the three stage retrospectives + `M06.5-summary.md`).

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M06.5 Stage `C.fix`, authored per `CLAUDE.md` §19. User review and approval pending. D.fix-class work stage — no closeout / no gap-analysis entry (ADR-0008 + §20); the M06.5 PR drafts now (C.fix is the final stage of the cycle).

**Claude:** I have completed M06.5 Stage `C.fix` per the stage prompt. The full v1.6 canonical gate suite is green including the A.fix + B.fix assembled-app regression tests; the `## Resolution (M06.5 fix cycle)` section is appended to `docs/M06-irl-findings.md` (153 insertions / 0 deletions — prior lines untouched); the CHANGELOG is finalized (incl. the previously-uncited ADR-0013); `M06.5-summary.md` is written. The stale "Resume M06.5.B.fix" instruction was surfaced as a false premise and **not acted on**. I have **NOT** committed or pushed. Surfacing the diff + gate results + this retrospective + the summary for final approval.

**Surfaced at:** 2026-05-18
