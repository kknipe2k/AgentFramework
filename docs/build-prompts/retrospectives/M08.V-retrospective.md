# M08.V — Verifier Retrospective

**Date:** 2026-05-21
**Verifier session ID:** session_01E2eRs1knKT1wePJmxFTKTM
**Milestone phase doc:** docs/build-prompts/M08-workbench.md
**Iteration:** 1
**Prior V iteration retros:** N/A
**Cross-machine state at start:**
  git log --oneline main..HEAD: 38 commits — Stage A (`8eff5cd`) … Stage G
  (`3260709`) + the nine stage retrospectives; HEAD `6bbab94`
  docs(M08.G).
  ls docs/build-prompts/retrospectives/M08.*-retrospective.md:
  M08.A, M08.B, M08.C, M08.D1, M08.D2, M08.E, M08.F1, M08.F2, M08.G
  (the per-stage retros — NOT read; bias guard).

## Per-pass summary

| Pass | Time | Findings | Notable |
|---|---|---|---|
| Inventory | ~25 min | 0🔴 / 0🟡 / 0🟢 | Every claimed file present + shape-matching; two intentional extra components (`TesterGraphPane.tsx`, `NodeValidationBadge.tsx`) are tested component-splits, not drift. |
| Wire | ~55 min | 0🔴 / 2🟡 / 1🟢 | All three M07.V Dec-6 wires have genuine production callers — discharge is real. `plan_loop` driver-absent; `TestOutcome.vdr` always Null; `replaceFramework` skips continuous validation. |
| Behavior | ~50 min | 0🔴 / 0🟡 / 0🟢 | Mandatory `--features integration` reference-MCP-server smoke ran 1/1 (not 0/0); all gates green; renderer 97.08% / runtime-main 96.57%. |
| Multi-call invariants | ~20 min | 0🔴 / 0🟡 / 1🟢 | Every M08 surface has a twice-in-sequence test; `removeNode` is a permanent no-op stub. |

## Findings

#### 🟡 #1 — Wire/Behavior: `TestOutcome.vdr` is structurally dead (always `Value::Null`)

**Spec claim:** MVP §M8 criterion 5 — "Click Test → … → graph + VDR + token spend + pass/fail surface". Phase doc F2.3.4 — "VDR + signals — `TestOutcome.vdr` … the Verification & Decision Record review". `TestOutcome.vdr` doc-comment (`tester.rs:61`) — "The VDR … the test session produced".
**Observed:** `fold_outcome` (`crates/runtime-main/src/builder/tester.rs:153`) hardcodes `vdr: serde_json::Value::Null` on every path. No code populates it — `fold_outcome` folds only `CapabilityViolation` / `TokenUsage` / `ArtifactHashMismatch`, never the `decision_record` `AgentEvent` variant the run emits (`event.rs:735`). `TesterModal` renders `<pre>{JSON.stringify(outcome.vdr)}</pre>` (`TesterModal.tsx:59`) — always `null`. The phase doc's own F1.3.1 pseudocode (line 4316) shipped `vdr: Value::Null`, so this is a phase-doc-level gap, not a build deviation.
**Expected (per spec):** the VDR the test session produced, surfaced for review.
**Trace / harness:** Wire 5-step trace — source event (`decision_record`) → projector (`fold_outcome`) → consumer (`TesterModal` VDR `<pre>`): the trace breaks at step 2 — `fold_outcome` never reads `decision_record`. The other four surfaces of criterion 5 (the scoped graph pane, `token_spend`, pass/fail, the full `trace`) all populate correctly; only the distinct VDR object is dead.
**Action:** Carry forward to M09 Stage A intake. Populate `TestOutcome.vdr` — either fold the trace's `decision_record` events in `fold_outcome`, or query the drone's VDR projection from the throwaway DB before teardown. Not 🔴: 4/5 of criterion 5 works, the field renders harmlessly, and the gap originates in the phase doc's own F1.3.1 pseudocode.

#### 🟡 #2 — Wire: `plan_loop` / `drive_plan` shipped but has no production caller

**Spec claim:** Phase doc V.2 "Scope to verify" — the M04 `plan_loop` row's "What V checks" column: "Wire pass: `plan_loop` now has a production caller from the session entrypoint".
**Observed:** `crates/runtime-main/src/plan/plan_loop.rs::drive_plan` shipped at M08.A — delivered, 7 tests in `crates/runtime-main/tests/plan_loop.rs`, 100% line coverage. But it has NO production caller: `src-tauri/src/commands.rs:433-434` and `:2484-2485` both explicitly document "the M04 `plan_loop` driver shell landed at M08.A … but has no production caller yet". The V.2 table's "production caller from the session entrypoint" claim is contradicted by the shipped code AND by the phase doc's own A.2 ("wire the `plan_loop` driver *reference* at the documented deferral site") + A.3.1 ("a `plan_loop` module exists and drives the FSM" — "driver shell only") + A.4.6 (no production-caller acceptance criterion). The V.2 table over-states what Stage A contracted.
**Expected (per spec):** the V.2 table led the verifier to expect a production caller; the actual Stage A contract was a tested driver shell + an updated deferral-site doc reference — which is what shipped.
**Trace / harness:** Wire trace — `drive_plan` callers: `plan/mod.rs:19` (re-export) + `tests/plan_loop.rs` (7 tests) only. No production call site. Driver-absence documented inline (not silent) → M06.V trace #11-class 🟡, not 🔴. v0.1 has no plan-orchestrated session entrypoint at all (smoke + Tester both call `run_agent` directly), so the wire-up has no obvious v0.1 home.
**Action:** Carry forward to M09 Stage A intake — decide whether `plan_loop` wiring has a v0.1 home or is a v1.0 item, and (Dec-6 standing rule) file the ADR-class carry-forward record the deferral currently lacks. Reconcile the V.2 table's over-claim at phase-doc-authoring time.

#### 🟢 #3 — Wire: a JSON-tab edit / framework load does not re-trigger continuous validation

**Spec claim:** Phase doc D2.3.4 — the debounced continuous-validation trigger fires on "every `framework` mutation".
**Observed:** `builderStore.replaceFramework` (`builderStore.ts:466`) — the action a valid `JsonView` edit and the Inspector's Load both call — is `(fw) => set({ framework: fw })`; it does NOT call `scheduleValidation()`, unlike `addNode` / `updateNode` / `connectEdge`. After a JSON-tab edit or a `load_framework`, the canvas red badges (`builderStore.validation`) reflect the pre-edit state until the user clicks the explicit Validate button or makes a canvas edit.
**Expected (per spec):** continuous validation on every framework mutation.
**Trace / harness:** code read — `replaceFramework` body vs the three canvas-mutation actions. MVP §M8 criterion 6 (the canvas re-derives on a JSON edit) still passes — the projection is unconditional; only the badge freshness lags. Explicit Validate works. Trivial fix (one `scheduleValidation()` call).
**Action:** Log to `docs/tech-debt.md` under "observability". Add `scheduleValidation()` to `replaceFramework` at any M09+ builder touch.

#### 🟢 #4 — Multi-call: `builderStore.removeNode` is a permanent no-op stub

**Spec claim:** ADR-0020 — Stage C ships the canvas-mutation actions "as typed no-op stubs D1/D2 fill".
**Observed:** `removeNode: () => set((s) => s)` (`builderStore.ts:498`) — never filled; documented "node deletion is not in D2's scope; a later stage fills it". Exposed in the `BuilderState` public interface. No component calls it (grep: zero non-store references), so it is harmless dead interface surface today; a future delete affordance wired to it before implementation would silently no-op.
**Expected (per spec):** node deletion is genuinely out of M08 scope — no MVP §M8 criterion needs it.
**Trace / harness:** grep — `removeNode` has no caller outside the store definition.
**Action:** Log to `docs/tech-debt.md` under "extensibility". Implement (or remove from the interface) when node deletion enters scope.

## Verification axes scoring

| Axis | Score (1–5) | Rationale |
|---|---|---|
| **Coverage adequacy** | 5 | All four passes exercised the full surface — 8 MVP §M8 criteria, ADR-0019 + ADR-0020, all eight carry-forward closures, the mandatory integration smoke, every multi-call surface. No primitive skipped. |
| **Finding signal-to-noise** | 5 | Every finding cites a concrete file:line + the spec/MVP/phase-doc line it contradicts; no false positives — the two 🟡 are real gaps, the two 🟢 are real (harmless) debt. |
| **Fresh-context discipline** | 5 | Clear-and-paste held; `<read_first>` honored; no `*-retrospective.md` / M08 summary / gap-analysis read. The Stage A + F1 `<construction_reachability_check>` blocks (on the read-list) were the only stage-internal artifacts consulted. |

Sum: 15 / 15 — no soft signal.

## Standing-rule compliance (v1.8 — M06.V Decisions 6 + 7)

- **Decision 6 — delivered+tested / driver-absent / root = accepted-ADR carry-forward → 🟡 + mandatory enumeration:** Applied to finding **#2** (`plan_loop`). The primitive is delivered + tested (7 tests, 100% line coverage) but production-driver-absent. Enumeration: the deferral is an M04 carry-forward absorbed at M08.A; it is NOT yet rooted in an accepted ADR (unlike the clean M06.V precedent) — so the finding additionally flags that the deferral needs an ADR-class record. Owning next stage: M09 Stage A intake. The M08 phase doc has no `<scope_change>` / `<wire_trace_vs_adr_reconcile>` blocks to cross-check. Finding #1 (`vdr`) is a phase-doc-pseudocode-level incompleteness, not an accepted-ADR carry-forward, and is classified 🟡 on its own merits (MVP §M8 criterion 5 partial).
- **Decision 7 — `--features integration` reference-MCP-server smoke (binding from M07.V):** integration smoke executed: **1/1, not 0/0**. `cargo test -p runtime-mcp --features integration --test integration` — `stdio_against_reference_server_everything` spawned `npx -y @modelcontextprotocol/server-everything`, connected over stdio, listed tools, found `echo`, pinged. The rmcp wire format is exercised against a real server.

## Outcome

**Sound** — all four passes produced clean signal; no 🔴. The discharge of the entire post-M07 carry-forward backlog is real (verified below). Findings are 2🟡 + 2🟢. Merge recommendation: **Proceed to closeout (Stage H)**.

## Special-log observations (V.5 `<special_log>`)

1. **Mandatory integration smoke:** ran **1/1**, not 0/0 (Decision-7 detail above).

2. **M07.V Dec-6 discharge — all three wires confirmed with genuine production callers (grep + file:line):**
   - **🟡 #2 `skills_lock::verify`** — `crates/runtime-main/src/builder/tester.rs:181` (`load_verified_artifact`). Production path: `test_framework` (Tauri command, `commands.rs:1630`) → `test_framework_with:1562` → `run_test_session_with` → `verify_framework_artifacts` (`tester.rs:310`) → `load_verified_artifact` (`tester.rs:247`) → `crate::skills_lock::verify`. Not a `#[cfg(test)]` block — `load_verified_artifact` and `verify_framework_artifacts` are `pub`/private production fns on the real seam.
   - **🟡 #3 `McpDispatcher::on_server_connected`** — `src-tauri/src/commands.rs:1525` (`connect_test_session_mcp`). Production path: `test_framework` (`commands.rs:1653`) → `connect_test_session_mcp` → `dispatcher.on_server_connected(server)`. The file diverges from the F1 `<construction_reachability_check>` block's `constructor=` claim of `crates/runtime-main/src/builder/tester.rs` — ADR-0019 explicitly documents and reconciles this ("forced by the crate dependency graph; runtime-main cannot reference `runtime_mcp::McpDispatcher`"). Discharge is real; only the phase-doc block's path string is stale — reconciled by an accepted ADR, so not a finding.
   - **🟡 #5 agent-with-tools production driver** — `crates/runtime-main/src/builder/tester.rs:333` (`run_test_session_with`: `sdk.run_agent(config)`). Production path: `test_framework` → `test_framework_with` → `run_test_session_with` → `AgentSdk::run_agent`. `run_agent`'s only prior production caller was the no-tools `run_smoke_session`; the Tester is the second, and it runs a candidate framework that can declare tools. The assembled regression `tester_runs_a_tool_bearing_framework_through_the_real_loop_and_persists_signals` (4/4 in `tester_isolated_session.rs`) drives a real `ProviderEvent::ToolUse` through the loop with `token_usage > 0`.
   Stage F1's `<construction_reachability_check>` closure (all three wires inverted to `inputs_reachable="true"`) **holds** — borne out by the shipped code.

3. **ADR-0020 canvas↔JSON two-way binding — verified as a real bidirectional contract, not a one-way preview.** `builderStore.framework` is the single source of truth. Canvas→JSON: `addNode`/`updateNode`/`connectEdge` mutate `framework`; `JsonView`'s `useEffect` re-seeds the textarea draft from it. JSON→Canvas: a valid `JsonView` edit calls `replaceFramework(parsed)` and the canvas projection (`projectCanvasNodes`/`projectCanvasEdges`) re-derives. Both directions reduce to the same one-way flow `framework → view`. The invalid-JSON no-desync guard (`JsonView.tsx:43-50` — garbage never reaches `replaceFramework`) is tested. The Inspector's `<pre>` is a separate read-only preview; the editable two-way surface is the `JsonView` JSON tab.

4. **ADR-0019 Tester isolation — verified.** Throwaway DB: `throwaway_test_db_path()` (`commands.rs:1505`) resolves `std::env::temp_dir().join("runtime-tester-<uuid>.sqlite")` — never `app_local_data_dir`. Test-defaults-not-HITL: `HitlSeam::test_defaults()` woven at `tester.rs:325`; capability violations fold onto `TestOutcome.capability_failures` (`run_test_session_with_blocked_mcp_dispatch_is_a_failed_test_not_an_error` test). Teardown: `lifecycle.shutdown()` + `std::fs::remove_file(&db_path)` (`commands.rs:1677-1678`); `tester_session_teardown_removes_the_throwaway_db` + `tester_throwaway_db_is_isolated_from_a_user_session_db` (assembled regression) pass. The Tester is a sequential build-time session — not §1c multi-session.

5. **Dec-6 standing rule for a NEW delivered/driver-absent primitive:** the only delivered/driver-absent primitive is `plan_loop` — finding #2. It is an M04 carry-forward absorbed at M08.A rather than a primitive M08 itself originated; classified 🟡 with the mandatory enumeration (carry-forward target M09.A; ADR-class record still owed). No `<scope_change>` block exists in the M08 phase doc to cross-check against.

6. **Carry-forward convergence for M09 (Generators → Workbench shell):** M09 wires "Generate Tool/Skill/Agent" into the Builder. ADR-0020 already designates `builderStore.framework` as the Generators' single write target — the convergence point is in place. The two 🟡 (VDR population, `plan_loop` disposition) + the two 🟢 are routed to M09 Stage A intake. The post-M08 IRL pass can re-confirm M06.5 IRL 🔴-1 (MCP-registry) now that Stage G makes the Promoted tier — and therefore MCP-add — reachable.

7. **Protocol-calibration observations for v1.9:** (a) The V.2 "Scope to verify" table's "What V checks" column can over-claim relative to the stage's actual X.2/X.3/X.4 contract — the `plan_loop` row claimed a "production caller" that A.2/A.3.1/A.4.6 never contracted. A v1.9 authoring check could require the V.2 "What V checks" column to be derived from the owning stage's acceptance criteria, not aspirationally. (b) A phase-doc pseudocode block that ships a placeholder value (`vdr: Value::Null`) with an illustrative `/* folded from the trace */` comment can become the shipped behavior verbatim — the v1.9 `<data_flow_audit>` candidate would catch a contracted output field that no code path populates.

## `[END] Decisions for D.fix or next milestone`

- Decision 1: 🟡 finding #1 (`TestOutcome.vdr` always Null) → M09 Stage A `<read_prior_milestones>` references this entry; absorb into M09.A intake — populate `vdr` from the trace's `decision_record` events in `fold_outcome` (`tester.rs:121`) or query the drone VDR projection before teardown. M08 closeout records it in the gap-analysis Carry-forward section.
- Decision 2: 🟡 finding #2 (`plan_loop` driver-absent) → M09 Stage A intake decides whether `plan_loop` wiring has a v0.1 home or is a v1.0 item, and files the ADR-class carry-forward record the deferral lacks. The M08 closeout reconciles the V.2 table's over-claim.
- Decision 3: 🟢 finding #3 (`replaceFramework` skips continuous validation) → log to `docs/tech-debt.md` under "observability" with the one-line fix.
- Decision 4: 🟢 finding #4 (`removeNode` permanent no-op stub) → log to `docs/tech-debt.md` under "extensibility".
- Decision 5: refine V protocol — see special-log item 7 (a) + (b); route to the v1.9 protocol-iteration candidate set (the V.2-column-derivation check + the `<data_flow_audit>` contracted-output check).

## Cross-machine state at end

**Cross-machine state at end:**
  git log --oneline main..HEAD: 38 commits + this V findings commit pending
  (`verify(M08): in-band V — four passes + mandatory --features integration smoke`).
  ls docs/build-prompts/retrospectives/M08.*-retrospective.md:
  M08.A, M08.B, M08.C, M08.D1, M08.D2, M08.E, M08.F1, M08.F2, M08.G,
  M08.V (this file).
