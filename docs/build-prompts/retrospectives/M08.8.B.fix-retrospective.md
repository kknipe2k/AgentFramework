# M08.8.B.fix — Stage Retrospective

> **Stage:** M08.8 Stage `B.fix` — Complete the Light Instrument restyle (the entire design) — see `docs/build-prompts/M08.8-design-workbench.md` (Stage B.fix B.fix.1–B.fix.7)
> **Parent milestone:** M08.8 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m08.8-design-workbench`
> **Prior stage:** M08.8.B (Light Instrument visual foundation — tokens + shell + Toast + Modal; Sound, IRL "works but doesn't look like the mockup")
> **Type:** renderer/CSS + 2 new surfaces' markup — no feature logic, no run-loop/enforcer/persistence/schema change

---

## Pre-flight (claimed before session start)

- [x] `CLAUDE.md` loaded; §4 Hard Rules held throughout (no commit/push; nothing bypassed).
- [x] Read-first read in order: the phase doc Stage B.fix (B.fix.1–B.fix.6, the per-surface map + global discipline), `docs/design/workbench-mockup/workbench.css` (the 368-line target) + `tokens.css`, `DESIGN.md`, the recolored-not-restyled `src/styles.css` (2786 lines) + the touched components, the B retrospective `[END] Decisions for Stage C`, ADR-0021, `cluster-pattern.md`, `CLAUDE.md` §5/§6.
- [x] **Prior-stage retrospective applied (the §19 non-negotiable).** B's decisions were load-bearing here: (1) `Modal` is the migration target and the migration is **test-bearing** (B deferred it because its tests pinned `tester-close`/`tester-modal__*`); B.fix moves those test updates into the RED commit (TD-043 closed). (2) Token names are DESIGN.md-identical — restyle keeps the app class names rename-free, exactly as B kept the tokens. (3) `window.__toastStore` exposure pattern reused for the e2e.
- [x] `<wire_signature_audit>` pinned: TARGET = `workbench.css` component specs; CURRENT = `src/styles.css` @ the B.fix.2 anchors; keep app class names, adopt the SPEC.
- [x] Baseline green before starting (vitest 618 passed before the RED additions).

---

## [LIVE] Friction events

| When (step) | What happened | Severity (1–5) |
|---|---|---|
| green-phase vitest (1 red-phase test path correction) | `styles.restyle.test.ts` read `../src/styles.css` (resolves to `tests/src/...`) instead of `../../src/styles.css` — an `ENOENT` failed-suite, not an assertion failure. The sibling `styles.tokens.test.ts` uses `../../`. Corrected the relative path in the RED test file (a red-phase authoring fix, like B's three — it rides the RED commit, so the red→impl diff over test paths stays EMPTY). | 1 |

No Severity-≥2 friction. The single largest risk — a ~1500-line per-surface CSS rewrite breaking the ~620 existing class-existence (gotcha #67) tests — was avoided **by the rename-free discipline**: every edit changed property VALUES and added new rules; not one app class name changed (only the dead `.tester-modal` shell rules were removed, and their tests were rewritten in RED). Full vitest stayed green (624 passed).

---

## [LIVE] Ambiguity events

| Source in conflict | What was unclear | How resolved |
|---|---|---|
| "restyle EVERY surface" vs strict-TDD-no-test-edits | The Tester migration (`.tester-modal` shell → `Modal`) deletes classes the unit + Playwright tests pinned (`tester-close`, the gotcha-#67 `.tester-modal__*` CSS check). | Rewrote `TesterModal.test.tsx` (close via Modal's `aria-label="Close"`; new Modal-as-dialog/Esc/scrim block; dropped the dead-class CSS check) **and** the Playwright `tester_modal.spec.ts` close selector in the **RED** commit (the phase doc's explicit "the `tester-modal__*` test rewrite rides the RED commit"). Impl touched no test file thereafter. |
| err-card on-screen home (styled-with-stub here; F wires real data) | Where to mount the NEW validation card so the maintainer can SEE it, without a test-bearing change to a non-RED test. | Wired `ValidationCard` into `JsonView`'s parse-error slot — kept the `builder-json-error` testid on a wrapper (the JsonView test only checks that testid's presence/absence), so the err-card is real + visible (type invalid JSON in the Builder JSON tab) with zero test churn. |
| Tester `.metric` cards data (stub vs real) | Spend ($) has no wire field on `TestOutcome` yet (C/F wire cost). | Result/Verify/Tokens render from the real `TestOutcome`; Spend is a stub `—` (logged; F wires it). The metric grid is additive — the existing `tester-result-*` testids the behavior tests pin are untouched. |

---

## [LIVE] Surface / protocol-drift / surprise events

- **Surprise (good):** `vite build` fingerprinted all 7 IBM Plex `woff2` into `dist/assets/` and the `@font-face` `url('./assets/fonts/…')` resolved — confirming the bundled font is wired correctly and the real Tauri app no longer depends on the WebView2-blockable Google `@import` (the B-IRL font question). The `@import` is kept as an online enhancement, declared BEFORE the `@font-face` so the local files win in source order.
- **Protocol drift:** none. Hard Rule 1 held (nothing committed/pushed). Strict-TDD two-commit invariant holds by construction (impl edits zero test files post-RED).

---

## [END] Decisions for the next stage (Stage C)

> **Re-letter note (2026-06-04):** the decisions below are **budget** decisions — budget was re-lettered **Stage C → Stage D** (execution order is now C-tier → D-budget → E-gap → F). They apply at **Stage D (budget)**. The stage that *executes next* is **Stage C (tier)**, which has its own scope. (Every "C" below = the budget stage as labelled when written.)

- **C builds into the fully-restyled surfaces.** `BudgetHeaderBar` is now token-banded (ok/warn/error families, mono-tabular nums) — C parameterizes its store + paints `budget_*` into the already-styled bar; no restyle needed. The cap `Toast` lands on B's primitive.
- **The Tester is a `Modal` now (TD-043 closed).** C's Tester full-screen needs no special handling — it's `<Modal size="full">`. The `.tester-modal__*` selectors that remain style the Modal BODY content only.
- **`MetricCard` + `ValidationCard` are reusable.** C/F wire real data: F replaces the Tester Spend stub `—` and feeds the real validator output into `ValidationCard` (it already takes `plain`/`fix`/`raw`).
- **The `@import` is still present** as an online fallback; if a future IRL wants it gone, deleting line `@import …` is safe (the bundled `@font-face` covers 400–700 sans + 400–600 mono).

---

## [END] Stage B.fix special log (per `<retrospective_requirements>`)

**Which surfaces needed the deepest restyle vs were close.**
- *Deepest (M03 spec → mockup spec):* the **nodes** (2px→1px hairline border, `system-ui`→`--font-sans`, 16px→22px glyph chip, 8px→9px status dot, mono sub-lines); the **view-switch** (filled-active button → segmented grey well with a raised white selected tab); the **Builder palette** (item → hairline card + kind/source accent bar + mono ref label; tabs → accent-underline); the **BudgetHeaderBar** (saturated `#2e7d32`/`#f59e0b`/… fills → restrained ok/warn/error tints + mono-tabular nums); the **Tester** (hand-rolled full-screen shell → `Modal size="full"` + the new metric grid).
- *Close (token swap + radii):* `ApprovalPanel`/`GapPanel` → `.panel` (hairline, `--r-md`, `e1`); MCP + import modals (6px→`--r-md`, literal greys → tokens); Settings controls (→ `.btn`/`.input` spec, mono-tabular budget input).
- *New surfaces built:* the Tester **`.metric` grid** (Result/Verify/Tokens/Spend, 22px mono-tabular) and the **`.err-card`** validation surface (plain cause + `→ fix` + a "Show raw error" disclosure, collapsed by default).
- *Global discipline applied:* the mono register went from 8 → **30** `var(--font-mono)` selectors; segmented controls now on view-switch + transport speed; the `color: var(--node-hitl)` amber-text-on-accent recolor artifacts (view-switch, Tester run/promote, inspector actions) fixed to `--text-on-accent`.

**The Plex-woff2 outcome.** Bundled 7 weights (Sans 400/500/600/700 + Mono 400/500/600) from the @fontsource CDN into `src/assets/fonts/`; added a local `@font-face` block; verified valid `wOF2` magic + that `vite build` emits all 7 into `dist/assets`. The blockable Google `@import` is retained only as an online enhancement.

**TD-043 closed.** `TesterModal` is the `Modal` primitive at `size="full"`; the hand-rolled `.tester-modal`/`__header`/`__close` shell + its test/Playwright pins are gone (rewritten in RED).

**The close was design-conformance, not an execution flip.** B.fix ships **no execution behavior** → flips **no** `docs/execution-status.md` row. Its authoritative close is the **real Tauri app via `tauri-driver` (`design_conformance.e2e.ts`) + the maintainer "it looks like the mockup, fully" watch** (rule 11 / ADR-0021) — which run on the build machine + the maintainer, NOT in this session. The agent-side gates below are necessary, not sufficient.

---

## [END] Gate evidence (agent-side; CI-parity §6 frontend ordering)

- `npx prettier --check` — clean (CSS excluded from the glob by repo policy; CSS hand-formatted).
- `npx eslint .` — exit 0.
- `npx tsc --noEmit` — exit 0.
- `npm run test` (vitest `--coverage`) — **624 passed** (618 prior + 6 net; the RED rewrite of TesterModal added the TD-043 block); lines **93.6%** ≥ 80%.
- `npm run test:e2e` (Playwright renderer) — **59 passed**, no regression (incl. the migrated `tester_modal.spec.ts` closing via Modal).
- `vite build` — **green**; all 7 `woff2` bundled into `dist/assets`.
- `npm run test:e2e:tauri` (`design_conformance.e2e.ts` + `visual_foundation.e2e.ts`) — **PENDING the build machine** (needs the built binary + `tauri-driver` + `msedgedriver`) + the maintainer real-app IRL. **This is the authoritative close** (rule 11 / ADR-0021).
- Mutation gate — **advisory** (renderer/CSS; not execution-wiring / a §6 critical module).

**Strict-TDD invariant:** RED commit = the 6 test files (MetricCard/ValidationCard/styles.restyle new + TesterModal/tester_modal/design_conformance rewritten, path-correction included); impl commit = `src/**` + `src/assets/fonts/**` only. `git diff <red>..<impl> -- '**/tests/**'` EMPTY by construction.

---

## [END] Outcome (self-assessment, pending the real-app close)

**Sound (agent-side) — design-conformance close pending.** All runnable gates green; rename-free discipline kept the existing suite green; TD-043 closed; the 2 missing mockup surfaces built; Plex bundled + build-verified. The "it looks like the mockup, **fully**" verdict is the maintainer's to give on the real Tauri app — not claimed here. Nothing committed or pushed.

---

## [END] User-review notes

- [Empty until user reviews.]
