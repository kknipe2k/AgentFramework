# M08.7.A — Stage Retrospective

> **Stage:** M08.7 Stage `A` (rung 1 — built-in tool execution) — see `docs/build-prompts/M08.7-execution-engine.md`
> **Parent milestone:** M08.7 (Execution Engine) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-29
> **Session end:** 2026-05-29
> **Branch:** `claude/m08.7-execution-engine`
> **Starting commit:** `68dffd8`
> **Ending commits:** `f7f9b7e` (red) → `a1eac90` (impl) → `6f9cbbc` (follow-up additive) — pushed only on approval
> **Estimated effort (per prompt):** 4–6 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in scope
- [x] All "Read first" files read in order (CLAUDE.md, cluster-pattern.md, M08.7 phase doc, gotchas, retro template) + the read_reference source files
- [x] Deliverable + test plan stated before code; ground-at-red findings surfaced before tests
- [x] User confirmed (ground-at-red checkpoint approved; red approved; green approved)
- [x] Branch correct (`claude/m08.7-execution-engine`, cut from main; `main..HEAD` empty at start)
- [x] `git status` clean except pre-existing untracked `.claude/*.lock` files

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| green / first `cargo test --workspace` | Built-in `Read`/`Write` now executing broke **4 pre-existing M06 tests** (2 files) that pinned the *painted* model (gate on `Exec/<toolname>`, no execution) using `"Read"` as the example tool. Two hung 3600 s on the HITL await. The phase doc's ground-at-red did not anticipate that existing contract tests used the now-reserved built-in names. | The §1.3 ground-at-red sub-steps could add: "grep existing tests for wired `Read`/`Write` `ToolUse` injections — rung 1's execution change will collide with any that pin the painted Exec-grant behavior." | 3 |
| mutation_gate step | `cargo-mutants` not installed; `cargo install cargo-mutants` denied by the sandbox. Automated mutation gate not runnable locally. | The cluster-pattern §5 / stage prompt should note cargo-mutants may be unavailable locally (it is nightly-on-main per §5) and prescribe the manual-spot-check + `!`-command fallback. | 2 |
| green / first workspace run | `cargo test` stops at the first failing binary (alphabetical), masking the full blast radius (it died at `mcp_dispatch_runloop`, never reached `s*`/`t*`). Required a `--no-fail-fast` re-run to see all affected tests. | A note that scoping/blast-radius assessment needs `--no-fail-fast`. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M06 contract tests (`Exec/<toolname>` grant) vs M08.7 §1.4 BDD (`file_access` gate) | Which capability dimension gates a built-in `Read`/`Write` — the M06 tool-invocation `Exec` grant, or `file_access` scope? | Maintainer confirmed `file_access` (per the §1.4 BDD); the `Exec/<toolname>` pipeline path stays for non-built-in tools. Defense-in-depth (check both) logged as TD-033 for v1.0. | Resolved in-cluster; the model is now explicit in `builtin_tools.rs` docs + TD-033. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After ground-at-red | Findings A (`config.tools` empty) + B (no production grant-loading path) + the build plan + Finding B disposition triage | Yes — maintainer approved fix-in-cluster | Yes — before any test written, per the stage's red_phase instruction |
| After red commit | Red-phase surface (right-reason red, 12/12 fail, strict-TDD note) | Yes — maintainer approved red | Yes |
| Mid-impl (green) | The 4 pre-existing painted-`Read` tests broke; design confirmation + `Read`→`Echo` rename recommendation + strict-TDD disclosure | Yes — maintainer approved the rename + directed the TD-033 log | Yes — surfaced as soon as the workspace gate revealed it, before rewriting contract tests |

---

## [LIVE] Protocol drift events

None observed. The do-not-commit-without-approval rule held: red committed only after "proceed into the red phase"; impl + follow-up committed only after "proceed to green". No push performed (awaiting final approval). The sandbox protocol was not touched (Hard Rule 8 / scope lock).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The grant-loading gap (Finding B) — *no* production path ever loaded a framework's `file_access` into the L1 enforcer | The enforcer + `capabilities_to_declarations` existed since M05/M06; the wire between them was simply never built — the painted-execution era never needed it | Confirms the M08.7 thesis: scaffolding was built *around* an execution core that never ran. Each rung will likely surface a similar "never-wired" gap. |
| `cargo test`'s stop-at-first-binary masked 3 of 4 broken tests | The first run looked like "1 test broke"; it was 4 | Blast-radius assessment must use `--no-fail-fast`. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test (the mechanical regression IS the discovery) + IRL gate pending | `crates/runtime-main/tests/builtin_tool_execution.rs` — drives the REAL `run_test_session_with` → `run_agent` loop against a tempfile fixture; asserts the agent's next turn receives the file contents as a `tool_result` (observed on the real config) + final text quotes `ladder-rung-1` | Finding A (config.tools advertisement — IRL-gated, CI stub doesn't cover) → implemented + unit-tested; Finding B (grant-loading gap) → fixed in-cluster; painted-`Read` test collision → `Read`→`Echo` rename (4 tests); defense-in-depth gap → TD-033. **Zero propagation.** |

This is a backend stage with an **assembled Rust integration** surface (not `tests/e2e-tauri/`). The cluster does **not** close on tests-green — the maintainer IRL watch (run a one-agent framework through the Tester, watch the agent quote a file) is the remaining close gate.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | Rules 11 + §5 + §6 + §8 directly drove the work |
| 2 | "Read first" list oriented correctly? | 5 | Phase doc + cluster-pattern + source refs were exactly what was needed |
| 3 | Deliverable + test plan before code? | 5 | Ground-at-red findings + plan surfaced before any test |
| 4 | Self-correct in ≤3 rounds? | 4 | The painted-test breakage needed a maintainer decision (1 surface), not blind self-correction; resolved cleanly |
| 5 | Do-not-commit-until-approved held? | 5 | Commits only after explicit phase approvals; no push |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the capability-model fork + contract-test rewrite; proceeded on mechanical wiring |
| 7 | Human-direction load reasonable? | 4 | 3 approval checkpoints (ground-at-red, red, mid-impl decision) — appropriate for a foundational rung touching a verified contract surface |
| 8 | Surfaces actionable? | 5 | Each surface had a clear recommendation + decision request |
| **Process axis total** | | **38/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case, small fns, `&Path`-style, no magic, doc-commented |
| 2 | Behavior tests not tautology (§5)? | 5 | Assembled test asserts observable side-effects (file contents fed back + quoted), not events; mutation spot-check confirms tests bite |
| 3 | Public APIs documented + compile-checked? | 5 | All pub items doc'd; `cargo doc -D missing_docs` green |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | Read/Write executor + feedback + grant-loading (Finding B) + advertisement (Finding A); sandbox untouched |
| 5 | Stranger-understandable? | 5 | Module doc explains the model + scope lock + the §1.3-B closure |
| 6 | §9 anti-patterns absent? | 5 | No `unsafe`, no stringly-typed, no silent drops, no ad-hoc allows |
| 7 | Coverage measured? | 5 | runtime-main 95.30% ≥95 (measured); builtin_tools.rs 98.04% |
| 8 | CI green across the matrix? | 3 | Predicted green; only Windows-local run performed (CI Linux/macOS pending the PR run) — stated, not assumed |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | The cluster-gate + ground-at-red + strict-TDD slots all earned their keep |
| 2 | Sections missing? | 4 | Ground-at-red could prescribe a grep for existing tests that pin behavior the rung changes (see Decisions) |
| 3 | Gotchas local or generalize? | 4 | "cargo test stops at first binary; use --no-fail-fast for blast radius" generalizes (gotcha candidate) |
| 4 | Time-box matched? | 4 | Within range; the painted-test reconciliation added unplanned scope |
| 5 | Implicit protocol moments? | 4 | The "pre-existing test that pins the painted behavior the rung removes" pattern will recur every rung; worth codifying |
| 6 | Re-runnable by a fresh session? | 5 | The ground-at-red findings + cited surfaces make it reproducible |
| 7 | Anything to change before copying format? | 4 | Add the existing-test-collision grep to the ground-at-red step for rungs 2–9 |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red committed after "proceed into the red phase"; impl/follow-up after "proceed to green"; **no push** (awaiting final approval). `git log` shows the three commits ahead of main.
- [x] **G2: no Severity-5 friction** — max severity 3.
- [x] **G3: no unaddressed protocol drift** — none observed.
- [x] **G4: stage completed** — `a1eac90` + `6f9cbbc`; §1.4 BDD assembled test green; all gates pass.
- [x] **G5: every row ≥3** — lowest is 3 (CI-matrix row: only Windows-local run done, CI pending; honest per rule 11).
- [x] **G6: CI-parity** — gate commands run verbatim per `CLAUDE.md` §6 (see Coverage holdouts + the green surface gate table). No CI-divergent flags. Mutation gate: cargo-mutants not locally installable (install denied) → manual spot-check substitute, disclosed.

### Soft gates

- [x] **S1:** Process 38/40 ≥30
- [x] **S2:** Product 38/40 ≥32
- [x] **S3:** Pattern 30/35 ≥25
- [x] **S4:** Time-box within 2×
- [x] **S5:** ≤3 Severity-3 — one (the painted-test reconciliation)

### Outcome

- [x] **Pattern is sound.** All hard + soft gates pass. **BUT** per the cluster-gate (`docs/cluster-pattern.md` §1 step 4), the cluster is **not closed on tests-green** — it closes only on the maintainer's IRL watch of the assembled run. Until then the close is a **hypothesis backed by the assembled CI test**, labeled as such.

---

## [END] Coverage holdouts

### Per-package gate (runtime-main ≥95%)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 95.30% line (TOTAL), exit 0
- **Exclusions added this stage:** None — `sdk/builtin_tools.rs` is fully testable in-process (no OS-signal / seam-wrapper holdout); measured at **98.04%** line.
- **Exclusion list as of stage end:** unchanged — `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs`

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `sdk/builtin_tools.rs` | n/a (new) | 98.04% | new | — |
| `sdk/agent_sdk.rs` | ~93.1% (M07/M08) | 94.48% | ↑ | additive Op-path test covered dispatch_builtin's Op arm |

### CI workflow drift check

- [x] No coverage-config change this stage (no exclusion/threshold change) → the four mirrors (`codecov.yml`, CLAUDE.md §5/§6, `docs/coverage-policy.md`) are untouched and remain consistent. No `docs/coverage-policy.md` §C entry required.

---

## [END] Decisions for the next stage

### `CLAUDE.md` updates
- [ ] Consider a gotcha: "`cargo test` stops at the first failing test binary (alphabetical); use `--no-fail-fast` when assessing the blast radius of a behavior change."

### Milestone document updates (M08.7 phase doc — rungs 2–9 authoring)
- [ ] Add to each rung's ground-at-red: "grep existing tests for assertions that pin the *painted* behavior this rung replaces (wired `ToolUse` injections for the rung's tool/primitive); migrating them is in-scope and a strict-TDD disclosure."
- [ ] Rung 2 carries the denied-execution BDD; note that M08.7.A already implements + tests the denied path (CapabilityViolation/TierViolation + no side effect), so rung 2 is largely verification.

### Protocol additions
- [ ] cluster-pattern §5: note cargo-mutants may be unavailable locally; prescribe the manual mutation spot-check (+ `!`-command for the user) as the disclosed fallback.

### Open issues to file
- [ ] TD-033 (defense-in-depth: execution-time tool-authorization) — logged.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M08.7.A (rung 1 — built-in tool execution) per the stage prompt. The executor runs `Read`/`Write` in-process under `file_access` capability scope and feeds results back through the MCP-shared multi-turn contract; Findings A + B are closed in-cluster; the sandbox protocol is untouched. All machine gates pass (Windows-local, CI-parity commands). The mutation gate's automated tool was not locally installable; a manual spot-check on the two core-logic mutations (the `is_builtin_tool` gate and the capability-check security boundary) confirmed the tests bite.

**Per rule 11 / the cluster-gate: the cluster is NOT closed on tests-green.** The assembled CI test ran and the agent quoted the file; the remaining close gate is the maintainer's IRL watch. I have committed (red → impl → follow-up) but **will not push until you approve**.

**Surfaced at:** 2026-05-29
