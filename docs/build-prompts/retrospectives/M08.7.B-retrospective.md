# M08.7.B — Stage Retrospective

> **Stage:** M08.7 Stage `B` (rung 2 — capability enforcement on a live tool) — see `docs/build-prompts/M08.7-execution-engine.md`
> **Parent milestone:** M08.7 (Execution Engine) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-31
> **Session end:** 2026-05-31
> **Branch:** `claude/m08.7-execution-engine`
> **Starting commit:** `c980671`
> **Ending commits:** `5480c46` (B.3.0 docs) → `c769b43` (red) → `ae62b17` (impl) → (retro) — pushed only on approval
> **Estimated effort (per prompt):** 2–4 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in scope (stated §4 rule 11 + the rest at session start)
- [x] All "Read first" files read in order (CLAUDE.md, cluster-pattern.md, M08.7 phase doc, M08.7.A retro + IRL addendum, gotchas, retro template) + the 5 read_reference source files
- [x] Deliverable + test plan stated before code; ground-at-red findings surfaced before tests (the first checkpoint)
- [x] User confirmed (ground-at-red decision approved — Option 1; red approved; green approved)
- [x] Branch correct (`claude/m08.7-execution-engine`, rung-1 present `f7f9b7e..f9b02bf`)
- [x] `git status` clean except pre-existing untracked `.claude/*` files

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| ground_at_red | The stage prompt's central premise — "`run_test_session_with` is called with `hitl_seam=None`" / "the `hitl_seam` None arg" — is **factually inaccurate**: `run_test_session_with` has **no `hitl_seam` parameter at all**; the `HitlSeam` is hardcoded to `test_defaults()` (`tester.rs:345`). The ground-at-red step caught it before writing tests (that is its purpose), and it cleanly resolved the B.3.3 HITL triage to outcome (b). | The phase doc B.1/B.3.3 + the §X.5 context could say "the Tester weaves a hardcoded `test_defaults` seam (no `hitl_seam` param)" rather than implying a None arg exists. The ground-at-red note ("do not assume HITL surfaces on the Tester path") was the right backstop. | 2 |
| green / clippy -D warnings | The 8-arg `run_test_session_with_tier` tripped `clippy::too_many_arguments` (the existing `run_test_session_with` sits at the 7-arg threshold; +tier = 8). | A note that a tier param pushes the already-wide `*_with` seam past the lint; the codebase's established `#[allow(clippy::too_many_arguments)]` precedent (6 existing uses) is the proportionate fix. | 1 |
| mutation_gate | `cargo mutants --in-diff` produced **2 mutants, both unviable, 0 viable** — the new logic (`enforcer.set_tier(tier)`, a unit-returning statement) is below cargo-mutants' default mutation granularity, and the only candidates (function-body replacement with `Ok(Default::default())`) are unviable because `TestOutcome` is not `Default`. The automated gate did not meaningfully exercise the wire; the manual spot-check substitutes. | cluster-pattern §5 already allows the manual spot-check as the disclosed fallback; worth noting that a one-line `()`-returning wire often yields no viable mutant, so the manual analysis is primary for such changes. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| The stage prompt's `hitl_seam=None` framing vs `tester.rs` (no such param) vs `seam.rs` (test_defaults auto-resolves) vs the module doc (violations → test failures, not live HITL) | Whether "a HITL prompt surfaces" (the BDD line) is a Tester-path concern at all | Ground-at-red resolved to triage outcome **(b)**: the Tester is HITL-less by design (ADR-0019); the violation surfaces as a `TestOutcome.capability_failures` fold + the run stays unattended. Live HITL is a live-session concern, tested where a live `HitlSeam` is used. | Resolved in-cluster; recorded in the test file's B.4.3 doc-comment + this retro. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After ground_at_red | The 4 findings (hitl_seam premise inaccurate; no production `set_tier`/Tester-runs-Novice; tier-precedes-scope; enforcement already correct) + the BDD-already-green triage + the minimal-seam plan + **one maintainer-domain decision** (test-seam-only vs wire production tier vs ADR scope-out) | Yes — maintainer chose Option 1 (test-seam + log TD) with explicit honesty directives | Yes — before any test written, per ground_at_red's "surface before writing tests" |
| After red commit | Right-reason red (E0432 unresolved import — the missing seam), the 3 pinned scenarios, the green plan, the strict-TDD commitment | Yes — maintainer approved red | Yes |

---

## [LIVE] Protocol drift events

None observed. Do-not-commit-without-approval held: B.3.0 committed only after the ground-at-red decision approved it; red committed only after "proceed to red"; impl committed only after "approved" (proceed to green). No push (awaiting final approval). Sandbox protocol untouched (Hard Rule 8 / scope lock). Strict-TDD two-commit held — `git diff c769b43..ae62b17 -- 'crates/runtime-main/tests/'` is EMPTY.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| **No production code anywhere calls `set_tier`** — both the Tester and the smoke session always run at the default Novice; the user's tracked tier (`CurrentTierState`) is never pushed into the run-loop enforcer. | The tier primitive (`Tier`, `TierEvaluator`, L4 gate, `set_tier`) is fully built + unit-tested since M05; the enforcer's own doc describes the intended Tauri-shell wire — which was never built. | Confirms the M08.7 thesis a third time (after rung-1's grant-loading gap): scaffolding built around an execution core that never ran. Each rung surfaces a "never-wired" gap. Logged as TD-036. |
| Rung 2 needed **zero** enforcement/executor/event code — the entire denial+allow behavior was already correct; the only gap was the assembled path's inability to express Promoted. | The phase doc framed rung 2 as "verification-plus"; it was even more verification-weighted than expected (one ~3-line wire). | "Verification is a legitimate cluster outcome" held — no code was added to "earn" already-correct logic. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test (the assembled integration test IS the discovery) + the real-app IRL **explicitly deferred** | `crates/runtime-main/tests/capability_live_tool.rs` — drives the REAL `run_test_session_with_tier → run_agent → drive_stream → dispatch_builtin → execute_builtin` loop at Promoted; asserts the file-on-disk side effect (absent on scope-denial, present with content on allow) + the `CapabilityViolation(Write)`/no-`TierViolation` discriminator + the `capability_failures` fold | Finding B (no production tier wire) → **TD-036** (new, routed to the live-session rung — zero propagation, NOT "→ next milestone"); HITL triage → outcome (b) recorded; the clippy wide-seam → established `#[allow]` precedent. |

This is a backend stage with an **assembled Rust integration** surface. Per the maintainer's Option-1 decision, the cluster closes on the **assembled integration test observed** (the 3 grounded-assertion tests pass against the real loop), NOT on a real-app IRL watch — the real-app/Tester scope-gate watch is **NOT exercised today** (production runs at Novice, so a real-app out-of-scope Write is tier-denied before the scope gate; plus TD-034's no-in-app-output gap) and is **deferred** to the tier-wire + UI rung's walkthrough. Stated honestly per rule 11.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4 rule 11 + §5 + §6 + §8 + Hard Rule 8 directly drove the work |
| 2 | "Read first" list oriented correctly? | 5 | The 5 read_reference files were exactly what ground-at-red needed; the M08.7.A retro's grant-loading + tier findings were load-bearing |
| 3 | Deliverable + test plan before code? | 5 | Ground-at-red findings + the BDD-already-green triage + the minimal-seam plan surfaced before any test |
| 4 | Self-correct in ≤3 rounds? | 5 | Gates green effectively first-pass (one mechanical clippy allow); no self-correction loop |
| 5 | Do-not-commit-until-approved held? | 5 | B.3.0 after decision; red after "proceed"; impl after "approved"; no push |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the genuine maintainer-domain fork (test-seam vs production-tier wire vs ADR); proceeded on the mechanical seam + API-design (sibling vs param) |
| 7 | Human-direction load reasonable? | 5 | 3 checkpoints (ground-at-red decision, red, final) — appropriate; the one AskUserQuestion was a real fork |
| 8 | Surfaces actionable? | 5 | Each surface carried a recommendation + (for the fork) a labelled decision request |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case, the `*_with`/`*_with_tier` seam convention, doc-commented, the `#[allow]` follows codebase precedent (not ad-hoc) |
| 2 | Behavior tests not tautology (§5)? | 5 | Assertions on the file-on-disk side effect + the CapabilityViolation/TierViolation discriminator, never an event alone; manual mutation spot-check confirms the wire bites |
| 3 | Public APIs documented + compile-checked? | 5 | `run_test_session_with_tier` fully doc'd (incl. the TD-036 scope note); `cargo doc -D missing_docs` green |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | One ~3-line wire + 3 assembled tests + TD-036; production tier-wire deliberately NOT built (scope lock); sandbox untouched |
| 5 | Stranger-understandable? | 5 | The test-file module doc + the seam doc explain the tier-precedes-scope model + why the seam exists + the TD-036 deferral |
| 6 | §9 anti-patterns absent? | 5 | No unsafe, no stringly-typed, no silent drops; the one `#[allow]` is documented + precedented |
| 7 | Coverage measured? | 5 | runtime-main 95.32% ≥95 (measured, exit 0) |
| 8 | CI green across the matrix? | 3 | Predicted green; only Windows-local run performed (CI Linux/macOS pending the PR run) — stated, not assumed |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | ground_at_red + the close_gate + strict-TDD + adr_triggers all earned their keep (the adr_trigger #2 framing drove the fork surface) |
| 2 | Sections missing? | 4 | The prompt's `hitl_seam=None` premise was a factual error in the context — a pre-flight grep of the seam signature could be a ground-at-red sub-step |
| 3 | Gotchas local or generalize? | 4 | "a one-line `()`-returning wire yields no viable cargo-mutants mutant; manual spot-check is primary" generalizes |
| 4 | Time-box matched? | 5 | Within the 2–4 h range; verification-weighted as predicted |
| 5 | Implicit protocol moments? | 4 | The "phase-doc premise is factually wrong; ground-at-red reshapes it" pattern is worth codifying for rungs 3–9 |
| 6 | Re-runnable by a fresh session? | 5 | The ground-at-red findings + the cited surfaces + the decision make it reproducible |
| 7 | Anything to change before copying format? | 4 | Add the "grep the actual signature the prompt references before trusting its arg description" note to ground-at-red |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — B.3.0/red/impl each after explicit approval; **no push**. `git log` shows `5480c46`, `c769b43`, `ae62b17` ahead of main.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — none observed.
- [x] **G4: stage completed** — `ae62b17`; all gates pass; the 3 assembled tests green against the real loop.
- [x] **G5: every row ≥3** — lowest is 3 (CI-matrix row: Windows-local only, CI pending; honest per rule 11).
- [x] **G6: CI-parity** — gate commands run verbatim per §6 (no CI-divergent flags). Mutation gate via `cargo mutants --in-place --in-diff` (gotcha #92 Windows workaround); 0 viable mutants → manual spot-check substitute, disclosed.

### Soft gates

- [x] **S1:** Process 40/40 ≥30
- [x] **S2:** Product 38/40 ≥32
- [x] **S3:** Pattern 31/35 ≥25
- [x] **S4:** Time-box within 2×
- [x] **S5:** ≤3 Severity-3 — zero (max severity 2)

### Outcome

- [x] **Pattern is sound.** All hard + soft gates pass. **BUT** per the cluster-gate (`cluster-pattern.md` §1 step 4) the cluster does **not** close on tests-green. Per the maintainer's Option-1 decision it closes on the **assembled integration test observed** (the 3 grounded-assertion tests run the real loop and pass); the real-app/Tester scope-gate IRL watch is **explicitly deferred** (production-Novice + TD-034/TD-036) — NOT claimed as a real-app close.

---

## [END] Coverage holdouts

### Per-package gate (runtime-main ≥95%)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 95.32% line (TOTAL), exit 0
- **Exclusions added this stage:** None — `builder/tester.rs` is fully `tempfile`/seam-testable; the new seam is exercised by the 3 assembled tests + the existing F1 tests.
- **Exclusion list as of stage end:** unchanged — `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs`

### CI workflow drift check

- [x] No coverage-config change this stage (no exclusion/threshold change) → the four mirrors (`codecov.yml`, CLAUDE.md §5/§6, `docs/coverage-policy.md`) are untouched and consistent. No `docs/coverage-policy.md` §C entry required.

---

## [END] Decisions for the next stage

### Milestone document updates (M08.7 phase doc — rungs 3–9 authoring)
- [ ] Add to ground_at_red: "grep the ACTUAL signature of any seam the prompt references before trusting its arg description — the M08.7.B prompt's `hitl_seam=None` premise did not match `run_test_session_with` (no such param)."
- [ ] Rung 3+ that touches the run-loop enforcer should know TD-036 (production runs at Novice) — a skill/budget/gap rung that needs a non-Novice tier through the assembled loop reuses `run_test_session_with_tier`.

### Protocol additions
- [ ] cluster-pattern §5: note that a one-line `()`-returning wire (e.g. `set_tier`) often yields **no viable cargo-mutants mutant** (below mutation granularity); the manual spot-check is then primary, not a fallback — disclose it as such.

### Open issues filed
- [x] **TD-036** — production never wires the user's tier into the run-loop enforcer (Tester + smoke always Novice). Routed to the live-session / tier-wiring rung.

---

## [END] Special log (per `<retrospective_requirements>`)

- **BDD scenarios already green from rung 1 vs newly proven:** The *enforcement logic* for both write scenarios was already correct from rung 1 (the executor + `dispatch_builtin` + event emission emit `CapabilityViolation{Write}` on a Promoted scope-denial and write the file on an in-scope allow). What was **newly proven through the assembled loop** is (1) the Promoted out-of-scope Write scope-gate denial (rung 1's blocked-Write denied at the *Novice tier* gate, never reaching scope) and (2) the in-scope happy path (rung 1 unit-tested it via a direct `execute_builtin` call, not through `run_test_session_with`). No production code was added to "earn" the already-correct logic — the only production change is the tier-aware seam.
- **HITL ground_at_red finding + triage disposition:** Outcome **(b)**. `run_test_session_with` has no `hitl_seam` param (hardcoded `test_defaults`); the Tester folds violations into `TestOutcome.capability_failures` and never raises a live HITL prompt (ADR-0019 + `seam.rs:104-105`). B.4.3 asserts the fold + unattended completion; live HITL surfacing is tested where a live `HitlSeam` is used.
- **Mutation-gate result (`--in-place`):** 2 mutants in-diff, **both unviable, 0 viable → 0 surviving** (`TestOutcome` not `Default` ⇒ function-body-replacement mutants don't compile; the `set_tier` statement is below cargo-mutants granularity). Manual spot-check substitutes and is strong: removing `enforcer.set_tier(tier)` → B.4.1 + B.4.2 fail (enforcer stays Novice → TierViolation / write blocked); flipping the `run_test_session_with` delegation `Tier::Novice → Promoted` → rung-1's `write_under_novice_tier_is_blocked` fails (expects TierViolation). Both possible mutations of the new logic are killed by existing+new tests.
- **The cluster closed on the assembled run, not tests-green — and what was NOT exercised:** Closed on the assembled integration test observed (3 grounded-assertion tests against the real `run_test_session_with_tier → run_agent` loop). **NOT exercised:** (a) the real-app/Tester scope-gate watch — production runs at Novice, so a real-app out-of-scope Write is *tier*-denied before the scope gate (the scope-gate `CapabilityViolation(Write)` is not observable in the running app today; TD-036); (b) in-app agent-output surfacing (TD-034); (c) CI Linux/macOS (Windows-local only, PR-pending). Per the maintainer's Option-1 decision, no real-app IRL close is claimed for the scope gate — it is deferred to the tier-wire + UI rung.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M08.7.B (rung 2 — capability enforcement on a live tool) per the stage prompt. The Promoted-tier `file_access`-scope Write denial and the in-scope happy path are now proven **through the assembled loop** via the tier-aware `run_test_session_with_tier` seam; the enforcement/executor/event logic was already correct (rung 1), so the only production change is the ~3-line tier wire. The HITL line resolved to triage outcome (b) (the Tester is HITL-less by design — ADR-0019). All machine gates pass (Windows-local, CI-parity commands): clippy 0, workspace test 0, doc 0, audit 0, deny 0, runtime-main coverage 95.32% ≥95. The mutation gate produced no viable mutant for the one-line wire; the manual spot-check confirms the tests bite.

**Per rule 11 / the cluster-gate: the cluster did NOT close on tests-green.** It closed on the assembled integration test observed; the real-app scope-gate IRL watch is **explicitly deferred** (production-Novice / TD-036, no in-app output / TD-034) — I do not claim a real-app close for the scope gate. Finding B is logged as TD-036 with zero propagation. I have committed (B.3.0 → red → impl → retro) but **will not push until you approve**.

**Surfaced at:** 2026-05-31
