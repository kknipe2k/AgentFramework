# M09.5.F — Stage Retrospective

> **Stage:** M09.5 Stage F (honest key state — chip truthfulness + the vanish forensic trail) — see `docs/build-prompts/M09.5-perimeter-hardening.md`
> **Parent milestone:** M09.5 (perimeter hardening, between M09 and M10 of the ADR-0032 slices)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-11 ~14:00 (local, build machine)
> **Session end:** 2026-06-11 ~15:45
> **Branch:** `claude/m09.5-perimeter-hardening`
> **Starting commit:** `261626a` (fast-forwarded to `f71a986` — the orchestrator's F-stage authoring commit was on origin but not local)
> **Ending commit:** `ac18e27` (red `fd7f0d1` → impl `67f15f0` → additive `ac18e27`; retro commit follows)
> **Estimated effort (per prompt):** ~2–3 hours
> **Actual elapsed:** ~1.75 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; Hard Rules honored (rule 8 plan-first WAS the stage's first step; rule 11 governs every vanish claim below)
- [x] All `<read_first>` files read in order — including the F.1–F.6 section, which required a `git pull --ff-only` first (see Friction #1)
- [x] Deliverable + test plan stated in the Hard-Rule-8 capability plan surface BEFORE any code
- [x] User approved the plan (with riders 1+2) and the red commit before each phase proceeded
- [x] Branch correct (`claude/m09.5-perimeter-hardening`)
- [x] Working tree clean at start (one pre-existing untracked `out/` directory, not touched)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| read_first | The stage prompt cited "Stage M09.5.F — F.1-F.6" but the local phase doc ended at Stage E; the section existed only as an unpulled origin commit (`f71a986`). Resolved with `git fetch` + `--ff-only` pull. | The prompt could note "pull origin first — the F section landed after your branch head." Mild; the fetch was the obvious move. | 2 |
| wire audit | F.2/F.3 said "in handleSmoke **and the Tester run handler**, call `setHasKey(false)`" — but the Tester catch lives in `TesterModal.tsx`, mounted prop-less in `BuilderShell`, with no path to App's `useState` setter. The plan had to add a design decision (hasKey → graphStore, the `currentTier` precedent) the doc's file list didn't anticipate. | The doc's own `<wire_signature_audit>` caught this at plan time — working as designed. Logged because the F.2 file list ("`src/App.tsx`…") under-scoped the change. | 2 |
| verify_gates | First post-`llvm-cov clean` workspace coverage run failed: 2 of 5 `tester_isolated_session` tests red under the instrumented target while plain `cargo test --workspace` was fully green. Hypothesis: the clean wiped `target/llvm-cov-target`, so `ensure_drone_built` (always-build per `27a40a4`) raced the parallel suites on the first instrumented run. Re-run in isolation: 5/5; full gate re-run: green at 91.64%, **no flag changes** (no `--test-threads` divergence used). | Gotcha candidate: "first `cargo llvm-cov` run after `clean` can flake subprocess-fixture suites while the instrumented drone builds; re-run before diagnosing deeper." | 3 |
| verify_gates | The background gate chain `cargo llvm-cov … \| tail -4 && echo WORKSPACE_COV_OK` echoed the OK marker on a FAILED run — the pipe made `tail`'s exit status the chain's. Caught immediately (the failure text was visible above the marker); all subsequent gate runs captured `$?` from the cargo command directly. | Self-inflicted shell-scripting trap, not a protocol gap. Logged for honesty: a marker echoed after a pipe is not evidence. | 2 |
| impl | One pre-existing test (`smoke_error_banner_cleared_when_new_run_starts`) regressed under the new settled-run re-poll: it leaves `has_api_key` unrouted (mock resolves `undefined`), so the re-poll wrote `undefined` into `hasKey` and disabled the smoke button. Fixed in production (not the test): `refreshHasKey` ignores non-boolean payloads — the exact `getCurrentTier` mount-seed / `listInstalledArtifacts` defensive-coercion precedent. | None — this is the strict-TDD constraint working: the impl could not touch the test, so the fix had to be (and was) a legitimate production hardening. | 2 |
| verify_gates | `vitest --coverage`'s text reporter omitted the `src/lib/keyState.ts` row entirely; the lcov record (what Codecov consumes) shows it fully covered (5/5 lines, 2/2 branches, 37 hits). Verified via `coverage/lcov.info` before claiming coverage. | Gotcha candidate: don't conclude "file not covered" (or "covered") from the v8 text table; the lcov record is the evidence. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Stage prompt vs git log | Stages D and E are authored but unexecuted; the orchestrator dispatched F directly. | Flagged in the plan surface; user confirmed "D/E-before-F noted, intended (independent surfaces)." | Nothing to fix. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Before any code | Hard-Rule-8 capability plan: log placement (inside `read_api_key`), §13 source+key_len-only guarantee, the hasKey→graphStore design decision, test list, no-Rust-unit rationale | Yes — approved with two riders that materially improved the design (the sticky-flip invariant + its red test) | Yes — rule 8 requires exactly this ordering |
| After red commit | Red surface: 4 failing tests + reasons, hygiene state, cross-machine state | Yes — approved verbatim | Yes |

**Rider 1 deserves the log entry:** the plan as first surfaced had the finally re-poll racing the SetupRequired flip — in the vanish case (keychain probe still true, run resolved none) the async poll would have re-greened the chip beside the failed run, recreating the exact lie the stage exists to kill. The maintainer caught it at plan review; the fix (skip the re-poll on SetupRequired) is now pinned by the `has_api_key`-called-exactly-once red test.

---

## [LIVE] Protocol drift events

None observed. (Commits at each phase were made only after the corresponding explicit approval: plan → red commit authorized by the plan approval's "Proceed … red"; impl + additive + push authorized by the red approval's "PROCEED TO IMPLEMENT … green commit, push, stop.")

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The strict no-touch-tests constraint converted a would-be test hack into a production fix | The regressed banner test could not be "fixed" — so the only path was asking whether the production code should tolerate the malformed payload. It should (documented precedent existed). | The two-commit discipline is doing real design work, not just process hygiene. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **Deferred to maintainer IRL** (per the red-approval: "push, stop — the maintainer dispatches CI + runs the IRL") | The close-gate IRL script: no-key run → chip flips "no key" + terminal `key source=none`; good-key run → chip stays "key active" + `key source=keychain key_len=…` | None |

**The assembled app was NOT run in this session — the chip-flip behavior is unit/wire-verified only; the key-source terminal line has never been observed in the running app. Both await the maintainer IRL. CI (3-OS matrix, Codecov delta, e2e-tauri job) has NOT yet run on these commits.**

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | CLAUDE.md sufficient orientation? | 5 | Rules 8 + 11 drove the stage's shape end-to-end |
| 2 | Read-first list correct? | 4 | Complete once pulled (Friction #1); the F.2 file list under-scoped (Friction #2) |
| 3 | Deliverable + test plan stated before code? | 5 | The Hard-Rule-8 plan surface |
| 4 | Self-correct in ≤3 rounds? | 5 | verify_gates used 2 of 3 iterations (the re-poll/banner-test fix; the llvm-cov first-run flake) |
| 5 | Do-not-commit-until-approved held? | 5 | Plan → red → impl each gated on an explicit approval |
| 6 | Escalate right / proceed right? | 5 | Escalated the graphStore design deviation + the no-Rust-unit call in the plan rather than burying them |
| 7 | Human-direction load reasonable? | 5 | Two approvals, two riders — both load-bearing |
| 8 | Surfaces actionable? | 5 | Rider 1 proves the plan surface carried enough detail to be reviewed substantively |
| **Process axis total** | | **39/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | §9 style + naming? | 5 | Comments state constraints (sticky-flip why, §13 guarantee), not narration |
| 2 | Behavior tests, not tautology (§5)? | 5 | All four reds assert user-observable chip text / wire-shape rejections; the additive kills a named mutant |
| 3 | Public APIs documented? | 5 | `isSetupRequired`, `refreshHasKey`, `setHasKey`, the `read_api_key` forensic-trail doc |
| 4 | Deliverables exact — no creep/cuts? | 5 | Scope locks held: ADR-0025 untouched, no new command, no e2e/schema change; the one file-list deviation (graphStore) was plan-approved |
| 5 | Stranger-readable? | 4 | The sticky-flip logic needs the vanish context; the comments carry it |
| 6 | §9 anti-patterns absent? | 5 | No allows, no magic values, no silent drops (the non-boolean guard logs nothing silently — it leaves state, documented) |
| 7 | Coverage measured? | 5 | vitest 93.42% lines (lcov-verified for keyState.ts); workspace llvm-cov 91.64%; runtime-main 96.12% — all exit-code-captured |
| 8 | CI green across matrix? | 2 | **NOT run — commits are local-then-pushed; CI dispatch is the maintainer's next step. Unverified, stated.** |
| **Product axis total** | | **36/40** | |

### Axis 3: Pattern — does this generalize?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Template sections useful? | 5 | The plan-first step caught a real design hole (rider 1) |
| 2 | Sections missing? | 4 | A "pull origin first" pre-step for stage prompts authored after the branch head |
| 3 | Gotchas local or general? | 4 | Two general candidates: llvm-cov first-run fixture race; v8 text-table omission |
| 4 | Time-box within 2×? | 5 | Under estimate |
| 5 | Implicit protocol moments? | 4 | "Marker echoed after a pipe is not evidence" — now explicit here |
| 6 | Re-runnable by a fresh session? | 5 | The stage prompt + riders fully determine the result |
| 7 | Format changes needed? | 5 | None |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red committed after plan approval explicitly authorized it; impl/additive/push after red approval's "PROCEED TO IMPLEMENT … green commit, push, stop"
- [x] **G2: no Severity-5 friction** — max severity 3 (llvm-cov first-run flake)
- [x] **G3: no unaddressed protocol drift** — none observed
- [x] **G4: stage completed** — `fd7f0d1` (red) + `67f15f0` (impl) + `ac18e27` (additive) on the milestone branch; all F.4 BDD rows covered (the third row — the honest non-claim — is this document)
- [x] **G5: scores ≥3 every row** — one exception, stated: Product #8 (CI) scores 2 because **CI has not run**; this is the claim-evidence-parity rule applied to the rubric itself, not a process failure. The row's evidence arrives with the maintainer's CI dispatch.
- [x] **G6: CI-parity** — every gate ran with the exact §6 command: `cargo fmt --all -- --check` ✓ · `cargo clippy --workspace --all-targets -- -D warnings` ✓ · `cargo test --workspace` ✓ (76 suites, 0 failures) · `cargo test --workspace --doc` ✓ · `RUSTDOCFLAGS="-D missing_docs -D rustdoc::broken_intra_doc_links" cargo doc --workspace --no-deps` ✓ · `cargo audit` ✓ · `cargo deny check` ✓ · `cargo llvm-cov clean --workspace` then workspace gate ✓ 91.64% (exit 0) · runtime-main gate ✓ 96.12% (exit 0) · `npx prettier --check` ✓ · `npx eslint .` ✓ · `npx tsc --noEmit` ✓ · `npm run test` ✓ 680/680, 93.42% lines · `npm audit --audit-level=high` ✓ exit 0 (2 moderates below threshold). **Zero flag divergences** — the llvm-cov flake was resolved by re-run, not by `--test-threads`. Untouched-crate gates (drone/sandbox/mcp) not re-run locally — no file in those crates changed; CI runs them.

### Soft gates

- [x] **S1:** Process 39/40 ≥ 30
- [x] **S2:** Product 36/40 ≥ 32
- [x] **S3:** Pattern 32/35 ≥ 25
- [x] **S4:** within 2× (under estimate)
- [x] **S5:** one Severity-3 event ≤ 3

### Outcome

- [x] **Pattern is sound.** All hard gates pass (G5's CI row is an honest pending-evidence marker, with the CI dispatch as the close); all soft gates pass. **Verdict scope: unit + wire + local gates green; the assembled app was NOT run and CI has NOT run — the maintainer IRL + CI dispatch are the outstanding evidence.**

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.64% (exit 0)
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged)

### Per-package gates (≥95% line on safety primitives)

- **Crate:** runtime-main
- **Coverage actual this stage:** 96.12% (exit 0)
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs` (unchanged)

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `runtime-main/src/key_store.rs` | not individually gated (documented OS-keychain seam-vs-wrapper holdout, `docs/coverage-policy.md` §A note) | 66.02% per-file | ↓ (per-file only) | The forensic-trail lines land one per existing match arm; the NoEntry/backend-error arms are keychain-state-dependent on any given runner (same conditional class as the pre-existing arms). The package gate — the actual contract — holds at 96.12%. Codecov's delta gate on CI is the cross-runner arbiter. |
| `src/lib/keyState.ts` (new) | — | 100% (lcov: 5/5 lines, 2/2 branches) | new | — |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage; the workflow file untouched.

---

## [END] Decisions for the next stage (D or E — whichever the orchestrator dispatches)

### `CLAUDE.md` updates (gotcha candidates — queue with B's and C's for Stage G)

- [ ] Gotcha candidate (a): **the first `cargo llvm-cov` run after `clean --workspace` can flake subprocess-fixture suites** — the clean wipes the instrumented target dir, so always-build fixtures (`ensure_drone_built`) race parallel test execution on the first run. Re-run once before deeper diagnosis; do NOT reach for `--test-threads=1` (gotcha #56's divergence class).
- [ ] Gotcha candidate (b): **vitest's v8 text coverage table can omit a file the lcov record covers** — `src/lib/keyState.ts` was absent from the text table while `coverage/lcov.info` showed it 100%. The lcov record (what Codecov consumes) is the evidence; never conclude coverage presence/absence from the text table alone.

### Milestone document updates

- [ ] None — D's and E's prompts are engine/provider-side; the F-specific learnings (graphStore chip-state pattern, sticky-flip) are encoded in code comments + this retro.

### Protocol additions

- [ ] Candidate for `STAGE-PROMPT-PROTOCOL.md` or the stage-prompt boilerplate: a `<read_first>` pre-step "fetch origin; the stage section may postdate your branch head" (this is the second time a stage section lived only on origin at session start).

### Open issues to file

- [ ] None. The vanish remains OPEN by design — F's deliverable is the trail, not the fix. The next occurrence produces a `key source=` line that converts it from mystery to diagnosis.

---

## [END] Special log (per the stage's `<retrospective_requirements>`)

- **The confirmed mechanism:** `App.tsx` `hasKey` was seeded at mount + set in `handleSetKey`; neither `handleSmoke` (App.tsx:196-211 pre-change) nor the Tester catch ever wrote it. A `SetupRequired` run failure therefore left the chip green beside the failure — the DESIGN.md principle-8 violation the maintainer IRL observed. Fixed and pinned by four behavior tests.
- **The unreproduced vanish:** a run failing no-key while the keychain entry exists. Hypotheses ruled out before this stage: write target == read target (`Entry::new("agent-runtime","anthropic")`, key_store.rs:106/:74); `ANTHROPIC_API_KEY` confirmed unset on the maintainer's shell (ADR-0025 shadowing excluded); the e2e harness never touches the keychain (wdio `beforeSession` seeds `tier.json` only).
- **Rule 11 statement:** **F instruments the vanish; it does not fix it and does not claim to.** A real fix waits on a reproduction. What F guarantees: the next occurrence emits `key source=none` (or contradicting `source=keychain`/`env` lines around it), making the failure class identifiable from one terminal line.
- **§13 discipline:** every new log line carries the source tag + `key_len` only — never the value, a prefix, or the `SecretString`. The backend-error arm's `error = %e` displays a `keyring::Error`, which carries no credential material (and matches the pre-existing call-site log at commands.rs:270). Mirrors the `set_api_key_with` precedent (commands.rs:218).
- **The maintainer IRL:** pending — the close-gate script is in the Stage-0 section above. Nothing in this retro claims the assembled behavior has been observed.

### Mutation gate (advisory per the stage prompt)

Not run this stage, stated plainly: the Rust diff is log lines on existing covered branches (cargo-mutants' value-replacement mutants on `read_api_key` are already killed by the three env-precedence tests asserting the returned value); the behavioral logic is renderer TS, outside cargo-mutants' reach. The TD-056 nightly workflow covers runtime-main on main. The one store-logic mutant the unit suite would have missed (dropping `hasKey: state.hasKey` from `clear()`) is killed by the additive test in `ac18e27`.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M09.5 Stage F, authored per `CLAUDE.md` §19. Commits `fd7f0d1` / `67f15f0` / `ac18e27` were each made under an explicit prior approval (plan approval / red approval). The push below is authorized by the red approval's "green commit, push, stop." The assembled-app IRL and the CI matrix run are the outstanding evidence; no claim above exceeds what was locally observed.

**Claude:** M09.5.F's build work is complete and pushed per the red approval's instruction. The maintainer dispatches CI and runs the close-gate IRL. The stage closes on those two observations, not on this document.

**Surfaced at:** 2026-06-11 15:45 local
