# M08.G — Stage Retrospective

> **Stage:** M08 Stage `G` — Settings panel + Novice↔Promoted tier promotion — see `docs/build-prompts/M08-workbench.md`
> **Parent milestone:** M08 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-21
> **Session end:** 2026-05-21
> **Branch:** `claude/m08-workbench` (parent-milestone feature branch)
> **Starting commit:** `4c597ec` (M08.F2 retrospective)
> **Red commit:** `5ec4b85` (`test(M08.G): …` — amended once: see Friction / Protocol)
> **Impl commit:** `3260709` (`feat(renderer): M08 Stage G …`)
> **E2E-fix commit:** `32f3fc8` (`test(M08.G): make the D2 Agent↔Agent edge e2e robust …`)
> **Ending commit:** `32f3fc8` (retro commit pending approval)
> **Estimated effort (per prompt):** 6–9 hours
> **Actual elapsed:** ~4 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded and the §4 Hard Rules were observed (do-not-commit-without-approval, don't-push, no skipped gates, CODEOWNERS plan-first, no `unsafe` outside the sandbox)
- [x] All "Read first" files were read in order (CLAUDE.md §10, STAGE-PROMPT-PROTOCOL v1.8, M08 phase doc G.1–G.6 + Stage C C.3.2, M07-irl-findings #5, spec §8.security L4 / §0d / §2a, M08.F2 retro, M08.A retro, gotchas)
- [x] The deliverable + test plan were stated and surfaced for red-phase approval before any green implementation
- [x] User confirmed the red phase ("M08.G red approved — proceed to green") before green began
- [x] The branch was correct (`claude/m08-workbench`, M08.F2 impl `701facf` present in `git log main..HEAD`)
- [x] No uncommitted changes carried over (`git status` clean apart from the pre-existing untracked `.claude/` junk)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Wire pinning (pre-pseudocode) | The stage prompt's `<phase_doc_inventory_audit>` + `<wire_signature_audit>` both cite `request_tier_transition` at `commands.rs:542`; the shipped command is at `:573` (`_with` seam at `:599`, idempotent no-op at `:621`). The params themselves were correct — only the line number drifted (intervening M05.6/M07.5 edits shifted `commands.rs`). | The phase doc's line numbers are advisory; `<wire_signature_audit>` says "PIN both verbatim" — the `<read_reference>` to `commands.rs` is the mechanism that caught it. No prompt change needed beyond noting line numbers rot. | 1 |
| Green gate (full e2e suite) | The `<SettingsPanel/>` mount adds App-level chrome above the Builder canvas (cross-mode, C.3.2), shrinking the canvas. A pre-existing M08.D2 e2e (`builder_edges.spec.ts` Agent→Agent narrowing) regressed: React Flow's fitView landed the two large agent nodes at max zoom, too tall to stack with unobscured handles, so the handle-to-handle edge drag could not connect (`spawns` stayed `[]`). The first robust-fix attempt (canvas-relative top/bottom placement) did NOT fix it — at max zoom the nodes overlap regardless of placement. The second attempt (zoom-out-until-small loop) fixed it. | A `<layout_shift_audit>` step — "this stage adds App-level chrome above the React Flow Builder canvas → re-verify every coordinate-sensitive Builder e2e" — would have predicted the regression. The `<existing_pattern_audit>` flagged the BudgetHeaderBar *mount pattern* but not its *layout consequence*. The exact M08.E fragility class (commits `f1f3a72` / `b3a9cfa` fixed it twice). | 3 |

---

## [LIVE] Ambiguity events

None observed. The phase doc G.3.1–G.3.5 pseudocode was precise and pinned to file:line; the only drift was the stale line number (Friction row 1), a fact-check not an ambiguity.

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After the red commit (`5805234`, pre-amend) | The wire-signature audit (pinned `request_tier_transition` params + the `:542→:573` line correction), the red-failure proof (15/15 right-reason), the green plan, and an explicit flag that the commits lacked the session-URL footer. | Yes — the user approved and gave three follow-up instructions (session URL, proceed, confirm pre-red sweep). | Yes — before green, so a different decision cost nothing. |
| Stage end (this surface) | The full green bundle: gate results, the strict-TDD invariant proof, the three-commit structure, the e2e-regression diagnosis + fix. | Yes | Yes |

---

## [LIVE] Protocol drift events

None observed. Two intentional, user-directed deviations are NOT drift:

- The red commit `5805234` was **amended** (→ `5ec4b85`) to (a) add the `https://claude.ai/code/session_…` footer and (b) move the behaviour-neutral type scaffolding (the `requestTierTransition` wrapper + the `globalBudgetCap` store slot) INTO the red commit so the frozen test files type-check — the v1.9 `<pre_red_mechanical_sweep>` the M08.A retro recommended, here explicitly invoked by the user ("confirm the red commit is eslint/prettier/tsc clean before green"). This is the F2 `aa9ec49` precedent ("the testFramework wrapper … landed in the red commit as the strict-TDD scaffolding"). Amending a surfaced-but-unpushed commit on direct user instruction is not a Hard-Rule-1 violation.
- The `builder_edges.spec.ts` fix is a **separate `test(M08.G)` commit** (`32f3fc8`) AFTER the green `feat` impl (`3260709`), not folded into it — so `git diff 5ec4b85..3260709 -- '**/tests/**'` stays EMPTY (the strict-TDD invariant). This is the M08.E precedent (`f1f3a72` / `b3a9cfa`).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The first e2e-regression fix (canvas-relative top/bottom node placement) did not work. | I expected measuring the canvas box and placing the parent at the top / child at the bottom to guarantee separation. But React Flow's fitView pins the canvas at MAX zoom when two nodes are dropped close together ("Zoom In" disabled in the failure snapshot) — at max zoom the two large agent nodes are taller than half the shortened canvas, so any vertical placement still overlaps their handles. | A coordinate-sensitive React Flow e2e on a canvas of variable height needs a *zoom-normalising* step (zoom out until the content is small relative to the pane), not just position normalisation. Worth a gotcha. |
| `SettingsPanel.tsx` reached 100% line/branch/function coverage with the 14 frozen red-phase tests — no green-phase coverage gap. | The component's only un-obvious branch (`!Number.isFinite` in `handleSave`) is unreachable from a `type="number"` input, yet the file still hit 100% because v8 counts the guard line as covered by the `parsed < 0` test. | The frozen-test-list discipline worked — writing the 14 tests against the final API surface in the red phase left no coverage hole to backfill. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation? | 5 | §5 strict-TDD two-commit, §6 gate ordering, §8 commit workflow, §10 Hard Rule 8, §12 own-technical-decisions all applied directly. |
| 2 | Did the "Read first" list orient correctly? | 5 | All read-first + read-reference files were load-bearing; the `:542→:573` line drift was caught by reading `commands.rs` per `<read_reference>`. |
| 3 | Did Claude state deliverable + test plan before code? | 5 | The red surface gave the wire audit + the 15-test plan + the green plan before green. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | The e2e regression took 2 fix rounds (canvas-relative placement → zoom-out loop). Within the 3-round budget. Every other gate (prettier/eslint/tsc/vitest/audit) was first-pass green. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | Red `5ec4b85` committed locally + surfaced + approved before green; green `3260709` + e2e-fix `32f3fc8` after "proceed to green"; nothing pushed. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Escalated the red phase for approval; proceeded on technical decisions (scaffolding-into-red, the zoom-out e2e fix, the separate test-fix commit) per §12. |
| 7 | Was the human-direction load reasonable? | 5 | One red-phase approval (with 3 crisp follow-up instructions), one final approval — the §12 model. |
| 8 | Were the surfaces actionable? | 5 | The red surface gave the wire reconciliation + green plan + the footer flag, so the user's response was three precise instructions, not a question. |
| **Process axis total** | | **39/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | camelCase, one-thing functions (`TierControl`/`BudgetControl`/`handleTransition`/`handleSave`), comments explain *why* (the no-optimistic-set invariant, the configured-vs-spend distinction), no marketing language. |
| 2 | Are tests behavior tests, not tautology tests? | 5 | `tier_control_does_not_optimistically_set_currentTier` is the load-bearing behavior test (the reducer, not the component, owns the write); the Playwright spec drives the real promote → scripted-`tier_transition` → reducer flow. |
| 3 | Are public APIs documented? | 5 | `requestTierTransition`, `SettingsPanel`, `globalBudgetCap`, `setGlobalBudgetCap` all carry doc comments stating the wire pin / the surfaces-not-reimplements contract / the configured-vs-spend distinction. |
| 4 | Are deliverables exactly what was promised? | 5 | All G.3 deliverables shipped; no scope creep (Operator not surfaced, the API-key UI left in SetupPanel), no scope cuts. |
| 5 | Would a stranger understand the code? | 5 | The component docs state the cross-mode mount rationale, the single-writer invariant, and the M07-IRL #5 / M06.5 🟡-4 closures inline. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No `any`, no ad-hoc `@ts-ignore`/`#[allow]`, no magic numbers, no stringly-typed APIs, no silent error drops (`unwrapCmdError` + `console.error`). |
| 7 | Is coverage measured, ≥ gates? | 5 | Measured: `SettingsPanel.tsx` 100% line/branch/function; renderer aggregate 97.08% line (≥80). 576 vitest green. |
| 8 | Did CI green hold cross-platform? | 4 | All local gates green; renderer-only, no `cfg`-gated platform branches (gotcha #74 N/A). CI cross-OS prediction: green. Scored 4 — CI is the cross-platform proof, run post-push. |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern — does this generalize?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all template sections useful? | 5 | `<wire_signature_audit>` forced the pin (caught the `:542` drift); `<pre_flight_check>` confirmed the backend + view-switch before wiring; `<architecture_check>` codified the no-optimistic-set + cross-mode-mount invariants. |
| 2 | Were any sections needed but missing? | 4 | The `<existing_pattern_audit>` flagged the `BudgetHeaderBar` *mount pattern* but not its *layout consequence* — that adding App-level chrome above the Builder canvas shifts coordinate-sensitive React Flow e2e tests. A `<layout_shift_audit>` would have predicted the `builder_edges.spec.ts` regression. |
| 3 | Do milestone gotchas generalize? | 4 | "New App-level chrome above the React Flow Builder canvas shifts coordinate-sensitive e2e tests; fitView pins max zoom for close-dropped nodes — normalise zoom, not just position" generalises to any future Builder-chrome stage. Worth a gotcha. |
| 4 | Did the time-box match reality? | 5 | ~4h vs the 6–9h estimate — a renderer-only stage over a working backend, with the panel pattern locked from M06.E. Under the box, within 2×. |
| 5 | Implicit protocol moments? | 4 | The "scaffolding into the red commit so the frozen test files type-check" was a user instruction (the v1.9 `<pre_red_mechanical_sweep>` candidate) — still not formally in the strict-TDD protocol text. The "pre-existing e2e regression → separate `test()` commit after impl" is the M08.E precedent but implicit. |
| 6 | Re-runnable by a fresh session? | 4 | Yes — the pins are deterministic; the `builder_edges` regression would recur but the fix + its rationale are now committed. |
| 7 | Anything to change before re-use? | 4 | Fold the `:542→:573` line numbers into the M08 phase doc; add the `<layout_shift_audit>` step. |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `5ec4b85` committed locally then surfaced for red approval; green `3260709` + e2e-fix `32f3fc8` after the user's "proceed to green"; nothing pushed to origin; this final surface precedes any push.
- [x] **G2: no Severity-5 friction** — max severity 3 (the `builder_edges.spec.ts` layout-shift regression).
- [x] **G3: no protocol drift unaddressed** — drift table empty; the two user-directed deviations (red amend, separate e2e-fix commit) are documented as non-drift.
- [x] **G4: the stage completed** — `5ec4b85` / `3260709` / `32f3fc8` on `claude/m08-workbench`; all G.4.5 acceptance criteria met (15 vitest + 4 Playwright green; M07-IRL #5 + M06.5 🟡-4 closed; the strict v1.8 two-commit invariant held).
- [x] **G5: every axis row ≥3** — lowest row is 4.
- [x] **G6: CI-parity** — every gate run with the exact `CLAUDE.md` §6 frontend command: `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'`, `npx eslint .`, `npx tsc --noEmit`, `npm run test` (`vitest run --coverage` — 576 passed, 97.08% line), `npm audit --audit-level=high` (exit 0 — 8 moderate, none high+), `npm run test:e2e` (full Playwright suite — 59 tests, not just `settings_tier_promotion.spec.ts`). No CI-divergent flags. Renderer-only stage — no `cargo` gate applies (no Rust crate touched; `git diff 4c597ec..HEAD -- crates/ src-tauri/ schemas/` is empty).

### Soft gates

- [x] **S1:** Process 39/40 ≥ 30
- [x] **S2:** Product 39/40 ≥ 32
- [x] **S3:** Pattern 30/35 ≥ 25
- [x] **S4:** Time-box — ~4h vs 6–9h estimate, ratio ~0.5 (within 2×)
- [x] **S5:** ≤3 Severity-3 friction — actual: 1

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to the M08 closeout (Stage V + summary + gap-analysis + PR) with the minor phase-doc / protocol updates in the Decisions section applied.

---

## [END] Coverage holdouts

M08.G is a **renderer-only** stage — it touches no Rust crate (`git diff 4c597ec..HEAD -- crates/ src-tauri/ schemas/` is empty), so no `cargo llvm-cov` workspace / per-package gate applies. Coverage is the Vitest renderer gate (`vitest.config.ts` — ≥80% lines/functions/branches/statements over `src/**`).

### Renderer gate (≥80%, Vitest)

- **Coverage actual this stage:** 97.08% lines / 92.43% branches / 94.09% functions / 97.08% statements (aggregate over `src/**`).
- **New file:** `src/components/SettingsPanel.tsx` — 100% line / 100% branch / 100% function / 100% statement.
- **Exclusions added this stage:** None.

### CI workflow drift check

- [x] No coverage-policy change this stage — no Rust gate touched, no `--ignore-filename-regex` change, no threshold change. The Vitest `src/types/**` exclusion is unchanged.

---

## [END] Decisions for the next stage (M08 closeout)

### `CLAUDE.md` updates

- [ ] Consider a gotcha: **adding App-level chrome above the React Flow Builder canvas shifts coordinate-sensitive e2e tests, and React Flow's `fitView` pins MAX zoom when two nodes are dropped close together** — a robust handle-to-handle-drag e2e must normalise zoom (zoom out until content is small relative to the pane), not just node position. Recurred M08.E (`f1f3a72`/`b3a9cfa`) and M08.G (`32f3fc8`).

### Milestone document updates (`M08-workbench.md`)

- [ ] Stage G `<phase_doc_inventory_audit>` + `<wire_signature_audit>` + G.3.1: `request_tier_transition` is at `commands.rs:573` (not `:542`); the `_with` seam at `:599`; the idempotent no-op return at `:621`. The params were correct — only the line numbers drifted.

### Protocol additions (`STAGE-PROMPT-PROTOCOL.md`)

- [ ] Consider a `<layout_shift_audit>` slot: when a stage mounts new App-level / always-visible chrome, enumerate the coordinate-sensitive e2e specs (React Flow canvases, drag-drop) that must be re-verified, so the regression is predicted at authoring time rather than caught at the green gate.
- [ ] Formalise the v1.9 `<pre_red_mechanical_sweep>` (M08.A's named candidate, user-invoked here): the red phase prettier/eslint/tsc-sweeps the test files + the behaviour-neutral type scaffolding so the red commit is gate-clean and the frozen test files type-check.

### Open issues to file

- [ ] None — the `builder_edges.spec.ts` regression is closed by `32f3fc8`; the fix is canvas-relative + zoom-normalising, robust to any future chrome.

---

## [END] Special log (per `<retrospective_requirements>`)

**Pinned `request_tier_transition` wire (from the shipped command).** `request_tier_transition(app: AppHandle, target_tier: Tier, reason: String, state: State<CurrentTierState>) -> Result<(), CmdError>` at `src-tauri/src/commands.rs:573`. `app` + `state` are Tauri-injected and do NOT cross the IPC bridge; the two renderer-supplied args are `target_tier` (Tauri auto-camelCases the snake_case Rust name → JS key `targetTier`) and `reason`. The Rust `Tier` enum serialises to `"novice" | "promoted"` — byte-identical to the generated `TierRef` (`agent_event.ts:157`), so the `requestTierTransition` wrapper reuses `TierRef`; no new type. The command is idempotent when `target == current` (`commands.rs:621` returns `Ok` for the no-op).

**G surfaces — does not reimplement — the tier command.** `SettingsPanel.tsx` calls `requestTierTransition` and *reads* `s.currentTier`; it never sets `currentTier`. The `tier_transition` event the backend emits is reduced into `currentTier` by the EXISTING `graphStore` branch (`graphStore.ts:1549`) — no new reducer case, and zero edits under `crates/runtime-main/src/tier/` (Hard Rule 8 held). The displayed tier updates through the existing event path; the Playwright spec proves it (`promoting via Settings updates the displayed tier through the tier_transition reducer` — click promote, then script the `tier_transition` event, then assert the tier flips).

**Operator is not surfaced anywhere in the DOM.** The `TierControl` transition target is derived purely from the two-member `TierRef` (`tier === 'novice' ? 'promoted' : 'novice'`) — there is no third branch because the type has no third member. Behavior-asserted twice: vitest `tier_control_never_renders_an_operator_option` (the rendered `settings-panel` contains no `/operator/i` text) and Playwright `the Settings tier control never offers an Operator option`.

**M07-IRL #5 closed.** The Settings panel exists; the Novice↔Promoted control calls the existing `request_tier_transition`; the Promoted tier — a §0d v0.1-scope capability — is reachable for the first time. This unblocks the (correctly tier-blocked) M06 MCP Add modal and **sets up the post-M08 IRL re-confirm of M06.5 🔴-1** (MCP-registry real-app re-confirm — "gated on #5"); that re-confirm is an IRL-pass item, not an M08.G deliverable.

**M06.5 IRL 🟡-4 closed.** The `graphStore.globalBudgetCap` slot + `setGlobalBudgetCap` action wire the budget-cap input to reflect the configured cap and persist it via the existing `set_global_budget` command (`invokeSetGlobalBudget`). `globalBudgetCap` is distinct from the per-session `budget` spend snapshot, and is preserved across `clear()` like `currentTier`. Behavior-asserted: `saving_cap_updates_globalBudgetCap_slot_so_input_reflects_it` (vitest) + `the budget cap input reflects and persists a set value` (Playwright).

**Cross-mode mount.** `<SettingsPanel/>` mounts at `App.tsx:178` — App top level, outside the `view` conditional, a sibling of `BudgetHeaderBar` / `ViewSwitch`. Playwright-asserted that it renders in BOTH Runtime (default) and Builder modes (`the Settings panel is reachable in Builder mode (cross-mode chrome)`). No Settings-tab / routing infrastructure (the M06.E no-routing rule).

**Strict v1.8 two-commit TDD.** `git diff 5ec4b85..3260709 -- '**/tests/**'` is EMPTY — the M08.G stage test files (`SettingsPanel.test.tsx`, `settings_tier_promotion.spec.ts`) are byte-identical red→impl. The `builder_edges.spec.ts` layout-shift fix is the separate, post-impl `test(M08.G)` commit `32f3fc8` (the M08.E precedent), so it does not enter the red→impl test-diff.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M08 Stage `G`, authored per `CLAUDE.md` §19. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch; the M08 PR drafts and pushes at the closeout (after Stage V + the summary + the gap-analysis entry).

**Claude:** I have completed M08 Stage G per the stage prompt. The Settings panel ships as a renderer-only surface over the existing M05.D `request_tier_transition` backend command: a focused `TierControl` (current-tier display + a single Novice↔Promoted button) + `BudgetControl` (the global-budget-cap input), mounted at App top level as cross-mode chrome. It surfaces the tier command — it does not reimplement tier-transition or enforcement logic (Hard Rule 8); the displayed tier updates through the existing `tier_transition` reducer. M07-IRL #5 (no tier-promotion UI → the Promoted tier unreachable) and M06.5 IRL 🟡-4 (budget settings not state-wired) are both closed. Operator is never surfaced (§0d). All gates are green (15 vitest + 4 new Playwright; 576 vitest + 59 Playwright total; `SettingsPanel.tsx` 100% line; prettier/eslint/tsc/audit clean). The strict v1.8 two-commit invariant held — `git diff 5ec4b85..3260709 -- '**/tests/**'` is empty. One pre-existing M08.D2 e2e regressed from the new chrome's layout shift and is fixed in the separate `test(M08.G)` commit `32f3fc8`. I have NOT pushed; I am surfacing this retrospective + the diff stat + the commit set for user approval.

**Surfaced at:** 2026-05-21
