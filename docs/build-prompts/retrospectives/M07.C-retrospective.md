# M07.C — Stage Retrospective

> **Stage:** M07 Stage `C` — import-pipeline backend (spec Phase 7 §2152-2211; MVP §M7; ADR-0005). See `docs/build-prompts/M07-registry-import.md` §C.
> **Parent milestone:** M07 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-18
> **Session end:** 2026-05-18
> **Branch:** `claude/m07-registry-import` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `b4936b2` (M07.B retrospective; HEAD before this stage)
> **Ending commits:** red/test `3a586be`; impl/green `849fd0c`; mechanical follow-up `724e4cb`
> **Estimated effort (per prompt):** 7–9 h
> **Actual elapsed:** ~5 h equivalent (0.6×, in the M06/M07.B calibration band)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in force (no push without approval; strict-TDD two-commit; no Co-Authored-By).
- [x] All `<read_first>` files read in order (CLAUDE.md, STAGE-PROMPT-PROTOCOL v1.8, M07 phase doc Background/Key-constraints/Stage C C.1–C.6 + Stage B for the consumed surface, spec §2152-2211 + §15/§15c/§15d, ADR-0005 fields confirmed already in `framework.v1.json`, ADR-0007 HitlSeam, the M07.B `skills_lock` module + its generated types + L3 `sandbox_ipc`/`runtime-sandbox` + L4 `tier` + `runtime-mcp` registry + `sdk::mcp_dispatch` dependency-inversion archetype + `src-tauri` command/state patterns, M07.B retrospective `[END]`).
- [x] **M07.B `[END]` Decisions read + applied:** `install`→`skills_lock::write_entry`; `content_hash` over fetched bytes so `verify` of the same bytes passes (pinned in `promoted_url_import_…`); `ImportSource::Url/File` serialize to B's discriminated `Source` shape (dedicated assertions); B's `verify` `NotFound`/un-locked = HARD block (no un-locked load path introduced). The B phase-doc-defect note (B.3.1/B.3.2) is orchestration-session-owned — not edited here.
- [x] `<pre_flight_check>`: HEAD == `claude/m07-registry-import`, M07.B impl commit (`28c16b4`) present. `reuse_surfaces` grep-confirmed: `sandbox_ipc::SandboxClient` (L3), `tier::Tier`/`TierEvaluator` (L4), `runtime_mcp::client::registry::Registry::{open,insert,remove}` (M06), `capability::CapabilityEnforcer::{new,grant,check}` (L1), `skills_lock::{content_hash,write_entry,verify}` (B) — all exist before wiring.
- [x] Deliverable + 17-test plan stated; the §15c/§15d carrier ambiguity surfaced in the red surface (technical-decision-owned per §12, not a user-domain fork).
- [x] `git status` clean except untracked `.claude/scheduled_tasks.lock` (harness artifact; never staged).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Orientation | Phase-doc C.3.1 pseudocode treats `art.compatible_os` as if every artifact kind carries §15c/§15d; but those + `share_provenance` are **framework-schema** fields and the skills-lock `ArtifactKind` enum has no `framework` variant. Resolved as an owned technical decision (§12): generic metadata extraction off the imported JSON with schema defaults — kept `validate` kind-typed for skill/tool/mcp_server, lenient (identity+metadata) for agent (agent-graph deferred to `framework_loader` at load). Documented in the module doc + commit + retro. | C.3.3 could note that §15c/§15d/`share_provenance` are framework-level fields read generically off the imported JSON (absent → schema default), not per-kind-schema fields. | 2 |
| `cargo test` (first green run) | `file_import_locks_source_as_file_shape` (Tier::Novice, full pipeline) expected install success, but `tier_gate(Novice)` (standalone, pinned by `tier_gate_novice_requires_review_promoted_passes`) returns `Err(TierReviewRequired)`. The two committed contract tests jointly require: seam always installs+hash-locks (M07.B integrity model); `tier_gate` is the gate the **Tauri command / Stage E renderer** applies. Resolved by removing the in-seam `tier_gate?` propagation (the renderer drives confirm-before-use, phase-doc E.3.4 "renderer renders the outcome"). | C.3.1's `tier_gate(&art, tier)?;` is misleading — it should state the seam installs and the **command/renderer** applies the L4 review (the `?` would block the Novice install the spec wants reproducibly locked). | 2 |
| Gate: `cargo clippy -D warnings` | `clippy::too_long_first_doc_paragraph` ×5 on the new module/function docs (mod.rs, fetch.rs) — 2 self-correction passes (function docs, then the module/`//!` + lib.rs doc). Within the 3-budget. | A note that new module/function docs need a ≤1-line summary paragraph then a blank `///` (clippy 1.95 `too_long_first_doc_paragraph`) would have pre-empted both passes. | 2 |
| Gate: `cargo doc -D` | `rustdoc::broken_intra_doc_links` on the `//!` module-doc + lib.rs `[`X`]` links (the exact M07.B friction #5 recurrence) — converted to plain code spans (codebase style for module-level cross-refs). 1 pass. | This is M07.B's recorded gotcha-candidate; it had not yet graduated to §15 / a `<read_first>` note, so it recurred verbatim. | 2 |
| Gate: `cargo clippy -D warnings` (test target) | 4 mechanical clippy on the red test file (doc-backticks ×2, `needless_pass_by_value`, `significant_drop_tightening`) → routed to the labelled `style(M07.C)` follow-up so the impl commit's `**/tests/**` diff stays EMPTY (M07.B/M06.D precedent). | n/a — standard; the protocol handles it exactly. | 1 |
| Gate: final workspace `clippy -D warnings` | One production `needless_pass_by_value` (`import_err_to_cmd` in src-tauri) missed by the scoped step-2 autofix (it was in the `--all-targets` set, not the scoped `-p` lib pass). Folded into the impl commit via a clean soft-reset/amend (nothing pushed) so history stayed red→impl→style. | Step-2 `clippy --fix` should scope `-p runtime-main -p agent-runtime --all-targets`, not just `--lib`, so src-tauri command lints autofix in the same pass. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase-doc C.3.1 (`art.compatible_os`, every kind) vs `schemas/*` (only `framework.v1.json` has §15c/§15d) vs skills-lock `ArtifactKind` (no `framework`) | Whether `validate` strict-types every kind and where §15c metadata lives | Owned technical decision (§12 — API shape, not a durable user-VCS wire key like M07.B's `installed`): generic metadata extraction + schema defaults; kind-typed gate for skill/tool/mcp, identity-gate for agent (agent graph = `framework_loader` at load). Documented in the module doc, impl commit body, and red surface. | Phase-doc C.3.3 wording (future-pass, orchestration-session-owned per §8 — NOT this build session; M06/M07.B grandfathering precedent) |
| Phase-doc C.3.1 `tier_gate(&art, tier)?;` vs the two committed tier tests (standalone `tier_gate(Novice)`→Err **and** full-pipeline `Novice`→installs) | Whether the backend seam blocks a Novice import | Seam always installs+hash-locks (M07.B integrity); `tier_gate` is applied by the Tauri command / Stage E renderer (E.3.4). `tier_gate` stays public + contract-tested; documented as the E-consumed helper (the M05 callable-but-unwired-until-consumer-stage precedent). | Phase-doc C.3.1 (same future-pass channel) |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After red/test commit `3a586be` (amended for an accurate 17-count) | Red-phase surface: cross-machine state, right-reason-red evidence (`could not find import in runtime_main`), the reuse table, the §15c/§15d generic-extraction technical decision (flagged for transparency, not escalated), the dependency-inversion cycle finding, the new coverage exclusion + four-mirror plan, the strict-invariant pre-statement | Yes — the user approved ("approved") decisively | Yes — strict-TDD `surface_for_red_approval`, before any production code |

---

## [LIVE] Protocol drift events

None observed. Red/test commit made locally per the v1.8 strict-TDD two-commit protocol, surfaced for red approval, nothing pushed. Mechanical test-file clippy/fmt correctly went to the separate labelled `style(M07.C)` commit so the impl commit's `**/tests/**` diff is EMPTY by construction (proven: `git diff 3a586be..849fd0c -- '**/tests/**'` empty). The one production clippy miss was folded into the impl commit via a local soft-reset/amend (nothing pushed) — history stayed clean red→impl→style. No Hard Rule approached. No `--no-verify`; the llvm-cov commands are byte-identical to CLAUDE.md §6 (the regex now includes `src.import.fetch\.rs`, synced across the four mirrors this commit).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| `runtime-mcp` depends on `runtime-main` (M06.C audit), so the "reuse the M06 registry" instruction cannot be a direct dependency | The phase-doc C.3.1 `reg: &impl McpRegistry` already implied a seam, but the *reason* (the Cargo cycle) is only visible by reading `runtime-mcp/Cargo.toml` | Not friction — the `sdk::mcp_dispatch` (ADR-0010) dependency-inversion archetype applied verbatim (trait in `runtime-main`, concrete adapter in `src-tauri`). A phase-doc note "MCP-config reuse is dependency-inverted (cycle) — concrete adapter in the shell" would make the seam's necessity explicit up-front. |
| `drone_reconnect_events` ×2 fail multi-threaded but pass `--test-threads=1` | The known gotcha #56 / TD-005 Windows-local subprocess flake — not a Stage C regression (C touches no drone IPC) | Already a documented gotcha; CI handles it. Recorded here so the surface's "gates green" claim is honest about the Windows-local-only flake. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §12 "own technical decisions / escalate user-domain forks" drove the correct calls (generic-metadata + tier_gate-in-command owned; nothing escalated since none was user-domain); §2 informed surfacing-for-transparency |
| 2 | Milestone prompt "Read first" oriented correctly? | 4 | Yes; C.3.1/C.3.3 pseudocode were the two defects the read-list could not pre-empt (owned, documented) |
| 3 | Deliverable + test plan before code per §16? | 5 | 17-test plan + the reuse map stated before any red test; the API contract designed against the real reuse surfaces |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | doc-paragraph (2) + intra-doc (1) + test-file clippy (mechanical, follow-up) + 1 prod clippy miss (folded) — all within budget; no thrash |
| 5 | Do-not-commit-until-approved held? | 5 | Red local + surfaced + user-approved; impl/style local only; nothing pushed; final push gated on approval |
| 6 | Escalated right things, proceeded on right things? | 5 | Surfaced the §15c/§15d technical decision for transparency without over-escalating (it is API shape, not a user-domain fork — the M07.B `installed` contrast applied correctly) |
| 7 | Human-direction load reasonable? | 5 | One red-approval interaction |
| 8 | Surfaces actionable? | 5 | Red surface carried the reuse table, the cycle finding, the technical decision, the four-mirror plan, and the strict-invariant pre-statement; approved in one word |
| **Process axis total** | | **39/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Code matches §9 style + naming? | 5 | Mirrors the `sdk::mcp_dispatch` dependency-inversion archetype + the `*_with` seam archetype; module-level cross-refs in the codebase's plain-code-span style; no comments-as-what |
| 2 | Behaviour tests, not tautology (§5)? | 5 | 17 tests assert observable contracts (capability-denied blocks the GET, §15c blocks before L3 via `PanicSandbox`, lock `source` discriminated shape, locked hash verifies fetched bytes, MCP-config lands in the injected registry, provenance `rebake_changes==[]`) through the public seam + schema-shaped JSON — not typify internals |
| 3 | Public APIs documented w/ compile-checked examples? | 4 | All public items + `# Errors` documented; no doctest examples (the integration suite is the executable contract — consistent with `skills_lock`/`tier`/`audit` precedent) |
| 4 | Deliverables exactly promised — no creep/cuts? | 5 | Pipeline + fetch holdout + share_provenance + the `import_artifact` shell command + four-mirror sync exactly; no schema/ADR needed (ADR-0005 fields already in `framework.v1.json`); renderer/agent-loop correctly NOT touched |
| 5 | Stranger-understandable without the spec? | 5 | Module doc states the reuse-not-rebuild composition + the cycle inversion + the §15c-before-L3 ordering + the v0.1 runtime-to-runtime-only provenance posture inline |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no stringly-typed wire (kind/tier mapped explicitly), no ad-hoc `#[allow]`; `chrono` is in-tree (runtime-core), deny-clean, type-matches the generated `installed_at` |
| 7 | Coverage ≥80 / ≥95 — measured? | 4 | runtime-main **96.65% line ≥ 95** (CI-parity command, exit 0, gotcha #81 clean run); workspace ≥80 (below); `import/fetch.rs` correctly excluded; `import/mod.rs` 94.77% line sits within the passing aggregate (parallel to `event_pipeline.rs` 89.25% / `skills_lock` 98.15% — the gate is the runtime-main aggregate, not per-module) |
| 8 | CI green across OS × toolchain? | 4 | All local gates green incl. CI-parity llvm-cov; `drone_reconnect_events` ×2 is the known gotcha #56 Windows-local-only multi-thread flake (passes `--test-threads=1`; not a C regression — C touches no drone IPC); CI prediction strong; not yet observed on CI |
| **Product axis total** | | **37/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | `<tdd_discipline strict>` + `<pre_flight_check reuse_surfaces>` + the reuse `<gotchas>` traps were exactly the right tools; the §12 own-vs-escalate split fit the two pseudocode defects |
| 2 | Sections needed but missing? | 4 | A `<wire_trace_vs_adr_reconcile>`-class slot pinning "MCP-config reuse is dependency-inverted (the runtime-mcp→runtime-main cycle) — concrete adapter in the shell" would have pre-stated the surprise |
| 3 | Gotchas local or generalize to §15? | 3 | Two RECURRENCES that should already be in §15: (a) `rustdoc::broken_intra_doc_links` on module-level `//!`/lib.rs cross-refs ⇒ plain code spans (M07.B gotcha-candidate, never graduated → recurred verbatim); (b) clippy 1.95 `too_long_first_doc_paragraph` on new module/fn docs. Both belong in §15 / a stage `<read_first>` note now. |
| 4 | Time-box matched (within 2×)? | 5 | ~5 h vs 7–9 h estimate (0.6×, in band) |
| 5 | Implicit protocol moments? | 4 | The "two committed tests jointly pin a contract the phase-doc pseudocode contradicts → make the impl satisfy both, don't edit tests" move (strict-TDD) is worth an explicit protocol example |
| 6 | Re-runnable by a fresh session → comparable? | 5 | Yes — the reuse surfaces + the cycle + the two pseudocode tensions are deterministic; a fresh session reading the same files reaches the same owned decisions |
| 7 | Copy format for remaining milestones — change anything? | 4 | Step-2 `clippy --fix` should scope all touched crates `--all-targets` (the src-tauri prod-lint slipped past a `--lib`-only autofix) |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `3a586be` surfaced + user-approved; impl `849fd0c` + style `724e4cb` committed locally only; nothing pushed; final push gated on approval.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — none; the prod-clippy-miss was folded cleanly with no pushed history.
- [x] **G4: stage completed** — all C.4 acceptance criteria met (URL+file fetch, schema-invalid reject, L3-fail block, tier review, install→lock, §15c OsMismatch-before-L3, share_provenance round-trip, MCP-config→M06 registry); the four-mirror sync done this commit; strict two-commit invariant proven.
- [x] **G5: every axis row ≥3** — lowest individual score is 3 (Axis 3 #3, the two recurring gotchas).
- [x] **G6: CI-parity** — runtime-main llvm-cov run with the **exact** CLAUDE.md §6 command (regex byte-identical incl. the new `|src.import.fetch\.rs`, `--fail-under-lines 95`) → 96.65% exit 0; workspace gate `src.main\.rs|generated` `--fail-under-lines 80`; `cargo llvm-cov clean --workspace` first (gotcha #81). `cargo fmt --all -- --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo doc` clean; `cargo audit`/`cargo deny` pass (warnings pre-existing, unrelated to in-tree `chrono`). One CI-divergent observation cited: `drone_reconnect_events` ×2 is the gotcha #56 Windows-local multi-thread subprocess flake (passes `--test-threads=1`; CI's Linux job + the TD-005 shared-fixture handle it) — NOT a Stage C regression and NOT a divergent flag used to claim green (the runtime-main llvm-cov gate ran to a clean exit-0 measurement).

### Soft gates

- [x] **S1:** Process ≥30/40 — 39
- [x] **S2:** Product ≥32/40 — 37
- [x] **S3:** Pattern ≥25/35 — 30
- [x] **S4:** Time-box within 2× — ~5 h vs 7–9 h (0.6×)
- [x] **S5:** ≤3 Severity-3 friction — 0 (max severity 2)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage D1 with the Decisions applied (esp. graduating the two recurring gotchas to §15).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** **91.40% line** (CI-parity command, exit 0).
- **Exclusions added this stage:** None. Regex unchanged: `src.main\.rs|generated`.

### Per-package gate — runtime-main (≥95% line)

- **Coverage actual this stage:** **96.65% line** (CI-parity command, exit 0; `cargo llvm-cov clean` first per gotcha #81).
- **Exclusions added this stage:** `crates/runtime-main/src/import/fetch.rs` — **Category 3 (seam-vs-wrapper / OS-call holdout)**: the real `reqwest` artifact GET (Hard Rule 4: only the user-supplied URL is hit). Seam-tested via `fetch_with` + the injected `Fetcher`; `HttpFetcher` is behaviourally smoke-tested against a local `wiremock` server (no live network in the gate); the `--features integration` live smoke is the optional real-endpoint check — the `providers/anthropic.rs` precedent exactly.
- **Regex as of stage end (byte-identical to CLAUDE.md §6 + `docs/coverage-policy.md` §A):** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs`
- **Four-mirror sync done this commit:** CLAUDE.md §6 command + `docs/coverage-policy.md` §A category-3 list + §A table row + a §C M07.C entry. CLAUDE.md §5 (names categories generically, not files) and `codecov.yml` (global `ignore:` = generated/main/build/sandbox-OS only; runtime-main per-file holdouts are enforced by the §6 absolute-floor command, NOT codecov's ignore — Codecov's `project.runtime-main` flag legitimately still counts the wiremock-covered file) need **no change** — exactly the `providers/anthropic.rs` precedent (documented in the §C entry). G's `<coverage_policy_reconciliation>` verifies.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `import/mod.rs` | (new) | 94.77% line | new | new module; sits within the passing 96.65% runtime-main aggregate (the gate is the aggregate, parallel to `event_pipeline.rs` 89.25% / `recovery/resume.rs` 96.46%). The 15 missed lines are defensive error-mapping branches + the in-source `host_of`/`SystemClock` unit-test arms |
| `import/fetch.rs` | (new) | excluded | new | Category-3 OS-call holdout (above) — verified absent from the gated report |
| all prior runtime-main modules | ≥ M07.A | unchanged | → | no prior module regressed |

### CI workflow drift check

- [x] `.github/workflows/ci.yml` runtime-main/workspace `--ignore-filename-regex` — the new `|src.import.fetch\.rs` term must also be reflected in the CI workflow's runtime-main llvm-cov + lcov-export steps. **Flagged for the Stage G `<coverage_policy_reconciliation>`** alongside the M07.B `skills_lock` ≥95 gate (the v1.8 four-mirror rule's CI-workflow surface lands at closeout G per the phase-doc workflow step 7; CLAUDE.md §6 + `docs/coverage-policy.md` §A/§C were synced byte-consistently this commit).

---

## [END] Decisions for the next stage

### special_log (required by the stage prompt)

- **Seam/wrapper split:** every pipeline stage is an injected-`&dyn`-seam (`Fetcher`/`NetworkGate`/`Sandbox`/`McpRegistry`/`Clock`) fully unit-tested via `import_artifact_with`; the ONLY OS-call wrapper is `import::fetch::HttpFetcher` (`src.import.fetch.rs`, the new Category-3 exclusion). The `import_artifact` Tauri command is the thin §5 shell holdout (covered by the codecov `tauri-shell` 50% patch gate).
- **New `src.import.fetch.rs` exclusion + four-mirror sync:** done in the impl commit `849fd0c` — CLAUDE.md §6 + `docs/coverage-policy.md` §A (category-3 list + table) + §C (M07.C entry). CLAUDE.md §5 + `codecov.yml` need no change (the documented `providers/anthropic.rs` precedent). CI-workflow `ci.yml` surface flagged for G.
- **Reuse confirmed, nothing rebuilt:** L3 = `runtime-sandbox` via the `Sandbox` seam (real adapter wraps `sandbox_ipc::SandboxClient`); L4 = `tier::Tier`; lock = M07.B `skills_lock::{content_hash,write_entry}`; MCP-config = the M06 `runtime_mcp::Registry` via the `McpRegistry` dependency-inversion seam (concrete adapter in `src-tauri` — `runtime-mcp` depends on `runtime-main`, a direct dep is a Cargo cycle; the `sdk::mcp_dispatch`/ADR-0010 archetype). HitlSeam (ADR-0007) NOT rebuilt and not needed in C — the backend returns the `TierReviewRequired` outcome; the Stage E renderer drives confirm-before-use.
- **share_provenance + §15c pinned:** `export_with_provenance` always sets `rebake_changes: []` (runtime-to-runtime only, no Share It module — ADR-0005), `for_os` mirrors `compatible_os`; round-trip export→`read_share_provenance` asserted. §15c `compatible_os` mismatch is a BLOCKING `OsMismatch` proven to short-circuit BEFORE L3 (the `PanicSandbox` test).
- **Strict v1.8 invariant:** `git diff 3a586be..849fd0c -- '**/tests/**'` EMPTY (proven); mechanical test-file clippy in the separate labelled `style(M07.C)` `724e4cb`. No Co-Authored-By; DCO `-s`; session-URL footer.

### Carry-forward to Stage D1

- `tier_gate` is a public, contract-tested seam **consumed by the Stage E review screen** (E.3.4) — the renderer reads the tier + L3 report and renders the Novice capability-disclosure modal before completing the import. Documented as the M05 callable-but-unwired-until-consumer-stage precedent; D1/E own no change to it.
- The `import_artifact` command + `Arc<Registry>` standalone management are in place; D1's concrete `McpDispatcher` construction in `src-tauri` is independent of the import path (shared only the registry DB).

### `CLAUDE.md` / protocol updates (candidates — apply if substantive)

- [ ] **§15 gotcha (graduate — RECURRED from M07.B):** "Module-level `//!` doc and `lib.rs` `pub mod` doc cross-references must be plain code spans `` `X` ``, never intra-doc links `[`X`]` — `rustdoc::broken_intra_doc_links` denies them outside item-doc context. (Item-level `# Errors`/struct-field `[`Type`]` links resolve fine.)" — was an M07.B gotcha-candidate that never graduated and recurred verbatim.
- [ ] **§15 gotcha:** "clippy 1.95 `too_long_first_doc_paragraph` — a new module/fn doc's FIRST paragraph must be a ≤1-line summary, then a blank `///`/`//!`, then the body."
- [ ] **Gate-ordering note (CLAUDE.md §6 / STAGE-PROMPT-PROTOCOL §10):** step-2 `cargo clippy --fix` should scope **all touched crates** `--all-targets` (e.g. `-p runtime-main -p agent-runtime --all-targets`), not `--lib` only — a src-tauri command `needless_pass_by_value` slipped past a lib-only autofix and only surfaced at the final `--workspace --all-targets` verification.
- [ ] **Phase-doc-authoring note:** stage prompts should pre-state when a "reuse the M0x X" instruction is **dependency-inverted** (a Cargo cycle forces a trait-in-consumer + concrete-adapter-in-shell), and when phase-doc pseudocode `?`-propagates an outcome the committed tests require the seam to NOT block on (C.3.1 `tier_gate?`).

### Milestone document updates

- [ ] M07 phase-doc C.3.1 (`tier_gate(&art, tier)?;` — the seam must NOT block the Novice install; the command/renderer applies L4) + C.3.3 (§15c/§15d are framework-schema fields read generically, not per-kind) are imprecise. **Not edited mid-flight** (§8 phase-doc-edit pre-flight + grandfathering — orchestration-session-owned). Recorded here + the impl-commit body. Stage D1+ reads this `[END]` first.

### Open issues to file

- [ ] None blocking.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M07 Stage C per `CLAUDE.md` §19. User review pending. This stage's commits land on `claude/m07-registry-import`; the PR drafts/pushes only at the end of the final M07 stage.

**Claude:** M07 Stage C complete per the stage prompt. Red/test `3a586be` (user-approved red phase) → impl/green `849fd0c` (zero `**/tests/**` diff vs `3a586be` — strict v1.8 two-commit invariant proven) → mechanical `style(M07.C)` `724e4cb` (test-file clippy, separated per CLAUDE.md §6). Pipeline composes M05 L3/L4 + M07.B `skills.lock` + a capability-gated fetch with zero rebuilds; MCP-config import dependency-inverted around the `runtime-mcp→runtime-main` cycle (ADR-0010 archetype); §15c blocks before L3; ADR-0005 `share_provenance` runtime-to-runtime only. New `src.import.fetch.rs` Category-3 exclusion four-mirror-synced (CI-workflow surface flagged for G). All Rust gates green, CI-parity (runtime-main 96.65% ≥ 95; the `drone_reconnect_events` ×2 is the documented gotcha #56 Windows-local multi-thread flake, not a C regression). I have NOT pushed; surfacing for final approval.

**Surfaced at:** 2026-05-18
