# M01.A — Stage Retrospective

> **Stage:** M01 Stage A — Workspace Skeleton — see `docs/build-prompts/M01-foundation.md`
> **Parent milestone:** M01 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-02
> **Branch:** `claude/m01-foundation`
> **Starting commit:** `1f7942a` (Merge pull request #27)
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 5–8 hours
> **Actual elapsed:** ~2 hours

---

## Pre-flight (claimed before session start)

These were satisfied before code was written:

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response
- [x] All "Read first" files in the milestone prompt were read in the order listed
- [x] The deliverable was stated in 1–3 sentences and the test plan in 3–5 bullets BEFORE writing code
- [x] User confirmed the deliverable + test plan before code began (CLI prompt specified exact steps)
- [x] The branch was correct per the milestone prompt (`claude/m01-foundation`)
- [x] No uncommitted changes carried over from a prior session (`git status` was clean)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| STEP 2, rust-toolchain.toml | Pinned `channel = "1.80"` per template, but Tauri 2.x transitive deps require Rust 1.88+ (edition 2024, newer crate MSRVs). Had to escalate through 1.85 then to `stable`. | Template should state: "Use `channel = \"stable\"` — MSRV enforcement happens via `rust-version` in Cargo.toml, not via toolchain pin. The template's `1.80` was aspirational." | 3 |
| STEP 2, sandbox Cargo.toml | Used `[lints] workspace = true` alongside `[lints.rust] unsafe_code = "warn"` override. Cargo rejects this combination — can't inherit workspace lints AND override individual keys. | Template should note: "Sandbox cannot use `workspace = true` in `[lints]` when overriding `unsafe_code`. Manually replicate all workspace lints in sandbox's Cargo.toml." | 2 |
| STEP 2, Tauri icon | Tauri build on Windows requires `icon.ico`, not just `icon.png`. Build failed with "`icons/icon.ico` not found". | Template should note: "Windows requires `.ico`; include both `.png` and `.ico` in icons list." | 2 |
| STEP 3, cargo deny | `deny.toml` needed additional allowed licenses (`MPL-2.0`, `Unicode-3.0`, `Apache-2.0 WITH LLVM-exception`) for Tauri's transitive deps, and advisory ignores for unmaintained GTK3 crates. | Template's `deny.toml` should include these licenses and advisory ignores from the start since Tauri is a known dependency. | 2 |
| STEP 3, clippy nursery | `clippy::missing_const_for_fn` fired on the placeholder functions. Had to add `const` to all five `placeholder()` functions. | Template `lib.rs` should use `pub const fn` from the start. | 1 |
| STEP 3, cargo doc | Gate specifies `cargo doc --workspace --no-deps -- -D rustdoc::missing_docs` but `rustdoc::missing_docs` is not a valid lint name; the correct lint is `missing_docs` via RUSTDOCFLAGS. Also needed crate-level doc on `src-tauri/src/main.rs`. | Gate definition should use `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps`. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M01-foundation.md A.3 `rust-toolchain.toml` template vs. Tauri 2.x dep requirements | Template says `channel = "1.80"  # MSRV pin` but also says "Update channel to the latest stable as of work-start." Contradictory: is it the MSRV or latest stable? | Used `channel = "stable"` since Tauri deps require 1.88+. MSRV enforcement lives in `rust-version` field. | M01-foundation.md A.3 should clarify: toolchain pin is for dev environment (use stable); MSRV enforcement is via `rust-version` in Cargo.toml. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| End of stage | Full diff stat + gate results + retrospective + draft commit message | Yes | Yes — after all gates pass |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Tauri 2.x has 444 transitive dependencies | Expected a lighter dependency tree for a framework meant to use the OS webview | The `cargo deny check` ignore list will need maintenance as Tauri releases; consider tracking in a dedicated section of deny.toml |
| `cargo build --workspace --release` took 3.5 minutes | For placeholder crates with zero logic, this is a lot of compile time (all from Tauri) | Release builds in CI will be slow; should use caching aggressively |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Clear rules, no confusion on process |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | Template had some inaccuracies (toolchain, lints) but overall structure was clear |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated before any file creation |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | All issues resolved in 1 round each |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Toolchain version change was within "proceed" scope (template said to update) |
| 7 | Was the human-direction load reasonable? | 5 | CLI prompt provided all instructions; no questions needed |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 4 | Surfacing at end with all details |
| **Process axis total** | | **38/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case, kebab-case crate names, no magic numbers |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 3 | Tests are trivial by design (placeholder stage); they test observable output but are simple assertions |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 5 | Every placeholder has a doc-test that compiles and passes |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All A.2 files created, no extras |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | Module docs explain purpose; crate names are self-documenting |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers, no `any`, no silent error swallowing |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | N/A | Stage A has no coverage gate (per A.4) |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Verified on Windows only (local); CI will verify cross-OS |
| **Product axis total** | | **32/35** (N/A excluded) | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 4 | All sections used; A.4 Tests section was brief but appropriate for Stage A |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | Could use a "known Tauri quirks" section for platform-specific build requirements |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | Toolchain pin vs MSRV distinction should go in §15 |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 5–8h, actual ~2h. Within 2× (faster than estimated) |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | The deny.toml license/advisory handling for Tauri deps was implicit knowledge |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 4 | Yes, with the friction events noted — the template inaccuracies would cause same issues |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Template `lib.rs` should use `const fn`; toolchain guidance needs fixing |
| **Pattern axis total** | | **29/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* git log shows no new commits since `1f7942a`; working tree has unstaged changes only
- [x] **G2: no Severity-5 friction events** — *evidence:* highest severity was 3 (toolchain pin issue); no blocking events
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift-events table is empty
- [x] **G4: the milestone or stage actually completed** — *evidence:* all A.2 files created; all gates pass (fmt, clippy, test, doc, audit, deny); 5 unit + 5 doc tests green
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest score is 3 (Product axis #2, tests are trivially correct by stage design)

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 38
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 32/35 (adjusted for N/A row)
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 29
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 5–8h, took ~2h, ratio 0.3× (faster)
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 1

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to next stage with minor `CLAUDE.md` updates per Axis 3 notes.

---

## [END] Decisions for the next stage (or next parent milestone if this is the final stage)

### `CLAUDE.md` updates

- [ ] Add gotcha #21: "rust-toolchain.toml `channel` should be `stable` (not MSRV). MSRV enforcement happens via `rust-version` in workspace Cargo.toml. Tauri 2.x transitively requires Rust 1.88+."
- [ ] Add gotcha #22: "Cargo `[lints] workspace = true` cannot coexist with per-crate lint overrides in the same `[lints]` table. Crates that override workspace lints must replicate all workspace lint settings manually."

### `TEMPLATE.md` updates

- [ ] None needed for Stage B's scope.

### Milestone document updates

- [ ] Stage A template `lib.rs` should show `pub const fn` (clippy nursery `missing_const_for_fn`).
- [ ] Stage A `rust-toolchain.toml` guidance should state `channel = "stable"` with note about MSRV.
- [ ] Stage A `deny.toml` should pre-include Tauri-required licenses and advisory ignores.
- [ ] Stage A gate for `cargo doc` should use `RUSTDOCFLAGS="-D missing_docs"` not `-- -D rustdoc::missing_docs`.

### Protocol additions

- [ ] None needed.

### Open issues to file

- [ ] None — all friction was resolved within the session.

---

## [END] User-review notes

> The user reviews this retrospective alongside the PR. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M01 Stage A, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch; the PR drafts and pushes only at the end of the final stage.

**Claude:** I have completed M01 Stage A per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until the final stage.

**Surfaced at:** 2026-05-02
