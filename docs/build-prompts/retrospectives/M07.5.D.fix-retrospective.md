# M07.5.D.fix — Stage Retrospective

> **Stage:** M07.5 Stage `D.fix` — see `docs/build-prompts/M07.5-tier-gate-fix.md`
> **Parent milestone:** M07.5 fix-cycle (work-stage-class D.fix per ADR-0008; the closing re-verification + reconciliation that unblocks M08 Stage A)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-20
> **Session end:** 2026-05-20
> **Branch:** `claude/m07.5-tier-gate-fix`
> **Starting commit:** `d7fe47b` (M07.5.C.fix retrospective)
> **Ending commit:** uncommitted — awaiting approval (the D.fix commit bundles `CHANGELOG.md` + `M07.5-summary.md` + this retro)
> **Estimated effort (per prompt):** 2–3 hours
> **Actual elapsed:** ~1 focused session (gate suite ran in the background)

---

## Pre-flight (claimed before session start)

- [x] `CLAUDE.md` loaded; §4 Hard Rules stated at the top of the first work response
- [x] All `<read_first>` files read in order (CLAUDE.md, STAGE-PROMPT-PROTOCOL v1.8 reference, M07.V retro Finding #1 + the 🟡 #2/#3/#4/#5, ADR-0016, M07.5 phase doc Background + Key constraints + Stage D.fix D.1–D.6, M07.5.A.fix + B.fix + C.fix retros, M06.5-summary, gotchas #23/#81, RETROSPECTIVE-TEMPLATE)
- [x] Deliverable stated in 3 sentences + test plan in 5 bullets before any work
- [x] Branch correct (`claude/m07.5-tier-gate-fix`); pre-flight checks passed — A.fix `51ef6eb` + B.fix `e642843` + C.fix `6178219` impl commits present; ADR-0017 + ADR-0018 both `Status: Accepted`
- [x] `git status` clean at start (only untracked `.claude/scheduled_tasks.lock`, not in scope)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity |
|---|---|---|---|
| verify_gates step, gate run | The Rust canonical gate suite and the frontend canonical gate suite were launched as two **concurrent** background jobs. Two false-negative gate failures resulted, both pure resource contention: (a) `cargo test --workspace`'s `xtask` `drift_detection_tests` failed — the frontend suite's `npm ci` had transiently wiped `node_modules`, so the drift check's `regenerate-types` could not find `node_modules/.bin/json2ts`; (b) Playwright's first 3 `budget_threshold.spec.ts` tests timed out (`page.goto` 60 s, `net::ERR_ABORTED`) — the Vite dev server was CPU-starved by the concurrent Rust llvm-cov instrumentation compile. Both were diagnosed as contention (the same `drift_detection_tests` passed in the later llvm-cov-workspace run @ log line 9211; C.fix's retro recorded all 29 e2e green). One isolated, **sequential** re-run of just the two affected gates → both green (`drift_detection_tests ... ok`; Playwright `29 passed`). | D.fix's full-suite verification should run the Rust gate suite and the frontend gate suite **sequentially, not concurrently** — CI runs them as separate jobs on separate runners, so concurrent local execution introduces a `node_modules`-wipe / CPU-contention failure class CI never sees. Phase-doc D.3/D.4 (and a `CLAUDE.md` §6 note) should say so. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc D.3/D.4 "Manual (the original repros) … on the built app on Windows" vs gotcha #23 | D.3/D.4 mandate a manual GUI repro of 🔴 #1 + CQ-M07-1 against the built app, but gotcha #23 (which the phase doc itself cites) makes a Tauri 2.x window undrivable / unobservable from the agent side — the GUI clicking is a human-only step. | Surfaced the genuine user-domain fork (CLAUDE.md §12): the user chose **defer per the M06.5 precedent** — the three automated assembled-app regression tests are the objective verification of record; the real-app manual GUI re-confirmation is deferred-and-tracked to the post-M07.5 / M08 IRL pass (the M06.5 between-milestone IRL model, maintainer-directed). | The phase doc D.3/D.4 manual-repro bullet should frame the GUI step explicitly as a human-in-the-loop / deferred-and-tracked item from the start (the M06.5.C.fix precedent), not as agent work. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Start of session | The manual-repro fork — defer per the M06.5 precedent vs the user runs the GUI repro now — surfaced as a 2-option `AskUserQuestion` while the gate suites ran in the background. | Yes — a labelled user-domain decision with the precedent named; the user picked "defer." | Yes — asked up front so the retro/summary could record the chosen disposition; parallelised against the long gate run. |
| End of stage (this surface) | Final surface — cross-machine state, the full canonical gate suite result (incl. the contention diagnosis + clean re-run), the 🔴 #1 + CQ-M07-1 closure record, the M08.A unblock statement, retro + summary. | — | — |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| — | None observed. No code committed; nothing pushed (surfacing for approval per `CLAUDE.md` §8). No gate skipped, no `--no-verify`. The two contention false-negatives were cleared by an isolated re-run of the **identical** CI gate commands — not by any CI-divergent flag (no `--test-threads`, no `--skip`, no `--features` override). No `docs/gap-analysis.md` entry added (CLAUDE.md §20 — fix-cycle). The M07.V 🟡 carry-forwards + the Reinstall round-trip were not touched. | n/a | — |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Running the two gate suites concurrently produced **two unrelated** false-negative test failures — an `xtask` schema-drift test and three M04-era Playwright tests — neither touching any M07.5 code path. | The contention surface was wider than expected: it was not just CPU starvation (the Playwright timeouts) but also a shared-filesystem hazard (`npm ci` wiping `node_modules` mid-`cargo test`, breaking the Rust-side `xtask` drift check that shells out to `json2ts`). The Rust and frontend gate suites are not as independent on one machine as their separate-CI-jobs structure implies. | A verification stage that runs "the full canonical gate suite" must treat the Rust and frontend suites as mutually exclusive on a single machine. Worth a `CLAUDE.md` §6 / gotcha note: run them sequentially. |

---

## [SPECIAL LOG] Stage-required confirmations (per `<retrospective_requirements>`)

- **The three regression tests are GREEN in the canonical CI suite — confirmed (named + job).**
  - `reject_rolls_back_lock_and_registry` (A.fix) — Rust, `crates/runtime-main/tests/import_pipeline_integration.rs`; runs in the **`cargo test --workspace`** gate (the runtime-main integration-test job — `import_pipeline_integration.rs`: `test result: ok. 44 passed; 0 failed`). `... ok`.
  - `redirect_to_private_address_is_blocked` (B.fix) — Rust, same file / same gate / same job. `... ok`.
  - `reject_invokes_cancel_pending_import` (C.fix) — TypeScript/vitest, `tests/unit/components/ImportPanel.test.tsx`; runs in the **`npm run test`** gate (vitest — `✓ ImportPanel > reject_invokes_cancel_pending_import 358 ms`; suite: 31 files / 350 tests passed).
- **🔴 #1 + CQ-M07-1 manual repro — before/after, verbatim disposition.** The real-app manual GUI repro is **agent-blocked per gotcha #23** — a Tauri 2.x window cannot be driven or observed from the agent side; the GUI clicking (set tier → paste URL → click Reject/Install; paste `http://` + private-address URLs) is a human-only step. Per the user's decision at the start of this session, it is **deferred-and-tracked** to the post-M07.5 / M08 IRL pass (the M06.5 between-milestone IRL precedent — M06.5's real-app 🔴 re-confirmation was likewise deferred, maintainer-directed). **No verbatim before/after GUI observations exist for D.fix** because no GUI repro was run; the objective evidence the 🔴 cards turn on — a Novice import leaves no `skills.lock` entry / no MCP registry row, and an `http://` or private-address URL is egress-blocked — is asserted automatically by the three assembled-app regression tests above, which exercise the real composition (`import_artifact_with` and the real `ImportPanel` component), the durable verification of record.
- **M08.A carry-forward — stated explicitly.** 🔴 #1 + CQ-M07-1 are RESOLVED and re-tested green in the assembled app → **M08 Stage A is unblocked**. M07.V 🟡 #2 (`skills_lock::verify` no production caller), 🟡 #3 (`McpDispatcher::on_server_connected` no production caller), 🟡 #4 (local-file picker UI), 🟡 #5 (agent-with-tools production driver) — the coupled Dec-6 set — **still carry to M08 Stage A unchanged** (M07.5 did not touch them). The **Reinstall source round-trip** also **carries to M08** (M07.5 was scoped to the 🔴 + the egress hardening). ADR-0016 §Consequences requires M08.A's `<read_prior_milestones>` to reference the ADR-0016 waiver, the M07.V retrospective, and this M07.5 outcome.
- **M07.5 added no `docs/gap-analysis.md` entry — confirmed.** Per `CLAUDE.md` §20 and ADR-0016, this is a fix-cycle (work-stage-class D.fix), not a parent milestone — the 🔴 #1 + CQ-M07-1 resolutions flow into **M08's** gap-analysis Carry-forward section. `git status` / `git diff` show `docs/gap-analysis.md` untouched this cycle.
- **ADR-0017 + ADR-0018 — both Accepted (verified, not re-touched).** ADR-0017 was flipped `Proposed → Accepted` in the A.fix impl commit (`51ef6eb`); ADR-0018 in the B.fix impl commit (`e642843`). D.fix only verified both show `Status: Accepted` — it did not re-touch either.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation? | 5 | §4/§6/§8/§12/§20 drove the gate ordering, the do-not-commit posture, the user-domain surface, and the no-gap-analysis-entry rule. |
| 2 | Did the `<read_first>` list orient correctly? | 5 | The M07.5 phase doc D.1–D.6 + ADR-0016 + the A/B/C.fix retros fully specified the verification + reconciliation. |
| 3 | Deliverable + test plan stated before work (§16)? | 5 | 3-sentence deliverable + 5-bullet test plan stated; pre-flight checks run. |
| 4 | Self-correct in ≤3 rounds (§7)? | 4 | The self-inflicted concurrent-gate-suite contention produced two false negatives; diagnosed correctly first time, one isolated sequential re-run cleared both. Within budget, but the round was avoidable. |
| 5 | Do-not-commit-until-approved held? | 5 | Nothing committed, nothing pushed; surfacing for approval. |
| 6 | Escalated/proceeded correctly? | 5 | Surfaced the manual-repro fork as a user-domain decision (§12); proceeded autonomously on the gate re-run (technical). |
| 7 | Human-direction load reasonable? | 5 | One question (the manual-repro disposition). |
| 8 | Surfaces actionable? | 5 | The manual-repro question named the precedent + both options; the final surface carries the contention diagnosis + clean re-run evidence. |
| **Process axis total** | | **39/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Code matches `CLAUDE.md` §9 style/naming? | 5 | No code this stage; the `CHANGELOG` / summary / retro follow the established M07.5 documentation style. |
| 2 | Behavior tests, not tautology (§5)? | 5 | No new tests. The "test" is the assembled-app re-verification; the three A/B/C.fix regression tests it re-confirms are themselves assembled-composition behavior tests (gotcha #82). |
| 3 | Public APIs documented? | 5 | N/A — docs/verification stage, no public API. `cargo doc -D missing_docs` re-confirmed clean on the branch tip. |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | D.2/D.3 delivered: `CHANGELOG` consolidated, `M07.5-summary.md` + this retro written. No 🟡 / Reinstall / gap-analysis touched. |
| 5 | Stranger-readable? | 5 | The summary + the `CHANGELOG` D.fix entry explain the closure, the verification of record, and the M08.A carry-forward standalone. |
| 6 | §9 anti-patterns absent? | 5 | None. |
| 7 | Coverage measured ≥ gate? | 5 | All 5 `cargo llvm-cov` gates green (workspace 92.39%, drone 95.73%, main 96.72%, sandbox 96.11%, mcp 95.83%); vitest 350/350. Measured, identical to the B.fix/C.fix tip — D.fix introduced no code, hence no coverage drift. |
| 8 | CI green predicted across the matrix? | 5 | All gates green with CI-parity commands; the contention false-negatives are a single-machine artifact CI's separate-job model does not see (cross-OS predicted green — no OS-specific code). |
| **Product axis total** | | **40/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all template sections useful? | 4 | All used; the Rust-oriented "Coverage holdouts" section needed a "no change this stage" adaptation for a docs/verification stage (the C.fix retro flagged the same for a renderer stage). |
| 2 | Sections needed but missing? | 4 | A verification-stage retro variant (gate-result table + the assembled-regression re-confirmation, in place of the work-axis Product section) would fit a D.fix-class stage better. |
| 3 | Gotchas local or generalize? | 3 | "Run the Rust and frontend canonical gate suites sequentially, not concurrently — `npm ci` wipes `node_modules` mid-`cargo test` and CPU contention times out Playwright" generalizes to every full-suite verification stage — candidate `CLAUDE.md` §6 / §15 note. |
| 4 | Time-box matched (within 2×)? | 5 | Within the 2–3 h estimate (the long gate suite ran in the background). |
| 5 | Implicit protocol moments? | 3 | The concurrent-gate-suite contention class is an implicit trap the phase doc did not warn about; the manual-repro-vs-gotcha-#23 fork should be pre-framed as deferred-and-tracked. |
| 6 | Re-runnable by a fresh session? | 5 | The phase doc D.1–D.6 fully specifies D.fix; the verification is deterministic given the sequential-gate-run lesson. |
| 7 | Anything to change before reusing the format? | 4 | Add the sequential-gate-suite note; pre-frame the manual GUI repro as a human-in-the-loop / deferred item. |
| **Pattern axis total** | | **28/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — no commit, no push; the D.fix commit (`CHANGELOG` + summary + retro) is drafted and surfaced for approval.
- [x] **G2: no Severity-5 friction** — max severity 2 (the self-inflicted concurrent-gate-suite contention, cleanly diagnosed + cleared by one isolated re-run).
- [x] **G3: no unaddressed protocol drift** — drift table empty.
- [x] **G4: stage completed** — `CHANGELOG.md` consolidated (C.fix + D.fix sections); `M07.5-summary.md` + this retro written; all D.4 acceptance criteria met; full canonical gate suite green.
- [x] **G5: every axis row ≥3** — lowest rows are 3 (Pattern #3, #5); none below.
- [x] **G6: CI-parity** — every gate command run verbatim from `CLAUDE.md` §6: `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `cargo test --workspace --doc`; `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps`; `cargo audit`; `cargo deny check`; `cargo llvm-cov clean --workspace` then the 5 `cargo llvm-cov` gates with the byte-identical §6 `--ignore-filename-regex` sets; `npm ci`; `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'`; `npx eslint .`; `npx tsc --noEmit`; `npm run test`; `npm audit --audit-level=high`; `npm run test:e2e`. **No divergent flags.** The two contention false-negatives (`cargo test`'s `drift_detection_tests`; 3 Playwright `budget_threshold.spec.ts` timeouts) were cleared by an isolated re-run of the **identical** commands once the suites were not contending — this is a local-environment fix (run the suites sequentially), **not** a CI-divergent flag and **not** a CI-parity break. `cargo llvm-cov clean --workspace` was run before the coverage gates (gotcha #81).

### Soft gates

- [x] **S1:** Process 39/40 ≥30
- [x] **S2:** Product 40/40 ≥32
- [x] **S3:** Pattern 28/35 ≥25
- [x] **S4:** Time-box within 2× (within the 2–3 h estimate)
- [x] **S5:** ≤3 Severity-3 friction events — 0 (one Severity-2 event)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. M07.5 is complete — surface the fix-cycle PR for approval.

---

## [END] Coverage holdouts

D.fix changes no production code — no `crates/` / `src-tauri/` / `src/` source file is touched. The coverage gates were re-run as part of the canonical-suite re-verification; the numbers are identical to the B.fix / C.fix branch tip (no code = no drift).

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 92.39% line (re-confirmed; = B.fix tip)
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs`, `generated`

### Per-package gates (≥95% line)

- **`runtime-drone`:** 95.73% — exclusions unchanged (`src.main\.rs`, `generated`, `src.lib\.rs`, `src.shutdown\.rs`).
- **`runtime-main`:** 96.72% — exclusions unchanged (`src.main\.rs`, `generated`, `src.providers.anthropic\.rs`, `src.drone_ipc.connection\.rs`, `src.sandbox_ipc.connection\.rs`, `src.import.fetch\.rs`).
- **`runtime-sandbox`:** 96.11% — exclusions unchanged (`src.main\.rs`, `generated`, `src.lib\.rs`, `src.seccomp\.rs`, `src.landlock\.rs`).
- **`runtime-mcp`:** 95.83% — exclusions unchanged (`src.main\.rs`, `generated`, `src.lib\.rs`, `src.transport.stdio\.rs`, `src.transport.http\.rs`, `src.client.auth_keyring\.rs`, `src.client.lifecycle\.rs`).

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| (none) | — | — | — | D.fix touches no source file; no module coverage moved. |

### CI workflow drift check

- [x] No `--ignore-filename-regex` / `codecov.yml` / `ci.yml` change this stage. No `docs/coverage-policy.md` entry needed (no threshold or exclusion moved). The renderer is not under a `cargo llvm-cov` gate; vitest threshold (≥80, `vitest.config.ts`) re-confirmed green.

---

## [END] Decisions for the next stage (M08 Stage A)

### `CLAUDE.md` updates

- [ ] Candidate `CLAUDE.md` §6 / §15 note: a verification stage that runs "the full canonical gate suite" on one machine must run the Rust gate suite and the frontend gate suite **sequentially, not concurrently** — `npm ci` wipes `node_modules` mid-`cargo test` (breaking the `xtask` drift check that shells out to `json2ts`), and the Rust llvm-cov instrumentation compile CPU-starves the Playwright Vite dev server. CI does not see this class (separate jobs / runners).
- [ ] Carries the standing A.fix/B.fix/C.fix candidate forward: "Phase-doc illustrative code blocks are not gate-verified." (No code this stage; recorded for the next phase-doc author.)

### Phase-doc / protocol notes for M08.A

- [ ] **M08.A `<read_prior_milestones>` must reference** (ADR-0016 §Consequences): ADR-0016 (the waiver), the M07.V retrospective, and this M07.5 outcome (`M07.5-summary.md`).
- [ ] **🔴 #1 + CQ-M07-1 RESOLVED → M08.A unblocked.** Per `CLAUDE.md` §20 the resolutions flow into M08's gap-analysis Carry-forward section — M07.5 added no gap-analysis entry.
- [ ] **M07.V 🟡 #2 / #3 / #4 / #5 carry to M08.A unchanged** — the coupled Dec-6 set (skills_lock verify-on-load; `McpDispatcher::on_server_connected`; local-file picker UI; agent-with-tools production driver), all converging on the M08 Builder Canvas Test/Run path.
- [ ] **The Reinstall source round-trip carries to M08** — `Installed` does not round-trip the original `ImportSource`; the M07 gap-analysis listed it as "Target: M07.5 *or* M08" and M07.5 was scoped out of it.
- [ ] The D.fix manual GUI repro of 🔴 #1 + CQ-M07-1 is **deferred-and-tracked** to the post-M07.5 / M08 IRL pass (gotcha #23; the M06.5 between-milestone IRL precedent).

### Open issues to file

- [ ] None blocking. The four M07.V 🟡 carry-forwards + the Reinstall round-trip are M08 scope (untouched, as scoped).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M07.5 Stage `D.fix`, authored per `CLAUDE.md` §19. User review and approval pending.

**Claude:** I have completed M07.5 Stage D.fix per the stage prompt. The retrospective above reflects my honest self-assessment. The D.fix changes (`CHANGELOG.md` consolidation + `M07.5-summary.md` + this retro) are staged on `claude/m07.5-tier-gate-fix` locally and uncommitted; nothing is pushed. I am surfacing this retrospective + the summary + the gate results for final approval. I will not push until you approve.

**Surfaced at:** 2026-05-20
