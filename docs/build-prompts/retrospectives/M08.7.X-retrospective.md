<!--
Protocol-iteration retrospective (mirrors M05.6 / M06.6 shape).
Single-stage, docs/process only. No gap-analysis entry (§20).
-->

# M08.7.X — Process-Hardening Retrospective (standing execution-observability)

> **Stage:** M08.7.X — go-forward process-hardening (items 1–5), single-stage, docs/process only
> **Parent milestone:** standing change M08.7.B-forward → v0.1 ship (M08.6.7 / M09 / M10 / M11)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-31
> **Session end:** 2026-05-31
> **Branch:** `claude/nice-fermat-IrQfH`
> **Starting commit:** `d563f97`
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** assess + classify + author (single stage)
> **Actual elapsed:** one session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules stated at the top of the first response (incl. rule 11 as the cardinal rule of this session)
- [x] All `<read_first>` files read (two referenced files do not exist — see Ambiguity #1)
- [x] Deliverable stated as the five dispositions as the read grounded each one (test_plan_required=false)
- [~] User confirmed before code: n/a — this surface IS the confirmation point; only additive items 1+5 were written, escalations were NOT applied pending decision
- [x] Branch correct (`claude/nice-fermat-IrQfH`)
- [x] `git status` clean at start

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| assess_current_state | `<read_first>` listed `docs/methodology-delta.md` + `RUNTIME-DELTA.md`; neither exists (ORCHESTRATOR §9 calls them externally-maintained delta logs). Located the thread in ORCHESTRATOR §9 + `docs/proposals/` + DESIGN.md instead. | Note that the starter-kit delta logs are external/not-yet-in-repo, so ITEM 5's "methodology-delta entry" cannot be a same-repo edit. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `<read_first>` vs repo | Two referenced files absent | Confirmed via `ls`/grep; routed ITEM 5's delta-entry to the surfaced dependency (do not self-author the external log) | This retro + the surface |
| M08.V Decisions 1/2 (→M9.A) vs ORCHESTRATOR §9 (→M08.7 rungs 6/7) | Which routing for `plan_loop` + `vdr` is authoritative | Did NOT pick (ORCHESTRATOR §3 / CLAUDE.md §2) — escalated as ITEM 3 | maintainer decision (ITEM 3) |
| ORCHESTRATOR §9 ("vdr → rung 6") vs M08.7 rung table (rung 6 = sequential spawn; vdr absent) | `vdr` has no concrete ladder home | Surfaced as a NEW contradiction inside ITEM 3 | maintainer decision (ITEM 3) |
| CLAUDE.md §5 (mutation "advisory") vs cluster-pattern.md §5 + `<close_gate><mutation_gate>` + rule 11 line 97 ("blocks close") | Is the mutation gate advisory or blocking | Surfaced as ITEM 4 posture/internal-contradiction; recommended scoped resolution | maintainer decision (ITEM 4) |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| session end | Per-item disposition table + four escalation write-ups (items 2/3/4 + item-5 now-call) with recommendations + the additive diff (items 1+5) | Yes — each escalation states the quoted accepted-decision text, the contradiction, and a recommended resolution | Yes — escalations surfaced BEFORE landing (the cardinal rule) |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §4 rule 11 (grounded-claims) | Every "absent" claim was backed by a grep/read cite before asserting it | n/a — held | No "checked, it's fine" — verbatim cites for each disposition |
| Additive-or-skip-or-escalate | No accepted decision silently re-routed; items 2/3/4 NOT applied | n/a — held | The four watches were treated item-(c)-by-default; only items 1+5-rule landed |

**None observed** beyond the held-line entries above.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| ITEM 4's BLOCKING mutation gate ALREADY exists in three places (`<close_gate><mutation_gate>`, cluster-pattern §5, rule-11 line 97) — only CLAUDE.md §5's standalone "advisory" subsection is unreconciled | The watch framed it as "advisory→blocking posture change"; the read found the blocking posture is already adopted everywhere except §5 | ITEM 4 is narrower than framed: it is a §5-wording reconciliation, not a net-new gate |
| ITEM 3's re-route to M08.7 has ALREADY happened in ORCHESTRATOR §9 (zero-propagation) — so the watch's "you'd be re-routing M9.A" partly dissolved, but the §9 re-route then CONTRADICTS the still-standing M08.V Decisions AND mislabels vdr's rung | The re-route created a doc-disagreement rather than resolving the carry-forward cleanly | zero-propagation re-routes must update the source carry-forward (M08.V/gap-analysis), not just §9 |
| `docs/cluster-pattern.md` "Status of this doc's enforcement" section is STALE (lists the v1.9 fold-in / Stage-V 5th pass / Stryker spec as "Not yet done" though §9 + protocol v1.9 show Phase 0a CLOSED) | Adjacent drift, not one of the five items | Flagged, NOT fixed (scope lock) — candidate for the next cluster-pattern touch |

---

## [LIVE] Stage 0 real-app discovery routing

n/a — no real-app surface (process/docs only; no `tests/e2e-tauri/` touch; no execution code).

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4.11 + §12 + §20 drove the additive-or-skip-or-escalate contract directly |
| 2 | `<read_first>` correctly oriented? | 4 | Yes, minus two dangling file refs (Friction #1) |
| 3 | Deliverable stated before edits? | 5 | Each disposition grounded by a cite before authoring |
| 4 | Self-correct in ≤3 rounds? | 5 | No self-correction needed; validator PASS first run |
| 5 | Do-not-commit-until-approved held? | 5 | Nothing committed; escalations NOT applied |

### Axis 2: Product (process artifacts)

| Item | Score | Evidence |
|---|---|---|
| Additive artifacts correct + forward-shaped | 5 | ITEM 1 ledger persists-through-ship; ITEM 5 reads "every UI-bearing milestone…"; both cite-grounded |
| Escalations honest + actionable | 5 | Quoted accepted-decision text + contradiction + recommendation each |

### Axis 3: Friction signal

| Score | Note |
|---|---|
| 5 | One severity-2 friction (dangling read-first refs); clean otherwise |

**Outcome: Sound.** Additive items landed cite-grounded; four escalations surfaced unapplied; zero gap-analysis entry; zero execution code; zero retroactive edits.

---

## [END] `<special_log>` — per-item disposition, forward-shape, contradiction-watch verdicts

### Per item (1–5): disposition · evidence · forward-shape

| Item | Disposition | Forward-shape | Evidence (verbatim cite) |
|---|---|---|---|
| **1** Ledger | **ADDED** | seeds-now → persists-through-ship | No ledger existed: MVP grep returned only an acceptance line (`MVP-v0.1.md:360`); the M08.7 rung table cites *code reality*, not an *eval cite* (`M08.7-execution-engine.md:107-119`). Created `docs/execution-status.md`, seeded from the six-primitive engine map (`M08.7-execution-engine.md` §Background:49-74). |
| **2** Close-on-run + cumulative-regression | **ESCALATED → approved (Append-on-close) → APPLIED** | standing convention (M08.7.B→M11) | close-on-run is PRESENT (`cluster-pattern.md` §1 step 4:45-49 + §6:160; `<close_gate><assembled_run>` STAGE-PROMPT-PROTOCOL:847). Cumulative-regression is ABSENT (grep empty). The append-on-close mechanism changes Revision-3 timing ("AFTER the workflow stabilizes" `protocol-revisions-irl-fail-rate.md:183-184,212-214`) → "surface, do not just apply." |
| **3** M08.V reconciliation | **ESCALATED → approved (Split) → APPLIED (ADR-0026)** | one-time routing (NOT generalized) | M08.V Dec-1/2 (→M9.A) `M08.V-retrospective.md:100-101` vs ORCHESTRATOR §9 (→M08.7 rungs 6/7) `ORCHESTRATOR.md:178`; §9's "vdr→rung 6" ≠ rung table (rung 6 = spawn `M08.7-execution-engine.md:115`); no plan_loop carry-forward ADR exists (`ls docs/adr/` — absent). Surfaced; did not pick. |
| **4** Mutation gate | **ESCALATED → approved (Scoped blocking) → APPLIED** | standing convention (M08.7.B→M11) | Blocking `<mutation_gate>` ALREADY present (`STAGE-PROMPT-PROTOCOL:847,859`; `cluster-pattern.md` §5:133-141; rule-11 line 97). CLAUDE.md §5 still "advisory" (`CLAUDE.md:157-159`) → §5↔cluster-pattern internal contradiction; recommended scoped reconciliation, surfaced not promoted. |
| **5** Design-before-UI | **ADDED** (standing rule); now-call → approved (Keep before-M9) — no §9 edit | standing convention (M08.7.B→M11) | Standing rule absent (grep: only DESIGN.md local principle `DESIGN.md:30,34,142` + §9 0b sequencing). Added `cluster-pattern.md` §8, forward-applied. The now-sequencing call (0b explicitly before M08.6.7) + the user-driven Claude-Design dependency + the external methodology-delta log → SURFACED (user-domain / dependency-gated), not self-authored. |

### Contradiction-watch verdicts (per the four named watches)

- **ITEM-2 watch — CONFIRMED.** The read confirmed the Revision-3 timing tension (a frozen cumulative eval thrashes a moving surface). Resolution (append-on-close) recommended; escalated because adopting it changes the Revision-3 timing decision.
- **ITEM-3 watch — CONFIRMED + amplified.** The watch's "re-routing M9.A" worry is real but the re-route already exists in §9; the read surfaced TWO contradictions (M08.V vs §9; §9-vdr-rung-6 vs the rung table). Escalated; did not pick.
- **ITEM-4 watch — CONFIRMED (narrowed).** §5↔cluster-pattern internal contradiction confirmed; but the blocking element already exists in three places, so ITEM 4 is a §5-wording reconciliation, not a net-new gate. Escalated with scoped resolution.
- **ITEM-5 watch — DISSOLVED to additive + dependency.** The standing-rule half is cleanly additive (absent, cited); the now-call + visual-layer dependency are user-driven, surfaced not assumed.

### NEW contradictions surfaced

1. ORCHESTRATOR §9 "`TestOutcome.vdr` rung 6" ≠ M08.7 rung table (rung 6 = sequential spawn; vdr is not a rung) — vdr has no concrete ladder home (inside ITEM 3).
2. `cluster-pattern.md` "Status of this doc's enforcement" is stale vs §9's "Phase 0a CLOSED" + protocol v1.9 (adjacent drift; flagged, NOT fixed — scope lock).

### Confirmations

- **Zero `docs/gap-analysis.md` entry** (process iteration, not a parent milestone — §20). ✓
- **Zero retroactive edits** to closed milestones / prior retros / accepted ADRs. ✓
- **Zero execution code / schema change** — only `docs/execution-status.md` (new) + `cluster-pattern.md` §8 (+50 lines). ✓
- Items 2/4/5 authored as STANDING (text reads "every UI-bearing milestone…" / "every milestone close…"); item 1 seeds-and-persists; item 3 recorded as a ONE-TIME routing decision, not generalized. ✓

---

## [END] Decisions received (2026-05-31) + applied

The maintainer answered the four escalations on the decision surface;
each approved resolution was then applied (no accepted decision silently
re-routed — every applied edit traces to an explicit pick):

1. **ITEM 2 → "Append-on-close."** Added `cluster-pattern.md` §9
   (cumulative-regression standing rule, append-on-close, refining the
   Revision-3 timing) + the `<close_gate><cumulative_regression>` element
   + v1.10 changelog in `STAGE-PROMPT-PROTOCOL.md` + the append-on-close
   line in the ledger's Maintenance protocol.
2. **ITEM 3 → "Split: plan_loop→M08.7, vdr→M9.A."** Filed
   `docs/adr/0026-plan-loop-vdr-carry-forward-routing.md` (Accepted);
   set ledger row-5 owner = rung 7 (ADR-0026); corrected ORCHESTRATOR §9's
   "vdr rung 6" mislabel. The M08 `gap-analysis.md` is append-only (§20) —
   NOT edited; M08.7's carry-forward will cite ADR-0026.
3. **ITEM 4 → "Scoped blocking."** Reconciled CLAUDE.md §5 to the two-tier
   posture (nightly-advisory on `main`; blocking at cluster-close on
   execution-wiring + critical modules) + a scoping clarifier in
   `cluster-pattern.md` §5.
4. **ITEM 5 now-call → "Keep before-M9."** No ORCHESTRATOR §9
   now-sequencing edit. The standing design-before-UI rule
   (`cluster-pattern.md` §8) still landed (the additive half of ITEM 5).

**Adjacent (out of scope here, NOT fixed):** the stale `cluster-pattern.md`
"Status of this doc's enforcement" section — refresh at the next
cluster-pattern touch.

**Commit:** all of the above land in ONE `docs(process)` commit (DCO
`-s`, no Co-Authored-By) once the maintainer approves the final diff.
