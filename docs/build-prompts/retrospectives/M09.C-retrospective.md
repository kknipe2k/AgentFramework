# M09.C — Stage Retrospective

> **Stage:** M09 Stage `C` — see `docs/build-prompts/M09-workbench-vertical-slice.md` (Stage M09.C)
> **Parent milestone:** M09 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m09-prep` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `a31206d`
> **Ending commit:** `6d27431` (red `dd607a0` → impl `ab5957f` → tsc+fmt fixups `6d27431`)
> **Estimated effort (per prompt):** ~1.5 hours
> **Actual elapsed:** ~0.9 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md auto-loaded; §4 Hard Rules honored (no commit before approval; no push to main; no gate skips).
- [x] All "Read first" files read in order (M09 phase doc Stage C C.1–C.6; commands.rs mcp_test_connection/_with @1049/1061 + mcp_list_servers @1083 + test_framework @1745 with the dispatcher wiring @1758/1769-1775/1782-1792; client/mod.rs test_connection @246 + list_servers @270 + get_connection @289; connection_resolver.rs the ConnectionResolver impl + record_to_transport; dispatch.rs McpDispatcher @91; Palette.tsx @143-160 + the on-mount fetch + dedupeByKindRef + drag contract; ipc.ts mcpTestConnection/mcpListServers + the McpTool type; builderStore.ts applyDrop 'tool' + connectEdgeReducer @451; + the M09.A + M09.B retrospectives per §19 rule 1).
- [x] Deliverable (3 sentences) + test plan (4 bullets) stated before any test file was written.
- [x] User confirmed the red phase before implementation began ("M09.C red approved — implement…").
- [x] Branch correct (`claude/m09-prep`).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| verify_gates / cargo fmt | `cargo fmt --all` re-wrapped the in-source `assert_eq!` in the connection_resolver.rs `list_server_tools` happy-path test (the line >100 cols). To keep the in-source `#[cfg(test)]` block byte-identical red→impl (the binary-crate strict-TDD variant), the wrap was reverted in the impl commit and re-applied in a separate labelled `test(M09.C)` fixup commit — the M09.B `be71df9` prettier-wrap precedent. | The strict-TDD red phase could note "run `cargo fmt`/`prettier --check` on new in-source test blocks + test files before the red commit" so red lands fmt-clean (companion to M09.A/M09.B's same note — now seen three stages running). | 1 |
| verify_gates / tsc | The red `Palette.test.tsx` MCP `mockImplementation` typed its params `(command: string, args?: unknown)`, which `tsc --noEmit` rejects against the `vi.fn((...args: unknown[]) => …)` signature (vitest/esbuild strips types so the red vitest run was green; `tsc` is the separate gate that caught it). Retyped to `(...args: unknown[])` reading `args[0]`/`args[1]` in the same labelled fixup commit (the M09.A `683ce48` tsc-cast precedent). | Same as above — a "tsc the new test file" nudge in the red phase. | 1 |
| verify_gates / renderer e2e | The full `test:e2e` Playwright run reported 2 `budget_threshold.spec.ts` failures (`page.goto('/')` timeout in `beforeEach`); isolating the spec showed 1 fail then 4/4 pass with no competing load — the documented gotcha #53 Vite cold-start (test #1 took 2.0m), pushed past even the 120s timeout by the concurrent llvm-cov + cargo builds I was running. Not an M09.C regression: `clearStore` touches only `__graphStore`, the spec is a Runtime-view budget test untouched by the Builder-view change. | The verify_gates step could note "do not run the heavy llvm-cov gates concurrently with the renderer Playwright suite on Windows — gotcha #53 cold-start starves under load." | 2 |

---

## [LIVE] Ambiguity events

| When | What was ambiguous | How resolved |
|---|---|---|
| C.3 #4 / the `tools_called` mirror | The phase doc said both "an Agent→Tool edge records it in `allowed_tools` (connectEdgeReducer)" AND "the M09.B file_access editor's sibling pattern adds it to `capabilities.tools_called`" — ambiguous whether the edge or a (non-existent) editor records `tools_called`. The deliverable text settled it: "a drop + Agent→Tool edge records `allowed_tools` + `capabilities.tools_called`." Resolved by making the edge record both (a dedicated `pushAgentTool` helper), which is the single coherent drag+connect action and matches `tools_called`'s schema meaning ("tools this artifact may invoke" = the same concept as `allowed_tools`). | Edge records both; idempotent on both lists. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After red tests confirmed failing right-reason | Red-phase bundle (cross-machine state, the test_framework-unchanged scope-lock confirmation @1758/1771/1774/1782-1792, the corrected McpTool path, the 4 failing surfaces + right-reason evidence, the EMPTY-test-path-diff plan) + "I will not commit until you approve." | Yes — approved + directed implement in one reply | Yes — at the red checkpoint |
| After green gates | This final surface (green evidence + retrospective) | (pending user review) | At the green checkpoint |

---

## [LIVE] Protocol drift events

None. Red committed only after explicit approval. The impl commit (`ab5957f`) does not touch any `tests/**` test file (`git diff dd607a0 ab5957f -- 'tests/**' '**/*.test.ts' '**/*.test.tsx' '**/*.e2e.ts'` is EMPTY) and the in-source `#[cfg(test)]` blocks of `connection_resolver.rs` + `commands.rs` are byte-identical red→impl (the scoped `git diff dd607a0 ab5957f` touches only production hunks: the import line + the `impl McpClient` block + the new command). The two mechanical fixups (tsc retype + fmt wrap) are a separate labelled commit (`6d27431`).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The production diff was tiny: a 4-line `McpClient::list_server_tools` (reusing the existing `ConnectionResolver::connection` + `list_tools`), a ~15-line thin command + `_with` seam, one `mcpListServerTools` ipc wrapper, a `source:'mcp'` palette fetch+items, and a `pushAgentTool` helper. No new transport, no dispatch change, no `test_framework` change. | The phase-doc thesis ("the substrate already executes; MCP dispatch + the Tester dispatcher are already wired — the gap is enumeration + palette + the recorded declaration") held exactly, as it did at M09.A/B. The `ConnectionResolver::connection` impl (M07.D1) already did the registry→record→transport→connection resolution; `list_server_tools` is one line of reuse on top. | Confirms the vertical-slice scoping: each M09 affordance is thin reuse over an already-executing substrate. Carry to M09.D (the dispatched-in-a-run observation). |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **replaced by an assembled test** — the recorded-state IS the discovery | `tests/e2e-tauri/builder_mcp_tool.e2e.ts` — in the real WebView2 app, wiring an agent to an MCP-canonical tool ref (`fs__read_file`) through the real `connectEdge` reducer records it in BOTH `allowed_tools` and `capabilities.tools_called` off the live `window.__builderStore`. | None. The palette-from-a-real-server is the maintainer IRL close bar (CI has no connected MCP server); the *dispatched-in-a-run* observation is M09.D (CLAUDE.md §4 rule 11). |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | CLAUDE.md sufficient orientation? | 5 | Read-first + wire-audit landed the change precisely; the test_framework-unchanged precondition was confirmable by grep. |
| 2 | Did the "Read first" list orient correctly? | 5 | Every pinned wire was load-bearing; the McpTool path correction (real `transport/mod.rs:61`, not `types/mcp.ts`) was pre-flagged and confirmed. |
| 3 | Deliverable + test plan before code per §16? | 5 | Stated before any test file. |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | Three minor mechanical rounds (fmt wrap, tsc retype, the e2e cold-start flake diagnosis), each diagnosed correctly first try. |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed only after explicit approval; no push. |
| 6 | Escalate/proceed split right? | 5 | The `tools_called`-mirror ambiguity resolved from the deliverable text (technical, owned); no spurious escalation. |
| 7 | Human-direction load reasonable? | 5 | One approval at the red checkpoint. |
| 8 | Surfaces actionable? | 5 | The red surface drove a one-line approval. |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | `list_server_tools` / `mcp_list_server_tools` / `mcpListServerTools` / `pushAgentTool` named for what they are; comments explain *why* (reuse, declaration completeness, the canonical ref). |
| 2 | Behavior tests, not tautology? | 5 | Asserts observable outcomes — enumeration via a seeded mock, the unregistered-name error, the palette `source:'mcp'` item + canonical drag payload, the degrade-to-built-ins resilience, and the assembled-store `allowed_tools`+`tools_called` record. |
| 3 | Public APIs documented? | 5 | `list_server_tools` + both commands + the ipc wrapper carry doc comments with `# Errors` + the reuse/read-only rationale. |
| 4 | Exactly what was promised? | 5 | The command + `_with` seam + ipc + `source:'mcp'` palette + the `tools_called` mirror; no `test_framework` touch, no new transport, no schema change. |
| 5 | Stranger-understandable? | 5 | Comments name the M09.C intent, the already-wired dispatcher, and the M13 deferral (data-source catalog / MCP-as-node). |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no stringly-typed surprises, no ad-hoc allows; per-server fetch failures are caught + logged (not silently dropped). |
| 7 | Coverage measured ≥80%? | 5 | runtime-mcp 96.00% lines (≥95; connection_resolver.rs 95.74%); workspace 91.91% lines (≥80); vitest 93.49% lines, 665/665. |
| 8 | CI green across the matrix? | 4 | All Rust + frontend gates + both e2e legs green on Windows (renderer cold-start flake ruled out by isolated 4/4 re-run); Linux/macOS legs run in CI on push. |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | Wire-audit + construction-reachability + the scope-lock (test_framework unchanged) were directly load-bearing. |
| 2 | Sections needed but missing? | 4 | A "fmt/tsc the new in-source test blocks + test files before the red commit" nudge would pre-empt friction #1+#2 (now a three-stage running pattern: M09.A tsc-cast, M09.B prettier-wrap, M09.C both). |
| 3 | Gotchas local or generalize? | 4 | "Adding an on-mount IPC fetch must degrade gracefully (catch+log) so one offline server never blanks the palette" is reusable; gotcha #53 (Vite cold-start under concurrent load) is already logged — reaffirmed. |
| 4 | Time-box within 2×? | 5 | ~0.9h vs ~1.5h. |
| 5 | Implicit protocol moments? | 5 | The red/impl/fixup three-commit shape + the byte-identical scoped-diff proof were explicit and clean. |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the seeded-mock unit + the store-driven e2e are key-independent and run in CI. |
| 7 | Copyable to remaining stages? | 5 | M09.D inherits the green seams: an MCP tool now records in `allowed_tools`+`tools_called` and dispatches via the already-wired Tester dispatcher. |
| **Pattern axis total** | | **33/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `dd607a0` committed only after "M09.C red approved…"; impl `ab5957f` + fixup `6d27431` per the same instruction. No push.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — table empty; the red→impl test-path diff is EMPTY and the in-source test blocks are byte-identical (scoped diff touches only production).
- [x] **G4: stage completed** — impl on `claude/m09-prep`; C.4 acceptance met (an installed server's tools are draggable + an Agent→Tool edge records `allowed_tools`+`tools_called`); no execution-status flip (correct — that's M09.D).
- [x] **G5: every row ≥3** — lowest individual score is 4.
- [x] **G6: CI-parity** — commands run match CI; no CI-divergent flags:
  - `cargo fmt --all --check` ✓ · `cargo clippy --workspace --all-targets -- -D warnings` ✓
  - `cargo test --workspace` ✓ (exit 0) · `cargo test --workspace --doc` ✓
  - `cargo llvm-cov --package runtime-mcp --features test-helpers … --fail-under-lines 95` → 96.00% ✓
  - `cargo llvm-cov --workspace … --fail-under-lines 80` → 91.91% ✓
  - `npx prettier --check` ✓ · `npx eslint .` ✓ · `npx tsc --noEmit` ✓
  - `npm run test` → 665/665, 93.49% lines ✓ · `npm audit --audit-level=high` → 0 high/critical ✓
  - `npm run test:e2e` (Playwright renderer) → 59/59 (the 2 budget_threshold cold-start flakes pass 4/4 on an unloaded isolated re-run; gotcha #53) ✓
  - `npm run test:e2e:tauri` (wdio + tauri-driver against a freshly `npx tauri build`-rebuilt binary) → **15 spec files passed, 15 total (100%)**, including the new `builder_mcp_tool.e2e.ts` (`✓ wiring_an_agent_to_an_mcp_tool_records_allowed_tools_and_tools_called`) ✓

### Soft gates

- [x] **S1:** Process ≥30/40 — actual 40
- [x] **S2:** Product ≥32/40 — actual 39
- [x] **S3:** Pattern ≥25/35 — actual 33
- [x] **S4:** Time-box within 2× — ~0.9h vs ~1.5h
- [x] **S5:** ≤3 Severity-3 events — 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to M09.D.

---

## [END] Coverage holdouts

No `--ignore-filename-regex` change. The new production logic landed in `connection_resolver.rs` (95.74% lines, inside the runtime-mcp ≥95 gate, NOT excluded) + `commands.rs` (the `_with` seam tested; the thin `#[tauri::command]` wrapper untested, under the `src-tauri/**` tauri-shell 50% patch gate per `docs/coverage-policy.md` §D, the `mcp_list_servers`/`mcp_test_connection` wrapper precedent). No new exclusion; no §C entry required.

---

## [END] Decisions for the next stage (M09.D)

### `CLAUDE.md` updates

- [ ] None required this stage.

### `TEMPLATE.md` updates

- [ ] None.

### Milestone document updates

- [ ] M09.D inherits the green M09.C seams: an MCP tool authored on the canvas now records in BOTH `allowed_tools` and `capabilities.tools_called`, and dispatches in a Tester run via the already-wired `build_test_mcp_dispatcher` + `connect_test_session_mcp` (`commands.rs:1769-1775`) — M09.D's job is to *observe the dispatched-in-a-run result* (CLAUDE.md §4 rule 11), not to wire it.

### Protocol additions

- [ ] Candidate `docs/gotchas.md` entry (now a three-stage running pattern — M09.A tsc-cast, M09.B prettier-wrap, M09.C both): a strict-TDD red commit should be run through `cargo fmt` + `prettier --check` + `tsc --noEmit` on the new test files / in-source test blocks BEFORE the red commit, so the mechanical fixups don't become a separate post-impl commit. Low priority (the separate-labelled-commit pattern works and preserves the byte-identical invariant), but graduating it would shave a commit per TS/Rust stage.
- [ ] Reaffirm gotcha #53: do not run the heavy `cargo llvm-cov` gates concurrently with the renderer Playwright suite on Windows — the Vite cold-start starves under load and trips the 120s `budget_threshold` `beforeEach` timeout.

### Open issues to file

- [ ] None.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M09 Stage `C`, per `CLAUDE.md` §19. The stage's commits land on the `claude/m09-prep` feature branch; the milestone PR drafts only at the end of the final stage (M09.D).

**Claude:** I have completed M09 Stage `C`. The red phase was approved before commit; the impl commit does not touch any `tests/**` test file (`git diff dd607a0 ab5957f -- 'tests/**' …` is EMPTY) and the in-source `#[cfg(test)]` blocks are byte-identical red→impl (the scoped diff touches only production hunks); the tsc-retype + fmt-wrap are a separate labelled commit (`6d27431`). All Rust + frontend gates are green, and both e2e legs pass — the real-app `tauri-driver` suite is 15/15 spec files including the new `builder_mcp_tool.e2e.ts`. No execution-status flip (correct for M09.C — the dispatched-in-a-run observation is M09.D, CLAUDE.md §4 rule 11). I will not push or open a PR until directed.

**Surfaced at:** 2026-06-07 (local).
