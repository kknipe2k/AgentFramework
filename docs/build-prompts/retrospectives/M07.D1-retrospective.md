# M07.D1 — Stage Retrospective

> **Stage:** M07 Stage `D1` — ADR-0011 (a)-(c) concrete dispatch construction + `ConnectionResolver` + CQ-6/EFF-4. See `docs/build-prompts/M07-registry-import.md` §D1.
> **Parent milestone:** M07 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-18
> **Session end:** 2026-05-18
> **Branch:** `claude/m07-registry-import` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `b9e2da5` (M07.C retrospective; HEAD before this stage)
> **Ending commits:** red `689592f` → impl `f5ef25a` → additive-coverage `befb3a6` → style `3eae0fc`
> **Estimated effort (per prompt):** 4–5 h
> **Actual elapsed:** ~6.5 h equivalent (~1.4×; the CQ-6→DB-CHECK ripple was the over-run, see Surprise)

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in force (no push without approval; strict-TDD two-commit; no Co-Authored-By; Hard Rule 8 CODEOWNERS plan-first).
- [x] All `<read_first>` files read in order (CLAUDE.md, STAGE-PROMPT-PROTOCOL v1.8 audit slots, M07 phase doc Background/Key-constraints/Stage D1, ADR-0010 + ADR-0011 Context #1–#3 verbatim, spec §5a + §8.security L1/L2a, M06.V 🟡#1 + Dec-6 wire-trace-vs-ADR, M07.A construction-reachability map + M07.C `[END]`, gap-analysis CQ-6/EFF-4 + transport/mod.rs baseline, gotchas #56/#68/#69/#74/#81).
- [x] **M07.A + M07.C `[END]` Decisions read + applied:** A's `<construction_reachability_check>` (D1 owns each ADR-0011 `inputs_reachable=false`); C's "import_artifact + Arc<Registry> in place; D1's McpDispatcher ctor independent of import (shared registry DB only)"; the recurring `rustdoc::broken_intra_doc_links` / `too_long_first_doc_paragraph` gotchas (the doc-link-code one recurred — see Friction); step-2 clippy scope-all-touched-crates.
- [x] `<pre_flight_check>`: HEAD == `claude/m07-registry-import`; M07.A (`348d1ef`) + M07.C (`849fd0c`) impl commits present. `adr0011_inputs` grep: the only `CapabilityEnforcer`/`NamespaceResolver` ref in `src-tauri/src/` is the M07.C import-network-gate (`commands.rs:989 CapabilityEnforcer::new()`), NOT the dispatch construction graph — A's zero-McpDispatcher-ctor premise holds.
- [x] Deliverable + 5-area test plan stated; the phase-doc-vs-shipped-API reconciliations surfaced in the red surface (technical-decision-owned per §12).
- [x] `git status` clean except untracked `.claude/scheduled_tasks.lock` (harness; never staged).

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Orientation (before red) | D1.3.1 pseudocode shows `on_connect`/`on_disconnect` as **`ConnectionResolver` trait methods**; the M06-shipped trait is `connection(server)` ONLY. D1.3.2 pseudocode `McpClient::new(registry)` / `McpDispatcher::new(client,resolver,enforcer,audit)` are wrong arity/order; the seam is the existing `run_smoke_session_with`, not a new `run_session_with`. | D1.3.1/D1.3.2 should derive the trait + ctor signatures from shipped code (gotcha #74) rather than illustrative pseudocode; the `<wire_trace_vs_adr_reconcile>` already says the re-resolution driver is on `McpDispatcher` — D1.3.1 contradicted its own reconcile block. | 2 |
| First green `cargo test` (red verify + impl) | CQ-6 (`status: String`→enum) failed at runtime: `add_server` → `AlreadyExists` because `000_initial.sql` pins a `status` CHECK to the **pre-schema** vocabulary (`configured\|connected\|errored\|disabled\|failed`); writing the schema-enum `disconnected` violates it and is mis-mapped to AlreadyExists. CQ-6 is unbuildable without migration 003 (table rebuild — SQLite can't ALTER a CHECK). | D1.3.3 ("M06.B shipped the enum; no schema touch") materially under-scoped CQ-6 — it never mentions the stale DB CHECK mirror. Should have flagged "the `000_initial.sql` status CHECK is a drifted mirror; CQ-6 requires a realignment migration." | 3 |
| Migration-003 ripple (×3 red amends) | Migration 003 changed the migration set + status vocabulary, breaking THREE in-source test blocks that pinned the old state: `db.rs` (`mcp_servers_status_transitions`/`_default_values_applied`), `session_db.rs` (`status:"configured"`), `migration_runner.rs` (migration count/names = 3→4). Each discovered serially via gate runs, each fixed by a clean unpushed red-commit amend (M07.C soft-reset/amend precedent). | A phase-doc "CQ-6 ripple inventory" (the DB CHECK + every in-source test asserting the old vocabulary/migration count) would have made these one red commit, not three amends. | 3 |
| Coverage gate | The new `connection_resolver.rs` (happy path = the ADR-0011 seam↔concrete OS-call holdout, structurally untestable without a live server) dragged runtime-mcp 96.64%→91.49%. Resolved by a separate labelled `test(M07.D1): additive coverage` follow-up (CLAUDE.md §6) unit-testing the *pure* helpers (`record_to_transport`/`lifecycle_to_mcp`) + `update_health_batch` → 95.83% ≥95, without altering the fixed gate regex (CI-parity preserved). | The phase doc anticipated only a `transport/mod.rs` reconcile; it should have flagged that a new OS-call-holdout file needs either a Category-3 exclusion (four-mirror) OR pure-helper unit tests to hold the aggregate. | 2 |
| Test authoring (×3) | `Arc<dyn Connection>`/`Arc<dyn Transport>` are not `Debug`, so `expect_err` / `match { .. other => panic!("{other:?}") }` don't compile. Three occurrences; fixed in-red (test-only). `clippy --fix` then could not autofix the resulting `manual_let_else` (diverging-branch match) — hand-rewritten as `let Ok(_)…else`. | A gotcha-candidate: "`Arc<dyn Trait>` results are not `Debug`; use `let…else`/`matches!`, never `expect_err`/`{:?}`." | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase-doc D1.3.1 pseudocode vs the M06-shipped `ConnectionResolver` trait (`dispatch.rs:47`) vs the prompt's own `<wire_trace_vs_adr_reconcile>` #6 | Whether the §5a re-resolution driver is `ConnectionResolver` trait methods on `McpClient` or methods on `McpDispatcher` | Owned §12 technical decision: (a) = impl the real `connection()`; the re-resolution driver = `McpDispatcher::on_server_connected/on_server_disconnected` (resolver lives in `McpDispatcher` per ADR-0010 — exactly the `<wire_trace_vs_adr_reconcile>` #6 directive). Documented in module docs + impl-commit body + red surface. | M07 phase-doc D1.3.1 wording (future-pass, §8 phase-doc-edit pre-flight — orchestration-session-owned; M07.A/B/C grandfathering precedent) |
| Phase-doc D1.3.3 ("CQ-6: M06.B shipped the enum; schema-first NOT needed") vs `000_initial.sql:121-122` (status CHECK = pre-schema vocabulary) | Whether CQ-6 is a pure Rust field-type swap or needs a migration | Owned §12 technical decision: migration `003_mcp_server_status.sql` (table rebuild + value remap) realigns the DB CHECK mirror to the already-accepted `mcp.v1.json::McpServerStatus` — no `schemas/*.json` change, no ADR trigger (the ADR triggers are schema-file/protocol/capability changes; a SQL-mirror realignment to an accepted schema is not one). Documented in CHANGELOG + impl-commit body + the migration header. | M07 phase-doc D1.3.3 (same future-pass channel) |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-red (Hard Rule 8 plan-first) | The ADR-0011 (a)-(c) construction-reachability closure map (file:line per input) + the CapabilityEnforcer-construction CODEOWNERS plan + the phase-doc reconciliations | Yes — stated before touching the CODEOWNERS construction; the construction map IS the plan per the prompt | Yes — before the red commit |
| After red `689592f` (amended ×3) | Red-phase surface: cross-machine state, right-reason-red evidence, the construction map, the 5 phase-doc/shipped-API reconciliations, strict-invariant pre-statement | Yes — user approved ("approved") | Yes — strict-TDD `surface_for_red_approval`, before any production code. (The 3 subsequent red amends were post-approval clean discoveries of the same CQ-6 ripple, nothing pushed — M07.C precedent; logged not re-surfaced) |

---

## [LIVE] Protocol drift events

None observed. The red commit was amended three times (`d10ec31`→`ac4f4e8`→`5f6a1c6`→`689592f`) as the CQ-6→migration-003 ripple surfaced into successive in-source test blocks — each a **local, unpushed** soft-amend (the explicit M07.C precedent: "folded via a clean soft-reset/amend, nothing pushed, history stayed clean"). Strict v1.8 two-commit invariant preserved and **verified**: `git diff 689592f..f5ef25a -- '**/tests/**'` = 0 lines; the `commands.rs`/`db.rs`/`session_db.rs` in-source `#[cfg(test)]` blocks proven byte-identical red→impl via scoped diff (the binary/in-source caveat, CLAUDE.md §6 v1.8). Net-new additive tests + mechanical fmt/clippy went to separate labelled `test(M07.D1): additive coverage` + `style(M07.D1)` commits per CLAUDE.md §6. No Hard Rule approached; nothing pushed; final push gated on approval.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| CQ-6 — framed by the phase doc as the *small* item ("M06.B shipped the enum, no schema touch") — was the **largest** source of work: a stale DB CHECK mirror forced a table-rebuild migration, which rippled into 3 in-source test blocks across 2 crates + a coverage follow-up. | The phase-doc sizing inverted reality (the "minor" CQ-6 dwarfed the headline ADR-0011 (a)-(c) construction, which went cleanly). A schema/DB-mirror change is never "just a Rust field swap" when a CHECK constraint encodes the old vocabulary. | Phase-doc authoring of any `String→generated-enum` field swap must inventory the DB CHECK + every in-source test asserting the old literal/migration-count. Generalizable gotcha candidate (see Decisions). |
| The ADR-0011 (a)-(c) construction graph — the headline, CODEOWNERS-flagged, "largest carry-forward in the ledger" — closed in ~one clean pass once the shipped signatures were read (gotcha #74). | The construction-reachability map (A authored it `false`, D1 inverted it `true`) worked exactly as the v1.8 slot intended; the hard part was the *un*-flagged CQ-6. | The v1.8 `<construction_reachability_check>` A→D1 inversion is sound; the protocol gap is on schema-mirror ripple, not construction graphs. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §12 own-technical-decisions / escalate-only-user-domain drove the correct calls on all 5 phase-doc reconciliations (none was user-domain → none escalated, all documented); §6 binary/in-source caveat + M07.C clean-amend precedent governed the red amends |
| 2 | Milestone prompt "Read first" oriented correctly? | 3 | ADR-0010/0011 + M06.V Dec-6 were exactly right for (a)-(c); but D1.3.1/D1.3.2/D1.3.3 pseudocode were defective (wrong trait shape, wrong ctor arity, under-scoped CQ-6) — the read-list could not pre-empt the in-doc defects |
| 3 | Deliverable + test plan before code per §16? | 5 | 5-area plan + the construction map + reconciliations stated before any red test |
| 4 | Self-correct in ≤3 rounds per §7? | 3 | The CQ-6 ripple took 3 serial discovery rounds (db.rs→session_db.rs→migration_runner.rs) — within the spirit (each a deterministic gate-driven discovery + clean amend, not thrash) but more rounds than a correctly-scoped prompt needed; coverage took 1 follow-up |
| 5 | Do-not-commit-until-approved held? | 5 | Red surfaced + approved; impl/additive/style local only; nothing pushed; final push gated |
| 6 | Escalated right things, proceeded on right things? | 5 | All 5 reconciliations were API/DB-mechanism technical decisions (§12-owned, documented) — correctly NOT escalated as user-domain forks; Hard Rule 8 plan surfaced pre-red |
| 7 | Human-direction load reasonable? | 5 | One decision surface (pre-red plan + reconciliations) + one red approval |
| 8 | Surfaces actionable? | 5 | Red surface carried the construction map, the 5 reconciliations, right-reason-red evidence, strict-invariant pre-statement; approved in one word |
| **Process axis total** | | **36/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Code matches §9 style + naming? | 5 | Mirrors the ADR-0010 seam archetype + `transport_from_config` shape; module docs state the *why* (ADR-0011 reconcile, the DB-mirror rationale); migration header documents the SQLite-can't-ALTER-CHECK constraint |
| 2 | Behaviour tests, not tautology (§5)? | 5 | Re-resolution asserts observable `NewAmbiguity` + post-connect `Ambiguous` dispatch + post-disconnect `Invoked`; construction-seam asserts concrete-vs-mock by behavior (empty-resolver→`None`); CQ-6 round-trips through real SQLite; EFF-4 asserts per-server persisted status |
| 3 | Public APIs documented w/ compile-checked examples? | 4 | All new public items + `# Errors`/`# Panics` documented; no doctests (the integration suite is the executable contract — `skills_lock`/`import` precedent) |
| 4 | Deliverables exactly promised — no creep/cuts? | 5 | (a) ConnectionResolver + (b) §5a driver + (c) build_mcp_dispatcher + CQ-6 + EFF-4 exactly; **NO agent loop, NO token_usage** (confirmed D2-scoped); migration 003 is the necessary in-scope consequence of CQ-6, not creep |
| 5 | Stranger-understandable without the spec? | 5 | Module/commit/migration docs state the ADR-0011 construction-graph closure, the McpDispatcher-not-McpClient reconcile, and the DB-mirror realignment inline |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers/stringly-typed (status is the generated enum, Hard Rule 5); no ad-hoc `#[allow]`; `lifecycle_to_mcp` captures root cause per variant |
| 7 | Coverage ≥80 / ≥95 — measured? | 5 | runtime-mcp **95.83% line ≥95** (CI-parity command, clean `llvm-cov clean` first per gotcha #81); workspace ≥80 exit 0; runtime-drone gate run (db.rs touched) | 
| 8 | CI green across OS × toolchain? | 4 | All local gates green incl. CI-parity llvm-cov + `fmt --check` + `clippy --workspace --all-targets -D warnings` + `--doc` + `deny`; CI prediction strong (migration 003 is portable SQL; no Windows-divergent flags used); not yet observed on CI |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | `<construction_reachability_check>` (A→D1 inversion) + `<wire_trace_vs_adr_reconcile>` #6 were precisely the right tools and worked as intended |
| 2 | Sections needed but missing? | 3 | A `<schema_mirror_ripple_check>`-class slot ("a `String→enum` field swap: inventory the DB CHECK + every in-source test asserting the old literal/migration count") would have collapsed the 3 red amends into one |
| 3 | Gotchas local or generalize to §15? | 3 | Two generalize: (a) "`String`→generated-enum field swap ripples to the DB CHECK mirror + in-source tests asserting the old vocabulary/migration count"; (b) "`Arc<dyn Trait>` results aren't `Debug` — `let…else`/`matches!`, never `expect_err`/`{:?}`". The `doc_link_code`/`broken_intra_doc_links` gotcha RECURRED again (M07.B/C → now M07.D1) — still not graduated to §15 |
| 4 | Time-box matched (within 2×)? | 4 | ~6.5 h vs 4–5 h (~1.4×, within 2×); the over-run is entirely the under-scoped-CQ-6 ripple, not the headline construction |
| 5 | Implicit protocol moments? | 4 | "A schema/DB-mirror change red-amends iteratively as each in-source test surfaces" — the M07.C clean-amend precedent covered it, but a phase-doc ripple-inventory would make it a single red |
| 6 | Re-runnable by a fresh session → comparable? | 4 | Yes — the shipped signatures + the DB CHECK + the in-source-test ripple are deterministic; a fresh session reading the same files reaches the same owned decisions + the same migration |
| 7 | Copy format for remaining milestones — change anything? | 4 | Phase-doc authoring of any generated-type field swap must run the schema-mirror ripple inventory (Decisions) |
| **Pattern axis total** | | **27/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `689592f` surfaced + user-approved; impl `f5ef25a` + additive `befb3a6` + style `3eae0fc` local only; nothing pushed; final push gated on this surface.
- [x] **G2: no Severity-5 friction** — max severity 3 (the CQ-6 under-scoping + its 3-amend ripple).
- [x] **G3: no unaddressed protocol drift** — none; the 3 red amends are the documented M07.C clean-unpushed-amend pattern; strict invariant verified (0-line tests diff red→impl).
- [x] **G4: stage completed** — all D1.4 acceptance met: ConnectionResolver re-resolution (connect/disconnect/idempotent), concrete-construction seam (concrete-vs-mock + assembled smoke), CQ-6 round-trip, EFF-4 batched; runtime-mcp ≥95; strict two-commit invariant proven; transport/mod.rs baseline → G `<coverage_policy_reconciliation>` (phase-doc option b).
- [x] **G5: every axis row ≥3** — lowest individual row is 3 (Process #2/#4, Pattern #2/#3).
- [x] **G6: CI-parity** — runtime-mcp llvm-cov run with the **exact** stage `<coverage_gate>` regex (`src.main\.rs|generated|src.lib\.rs|src.transport.stdio\.rs|src.transport.http\.rs|src.client.auth_keyring\.rs|src.client.lifecycle\.rs`, `--fail-under-lines 95`) → 95.83% exit 0; workspace `src.main\.rs|generated` `--fail-under-lines 80` exit 0; runtime-drone gate (db.rs touched) run; `cargo llvm-cov clean --workspace` before each authoritative measure (gotcha #81); `cargo fmt --all -- --check` exit 0; `cargo clippy --workspace --all-targets -- -D warnings` clean; `cargo test --workspace` green; `--doc`/`cargo doc -D warnings`/`deny` clean. **No CI-divergent flags** (no `--test-threads`/`--skip`); `cargo test --workspace` ran with no parallelism override — the gotcha #56 Windows subprocess flake did not surface this run (D1 touches no drone-spawn test path beyond the migration-runner, which is in-process).

### Soft gates

- [x] **S1:** Process ≥30/40 — 36
- [x] **S2:** Product ≥32/40 — 38
- [x] **S3:** Pattern ≥25/35 — 27
- [x] **S4:** Time-box within 2× — ~6.5 h vs 4–5 h (~1.4×)
- [x] **S5:** ≤3 Severity-3 friction — 2 (CQ-6 under-scoping; the 3-amend ripple)

### Outcome

- [x] **Pattern is sound but rough.** All hard gates pass; all soft gates pass, but the CQ-6 phase-doc under-scoping produced 2 Severity-3 events + a 1.4× over-run + 3 red amends. The headline ADR-0011 (a)-(c) construction was clean; the roughness is isolated to the schema-mirror ripple. Apply the schema-mirror-ripple-inventory note before the next generated-type field-swap stage.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** ≥80 (CI-parity `--fail-under-lines 80` exit 0).
- **Exclusions added this stage:** None. Regex unchanged: `src.main\.rs|generated`.

### Per-package gate — runtime-mcp (≥95% line)

- **Coverage actual this stage:** **95.83% line** (CI-parity command, exit 0; `cargo llvm-cov clean` first per gotcha #81). New `connection_resolver.rs` 13.79%→covered via the pure-helper additive tests; aggregate 91.49%→95.83%.
- **Exclusions added this stage:** **None.** The fixed stage `<coverage_gate>` regex was preserved (CI-parity hard rule) — `connection_resolver.rs` was NOT excluded; instead its pure helpers (`record_to_transport`/`lifecycle_to_mcp`) got in-source unit tests, the OS-call holdout (`connection()` happy delegate to `get_connection`, the ADR-0011 seam↔concrete boundary) is the residual covered by the mandatory Stage V `--features integration` smoke.
- **Regex as of stage end (byte-identical to CLAUDE.md §6 + `docs/coverage-policy.md` §A):** `src.main\.rs|generated|src.lib\.rs|src.transport.stdio\.rs|src.transport.http\.rs|src.client.auth_keyring\.rs|src.client.lifecycle\.rs`
- **No four-mirror sync this stage.** `transport/mod.rs` stays 87.50% (the pre-existing M06.G CQ-1 carry-forward); the aggregate ≥95 holds without touching it — phase-doc D1 option (b): rationale extended here, reconciled at the v1.8 closeout G `<coverage_policy_reconciliation>` (NOT a D1 four-mirror change).

### Per-package gate — runtime-drone (≥95% line)

- **Coverage actual this stage:** gate run (db.rs MIGRATIONS slice +5 lines + migration 003 SQL, exercised by the updated `migration_runner.rs` 4-migration tests + the db.rs status tests).
- **Exclusions added this stage:** None. Regex unchanged: `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs`.

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `runtime-mcp/src/client/connection_resolver.rs` | (new) | covered via in-source pure-helper tests; `connection()` happy delegate = ADR-0011 OS-call holdout (Stage V smoke) | new | new file; the OS-call residue is the documented ADR-0011 seam↔concrete gap |
| `runtime-mcp/src/client/registry.rs` | ≥M07.C | `update_health_batch` + `row_to_record` enum-parse covered (additive) | → | — |
| `runtime-mcp/src/client/mod.rs` | ≥M07.C | EFF-4 `run_health_pass` rewrite, ~95.49% (within passing aggregate) | → | — |
| `runtime-mcp/src/dispatch.rs` | ≥M07.C | `on_server_connected/disconnected` covered by mcp_dispatch_integration | ↑ | — |
| `runtime-mcp/src/transport/mod.rs` | 87.50% (M06.G CQ-1) | 87.50% | → | unchanged pre-existing carry-forward; phase-doc D1 option (b); G reconciles |

### CI workflow drift check

- [x] `.github/workflows/ci.yml` runtime-mcp/workspace/runtime-drone `--ignore-filename-regex` unchanged this stage (no exclusion added); no workflow change needed. Migration 003 + the new `mcp_server_status` MIGRATIONS entry are exercised by the existing CI test job; no coverage-mirror change.

---

## [END] Decisions for the next stage

### special_log (required by the stage prompt)

- **ADR-0011 (a)-(c) construction graph CLOSED — each A-mapped `inputs_reachable=false` now `true`:**
  | Wire | Constructor (file:line) | Now reachable |
  |---|---|---|
  | `impl ConnectionResolver for McpClient` | `crates/runtime-mcp/src/client/connection_resolver.rs:102` (`connection()` → registry record → `record_to_transport` → `get_connection`) | true |
  | concrete `McpDispatcher` in src-tauri | `src-tauri/src/commands.rs:218` `build_mcp_dispatcher` → `McpDispatcher::new(resolver, enforcer, Arc<McpClient> as Arc<dyn ConnectionResolver>, audit, session_id)`; `run_smoke_session` threads `Some(..)` (was `None`) | true — `NamespaceResolver::new(BTreeMap::new())` + `CapabilityEnforcer::new()` + Tauri-managed `Arc<McpClient>` all in-shell |
  | §5a re-resolution driver (M06.V 🟡#1) | `crates/runtime-mcp/src/dispatch.rs:141` `McpDispatcher::on_server_connected` / `:155` `on_server_disconnected` — on `McpDispatcher`, NOT `McpClient` | true |
  D2 consumes: the agent-with-tools loop calls `on_server_connected` and translates `Vec<NewAmbiguity>`→`ToolAliasAmbiguous`.
- **§5a trace authored against `McpDispatcher`** (M06.V Dec-6 `<wire_trace_vs_adr_reconcile>` #6) — `McpClient` only *impls* `ConnectionResolver`; the resolver re-evaluation is the dispatcher's. M07.V Wire trace #6 endpoint = `McpDispatcher::on_server_connected`.
- **CQ-6 disposition:** `McpServerRecord`/`McpServerSummary.status` → `runtime_mcp::ServerStatus` (re-exported generated `McpServerStatus`). Migration **003_mcp_server_status.sql** (table rebuild + value remap) realigns the DB CHECK to the M06.B schema enum. No `schemas/*.json` change, no ADR trigger.
- **EFF-4 disposition:** `run_health_pass` → one batched `Registry::update_health_batch` transaction writing CQ-6 status.
- **NO agent loop / NO `token_usage` shipped** — confirmed D2-scoped.
- **transport/mod.rs four-mirror:** none this stage (aggregate ≥95 without it); the 87.50% carry-forward → G `<coverage_policy_reconciliation>` (phase-doc D1 option b).
- **Strict v1.8 invariant:** `git diff 689592f..f5ef25a -- '**/tests/**'` = 0 lines (verified); commands.rs/db.rs/session_db.rs in-source `#[cfg(test)]` blocks byte-identical red→impl (scoped diff). Net-new additive + mechanical fmt/clippy in separate labelled `befb3a6`/`3eae0fc`. No Co-Authored-By; DCO `-s`; session-URL footer.

### `CLAUDE.md` / protocol updates (candidates — apply if substantive)

- [ ] **§15 gotcha (graduate — RECURRED M07.B→C→D1):** module-level `//!` / `pub use` doc cross-refs must be plain code spans `` `X` ``, never intra-doc `[`X`]` / adjacent code-link (`rustdoc::broken_intra_doc_links` / `clippy::doc_link_code`). Three milestones running; overdue for §15 / a stage `<read_first>` note.
- [ ] **§15 gotcha candidate:** a `String → generated-enum` field swap (CQ-6 class) ripples beyond the field: (a) the SQLite CHECK constraint mirror in `migrations/000_*.sql` (SQLite can't ALTER a CHECK → table-rebuild migration), (b) every in-source test asserting the old literal, (c) every migration-count/name test. Inventory all three at phase-doc authoring time, not via serial gate-driven red amends.
- [ ] **§15 gotcha candidate:** `Arc<dyn Trait>` `Result`s aren't `Debug` — use `let…else` / `matches!`, never `expect_err` / `{other:?}` (recurred 3× this stage; also bit M07.D1 red).
- [ ] **STAGE-PROMPT-PROTOCOL note:** a `<schema_mirror_ripple_check>` authoring slot for any generated-type field swap (the inverse of the construction-reachability slot — it worked; this is the missing sibling for schema-mirror changes).

### Milestone document updates

- [ ] M07 phase-doc D1.3.1 (pseudocode shows `on_connect`/`on_disconnect` as `ConnectionResolver` trait methods — contradicts the shipped trait + its own `<wire_trace_vs_adr_reconcile>` #6), D1.3.2 (`McpClient::new(registry)` / `McpDispatcher::new(client,resolver,enforcer,audit)` wrong arity/order; `run_session_with` is actually `run_smoke_session_with`), D1.3.3 (CQ-6 under-scoped — no mention of the stale DB CHECK / migration 003) are imprecise. **Not edited mid-flight** (§8 phase-doc-edit pre-flight + M07.A/B/C grandfathering — orchestration-session-owned). Recorded here + impl-commit body. Stage D2+ / Stage V read this `[END]` first; M07.V Wire trace #6 endpoint = `McpDispatcher::on_server_connected`.

### Open issues to file

- [ ] None blocking. `transport/mod.rs` 87.50% remains the explicit G `<coverage_policy_reconciliation>` item (phase-doc-sanctioned, option b).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M07 Stage D1 per `CLAUDE.md` §19. User review pending. This stage's commits land on `claude/m07-registry-import`; the PR drafts/pushes only at the end of the final M07 stage.

**Claude:** M07 Stage D1 complete per the stage prompt. Red `689592f` (user-approved; amended ×3 unpushed for the CQ-6→migration-003 in-source-test ripple — M07.C clean-amend precedent) → impl `f5ef25a` (production only; `git diff 689592f..f5ef25a -- '**/tests/**'` = 0 lines, strict v1.8 invariant proven; in-source test blocks byte-identical red→impl via scoped diff) → additive-coverage `befb3a6` → style `3eae0fc` (CLAUDE.md §6 separations). ADR-0011 (a)-(c) construction graph closed; §5a driver authored against `McpDispatcher` per M06.V Dec-6; CQ-6 (+ migration 003 DB-mirror realignment) + EFF-4; **no agent loop / no token_usage** (D2). All Rust gates green, CI-parity (runtime-mcp 95.83% ≥95). I have NOT pushed; surfacing the retrospective + cross-machine state + draft commit messages for final approval.

**Surfaced at:** 2026-05-18
