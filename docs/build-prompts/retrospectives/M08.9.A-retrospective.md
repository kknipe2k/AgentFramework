# M08.9.A — Stage Retrospective

> **Stage:** M08.9 Stage `A` — Truthful verdict (closes TD-047) — see `docs/build-prompts/M08.9-honest-tester.md`
> **Parent milestone:** M08.9 (honest Tester) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-06
> **Session end:** 2026-06-06
> **Branch:** `claude/m08.9-honest-tester` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `7143e92`
> **Ending commit:** `3e4ee09` (impl) on `37389ae` (red) — local, unpushed, awaiting approval
> **Estimated effort (per prompt):** ~1–1.5 hours (small — signal + primitives existed)
> **Actual elapsed:** ~1 hour

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded; §4 Hard Rules in force
- [x] All "Read first" files read (phase doc Stage A; tester.rs; event.rs TierViolation; ipc.ts + TesterModal.tsx; ADR-0030; gotcha #94; the existing TesterModal vitest + tier_enforcement/smoke e2e for the key-guard pattern)
- [x] Deliverable + test plan stated before code
- [x] Branch correct (`claude/m08.9-honest-tester`)
- [x] No carried-over uncommitted tracked changes (only pre-existing untracked IRL dirs: `budget-irl/`, `gap-irl/`, `rung2/`, `.claude/*`)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| impl / clippy verify | `too_long_first_doc_paragraph` fired on the new `TierBlock` + `TestVerdict` doc comments | (general gotcha, not stage-specific) — first doc sentence must be short with a blank line before the detail paragraph | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `CLAUDE.md` §4.1/§8 (do-not-commit) vs `<tdd_discipline strict>` + `<approval_surface>` (red→impl diff EMPTY) | §8 forbids `git commit` before approval, but the strict-TDD two-commit protocol and the approval surface both *require* two local commits to exist (to diff red..impl) | Made the two commits **local-only and unpushed**; nothing reaches the remote / a PR without approval. This matches the build-machine history (test()+feat() commit pairs already land on branches; approval gates the push). | Already the de-facto protocol; noting the tension for the closeout. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Stage end | The full approval bundle (cross-machine state, red→impl EMPTY-diff evidence, gate results, IRL walkthrough, "will not commit/push until approved") | Yes | Yes — after both phases complete + all CI-parity gates green |

---

## [LIVE] Protocol drift events

None observed. (The §8-vs-strict-TDD tension above was resolved by keeping commits local/unpushed — not a drift, a reconciliation; logged under Ambiguity.)

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The fix was ~3 lines of real logic | The whole bug was a dropped `_ => {}` arm; the `TierViolation` signal, the `capability_kind_label` helper, the disclosure surfaces were all already present | Confirms the phase-doc framing — "fold it, don't detect it." Most of the diff is doc comments + the TS mirror + the render. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **deferred to Stage V** (assembled-execution pass) + maintainer IRL | `tests/e2e-tauri/tester_verdict.e2e.ts` (key-guarded real-app run) + the producer-driven Rust units | None. The verdict-in-app assertion is key-dependent (a real tier block needs a live model to emit a Write `ToolUse`); CI skips it like the smoke real-Anthropic chain. The maintainer IRL is the authoritative close (TD-047). `test:e2e:tauri` was **NOT run in this build session** (no `tauri-driver`/`msedgedriver` + built app available here) — honestly disclosed. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | Hard Rules + §5/§6 gate ordering applied directly |
| 2 | "Read first" list correct? | 5 | Every pin (event.rs:668, tester.rs:127/150/155, ipc.ts:615) was exactly load-bearing |
| 3 | Deliverable + test plan before code? | 5 | Stated; red phase encoded it |
| 4 | Self-correct in ≤3 rounds? | 5 | One clippy round (doc-paragraph), fixed first try |
| 5 | Do-not-commit-until-approved held? | 5 | Local commits only; unpushed; no PR |
| 6 | Escalate right things / proceed right things? | 5 | No escalation needed; technical choices (Promote affordance reusing `requestTierTransition`) owned per §12 |
| 7 | Human-direction load reasonable? | 5 | Zero mid-flight input needed |
| 8 | Surfaces actionable? | 5 | Single approval bundle |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case Rust / camelCase TS / kebab artifacts; doc-why comments |
| 2 | Behavior tests, not tautology (§5)? | 5 | Producer-driven fold units assert observable verdict/tier_blocks/passed; vitest asserts the rendered TIER-LIMITED + blocked action |
| 3 | Public APIs documented? | 5 | `TierBlock`, `TestVerdict`, new fields all doc'd; rustdoc -D missing_docs green |
| 4 | Exactly promised — no creep/cut? | 5 | Verdict only; drill-down explicitly deferred to Stage B; `passed` unchanged |
| 5 | Stranger-readable? | 5 | Docs spell out tier ≠ defect (ADR-0030) inline |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no `any`, no ad-hoc allow |
| 7 | Coverage ≥95 on runtime-main — measured? | 5 | `cargo llvm-cov -p runtime-main … --fail-under-lines 95` → **95.03%**, exit 0 |
| 8 | CI green across OS×toolchain? | 3 | Local gates green; `test:e2e:tauri` not runnable in this session (driver/built-app absent) — CI + maintainer IRL close it |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | wire_signature_audit + construction_reachability_check both paid off |
| 2 | Sections needed but missing? | 5 | None |
| 3 | Gotchas local or generalize? | 4 | The doc-paragraph clippy trip is general; gotcha #94 (producer-driven) reaffirmed |
| 4 | Time-box matched (within 2×)? | 5 | ~1h vs ~1–1.5h estimate |
| 5 | Implicit protocol moments? | 4 | The §8-vs-strict-TDD commit tension (logged) is implicit — worth a one-line note in the protocol |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the pins make it mechanical |
| 7 | Format needs change before reuse? | 5 | No |
| **Pattern axis total** | | **33/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — commits `37389ae`/`3e4ee09` are local + unpushed; no PR; surfacing for approval now.
- [x] **G2: no Severity-5 friction** — max severity 1.
- [x] **G3: no unaddressed protocol drift** — none.
- [x] **G4: stage completed** — `3e4ee09`; acceptance criteria met (verdict 3-state; tier_blocks folded; passed unchanged; TS mirror; render). The real-app IRL is the maintainer's pending close per the prompt's `<real_app_irl>`.
- [x] **G5: every axis row ≥3** — lowest is 3 (Product #8, CI/e2e-tauri not run locally).
- [x] **G6: CI-parity** — gates run with the exact `CLAUDE.md` §6 commands (see below). No divergent flags.

**CI-parity evidence (exact commands):**
- `cargo fmt --all` ; `cargo clippy --workspace --all-targets -- -D warnings` (green)
- `cargo test --workspace` → 72 suites, 1189 passed, 0 failed
- `cargo test --workspace --doc` (green) ; `RUSTDOCFLAGS="-D missing_docs -D rustdoc::broken_intra_doc_links" cargo doc --workspace --no-deps` (exit 0)
- `cargo audit` (exit 0) ; `cargo deny check` (advisories/bans/licenses/sources ok)
- `cargo llvm-cov clean --workspace` then `cargo llvm-cov --package runtime-main --ignore-filename-regex "src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs" --fail-under-lines 95` → **95.03%**, exit 0
- `npx prettier --check` (green) ; `npx eslint` (green) ; `npx tsc --noEmit` (green) ; `npm run test` → 59 files, 631 passed ; `npm audit --audit-level=high` (exit 0; 2 pre-existing moderate)
- **NOT run this session:** `npm run test:e2e:tauri` — requires the built Tauri app + `tauri-driver`/`msedgedriver`, unavailable in this build session. The maintainer IRL + the CI `e2e-tauri-driver` job are the close (the verdict-in-app assertion is key-dependent and skips on CI without an Anthropic key, like `smoke.e2e.ts` 3–6).

### Soft gates

- [x] **S1:** Process 40/40 ≥ 30
- [x] **S2:** Product 38/40 ≥ 32
- [x] **S3:** Pattern 33/35 ≥ 25
- [x] **S4:** ~1h vs ~1–1.5h (ratio <2×)
- [x] **S5:** 0 Severity-3 friction

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage B (run drill-down).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** not separately re-measured (the runtime-main ≥95 gate is the binding one this stage; workspace floor unaffected — change is renderer + one folder arm)
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged)

### Per-package gates (≥95% line)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** **95.03%** (TOTAL, exit 0)
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs` (unchanged)

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `builder/tester.rs` | covered (M08.8) | grew with the new arm + struct/enum, all exercised by the 4 producer-driven units | ↑ | n/a |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage; the four mirrors (§6 commands, §5 categories, `codecov.yml`, `docs/coverage-policy.md`) stay consistent. The closeout (Stage G) appends the §C M08.9 entry.

---

## [END] Decisions for the next stage

### `CLAUDE.md` updates
- [ ] None required (the §8-vs-strict-TDD commit tension is already de-facto resolved; flag at closeout if worth a one-liner).

### Milestone document updates
- [ ] None — Stage B prompt (drill-down) is accurate; it builds on `outcome.trace` + the verdict A shipped.

### Protocol additions
- [ ] None.

### Open issues to file
- [ ] None — TD-047 closes here (record RESOLVED at closeout, citing `tester.rs::fold_outcome` TierViolation arm + the verdict + the real-app IRL).

---

## [END] Sign-off

**Claude:** I have completed M08.9 Stage `A` per the stage prompt. The TierViolation signal already existed in `outcome.trace` — the fix was **folding** it (a new match arm + `tier_blocks` + a derived `verdict`), not detecting anything new. `passed` is **unchanged** (a tier block is the user's tier setting, not a framework defect — ADR-0030); `verdict` is the new UI truth. The fold unit is **producer-driven** (constructs the real `AgentEvent::TierViolation`; gotcha #94). **TD-047** closes here. The **TIER-LIMITED real-app IRL** is the maintainer's pending close. Commits `37389ae` (red) + `3e4ee09` (impl) are local and unpushed; I will NOT push or open a PR until approved.

**Surfaced at:** 2026-06-06
