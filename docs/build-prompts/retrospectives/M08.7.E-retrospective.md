# M08.7.E — Stage Retrospective

> **Stage:** M08.7 Stage `E` (rung 5 — budget enforcement at the run loop) — see `docs/build-prompts/M08.7-execution-engine.md`
> **Parent milestone:** M08.7 (Execution Engine) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-01
> **Session end:** 2026-06-02
> **Branch:** `claude/m08.7-execution-engine`
> **Starting commit:** `3705a04`
> **Ending commits:** `2e94e12` (red — failing budget-at-the-run-loop tests + TD-038) → `b265c0b` (impl — the wire + CHANGELOG) → `bdf34be` (additive — the isolated Suspend-threshold test, maintainer-requested) → `59bf723` (additive — the mutation-kill test for the `turn_output` accumulation) → this retro
> **Estimated effort (per prompt):** 3–5 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in scope (esp. §4 rule 11 grounded-claims, §8 commit workflow, §10 Hard Rule 8, §11 schema-change rule)
- [x] All "Read first" files read (CLAUDE.md; cluster-pattern.md §1/§2/§4/§5; M08.7 phase doc Background + Stage A + Stage E E.1–E.6; M08.7.D retro — the suspend triage precedent + assembled-test harness; spec §2a + §13; retro template) + the 6 read_reference source files (enforcer/hook/cost, providers/mod, agent_sdk, tester)
- [x] Deliverable + test plan stated before code; E.3.0 ground findings surfaced before tests (the first checkpoint); user approved with 3 conditions
- [x] Branch correct (`claude/m08.7-execution-engine`, rungs 1–4 present, `3705a04` HEAD)
- [x] `git status` clean except pre-existing untracked `.claude/*`, `gap-irl/`, `rung2/`
- [x] Pre-flight `problem_live` confirmed by grep: NO `record_spend` / `BudgetEnforcer` caller in `src/sdk/` or `src/builder/`; the only budget test (`budget_threshold.rs`) exercises the standalone enforcer, not the run loop — no painted run-loop test to delete

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| mutation_gate | `cargo mutants --in-place` reported the body-replacement mutants (`dispatch_budget_actions → Ok(())`, `build_session_budget → None`) as **unviable** rather than caught/survived — because cargo-mutants builds the lib with the crate's `[lints] -D warnings`, and a replaced body leaves params unused → `unused_variable` → compile error → "unviable". The gate did not actually exercise those mutants until re-run with `RUSTFLAGS="--cap-lints=allow"`. | The mutation-gate gotcha should note that a crate with `[lints] -D warnings` makes cargo-mutants spuriously mark body-replacement mutants unviable; run with `RUSTFLAGS="--cap-lints=allow"`. Worth a `docs/gotchas.md` entry (proposed in Decisions). | 2 |
| ground_at_red | Phase-doc E.2 lists a `tester.rs` change ("`run_test_session_with` constructs a `BudgetEnforcer`…"). Grounding found the framework's `budget` block is ALREADY threaded into `CapabilityWiring.framework` by `run_test_session_with` (tester.rs:484), so `run_agent` builds the enforcer directly — `tester.rs` needs NO change. The actual minimal wire is a single production file (`agent_sdk.rs`). | E.2 over-specified the file set; the construction belongs in `run_agent` reading the already-threaded framework, not in the Tester seam. Minor — grounding caught it before any edit. | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase-doc E.3.2 ("budget_warn event + a notifier dispatch (the `NotifierDispatched` path)") vs `event.rs:596` `NotifierDispatched` being bound to a `HitlTriggerRef` | Whether `Warn` must emit `NotifierDispatched` (which would require fabricating a HITL trigger that does not fit a budget warning) | Scoped out: `Warn` emits `BudgetWarn` (the renderer toast is event-driven by it); the OS desktop notifier is **TD-038**. Surfaced in the ground findings; maintainer's red-approval condition 3 directed logging the TD. | TD-038; the phase doc's "notifier" wording should be read as the in-app toast, not `NotifierDispatched`. |
| Phase-doc + handler framing of budget `Suspend` as a v0.1 "blind spot" vs the rung-4 ADR-0029 resolve-and-resume precedent | Whether budget `Suspend` is a permanent dead-end | Reframed (maintainer red-approval condition 2): not a dead-end — the same resolve-and-resume pattern as the rung-4 gap (suspend → human approves budget → resume), folded into **ADR-0029 generalized to budget**. Rung 5 records the suspend; the resume joins that rung. Documented in the test-file module doc, the `budget_suspended` field doc, the CHANGELOG, and TD-038. | Recorded in-cluster (this retro + the code docs). |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After ground_at_red | The E.3.0 findings (per-turn boundary = Usage-peek + post-loop record; enforcer built in `run_agent` from the already-threaded framework, `tester.rs` unchanged; HardStop-on-Tester-path = loop-break + `BudgetExceeded` in trace, no live `StopProcess`, fits ADR-0019); the dispatch mapping table; the 5-test plan; scope-lock confirm; "proceed to write tests?" | Yes — maintainer approved with 3 conditions (cite the 4 events as existing/no-schema-change; reframe Suspend; log a TD for the notifier) | Yes — before any test written |
| After the 3 conditions | The four `Budget*` events line-cited in BOTH `event.v1.json` (`:747/759/771/782`) AND `event.rs` (`:694/703/712/719`) with byte-exact field match → STOP not triggered; Suspend reframe; TD-038 logged | Yes — discharged all three before red | Yes |
| After red commit | Right-reason red (every trace carries `TokenUsage` but ZERO `Budget*` events; tiny-cap run did not halt — both turns ran); red-phase gates (fmt + clippy clean); the green plan (single-file wire); strict-TDD commitment | Yes — maintainer approved ("Green approved") + requested the isolated Suspend test before the mutation gate | Yes |

---

## [LIVE] Protocol drift events

None observed. Do-not-commit-without-approval held: ground surfaced + approved before tests; red committed under the ground approval; impl committed under the red ("Green approved") approval; the additive Suspend test committed under the same approval (maintainer-requested); **no push** (awaiting final approval). Strict-TDD two-commit held — `git diff 2e94e12..b265c0b -- 'crates/runtime-main/tests/**'` is **EMPTY** (impl touched only `agent_sdk.rs` + `CHANGELOG.md`); the isolated Suspend test landed as a **separate labelled additive follow-up** (`bdf34be`), not folded into impl. No schema change (the four `Budget*` events pre-exist — line-cited). The enforcer + ladder + events were REUSED, not rebuilt (scope lock). Sandbox/Bash untouched. #22 budget-cap persistence (M08.6.7) NOT touched (zero-propagation).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| `tester.rs` needed NO change. | The phase doc E.2 named it. But the framework (with its `budget` block) is already in `CapabilityWiring.framework`, so `run_agent` builds the enforcer from it — the wire is one production file. | The construction_reachability_check ("the session BudgetEnforcer is constructible from the framework caps + threadable into drive_stream") was satisfied more cheaply than the file table implied. Grounding the actual data path beats trusting the file list. |
| The M08.7 thesis confirmed a **fifth** time. | After rung-1 grant-loading, rung-2 tier-wiring, rung-3 resolved-body threading, rung-4 gap-suspend: the budget primitive too was fully built + unit-tested (enforcer + ladder + cost cache, M04) with an explicit docstring naming a "caller (SDK turn loop)" that **never existed** on the live loop. Per-turn spend was produced (`TokenUsage`) and never enforced. | The "scaffolding built around an execution core, never wired to the loop" pattern is the through-line of the entire M08.7 ladder. Rung 5 adds the caller; the resume half (budget + gap) is ADR-0029. |
| cargo-mutants + `[lints] -D warnings` ⇒ spurious "unviable". | A body-replace mutant compiles fine under normal `cargo build` (unused-var is a warning) but the crate denies warnings, so the mutant fails to build and reads as "unviable" rather than tested. | `RUSTFLAGS="--cap-lints=allow"` is required for the mutation gate on any `-D warnings` crate. Proposed `docs/gotchas.md` entry. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test (the assembled integration test IS the discovery) + the real-app IRL **required as the close gate** | `crates/runtime-main/tests/budget_runloop_execution.rs` — drives the REAL `run_test_session_with → run_agent → drive_stream` loop (only the provider stubbed); asserts the tiny-cap HARD-STOP (budget_exceeded + no further turn, counted via `seen`), per-turn accumulation, the downshift model swap (observed on `configs[1].model`), warn-emits-but-not-halts, idempotence, and the isolated Suspend halt | NotifierDispatched (budget OS notifier) → **TD-038** (scope-out, in-app toast covers v0.1). Suspend HITL **resume** → ADR-0029 generalized to budget (zero-propagation: scheduled, not "→ next milestone"). #22 budget-cap persistence → confirmed an M08.6.7 cluster, NOT absorbed. No new TD beyond TD-038. |

**Mutation gate (cluster-pattern §5 — blocking; execution wiring):**
`cargo mutants --in-place` on the budget cluster (`dispatch_budget_actions`
+ `build_session_budget` + the `run_agent` `||` break + the `drive_stream`
token accumulation), run with `RUSTFLAGS="--cap-lints=allow"` (the
`-D warnings` unviable-mutant fix). First pass: **7 caught, 1 unviable**
(`build_session_budget → Some(Default::default())` — `BudgetEnforcer` has no
`Default`), **1 MISSED** — `turn_output += → *=` (output-token accumulation;
all 6 tests used input-only `usage(N, 0)`, so a corrupted `turn_output` was
a no-op). Killed in-cluster with an additive test (`output_tokens_contribute_to_budget_spend`, `59bf723`); re-run confirms
both `*=` mutants **CAUGHT, missed empty** — **8 viable caught, 1 unviable**.
The gate bites the dispatch + the break + the per-turn cost accumulation.

This is a backend stage with an **assembled Rust integration** surface (no UI — exempt from the §8 design-before-UI gate). The cluster's CI close is the **assembled tests observed** (7 tests green against the real loop; the four threshold actions + the run-halt observed). Per rule 11 the **behavioral** close — a real Anthropic model run halting at a tiny `session_usd_cap` with no runaway spend — is the **IRL gate, maintainer-run**, and is **NOT exercised by CI** (the `BudgetScriptStub` is not a real model + real pricing).

**What was NOT exercised (rule 11 — the honest blind spots):**
- **The real-Anthropic tiny-cap run-halt has not been run** — it is the maintainer IRL gate; CI proves the wire + the four-action dispatch + the run-halt mechanics with a stubbed provider, not a real model spending real dollars.
- **The budget `Suspend` HITL round-trip (resume after human approval) is NOT exercised** — only the suspend-**halt** (suspend-and-record) + the hard-stop-**halt** are. The resume half is ADR-0029 (generalized to budget). The assembled tests prove `BudgetSuspended` is emitted and the run halts; they do NOT prove a resume (there is none in v0.1).
- The downshift `sonnet→haiku` branch is not exercised (only `opus→sonnet`); the haiku-terminal "no further downshift" case is exercised implicitly (downshift threshold crossed at haiku ⇒ ladder `None` ⇒ no `BudgetDownshift` event).

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 rule 11 + §11 schema rule drove the event-citation check that prevented a sneaked-in variant. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 5 | The 6 read_reference files were exactly the wire surface; the D-retro suspend precedent was load-bearing for the reframe. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | E.3.0 ground findings + 5-test plan surfaced before any test. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | The cap-lints mutation issue was diagnosed from the build log in one pass. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | Ground approval → red; red approval → impl + additive test; no push. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | Schema-event citations, Suspend reframe, TD — all surfaced; technical wire decisions (single-file, ladder reuse) made autonomously. |
| 7 | Was the human-direction load reasonable? | 5 | Two approval gates + 3 conditions; no step-level hand-holding. |
| 8 | Were the surfaces actionable? | 5 | The dispatch-mapping table + the line-cited event check were directly checkable. |
| **Process axis total** | | **40/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | Small helpers, effects at the edges (`dispatch_budget_actions`), why-comments only. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | All 6 drive the assembled loop and assert run-halt side effects (counted `seen`), not events alone (#66). |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 5 | No new public API (the wire is internal); doc comments on the new fields/fns. rustdoc gate green. |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | The wire + 4-action dispatch; enforcer/ladder/events reused; no schema change; notifier scoped to TD-038 (recorded, not silently dropped). |
| 5 | Would a stranger understand this without the spec? | 5 | The dispatch helper's doc maps each action → event + side effect + the ADR-0029/TD-038 pointers. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers (caps/thresholds from the framework), no stringly-typed dispatch, no silent drops. |
| 7 | Is coverage ≥95% on the safety primitive — measured? | 5 | `runtime-main` lines **96.23% ≥ 95** (measured, exit 0). |
| 8 | Did CI green hold across the matrix? | 4 | All gates green locally at CI-parity; the cross-OS/MSRV matrix runs on CI (not re-run locally — the standard stage posture). |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern — does this generalize?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all sections useful? | 5 | The construction_reachability_check + ground-at-red caught the tester.rs over-specification. |
| 2 | Were any sections needed but missing? | 4 | A mutation-gate note on `-D warnings`/cap-lints would have saved one round. |
| 3 | Do milestone gotchas generalize to §15? | 4 | The cap-lints mutation gotcha generalizes to every `-D warnings` crate — proposed gotcha entry. |
| 4 | Did the time-box match (within 2×)? | 5 | 3–5h estimate; ~1 session. |
| 5 | Implicit protocol moments? | 4 | "Which file the enforcer is constructed in" was implicit; grounding resolved it. |
| 6 | Could a different fresh session reproduce this? | 5 | The ground findings + test plan are deterministic. |
| 7 | Does the prompt format need changes before reuse? | 5 | No — the cluster-gate + strict-TDD shape held. |
| **Pattern axis total** | | **32/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — ground approval before tests; red committed under ground approval; impl + additive test under red approval; no push.
- [x] **G2: no Severity-5 friction events** — max severity 2 (the cap-lints mutation discovery).
- [x] **G3: no protocol drift events left unaddressed** — table empty.
- [x] **G4: the stage actually completed** — `2e94e12` (red) → `b265c0b` (impl) → `bdf34be` (additive); 6 assembled tests green; the BDD close contract (tiny cap → halt + no further turn) observed in the assembled run.
- [x] **G5: scores ≥3 in every row** — lowest row is 4.
- [x] **G6: CI-parity** — gates run with the exact CI commands (see Coverage holdouts + the surface). The mutation gate used `RUSTFLAGS="--cap-lints=allow"` (NOT a CI gate command — cargo-mutants is the nightly/cluster-close tool, not a CI build step; the cap-lints flag is a mutation-tooling necessity, not a coverage/clippy divergence).

### Soft gates

- [x] **S1:** Process ≥30/40 — **40**
- [x] **S2:** Product ≥32/40 — **39**
- [x] **S3:** Pattern ≥25/35 — **32**
- [x] **S4:** Time-box within 2× — estimated 3–5h, ~1 session
- [x] **S5:** ≤3 Severity-3 friction events — **0** (max severity 2)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. The behavioral close (real-Anthropic tiny-cap halt) is the maintainer IRL gate — the cluster does NOT close on tests-green (rule 11; stated above).

---

## [END] Coverage holdouts

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** **96.23%** lines (gate `--fail-under-lines 95`, exit 0)
- **Exclusions added this stage:** None added. (The wire is in `agent_sdk.rs`, already covered by the assembled tests; no new exclusion.)
- **Exclusion list as of stage end:** `src/main.rs | generated | src/providers/anthropic.rs | src/drone_ipc/connection.rs | src/sandbox_ipc/connection.rs | src/import/fetch.rs` — unchanged from M08.7.D.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `src/sdk/agent_sdk.rs` | (rung-4) | 92.35% lines | → | New budget wire added; the crate-aggregate gate (96.23% ≥ 95) holds. A few uncovered lines are in the dispatch arms not hit by the 6 tests (e.g. the `Downshift` event-emit branch is exercised; the haiku-`None` branch is exercised implicitly). No regression to the crate gate. |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage (no new exclusion); the §6 command set is unchanged. No coverage-policy reconciliation needed.

---

## [END] Decisions for the next stage (or next parent milestone)

1. **Propose a `docs/gotchas.md` entry:** cargo-mutants on a crate with `[lints] -D warnings` marks body-replacement mutants **unviable** (unused-var → deny-warnings → compile fail); run the mutation gate with `RUSTFLAGS="--cap-lints=allow"`. (Pairs with gotcha #92 — the os-1314 `--in-place` note.)
2. **ADR-0029 is now generalized to budget** — the gap **and** budget `Suspend` resolve-and-resume share one rung. When that rung lands, the budget-`Suspend` resume (human approves the cap → run continues) + the budget OS notifier (TD-038) should be wired together.
3. **The real-Anthropic IRL (tiny-cap halt) is the close gate** — surface it to the maintainer for the in-real-app watch; the assembled tests + this retro do NOT close the cluster on their own (rule 11).
4. **M08.7a ladder note:** rung 5 completes the backend execution ladder (built-in tools → capability gate → skill load → gap detection → budget). The execution-status ledger (`docs/execution-status.md`) row for budget should flip to `executes — observed, eval E-05` only after the IRL run-halt watch.
