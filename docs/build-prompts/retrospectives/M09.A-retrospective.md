# M09.A — Stage Retrospective

> **Stage:** M09 Stage `A` — see `docs/build-prompts/M09-workbench-vertical-slice.md` (Stage M09.A)
> **Parent milestone:** M09 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-07 18:30 local
> **Session end:** 2026-06-07 19:05 local
> **Branch:** `claude/m09-prep` (parent-milestone feature branch; stages commit on the same branch)
> **Starting commit:** `4f77a4e`
> **Ending commit:** `683ce48` (red `85c24b3` → impl `fe04140` → test-cast fixup `683ce48`)
> **Estimated effort (per prompt):** ~1.5 hours
> **Actual elapsed:** ~0.6 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded) — Hard Rules honored throughout (no commit before approval; the red commit landed only after the user's explicit "Red approved. Commit it…").
- [x] All "Read first" files were read in order (M09 phase doc Stage A + Background; Palette.tsx; builderStore.ts addNode/applyDrop/builderAgent/emptyFramework + BuilderCanvas.onDrop; agent.v1.json id pattern).
- [x] Deliverable + test plan stated in 1–3 sentences / 3–5 bullets before code.
- [x] User confirmed the red phase (deliverable + failing tests) before implementation began.
- [x] Branch correct (`claude/m09-prep`).
- [x] `git status` clean at start.

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| verify_gates / tsc | The red-phase `nextAgentRef` test cast `…)) as Framework['agents']` tripped TS2352 (the generated `agents` is a non-empty tuple). Vitest (esbuild, no type-check) passed it at red, so the type error only surfaced at the `tsc --noEmit` gate after impl. Resolved with the suite's existing `as unknown as` idiom in a separate labelled fixup commit (`683ce48`) so the strict red→impl test diff stayed EMPTY. | The strict-TDD red phase could note "run `tsc --noEmit` on new test files before the red commit so a latent type error doesn't surface only after impl." | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Hard Rule 1 (no commit without approval) vs strict-TDD `red_phase_commit` ordering | Whether to commit the red phase before surfacing | Honored Hard Rule 1: surfaced the red bundle WITHOUT committing, committed only after the user's explicit approval. | Already consistent in practice; no doc change needed. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After red tests confirmed failing | Red-phase bundle (cross-machine state, 8 failing tests + right-reason evidence, pinned wires, design note) + "will not commit until you approve" | Yes — the user approved and directed commit + impl in one reply | Yes — at the red checkpoint |
| After green gates | This final surface (green evidence + retrospective) | (pending user review) | At the green checkpoint |

---

## [LIVE] Protocol drift events

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The store genuinely needed zero change | The phase doc claimed "Palette-only," and it held exactly — `addNode → applyDrop → builderAgent` minted the agent with no edit; the diff was 33 src lines (helper + one palette item). | Confirms the M09 thesis: the authoring gap is UI-thin because the substrate already executes. Good signal for B/C/D scoping. |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| **replaced by an assembled test** — the mechanical regression IS the discovery | `tests/e2e-tauri/builder_create_agent.e2e.ts` — `✓ new_agent_palette_item_drags_a_fresh_agent_onto_the_canvas` on the real WebView2 app (agent-1 node renders on drop; the New-agent item advances to agent-2; agent-2 node renders) | None. The created agent is intentionally schema-invalid until M09.B (no `capabilities`/`role`) — by design, not a finding. |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | CLAUDE.md sufficient orientation? | 5 | Read-first list + wire-audit landed the change precisely. |
| 2 | Did the "Read first" list orient correctly? | 5 | Every pinned line (addNode:506/509, builderAgent:145, drag contract, id pattern) was load-bearing. |
| 3 | Deliverable + test plan before code per §16? | 5 | Stated before any test file was written. |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | One round (tsc cast) — resolved immediately. |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed only after explicit approval; no push. |
| 6 | Escalate/proceed split right? | 5 | No escalation needed; proceeded within scope. |
| 7 | Human-direction load reasonable? | 5 | One approval at the red checkpoint. |
| 8 | Surfaces actionable? | 5 | Red surface drove a one-line approval. |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | `nextAgentRef` pure, named for what it returns; comment explains *why* (idempotence-guard interplay). |
| 2 | Behavior tests, not tautology? | 5 | Asserts observable outcomes — palette item present, drag payload, real-app node renders, distinct second id. |
| 3 | Public APIs documented? | 5 | `nextAgentRef` carries a doc comment with the schema-pattern rationale. |
| 4 | Exactly what was promised? | 5 | Palette item + pure helper; no drop-handler/store-core change; no capabilities (deferred to B). |
| 5 | Stranger-understandable? | 5 | Comments name the M09.A intent + the ADR-0032 widening note. |
| 6 | §9 anti-patterns absent? | 5 | No magic numbers, no stringly-typed surprises, no ad-hoc allows. |
| 7 | Coverage measured ≥80%? | 5 | Workspace vitest 93.56% lines; Palette.tsx 91.07%, builderStore.ts 95.2% — measured, 654/654 pass. |
| 8 | CI green across the matrix? | 4 | Frontend gates + both e2e gates green locally on Windows; the Linux/macOS legs run in CI on push (renderer e2e is OS-portable; tauri-driver runs Linux+Windows). |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | Wire-audit + construction-reachability-check were directly load-bearing. |
| 2 | Sections needed but missing? | 4 | A "tsc the red tests" nudge would have pre-empted the one friction event. |
| 3 | Gotchas local or generalize? | 4 | The "vitest passes type-loose test casts that tsc later rejects" trap is general to TS stages — candidate gotcha. |
| 4 | Time-box within 2×? | 5 | ~0.6h vs ~1.5h estimate. |
| 5 | Implicit protocol moments? | 5 | None — the red/impl/fixup three-commit shape was explicit. |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the e2e is key-independent and runs in CI. |
| 7 | Copyable to remaining stages? | 5 | B/C/D inherit the same Palette/store seams. |
| **Pattern axis total** | | **33/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `85c24b3` committed only after the user's "Red approved. Commit it…"; impl `fe04140` + fixup `683ce48` per the same instruction. No push.
- [x] **G2: no Severity-5 friction** — max severity was 2 (tsc cast).
- [x] **G3: no unaddressed protocol drift** — table empty.
- [x] **G4: stage completed** — impl `fe04140` on `claude/m09-prep`; A.4 acceptance met (real-app e2e green); no execution-status flip (correct — that's M09.D).
- [x] **G5: every row ≥3** — lowest individual score is 4.
- [x] **G6: CI-parity** — commands run match CI:
  - `npx prettier --check '**/*.{ts,tsx,js,jsx,json}'` ✓
  - `npx eslint .` ✓
  - `npx tsc --noEmit` ✓
  - `npm run test` (= `vitest run --coverage`) → 654/654, 93.56% lines ✓
  - `npm audit --audit-level=high` → 0 high/critical (2 moderate, below the gate) ✓
  - `npm run test:e2e` (Playwright renderer) → 59 passed ✓
  - `npm run test:e2e:tauri` (wdio + tauri-driver) → 13/13 specs, incl. `builder_create_agent.e2e.ts` ✓
  - No CI-divergent flags used.

### Soft gates

- [x] **S1:** Process ≥30/40 — actual 40
- [x] **S2:** Product ≥32/40 — actual 39
- [x] **S3:** Pattern ≥25/35 — actual 33
- [x] **S4:** Time-box within 2× — ~0.6h vs ~1.5h
- [x] **S5:** ≤3 Severity-3 events — 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to M09.B with the minor decision below.

---

## [END] Coverage holdouts

n/a — renderer/TS-only stage; no Rust crate gate touched, no `--ignore-filename-regex` change. Workspace vitest line coverage 93.56% (≥80% gate). No exclusions added.

---

## [END] Decisions for the next stage (or next parent milestone if this is the final stage)

### `CLAUDE.md` updates

- [ ] None required this stage.

### `TEMPLATE.md` updates

- [ ] None.

### Milestone document updates

- [ ] M09.B inherits the green M09.A seams as-is; `builderAgent` (`builderStore.ts:145`) still omits `capabilities` exactly as B.3 expects — B's `inputs_reachable="false"` premise holds.

### Protocol additions

- [ ] Consider noting in the strict-TDD red phase: run `tsc --noEmit` on new test files before the red commit (vitest's esbuild transform does not type-check, so a loose cast surfaces only at the later gate). Candidate `docs/gotchas.md` entry — low priority.

### Open issues to file

- [ ] None.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M09 Stage `A`, per `CLAUDE.md` §19. The stage's commits land on the `claude/m09-prep` feature branch; the milestone PR drafts only at the end of the final stage (M09.D).

**Claude:** I have completed M09 Stage `A`. The red phase was approved before commit; the impl commit does not touch the test files (`git diff 85c24b3..fe04140 -- '**/tests/**'` is EMPTY); the test-cast fixup is a separate labelled commit. Frontend gates + both e2e gates are green, including the real-app `builder_create_agent.e2e.ts`. No execution-status flip (correct for M09.A). I will not push or open a PR until directed.

**Surfaced at:** 2026-06-07 19:05 local
