# M08.8.A — Stage Retrospective (backfilled at the M08.8.G closeout)

> **Stage:** M08.8 Stage `A` — live-graph execution view (TD-034) — see `docs/build-prompts/M08.8-design-workbench.md` (Stage A A.1–A.7)
> **Parent milestone:** M08.8 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Branch:** `claude/m08.8-design-workbench`
> **Red commit:** `c8de61c` (test — failing execution-view tests)
> **Impl commit:** `a71c2ed` (feat — Output rail + tool payloads)
> **Type:** renderer/TS only — no wire change, no schema change
> **Estimated effort (per prompt):** the observability foundation; ~1 session

---

> **⚠️ Backfill notice (rule 11 — honest provenance).** Stage A shipped at
> `a71c2ed` on 2026-06-02 **with no contemporaneous retrospective** — a
> bookkeeping gap the M08.8.V verifier flagged (V Decision 4: the
> "retro-presence = stage executed" cross-machine invariant requires this
> file). This retro is **reconstructed at the M08.8.G closeout (2026-06-05)**
> from the two commit records (red `c8de61c` + impl `a71c2ed`), the phase-doc
> Stage A spec, and the **independent** M08.8.V grounding of A
> (`execution_view.e2e.ts` 2/2 on the real Tauri app, 2026-06-05). The
> [LIVE] sections below are therefore reconstructed-from-evidence, not
> logged-in-the-moment — stated plainly so the reader does not read a
> live-observation confidence this file cannot carry. The product facts
> (what shipped, what the real app showed) are grounded by the commits + V;
> the process-friction detail is necessarily thinner than a live retro.

---

## Pre-flight (reconstructed from the commit record)

- [x] `CLAUDE.md` loaded; renderer-only stage (the A.7 prompt's scope locks: do not change the `agent_event` wire / `ipc.ts:641` / `App.tsx:143` / the backend emit; no schema change).
- [x] Read-first per the A.7 prompt: phase-doc Stage A, DESIGN.md (the 360px Inspector/Output rail + the node-kind two-axis model + the mono "instrument register"), TD-034, ADR-0021, `cluster-pattern.md`, `graphStore.ts` (the `stream_text` no-op @1734; tool nodes), `builderStore.ts` (the scoped `useTestGraphStore`), `TesterGraphPane.tsx`, `ipc.ts`/`App.tsx` (the live event flow).
- [x] Strict v1.8 two-commit TDD: the red commit `c8de61c` holds the tests; the impl `a71c2ed` touches no test file (`git diff c8de61c..a71c2ed -- '**/tests/**' '**/*.test.*'` empty — asserted in the impl commit body).

---

## [LIVE] (reconstructed) Friction / ambiguity / surface / protocol-drift / surprise events

The contemporaneous live log was not captured. Reconstructed from the commits, nothing surfaced as a Severity-≥3 friction event in the record (the impl landed in a single commit immediately after red, no fix-up commits between red and impl, no protocol-drift markers in the commit bodies). Known shape from the artifacts:

| Class | Reconstructed observation |
|---|---|
| Friction | None recorded ≥ Sev 2. The single largest design risk (one rail component must bind to **both** the live `useGraphStore` and the scoped `useTestGraphStore`) was handled structurally — the shared `createGraphStore` factory already gives both stores the same shape, so the `stream_text`→`outputBuffer` + `{input,output}`-on-node surfacing landed once and worked over both via a `store` prop on `InspectorPanel`. |
| Surprise (good) | The `AgentEvent` union already carried `text` / tool `input` / `output` — the A.2 "no schema change" claim held; the gap was purely surfacing (`stream_text` was a `graphStore` no-op, tool nodes dropped their payload). The "painted, not surfaced" diagnosis of TD-034 was exact. |
| Protocol | Strict-TDD two-commit invariant held by construction (impl touches no test file). Hard Rule 1 (no commit without approval) held — the stage surfaced for approval before the commits landed. |

---

## [END] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings |
|---|---|---|
| Closed on the real-app run (the M08.8 close bar) | `tests/e2e-tauri/execution_view.e2e.ts` (real Tauri app via tauri-driver, ADR-0021) — 2/2; re-confirmed by V 2026-06-05 | None propagated. The rail mounts over both the live and the scoped Tester store; the node two-axis kind/status tokens added to `styles.css` were the substrate B/B.fix then completed into the full Light-Instrument system. |

---

## [END] Three-axis scoring (reconstructed; conservative where the live log is absent)

- **Process:** **4/5.** The deliverable shipped exactly as specced (Output rail + clickable tool payloads + two-axis nodes + both-stores) with strict-TDD held and the real-app close observed. The one deduction is **process bookkeeping, not build quality**: the stage shipped with no retrospective, breaking the §19 retro-presence invariant — the exact gap this backfill closes (and the reason V's Decision 4 exists).
- **Product:** **5/5.** Renderer-only, no wire/schema change as locked; the `AgentEvent` payloads were surfaced, not re-plumbed; the real Tauri app showed the streamed text + the teal/green node + the click-to-payload (V-confirmed 2/2). TD-034's surfacing half is closed-observed in the running app — the foundation B–C are watched through.
- **Pattern:** **5/5.** The "painted-not-surfaced" pattern (a `stream_text` no-op + payload-dropping nodes) is the renderer sibling of M08.7's "painted-not-executed"; the shared-store-factory-binds-both-stores approach generalizes to every later rail/inspector surface.

---

## [END] Threshold evaluation

- **G1 (do-not-commit-until-approved):** held — surfaced before the commits.
- **G2 (no Sev-5 friction):** none recorded.
- **G3 (no unaddressed protocol drift):** the **retro-absence** is the one drift; addressed by this backfill + V Decision 4 (now a standing closeout check).
- **G4 (stage completed):** red `c8de61c` + impl `a71c2ed`; `execution_view.e2e.ts` real-app 2/2 (V-confirmed).
- **G6 (CI-parity):** the frontend gates ran with the §6 commands (prettier/eslint/tsc/vitest ≥80% + `test:e2e:tauri`); the impl commit body asserts the empty red→impl test diff.

**Outcome:** **Sound — with one process-bookkeeping miss (the absent retro, closed here).** The product is correct and real-app-observed; the only blemish is that this self-assessment is reconstructed rather than live.

---

## [END] Decisions for the next stage (carried forward at the time)

- **A's rail re-homed at B.** A mounted the Output/Inspector rail in the stop-gap `.graph-layout` (320px); B re-homes it into the spec'd 360px right rail of the three-pane shell (the B.2 deliverable).
- **A's node two-axis tokens completed at B/B.fix.** A added the kind/status/mono tokens the new surfaces used; B ported the full DTCG token system and B.fix restyled every surface to the mockup.
- **The retro-absence is a standing lesson.** Every stage — even a clean single-commit renderer stage — files its retrospective at the stage, not "later." V Decision 4 graduated this into the closeout's standing cross-machine "retro-presence = stage executed" check.

---

## [END] Sign-off

> Reconstructed self-assessment of M08.8 Stage A, authored at the M08.8.G closeout per `CLAUDE.md` §19 + the M08.8.V Decision 4 backfill instruction. The product facts are grounded by the commit record + the independent M08.8.V real-app grounding; the live-process detail is reconstructed and labeled as such (rule 11). Outcome: **Sound** (with the retro-bookkeeping miss closed by this file).

**Backfilled at:** 2026-06-05 (M08.8.G closeout).
