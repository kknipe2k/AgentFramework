# M03.5.A — Stage Retrospective

> **Stage:** M03.5 Stage `A` — see `docs/build-prompts/M03.5-pre-m04-prep.md`
> **Parent milestone:** M03.5 (pre-M04 prep, doc/protocol-only mini-milestone)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-07 ~mid-day
> **Session end:** 2026-05-07 ~mid-day (single short session)
> **Branch:** `claude/m03.5-pre-m04-prep` (parent-milestone feature branch; Stage B will commit on the same branch)
> **Starting commit:** `5366fee`
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 3.5 hours
> **Actual elapsed:** ~30 minutes (well under the conservative 3.5h estimate; doc-only stage with text-matching edits)

---

## Pre-flight (claimed before session start)

These were satisfied before code was written:

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response
- [x] All "Read first" files in the milestone prompt were read in the order listed (CLAUDE.md, STAGE-PROMPT-PROTOCOL.md, M03.5-pre-m04-prep.md, docs/gotchas.md, RETROSPECTIVE-TEMPLATE.md)
- [x] The deliverable was stated in 1–3 sentences before writing code (22 doc-only edits across 4 files per A.3)
- [x] Test plan section was N/A per `<test_plan_required>false</test_plan_required>` — verification commands ran post-implementation per A.4
- [x] The branch was correct (`claude/m03.5-pre-m04-prep`)
- [x] No uncommitted changes carried over (`git status` clean at session start)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Verification step (post-implement) | Initial PowerShell loop script ran via bash heredoc was mangled by bash variable expansion (`$checks`, `$_`, etc. expanded as bash variables before powershell.exe got the script). Worked around by running each check individually as `powershell.exe -NoProfile -Command "..."` with single-quote-bash / double-quote-PS escaping. Cost ~2 minutes. | A.4 verification commands assume native PowerShell session. The `<runtime_environment>` v1.3 tag in Stage B's deliverable directly addresses this — pre-flagging Windows + PowerShell + escaping conventions would have routed straight to per-command invocation. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc Edit 13 (`schemas/error.v1.json` `$id` field) vs `<gotchas>` trap #5 in same prompt | Phase doc Edit 13 prescribes verbatim `$id: "https://kknipe2k.github.io/AgentFramework/schemas/error.v1.json"`. The same prompt's gotcha trap #5 instructs me to "verify against an existing schema (e.g., schemas/event.v1.json) for the exact $id format before authoring." Verification revealed all 6 existing schemas use `https://schemas.aria-runtime.dev/<name>.v1.json` — not the kknipe2k.github.io URL. Phase doc verbatim text and own trap directly contradicted. | Surfaced to user via `AskUserQuestion`; user chose "Use schemas.aria-runtime.dev (match existing)". `error.v1.json` shipped with the consistent URL. | M03.5 Phase doc Edit 13 prescribed text was authored against an out-of-date / hypothetical $id base URL. The trap rescued the inconsistency, so the trap design itself worked. Underlying issue: no `$id` URL is documented in `schemas/README.md` as the canonical project base, so future authors will keep tripping this. **Decision for Stage B / M04**: file as a Stage B candidate-tag pattern — `<fan_out_grep pattern='"$id"' purpose="verify schema $id base URL pattern is consistent across schemas/*"/>` would have caught this pre-question. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Before any file edits, after reading existing schemas | The `$id` URL conflict between Phase doc Edit 13 verbatim text and trap #5's verification instruction | Yes — direct multiple-choice via `AskUserQuestion` with both options + tradeoffs labeled | Yes — surfaced before any edit landed; user resolved in one round; no rework needed |
| End of stage | All 22 verification checks pass + diff stat + draft commit message + "I will not commit until you approve" | (Pending — this is the surface that gates user approval) | Right per CLAUDE.md §8 — surface-then-approve flow |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The `$id` URL discrepancy was caught entirely by the prompt's own self-protective gotcha trap | The Phase doc was carefully drafted, but Edit 13's verbatim text used a base URL that doesn't match any existing schema. Trap #5 was the safety valve. | The strict reference-first + verbatim-prose pattern is fragile when verbatim text contains values that the rest of the codebase already pins. v1.3's `<fan_out_grep>` (per Stage B deliverable) is the right structural cure — the prompt would explicitly direct verifying the value pre-authoring. Validates Stage B's design. |
| All 22 Edit-tool old_string anchors were unique on first pass; zero `not unique`/`not found` errors | I was prepared to deal with substring collision via larger surrounding context, but the spec's structure made every anchor unambiguous | Confirms the M03 Phase-doc authoring discipline (line-numbered references + unique surrounding context citations) is durable. No protocol change needed. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard Rules stated at top; §8 commit workflow + §19 retrospective protocol applied without ambiguity |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 5 | The 5 read-first files + 4 read-reference files covered everything needed; no orphan questions |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Deliverable stated in one paragraph; test_plan_required=false honored — verification per A.4 ran post-implement |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 5 | One verification iteration; no self-correction needed (all 22 checks passed first attempt) |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No `git commit` invoked; surface flow about to fire |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | $id ambiguity was a genuine conflict between Phase doc and its own trap — escalated correctly. All 22 deterministic edits proceeded without escalation. |
| 7 | Was the human-direction load reasonable? | 5 | Single user question (the $id URL choice); resolved in one round |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | $id surface was a binary choice with labeled tradeoffs; final surface is the standard approval bundle per A.5 `<approval_surface>` |
| **Process axis total** | | **40/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | Doc-only edits; spec edits preserve existing voice (no marketing/emoji); schema uses snake_case for variant names per gotcha #33; one-line ⚠️ blockquotes consistent with existing spec convention |
| 2 | Are tests behavior tests, not tautology tests (§5)? | N/A | Doc-only stage; no tests added. Verification is structural (Select-String / Test-Path) per A.4 |
| 3 | Are public APIs documented with compile-checked examples (§6)? | N/A | No new public APIs; new `error.v1.json` is a JSON Schema documenting an existing serde-tagged enum |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All 22 edits per A.3 landed verbatim (1 with user-resolved $id swap matching existing schema convention) |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | Edits add explanatory ⚠️ notes that link to gap-analysis entries by milestone tag; gotcha entries each cite their source retro |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers, no AI usage hints, no premature abstraction; all inserts are explanatory prose with named sources |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | N/A | Doc-only stage — no source code touched, no coverage delta |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | N/A (proxy: 5) | Doc-only stage — no CI build delta. Local proxies that did run: prettier on `**/*.{ts,tsx,js,jsx,json}` clean; JSON validity check pass; markdown unchanged in count of headings/lists |
| **Product axis total** | | **25/25 across applicable rows** | (rows 2/3/7/8 N/A on doc-only stage; remaining 5/8 rows = 25/25) |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | The Phase doc applied the v1.2 template cleanly; the strict reference-first sections (`<deliverable ref=`, `<acceptance_criteria ref=`, `<scope_locks ref=`, `<commit_message ref=`) all resolved correctly to Phase doc sections |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | The $id ambiguity surfaced the same Pattern that v1.3's `<fan_out_grep>` (Stage B deliverable) is designed to address. The stage prompt had a `<gotchas>` trap that caught it, but the discovery was reactive (after I'd loaded existing schemas to double-check the format). A `<fan_out_grep>` slot would have made the verification proactive. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 5 | The 8 gotchas appended (#33–#40) graduate from per-stage M03 retros to durable docs/gotchas.md entries — exactly the v1.2 graduation pattern operating correctly |
| 4 | Did the time-box estimate match reality (within 2×)? | 4 | Estimated 3.5h; actual ~30 min. ~7× under-estimate. The estimate was deliberately conservative ("~3–5h actual" per Phase doc Estimation calibration) since this is doc-only with no test cycle. The actual / estimate ratio (0.14×) tracks M01–M03 estimation pattern (0.3×–0.7×). |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | One: when Phase-doc verbatim prose contains a hard-coded value (URL, version pin, identifier), the value should be cross-referenced against existing pins in the codebase. The trap caught it; the protocol slot for "verify these values before authoring" is exactly what Stage B's `<fan_out_grep>` will introduce. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | Yes — the strict reference-first + verbatim-prose discipline + 22 deterministic Select-String checks make this prompt highly reproducible. The $id ambiguity would surface the same way (the trap is the same). |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Stage B's v1.3 work directly addresses what surfaced here. Specifically: `<fan_out_grep>` for hard-coded-value verification; `<runtime_environment>` for Windows+PowerShell command escaping (the only friction event in this stage). Both are already in scope for Stage B. |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git status` shows 3 modified + 1 untracked; no `git commit` invoked
- [x] **G2: no Severity-5 friction events** — *evidence:* friction-events table has 1 row at severity 2; nothing higher
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* protocol-drift-events table is empty
- [x] **G4: the milestone or stage actually completed** — *evidence:* all 22 acceptance criteria from A.4 verified; uncommitted-awaiting-approval per the surface flow
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* lowest individual score is 4 (Axis 3 rows 2, 4, 5, 7); no rows below 3

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 40/40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 25/25 across 5 applicable rows (3 rows N/A on doc-only stage). Effective 100%; pass.
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 31/35
- [ ] **S4:** Time-box within 2× of actual — *actual:* estimated 3.5h, took ~0.5h, ratio 0.14× (massively under-estimated; doc-only stages should be estimated at the PR #48 ~1h baseline, not full-milestone heuristics)
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 0 severity-3 events; 1 severity-2 event

### Outcome

- [x] **Pattern is sound but rough.** All hard gates pass; 1 soft gate (S4 time-box) failed in the under-estimate direction. Address with a TEMPLATE.md / `docs/build-prompts/README.md` note that doc-only mini-milestones (M03.5-style) should estimate at PR #48 baseline (~1h per stage), not at parent-milestone scale. No drama; the under-estimate didn't cause friction.

Outcome routing: **proceed to Stage B with minor TEMPLATE updates noted in Decisions section** (apply at TEMPLATE/README level when convenient, not blocking Stage B start).

---

## [END] Coverage holdouts

N/A — documentation/schema-only stage; no source code touched; no coverage gates run.

---

## [END] Decisions for the next stage (Stage B — STAGE-PROMPT-PROTOCOL.md v1.3 iteration)

Specific changes to apply BEFORE Stage B opens, OR specific findings Stage B's v1.3 deliverable should carry into the worked example design:

### Inputs to Stage B's v1.3 protocol design (from this stage's experience)

- [ ] **The `$id` ambiguity validates the `<fan_out_grep>` design.** Stage B's `<fan_out_grep>` worked example (currently scoped to "all callsites of a Rust enum being renamed") should additionally include a worked example where `<grep>` is used to verify a hard-coded value against existing pins — e.g., `<grep pattern='"$id"' purpose="verify schema $id base URL pattern is consistent across schemas/*"/>`. This widens the tag's purpose from "rename consumer fan-out" to "value-consistency fan-out", which is the exact failure mode that almost shipped here. Recommend: in Stage B's §B.3 Tag 3 description, add a second worked example covering value-pinning verification alongside the rename-fan-out example.
- [ ] **The PowerShell-loop friction validates `<runtime_environment>`.** The friction event at verification time was a bash↔PowerShell escaping issue when running the Phase doc's PowerShell verification commands inside a Bash-tool wrapper. Stage B's `<runtime_environment>` tag with `<command os="...">` variants would have flagged Bash-incompatible PowerShell commands at prompt-authoring time. Recommend: in Stage B's §B.3 Tag 5 worked example (multi-OS variant), include a Windows-PowerShell-with-Bash-wrapper case explicitly — this is the actual environment build agents run in (PowerShell shell + Bash tool fallback for cross-stack work).
- [ ] **The Phase doc's verbatim-prose discipline + own trap was load-bearing.** The trap caught the discrepancy that the verbatim-prose approach almost shipped. This is a positive signal for the v1.2 strict-reference-first design; v1.3 should not weaken it. Recommend: in Stage B's §13 anti-patterns additions, add a positive-pattern note (or anti-pattern of its inverse) about the role of `<gotchas>` traps as the safety valve for verbatim-prose blind spots.

### `CLAUDE.md` updates

- [ ] None recommended at CLAUDE.md level. The findings here are protocol-level (Stage B deliverable) or template-level (below).

### `TEMPLATE.md` updates

- [ ] Note in `docs/build-prompts/TEMPLATE.md` (Estimation calibration section, if present): doc-only mini-milestones (M03.5-style) should estimate at the PR #48 ~1h-per-stage baseline, not the parent-milestone heuristic. Apply this at convenience; not blocking Stage B.

### Milestone document updates (this milestone's `M03.5-pre-m04-prep.md`)

- [ ] None — Stage A executed cleanly against the Phase doc as written. The $id surface was caught by the prompt's own trap; no Phase doc rewrite needed.
- [ ] However: when the M03.5-summary.md aggregates Stage A + B at end of Stage B, note the $id catch as an example of the "trap as safety valve" pattern.

### Protocol additions (Stage B's deliverable surface)

- [ ] **Carry both findings above into Stage B's v1.3 design.** Specifically:
  - `<fan_out_grep>` worked example should cover BOTH rename-consumer fan-out AND value-consistency fan-out.
  - `<runtime_environment>` worked example should include the realistic Windows-PowerShell-via-Bash-tool case, not just the bare-shell case.
  - `<gotchas>` traps explicitly framed as safety valves for verbatim-prose blind spots in `<deliverable>` reference content.

### Open issues to file

- [ ] None at this time. The findings are absorbed into Stage B's deliverable.

---

## [END] User-review notes

> The user reviews this retrospective alongside the diff stat + draft commit. If the user sees a discrepancy between Claude's self-assessment and observable evidence, they push back here and Claude revises.

User-review notes:

- (Empty until user reviews.)

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M03.5 Stage A, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch (`claude/m03.5-pre-m04-prep`); the PR drafts and pushes only at the end of Stage B.

**Claude:** I have completed M03.5 Stage A per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until Stage B.

**Surfaced at:** 2026-05-07 (single-session)
