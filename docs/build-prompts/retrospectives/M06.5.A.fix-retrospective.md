# M06.5.A.fix — Stage Retrospective

> **Stage:** M06.5 Stage `A.fix` — see `docs/build-prompts/M06.5-irl-fix.md`
> **Parent milestone:** M06.5 IRL fix cycle (M07 Stage A gating remediation; work-stage-class D.fix per ADR-0008 — no closeout, no gap-analysis entry)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-17
> **Session end:** 2026-05-17
> **Branch:** `claude/m06.5-irl-fix`
> **Starting commit:** `5b441ca`
> **Red commit:** `68b45ed` (standalone failing-tests commit)
> **Ending commit:** `uncommitted — awaiting final approval` (impl staged)
> **Estimated effort (per prompt):** 2–3 hours
> **Actual elapsed:** ~1.5 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in force (do-not-commit/push without approval; strict-TDD two-commit; no Co-Authored-By)
- [x] Read-first files read in order: CLAUDE.md, STAGE-PROMPT-PROTOCOL (via project memory), `docs/M06-irl-findings.md` (🔴-1), `docs/build-prompts/M06.5-irl-fix.md` (Background/Key constraints/Workflow/A.1–A.6), `docs/adr/0012`, ADR-0007 context (drone system-of-record, from ADR-0012 Related), gotchas #66/#80/#81 (via prompt), RETROSPECTIVE-TEMPLATE
- [x] Deliverable stated in 1–3 sentences + test plan in 4 bullets BEFORE code
- [x] User confirmed via red-phase approval (`AskUserQuestion`) before green
- [x] Branch correct: `claude/m06.5-irl-fix`
- [x] `git status` clean at start

### Pre-flight checks (prompt-mandated)

- [x] branch == `claude/m06.5-irl-fix`
- [x] `docs/M06-irl-findings.md` present on branch
- [x] `docs/adr/0012-single-source-session-db-path.md` present
- [x] `grep 'mcp.sqlite' src-tauri/src/main.rs` → line 262 (bug present before fix)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| write_failing_tests / red compile | Workspace lints are `-D warnings`; the unused `SESSION_DB_FILENAME` const failed `cargo test` compile until `#[allow(dead_code)]` was added to the const too (the stub fn already had it). One extra round. | The red_phase note says "stub … just enough to compile" — could note that `-D warnings` means dead-code allows are needed on **every** new unused production item, not just the stubbed fn. | 2 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| `M06.5-irl-fix.md` A.2/A.4 ("new test file in `src-tauri/tests/`") vs the actual `src-tauri` crate structure (binary-only, no lib target) + CLAUDE.md §5 archetype | A binary-only crate's `tests/` integration tests cannot reach private composition fns (`session_db_path`, `open_mcp_client`); the literal phase-doc instruction is structurally infeasible | Surfaced as an explicit decision via `AskUserQuestion`; user accepted the in-source `#[cfg(test)] mod tests` placement (the established CLAUDE.md §5 `commands.rs` seam-test archetype for this crate). Strict-TDD invariant adapted: `git diff <red>..<impl> -- src-tauri/src/session_db.rs` shows only the seam (const + fn) changed; the `mod tests` block is byte-identical red→impl. | Phase-doc detail; recorded here for M06.5 Cycle-2 (M06.6) input — the assembled-app-regression mandate is satisfiable in-source for binary crates, future fix-cycle phase docs targeting `src-tauri` should say "in-source seam test per §5" not "`tests/`" |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After red commit `68b45ed` | Cross-machine state + right-reason red proof + the phase-doc test-placement deviation, via `AskUserQuestion` (2 questions) | Yes — user approved red + accepted the deviation in one round | Yes — exactly at the protocol's `surface_for_red_approval` step, before any impl |

---

## [LIVE] Protocol drift events

None observed. The harness default "end commits with Co-Authored-By" was correctly overridden by CLAUDE.md / phase-doc "No Co-Authored-By" (session-URL + DCO footer only).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| `clippy --fix` rewrote `pub(crate)` → `pub` on the new seam (private module → `redundant_pub_crate`) and touched `session_db.rs` | The file also holds the test module; momentarily looked like an impl-touches-tests violation | It was not — the change was confined to the production const/fn lines; the `mod tests` block stayed byte-identical. Confirms the canonical fmt→clippy-fix ordering is correct and the in-source-seam adapted invariant must be checked by scoped diff, not by the `**/tests/**` glob (which is vacuous for binary crates) |

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4/§5/§8/§9 directly governed every step |
| 2 | "Read first" list oriented correctly? | 5 | ADR-0012 + findings 🔴-1 + main.rs citations were exact |
| 3 | Deliverable + test plan before code per §16? | 5 | Stated before the Write |
| 4 | Self-correct in ≤3 rounds per §7? | 5 | 1 round (dead-code allow on const) |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed local-only then surfaced; impl uncommitted awaiting approval; nothing pushed |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the phase-doc/structure conflict (user-domain-adjacent); proceeded on test content/seam form (delegated technical) |
| 7 | Human-direction load reasonable? | 5 | One approval interaction |
| 8 | Surfaces actionable? | 5 | Red surface had right-reason proof + the deviation, one-round approval |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | Path-agnostic seam, descriptive names, why-not-what doc comments |
| 2 | Behavior tests not tautology (§5)? | 5 | Asserts the observable assembled contract (path equality + cross-connection round-trip), not Registry-in-isolation (the Stage-V blind spot) |
| 3 | Public APIs documented? | 5 | Module + const + fn doc comments; rustdoc `-D missing_docs` green |
| 4 | Exactly promised, no creep/cuts? | 5 | Only the composition layer + ADR + CHANGELOG; Registry/runtime-mcp untouched; no 🟡/🟢 touched |
| 5 | Stranger-understandable? | 5 | Doc comment cites ADR-0012 + 🔴-1; the why is in the code |
| 6 | §9 anti-patterns absent? | 5 | No magic strings (one `SESSION_DB_FILENAME` const), no stringly-typed drift |
| 7 | Coverage measured ≥80%? | 5 | Workspace TOTAL **91.61%** line (gate exit 0); `session_db.rs` **100.00%** line/region |
| 8 | CI green across the matrix? | 4 | Predicted green (cross-platform `tempfile` + `Registry`; no cfg/platform code); CI confirms on push |
| **Product axis total** | | **39/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All template sections useful? | 5 | — |
| 2 | Sections missing? | 4 | A "structural-feasibility check on phase-doc test-file location" prompt note would have pre-empted the deviation surface |
| 3 | Gotchas generalize? | 4 | "binary crate ⇒ in-source seam tests, `**/tests/**` invariant is vacuous, use scoped diff" generalizes to any future `src-tauri` fix cycle |
| 4 | Time-box matched (within 2×)? | 5 | ~1.5h vs 2–3h estimate |
| 5 | Implicit protocol moments? | 4 | The adapted strict-TDD invariant for in-source seams was implicit; now made explicit here |
| 6 | Re-runnable by a fresh session? | 5 | Deterministic; the deviation is now documented |
| 7 | Anything to change before reuse? | 4 | Future `src-tauri`-targeting fix-cycle phase docs should specify in-source seam tests |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `68b45ed` committed local-only then surfaced; impl staged uncommitted awaiting this approval; **nothing pushed**
- [x] **G2: no Severity-5 friction** — max severity 2
- [x] **G3: no unaddressed protocol drift** — none
- [x] **G4: stage completed** — 3 regression tests red→green; ADR-0012 Accepted; CHANGELOG updated; grep confirms no stray production literal
- [x] **G5: scores ≥3 every row** — every row ≥4 across all three axes (lowest is 4)
- [x] **G6: CI-parity** — gates run with the exact CLAUDE.md §6 canonical commands; **zero divergent flags**. `cargo fmt --all` · `cargo clippy --fix --allow-dirty -p agent-runtime` · `cargo clippy --workspace --all-targets -- -D warnings` · `cargo test --workspace` · `cargo test --workspace --doc` · `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps` · `cargo deny check` · `cargo audit` · `cargo llvm-cov clean --workspace` then `cargo llvm-cov --workspace --ignore-filename-regex "src.main\.rs|generated" --fail-under-lines 80`. No `--skip`, no `--test-threads`, no `--features`, no `--target`, no env override. (`cargo audit` "17 allowed warnings" is the pre-existing gtk/atk baseline — no dependency added this stage.)

### Soft gates

- [x] **S1:** Process ≥30/40 — 40
- [x] **S2:** Product ≥32/40 — 39
- [x] **S3:** Pattern ≥25/35 — 31
- [x] **S4:** Time-box within 2× — ~1.5h vs 2–3h
- [x] **S5:** ≤3 Severity-3 friction — 0

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Workspace coverage 91.61% (gate exit 0).

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 91.61% workspace line (gate exit 0; ≥80). `src-tauri/src/session_db.rs` 100.00% line / 100.00% region (85 lines, 0 missed).
- **Exclusions added this stage:** None added this stage. (`src-tauri/src/session_db.rs` is NOT excluded — the seam + the 3 in-source tests are fully measured; the `AppHandle` wrappers `open_mcp_client`/`resolve_db_path` in `main.rs` remain the documented CLAUDE.md §5 tauri-shell holdout, already covered by the `src.main\.rs` regex term + the codecov tauri-shell patch-gate.)
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged).

### Per-package gates

None gated this stage (workspace ≥80 only per the stage prompt `<coverage_gate>`).

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `src-tauri/src/session_db.rs` | n/a (new) | 100% (seam: 1-line fn + const, fully exercised by the 3 tests) | new | — |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change needed; `.github/workflows/ci.yml` workspace term `src.main\.rs|generated` already covers the `main.rs` shell wrappers; the new seam is intentionally measured.

---

## [END] Decisions for the next stage (B.fix)

### `CLAUDE.md` updates

- [ ] None this stage (scope lock: no protocol edits in this fix cycle; recorded here as Cycle-2 input).

### Milestone document updates

- [ ] M06.5-irl-fix.md A.2/A.4 reference `src-tauri/tests/`; for any future `src-tauri`-targeting fix cycle, specify **in-source `#[cfg(test)] mod tests` seam test per CLAUDE.md §5** (binary crate has no lib target — `tests/` integration cannot reach private composition fns; the `git diff … '**/tests/**'` invariant is vacuous for binary crates, use a scoped `git diff … src-tauri/src/<seam>.rs` showing the `mod tests` block byte-identical instead). **Recorded for M06.6 Cycle-2; not acted on here per scope lock.**

### Protocol additions

- [ ] (Cycle-2) The strict-TDD invariant phrasing should have an explicit in-source-seam variant for binary crates.

### Open issues to file

- [ ] None.

---

## [END] Special-log (prompt-mandated)

- **Resolver form:** Extracted a shared path-agnostic helper `session_db::session_db_path(dir: &Path) -> PathBuf` + a `SESSION_DB_FILENAME` constant, rather than calling `resolve_db_path` directly. **Why:** `resolve_db_path` takes `&AppHandle<R>` (the Tauri shell wrapper — structurally untestable without a Tauri runtime; the CLAUDE.md §5 holdout). A path-agnostic helper that *both* `resolve_db_path` and `open_mcp_client` call makes the resolution **provably identical** AND testable with an injected `tempdir`, exactly the `*_with`/seam-vs-wrapper archetype. This is the form the phase doc A.3 explicitly offered as the preferred option.
- **Regression test asserts the assembled contract:** `registry_path_equals_drone_session_db_path` asserts the path **both call sites resolve** is byte-identical (via the shared seam both `main.rs` sites now call), NOT `Registry` in isolation against one path — that isolation test already passes upstream and is precisely the Stage-V blind spot. `add_server_then_list_round_trips_through_the_same_store` adds the cross-connection (separately-opened `Registry`) round-trip pinning ADR-0012's two-connection invariant.
- **Grep result:** Only the production literal at `src-tauri/src/main.rs:262` (+ the doc comment at `:240`) was the divergent registry path; both removed/corrected. Post-fix `grep -rn 'mcp.sqlite|mcp_servers.sqlite' src-tauri/ crates/` shows **no production occurrence** — remaining hits are `crates/runtime-mcp/tests/{registry.rs,client_lifecycle.rs}` which use `tempdir().join("mcp.sqlite")` as **isolated test-fixture filenames** for the path-agnostic Registry's own unit tests (not the composition layer; scope-locked: runtime-mcp untouched). Documented distinction, not a remaining defect.
- **src-tauri patch-gate / seam extraction:** The shared resolver IS the seam extraction that keeps the fix testable; no further extraction needed. `main.rs`'s `open_mcp_client`/`resolve_db_path` `AppHandle` wrappers stay the documented coverage holdout (CLAUDE.md §5, codecov tauri-shell patch-gate 50%); `session_db.rs` is measured at 100%.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> Claude's self-assessment of M06.5 Stage `A.fix`, authored per `CLAUDE.md` §19. User review and approval pending. This is a D.fix-class work stage (no closeout / no gap-analysis entry per ADR-0008 + CLAUDE.md §20); the M06.5 PR drafts at the end of Stage C.fix.

**Claude:** I have completed M06.5 Stage `A.fix` per the stage prompt. Red phase `68b45ed` was committed standalone and surfaced for (and received) red-phase approval; the impl is staged and uncommitted. I have NOT pushed. Surfacing the impl diff + gate results + this retrospective for final approval.

**Surfaced at:** 2026-05-17
