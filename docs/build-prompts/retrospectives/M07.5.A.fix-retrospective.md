# M07.5.A.fix — Stage Retrospective

> **Stage:** M07.5 Stage `A.fix` — see `docs/build-prompts/M07.5-tier-gate-fix.md`
> **Parent milestone:** M07.5 fix-cycle (work-stage-class D.fix per ADR-0008; closes M07.V 🔴 #1 backend)
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-20
> **Session end:** 2026-05-20
> **Branch:** `claude/m07.5-tier-gate-fix` (cut from `main` @ `5c74797`)
> **Starting commit:** `5c74797`
> **Ending commit:** `d78a150` (red `e6bf3ff` → impl `51ef6eb` → style `d78a150`) — awaiting final approval
> **Estimated effort (per prompt):** 5–7 hours
> **Actual elapsed:** ~1 focused session (within range)

---

## Pre-flight (claimed before session start)

- [x] `CLAUDE.md` loaded; §4 Hard Rules stated at the top of the first work response
- [x] All `<read_first>` files read in order (CLAUDE.md, STAGE-PROMPT-PROTOCOL, M07.V retro Finding #1, M07.5 phase doc, ADR-0017/0016/0014/0015, gotchas, RETROSPECTIVE-TEMPLATE)
- [x] Deliverable stated in 3 sentences + test plan in 5 bullets before code
- [x] Branch correct (`claude/m07.5-tier-gate-fix`); pre-flight checks passed (ADR-0017 Proposed present; `tier_gate` uncalled inside `import_artifact_with` confirmed live)
- [x] `git status` clean at start (only untracked `.claude/scheduled_tasks.lock`, not in scope)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity |
|---|---|---|---|
| green-phase, clippy gate | Adding `share_provenance` to `TierReview` grew `ImportError` past the `result_large_err` 128-byte threshold (5 fns) **and** created `large_enum_variant` on the new `ImportOutcome` — 6 clippy errors the phase-doc type definitions did not anticipate. Fixed by boxing `TierReview` in `ImportError::TierReviewRequired` + `ImportOutcome::Pending` (deref coercion kept it ripple-free). | A.3.1 should have shown `TierReviewRequired(Box<TierReview>)` / `Pending { review: Box<TierReview> }` — growing an error-carried struct past the size thresholds is a foreseeable clippy cascade. | 3 |
| green-phase, clippy/Tauri | Phase doc A.3.2 specified `cancel_pending_import` as `pub fn` (sync) returning `()`. Sync → clippy `needless_pass_by_value` on the `String`/`State` params; switching to `async` → Tauri "async commands with reference inputs must return `Result`". Resolved as `async fn -> Result<(), CmdError>` (consistent with every other command in the file). | A.3.2 should have shown `cancel_pending_import` async returning `Result<(), CmdError>` — a sync Tauri command taking `State` cannot pass clippy as written. | 2 |
| green-phase, clippy gate | `import_artifact` gained an 8th parameter (`pending: State`) → `too_many_arguments` (8/7). Resolved with a `reason`-documented `#[allow]` (the `import_artifact_with` precedent). | Minor — foreseeable from "add a `State` param"; A.3.2 could have noted the `#[allow]`. | 1 |
| pre-red-commit | `cargo fmt` was not run before the red commit, so it reflowed two A.fix test bodies post-red → a separate `style` commit was needed. This is the standard M07 test→impl→style shape, but pre-fmt-ing red would have avoided the third commit. | Pre-flight could note "run `cargo fmt` on new test code before the red commit". | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| M07.5 phase doc `<construction_reachability_check>` vs `src-tauri/src/main.rs` | The check said manage `PendingImportState` "at main.rs ~296 alongside the existing registry manage" — but that line is inside `open_mcp_client`, which early-returns `None` when MCP setup fails, leaving `PendingImportState` unmanaged and the import command trio panicking at dispatch. | Placed `app_handle.manage(PendingImportState::default())` **unconditionally** in the `.setup()` closure (alongside `global_budget`) — `cancel_pending_import` then works even when MCP setup is unavailable. Deliberate technical decision per `CLAUDE.md` §12. | Phase-doc construction-check hint was slightly off; noted for B/C.fix authors. |
| Phase doc A.3.1/A.3.2 type definitions vs the clippy gate | The `TierReview` / `ImportError` / `ImportOutcome` / `cancel_pending_import` shapes shown in the phase doc do not pass `clippy --workspace -- -D warnings` as written (size thresholds + sync-command lint). | Owned the technical fixes (boxing, async signature) per `CLAUDE.md` §12; behaviour and wire shape unchanged. | Phase-doc code blocks are illustrative, not gate-verified — green-phase iteration absorbed it within the self-correction budget. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After the red commit (`e6bf3ff`) | Red-phase surface: cross-machine state, the 4-file diff, confirmed 7-failure red class (5 runtime-main + 2 commands.rs), the `warnings=deny`-forces-scaffolding note, the predicted impl-commit shape. | Yes — concrete failure list + the right-reason explanation. | Yes — before any green-phase code, as the strict-TDD red gate requires. |
| End of green phase (this surface) | Final surface — three-commit log, strict-TDD invariants, gate results, 🔴 #1 closure proof, retrospective. | — | — |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| — | None observed. Do-not-commit-until-approved held (red committed locally then surfaced for red-phase approval per `CLAUDE.md` §6 strict-TDD; nothing pushed). No gate skipped, no `--no-verify`. The impl commit modified zero test files. | n/a | — |

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The impl (green) commit is a ~10-line behavioural delta; almost all of the new code landed in the red commit. | `warnings = "deny"` (workspace) means a partial/`todo!()`-stub production scaffold does not compile (dead-code / unused-param). So the `ImportOutcome` enum, `PendingImport`, `PendingImportState`, the command trio, `commit_import`, `complete_import_with` all had to be compile-complete in the red commit. The genuinely-deferred behaviour was a single function tail (`import_artifact_with` calling `tier_gate`). | For a `warnings=deny` workspace, strict-TDD on a signature-changing stage naturally produces a large red commit + a small impl commit. The strict-TDD invariant (impl touches no test files) is still cleanly satisfied — and is in fact *easier* to satisfy this way. Worth noting in `STAGE-PROMPT-PROTOCOL` for future binary-crate `.fix` stages. |

---

## [SPECIAL LOG] Stage-required confirmations (per `<retrospective_requirements>`)

- **Assembled-composition regression confirmed.** `reject_rolls_back_lock_and_registry` drives the assembled `import_artifact_with` composition (a Novice `ArtifactKind::McpServer` import), asserts `ImportOutcome::Pending`, then asserts **no `skills.lock` entry** AND **zero `McpRegistry` upserts**; after dropping the `PendingImport` ("Reject") it re-asserts both stores empty. It is **not** `tier_gate` in isolation (the M07.V Stage-V blind spot, gotcha #82) — it failed on the red commit precisely because `import_artifact_with` installed unconditionally.
- **`commit_import`** is a private fn in `crates/runtime-main/src/import/mod.rs` shared by the inline Promoted path (`import_artifact_with`'s `Ok` arm) and the deferred Novice path (`complete_import_with`). It orders **`install_with` (lock write) BEFORE `reg.upsert`** — the more recoverable partial state (locked-but-unregistered reconciles on re-import).
- **ADR-0014 lock-on-first-install preserved.** `install_with` → `skills_lock::write_entry` runs exactly once, inside `commit_import`, at true install — inline for Promoted, on confirm for Novice. The lock content / schema / SRI hash are unchanged (no `schemas/skills-lock.v1.json` change).
- **Strict-TDD invariant — both directions.** `git diff e6bf3ff..51ef6eb -- 'crates/runtime-main/tests/**'` is EMPTY (verified). `git diff e6bf3ff..51ef6eb -- src-tauri/src/commands.rs` changes only production code (hunks at lines 1093/1225/1287/1352); the `#[cfg(test)]` block (line 1375+) is byte-identical red→impl (the binary-crate variant).
- **B.fix scope untouched.** `EnforcerGate`, the `NetworkGate` trait, `import/fetch.rs`, `host_of` — all unchanged. `import_artifact_with`'s `gate: &dyn NetworkGate` parameter is exactly as on `main`. The SSRF egress hardening (CQ-M07-1) is Stage B.fix.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation? | 5 | §4/§6/§8 drove the strict-TDD two-commit flow and the gate ordering. |
| 2 | Did the `<read_first>` list orient correctly? | 5 | M07.V retro Finding #1 + ADR-0017 fully specified the design. |
| 3 | Deliverable + test plan stated before code (§16)? | 5 | Both stated; pre-flight checks run. |
| 4 | Self-correct in ≤3 rounds (§7)? | 4 | The clippy cascade took ~2 focused rounds (runtime-main box fixes, then agent-runtime command fixes) — within budget. |
| 5 | Do-not-commit-until-approved held? | 5 | Red committed locally → surfaced → approved → green. Nothing pushed. |
| 6 | Escalated/proceeded correctly? | 5 | Owned the boxing / async-signature technical fixes (§12); surfaced only at the red and final gates. |
| 7 | Human-direction load reasonable? | 5 | One approval between red and green; no sub-step consulting. |
| 8 | Surfaces actionable? | 4 | Red surface carried the confirmed failure class + the impl-shape prediction. |
| **Process axis total** | | **38/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Code matches `CLAUDE.md` §9 style/naming? | 5 | snake_case, doc comments explain *why*; `#[allow]` is `reason`-documented. |
| 2 | Behavior tests, not tautology (§5)? | 5 | `reject_rolls_back_lock_and_registry` asserts observable store state through the assembled pipeline. |
| 3 | Public APIs documented w/ compile-checked items? | 5 | `RUSTDOCFLAGS=-D missing_docs cargo doc` clean. |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | A.3.1 + A.3.2 delivered; B.fix surface untouched; no 🟡 / Reinstall / TD work pulled in. |
| 5 | Stranger-readable? | 5 | Doc comments + the ADR-0017 cross-refs explain the lifecycle. |
| 6 | §9 anti-patterns absent? | 4 | `#[allow]`s present but justified (Tauri param count) with `reason`, not ad-hoc. |
| 7 | Coverage measured ≥ gate? | 5 | workspace 92.30% ≥80; runtime-main 96.71% ≥95; `import/mod.rs` 96.37% (up from 94.77% M07.C baseline). |
| 8 | CI green predicted across the matrix? | 4 | Rust gates all green locally with CI-parity commands; cross-OS predicted green (no OS-specific code; injected-seam tests). |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all template sections useful? | 4 | All used; the special-log section carried the stage-required confirmations. |
| 2 | Sections needed but missing? | 4 | A "phase-doc code blocks are illustrative, not gate-verified" caveat would help. |
| 3 | Gotchas local or generalize? | 3 | "Growing an error-carried struct past `result_large_err`/`large_enum_variant` thresholds" generalizes — candidate `CLAUDE.md` §15 gotcha. |
| 4 | Time-box matched (within 2×)? | 5 | Within the 5–7h estimate. |
| 5 | Implicit protocol moments? | 3 | The `warnings=deny` ⇒ large-red / small-impl shape for binary-crate strict-TDD stages is implicit; worth writing down. |
| 6 | Re-runnable by a fresh session? | 5 | The phase doc + ADR-0017 fully specify the design; clippy iteration is deterministic. |
| 7 | Anything to change before reusing the format? | 4 | Phase-doc type blocks should be gate-checked (boxing, async signatures) before authoring. |
| **Pattern axis total** | | **28/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — red `e6bf3ff` committed locally and surfaced for red-phase approval; green committed after the user approved "proceed to green phase"; nothing pushed.
- [x] **G2: no Severity-5 friction** — max severity 3 (the clippy cascade).
- [x] **G3: no unaddressed protocol drift** — drift table empty.
- [x] **G4: stage completed** — red `e6bf3ff` + impl `51ef6eb` + style `d78a150`; all A.4 acceptance criteria met; full Rust gate suite green.
- [x] **G5: every axis row ≥3** — lowest rows are 3 (Pattern #3, #5); none below.
- [x] **G6: CI-parity** — gate commands run verbatim from `CLAUDE.md` §6 / the phase-doc `<coverage_gate>`: `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo test --workspace`; `cargo test --workspace --doc`; `RUSTDOCFLAGS="-D missing_docs" cargo doc --workspace --no-deps`; `cargo audit`; `cargo deny check`; `cargo llvm-cov --workspace --ignore-filename-regex "src.main\.rs|generated" --fail-under-lines 80`; `cargo llvm-cov --package runtime-main --ignore-filename-regex "src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs" --fail-under-lines 95`. No divergent flags. `cargo llvm-cov clean --workspace` run before the coverage gates (gotcha #81). Frontend gates not run — A.fix touches no `src/` renderer file; the renderer is Stage C.fix.

### Soft gates

- [x] **S1:** Process 38/40 ≥30
- [x] **S2:** Product 38/40 ≥32
- [x] **S3:** Pattern 28/35 ≥25
- [x] **S4:** Time-box within 2× (within the 5–7h estimate)
- [x] **S5:** ≤3 Severity-3 friction events — 1 (the clippy cascade)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage B.fix.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 92.30% line
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs`, `generated`

### Per-package gate — `runtime-main` (≥95% line)

- **Coverage actual this stage:** 96.71% line
- **Exclusions added this stage:** None.
- **Exclusion list as of stage end:** `src.main\.rs`, `generated`, `src.providers.anthropic\.rs`, `src.drone_ipc.connection\.rs`, `src.sandbox_ipc.connection\.rs`, `src.import.fetch\.rs`

### Per-module baselines

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/import/mod.rs` | 94.77% (M07.C) | 96.37% | ↑ | new lifecycle-split code fully exercised by the 4 assembled tests |

### CI workflow drift check

- [x] No `--ignore-filename-regex` change this stage; the §6 / `codecov.yml` / `ci.yml` exclusion sets are unchanged. No `docs/coverage-policy.md` entry needed (no threshold/exclusion moved).

---

## [END] Decisions for the next stage (Stage B.fix)

### `CLAUDE.md` updates

- [ ] Consider a §15 gotcha: "Growing a struct carried inside an error or outcome enum can push it past clippy `result_large_err` (128 B) / `large_enum_variant` thresholds — box the payload; deref coercion keeps read sites unchanged." (Surfaced here; B.fix adds `EgressReject` to the error surface — same risk.)

### Phase-doc / protocol notes for B.fix

- [ ] B.fix also edits `crates/runtime-main/src/import/mod.rs`. `TierReview` is now boxed inside `ImportError::TierReviewRequired` and `ImportOutcome::Pending` — do not un-box it.
- [ ] B.fix adds `EgressReject` + reshapes `ImportError` / the `Fetcher` trait — check `result_large_err` / `large_enum_variant` early (run clippy in the green phase's first iteration, not last).
- [ ] `cancel_pending_import` shipped as `async fn -> Result<(), CmdError>` (not the phase-doc's sync `pub fn -> ()`). Transparent to the renderer (JS sees a Promise either way) — C.fix's IPC wrapper needs no special handling.
- [ ] `PendingImportState` is managed **unconditionally** in `main.rs`'s `.setup()` closure (not inside `open_mcp_client`). B.fix's `Resolver`/egress wiring touches the `import_artifact` call site — leave the `PendingImportState` manage placement as-is.

### Open issues to file

- [ ] None blocking. The four M07.V 🟡 carry-forwards + Reinstall round-trip remain M08 scope (untouched, as scoped).

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M07.5 Stage `A.fix`, authored per `CLAUDE.md` §19. User review and approval pending.

**Claude:** I have completed M07.5 Stage A.fix per the stage prompt. The retrospective above reflects my honest self-assessment. The three commits (`e6bf3ff` red, `51ef6eb` impl, `d78a150` style) are on `claude/m07.5-tier-gate-fix` locally; nothing is pushed. I am surfacing this retrospective + the diff + the gate results for final approval. I will not push until you approve.

**Surfaced at:** 2026-05-20
