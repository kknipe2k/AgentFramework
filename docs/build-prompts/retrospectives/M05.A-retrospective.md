# M05.A — Stage Retrospective

> **Stage:** M05 Stage `A` — §4b Gap Detection wiring (framework_loader + request_capability + M04.V carry-forwards). See `docs/build-prompts/M05-gap-capability.md` §A.
> **Parent milestone:** M5 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-13
> **Session end:** 2026-05-13
> **Branch:** `claude/m05-gap-capability` (parent-milestone feature branch; first work stage)
> **Starting commit:** `45adbec` (M05 phase doc merge into main)
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 6 hours
> **Actual elapsed:** ~3.5 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded (auto-loaded by Claude Code) and the §4 Hard Rules were stated at the top of the first response
- [x] All "Read first" files in the milestone prompt were read in the order listed
- [x] The deliverable was stated in 1–3 sentences and the test plan in 3–5 bullets BEFORE writing code
- [x] User confirmed the deliverable + test plan before code began — implicit approval via stage-prompt receipt; no clarification questions raised
- [x] The branch was correct per the milestone prompt (`claude/m05-gap-capability`; the prompt's pre-flight gate text named `claude/m05-gap-capability-phase-doc` which is the already-merged phase-doc PR branch — flagged the typo and proceeded on the milestone branch)
- [x] No uncommitted changes carried over from a prior session (only the untracked `ci-failures.txt` predates this session)

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Pre-flight | Stage prompt's `<pre_flight_check>` named branch `claude/m05-gap-capability-phase-doc` but that's the already-merged phase-doc sub-branch; the actual milestone work branch is `claude/m05-gap-capability` per CLAUDE.md §8 branch convention | Pre-flight should reference the milestone work branch (`claude/m05-gap-capability`), not the phase-doc PR sub-branch. Cosmetic typo. | 1 |
| Schema audit | Phase doc claimed three existing `*_missing` variants ("only the three existing *_missing variants should match"); grep showed only TWO (`skill_missing`, `tool_missing`). Phase doc deliverable section said "enrich `skill_missing` + `tool_missing` + `mcp_missing` + new `agent_missing`" — implying mcp_missing also exists. Surfaced before writing code; reconciled as: add BOTH `mcp_missing` AND `agent_missing` variants (phase doc factual error). | The `<schema_audit>` slot exists exactly for this — the survey caught the divergence cleanly. The phase doc itself should be the source-of-truth on existing variants; failing that, an authoring-time `git grep` of `*_missing` against the schema file would have caught it. | 2 |
| Phase-doc inventory | Phase doc claimed `tests/unit/lib/graphStore.test.ts exists`; actual path is `tests/unit/graphStore.test.ts` (no `lib/` sub-dir). Discovered when wiring the new graphStore tests; corrected without rework. | The `<phase_doc_inventory_audit>` slot should fail-loudly on path mismatches. v1.4 added the slot; v1.5 should warn on first-discovered mismatch and refuse to proceed if material. | 2 |
| typify-generated newtype access | `AgentId(::std::string::String)` has private tuple field — `agent.id.0` doesn't compile. Resolved via `Deref<Target=String>`-coerced `&agent.id` → `&str`. Test fixtures rewritten to use `serde_json::from_value` rather than the typify-generated builders (builders require knowing private-field paths). | The phase doc's A.3.2 walker example used `agent.id.0`; should use the public access pattern + reference the `serde_json::from_value` test-fixture approach. Logged for the phase-doc-edit pre-flight. | 2 |
| Generated TS prettier drift | `src/types/budget.ts` produced quote-style drift on regeneration (M04.F oversight — file missing from `.prettierignore` + `eslint.config.js`). Added incidentally per gotcha #44 pattern. | M04.F closeout's regenerated TS targets check should have caught this. Surfaced as a low-stakes additive fix (one line to each ignore list). | 1 |
| Hand-rolled vs generated event types | The codebase maintains BOTH `runtime_core::event::AgentEvent` (hand-rolled canonical, with `HitlTriggerRef`-style enums) AND `runtime_core::generated::event::AgentEvent` (typify-generated from schema). The walker initially used generated types; had to refactor to hand-rolled canonical (the actual public re-export). Added `GapSeverityRef` + `GapSourceRef` hand-rolled enums to event.rs mirroring the generated set (matches the M04.D `HookCategoryRef` pattern). | The phase doc's A.3.2 walker example imports from `runtime_core::generated::event::{GapSeverity, GapSource, SuggestedAction}` — would land if those types were used downstream, but the canonical re-exports are at `runtime_core::event::*`. Phase doc should reference the canonical re-exports. | 3 |
| clippy doc-link warnings | Several `[`HookCategoryRef`]` / `[`Framework`]` / `[`Agent`]` intra-doc links in new modules failed `rustdoc::broken-intra-doc-links` because those types live in other crates (no `use` statement makes them in-scope at the doc location). Resolved by switching to bare backticks. Familiar M04.D/E/F gotcha #55 pattern. | The phase doc could pre-warn for new modules in `runtime-main`: intra-doc links to types from `runtime_core::generated::*` require either explicit imports or bare backticks. Already in gotcha #55. | 2 |
| Windows-local llvm-cov subprocess flake | `cargo llvm-cov -p runtime-main` flaked on `drone_ipc_loopback.rs` first run; mitigated per gotcha #56 with `--test-threads=1 --skip recovery_lifecycle --skip plan_recovery --skip drone_reconnect`. Reproduces M04.D+E+F friction. | The phase doc's gates section should reference gotcha #56's mitigation incantation. Already in CLAUDE.md §5 + the existing M04 carry-forward; not a surprise this stage. | 2 |

If no friction events occurred during this session, write **"None observed"** below the table header.

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Spec §4b L1650 vs phase doc A.3.3 | Spec §4b restricts `request_capability` to `capability_kind: enum["tool", "skill"]` (2 kinds); phase doc A.3.3 expanded to 4 kinds (tool/skill/mcp/agent). Code-shape decision: follow phase doc per CLAUDE.md §12 (own technical decisions); surface for spec reconciliation. | Implemented 4-kind support per phase doc; flagged in `[END] Decisions` for maintainer to reconcile spec text. Analogous to M04.V Decision 2 (spec §4a `hook_*` vs code `verify_*`). | Spec §4b text (recommended) — update enum to 4-kind set to match v0.1 schema + walker emission surface. |
| Spec §4b L1620 ("Each layer emits `tool_missing` or `skill_missing`") vs `mcp_missing` mention in spec line 26 | Spec §4b explicitly enumerates 2 event kinds for the Layers but doesn't mention `mcp_missing` at all in the §4b detection mechanisms section. Phase doc M05-gap-capability.md L26 says "M05 only emits `mcp_missing` events when the framework references an MCP server that isn't installed" — but v0.1 framework schema has no MCP-server declaration field. | Implemented `mcp_missing` as a variant in the schema + walker `GapKind` enum (for future M06 emission) but Layer 1 walker does NOT emit `mcp_missing` in v0.1 (no source data); only `request_capability` emits it this milestone. Documented in walker.rs module docstring. | Spec §4b severity matrix (line 1671) — add `mcp_missing` rows for both layers, OR explicitly note "v0.1 emits `mcp_missing` only from Layer 2 until M06 framework JSON declares MCP servers." |
| ContextType reconcile direction | Spec §2b L1136 says "spec's artifact-source set is the default unless operational-context variants prove irreplaceable in M04 signal writes." M04 didn't write code that exercises the variants (M04 gap-analysis L1157 confirms: deferred). Per gotcha trap in stage prompt: spec wins absent maintainer signal. | Adopted spec set: `Skill / Framework / Code / Search / Verify / Commit / Subagent`. Updated round-trip tests with reasonable mappings (AgentLoop → Subagent; SkillLoad → Skill; ToolInvoke → Code; HookExecute → Verify; PlanCreate → Framework; HitlPrompt → Framework; SessionLifecycle → Framework). M02 + M03 + M04 carry-forward CLOSED. | None — this is the intended reconciliation per spec §2b. Surface in retro for maintainer review. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| Pre-code (after read-first sweep) | Stated Hard Rules + Deliverable + Test plan + four authoring decisions (mcp_missing addition; 4-kind request_capability vs spec's 2; ContextType direction; M04.V Decision 2 no-code-change) | Yes — user could have intercepted any of the four authoring decisions before code landed. None were intercepted (proceed default per CLAUDE.md §12). | Right — surfaced before any test or implementation file landed. |
| End-of-stage (this surface) | Cross-machine state + diff stat + gate results + retrospective + draft commit + four authoring decisions for retroactive review | Yes — user can approve / pushback per CLAUDE.md §8. | Right — at stage end per the protocol. |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|
| §4 Hard Rule 5 (no hand-rolled types that should be generated) | The hand-rolled `runtime_core::event::AgentEvent` already exists alongside the schema-generated `runtime_core::generated::event::AgentEvent` per the existing M04 pattern; I extended the hand-rolled version. Not a drift — this is the established `HitlTriggerRef` precedent in event.rs. Documented in the event.rs new `GapSeverityRef` / `GapSourceRef` doc comments. | N/A — not a drift; the pattern is established and CLAUDE.md §14 covers generated-from-schema; the hand-rolled canonical re-export is the public surface (M04 precedent for `HitlTriggerRef`, `HookCategoryRef`, `OnFailureRef`, `RailPolicy`). | None. |

If none, write **"None observed"** below the table header.

If any drift event occurred, **the user is alerted immediately during the session**, not at the end.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| Phase doc claimed "three existing *_missing variants" but only TWO exist (`skill_missing` + `tool_missing`); also claimed `tests/unit/lib/graphStore.test.ts` exists at a path that doesn't exist (actual: `tests/unit/graphStore.test.ts`). | The phase doc explicitly applies v1.4 `<schema_audit>` + `<phase_doc_inventory_audit>` slots, yet two factual errors made it through phase-doc authoring. Reinforces gotcha #41 (Phase doc claims must be grep-verified at authoring time). The v1.4 inventory_audit slot exists; this stage's surface confirms it caught the divergence quickly (~5 min during pre-flight), but at the cost of a Sev-2 friction event. | Phase-doc authoring should run the v1.4 slot checks BEFORE the doc PR opens — not just at stage-execution time. The retrospective protocol could add an explicit "phase-doc authoring pre-PR audit" pass. Already implied by gotcha #41 / #42; making it explicit in `STAGE-PROMPT-PROTOCOL.md` Authoring Rules would help. |
| The hand-rolled vs generated `AgentEvent` duality didn't trip a discoverable error until I tried to construct events from the walker. The two types share variant names; serde-wire-compatible but Rust-type-incompatible. | The canonical re-export at `runtime_core::AgentEvent` resolves to the hand-rolled one (per `lib.rs` `pub use event::*`). The generated one is `runtime_core::generated::event::AgentEvent`. The phase doc's A.3.2 code example used the generated path; the actual consumers (renderer event pipeline, drone projector, SDK) use the canonical re-export. | Phase doc code examples should be grep-verified against `pub use` chains in the consumer crate's `lib.rs`. Adding "imports must resolve to the canonical re-export, not the generated submodule" to gotcha #41's grep-verify rule would close this. |
| The walker tests work cleanly with `serde_json::from_value` against minimal JSON fixtures — building Framework via typify-generated builders required threading dozens of newtype constructors. The JSON-based test-fixture pattern is much simpler. | typify generates `Default::default()` constructors that the builder API depends on, but many required-field newtypes (AgentId, FrameworkName, SemVer) don't have a Default. Builders are great for production code (round-trip from JSON); for tests, serde_json's lenient deserialization is the right tool. | Could be a future gotcha: "For typify-generated complex types in tests, prefer `serde_json::from_value(json!({...}))` over the `T::builder()` API; mirrors `examples/aria/framework.json` shape and dodges private-newtype-field issues." |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | §4 hard rules + §8 PR workflow + §12 operating mode + §16 session-start checklist all load-bearing. No ambiguity surfaced from CLAUDE.md itself. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | Read-first list named the right files; the v1.4 `<read_reference>` + `<read_prior_milestones>` + `<gotchas>` slots gave excellent pre-code context. Lost a point for the two phase-doc factual errors (mcp_missing claim + graphStore test path) that the read-first reading would have caught at authoring time. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated 4-axis deliverable, 5-bullet test plan, and 4 explicit authoring decisions before any code landed. Surfaced as a single message. |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | clippy round 1 (5 doc-markdown + 1 doc-list errors) → fix → round 2 (1 doc-lazy-continuation) → fix → round 3 (3 implicit-clone + 2 significant-drop + 1 needless-pass-by-value + 2 first-paragraph-too-long + 1 doc-link broken) → fix → clean. 3 self-correction rounds; budget hit but not exceeded. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made. Surfacing for approval per CLAUDE.md §8. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | 4 authoring decisions surfaced before code; none escalated to user (per §12 "own technical decisions"). All 4 are noted in retrospective for retroactive review. No false-positive escalations. |
| 7 | Was the human-direction load reasonable? | 5 | Zero clarifying questions to the user during execution. Outcome-only surface at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | This retrospective + draft commit + diff stat + gate results assembled in the standard shape. |
| **Process axis total** | | **38/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case Rust + camelCase TS + PascalCase types/components + kebab-case .md files. No marketing language. Comments explain why, not what. |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | Walker tests assert observable Gap shape + severity + suggested_action non-empty; request_capability tests assert event-wire variant routing; graphStore tests assert applyEvent mutations; GapNode tests assert DOM attributes (data-kind, data-severity, aria-label, suggested_action text). Per gotcha #66 — every test asserts observable contract. The M04.V Decision 1 regression test asserts the actual DOM output of the fallback (not just the JS-layer logic). |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | All new public APIs have rustdoc; some have inline `# Errors` sections. No `# Examples` blocks with compile-checked code yet (could add for `load_and_validate` + `handle_request_capability`; v1 ships without to keep retrospective time bounded). |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 5 | All 7 deliverables from phase doc A.1 implemented (or surfaced as no-code-change for #7 per design). M04.V Decision 1 absorbed. The two additive fixes (budget.ts ignore lists; mcp_missing schema variant) were necessary corrections, not scope creep. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | walker.rs module docstring + each test name describes the contract. The `gap-node-${gapId}` testid + ARIA labels self-document. Spec §4b cross-refs throughout. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No `any` in TS (`GapNodeData['kind']`); no `_with` shadowing; one `#[allow(clippy::needless_pass_by_value, reason = "...")]` with explicit rationale per gotcha #62; no premature abstraction. |
| 7 | Is coverage ≥80% / 100% on safety primitives — measured, not estimated? | 5 | Measured: workspace 94.06%; runtime-drone 95.79%; runtime-main 96.94%; framework_loader/walker.rs 98.18%; framework_loader/mod.rs 99.17%; sdk/request_capability.rs 98.33%. All ≥ gates. |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | Local Windows green on all gates (fmt + clippy + test + doc + audit + deny + llvm-cov + prettier + eslint + tsc + vitest + schema-drift + prompt-validator). CI runs against PR; this stage is uncommitted so CI hasn't run yet. Lost 1 point for "uncommitted so CI not exercised yet" — gate runs on PR open. |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all v1.4 protocol slots useful, or were any dead weight? | 5 | `<schema_audit>` caught the mcp_missing phase-doc error; `<phase_doc_inventory_audit>` caught the graphStore test path drift; `<architecture_check>` referenced the in-process emitter pattern (matched). `<fan_out_grep>` surveyed `tool_missing|skill_missing|mcp_missing` callers cleanly (graphStore was the only one). `<runtime_environment>` Windows pin held. None dead weight. |
| 2 | Were any sections needed but missing from the protocol? | 3 | Phase-doc-edit pre-flight (CLAUDE.md §8 phase-doc pre-flight) is in place but didn't catch the mcp_missing phase-doc factual error before authoring — because phase-doc authoring happened on a different branch. Suggests: "authoring-time `<schema_audit>` self-run before opening phase-doc PR" should be encoded somewhere. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | The "use serde_json::from_value for typify-generated complex types in tests" pattern is M05.A-discovered but forward-applicable to M06+. Candidate for gotcha graduation. The "hand-rolled canonical AgentEvent vs generated AgentEvent" trap is M04+ established but the phase-doc-example-uses-wrong-path subset is M05.A-new. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 6h, actual ~3.5h (0.58×). Within 2×; on the faster side of M04's calibration data (M04 mean ratio 0.55×). Pattern: novel-architecture stages stay close to estimate when the read-first context is comprehensive. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | "Schema enrichment requires regenerating types BEFORE writing dependent test fixtures" — implicit but established pattern. The "use canonical re-export, not generated submodule, in downstream crates" rule could be explicit. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 5 | The XML stage prompt + phase doc + retrospective protocol are self-contained. A fresh session would catch the same phase-doc factual errors via `<schema_audit>` + `<phase_doc_inventory_audit>` and proceed similarly. The four authoring decisions might land differently (e.g., a session might restrict request_capability to 2 kinds per spec) — surface to user is the discriminator. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Two phase-doc factual errors caught at execution time (mcp_missing claim; graphStore test path). Both could be caught at phase-doc authoring time if the v1.4 slot self-runs were part of phase-doc PR pre-flight. Recommendation: add a phase-doc authoring CI step that runs the schema_audit + inventory_audit greps. |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates (any fail = stop and revise)

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log --oneline main..HEAD` shows zero commits since the M05 phase doc merge (commit `45adbec`). All work uncommitted; surfacing for approval per CLAUDE.md §8.
- [x] **G2: no Severity-5 friction events** — *evidence:* All friction events table entries Sev 1–3.
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* Protocol-drift-events table is empty (only the documented hand-rolled-vs-generated AgentEvent pattern which is established M04 precedent, not drift).
- [x] **G4: the milestone or stage actually completed** — *evidence:* All 7 phase-doc A.1 deliverables landed: (1) schema enrichment + `mcp_missing` + `agent_missing` + GapSeverity/GapSource $defs; (2) framework_loader/{mod,walker,error}.rs with 13 unit tests + 100% of A.4 test matrix; (3) sdk/request_capability.rs with 6 unit tests; (4) ContextType reconcile per spec §2b; (5) graphStore.ts applyEvent branches lit up with 7 new tests; (6) TaskNode regression test added; (7) M04.V Decision 2 surfaced for maintainer in this retro. 21 new Rust tests + 2 integration tests + 14 new TS tests = 37 tests for the new surface.
- [x] **G5: scores ≥3 in every individual row across all three axes** — *evidence:* All rows ≥3 (lowest = 3 in Axis 3 #2 + #3; no scores below).

### Soft gates

- [x] **S1:** Process axis total ≥30 / 40 — *actual:* 38/40
- [x] **S2:** Product axis total ≥32 / 40 — *actual:* 38/40
- [x] **S3:** Pattern axis total ≥25 / 35 — *actual:* 30/35
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 6h, took ~3.5h, ratio 0.58×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 1 Sev-3 (hand-rolled vs generated event types refactor); 5 Sev-2; 2 Sev-1

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage B with minor decisions in `[END] Decisions` applied.

---

## [END] Coverage holdouts

Per-stage record of coverage-gate exclusions and per-module baselines.

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 94.06%
- **Exclusions added this stage:** None added this stage
- **Exclusion list as of stage end:** `src.main\.rs|generated` (unchanged from M04)

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
  - **Coverage actual this stage:** 95.79%
  - **Exclusions added this stage:** None
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs`

- **Crate:** `runtime-main`
  - **Coverage actual this stage:** 96.94%
  - **Exclusions added this stage:** None
  - **Exclusion list as of stage end:** `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs`

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/framework_loader/mod.rs` | (new this stage) | 99.17% | (new) | — |
| `crates/runtime-main/src/framework_loader/walker.rs` | (new this stage) | 98.18% | (new) | — |
| `crates/runtime-main/src/sdk/request_capability.rs` | (new this stage) | 98.33% | (new) | — |
| `crates/runtime-main/src/drone_ipc/client.rs` | 89.45–89.90% (M04.F with --skip recovery_lifecycle) | unchanged (no edits in this stage) | → | — |
| `crates/runtime-core/src/signal.rs` | 100% (M02) | 100% | → | Round-trip tests updated for new ContextType variants; all 8 round-trip tests + 2 explicit tests passing. |
| `crates/runtime-core/src/event.rs` | 100% (M04) | 100% | → | Added `GapSeverityRef` + `GapSourceRef` enums + extended 4 `*_missing` variants; round_trip.rs integration test (8 tests) exercises every variant by serde-round-tripping the AgentEvent union. |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. Inspected — unchanged from M04.

---

## [END] Decisions for the next stage (Stage B)

Specific changes to apply BEFORE the Stage B session opens.

### Maintainer-decision surface

- **M04.V Decision 2 — §4a `hook_*` vs `verify_*` spec/code naming reconcile.** Per phase doc A.3.7 the recommendation is to update spec §4a text to use `verify_*` (the code is internally consistent across schema + Rust + TS + tests + renderer for all hook categories; renaming events would cascade across ~10 files for cosmetic-only gain). **No code change in this stage.** Maintainer adjudicates: (a) update spec §4a text to `verify_*` (recommended; ~10 min spec edit) OR (b) reshape events to `hook_*` in `event.v1.1.json` (~3h cascade). Default proceed: (a) for the post-M05 `docs(spec):` PR.
- **M05.A authoring decision — `request_capability` 4-kind vs spec's 2-kind.** Spec §4b L1650 declares `capability_kind: enum["tool", "skill"]`; M05.A implemented all 4 kinds (tool/skill/mcp/agent) per phase doc deliverable. The 4-kind surface matches the schema's `*_missing` variant set and the renderer's GapNode kind union. Maintainer adjudicates: (a) update spec §4b text to 4-kind enum (recommended; matches code reality) OR (b) reshape request_capability + schema to 2-kind. Default proceed: (a) per post-M05 `docs(spec):` PR.
- **M05.A authoring decision — `mcp_missing` Layer 1 emission deferred to M06.** v0.1 framework JSON schema declares no MCP-server field; the walker has nothing to walk against. Reserved variant fires only from Layer 2 (`request_capability`) this milestone. Maintainer ack: confirm M06 phase doc covers Layer-1 MCP emission once framework schema declares MCP servers, OR document the v0.1 deferral explicitly in spec §4b severity matrix.
- **M02/M03/M04 ContextType reconcile — CLOSED.** Adopted spec §2b set (`Skill / Framework / Code / Search / Verify / Commit / Subagent`). No existing consumer code exercised the old variants (M04 gap-analysis L1157 confirms). Round-trip tests updated with reasonable variant mappings. Maintainer ack: confirm spec set is authoritative; no further reconcile pending.

### `CLAUDE.md` updates

- [ ] §15 — graduate "use `serde_json::from_value` for typify-generated complex-type test fixtures" to gotcha #73 if M06+ surfaces the same pattern. Hold for now; add if it recurs.
- [ ] §15 — graduate "phase-doc-claims-mcp_missing-exists-but-doesn't" specific pattern to a sub-bullet of gotcha #41 (grep-verify-claims). One concrete instance per stage isn't enough to graduate; collect more if it recurs in M06+.

### `STAGE-PROMPT-PROTOCOL.md` updates

- [ ] `<phase_doc_inventory_audit>` slot — consider adding `path_must_exist` attribute that fails the pre-flight if a `status="exists"` row's path doesn't resolve. Currently the slot is descriptive; making it gating would have caught the `tests/unit/lib/graphStore.test.ts` divergence at stage-execution time. v1.6 candidate.
- [ ] Authoring Rules — add an explicit "phase-doc authoring should self-run schema_audit + inventory_audit greps before opening the PR." Captures the M05.A pattern where the v1.4 slots caught errors at stage-execute time that authoring-time greps would have caught at phase-doc-PR time.

### Milestone document updates (M05-gap-capability.md if a stage prompt was off)

- [ ] Stage A.2 inventory row `tests/unit/lib/graphStore.test.ts | exists` should be `tests/unit/graphStore.test.ts | exists` (no `lib/` sub-dir). Path mismatch caught at execution time; no impact on the work shipped, but Stage B-G inventory audits should not perpetuate the typo.
- [ ] Stage A.3.1 schema-audit prose: "the four existing `*_missing` variants" should be "the two existing + two new `*_missing` variants" — `mcp_missing` is new in this stage, not existing.
- [ ] Stage A.3.2 walker example: `agent.id.0.clone()` should be `agent.id.as_str().to_string()` or similar — the typify newtype's tuple field is private; Deref coercion is the public access path.
- [ ] Stage A.3.3 request_capability code example imports `runtime_core::generated::event::*` types; should be `runtime_core::event::*` (the canonical re-export with hand-rolled `GapSeverityRef` / `GapSourceRef` enums).

### Protocol additions

- [ ] None this stage; v1.4/v1.5 protocol is working as designed.

### Open issues to file

- [ ] Maintainer to file: post-M05 `docs(spec):` PR bundling the §4a `hook_*`/`verify_*` reconcile + §4b 4-kind request_capability + `mcp_missing` Layer-1-deferred-to-M06 note. Same vehicle as the M04 carry-forward spec-bundle.

### Stage B preparation

Stage B (§8.security L1 + L2a capability enforcer) reads:

- This retrospective's `[END] Decisions` section (mandatory per CLAUDE.md §19 rule 1).
- M05.A code at `crates/runtime-main/src/framework_loader/` + `crates/runtime-main/src/sdk/request_capability.rs` — Stage B's enforcer wraps tool dispatch + sub-agent spawn; the gap-event emission seam (Emitter trait) is the same one Stage B fires capability_violation events through.
- The existing `crates/runtime-main/src/hitl/policy.rs` `on_capability_violation` trigger (M04.E) — Stage B routes through this existing seam, does NOT redefine.
- `runtime_core::event::GapSeverityRef` + `GapSourceRef` — Stage B may extend with similar `*Ref` enums for capability-violation severity if the spec §8.security severity matrix requires (review at Stage B authoring).

Estimated Stage B effort: 4–6h per phase doc; 0.58× calibration → ~2.5–3.5h actual.

---

## [END] User-review notes

- (Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.)

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M05 Stage A, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m05-gap-capability`; the PR drafts and pushes only at the end of the final stage (Stage G closeout).

**Claude:** I have completed M05 Stage A per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message for user approval. The PR remains undrafted until the final stage.

**Surfaced at:** 2026-05-13
