# M04.D — Stage Retrospective

> **Stage:** M04 Stage D — see `docs/build-prompts/M04-plan-verify-hitl-budget.md` §D
> **Parent milestone:** M04 of M11 in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-05-10
> **Session end:** 2026-05-10
> **Branch:** `claude/m04-plan-verify-hitl-budget`
> **Starting commit:** `1138486` (M04 Stage C)
> **Ending commit:** uncommitted — awaiting approval
> **Estimated effort (per prompt):** 6.5 hours
> **Actual elapsed:** ~4 hours

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md was loaded and §4 Hard Rules stated at top of first response
- [x] All "Read first" files read in order (CLAUDE.md → STAGE-PROMPT-PROTOCOL.md → M04 phase doc Stage D sections → spec/MVP/gotchas/retros)
- [x] Deliverable + 5-bullet test plan stated BEFORE writing code (revised to 7 bullets to enumerate the schema + drone-side + integration test seams)
- [x] Branch correct: `claude/m04-plan-verify-hitl-budget`
- [x] Prior stage commit on branch: `1138486` (M04 Stage C)
- [x] All `<pre_flight_check>` items verified: branch + Stage C commit + Arc<DroneClient> managed at `src-tauri/src/main.rs:55-56` (the audit gotcha trap warned the regex was misleading; the literal text is `let client: Arc<DroneClient> = Arc::clone(&lifecycle.client); app_handle.manage(client);` — A2 deliverable in place) + audit-baseline verify_started/passed/failed/rail_triggered ALL exist in event.rs (lines 291-319) + audit-baseline RevertToSnapshot ALREADY exists at drone.rs:183 with RevertReason enum at drone.rs:289-296. **pwsh.exe was NOT available** (only `powershell.exe` Windows PowerShell 5.1 at `C:\Windows\System32\WindowsPowerShell\v1.0\powershell.exe`) — fell back to `powershell.exe` per the pre-flight check's explicit "or fall back to powershell.exe with explicit retro decision" directive; documented inline in `crates/runtime-main/src/hooks/shell.rs` module docs + CHANGELOG.

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| Step 1 (survey) — major scope discovery | The phase doc planned a new `schemas/hook.v1.json` listing Hook + HookRef + HookCategory + Rail. Audit at survey time discovered `Hook` + `HookRef` + `HookCategory` + `JsonLogicExpression` are ALREADY declared in `schemas/common.v1.json` and generated to `runtime_core::generated::common`; `Rail` is in `schemas/framework.v1.json` and generated to `runtime_core::generated::framework`. The framework schema already references all of them (`task_defaults.post_hooks`, `hooks.*`, `hook_defs`, `rails.hard/soft`, `dont_touch`). The phase doc never grep-verified these. Per CLAUDE.md §12 own-technical-decisions: dropped the new schema file; consumed existing generated types. | The phase doc D.2 listed `schemas/hook.v1.json` as new without a grep-verify check. Gotcha #41 (grep-verify-claims at authoring time) was triggered for the second time on M04 (first was M04.A2). The pre_flight_check's audit gotchas spotted events + RevertToSnapshot but missed the bigger fact that the Hook primitive itself was already authored. | 3 |
| Step 1 (survey) — integration site doesn't exist | The phase doc D.3 says "Globset-backed matcher. Every Write tool call (existing tool dispatcher, location TBD) routes through the rail evaluator BEFORE the OS write." But there's no Write tool dispatcher in `runtime-main/src/sdk/`: the SDK provides LLM streaming + structured-emitter parsing only. v0.1 has no central tool execution surface — that's M05 capability enforcer. Per CLAUDE.md §12: shipped `DontTouchEvaluator` as a callable primitive (fully unit-tested) that the future capability enforcer (M05) or plan loop (M07) wires through. Documented inline in `crates/runtime-main/src/hooks/dont_touch.rs` module docs + CHANGELOG. Same pattern as M04.B `plan_loop.rs` deferral. | The phase doc could have noted "the Write-tool dispatcher integration site lands at M05+; Stage D ships the evaluator standalone." Recurring pattern: when v0.1 primitives ship before their integration site, the phase doc should explicitly mark them as standalone-evaluator scope. | 2 |
| Step 3 (verify_gates) round 1 — typify panic on inline enum | First `cargo xtask regenerate-types` run panicked at `typify-impl-0.6.2/src/type_entry.rs:290:56: called Option::unwrap() on a None value`. Root cause: I added inline enum properties (`category`, `policy`, `on_failure`) directly inside event variants. Existing schema convention puts shared enums in `$defs` and references via `$ref` (e.g., `ApprovedBy`). Resolved by extracting `HookCategoryRef` + `OnFailureRef` + `RailPolicy` to `$defs` + replacing inline enums with `$ref`. Single-iteration fix. | A gotcha entry for "typify panics on inline enum properties — extract to $defs + $ref" would belong in `docs/gotchas.md` at M04 closeout. Pairs with gotcha #14 snake_case-schema-discipline + #33 schema-regen-before-sample-bodies. | 2 |
| Step 3 (verify_gates) round 2 — clippy batch | 13+ clippy lints in one batch: 7× `match_wildcard_for_single_variants` (in executor.rs tests + dont_touch.rs + hook_integration.rs), 5× `doc_markdown` (`JSONLogic` and `SpawnArgs` unbackticked), 2× `too_long_first_doc_paragraph` (executor.rs + lib.rs hooks doc), 1× `doc_lazy_continuation` (the executor.rs `+ agent/session ids` line where `+` at start was parsed as list-item continuation), 1× `single_char_pattern` (`.contains("7")`). All recurring gotcha-#21 patterns. Resolved across two rounds (round 1 grouped the simpler fixes; round 2 caught the `+` line-start trap + lib.rs first-paragraph length). Same recurring pattern as M02/M03/M04.B retros. | The phase doc's `<execution_steps>` could bake `cargo clippy --fix --allow-dirty -p runtime-main` into the `implement` step to auto-resolve the mechanical patterns before the `verify_gates` round, leaving only the genuinely-substantive issues. Pre-empts gotcha #34 fmt-first pattern. | 2 |
| Step 3 (verify_gates) round 3 — rustdoc intra-doc link scope | `cargo doc --workspace --no-deps` errored on `[`dont_touch`]` style intra-doc links in `crates/runtime-main/src/hooks/mod.rs` module doc. The `pub mod dont_touch;` declaration comes AFTER the module-level `//!` doc, so at doc-resolution time the submodule names aren't yet in scope. Resolved by dropping the link syntax (use backticked plain names). | A gotcha entry for "module-doc intra-doc links to submodules: declaration order matters — links resolve at end-of-module, but lints fire at the doc location" would help. | 1 |
| Step 3 (verify_gates) coverage gate — drone_reconnect_events flake under llvm-cov + parallelism | `cargo llvm-cov --package runtime-main --fail-under-lines 95` (default parallel test execution) fails with `connect: io: The system cannot find the file specified` on `drone_reconnect_events.rs` — the named-pipe-connect retry deadline (5s) expires because the llvm-cov-instrumented drone child takes longer to bind the pipe under cumulative subprocess pressure. Test PASSES under `cargo test --workspace` (no llvm-cov) AND under `cargo llvm-cov` with `--test-threads=1`. Documented mitigation: run llvm-cov gates with `-- --test-threads=1`. Pre-existing test infrastructure issue surfaced by Stage D's additional compile/test volume (was below the timing-edge before). | The phase doc could note "When llvm-cov subprocess-spawning tests time out, use `-- --test-threads=1` to serialize; root cause is named-pipe-bind latency under instrumentation." Forward-applicable to M05+ where capability-enforcer integration tests will also spawn drone subprocesses. Could surface as a follow-up to gotcha #22 (subprocess test path derivation) — both relate to the same llvm-cov-vs-cargo-test divergence. CI runs on Linux where the issue may not surface; this is a Windows-local llvm-cov edge. | 3 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Phase doc D.1 #1 + audit `<gotchas>` trap on event naming | D.1 lists hook_started/passed/failed/rail_triggered as new event variants; audit gotcha says "do NOT re-author. Adopt the existing `verify_*` names." Conflict: what fields go on the existing variants? | Per CLAUDE.md §12 own-technical-decisions: kept the existing `verify_*` names + extended their field set to match spec §4a `hook_started/passed/failed` fields. Approach: ADD spec §4a fields (category, firing_point, ref/level, output_preview, on_failure, policy, firing_point, agent_id) to the existing variants; field union since there are no producers yet. For `rail_triggered.severity` → `policy` rename: rename rather than additive since the existing field has no spec-compliant meaning (severity is not in spec §4a). Documented in CHANGELOG. | Phase doc D.1 #1 should be revised to clarify "the event variants adopt the existing `verify_*` names; the FIELDS are extended/renamed to match spec §4a hook_started/passed/failed/rail_triggered." Pairs with the "audit gotcha" pattern from M04.A2 — when an audit discovers spec/code drift, the gotcha trap should state the resolution explicitly, not just the discovery. |
| Phase doc D.3 #3 (don't-touch) + actual codebase | D.3 says "Every Write tool call (existing tool dispatcher, location TBD) routes through the rail evaluator BEFORE the OS write." But no Write tool / dispatcher exists in `runtime-main/src/sdk/`. | Same pattern as M04.B `plan_loop.rs` deferral and M04.C drone-resolves-seam mismatch — per CLAUDE.md §12 own-technical-decisions: shipped the `DontTouchEvaluator` as a standalone evaluator with full coverage; the future capability enforcer (M05) or plan loop (M07) wires through it. Documented inline + in CHANGELOG. The audit gotcha (gotcha #41 grep-verify-claims) didn't catch this because "Write tool dispatcher" isn't a function/file claim — it's a HOW-claim about architecture (gotcha #41's HOW-vs-WHAT distinction per M04.C). | The `<architecture_check>` slot proposed in M04.C's [END] decisions belongs in STAGE-PROMPT-PROTOCOL.md v1.4 for these exact HOW-claims (cross-process flow, integration-site presence). |
| Phase doc D.3 #1 + spec §4a HookRef carry-in-event | Spec §4a says `hook_started` carries `ref: HookRef` (the full HookRef union). D.3 sample bodies match. But carrying HookRef in the event payload increases schema complexity (HookRef references common.v1.json which the event schema would need to cross-import). | Per CLAUDE.md §12 own-technical-decisions: simplified by carrying only `category` + `firing_point` (renderer's actual visual needs); the full HookRef is recoverable from framework JSON by `hook_id`. Documented as a deliberate scope-down in CHANGELOG. The renderer doesn't display HookRef internals today — when M07 wires the SDK plan loop, the framework JSON cache will be available for lookup if needed. | Phase doc D.3 should note "event payload carries hook_id + category + firing_point; full HookRef recoverable by hook_id from framework JSON cache (M07)." Spec §4a's `ref: HookRef` description is aspirational; v0.1 carries the discriminator-friendly subset. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| (none mid-execution) | This stage proceeded without mid-execution surfaces. The three ambiguity events resolved per CLAUDE.md §12 own-technical-decisions; all three deferrals are documented in retrospective + CHANGELOG. | n/a | n/a |

---

## [LIVE] Protocol drift events

| Hard rule | What happened | Did Claude self-correct? | Notes |
|---|---|---|---|

None observed.

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The Hook + HookRef + Rail types were already fully authored in `common.v1.json` + `framework.v1.json` and generated to `runtime-core::generated`. The phase doc planned them as new but they shipped in some prior milestone (M01.B type generation likely). | The schemas are spec-faithful proto code that was authored ahead of milestone consumption. The phase doc's gotcha #41 grep-verify-claims rule was meant to catch this. The Stage D `<pre_flight_check>` audit baselines caught events + RevertToSnapshot but missed Hook + Rail. The audit was incomplete: it focused on the four specific items that the user pre-identified, not on the broader "what does the schema already declare for §4a." | Forward-applicable: when a milestone Phase doc claims new schema, the X.5 prompt's `<pre_flight_check>` should explicitly audit the entire spec section's $defs across all existing schemas (not just the specific items the Phase doc highlights). For M04.D, the right audit query was `grep -l "HookRef\|Hook\|Rail\|JsonLogic" schemas/*.json crates/runtime-core/src/generated/*.rs` BEFORE writing the phase doc. Add to STAGE-PROMPT-PROTOCOL.md as a corollary to gotcha #41. |
| The fmt-first discipline (gotcha #34) was bypassed in my workflow — I ran `cargo fmt --all` AFTER implement + before clippy/test, but the clippy lints that surfaced were structural (match_wildcard, doc_markdown, etc.), not formatting. fmt would not have caught any of them. | fmt-first matters for mechanical formatting churn (whitespace, indentation, line wrap). It doesn't catch lint patterns that require code reorganization. The recurring gotcha-#21 patterns (M02/M03/M04 stages) are structural. fmt is one of TWO mechanical first-passes; the other is `cargo clippy --fix` which DOES auto-resolve many gotcha-#21 patterns. | Add to gotcha #34: "Run `cargo clippy --fix` alongside `cargo fmt --write` as the mechanical first-pass." Forward-applicable to every Rust-heavy stage. |
| The drone_reconnect_events test flake under llvm-cov + parallelism (Severity-3 friction above) is a Windows-local issue that DOES NOT surface on Linux CI (where the actual gate runs). CI Linux is likely faster on subprocess spawn + named pipe bind. The local-Windows-only flake is a developer-experience cost, not a CI gate failure. | Coverage gates are run on Linux in CI; Windows-local llvm-cov is a developer's pre-commit check. The flake doesn't block PR merge if Linux passes. But it adds noise to local validation. | Add to gotcha cluster: "Windows-local llvm-cov may flake on subprocess-spawning tests under default parallelism. Use `-- --test-threads=1` for the local pre-commit coverage run. CI Linux runs are not affected." Could be a single line addition to gotcha #22 about subprocess paths. |

---

## [END] Three-axis scoring

### Axis 1: Process — did the prompt-driven workflow work?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Was `CLAUDE.md` sufficient orientation for a fresh session? | 5 | Hard rules stated; §5 TDD + §6 gate ordering + §12 own-technical-decisions guided all three ambiguity-event resolutions. |
| 2 | Did the milestone prompt's "Read first" list correctly orient Claude? | 4 | All reads applicable. -1 because the audit gotchas missed the bigger fact that Hook + Rail were already authored — the audit was narrow (4 specific items) when it should have surveyed §4a's full $defs surface. |
| 3 | Did Claude state deliverable + test plan before code per §16? | 5 | Stated Hard Rules + deliverable + 7-bullet test plan with explicit Stage D technical-best-practice forks called out (scope reductions vs. phase doc draft). |
| 4 | Did Claude self-correct in ≤3 rounds per §7? | 4 | Three self-correction rounds: (1) typify panic → schema $defs extraction in 1 round; (2) clippy batch → 13 lints batched + fixed in 1 round; (3) doc + intra-doc-link + coverage-gate flake → 3 fixes batched. The coverage-gate flake required diagnosis (Windows-llvm-cov-vs-cargo-test parallelism) which counted as the 3rd round. -1 because round 2 had a follow-on round 3 — the lib.rs first-paragraph + executor.rs doc_lazy_continuation should have surfaced earlier. |
| 5 | Did the do-not-commit-until-approved rule hold? | 5 | No commits made; this retrospective is being surfaced for approval. |
| 6 | Did Claude escalate the right things, proceed on the right things? | 5 | The three architectural / scope decisions (no new hook.v1.json; integration-site deferral; HookRef-in-event simplification) were technical-best-practice calls per §12 — proceeded with the right answer + documented. No false escalations. |
| 7 | Was the human-direction load reasonable? | 5 | Zero direction during execution; one approval gate at end. |
| 8 | Were the surfaces (PR drafts, escalations) actionable? | 5 | This surface (CHANGELOG + retrospective + diff stat + gate results + Playwright results + cross-platform shell-execution disposition + spec §4a edit disposition + retrospective decisions) is concrete and reviewable. |
| **Process axis total** | | **38/40** | |

### Axis 2: Product — did the artifact meet our standards?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Does the code match `CLAUDE.md` §9 style + naming? | 5 | snake_case Rust + camelCase TS; functions ≤50 lines; helper extraction (`build_spawn_args`, `map_shell_result`, `preview`, `apply_op`, `apply_var`, `nextHookPosition`, `updateVerifyData`, `updateHookData`, `truncateForBadge`); `*_with`-style seam preserved for shell.rs; comments only where the WHY is non-obvious (the case-insensitive globset rationale, the integration-site deferral rationale, the HookRef-in-event simplification rationale, the `--test-threads=1` rationale). |
| 2 | Are tests behavior tests, not tautology tests (§5)? | 5 | Hook executor tests exercise the user-observed flow (spawn → outcome → on_failure carry); rails tests cover every allowlisted operator + truthy-table boundary + UnsupportedOperator path; dont_touch tests cover matched/unmatched/multi-glob/recursive/case-insensitive/invalid-glob; shell tests cover SpawnArgs construction across platforms + dispatch + propagation + error-display invariants. Renderer tests exercise the state-driven DOM (verify_started inserts node; verify_passed transitions to pass; verify_failed transitions to fail with on_failure badge). Integration test exercises the full lifecycle outputs (executor flag + RevertReason serde round-trip). |
| 3 | Are public APIs documented with compile-checked examples (§6)? | 4 | All new Rust public APIs have rustdoc with `# Errors` sections. `evaluate` has `# Panics` documenting that the `expect("len == 1")` can't fire in practice. -1 because `execute_hook`, `evaluate_rail`, `DontTouchEvaluator::new` could have runnable `# Examples` showing the standalone-evaluator pattern. Deferred per the existing M04 precedent (none of the M04.B/C primitives shipped with `# Examples` either; the integration test demonstrates usage). |
| 4 | Are deliverables exactly what was promised — no scope creep, no scope cuts? | 4 | All Stage D work areas shipped with three documented scope reductions vs. the original phase doc draft: (a) no new `schemas/hook.v1.json` — types already exist; (b) `pre_file_edit` integration site deferred — evaluator standalone; (c) HookRef-in-event simplified to category + firing_point. All three are explicit deliberate scope-downs documented in CHANGELOG + retrospective per CLAUDE.md §12. -1 because the third is a deliberate scope reduction from spec §4a's `ref: HookRef` — a deliberate deferral, not a missing deliverable, but worth flagging. |
| 5 | Would a stranger picking up this code understand it without reading the spec? | 5 | Hook executor module doc enumerates the three HookRef variants + which are deferred to M05/M07 and why. Shell wrapper module doc names the pwsh.exe-vs-powershell.exe disposition + the wrapper-vs-seam coverage rationale. Don't-touch module doc names the case-insensitive cross-platform consistency rationale + integration-site deferral. Rails module doc names the JSONLogic operator allowlist + the no-silent-extension rule. graphStore.ts new wiring has inline rationale for verify-vs-non-verify routing + idempotent re-emit semantics + triggeredRails-on-store-not-as-nodes design. |
| 6 | Are `CLAUDE.md` §9 anti-patterns absent? | 5 | No magic numbers (`OUTPUT_PREVIEW_MAX_BYTES = 512`, `BADGE_MAX = 60` named); no stringly-typed APIs (HookCategory, OnFailureRef, RailPolicy are typed enums); no `any`; no ad-hoc `#[allow]`; no implementation-detail tests; no generic names (`build_spawn_args`, `evaluate_rail`, `DontTouchEvaluator` describe what they are). |
| 7 | Is coverage ≥80% / ≥95% on safety primitives — measured, not estimated? | 5 | Measured: workspace 93.27% line (≥80% ✓); runtime-main 96.79% line (≥95% ✓); runtime-drone 95.89% line (≥95% ✓); src/ 97.23% line (≥80% ✓). Per-module deltas: hooks/dont_touch.rs 98.11% (new); hooks/executor.rs 97.63% (new); hooks/rails.rs 94.22% (new — below the 95% per-module ideal, but the package overall is at 96.79%; the gap is in unreachable apply_op branches that pre-check via the allowlist); hooks/shell.rs 87.26% (new — the production `TokioShellSpawner::spawn` wrapper is the OS-spawn holdout per the M02/A2 pattern; build_spawn_args + execute_shell_with seam are tested). Renderer side: VerifyNode.tsx 100%; HookNode.tsx 100%; graphStore.ts 96.02% (was 96.83% — slight drop due to new verify_started branch coverage; within ≥80% gate). |
| 8 | Did CI green hold across Linux/macOS/Windows × stable/MSRV? | 4 | All gates green locally on Windows 11. Linux/macOS CI happens on PR push (Stage G). -1 because not yet observed. The drone_reconnect_events Windows-llvm-cov-parallelism flake won't affect Linux CI (which uses different process scheduling). |
| **Product axis total** | | **37/40** | |

### Axis 3: Pattern — does this generalize to remaining milestones?

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Were all `TEMPLATE.md` sections useful, or were any dead weight? | 5 | `<pre_flight_check>` directly load-bearing — verified A2 + C deliverables before D touched them; the audit gotchas saved real work even where they missed the bigger Hook+Rail point. `<read_reference>` enumerated the 4 archetype files (drone_lifecycle.rs for tokio::process pattern; snapshot.rs to extend; runtime-main/src/sdk for integration site; VerifyNode.tsx synthetic baseline). `<runtime_environment os="windows">` correctly anchored the pwsh-vs-powershell pre-flight directive. `<gotchas>` traps — 6 of 8 directly applicable (cross-platform shell, JSONLogic allowlist, don't-touch async write, spec firing-point table edit disposition); 2 audit-corrections proved useful (drone command consume-not-rebuild; events extend-not-re-author). |
| 2 | Were any sections needed but missing from `TEMPLATE.md`? | 4 | Two gaps: (a) The `<architecture_check>` slot proposed in M04.C's [END] decisions would have caught the "Write tool dispatcher doesn't exist in runtime-main" mismatch at authoring time — same HOW-claim category as M04.C's drone-resolves-seam. (b) The audit gotchas (correctly) caught specific items but missed the broader fact that Hook + Rail were already authored. A `<schema_audit>` slot prompting "grep all schemas/*.json for §<spec section> $defs before writing the new schema" would have caught this. -1 because both gaps cost real friction this stage. |
| 3 | Are milestone-specific gotchas truly local, or do they generalize to `CLAUDE.md` §15? | 4 | Three forward-applicable gotchas: (a) Typify panics on inline enum properties in event variants — extract to $defs + $ref (recurring pattern; ApprovedBy in M04.B was the first time, this stage was the second). (b) Module-doc intra-doc links to submodules resolve at end-of-module — the doc lints fire at the doc location before the submodule is declared. (c) Windows-local llvm-cov flake on subprocess-spawning tests under default parallelism — `-- --test-threads=1` is the documented fix. All three should graduate to `docs/gotchas.md` at M04 closeout. |
| 4 | Did the time-box estimate match reality (within 2×)? | 5 | Estimated 6.5h, actual ~4h. Ratio 0.6× — within the M03/M04 calibration band (Stage C was 0.5×, Stage B was 0.4×). The audit-driven scope reduction (no new hook.v1.json) was the major time-saver. |
| 5 | Were there *implicit* protocol moments — things that should have been written down but weren't? | 4 | Three implicit decisions: (a) The HookRef-in-event simplification was per §12 own-technical-decisions but is a deliberate spec §4a scope-down — could be more explicitly called out as "v0.1 event payload subset" vs "spec ideal." (b) The `--test-threads=1` coverage-gate mitigation is a workflow workaround not a gate change — could be a CLAUDE.md §6 note. (c) The `pre_file_edit` firing-point spec edit was decided in-stage (< 5 lines) rather than as a follow-up PR — the disposition decision rule could be more explicit in TEMPLATE.md. -1 because three implicit patterns is on the higher end. |
| 6 | Could this prompt be re-run by a different fresh session and produce comparable results? | 4 | Yes — most of the prompt is concrete (deliverables enumerated, audit gotchas, scope locks, gates mechanical). The three judgment calls (no-new-schema, integration-site deferral, HookRef simplification) might be made differently by another session, but all three are within CLAUDE.md §12 own-technical-decisions guidance and converge on similar answers. -1 because the schema-audit gap could derail a session that doesn't catch the Hook+Rail prior-art at read time. |
| 7 | If we copy this prompt format for remaining milestones, does anything need to change first? | 4 | Three minor improvements: (a) Audit gotchas should survey the full spec-section $defs surface (not just the items the phase doc highlights) per gotcha #41 corollary. (b) `<architecture_check>` slot (M04.C carry-forward) for HOW-claims about integration sites. (c) `<gotchas>` trap for "module-doc intra-doc links resolve at end-of-module — submodule references in `//!` doc must use plain backticks, not `[`name`]`." |
| **Pattern axis total** | | **30/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved rule held** — *evidence:* `git log --oneline -1` returns `1138486 M04 Stage C`; no new commits this session.
- [x] **G2: no Severity-5 friction events** — *evidence:* highest severity is 3 (the audit scope-discovery + the Windows-llvm-cov parallelism flake; both resolved within the stage budget).
- [x] **G3: no protocol drift events left unaddressed** — *evidence:* table is empty (none observed).
- [x] **G4: stage actually completed** — *evidence:* All Stage D work areas shipped (with three documented scope reductions per CLAUDE.md §12). 4 new runtime-main hook modules (`shell.rs`, `dont_touch.rs`, `rails.rs`, `executor.rs`) + `mod.rs` + integration test. Schema extensions to `event.v1.json` (4 variants extended + 3 new $defs) + `framework.v1.json` (`pre_file_edit` firing point). Hand-curated edits to `runtime-core/src/event.rs` + `drone.rs::RevertReason::HookRollback { hook_id }` + `runtime-drone/src/command_handler.rs::handle_revert`. Renderer wiring: graphStore.ts live-rendering + VerifyNode/HookNode field extensions + 11 new vitest cases. Spec §4a firing-point table edit (in-stage, 1 line). CHANGELOG. 171 runtime-main tests pass + 6 hook_integration tests + 176 vitest tests + 6 Playwright tests. Coverage gates green at workspace 93.27%, runtime-main 96.79%, runtime-drone 95.89%, src/ 97.23%.
- [x] **G5: scores ≥3 in every individual row** — *evidence:* lowest individual rows: Process Q2 + Q4 (4); Product Q3 + Q4 + Q8 (4); Pattern Q2 + Q3 + Q5 + Q6 + Q7 (4). All ≥4.

### Soft gates

- [x] **S1:** Process axis total ≥30/40 — *actual:* 38
- [x] **S2:** Product axis total ≥32/40 — *actual:* 37
- [x] **S3:** Pattern axis total ≥25/35 — *actual:* 30
- [x] **S4:** Time-box within 2× of actual — *actual:* estimated 6.5h, took ~4h, ratio 0.6×
- [x] **S5:** ≤3 Severity-3 friction events — *actual count:* 2 (the audit-scope-discovery + the Windows-llvm-cov flake)

### Outcome

- [x] **Pattern is sound.** All hard gates pass; all soft gates pass. Proceed to Stage E with the Decisions captured below.

---

## [END] Coverage holdouts

### Workspace gate (≥80% line)

- **Coverage actual this stage:** 93.27% line (`-- --test-threads=1` to bypass Windows-local parallelism flake; Linux CI not affected)
- **Exclusions added this stage:** None added.
- **Exclusion list as of stage end:** `src.main\.rs|generated`

### Per-package gates (≥95% line on safety primitives)

- **Crate:** `runtime-drone`
  - Coverage actual this stage: 95.89% line
  - Exclusions added this stage: None.
  - Exclusion list as of stage end: `src.main\.rs|generated|src.lib\.rs|src.shutdown\.rs`
- **Crate:** `runtime-main`
  - Coverage actual this stage: 96.79% line
  - Exclusions added this stage: None.
  - Exclusion list as of stage end: `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.key_store\.rs`
  - **Note:** The new `crates/runtime-main/src/hooks/shell.rs::TokioShellSpawner::spawn` is the M04.D OS-spawn holdout. Per the M02/A2 wrapper-vs-seam pattern, the holdout COULD be added to the exclusion regex (`src.hooks.shell\.rs`); however, the package overall is 96.79% > 95% gate and the testable seams (`build_spawn_args`, `execute_shell_with`) are exercised — so the gate passes without an explicit exclusion. If a future stage adds non-wrapper logic to shell.rs that drops the file coverage below an acceptable per-module floor, add the exclusion then. Documented as a deliberate non-exclusion this stage.

### Per-module baselines (preserved-or-improved invariant)

| Module | Prior baseline | This stage | Direction | Reason if down |
|---|---|---|---|---|
| `crates/runtime-main/src/hooks/shell.rs` | (new) | 87.26% line | ↑ from baseline | New module; `TokioShellSpawner::spawn` (real OS spawn) is excluded by being a real-OS wrapper. The seam (`build_spawn_args`, `execute_shell_with`) is tested via 7 unit tests. |
| `crates/runtime-main/src/hooks/dont_touch.rs` | (new) | 98.11% line | ↑ from baseline | New module; 9 unit tests covering empty/matched/unmatched/recursive/multi-glob/case-insensitive/invalid-glob paths. |
| `crates/runtime-main/src/hooks/rails.rs` | (new) | 94.22% line | ↑ from baseline | New module; 24 unit tests covering all 15 allowlisted operators + nested expressions + literal pass-through + truthy table. The 24 uncovered lines are `apply_op` defensive `_ => Err(UnsupportedOperator)` arm that the allowlist pre-check makes unreachable; same pattern as `state_machine.rs` exhaustive fallback. |
| `crates/runtime-main/src/hooks/executor.rs` | (new) | 97.63% line | ↑ from baseline | New module; 12 unit tests covering shell pass/fail per on_failure variant + tool/agent deferred + output preview truncation (ASCII + UTF-8). |
| `crates/runtime-drone/src/command_handler.rs` | 94.64% (M04.B) | 94.46% line | → | One-line extension to `handle_revert` (consume `&RevertReason`); existing tests still cover all variants. Slightly down due to additional reason-string match arms; package overall holds at 95.89%. |
| `src/lib/graphStore.ts` | 96.83% (M04.C) | 96.02% line | ↓ | New verify_started branching (verify category vs non-verify category) + verify_passed/failed update paths + rail_triggered append + nextHookPosition helper. The 11 new vitest cases cover the primary paths; the uncovered lines are the `updateHookData` fallback branches for missing nodes (idempotent no-op when no node exists yet). Within ≥80% gate. |
| `src/components/nodes/VerifyNode.tsx` | 100% (M03.C) | 100% line | → | Extended with output_preview / error / onFailure / firing_point rendering; new tests cover each branch. |
| `src/components/nodes/HookNode.tsx` | 100% (M03.C) | 100% line | → | Extended with firing_point / duration_ms / error rendering; new tests cover each branch. |

### CI workflow drift check

- [x] The `--ignore-filename-regex` patterns in `.github/workflows/ci.yml` (gate steps + lcov export steps) match what's documented above. Confirmed by inspecting `.github/workflows/ci.yml` (M02.E + A2 + B + C carry-forward applied; no Stage D drift).

---

## [END] Special log per the prompt's `<special_log>` directive

- **Cross-platform shell-execution surprises (gotcha #32 territory):** `pwsh.exe` was unavailable on the M04.D build machine (only `powershell.exe` Windows PowerShell 5.1). Fell back to `powershell.exe` per the pre-flight directive. Both accept identical `-NoProfile -Command "<string>"` semantics per Microsoft's `about_PowerShell_exe` reference (M04.D WEBCHECK, verified verbatim). Forward-applicable: framework JSON authors should use PowerShell-syntax commands on Windows hooks (e.g., `pwsh -File .\verify.ps1` or `bash .aria/verify.sh` via WSL/Git Bash) rather than direct bash-isms. The wrapper passes the framework-specified command string verbatim through `-Command`, leaving the choice of shell-syntax to the framework. CI may run pwsh.exe on the GitHub Actions Windows runner; the wrapper is portable to either binary because the flag spelling is identical.
- **JSONLogic operator allowlist needed extension:** No. The 15-operator allowlist per gotcha #18 is sufficient for the rails primitive's v0.1 surface. Spec §4a's example rails (`no_secrets`, `no_debug`) use the shell variant for the check (no operators needed) — the JSONLogic allowlist becomes load-bearing when M05 capability enforcer wires rails against typed events.
- **Spec §4a firing-point table edit disposition:** In-stage edit (1 line addition to the table). The edit is self-contained and < 5 lines per the phase doc decision rule. Committed alongside this stage's code per CLAUDE.md §17 reference index ("Spec §4a edit (in-stage, < 5 lines)"). No follow-up doc PR needed.
- **revert_to_snapshot integration with Stage F recovery flow (the same mechanism may apply to v0.1 startup recovery):** Per spec §1b, recovery rebuilds session state from snapshots; the drone's `handle_revert` is currently a SnapshotWritten-event-emitting confirmation handler (it doesn't actually restore in-memory state). For Stage F recovery, the same drone command will dispatch with `RevertReason::GapRecovery` or — at v0.1 startup — with `RevertReason::UserRollback` (user picks "resume from snapshot"). The drone handler is variant-agnostic by design; the SDK side decides which revert reason to dispatch based on the recovery flow. Forward-applicable: Stage F recovery flow can call `DroneCommand::RevertToSnapshot { snapshot_id, reason: UserRollback }` to drive the resume.

---

## [END] Decisions for the next stage

Stage E inherits the following:

### `CLAUDE.md` updates

- [ ] None required. The architecture-check slot proposal (carry-forward from M04.C) is more naturally addressed in STAGE-PROMPT-PROTOCOL.md, not CLAUDE.md.

### `STAGE-PROMPT-PROTOCOL.md` updates

- [ ] **Add a `<schema_audit>` slot** (v1.4 candidate) — corollary to gotcha #41 grep-verify-claims. For any milestone Phase doc that proposes a new schema, the X.5 prompt's pre_flight_check should explicitly audit the full spec-section $defs surface (grep all schemas/*.json + crates/runtime-core/src/generated/*.rs for the spec section's named types) BEFORE the build agent reads the X.3 detailed changes. Worked example: M04.D Phase doc proposed `schemas/hook.v1.json` but Hook + HookRef + Rail were already declared in common.v1.json + framework.v1.json. Audit gotchas in the M04.D prompt caught specific items (events, RevertToSnapshot) but missed the broader schema authoring.
- [ ] **Add an `<architecture_check>` slot** (v1.4 candidate; carry-forward from M04.C) — companion to gotcha #41's WHAT-claims. Covers HOW-claims (cross-process flow, IPC topology, integration-site presence). Worked examples: M04.C drone-resolves-seam mismatch + M04.D Write-tool-dispatcher integration site doesn't exist.

### `TEMPLATE.md` updates

- [ ] (Same as above; the `<schema_audit>` + `<architecture_check>` slots belong in the work-stage template.)

### Milestone document updates (`M04-plan-verify-hitl-budget.md`)

- [ ] **§D.1 #1 (event variants)** — revise to clarify "adopt existing `verify_*` variant names; EXTEND the field set to match spec §4a `hook_started/passed/failed`; do NOT re-author with new names." The audit gotcha caught the rename pitfall; the body text should match.
- [ ] **§D.1 #4 + D.3 #2 (don't-touch interception)** — note that the Write-tool dispatcher integration site doesn't exist in runtime-main at v0.1; Stage D ships `DontTouchEvaluator` as a callable primitive that M05 capability enforcer or M07 plan loop will wire through.
- [ ] **§D.2 schemas list** — drop `schemas/hook.v1.json` (already authored in common.v1.json + framework.v1.json). Stage D extends event.v1.json + framework.v1.json + the hand-curated event.rs + drone.rs.
- [ ] **§E.1 (Stage E)** — add `<pre_flight_check>` for: Hook + HookRef + Rail generated types available from `runtime_core::generated::{common, framework}`; verify_*/rail_triggered event variants extended with spec §4a fields; `RevertReason::HookRollback { hook_id }` struct-variant; `DontTouchEvaluator` callable; `pre_file_edit` firing point in framework.v1.json. Stage E HITL primitive will wire HITL triggers against the same hook+rail surface.

### Protocol additions (graduation candidates for `docs/gotchas.md` at M04 closeout)

- [ ] **Typify panics on inline enum properties in event variants — extract to $defs + $ref.** Pattern: "When adding a property with a string-enum constraint to an event variant or struct, declare the enum in the schema's `$defs` and reference via `$ref`. Inline `{ \"type\": \"string\", \"enum\": [...] }` properties trip typify's variant generator with an `Option::unwrap()` panic at `type_entry.rs:290`. Recurred M04.B (ApprovedBy moved to $defs) + M04.D (HookCategoryRef, OnFailureRef, RailPolicy moved to $defs)."
- [ ] **Module-doc intra-doc links to submodules resolve at end-of-module.** Pattern: "Rustdoc resolves intra-doc links (e.g., `[`submodule_name`]`) at the END of the module's declaration scope, but the lint that fires on unresolved links is anchored at the doc-location (BEFORE the submodule is declared). Use plain backticks for submodule names in module-level `//!` docs to avoid `cargo doc -D missing_docs` errors. Recurred M04.D `crates/runtime-main/src/hooks/mod.rs` initial draft."
- [ ] **Windows-local `cargo llvm-cov` flake on subprocess-spawning tests under default parallelism.** Pattern: "When running `cargo llvm-cov --package <crate>` locally on Windows, subprocess-spawning integration tests (drone subprocess, MCP server, etc.) may flake with `connect: io: The system cannot find the file specified` because the instrumented child binary takes longer to bind the named pipe than the test's retry deadline allows under cumulative subprocess pressure. Use `cargo llvm-cov ... -- --test-threads=1` for the local pre-commit coverage run. CI Linux is faster and not affected. Surfaced in M04.D for the first time; pairs with gotcha #22 (subprocess test path derivation) as a sibling llvm-cov-specific issue."

### Open issues to file

- [ ] None — all decisions are inline or queued for Stage G.

---

## [END] User-review notes

User-review notes:

- [Empty until user reviews. User adds notes here on review; Claude revises retrospective if needed.]

---

## [END] Sign-off

> This retrospective is Claude's self-assessment of M04 Stage D, authored per `CLAUDE.md` §19 Retrospective Protocol. User review and approval pending. Per the per-milestone-as-PR pattern, this stage's commit lands on the parent-milestone feature branch `claude/m04-plan-verify-hitl-budget`; the PR drafts and pushes only at the end of Stage G.

**Claude:** I have completed M04 Stage D per the stage prompt. The retrospective above reflects my honest self-assessment. I have NOT committed; I am surfacing this retrospective + the diff stat + the draft commit message + gate results for user approval.

**Surfaced at:** 2026-05-10
