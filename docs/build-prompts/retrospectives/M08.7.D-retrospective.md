# M08.7.D — Stage Retrospective

> **Stage:** M08.7 Stage `D` (rung 4 — gap detection / `request_capability` wired into the run loop) — see `docs/build-prompts/M08.7-execution-engine.md`
> **Parent milestone:** M08.7 (Execution Engine) in `docs/MVP-v0.1.md`
> **Authored by:** Claude (Claude-driven per `CLAUDE.md` §19)
> **Session start:** 2026-06-01
> **Session end:** 2026-06-01
> **Branch:** `claude/m08.7-execution-engine`
> **Starting commit:** `70fcd7f`
> **Ending commits:** `ada7d52` (red — failing wire tests) → `9e69af2` (impl — the wire) → `1258f74` (additive — mcp/agent kind arms + the malformed no-suspend path) → `0f90dc0` (retro) → `<close>` (ADR-0029 pull-forward + `gap_detection_live.rs` eval E-04 + doc reconciliation) — pushed only on approval
> **Estimated effort (per prompt):** 3–5 hours
> **Actual elapsed:** ~1 session

---

## Pre-flight (claimed before session start)

- [x] CLAUDE.md loaded; §4 Hard Rules in scope (esp. §4 rule 11 grounded-claims, §2 surface-contradictions, §8 commit workflow, §10 Hard Rule 8)
- [x] All "Read first" files read (CLAUDE.md, cluster-pattern.md §1/§2/§4/§5, M08.7 phase doc Background/Stage A/Stage D D.1–D.6, M08.7.C retro, spec §4/§4b/§1b, retro template) + the 6 read_reference source files
- [x] Deliverable + test plan stated before code; D.3.0 ground findings surfaced before tests (the first checkpoint); user approved
- [x] Branch correct (`claude/m08.7-execution-engine`, rungs 1+2+3 present, `70fcd7f` HEAD)
- [x] `git status` clean except pre-existing untracked `.claude/*`, `rung2/`
- [x] Pre-flight `problem_live` confirmed by grep: `handle_request_capability` has NO production caller; NO existing test pins the painted behavior

---

## [LIVE] Friction events

| When (commit / step) | What happened | What the prompt should have said | Severity (1–5) |
|---|---|---|---|
| ground_at_red | The phase doc + handler docstring frame the suspend as "the SDK turn loop awaits resolution before resuming" (an awaiter), implying reuse of `await_capability_violation_hitl`. Grounding found that awaiter is the WRONG shape: (a) its semantics are await-then-**continue** (a violation proceeds; a gap must halt); (b) on the Tester path the seam is `test_defaults()` → resolves instantly (no-op); (c) there is **no resolve-and-resume machinery** anywhere in the run loop for v0.1. The grounded v0.1 suspend is a loop-**break** (`TurnFeedback.gap_suspended`), not an awaiter. | D.3.0(a) could state outright: "v0.1 has no resolve-and-resume; the suspend is a loop-break, not a HITL await — do NOT reuse `await_capability_violation_hitl` (its continue-semantics + test-defaults no-op are both wrong for a gap)." The construction_reachability_check's "the awaiter mirrors `await_capability_violation_hitl`" was a misleading steer. | 2 |
| green / clippy | Three `too_long_first_doc_paragraph` + one `doc_markdown` clippy lints on the new doc comments — could not surface during red (the test file is pure-behavioral, no new-symbol imports; the production doc comments didn't exist until green). Mechanical fixes, no logic change. | A note that doc-markdown / first-paragraph-length lints on NEW production doc comments only surface at green — they are mechanical green-phase fixes, not red-phase signals. | 1 |

---

## [LIVE] Ambiguity events

| Source files in conflict | What was unclear | How resolved | Should be fixed where? |
|---|---|---|---|
| Handler docstring ("the SDK turn loop awaits resolution before resuming") + phase-doc construction_reachability_check ("the awaiter mirrors `await_capability_violation_hitl`") vs the absence of any resolve-and-resume code | Whether rung 4 reuses the violation awaiter or builds a new suspend wire | New minimal suspend-break (`gap_suspended` → `run_agent` break). Surfaced at ground-at-red; the maintainer approved. The adr_trigger (resolve-and-resume contradicting ADR-0019) did NOT fire — suspend-and-record fits ADR-0019. | Resolved in-cluster; recorded in this retro + the impl commit body. Phase doc for any future gap-resolution rung should note the awaiter is built then, not now. |
| Phase doc D.1 / table line 122 ("emits ToolInvoked for an unknown name and stops") vs the actual red trace | What the painted behavior actually is | The red trace showed the painted behavior is a **CapabilityViolation** ("tool 'request_capability' not declared in framework.tools[]"), not a `ToolInvoked` — the undeclared meta-tool hits the L1 enforcer in `pipeline.next_event`. Even more clearly wrong than the doc predicted. | Doc-only inaccuracy; the wire fixes it regardless. Noted as a surprise. |

---

## [LIVE] Surface events

| When | What was surfaced | Was the surface actionable? | Was the timing right? |
|---|---|---|---|
| After ground_at_red | The 3 D.3.0 findings (awaiter = new break not reuse; recoverable = suspend-and-record; invocation construction from the §4b input) + the painted-behavior grep + the full test/impl plan + scope-lock check + "proceed to write tests?" | Yes — maintainer approved ("approved") | Yes — before any test written, per ground_at_red's "surface before writing tests" |
| After red commit | Right-reason red (the painted `CapabilityViolation`, no gap, no suspend) + the 3 pinned scenarios + red-phase gates (fmt + clippy clean) + the green plan + the strict-TDD commitment | Yes — maintainer approved ("proceed to green") | Yes |

---

## [LIVE] Protocol drift events

None observed. Do-not-commit-without-approval held: ground surfaced + approved before tests; red committed under the ground approval; impl committed under the red ("proceed to green") approval; **no push** (awaiting final approval). Strict-TDD two-commit held — `git diff ada7d52..9e69af2 -- 'crates/runtime-main/tests/**'` is **EMPTY**; the mcp/agent-kind + malformed-path tests landed as a **separate labelled additive follow-up** (v1.8 convention), not folded into impl. No schema change; sandbox untouched (Hard Rule 8 / scope lock). The handler was REUSED (via a collecting `Emitter`), not reimplemented. The awaiter-shape decision was surfaced, not picked (§2).

---

## [LIVE] Surprise events

| What was surprising | Why it surprised | What this implies for the protocol |
|---|---|---|
| The painted behavior is a **CapabilityViolation**, not a `ToolInvoked`. | The phase doc predicted "emits ToolInvoked for an unknown name." But `request_capability` is not in `framework.tools[]`, so `pipeline.next_event`'s L1 enforcer treats it as an undeclared tool and DENIES it (`capability_kind: Exec`). The gap-detection flow not only never fires — the meta-tool is actively rejected. | The red trace is the source of truth for painted behavior, not the phase-doc prediction (rule 11). The "run the assembled thing" close gate caught the real shape; a unit test would not have. |
| There is **no resolve-and-resume machinery** in the run loop at all. | The handler docstring + spec §4b describe a full gap→HITL→grant→resume flow, but no code acts on a `HitlChoice` for a gap. The "suspend" the handler's docstring references has no resume counterpart in v0.1. | The M08.7 thesis confirmed a **fourth** time (after rung-1 grant-loading, rung-2 tier-wiring, rung-3 resolved-body threading): scaffolding built around an execution core never fully wired. Here the handler was built + the docstring describes a resume that does not exist. Rung 4 wires the **suspend half**; the resume half is **pulled forward into v0.1 as a scheduled gap-resume rung (ADR-0029)** — NOT M09-deferred (maintainer decision at close). |

---

## [LIVE] Stage 0 real-app discovery routing

| Stage-0 disposition | Evidence / regression file | Adjacent findings (triaged in-place — zero propagation) |
|---|---|---|
| Replaced by an assembled test (the assembled integration test IS the discovery) + the real-app IRL **required as the close gate** | `crates/runtime-main/tests/gap_detection_execution.rs` — drives the REAL `run_test_session_with → run_agent → drive_stream` loop (only the provider stubbed); asserts the `*Missing` gap (requested_via=request_capability) + the SUSPEND (one provider turn, clean Ok, no `ToolInvoked` for the meta-tool) + suspend-despite-a-co-dispatched-tool (the load-bearing break) + the four kind arms + the malformed no-suspend path | Awaiter-shape finding → resolved in-cluster (new `gap_suspended` break, not the violation awaiter). The uncovered mcp/agent kind arms + the malformed `Continue` path → covered by the additive follow-up (rule 11 — the assembled path is run for every branch). No new TD opened. |

This is a backend stage with an **assembled Rust integration** surface. The cluster's CI close is the **assembled tests observed** (6 tests green against the real loop; the gap + the suspend behavior observed). Per rule 11 the **behavioral** close — a real Anthropic model calling `request_capability` for a missing tool and the session suspending cleanly — is the **IRL gate, maintainer-run**, and is **NOT exercised by CI** (the `GapScriptStub` is not the model). Stated honestly: CI proves the wire + the suspend mechanics; the IRL proves a real model triggers it.

---

## [END] Three-axis scoring

### Axis 1: Process

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | `CLAUDE.md` sufficient orientation? | 5 | §4 rule 11 + §2 + §5 + §6 + §8 directly drove the work; §2 load-bearing for the awaiter-shape surface |
| 2 | "Read first" list oriented correctly? | 5 | spec §4b + the handler + `await_capability_violation_hitl` + the HITL seam were exactly what ground-at-red needed to find the awaiter mismatch |
| 3 | Deliverable + test plan before code? | 5 | D.3.0 findings + the green plan + the BDD-to-test mapping surfaced before any test |
| 4 | Self-correct in ≤3 rounds? | 5 | Gates green effectively first-pass; only mechanical doc-comment clippy fixes; no self-correction loop |
| 5 | Do-not-commit-until-approved held? | 5 | ground → (approved) red → (proceed-to-green) impl; no push |
| 6 | Escalated right things, proceeded on right things? | 5 | Escalated the awaiter-shape disposition at ground (a real design fork); proceeded on the wire design + API as §12 technical calls |
| 7 | Human-direction load reasonable? | 5 | 2 checkpoints (ground, red) + the pending final — appropriate |
| 8 | Surfaces actionable? | 5 | Each surface carried findings + a recommendation + a clear proceed-ask |
| **Process axis total** | | **40/40** | |

### Axis 2: Product

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | Matches §9 style + naming? | 5 | snake_case; the interception branch mirrors the rung-1/rung-3 branch placement; the `dispatch_*` method mirrors `dispatch_builtin`/`dispatch_load_skill`; doc-commented |
| 2 | Behavior tests not tautology (§5)? | 5 | Assertions on the SUSPEND behavior (one turn, clean Ok, suspend-despite-dispatch) + the four kind arms + the no-suspend malformed path, never the event alone; the mutation gate confirms the tests bite |
| 3 | Public APIs documented + compile-checked? | 5 | `REQUEST_CAPABILITY_TOOL`, `request_capability_tool_def`, `parse_capability_kind` fully doc'd; `cargo doc -D missing_docs` green |
| 4 | Deliverables exactly as promised — no creep/cuts? | 5 | The tool def + the interception branch + the suspend wire + the advertisement; the additive tests are rule-11 coverage of the kind arms + the malformed branch, not creep; sandbox untouched; handler reused |
| 5 | Stranger-understandable? | 5 | The test-file module doc + the impl comments explain suspend-and-record, the recoverable claim, and the CI-vs-IRL split |
| 6 | §9 anti-patterns absent? | 5 | No unsafe, no stringly-typed beyond the JSON wire I/O, no silent drops |
| 7 | Coverage measured? | 5 | runtime-main 95.23% line ≥95 (measured, exit 0); request_capability.rs 98.54% |
| 8 | CI green across the matrix? | 3 | Predicted green; only Windows-local run performed (CI Linux/macOS pending the PR run) — stated, not assumed |
| **Product axis total** | | **38/40** | |

### Axis 3: Pattern

| # | Question | Score | Evidence / notes |
|---|---|---|---|
| 1 | All sections useful? | 5 | ground_at_red + close_gate + strict-TDD + adr_triggers all earned their keep; the adr_trigger correctly did NOT fire (suspend-and-record fits ADR-0019) |
| 2 | Sections missing? | 4 | The construction_reachability_check's "the awaiter mirrors `await_capability_violation_hitl`" was a misleading steer — could say "confirm whether a resume path exists before assuming an awaiter" |
| 3 | Gotchas local or generalize? | 4 | "doc-markdown / first-paragraph-length lints on NEW production doc comments only surface at green" generalizes to every strict-TDD code stage |
| 4 | Time-box matched? | 5 | Within the 3–5 h range; the wire as predicted |
| 5 | Implicit protocol moments? | 4 | The "phase-doc/docstring describes a flow whose downstream half does not exist; ground-at-red wires only the reachable half" pattern (fourth M08.7 instance) is worth codifying |
| 6 | Re-runnable by a fresh session? | 5 | The ground findings + the cited surfaces + the decision make it reproducible |
| 7 | Anything to change before copying format? | 4 | Add to ground_at_red: "confirm the downstream half of a described flow (e.g. resume) EXISTS before assuming the upstream half (suspend) reuses its plumbing" |
| **Pattern axis total** | | **31/35** | |

---

## [END] Threshold evaluation

### Hard gates

- [x] **G1: do-not-commit-until-approved held** — ground (approved) → red → (proceed-to-green) impl; **no push**. `git log` shows `ada7d52`, `9e69af2` (+ pending additive/retro) ahead of main.
- [x] **G2: no Severity-5 friction** — max severity 2.
- [x] **G3: no unaddressed protocol drift** — none observed.
- [x] **G4: stage completed** — impl `9e69af2`; all gates pass; 6 assembled tests green against the real loop.
- [x] **G5: every row ≥3** — lowest is 3 (CI-matrix row: Windows-local only, CI pending; honest per rule 11).
- [x] **G6: CI-parity** — gate commands run verbatim per §6 (no CI-divergent flags). Mutation gate via `cargo mutants --in-place --in-diff` (gotcha #92 Windows workaround): 13 mutants, **5 caught, 8 unviable, 0 missed** (exit 0).

### Soft gates

- [x] **S1:** Process 40/40 ≥30
- [x] **S2:** Product 38/40 ≥32
- [x] **S3:** Pattern 31/35 ≥25
- [x] **S4:** Time-box within 2×
- [x] **S5:** ≤3 Severity-3 — zero Severity-3; max severity 2

### Outcome

- [x] **Pattern is sound.** All hard + soft gates pass. **BUT** per the cluster-gate (`cluster-pattern.md` §1 step 4) the cluster does **not** close on tests-green. The **CI assembled tests are observed** (the gap + the suspend mechanics); the **behavioral IRL gate** (a real Anthropic model calls `request_capability` → clean suspend) is **maintainer-run and REQUIRED** to close — stated, not claimed as done by CI.

---

## [END] Coverage holdouts

### Per-package gate (runtime-main ≥95%)

- **Crate:** `runtime-main`
- **Coverage actual this stage:** 95.23% line (TOTAL), exit 0
- **Exclusions added this stage:** None — `request_capability.rs` 98.54%; the `dispatch_request_capability` Ok + Err arms, the interception branch, the suspend break, and all four `parse_capability_kind` arms are exercised by the assembled + additive tests. No new OS-call holdout.
- **Exclusion list as of stage end:** unchanged — `src.main\.rs|generated|src.providers.anthropic\.rs|src.drone_ipc.connection\.rs|src.sandbox_ipc.connection\.rs|src.import.fetch\.rs`

### CI workflow drift check

- [x] No coverage-config change this stage (no exclusion/threshold change) → the four mirrors (`codecov.yml`, CLAUDE.md §5/§6, `docs/coverage-policy.md`) are untouched and consistent. No `docs/coverage-policy.md` §C entry required.

---

## [END] Decisions for the next stage

### Milestone document updates (M08.7 phase doc — rung 5 authoring / future gap-resolution rung)
- [ ] Add to ground_at_red: "confirm the DOWNSTREAM half of a described flow EXISTS before assuming the UPSTREAM half reuses its plumbing. Rung 4 found the handler docstring describes 'await resolution before resuming' but no resolve-and-resume code exists; the suspend is a loop-break (suspend-and-record), not a HITL await."
- [ ] The gap-resolution rung (grant/install/decline → resolve-and-resume) is a **scheduled v0.1 gap-resume rung (ADR-0029 — pulled forward at close, NOT M09-deferred)**. When it lands, the `gap_suspended` break grows an awaiter + a resume path; ADR-0019's Tester model may need an addendum. The `GapEventCollector` / `RequestCapabilityDisposition` seam is the documented extension point.

### Protocol additions
- [ ] cluster-pattern / STAGE-PROMPT-PROTOCOL: note that doc-markdown + `too_long_first_doc_paragraph` clippy lints on NEW production doc comments cannot surface during a pure-behavioral red; they are mechanical green-phase fixes (not a separate follow-up, since they are in the impl files, not the test files).

### Open issues filed
- **ADR-0029** (Proposed) filed at close (maintainer scope decision): resolve-and-resume is pulled forward into v0.1 as a scheduled gap-resume rung, NOT M09-deferred. The rung is authored to full X.1–X.6 detail at its sub-phase entry (its M08.6.7 HITL/grant-UI dependency grounded then, not guessed now).
- **`gap_detection_live.rs`** (eval E-04, `#[ignore]`d) added: the rung-4 live IRL — a real Anthropic model lacking a capability calls `request_capability` → clean suspend. Maintainer-run; on a clean suspend the maintainer flips `docs/execution-status.md` row 4.

---

## [END] Special log (per `<retrospective_requirements>`)

- **D.3.0 ground findings.** (1) **Awaiter shape:** a NEW minimal suspend-break (`TurnFeedback.gap_suspended` → `run_agent` break), NOT a reuse of `await_capability_violation_hitl` — that awaiter's continue-semantics are wrong for a gap (a gap must halt, a violation proceeds) and on the Tester path it is a `test_defaults()` no-op. There is no resolve-and-resume machinery in the run loop for v0.1. (2) **Recoverable:** suspend-and-record (ADR-0019) — the gap event is persisted through `self.emit` → drone signal chain (recoverable §1b) + in `outcome.trace`; the loop halts; the run returns Ok. Resolve-and-resume is NOT exercised. (3) **Invocation construction:** `agent_id` from the dispatch context (gotcha #68), `kind` parsed from `capability_kind` (unknown → Tool, conservative), `name`←`capability_name`, `justification`←`reason`.
- **Resolve-and-resume vs suspend-and-record (rule 11 — the blind spot).** Only **suspend-and-record** was exercised: the session halts at the gap, the gap event is persisted (recoverable), no further provider turn, clean Ok. **Resolve-and-resume was NOT exercised** — there is no grant/install/decline UI and no resume path wired this rung (it is the **scheduled v0.1 gap-resume rung, ADR-0029** — pulled forward at close, NOT M09-deferred). I do NOT claim the full gap-resolution flow works; I claim the suspend half is wired and observed on the assembled path.
- **Mutation-gate result.** `cargo mutants --in-place --in-diff rung4_cluster.diff` on the cluster's production diff (request_capability.rs + agent_sdk.rs + tester.rs additions): 13 mutants, **5 caught, 8 unviable, 0 missed** (exit 0). The viable mutants are the gate/branch logic (the kind routing + the suspend break + the disposition arms); all caught. The unviable ones are value-constructor body replacements (`ToolDef` / the disposition enum are not `Default`) + `json!`-macro arms (not mutated) — consistent with the rung-1/2/3 few-viable-mutant note. No surviving viable mutant on the execution-wiring logic → the blocking gate passes.
- **CI-structural vs IRL-behavioral split (rule 11).** The CI assembled tests prove the WIRE + the SUSPEND mechanics — a `request_capability` `ToolUse` routes to `handle_request_capability` (not `pipeline.next_event`), the `*Missing` gap fires with `requested_via=request_capability`, and the loop suspends (one turn, clean Ok, suspend-even-with-a-co-dispatched-tool). They do NOT prove a real model triggers it: the `GapScriptStub` is not the model. The **behavioral** assertion — a real Anthropic model calling `request_capability` for a missing tool → a clean suspend — is the **IRL gate** (maintainer-run, required to close).
- **What was NOT exercised.** (a) **Resolve-and-resume** (the grant/install/decline UI + the resume path — the **scheduled v0.1 gap-resume rung, ADR-0029**, pulled forward at close). (b) **The live-model IRL** — encoded as `gap_detection_live.rs` (eval E-04, `#[ignore]`d); needs `ANTHROPIC_API_KEY` (CLAUDE.md §10; I have no key in-session); it is **maintainer-run** and REQUIRED to close (on a clean suspend, the maintainer flips `docs/execution-status.md` row 4). (c) The un-wired smoke path (no `capability_wiring`) does not advertise/intercept `request_capability` — that is a non-framework session, out of scope. (d) CI Linux/macOS (Windows-local only, PR-pending). Per rule 11: CI proves the wire + suspend; the live IRL is the behavior gate — I do NOT claim a real model's behavior observed.

---

## [END] User-review notes

- [Empty until user reviews.]

---

## [END] Sign-off

**Claude:** I have completed M08.7.D (rung 4 — gap detection) per the stage prompt. The `drive_stream` interception branch parses a `request_capability` `ToolUse` into a `RequestCapabilityInvocation` (the requesting `agent_id` from the dispatch context — gotcha #68) and routes it to the REUSED `handle_request_capability` (no reimplementation), which emits the `*Missing` gap (`requested_via=request_capability`); the gap is re-emitted through `self.emit` so it persists (recoverable §1b). The suspend wire (`TurnFeedback.gap_suspended` → `run_agent` break) halts the session at the gap EVEN when the turn also dispatched a tool. ground-at-red found the suspend is a loop-break, not the violation awaiter (whose continue-semantics + test-defaults no-op are both wrong for a gap) — surfaced + approved. All machine gates pass (Windows-local, CI-parity commands): clippy 0, workspace test 0 failed, doc 0, rustdoc 0, audit (17 pre-existing allowed warnings), deny ok, runtime-main coverage 95.23% ≥95. Mutation gate: 13 mutants, **5 caught, 8 unviable, 0 missed** (exit 0) — no surviving viable mutant on the wire.

**Per rule 11 / the cluster-gate: the cluster does NOT close on tests-green.** The CI assembled tests prove the WIRE + the SUSPEND mechanics (suspend-and-record). The **behavioral gate** is the live-model IRL — **maintainer-run** (`ANTHROPIC_API_KEY` required; I have no key in-session, so a real model triggering the gap is **unverified by me**). **Resolve-and-resume was NOT exercised** (no grant/install/decline UI wired this rung — it is the **scheduled v0.1 gap-resume rung, ADR-0029**, pulled forward at close, NOT M09-deferred). Strict-TDD held — `git diff ada7d52..9e69af2 -- crates/runtime-main/tests/**` is EMPTY; the mcp/agent-kind + malformed-path tests are a separate labelled additive follow-up. No schema change; sandbox untouched. I have committed (red → impl → additive → retro) but **will not push until you approve**.

**Surfaced at:** 2026-06-01
