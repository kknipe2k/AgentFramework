# M08.7 — Execution Engine (the runtime that actually *runs*)

> **Protocol version:** v1.8 schema + the new **cluster-gate** pattern
> (`docs/cluster-pattern.md`, to be codified in Phase 0). Each rung is a
> cluster: an eval-first BDD acceptance authored before the build, an
> assembled `tests/e2e-tauri/` (or assembled Rust integration) test that
> runs the real path, a mutation gate where tooling exists, and a
> per-rung **IRL gate** (the maintainer watches it run). Strict
> `<tdd_discipline strict="true">` two-commit on every code rung.
>
> **Governed by CLAUDE.md §4 rule 11 (grounded-claims / no-gaslighting).**
> Every "runs / works / executes" claim in this doc is backed by a
> code citation observed this session, OR is explicitly labeled a
> **hypothesis to be grounded at rung entry**. Nothing here asserts
> behavior from structure.

## Background — the grounded engine map

The post-M08.6 IRL walk (`docs/M08.6-irl-findings.md`) and a direct read
of the execution path establish what the runtime **executes** versus what
it **paints** (emits as events for the live graph) or **checks**
(capability gates) without executing. This is verified, not inferred:

**Executes real work (cited):**

- Single-agent multi-turn provider streaming — `AgentSdk::run_agent`
  (`crates/runtime-main/src/sdk/agent_sdk.rs:256`): prelude once, then
  `provider.stream` → `drive_stream` per turn, terminating when a turn
  dispatches no tool (`agent_sdk.rs:261`) or hits `MAX_AGENT_TURNS = 16`.
- MCP tool dispatch — `try_mcp_dispatch` (`agent_sdk.rs:576`) calls the
  injected `McpToolDispatch` seam, emits agent-correct
  `ToolInvoked`/`ToolResult`, and returns a `DispatchedTool` the
  multi-turn loop feeds back as the next turn's `tool_result`
  (`agent_sdk.rs:269-294`). This is the **only** tool path that
  executes + feeds back. (Modulo M08.6 #23 — the Windows `npx` spawn
  bug.)
- L1 capability enforcement — `EventPipeline::next_event(ToolUse)`
  (`crates/runtime-main/src/sdk/event_pipeline.rs:102`) emits
  `ToolInvoked` or, on a blocked check, `CapabilityViolation` and omits
  `ToolInvoked`; the loop routes violations through
  `await_capability_violation_hitl` (`agent_sdk.rs:539`).
- L2a narrowing **check** at spawn — `spawn_framework_subagents`
  (`agent_sdk.rs:467`) runs `narrow(parent_grants, proposed)` per inline
  child and emits `AgentSpawned` or `CapabilityViolation`.
- Event emission + drone persistence + start-of-session snapshot —
  `emit` → `persist_signal` (`agent_sdk.rs:681`,`:717`) and the
  `SnapshotNow` in `session_prelude` (`agent_sdk.rs:370`).

**Painted or checked, NOT executed (verified gap):**

- **Built-in tools** (`Read`/`Write`/`Bash`/`Glob`/`Grep`/`WebFetch`) —
  **no executor exists.** `grep` across `crates/` + `src-tauri/` for any
  built-in tool executor (`execute/run/dispatch_tool`, `read_file`,
  `run_bash`, `ToolExecutor`) returns nothing. A built-in `ToolUse`
  emits `ToolInvoked` via the pipeline and is never run; the multi-turn
  loop only feeds back MCP tools, so `feedback.dispatched` stays empty
  and the loop breaks. The file is never read; no result feeds back.
- **Sub-agents** — `spawn_framework_subagents` emits `AgentSpawned` and
  stops. No child execution context is created; no child runs its own
  `drive_stream`. Only the session root streams.
- **`LoadSkill`** — no handler (grep). Skills are never injected into
  context.
- **gap / `request_capability`** — `handle_request_capability`
  (`crates/runtime-main/src/sdk/request_capability.rs:88`) exists but is
  **not wired into the run loop** (called only from its own tests). A
  model `request_capability` `ToolUse` falls through to
  `pipeline.next_event` (emits `ToolInvoked`) — the gap flow never fires.
- **Plans** — `drive_plan` (`crates/runtime-main/src/plan/plan_loop.rs:79`)
  advances the FSM and emits `plan_approval_requested → plan_approved →
  plan_complete`, but its own docstring states it "runs no tasks"
  (`plan_loop.rs:7-9`,`:128-129`), and it has **no production caller**
  (M08.V 🟡 #2).
- **Hooks / rails** — defined in framework JSON; no firing engine.
- **Modes** — STANDARD hardcoded (gotcha #3); no router.

The summary: M1–M8 built the streaming / event / capability-check / MCP /
Builder scaffolding **around** an execution core that does
provider-streaming + MCP dispatch + checks, and paints the rest. The
*external* protocol path (MCP) executes; the *built-in* path does not.
M08.7 builds the execution layer.

## What M08.7 is (and is not)

**Is:** the milestone that makes the runtime *run* what frameworks
declare — built-in tool execution, skill loading, gap-flow wiring,
sequential multi-agent spawn, plan-driven task execution, hook/rail
firing — climbed as an eval-first **ladder** of minimal frameworks, each
run and IRL-watched, ending at *import → fix → run ARIA* (the v0.1
subset).

**Is not:** concurrent multi-agent orchestration (v1.0 — sequential is
the v0.1 floor that proves the engine; parallel is the validated
high-value follow-on per the multi-agent research); the mode router
(STANDARD stays hardcoded, §0d); a new provider (Anthropic-only); the
capability **grant-right/use-right split** (v1.0 ADR — see "Capability
model" below). The Builder authoring surface is M08.6.7's track, not
this one — M08.7 fixtures are hand-authored JSON loaded from disk, so
M08.7 does not depend on Builder correctness.

## The ladder (full definition; rungs are clusters)

Each rung isolates one function so a break is diagnosable (the
opposite of throwing ARIA at a half-built engine). Rungs 1–5 are the
single-agent execution primitives; 6–8 are coordination/policy; 9 is
the one path that already runs; N is ARIA-as-integration.

| Rung | Function | Reality (cited) | Build |
|---|---|---|---|
| 0 | smoke baseline | ✓ runs (no tools) | none — the baseline |
| 1 | built-in tool execution | ✗ no executor | **build the built-in tool executor + feedback into the multi-turn loop** |
| 2 | capability enforce on a *live* tool | partial (checks; tool never ran) | gate a *real* execution: allowed → runs, blocked → does not |
| 3 | skill load | ✗ no handler | **build `LoadSkill`** (inject skill md into context) |
| 4 | gap detection | handler exists, unwired | **wire `request_capability` into the run loop** |
| 5 | budget | hypothesis: painted | **ground at rung entry**, then make spend drive warn/downshift/stop |
| 6 | sequential spawn ⭐ | ✗ emit-only | **build per-child execution context + sequential coordination** |
| 7 | plan-driven tasks | FSM-only, no caller | **wire `drive_plan` + build the task loop** |
| 8 | hooks + rails | ✗ no engine | **build the firing engine** |
| 9 | MCP | ✓ runs | fix M08.6 #23 (Windows npx) |
| N | ARIA | — | import → fix shape → run the v0.1 subset; document the v1.0 line |

## Milestone structure — three sub-phases, each with a clear ending

Per the maintainer's "clear ending in the current phase" requirement and
the construction-reachability discipline (do not write a detailed spec
that rests on an unverified upstream outcome — under §4 rule 11 that
would itself be an ungrounded claim), M08.7 splits into three sub-phases.
**Only M08.7a is detailed below**; M08.7b and M08.7c are structured, and
their per-rung detail is authored *after* M08.7a's evidence lands —
because rung 6+ specs depend on what the rung 1–5 execution layer
actually requires (e.g., whether per-child execution reuses
`drive_stream` as-is or needs a re-entrant variant is a rung-1/rung-6
empirical question). This deferral is grounded-reason, not effort.

| Sub-phase | Rungs | Ending |
|---|---|---|
| **M08.7a** | 1–5 | a single agent **executes** built-in tools, loads skills, triggers gaps, and respects budget — IRL-watched |
| **M08.7b** | 6–7 | a **multi-agent** framework runs sequentially; a plan drives tasks — IRL-watched |
| **M08.7c** | 8–9, N | hooks/rails fire; MCP fixed; **ARIA runs** the v0.1 subset — IRL-watched |

## Key constraints (all rungs)

- **Eval-first, IRL-gated, zero-propagation.** Each rung's BDD acceptance
  + assembled test is authored and committed *before* the build (red),
  runs the *real* path, and the rung does not close until the maintainer
  IRL-watches it run. Findings from a rung's IRL are triaged in-phase
  (fix-in-rung / new rung / explicit ADR-class scope-out) — never routed
  forward silently (§4 rule 11 + the zero-propagation rule).
- **Grounded claims (§4 rule 11).** Every rung surface states what was
  run and what was not. "Tool executes" requires the assembled app (or
  assembled integration test) to have run the tool and observed the
  result — not a unit test that injects a `ProviderEvent::ToolUse` and
  asserts the emitted event (that licenses "the event is emitted," the
  exact trap that hid this gap).
- **Capability model: strict child ⊆ parent narrowing stays** (validated
  — object-capability theory + AI-agent delegation best practice: you
  may only delegate authority you hold; attenuation only narrows). The
  shipped `narrow()` is correct. M08.6 #31 (ARIA's orchestrator
  under-declares the capabilities it delegates) is fixed at rung N by
  **declaring ARIA's grantable union on the orchestrator**, not by
  weakening narrowing. The **grant-right vs use-right split** (let a
  coordinator be the source of a delegated capability while attenuating
  its own use — AWS PassRole / macaroon pattern) is the correct **v1.0
  ADR**; it still requires the orchestrator to hold a grant-right, never
  to grant from nothing.
- **Sandbox boundary (Hard Rule 8 + §10).** Built-in tools that touch
  the OS (`Bash`, and `Write`/`Read` outside the workspace) execute
  through `crates/runtime-sandbox/`, never directly in main. The tool
  executor's design (rung 1) surfaces a plan for which built-ins route
  through the sandbox vs run in-process under capability scope — this is
  a CODEOWNERS-flagged path; the rung-1 prompt requires the plan before
  code.
- **No new third-party deps without `cargo deny`.** No telemetry. No
  `unsafe` outside `runtime-sandbox`. No schema change unless a rung
  proves one is needed (then: new `*.v2.json` + ADR).
- **Strict v1.8 two-commit TDD** on every code rung; the impl commit's
  `git diff <red>..<impl>` over the test paths is EMPTY.
- **Painted-behavior test migration (ground-at-red, every code rung).** A
  rung that makes a painted behavior *execute* can invalidate existing
  tests that pinned the *painted* behavior — e.g. a test that injects a
  `ProviderEvent::ToolUse` and asserts only `ToolInvoked` is emitted
  (pinning "the tool is painted, not run"). At red, grep the touched area
  for such emit-only ToolUse-injection assertions; migrating them to
  assert the real executed behavior is **in-scope** for the rung. The
  migration is a *semantic* change to existing tests (neither the net-new
  additive follow-up nor a mechanical reformat), so it is disclosed
  explicitly in the strict-TDD surface: the `git diff <red>..<impl>`
  empty-test-diff invariant is stated over the *non-migrated* tests, with
  each migrated test named and its old → new assertion shown.

---

## M08.7a — single-agent execution primitives (rungs 1–5)

> **Rungs 1–2 are authored to full build standard below (X.1–X.6 incl.
> the CLI stage prompt) — build-ready.** Rungs 3–4 carry their problem /
> deliverable / BDD acceptance here; their X.3 detailed-changes + X.5 CLI
> prompts are authored at each rung's entry (against the evidence the
> prior rung lands). Rung 5 (budget) additionally carries an explicit
> **ground-at-entry** step because the budget-tracking surface was not
> read at authoring time and §4 rule 11 forbids speccing it from
> assumption.

### Rung 1 (stage M08.7.A) — built-in tool execution

**1.1 Problem (cited).** A built-in `ProviderEvent::ToolUse` reaches
`drive_stream` (`agent_sdk.rs:412`), is offered to `try_mcp_dispatch`
which returns `Ok(None)` (not an MCP tool, `agent_sdk.rs:607`), and falls
through to `pipeline.next_event` which emits `ToolInvoked` and stops.
Nothing executes the tool; nothing feeds a `tool_result` back; the
multi-turn loop sees empty `feedback.dispatched` and breaks
(`agent_sdk.rs:261`). The agent cannot do work with a built-in tool.

**1.2 Deliverable (scoped — grounded).** A built-in tool executor for the
**in-process, capability-scoped file tools** — `Read` first, `Write` its
close sibling — that, on a `ProviderEvent::ToolUse` naming an in-process
built-in: builds the `CapabilityDeclaration` for the op and calls
`CapabilityEnforcer::check(agent, &decl)` (`enforcer.rs:215`); on `Ok`,
executes the file op against the agent's `file_access` scope, emits the
agent-correct `ToolInvoked` + `ToolResult`, and returns a result the
existing multi-turn feedback path (`agent_sdk.rs:269-294`) re-streams to
the model; on `Err`, the existing `CapabilityViolation` path carries it
(rung 2 verifies that half). Mirrors the MCP path's
`try_mcp_dispatch → DispatchedTool → feedback` shape so built-in and MCP
tools converge on **one** feedback contract.

**Sandbox-boundary plan (Hard Rule 8 — grounded, load-bearing scope
lock).** Verified: `SandboxRequest` is `ValidateArtifact | Shutdown`
(`runtime-sandbox/src/protocol.rs:29`) and `SandboxClient` exposes only
`validate()` + `shutdown()` (`sandbox_ipc/client.rs`) — **the sandbox
validates artifacts; it has no command-execution path.** Therefore:
- **Rung 1 scope = in-process file tools only** (`Read`/`Write` against
  paths inside the agent's `file_access` glob scope). The capability
  scope IS the boundary — an op outside scope is denied by
  `enforcer.check`; no process is spawned, no sandbox call is made.
- **`Bash` + any OS-spawning or out-of-scope op is a SEPARATE later rung**
  — it requires a new `SandboxRequest::RunCommand` variant + sandbox-side
  handler + `SandboxClient` method + the sandboxed spawn (seccomp /
  landlock / Job Objects around the child). That is an **IPC-protocol
  change → ADR-class (§11)** and a CODEOWNERS sandbox change. **Rung 1
  does NOT touch the sandbox protocol.** If the build finds it cannot
  satisfy rung 1 without the sandbox, it STOPS and surfaces — that is the
  signal to split the sandbox-exec rung with its ADR, not to widen rung 1.

**1.3 Detailed changes (grounded; finalized at red-phase against a fresh
read).** Verified surfaces the build wires:
- `drive_stream` (`agent_sdk.rs:412`) today branches MCP
  (`try_mcp_dispatch`) → else `pipeline.next_event` (emit-only). Add a
  built-in-execute branch BETWEEN them: a `ToolUse` naming an in-process
  built-in routes to the new executor; its result joins
  `feedback.dispatched` (the `TurnFeedback` / `DispatchedTool` shapes,
  `agent_sdk.rs:80-97`) so the multi-turn loop (`agent_sdk.rs:256-294`)
  re-streams it. Not-a-built-in falls through to `pipeline.next_event`
  unchanged. This is the one construction-reachability wire (§ check
  below).
- A new executor module (e.g. `sdk/builtin_tools.rs`) — `&Path`-
  parameterised per the path-agnostic archetype (CLAUDE.md §9),
  capability-checked via `enforcer.check`, `tempfile`-testable. Reads
  return `ToolResultContent::Text` (`providers/mod.rs:267`); the
  `DispatchedTool` `value` carries it back.
- `AgentConfig.tools: Vec<ToolDef>` (`providers/mod.rs:288`) — the built-in
  `allowed_tools` must be advertised as `ToolDef`s (name + description +
  `input_schema`) so the model emits the `ToolUse`.
- **Ground-at-red sub-step A:** read `test_agent_config` (`tester.rs:332`)
  + the smoke `AgentConfig` build (`commands.rs`) to confirm whether
  `config.tools` is populated today; if empty, populating it for the
  framework's built-ins is part of rung 1.
- **Ground-at-red sub-step B:** `run_test_session_with` builds
  `CapabilityEnforcer::new()` (`tester.rs:322-323`) — a fresh enforcer has
  no grants, and `check` denies with `NoDeclarations` when an agent has
  none (`enforcer.rs:237`). Verify how/where the agent's `file_access`
  grants are loaded into the enforcer for a run; resolve before asserting
  an allowed-path execution (else every tool is denied). This may itself
  be a gap rung 1 closes or a wiring rung 1 depends on — surface which.

**1.4 Acceptance (BDD — authored before the build; the close contract).**

```
Feature: A single agent executes a built-in tool
  Scenario: Read feeds file contents back into the agent's turn
    Given a one-agent framework whose agent has Read in allowed_tools
      And file_access.read covers the fixture path
      And a workspace file fixture "hello.txt" containing "ladder-rung-1"
     When I run the framework with task "read hello.txt and quote it"
     Then a ToolInvoked(Read) event is emitted
      And the agent's NEXT turn receives the file contents as a tool_result
      And the agent's final text contains "ladder-rung-1"
```

The last two `Then`s are the grounded-claims load-bearing assertions
(§4 rule 11) — they distinguish "the tool *ran* and *fed back*" from "the
event was emitted" (the trap that hid this gap). The **assembled test**
drives the real run loop via `run_test_session_with` (`tester.rs:295`)
against a `tempfile` workspace fixture. The **IRL close gate** = the
maintainer runs the framework and watches the agent quote the file. The
cluster does not close on tests-green — only on the assembled run +
IRL observation (`docs/cluster-pattern.md` §1 step 4).

**1.5 CLI prompt (build CLI consumes this).**

```xml
<work_stage_prompt id="M08.7.A">
  <context>
    M08.7 rung 1 (stage M08.7.A) — the built-in tool executor, the
    foundational execution primitive. Today built-in tools are PAINTED,
    not run: a ProviderEvent::ToolUse for Read reaches drive_stream
    (agent_sdk.rs:412), falls through try_mcp_dispatch (not MCP) to
    pipeline.next_event which emits ToolInvoked and stops — the file is
    never read, nothing feeds back, the multi-turn loop breaks. Build an
    in-process, capability-scoped executor for Read (+ Write) that runs
    the op behind CapabilityEnforcer::check, emits ToolInvoked/ToolResult,
    and returns a result the existing multi-turn feedback path
    (agent_sdk.rs:269-294) re-streams — mirroring the MCP path's
    try_mcp_dispatch → DispatchedTool → feedback shape. SCOPE LOCK: Bash
    and any OS-spawning/out-of-scope op are OUT — the sandbox has no exec
    path (SandboxRequest = ValidateArtifact|Shutdown) and adding one is a
    separate ADR-class rung. Do NOT touch the sandbox protocol.
    This stage runs under the CLUSTER-GATE (docs/cluster-pattern.md): the
    BDD acceptance is the close contract; the cluster does NOT close on
    tests-green — only on the assembled run observed (CI e2e/integration)
    + the maintainer IRL watch.
  </context>

  <read_first>
    <file>CLAUDE.md (esp. §4 rule 11 grounded-claims; §5 TDD; §6 gates; §8 commit workflow; §10 Hard Rule 8 sandbox)</file>
    <file>docs/cluster-pattern.md (the close discipline this stage runs under — §1 the five steps, §2 zero-propagation, §4 acceptance-assertion rule, §5 mutation gate)</file>
    <file>docs/build-prompts/M08.7-execution-engine.md (Background engine map; Key constraints; M08.7a; rung 1 §1.1–1.6 incl. the sandbox-boundary plan)</file>
    <file>docs/gotchas.md (#22 subprocess test path; #56 Windows llvm-cov; #66 tests-pass-contract-fails; #81 llvm-cov clean)</file>
    <file>docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md</file>
  </read_first>

  <read_reference>
    <file purpose="the run loop + the built-in-execute branch site + the multi-turn feedback path + DispatchedTool/TurnFeedback shapes">crates/runtime-main/src/sdk/agent_sdk.rs</file>
    <file purpose="the MCP path to mirror (try_mcp_dispatch → DispatchedTool → feedback)">crates/runtime-main/src/sdk/agent_sdk.rs</file>
    <file purpose="AgentConfig.tools + ToolDef + ProviderEvent::ToolUse + ToolResultContent">crates/runtime-main/src/providers/mod.rs</file>
    <file purpose="CapabilityEnforcer::check signature + DenyReason::NoDeclarations">crates/runtime-main/src/capability/enforcer.rs</file>
    <file purpose="CapabilityDeclaration + scope glob matching">crates/runtime-main/src/capability/declaration.rs</file>
    <file purpose="the assembled run path + test_agent_config + the fresh-enforcer ground-step">crates/runtime-main/src/builder/tester.rs</file>
    <file purpose="confirm there is NO sandbox exec variant (scope lock)">crates/runtime-sandbox/src/protocol.rs</file>
  </read_reference>

  <deliverable ref="docs/build-prompts/M08.7-execution-engine.md" section="1.2 Deliverable (scoped — grounded)."/>

  <test_plan_required>true</test_plan_required>

  <tdd_discipline strict="true">
    <red_phase>
      First complete the two ground-at-red sub-steps (1.3 A + B) and
      surface what they found (is config.tools populated? how are the
      enforcer's file_access grants loaded?) — these may reshape the
      build. Then write the failing tests: the assembled regression
      (run_test_session_with against a tempfile workspace fixture,
      asserting the agent's final text quotes the file — the §1.4 BDD
      contract) + executor unit tests (capability-scoped read allowed;
      read outside file_access scope denied; result shape feeds back).
      Confirm right-reason failure (no executor → the agent never quotes
      the file). Run npx-free Rust gates + clippy BEFORE the red commit.
      Commit STANDALONE test(M08.7.A): ... and surface for red approval.
    </red_phase>
    <green_phase>
      Add the executor + the drive_stream built-in branch + the
      config.tools advertisement until the tests pass. Do NOT modify the
      test files. The impl commit body states git diff &lt;red&gt;..&lt;impl&gt;
      over the test paths is EMPTY. No Co-Authored-By. Do NOT touch the
      sandbox protocol (scope lock) — if the build cannot proceed without
      it, STOP and surface (it is a separate ADR-class rung).
    </green_phase>
  </tdd_discipline>

  <execution_steps>
    <step name="ground_at_red" budget_iterations="1" note="1.3 sub-steps A+B; surface findings before writing tests"/>
    <step name="write_failing_tests" budget="1"/>
    <step name="red_phase_clippy" budget="1"/>
    <step name="red_phase_commit" budget="1"/>
    <step name="surface_for_red_approval"/>
    <step name="implement" budget="1"/>
    <step name="verify_gates" budget_iterations="3"/>
    <step name="mutation_gate" note="cargo-mutants on the executor module + the drive_stream branch; a surviving mutant on the cluster's logic blocks close or is justified inline"/>
    <step name="green_phase_commit" budget="1"/>
    <step name="surface_for_final_approval"/>
    <step name="fill_retrospective"/>
  </execution_steps>

  <acceptance_criteria ref="docs/build-prompts/M08.7-execution-engine.md" section="1.4 Acceptance (BDD — authored before the build; the close contract)."/>

  <scope_locks>
    <lock>In-process Read/Write only. NO Bash, NO process spawn, NO sandbox-protocol change — that is a separate ADR-class rung.</lock>
    <lock>One feedback contract — built-in results join feedback.dispatched exactly as MCP results do; do not fork the multi-turn loop.</lock>
    <lock>Capability scope is the boundary for in-process file ops — every op goes through enforcer.check; an out-of-scope op is denied, not executed.</lock>
  </scope_locks>

  <gates milestone="M08"/>

  <coverage_gate>
    <gate scope="runtime-main" target_lines="95"/>
  </coverage_gate>

  <self_correction_budget>3</self_correction_budget>

  <adr_triggers>
    <trigger>If rung 1 cannot be satisfied without a sandbox exec path, STOP — a new SandboxRequest::RunCommand variant is an IPC-protocol change requiring an ADR (§11) + CODEOWNERS review. Do not add it under rung 1.</trigger>
    <trigger>If config.tools / the enforcer-grant-loading ground-steps reveal a missing wire that is larger than rung 1, surface it for triage (fix-in-cluster vs new cluster) — zero-propagation (cluster-pattern.md §2).</trigger>
  </adr_triggers>

  <gotchas>
    <trap>#66 — assert the agent QUOTES the file (observable behavior), not just that ToolInvoked was emitted. The emitted-event assertion is the trap that hid this gap.</trap>
    <trap>#22 — any subprocess-spawning test derives paths from current_exe(); #56 — Windows llvm-cov may need --test-threads=1 (cite inline if used).</trap>
    <trap>The fresh CapabilityEnforcer::new() in run_test_session_with has no grants — a naive assembled test will see every tool denied until the grant-loading ground-step (1.3 B) is resolved.</trap>
  </gotchas>

  <execution_warnings>
    <warning>Do NOT touch crates/runtime-sandbox/ or its protocol — rung 1 is in-process only (scope lock + Hard Rule 8).</warning>
    <warning>The cluster does NOT close on tests-green. After green + gates + mutation, the CI assembled test must run, and the stage surfaces for the maintainer's IRL watch (cluster-pattern.md §1 step 4). State explicitly what was run and what was NOT (rule 11).</warning>
  </execution_warnings>

  <construction_reachability_check>
    <wire claim="route a built-in ToolUse through the executor into feedback.dispatched"
          constructor="drive_stream (crates/runtime-main/src/sdk/agent_sdk.rs:412)"
          inputs_reachable="true — drive_stream already owns the ProviderEvent::ToolUse + agent_id + the TurnFeedback it returns (agent_sdk.rs:80-97,412-456); the executor is a pure fn it calls between the MCP branch and pipeline.next_event"/>
  </construction_reachability_check>

  <pre_flight_check>
    <check name="branch">on the M08.7 build branch cut from main</check>
    <check name="problem_live">a built-in Read ToolUse today emits ToolInvoked but never reads the file (confirm against agent_sdk.rs drive_stream before red)</check>
  </pre_flight_check>

  <runtime_environment os="windows"/>

  <time_box hours="4-6"/>

  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>Record the two ground-at-red findings (config.tools population; enforcer grant-loading). Confirm the assembled test drives the real run loop and asserts the agent quotes the file (not the event). State the mutation-gate result. State explicitly that the cluster closed on the assembled run + IRL, not tests-green (rule 11) — and what, if anything, was NOT exercised.</special_log>
  </retrospective_requirements>

  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.7-execution-engine.md" section="1.6 Commit message."/>

  <approval_surface>
    <item>cross-machine state (verbatim git log --oneline main..HEAD + retro listing)</item>
    <item>the two ground-at-red findings (config.tools; enforcer grants) — verbatim</item>
    <item>strict-TDD invariant proof (verbatim git diff &lt;red&gt;..&lt;impl&gt; over test paths = empty)</item>
    <item>closure proof: the assembled test output (agent quoted "ladder-rung-1" after a real Read) + the IRL watch result — NOT "tests green"</item>
    <item>gate results (each gate: command + verbatim result; runtime-main ≥95; mutation-gate result)</item>
    <item>retrospective three-axis table + what was NOT exercised (rule 11)</item>
    <item>explicit: "M08.7.A is ready. I will not push until you approve."</item>
  </approval_surface>
</work_stage_prompt>
```

**1.6 Commit message.**

```
feat(runtime): M08.7.A — built-in tool executor (Read/Write run + feed back)

Built-in tools were painted, not run: a ProviderEvent::ToolUse for Read
reached drive_stream, fell through try_mcp_dispatch to pipeline.next_event
(emit ToolInvoked, stop) — the file was never read, nothing fed back, the
multi-turn loop broke (agent_sdk.rs:261). Only MCP tools executed.

Fix: an in-process, capability-scoped built-in executor. drive_stream
gains a built-in-execute branch between the MCP branch and
pipeline.next_event: a ToolUse naming an in-process built-in (Read/Write)
goes through CapabilityEnforcer::check, executes against the agent's
file_access scope, emits ToolInvoked/ToolResult, and returns a result
that joins feedback.dispatched — the same multi-turn feedback contract
MCP uses, so the model receives the file contents on the next turn.

Scope: in-process Read/Write only. Bash + sandboxed exec are a separate
ADR-class rung (the sandbox has no RunCommand path today — protocol.rs);
the sandbox protocol is untouched.

Cluster-gate close: the assembled test (run_test_session_with against a
tempfile fixture) asserts the agent QUOTES the file — observed running,
not an emitted-event check (rule 11 / gotcha #66) — and the stage
surfaced for the maintainer IRL watch. Strict v1.8 two-commit TDD: the
git diff <red>..<impl> over the test paths is EMPTY.

https://claude.ai/code/session_<id>
```

### Rung 2 — capability enforcement on a live tool

**2.1 Problem (cited).** `EventPipeline` enforcement
(`event_pipeline.rs:102`, `with_enforcement` at `agent_sdk.rs:391`)
already emits `CapabilityViolation` and omits `ToolInvoked` on a blocked
check — but because no built-in tool executed anyway (rung 1), "blocked"
and "allowed" were behaviorally identical (neither ran). Once rung 1
makes allowed tools *run*, rung 2 verifies blocked tools *do not run*.

**2.2 Deliverable.** With rung 1's executor in place: an allowed built-in
executes; a built-in outside the agent's capability scope is blocked
(no execution, `CapabilityViolation` emitted, HITL routed). The gate now
gates a real side effect.

**2.3 Detailed changes (grounded at entry against rung 1's landed code).**
Rung 1 landed the executor that calls `enforcer.check` before executing,
and `grant_framework_capabilities` that loads the agent's `file_access`
grants into the enforcer; its tests already cover scope-based **read**
denial (`read_outside_file_access_scope_is_denied_and_not_executed`) and
tier-based **write** block (`write_under_novice_tier_is_blocked_and_creates_no_file`).
Rung 2 is therefore **likely verification-leaning** — but that is a
hypothesis to ground, not an assertion (§4 rule 11). Two ground-at-entry
steps decide the rung's shape:

- **Ground-at-entry A — is scope-based WRITE enforced, or only tier?**
  Read the landed executor (`builtin_tools.rs`), `enforcer.check`, and the
  rung-1 denied-path tests. The §2.4 scenario blocks a write to `secret/x`
  when `file_access.write` is `["out/**"]` (glob scope) — distinct from
  the tier block rung 1 tested. If the executor's write declaration
  already carries the target path and `enforcer.check` matches it against
  the `file_access.write` glob, scope-write is enforced → rung 2 is
  verification (pin it assembled + mutation + IRL). If not, scope-write is
  a **gap rung 2 closes** → strict two-commit TDD applies. State which.
- **Ground-at-entry B — does `CapabilityViolation → HITL` surface in the
  assembled app?** The §2.4 contract requires a HITL prompt to surface.
  The L1 violation routes through `await_capability_violation_hitl`
  (`agent_sdk.rs:539`), but rung 1's IRL found a renderer observability
  gap (TD-034 — the Tester shows no agent text). Confirm whether the
  violation reaches a **user-observable** HITL surface in the running app.
  If it does not, that is a rung-2 finding → triage in-place (fix-in-rung
  if small; else a new cluster) — zero-propagation, never routed forward.

The two assembled tests are authored regardless of A's outcome: a blocked
write (run loop → `Write` to an out-of-scope path → assert **no file on
disk** at that path + `CapabilityViolation` emitted) and an allowed write
(run loop → `Write` inside scope → assert **the file exists on disk with
the written content**). Both drive the real run loop via
`run_test_session_with` (`tester.rs:295`) against a `tempfile` workspace.
Verify right-reason: if the tests pass immediately (verification case),
confirm they bite by temporarily breaking `enforcer.check` — the blocked
test must then create the file — before reverting; the mutation gate
formalizes this.

**2.4 Acceptance (BDD).**

```
Feature: Capability enforcement gates real execution
  Scenario: A write outside file_access scope does not run
    Given a one-agent framework whose agent's file_access.write is ["out/**"]
     When I run a task that tries to Write to "secret/x"
     Then a CapabilityViolation event is emitted
      And NO file is created at "secret/x" on disk
      And a HITL prompt surfaces
  Scenario: A write inside scope does run
     When I run a task that writes to "out/x"
     Then the file "out/x" exists on disk with the written content
```

"NO file on disk" / "file exists on disk" are the grounded assertions —
observable side effects, not events.

**2.5 CLI prompt (build CLI consumes this).**

```xml
<work_stage_prompt id="M08.7.B">
  <context>
    M08.7 rung 2 (stage M08.7.B) — capability enforcement on a LIVE tool.
    Rung 1 made built-in tools execute; before it, "blocked" and "allowed"
    were behaviorally identical (neither ran). Rung 2 verifies that now
    that allowed tools RUN, a tool outside the agent's capability scope
    does NOT run — the gate gates a real side effect. This is LIKELY
    verification-leaning (rung 1 already wired enforcer.check into the
    executor and tested scope-read denial + tier-write block), but that is
    a HYPOTHESIS to ground at entry, not an assertion (rule 11). Two
    ground-at-entry steps (§2.3 A + B) decide the rung's shape: (A) is
    scope-based WRITE enforced or only tier? (B) does CapabilityViolation
    surface a HITL prompt in the assembled app? Author the two assembled
    tests regardless (blocked write → NO file on disk; allowed write →
    file on disk with content). This stage runs under the CLUSTER-GATE
    (docs/cluster-pattern.md): the BDD acceptance is the close contract;
    the cluster does NOT close on tests-green — only on the assembled run
    observed + the maintainer IRL watch of a blocked write.
  </context>

  <read_first>
    <file>CLAUDE.md (esp. §4 rule 11 grounded-claims; §5 TDD; §6 gates; §8 commit workflow; §10 Hard Rule 8 sandbox)</file>
    <file>docs/cluster-pattern.md (the close discipline — §1 the five steps, §2 zero-propagation, §4 acceptance-assertion rule, §5 mutation gate)</file>
    <file>docs/build-prompts/M08.7-execution-engine.md (Key constraints incl. the painted-behavior test-migration note; M08.7a; rung 2 §2.1–2.6)</file>
    <file>docs/build-prompts/retrospectives/M08.7.A-retrospective.md (rung 1's landed evidence + the two ground-at-red findings + TD-034 — the state rung 2 grounds against)</file>
    <file>docs/gotchas.md (#66 tests-pass-contract-fails; #22 subprocess test path; #81 llvm-cov clean; + the rung-1-added entries)</file>
    <file>docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md</file>
  </read_first>

  <read_reference>
    <file purpose="rung 1's landed executor — does the write declaration carry the target path for scope matching? (ground-at-entry A)">crates/runtime-main/src/sdk/builtin_tools.rs</file>
    <file purpose="rung 1's drive_stream built-in branch + the CapabilityViolation → await_capability_violation_hitl route (ground-at-entry B)">crates/runtime-main/src/sdk/agent_sdk.rs</file>
    <file purpose="enforcer.check + file_access glob matching — does it gate scope-write, or only tier?">crates/runtime-main/src/capability/enforcer.rs</file>
    <file purpose="grant_framework_capabilities — how the agent's file_access scope reaches the enforcer">crates/runtime-main/src/framework_loader/capability_map.rs</file>
    <file purpose="the assembled run path the tests drive">crates/runtime-main/src/builder/tester.rs</file>
    <file purpose="rung 1's landed denied-path tests — what is already covered vs the §2.4 scope-write delta">crates/runtime-main/tests/builtin_tool_execution.rs</file>
  </read_reference>

  <deliverable ref="docs/build-prompts/M08.7-execution-engine.md" section="2.2 Deliverable."/>

  <test_plan_required>true</test_plan_required>

  <tdd_discipline strict="true">
    <red_phase>
      First complete ground-at-entry A + B (§2.3) and surface what they
      found — these decide whether rung 2 is a code rung or verification.
      Then author the two assembled tests (blocked Write outside
      file_access.write scope → NO file on disk + CapabilityViolation;
      allowed Write inside scope → file exists on disk with content), both
      driving the real loop via run_test_session_with against a tempfile
      workspace. Grep the touched test area for any emit-only
      ToolUse-injection assertions that pinned the PAINTED (pre-rung-1)
      behavior; migrate + disclose them per the Key-constraints
      painted-behavior-migration note. If A finds scope-write UNenforced,
      the blocked test fails at red (right-reason) → strict two-commit
      applies. If A finds it enforced, the tests pass immediately
      (verification) — confirm they BITE by temporarily breaking
      enforcer.check (the blocked test must then create the file) and
      reverting; the mutation gate formalizes this. Run npx-free Rust
      gates + clippy BEFORE the commit. Commit STANDALONE
      test(M08.7.B): ... and surface for red approval.
    </red_phase>
    <green_phase>
      ONLY if ground-at-entry A found a scope-write gap: close it (minimal
      change to the executor's write declaration / enforcer wiring) until
      the blocked test passes, WITHOUT modifying the test files; the impl
      commit body states git diff &lt;red&gt;..&lt;impl&gt; over the test
      paths is EMPTY (excluding any disclosed migrations, named in the
      body). If rung 2 is verification (no gap), there is NO impl commit —
      state that explicitly. Do NOT add new tool-execution code (rung 1
      owns it). Do NOT touch the sandbox protocol (scope lock).
    </green_phase>
  </tdd_discipline>

  <execution_steps>
    <step name="ground_at_entry" budget_iterations="1" note="§2.3 A (scope-write enforced or only tier?) + B (does CapabilityViolation surface HITL in the assembled app?); surface findings before writing tests"/>
    <step name="write_assembled_tests" budget="1"/>
    <step name="red_phase_clippy" budget="1"/>
    <step name="red_phase_commit" budget="1"/>
    <step name="surface_for_red_approval"/>
    <step name="implement" budget="1" note="ONLY if ground-at-entry A found a scope-write gap; else skip — a verification rung has no impl commit"/>
    <step name="verify_gates" budget_iterations="3"/>
    <step name="mutation_gate" note="cargo-mutants on the enforcer-check / executor scope-path logic; a surviving mutant on the gating logic blocks close or is justified inline. If verification-only, this is the bite-proof for the pinning tests."/>
    <step name="green_phase_commit" budget="1" note="impl commit only in the gap case"/>
    <step name="surface_for_final_approval"/>
    <step name="fill_retrospective"/>
  </execution_steps>

  <acceptance_criteria ref="docs/build-prompts/M08.7-execution-engine.md" section="2.4 Acceptance (BDD)."/>

  <scope_locks>
    <lock>Verification of rung 1's enforcement on a LIVE tool. No new tool-execution code — rung 1 owns the executor. Rung 2 adds assembled tests + (only if A finds a gap) a minimal scope-write fix.</lock>
    <lock>Assertions are observable side effects: NO file on disk (blocked) / file on disk with content (allowed) — never the CapabilityViolation event alone (gotcha #66).</lock>
    <lock>If ground-at-entry B finds CapabilityViolation does NOT surface a HITL prompt in the assembled app, triage in-place (fix-in-rung if small; else a new cluster) — zero-propagation, never routed forward (cluster-pattern.md §2).</lock>
    <lock>No sandbox-protocol change. No Bash. In-process Read/Write scope only.</lock>
  </scope_locks>

  <gates milestone="M08"/>

  <coverage_gate>
    <gate scope="runtime-main" target_lines="95"/>
  </coverage_gate>

  <self_correction_budget>3</self_correction_budget>

  <adr_triggers>
    <trigger>If closing a scope-write gap would require a capability-model or schema change (not just wiring the target path into the write declaration), STOP and surface — that is ADR-class (§11), not a rung-2 in-line fix.</trigger>
  </adr_triggers>

  <gotchas>
    <trap>#66 — assert NO file on disk for the blocked write (observable side effect), not just that CapabilityViolation was emitted. The emitted-event assertion is the trap.</trap>
    <trap>The painted-behavior test-migration note (Key constraints) — grep for emit-only ToolUse-injection assertions invalidated now that tools run; migrate + disclose.</trap>
    <trap>#22 subprocess test path; #81 cargo llvm-cov clean before coverage if any llvm-cov ran this session.</trap>
  </gotchas>

  <execution_warnings>
    <warning>Do NOT touch crates/runtime-sandbox/. Do NOT add new tool-execution code — rung 2 verifies rung 1's enforcement; the only code permitted is a minimal scope-write fix IF ground-at-entry A proves a gap.</warning>
    <warning>The cluster does NOT close on tests-green. After green + gates + mutation, the assembled tests must run, and the stage surfaces for the maintainer's IRL watch of a BLOCKED write (no file appears on disk). State explicitly what was run and what was NOT (rule 11) — including whether HITL surfaced (ground-at-entry B).</warning>
  </execution_warnings>

  <construction_reachability_check>
    <wire claim="a scope-violating Write reaches enforcer.check and is denied before any filesystem write"
          constructor="the rung-1 built-in branch in drive_stream → builtin_tools executor → enforcer.check"
          inputs_reachable="ground-at-entry A confirms the write declaration carries the target path so enforcer.check can match it against file_access.write; if it does not, that is the gap rung 2 closes"/>
    <wire claim="a denied write surfaces a HITL prompt in the assembled app"
          constructor="await_capability_violation_hitl (agent_sdk.rs:539)"
          inputs_reachable="ground-at-entry B — confirm the violation reaches a user-observable HITL surface in the running app, not only an internal route (TD-034 renderer observability gap)"/>
  </construction_reachability_check>

  <pre_flight_check>
    <check name="branch">on the M08.7 build branch (claude/m08.7-execution-engine), rung 1 landed</check>
    <check name="problem_live">confirm against rung 1's landed code that an allowed in-scope write RUNS (rung 1) so a blocked out-of-scope write not running is a meaningful gate (else there is nothing to verify)</check>
  </pre_flight_check>

  <runtime_environment os="windows"/>

  <time_box hours="2-4"/>

  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>Record ground-at-entry A (scope-write enforced or only tier — and whether rung 2 was code or verification) + B (did HITL surface in the assembled app). Confirm the assembled tests assert no-file-on-disk / file-on-disk (not the event). State the mutation-gate result (or the bite-proof for pinning tests). State explicitly that the cluster closed on the assembled run + the IRL blocked-write watch, not tests-green (rule 11) — and what was NOT exercised. List any migrated painted-behavior tests.</special_log>
  </retrospective_requirements>

  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.7-execution-engine.md" section="2.6 Commit message."/>

  <approval_surface>
    <item>cross-machine state (verbatim git log --oneline main..HEAD + retro listing)</item>
    <item>ground-at-entry A + B findings — verbatim (scope-write enforced? code or verification rung? HITL surfaces?)</item>
    <item>strict-TDD invariant proof (verbatim git diff &lt;red&gt;..&lt;impl&gt; over test paths = empty, excluding named disclosed migrations) — or explicit "verification rung, no impl commit"</item>
    <item>closure proof: the assembled test output (blocked write left NO file on disk; allowed write created the file) + the IRL blocked-write watch result — NOT "tests green"</item>
    <item>gate results (each gate: command + verbatim result; runtime-main ≥95; mutation-gate result)</item>
    <item>retrospective three-axis table + what was NOT exercised (rule 11)</item>
    <item>explicit: "M08.7.B is ready. I will not push until you approve."</item>
  </approval_surface>
</work_stage_prompt>
```

**2.6 Commit message.**

```
test(runtime): M08.7.B — capability enforcement gates live tool execution

(If ground-at-entry A proves a scope-write gap rung 2 closes, the type is
fix(runtime) and the body adds the minimal fix + the empty red→impl test
diff; if rung 2 is verification, the type stays test and the body states
"no impl commit — pinning tests, bite-proven by the mutation gate.")

Rung 1 made built-in tools execute; before it, blocked and allowed tools
were behaviorally identical (neither ran). Rung 2 makes the gate gate a
real side effect: an in-scope Write runs (file appears on disk); an
out-of-scope Write does not (CapabilityViolation emitted, no file on
disk, HITL routed).

Assembled tests drive the real run loop via run_test_session_with against
a tempfile workspace and assert observable side effects — file present /
absent on disk — not the CapabilityViolation event alone (gotcha #66).

Cluster-gate close: the assembled tests ran and the maintainer IRL-watched
a blocked write leave no file on disk — observed, not "tests green"
(rule 11). Ground-at-entry A/B findings + any migrated painted-behavior
tests are recorded in the retro.

https://claude.ai/code/session_<id>
```

### Rung 3 — skill load (`LoadSkill`)

**3.1 Problem (cited).** No `LoadSkill` handler exists (grep). A skill in
`allowed_skills` is never injected into the agent's context; the agent
cannot follow skill instructions.

**3.2 Deliverable.** A `LoadSkill` handler that, on the agent invoking it
(or per the framework's load policy — grounded at entry against the
skill model in `crates/runtime-core`), reads the resolved skill markdown
(M08.6's loader already resolves skill companions to bodies) and injects
it into the agent's context for subsequent turns, emitting `SkillLoaded`
(`event.rs:279`).

**3.4 Acceptance (BDD).**

```
Feature: A skill changes agent behavior
  Scenario: Loading a skill makes the agent follow its instruction
    Given a skill "shout.md" whose body says "always reply in ALL CAPS"
      And a one-agent framework with shout in allowed_skills
     When I run a task "say hello" that loads the skill first
     Then a SkillLoaded(shout) event is emitted
      And the agent's reply is in all caps
```

The "reply is in all caps" assertion proves the skill content reached the
model — behavior, not the `SkillLoaded` event alone.

### Rung 4 — gap detection (`request_capability`)

**4.1 Problem (cited).** `handle_request_capability`
(`request_capability.rs:88`) exists but is not called from the run loop;
a model `request_capability` `ToolUse` falls through to
`pipeline.next_event` and is treated as an ordinary (unknown) tool. The
signature gap-detection flow never fires.

**4.2 Deliverable.** Wire `drive_stream` to route a
`request_capability` `ToolUse` into `handle_request_capability`, which
emits the appropriate `*Missing` gap event (`event.rs:479-527`) and
suspends the session cleanly per the gap flow (spec §4) — the project's
signature behavior.

**4.4 Acceptance (BDD).**

```
Feature: A missing capability suspends the session cleanly
  Scenario: request_capability for an unheld tool raises a gap and suspends
    Given a one-agent framework whose agent lacks tool "deploy"
     When the agent calls request_capability(tool, "deploy", "<why>")
     Then a ToolMissing gap event is emitted
      And the session suspends (does not crash, does not silently continue)
      And the suspended state is recoverable
```

"Suspends, recoverable, does not crash/continue" are the grounded
assertions — the observable gap behavior, run.

### Rung 5 — budget

**5.0 Ground at entry (required).** The budget-tracking surface was not
read this session. Per §4 rule 11, before authoring 5.1–5.6 the rung
must read the actual budget path (token accounting, the
`budget.{warn,downshift,hitl,hard_stop}_at_percent` consumers, the
`select_cheaper_model` downshift hook) and state whether budget is
tracked-but-painted or tracked-and-enforced. The detailed spec is
authored against that finding, not assumed.

**5.2 Deliverable (shape).** Token spend drives the framework's budget
thresholds: `warn` → `NotifierDispatched`; `downshift` → the
`select_cheaper_model` hook actually swaps the model; `hard_stop` → the
session stops. Each threshold is an observable behavior, IRL-watched.

**5.4 Acceptance (BDD — shape; finalized after 5.0).**

```
Feature: Budget thresholds change runtime behavior
  Scenario: Hard-stop halts the run
    Given a one-agent framework with a tiny session_usd_cap
     When the run exceeds the cap
     Then the session stops with a budget-stop reason
      And no further provider turns are issued
```

### M08.7a ending

A single agent **executes** built-in tools (rung 1), is **gated** on real
execution (rung 2), **loads skills** that change its behavior (rung 3),
**raises gaps** that suspend cleanly (rung 4), and **respects budget**
(rung 5) — each rung IRL-watched against a minimal framework, each with a
permanent assembled regression test that runs the real path. The five
rung fixtures land in `examples/ladder/` (or `tests/fixtures/ladder/`,
decided at rung 1) as the start of the execution-regression suite.

---

## M08.7b — multi-agent + plan (rungs 6–7)

> **Structured, not detailed — grounded-reason deferral.** Rung 6's spec
> (per-child execution context: reuse `drive_stream` as-is, or a
> re-entrant variant; how a child's result returns to the parent's turn;
> how budget sub-allocates) depends on what M08.7a's executor + feedback
> wiring actually look like. Rung 7 (wire `drive_plan` + build the task
> loop) depends on rung 6's child-execution primitive. Authored after
> M08.7a lands; the construction-reachability wires are traced then,
> against real code, not assumed now.

- **Rung 6 — sequential spawn ⭐.** Build the per-agent execution context
  so `spawn_framework_subagents` (today emit-only, `agent_sdk.rs:467`)
  becomes: root runs → spawns child with narrowed grants → child runs its
  own loop → returns a summary to the parent → parent continues
  (sequential; concurrent is v1.0). The validated subagent contract
  (isolated context, invoked-as-tool, returns a summary to the parent who
  holds memory) is the target shape. **BDD shape:** load an
  orchestrator+1-child framework → run → the child's own work is
  observable in the live graph (events with the child's `agent_id`, not
  just an `AgentSpawned`-then-nothing) → the orchestrator continues after
  the child returns.
- **Rung 7 — plan-driven tasks.** Wire `drive_plan` (`plan_loop.rs:79`,
  currently no caller) into a production session entrypoint and build the
  task-execution loop it currently lacks (`plan_loop.rs:128-129`).
  **BDD shape:** a planner framework → plan created → HITL approve →
  tasks execute (each `TaskStarted`/`TaskCompleted` observable) →
  `PlanComplete`.

**M08.7b ending:** a multi-agent framework runs sequentially and a plan
drives real task execution — both IRL-watched.

## M08.7c — hooks/rails, MCP, ARIA (rungs 8–9, N)

> **Structured, not detailed — grounded-reason deferral.** Rung 8's
> firing engine depends on the hook/rail surfaces (not read this
> session). Rung N depends on the entire ladder.

- **Rung 8 — hooks + rails.** Build the firing engine for
  `hooks.{post_file_edit,pre_commit,session_end}` + `rails.{hard,soft}`
  (defined in framework JSON; no engine today). Ground the hook/rail
  consumer surfaces at entry. **BDD shape:** a framework with a
  `post_file_edit` hook + one hard rail → an edit fires the hook
  (observable) → a rail-violating action is blocked.
- **Rung 9 — MCP.** The one tool path that runs. Fix M08.6 #23 (Windows
  `npx` spawn: npx's `require` resolves into `src-tauri/node_modules` on
  Node 24). **BDD shape:** add an MCP server with an `npx` command on
  Windows → it spawns + initializes (no module-not-found, no infinite
  loop) → its tool dispatches.
- **Rung N — ARIA (the opus).** Import the real `examples/aria/`, **fix
  its shape** against the engine (M08.6 #31: declare the orchestrator's
  grantable union so strict narrowing passes; reconcile the
  built-in-vs-framework tool set, #28), and **run the v0.1-supported
  subset** — single + sequential-multi-agent, built-in + MCP tools,
  skills, gaps, budget, hooks/rails. **Document the v1.0 line**
  explicitly (concurrent orchestration, the grant/use capability split,
  the mode router) so what does *not* run is stated, not implied (§4
  rule 11). **BDD shape:** load ARIA → Run → watch ARIA's agents
  actually execute the subset in the live graph.

**M08.7c ending — the milestone ending:** ARIA runs. The runtime is a
real running agentic runtime for the v0.1 scope, with a permanent
ladder regression suite proving each function executes and a documented
v1.0 line for what doesn't.

---

## Stage V + closeout

- **Stage V** (fresh-context verifier) runs after M08.7c, with a **fifth
  pass added: "assembled-execution"** — V must run the assembled app /
  assembled integration tests and observe each ladder rung *execute*,
  not just confirm the tests exist (the M08.6.V escape — "Sound" without
  running — is exactly what §4 rule 11 now forbids; the verifier prompt
  encodes it).
- **Closeout** — the M08.7 summary + the immutable gap-analysis entry +
  simplify_pass + coverage reconciliation + PR draft. The gap-analysis
  Carry-forward records every M08.6 IRL 🔴 with **observed** disposition
  (run, not inferred).

## Sequencing note

M08.7 is the lead track before M9 (a runtime that runs is the dependency
root). It runs after Phase 0 (cluster-gate codification + DESIGN.md) and
independently of the M08.6.7 Builder track (ladder fixtures are
hand-authored JSON; no Builder dependency). M9 (the mentor) begins only
once ARIA runs.
