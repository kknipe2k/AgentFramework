# M08.7 — Execution Engine — Specification + Stage Prompts

**Date:** 2026-05-30
**Status:** Design approved — implement one stage at a time, in order. Rung 1 (Stage A) is **already built, IRL-observed, and pushed** to `claude/m08.7-execution-engine` (commits `f7f9b7e..f9b02bf`); B–E are the live authoring queue.
**Scope:** M08.7a (rungs 1–5 — the single-agent execution primitives) makes the runtime *execute* what frameworks declare. Rung 1 shipped the in-process built-in tool executor (`Read`/`Write`); B–E discharge capability-on-live-tool, skill load, gap/`request_capability` wiring, and budget enforcement at the run loop. M08.7b/c (rungs 6–N) are structured-not-detailed and authored at their sub-phase entry (grounded-reason deferral, §4 rule 11). The milestone ends at **ARIA runs** the v0.1 subset.

> **Protocol version:** v1.9 schema + the cluster-gate close discipline (`docs/cluster-pattern.md`, codified in Phase 0). Strict `<tdd_discipline strict="true">` two-commit on every code stage; every stage carries `<close_gate>` (the close contract is the assembled run + IRL watch, not tests-green); Stage V runs the 5th `assembled_execution` pass. **Governed by CLAUDE.md §4 rule 11 (grounded-claims).** Every "runs / works / executes" claim in this doc is backed by a code citation observed at authoring time, OR explicitly labeled a **hypothesis to be grounded at the stage's `ground_at_red`** — nothing here asserts behavior from structure.

---

## Background and Design Decision

**Problem.** M08.6 IRL surfaced the load-bearing observation that drove this milestone (`docs/M08.6-irl-findings.md` + `ORCHESTRATOR.md` §9): the runtime *paints* most of what it claims. **Only provider-streaming and MCP-tool dispatch actually execute.** A built-in `ToolUse` for `Read` emits `ToolInvoked` and stops — no file is read, no result feeds back. Sub-agents emit `AgentSpawned` and stop — no child runs. `LoadSkill` has no handler. `request_capability` has a 314-line handler with no production caller. `drive_plan` advances the FSM but its own docstring says "runs no tasks." Hooks/rails are defined in framework JSON with no firing engine. The Builder ships green; ARIA loads and renders; the v0.1 acceptance criteria depend on a runtime that runs, and at M08.6 end-of-milestone it doesn't.

**Solution.** M08.7 builds the execution layer as an **eval-first ladder of clusters** (`docs/cluster-pattern.md` §1) — each rung isolates one execution function against a minimal hand-authored framework so a break is diagnosable, each rung closes only when the assembled thing runs and is IRL-watched, and ARIA is rung N (the integration test, not the unit-of-work). M08.7a (rungs 1–5) ships the single-agent execution primitives — built-in tool execution (Stage A — already built), capability-on-live-tool (Stage B), skill load (Stage C), gap detection (Stage D), and budget enforcement at the run loop (Stage E). M08.7b (rungs 6–7) ships sequential multi-agent + plan-driven tasks; M08.7c (rungs 8–9, N) ships hooks/rails, MCP fix, and ARIA. M08.7b/c are structured here and authored to full X.1–X.6 detail at each sub-phase entry — because rung 6+ specs depend empirically on what M08.7a's executor + feedback wiring actually look like (e.g., whether per-child execution reuses `drive_stream` as-is or needs a re-entrant variant is a rung-6 question, not an authoring-time guess).

**Why one PR for the parent milestone.** Per the M01–M08 precedent: stages A–E commit sequentially on the parent-milestone branch (`claude/m08.7-execution-engine`); Stage V runs in a fresh CLI session; closeout authors the summary + the immutable `docs/gap-analysis.md` entry; the milestone PR drafts at closeout with every stage commit + every retrospective + the gap-analysis entry surfaced together (three-artifact review per CLAUDE.md §20). Rungs accumulate on one branch; no per-rung PR.

**Why six stages (A as-built + B–E + V + closeout) here, not "one stage per rung" or "one mega-stage."** Stage A is rung 1 (the executor + the multi-turn feedback contract every later rung mirrors) and is **already shipped on `claude/m08.7-execution-engine`** — committed `f7f9b7e..f9b02bf`, IRL-observed (real Anthropic model invoked `Read` → executor read `Cargo.toml` → model quoted contents back), mutation gate 0-missed, retro + IRL addendum committed. B–E are sized 2–4 h each — rung 2 is largely verification of an already-tested denial path; rung 3 is the only true greenfield (no `LoadSkill` handler exists); rungs 4 and 5 are wires (handler + budget enforcer both exist as unit-tested primitives — production callers are absent). Splitting per-rung gives each cluster its own §X.5 prompt + its own IRL gate + its own retro + zero-propagation triage in-rung. Stage V runs the protocol v1.9 5th pass (`assembled_execution`) and is the structural guard against another M08.6-class escape.

### What this milestone produces

1. **Built-in tool executor** (Stage A — shipped) — `crates/runtime-main/src/sdk/builtin_tools.rs` (195 lines, new); the `drive_stream` built-in branch routing a `Read`/`Write` `ToolUse` through `CapabilityEnforcer::check` against `file_access`, executing in-process, emitting `ToolInvoked`/`ToolResult`, and returning a result that joins `feedback.dispatched` exactly as the MCP path's `DispatchedTool` does. **The execution + multi-turn-feedback archetype every later rung mirrors.**
2. **Capability enforcement gates a live tool** (Stage B — rung 2) — a Promoted-tier `Write` outside `file_access.write` scope emits `CapabilityViolation(Write)` (the scope-gate denial, not the L4-tier denial that rung 1 already covers) and the target file does NOT exist on disk; an in-scope Write creates the file with its content, through the assembled loop. Closes the BDD `Scenario: A write outside file_access scope does not run` + `Scenario: A write inside scope does run`.
3. **Skill load** (Stage C — rung 3) — a `LoadSkill` handler that reads the resolved skill markdown (the M08.6 loader already resolves skill companions to bodies) and injects it into the agent's context for subsequent turns, emitting `SkillLoaded` (the variant exists at `crates/runtime-core/src/event.rs:279` — schema-defined, never emitted; `replay.rs:146` knows how to project it). The grounded acceptance is that **skill content changes observable agent behavior** (the BDD: a skill saying "always reply in ALL CAPS" makes the agent reply in all caps), not the `SkillLoaded` event alone.
4. **Gap detection wired into the run loop** (Stage D — rung 4) — a model `request_capability` `ToolUse` routes from `drive_stream` into `handle_request_capability` (`crates/runtime-main/src/sdk/request_capability.rs:88` — exists with full unit tests, no production caller), emits the appropriate `*Missing` gap event (`event.rs:479-527`), and suspends the session cleanly per the gap flow (spec §4 — the project's signature behavior). Grounded acceptance: the session **suspends, recoverable, does not crash/continue** — observed, not inferred.
5. **Budget enforcement at the run loop** (Stage E — rung 5) — the budget primitive is **further along than the original M08.7 doc claimed**: `crates/runtime-main/src/budget/` has `cost.rs` + `enforcer.rs` + `hook.rs` + `mod.rs`; `warn_at_percent` is wired in `enforcer.rs:73,116`; the opus→sonnet→haiku ladder is real (`hook.rs:6-11`); every provider call already produces a `CostBreakdown` (`providers/anthropic.rs:147,292`). The gap is the **run-loop wire**: spend produced per turn does not drive `warn` / `downshift` / `hard_stop` on the live session. Stage E wires the existing enforcer into `drive_stream`'s per-turn loop. Grounded acceptance: a session with a tiny `session_usd_cap` exceeds the cap → the session stops with a budget-stop reason → no further provider turns are issued, observed.
6. **Two ADRs.** ADR-0026 — **The built-in tool execution contract** (Stage A's archetype frozen as the multi-turn feedback contract every rung mirrors; the file_access-scope-is-the-boundary decision; the in-process-vs-sandbox split with `Bash` deferred to a separate ADR-class rung). ADR-0027 — **Skill-into-context injection model** (Stage C; what *section* of the assembled prompt the loaded skill body lands in; whether it persists across turns; how a second `LoadSkill` composes — these touch the §0b system-prompt-assembly job that proposals `0001-interaction-layer-roadmap.md` + `harness-review-takeaways.md` Item 2 flagged as v0.1-load-bearing).
7. **Stage V** five-pass verifier (Inventory + Wire + Behavior + Multi-call invariants + the v1.9 **`assembled_execution`** 5th pass — V runs the assembled app / integration tests and OBSERVES each rung execute, not just confirms tests exist).
8. **Closeout** — M08.7a summary + immutable `docs/gap-analysis.md` entry + `<simplify_pass>` + `<coverage_policy_reconciliation>` + the M08.7b/c sub-phase entry handoff.

### What's not in scope

- **`Bash` + any OS-spawning or out-of-scope op** — separate ADR-class rung (the sandbox has no `RunCommand` variant — `crates/runtime-sandbox/src/protocol.rs:29` is `ValidateArtifact | Shutdown` only; adding one is an IPC-protocol change → §11 ADR + CODEOWNERS-flagged). M08.7a is in-process file tools only. If a stage finds it cannot proceed without the sandbox exec path, it STOPS and surfaces — that's the trigger to split, not to widen.
- **Concurrent multi-agent orchestration** — v1.0 (sequential is the v0.1 floor that proves the engine; parallel is the v1.0 follow-on per the multi-agent research). M08.7b rung 6 ships *sequential* spawn — child runs, returns a summary, parent continues.
- **Mode router** — STANDARD stays hardcoded (§0d; gotcha #3). The mode router is v1.0.
- **New provider** — Anthropic-only per §0d. The capability **grant-right vs use-right split** (AWS PassRole / macaroon pattern — let a coordinator be the source of a delegated capability while attenuating its own use) is the correct v1.0 ADR; it still requires the orchestrator to hold a grant-right and is NOT a v0.1 capability-model change. Strict child ⊆ parent narrowing stays (validated against object-capability theory + AI-agent delegation best practice — you may only delegate authority you hold; attenuation only narrows).
- **The Builder authoring surface** — that's M08.6.7's track, not this one. M08.7 fixtures are hand-authored JSON loaded from disk, so M08.7 does not depend on Builder correctness. M08.6.7 absorbs the 32 M08.6 IRL findings as clusters after M08.7c closes (sequencing: M08.7 → Claude Design pass → M08.6.7 → M09 mentor).
- **No new third-party deps** without `cargo deny`. No telemetry. No `unsafe` outside `runtime-sandbox`. No schema change unless a rung proves one is needed (then: new `*.v2.json` + ADR).

### Why the ladder framing is preserved on top of M08-style staging

The M08.7 framing introduced in PR #109 (commit `f15f916`) was right and stays: each rung is a cluster; rungs accumulate on one branch; M08.7a/b/c are sub-phase boundaries with clear endings. The change in this rewrite is to apply the **M08 phase-doc shape** (per-stage X.1–X.6, single Document Structure table, single Implementation Workflow, single Pre-existing legacy file inventory, Summary Table + Verification Checklist at the end) so the build CLI consumes each stage's prompt from a doc that matches every prior milestone's authoring pattern — same parser, same retro template, same closeout shape. Rungs = stages; the ladder is the milestone's deliverable map; the stage shape is the build-CLI contract.

### Carry-forward absorbed into M08.7

The post-M08.6 findings split by zero-propagation (cluster-pattern.md §2 / ORCHESTRATOR.md §9): **execution gaps → M08.7**; **Builder 🔴/🟡 → M08.6.7**; **ARIA narrowing strict-fix → rung N**. The M08.7 carry-forwards (every item below is an in-rung deliverable, not a "carry forward to M09"):

| Carry-forward | Source | Discharged in |
|---|---|---|
| **Built-in tools painted, not executed** (the M08.6-IRL central finding) | `docs/M08.6-irl-findings.md` (the "Painted or checked, NOT executed" section) | **A** (rung 1 — shipped) + **B** (gates a real execution, not a paint) |
| **`LoadSkill` no handler** | `agent-runtime-spec.md` §0b + grep (`SkillLoaded` event exists; no emitter) | **C** (rung 3) |
| **`request_capability` no production caller** | `crates/runtime-main/src/sdk/request_capability.rs:88` (handler exists; only test callers) | **D** (rung 4) |
| **Budget tracked + enforced at unit level, unwired into the run loop** | `crates/runtime-main/src/budget/` + `providers/anthropic.rs:147,292` | **E** (rung 5) |
| **M08.V 🟡 #1 (`TestOutcome.vdr` structurally dead)** + **M08.V 🟡 #2 (`plan_loop` no production caller)** | M08 gap-analysis Carry-forward (immutable; routing recorded) | **B** (vdr is the violation surface rung 2 verifies) + **M08.7b rung 7** (plan_loop) |
| **TD-033** — executor gates `file_access`, not execution-time tool-auth | M08.7.A retro Decisions; filed as v1.0 hardening | NOT this milestone — explicit v1.0 scope-out; rung 2 must NOT re-open it |
| **TD-034** — no agent output is visible in the running app (IRL observability) | M08.7.A retro IRL prep | Mitigated at A via `RUST_LOG=debug`; full fix routes to **M08.7b** (live-graph execution view) |
| **TD-035** — relative file paths resolve against app CWD ≠ repo root | M08.7.A IRL observation (real Anthropic) | Forward-looking — no rung-1 defect; **any later rung that accepts user/model relative paths** defines what they resolve against (likely a workspace-root convention the Tauri shell resolves per CLAUDE.md §9 archetype) |

The M08.6.7 findings (#12 clear-canvas, #17 Palette template, #19 tier UI/backend desync, #22 budget-cap persistence, #23 MCP npx Windows spawn, #32 `save_framework` companion drop, validation/UX cluster) are **not absorbed here** — they're the M08.6.7 milestone's clusters (authored after M08.7c closes). ARIA's narrowing fix (M08.6-IRL #31) is **rung N's** strict-narrowing-fix-ARIA work (declare the orchestrator's grantable union; the grant/use split is the v1.0 ADR).

### Key constraints (all stages)

- **Cluster-gate close discipline** (`docs/cluster-pattern.md` §1 step 4). Every stage's BDD acceptance + assembled test is authored before the build (red), runs the *real* path, and the stage does not close until the maintainer IRL-watches it run. The §X.5 prompt's `<close_gate>` carries the assembled-run output + IRL-required + mutation-gate + triage children; the `<approval_surface>` carries the **closure proof** (assembled-run observed + IRL result — NOT "tests green").
- **Grounded claims** (§4 rule 11). Every rung surface states what was run and what was NOT. "Tool executes" requires the assembled app (or assembled integration test) to have run the tool and observed the result — not a unit test that injects a `ProviderEvent::ToolUse` and asserts the emitted event (the trap that hid the M08.6.7 #32 + the built-in-tools-don't-execute gap).
- **Strict v1.8 two-commit TDD on every code stage.** Red commit standalone (`test(M08.7.X): ...`) → surface for red approval → impl commit (`feat(runtime): M08.7.X — ...`) whose body states `git diff <red>..<impl>` over the test paths is EMPTY. No amend dances; no Co-Authored-By.
- **Acceptance-assertion rule** (cluster-pattern.md §4). Assertions on user-observable behavior / observable side effects, never on internal events alone. "A `ToolInvoked` event is emitted" licenses "the event is emitted" — NOT "the tool ran." "The file exists on disk with the written content" / "the agent's reply is in all caps" / "no further provider turns are issued after the cap" are the grounded assertions.
- **Mutation gate** (cluster-pattern.md §5). `cargo-mutants` on every Rust cluster; a surviving viable mutant on the cluster's logic blocks close or is justified inline. **Windows-local mitigation:** `cargo mutants --in-place` bypasses the os-1314 symlink-privilege block (the rung-1 finding; the §5 ledger captures it as a Phase-0a addendum the M08.7.B opening docs commit applies). Stryker on TS is build-side-pending — not needed for M08.7a (Rust-only).
- **CI-parity is a hard rule** (CLAUDE.md §6). Local gate `✓` requires the exact CI command. Gotcha #56 Windows `--test-threads=1` documented per CLAUDE.md §6; any other divergence is cited inline + filed as a CI-fix follow-up.
- **Sandbox boundary** (Hard Rule 8 + §10). Built-in tools that touch the OS (`Bash`, and `Write`/`Read` *outside the workspace* — though the workspace scope is the agent's `file_access`, not the project dir) execute through `crates/runtime-sandbox/`, never directly in main. The sandbox has no exec path today; rung 1's scope lock holds for B–E. If a stage cannot satisfy its rung without the sandbox, STOP and surface — that's the signal to split a separate ADR-class rung, not to widen the current one.
- **No schema change** unless a rung proves one is needed. ADR-0026 (Stage A's archetype) and ADR-0027 (Stage C — skill injection) are the two ADRs already in scope; additional ADRs per CLAUDE.md §11 if a stage surfaces a trigger. ADRs file `Proposed` at their stage, flip `Accepted` in the M08.7 PR before merge.
- **The cluster owns its findings — zero-propagation** (cluster-pattern.md §2). Every finding from the in-cluster IRL has three legal dispositions: **fixed in this cluster** (observed resolved in the running system), **a new cluster** (opened now, scheduled in this phase or M08.6.7), or **explicit ADR scope-out** (a one-line waiver-ADR stating it is v1.0+ and *why*, with the observable v0.1 line documented). "Route to next milestone Stage A" is **banned**.

---

## Document Structure

| Stage | Deliverable | Strict TDD | Estimate | Coverage gate |
|---|---|---|---|---|
| **A** | Rung 1 — built-in tool executor (`Read`/`Write` execute through `CapabilityEnforcer::check`; result joins `feedback.dispatched`; multi-turn loop re-streams to model). **SHIPPED** on `claude/m08.7-execution-engine`; this stage section is the as-built reference + the archetype B–E mirror. | yes (held) | n/a (closed) | runtime-main ≥95 (held) |
| **B** | Rung 2 — capability enforcement on a live tool. The Promoted-tier scope-gate denial on Write (the gap rung 1 left — rung 1's blocked-Write test denies via Novice L4, *before* scope is consulted); the in-scope happy path through the assembled loop; the HITL surfacing per the ground-at-red triage. | yes (v1.8) | 2–4 h | runtime-main ≥95 |
| **C** | Rung 3 — skill load. The `LoadSkill` handler that injects skill body into the agent's context; the assembled BDD that a skill changes observable agent behavior; ADR-0027 (skill injection model). | yes (v1.8) | 4–6 h | runtime-main ≥95 |
| **D** | Rung 4 — gap detection. The `drive_stream` wire that routes a `request_capability` `ToolUse` into the existing `handle_request_capability`; the suspend-cleanly invariant; the recoverable-state invariant. | yes (v1.8) | 3–5 h | runtime-main ≥95 |
| **E** | Rung 5 — budget at the run loop. The per-turn spend → `warn` / `downshift` / `hard_stop` wire; the assembled "tiny session_usd_cap stops the run" invariant; the no-further-provider-turns invariant. | yes (v1.8) | 3–5 h | runtime-main ≥95 |
| **V** | Five-pass verifier (Inventory + Wire + Behavior + Multi-call invariants + v1.9 **`assembled_execution`** — V runs the assembled app / integration tests + observes each rung execute) | n/a (verifier) | 3–4 h | n/a |
| **Closeout** | M08.7a summary + immutable `docs/gap-analysis.md` entry + `<simplify_pass>` + `<coverage_policy_reconciliation>` + the M08.7b/c sub-phase entry handoff | n/a | 4–6 h | n/a |

Total B–E + V + closeout: ~19–30 h estimated; at the M08.6 ~0.24–0.34× calibration (the M08.5/M08.6 strict-TDD pattern) the M08.7a finish-out is ~5–10 h actual. Rungs 6–N (M08.7b/c) are estimated at their sub-phase entry.

> **Stage A is not on the queue** — it is closed and pushed. Its X.1–X.6 below documents the shipped archetype so Stage B's author has the reference frozen. Live authoring opens at Stage B.

---

## Implementation Workflow

1. **Read first** — each stage's `<read_first>` block; Stage B+ reads the prior stage's retrospective `[END] Decisions` and applies them (CLAUDE.md §19 rule 1). The M08.7.A retrospective + IRL addendum are mandatory reading for Stage B (`docs/build-prompts/retrospectives/M08.7.A-retrospective.md`).
2. **Branch.** Develop on `claude/m08.7-execution-engine` — the branch already carries rung 1's 8 commits. Stage B opens with the carry-forward docs commit (the M08.7.A retro's 4 Decisions: cargo-mutants `--in-place` gotcha + cargo-test `--no-fail-fast` gotcha + the rungs-3–9 ground-at-red phase-doc edit already applied here + the `.gitignore` scratch-artifacts entries). The docs commit lands FIRST as a standalone `docs(M08.7): apply M08.7.A retro carry-forwards` commit, then the strict-TDD red+impl sequence for the stage's deliverable.
3. **Per-stage flow** (the cluster-gate cycle):
   - `ground_at_red` (resolve grounding sub-steps before writing tests; surface findings)
   - `write_failing_tests` → `red_phase_clippy` → `red_phase_commit` → **`surface_for_red_approval`** (HITL: maintainer relays orchestration adjudication; nothing proceeds without approval)
   - `implement` → `verify_gates` → `mutation_gate` (cargo-mutants `--in-place` on touched files) → `green_phase_commit`
   - **`assembled_run_irl`** (the close gate — assembled test runs + maintainer IRL-watches; the rung does not close on tests-green)
   - `fill_retrospective` → **`surface_for_final_approval`**
4. **Adjudicate the surface.** Each surface carries the cross-machine state (`git log --oneline main..HEAD` + retro listing) + the strict-TDD invariant proof (`git diff <red>..<impl>` over test paths = empty) + the closure proof (assembled-run output observed + IRL result) + gate results + the retro's three-axis table + an explicit "what was NOT exercised" statement (§4 rule 11). The orchestration session (me) adjudicates against the §X.5 prompt + this phase doc + the cluster-gate contract; you (the user, HITL) approve or request changes.
5. **Build CLI is cleared between stages.** Each new stage is a fresh session reading the §X.5 prompt + the per-stage `<read_first>` files. State lives in artifacts (this phase doc · the retros · the gap-analysis · git) — not session memory.
6. **v1.6 canonical gate ordering** (CLAUDE.md §6): `cargo fmt --all` → `cargo clippy --fix --allow-dirty -p <crate>` → `cargo clippy --workspace --all-targets -- -D warnings` → test/doc/audit/deny → `cargo llvm-cov clean --workspace` (gotcha #81) → the llvm-cov gates → frontend gates (n/a for M08.7a — Rust-only). CI-parity is a hard rule; cite any divergence inline with a gotcha reference.
7. **Stage V in a fresh CLI session** (the bias guard). V's `<read_first>` deliberately omits every `*-retrospective.md` / `M08.7-summary.md` / prior-gap-analysis but DOES consume this phase doc's `<construction_reachability_check>` blocks + the v1.9 5th `assembled_execution` pass (V runs the assembled rung — not just reads its tests). The verifier's 🔴 findings → scoped D.fix (max 2 iter) per ADR-0008; 🟡 → in-cluster fix-now / new cluster / explicit scope-out per cluster-pattern.md §2; 🟢 → `docs/tech-debt.md`.
8. **Closeout** runs `<simplify_pass>` (M08.7.A..HEAD diff) + the v1.8 `<coverage_policy_reconciliation>` (sync `docs/coverage-policy.md` §B/§C + CLAUDE.md §5/§6 + `codecov.yml` for any gate change). The M08.7 PR drafts at closeout. ADR-0026 + ADR-0027 flip `Proposed → Accepted` in the closeout commit, before push.
9. **ADRs.** Two expected (CLAUDE.md §11) — ADR-0026 (Stage A's archetype frozen) and ADR-0027 (Stage C skill injection). Additional ADRs per §11 if a stage surfaces a trigger.

---

## Pre-existing legacy file inventory

Grep-verified at authoring time against `origin/claude/m08.7-execution-engine` at `f9b02bf` (the post-M08.7.A close). Files Stage B+ CONSUMES or REFERENCES; shape claims are factual as of this snapshot.

| File | Shape (as-built post-M08.7.A) | Stage(s) that consume |
|---|---|---|
| `crates/runtime-main/src/sdk/agent_sdk.rs` | The run loop. `run_agent` (`:256`) prelude once then per-turn `provider.stream → drive_stream`, terminating when a turn dispatches no tool (`:261`) or hits `MAX_AGENT_TURNS = 16`. `drive_stream` (`:412`) carries the built-in branch (M08.7.A) BETWEEN the MCP branch (`try_mcp_dispatch :576`) and `pipeline.next_event` — a built-in `Read`/`Write` `ToolUse` is routed through the executor and joins `feedback.dispatched`; not-a-built-in falls through. `TurnFeedback` / `DispatchedTool` shapes at `:80-97`. The `with_enforcement` wrapper (`:391`) and `await_capability_violation_hitl` (`:539`) are the existing capability-violation routing. `emit` / `persist_signal` / `log_event_debug` (`:681`, `:717`) are the event-emission + debug-log unblock from rung 1. | **B** (the capability-violation routing; reads `with_enforcement`); **C** (the run loop is where a `LoadSkill` `ToolUse` routes — analogous branch site to the rung-1 built-in branch); **D** (the wire site for `request_capability` routing); **E** (the per-turn boundary where spend lands) |
| `crates/runtime-main/src/sdk/builtin_tools.rs` | **NEW from M08.7.A** (195 lines). The executor module. `execute_builtin(name, input, agent, enforcer, …) → Result<ToolResultContent::Text, BuiltinExecError>`. Builds a `CapabilityDeclaration` for the op via `read_decl`/`write_decl`, runs through `CapabilityEnforcer::check`, executes `std::fs::read_to_string` / `write` against the agent's `file_access` scope. `is_builtin_tool("Read"|"Write") → true`. `builtin_tool_defs(allowed_tools) → Vec<ToolDef>` (advertisement-gated by `allowed_tools`). | **B** (the executor IS the live tool rung 2 verifies); **C** (the path-agnostic `&Path`-parameterised + capability-checked archetype rung 3's `LoadSkill` handler mirrors) |
| `crates/runtime-main/src/sdk/event_pipeline.rs` | The capability-enforcement gate. `next_event(ToolUse)` (`:102`) emits `ToolInvoked` on `check Ok`, or `CapabilityViolation` + omits `ToolInvoked` on a blocked check. The loop routes violations through `await_capability_violation_hitl` (`agent_sdk.rs:539`). Today's gate is correct; rungs 1+2 prove it gates *real* execution (not just paint). | **B** (the gate's denial path is what rung 2's assembled tests verify against a real Write) |
| `crates/runtime-main/src/sdk/request_capability.rs` | **314 lines.** The Layer-2 gap handler. `handle_request_capability(invocation) → AgentEvent::{ToolMissing,SkillMissing,McpMissing,ResourceMissing,SpawnMissing}` — fully implemented with unit tests at `:201`, `:216`, `:239`, `:291`. **No production caller** (grep confirms — only its own test fns). A model `request_capability` `ToolUse` today falls through `drive_stream` to `pipeline.next_event` and is treated as an ordinary tool (emits `ToolInvoked` for an unknown name and stops). | **D** (the wire that adds the production caller in `drive_stream`) |
| `crates/runtime-main/src/sdk/replay.rs` | Event replay. `:146` constructs `AgentEvent::SkillLoaded { … }` — the runtime can already *project* a SkillLoaded event from persisted signals; **no code emits it.** Rung 3's emitter is the first producer. | **C** (the projection path V's wire pass grep-confirms is reached when rung 3 emits SkillLoaded) |
| `crates/runtime-core/src/event.rs` | `AgentEvent::SkillLoaded` (`:279`); the gap variants `ToolMissing` / `SkillMissing` / `McpMissing` / `ResourceMissing` / `SpawnMissing` (`:479-527`). All schema-defined; some emitted by `framework_loader/walker.rs` (Layer 1 — load-time gaps); the `request_capability` (Layer 2) emission is wired by rung 4. | **C** (`SkillLoaded` emitter); **D** (the Layer-2 gap emission via `handle_request_capability`) |
| `crates/runtime-main/src/capability/enforcer.rs` | L1 + L4 capability checks. `CapabilityEnforcer::check(agent, &decl)` (`:215`); `DenyReason::NoDeclarations` (`:237`); tier-based L4 (`set_tier` + the Novice-blocks-Write rule rung 1 surfaced). | **B** (the scope-gate denial path is what rung 2 verifies against a Promoted-tier Write to an out-of-scope path) |
| `crates/runtime-main/src/budget/{cost,enforcer,hook,mod}.rs` | **The budget primitive is built, not painted** (the grounded correction to the original M08.7 doc's "ground at entry" framing). `enforcer.rs` has `warn_at_percent` at `:73,116` + the 4-threshold model (`warn / downshift / hitl / hard_stop` — `lib.rs:21-22`). `hook.rs` ships the hardcoded `opus→sonnet→haiku` ladder (`:6-11`); frameworks can override via `framework.budget.downshift_hook`. `cost.rs` carries spend tracking. The gap is **purely the run-loop wire** — per-turn `CostBreakdown` (every `providers/anthropic.rs:147` already produces one) doesn't drive the enforcer on the live session. | **E** (the per-turn wire from `CostBreakdown` → `budget::enforcer` in `drive_stream`'s per-turn boundary; the assembled "tiny cap stops the run" invariant) |
| `crates/runtime-main/src/providers/{mod,anthropic}.rs` | `LLMProvider` trait. `AgentConfig.tools: Vec<ToolDef>` (`:288` — the built-in advertisement target M08.7.A populates via `builtin_tool_defs`). `ToolResultContent::Text` (`:267`). `CostBreakdown` (`:321`, fields populated by `anthropic.rs:147`). `estimate_cost` (`:434`, `anthropic.rs:292`). | **C** (skill body lands somewhere in the `AgentConfig` the next turn sends — the §0b system-prompt-assembly job — ADR-0027 names where); **E** (the `CostBreakdown` the per-turn wire consumes) |
| `crates/runtime-main/src/builder/tester.rs` | The assembled run path. `run_test_session_with(fw, task, db_path, provider, drone, hitl_seam, session_id)` builds the enforcer, wires grants via `grant_framework_capabilities`, runs the loop, returns a `TestOutcome` with `trace: Vec<AgentEvent>` + `passed: bool`. **The `hitl_seam` arg is `None` on the rung-1 tests** — the ground_at_red triage for rung 2 surfaces whether HITL can be injected here. `test_agent_config` (`:332`) is where `config.tools` is built; M08.7.A confirmed advertising via `builtin_tool_defs(allowed_tools)`. | **B** (the assembled-test harness rung 2 extends); **C/D/E** (the same harness; each adds scenarios) |
| `crates/runtime-main/src/framework_loader/{mod,walker,capability_map}.rs` | The Layer-1 loader. `walker.rs:67,74` emits `ToolMissing`/`SkillMissing` at load time. `mod.rs:7,46` documents the two-layer model: Layer 1 (loader) + Layer 2 (`request_capability` at runtime) both produce gap events. `capability_map::inline_agents` filters loaded agents and feeds the capability summary (M08.6's `Object → Agent(_)` resolution is upstream of this). The M08.6 loader resolves skill companions to bodies — Stage C's handler reads from the resolved skill body, no re-resolution. | **C** (the resolved skill body is the input the handler reads; no Layer-1 schema change); **D** (the Layer-1/Layer-2 shared `requested_via` convention rung 4 mirrors) |
| `crates/runtime-main/src/plan/{plan_loop,state_machine,mod}.rs` | The plan FSM. `state_machine.rs` ships `PlanStateMachine` / `TaskStateMachine` (the M04 safety primitive at ≥95%). `plan_loop.rs:79` has `drive_plan` advancing the FSM — its own docstring says "runs no tasks" (`:7-9`, `:128-129`). **No production caller** (M08.V 🟡 #2). | NOT M08.7a — **M08.7b rung 7** wires this. |
| `crates/runtime-main/tests/builtin_tool_execution.rs` | **701 lines (NEW from M08.7.A).** The assembled-regression archetype. Header doc-comment is the cluster-gate contract statement (lines 4-17). `ScriptedToolStub` (`:159`) — the LLMProvider stub (the ONLY stub; everything else is real — the executor, the enforcer with `grant_framework_capabilities`, the filesystem read, the multi-turn feedback). Tests drive `run_test_session_with` against a `tempfile` workspace. Assertion archetype: the agent's NEXT turn receives file contents as `tool_result` AND the final text quotes `ladder-rung-1`. Already covers (partially) the rung-2 denial paths: `read_outside_file_access_scope_is_denied_and_not_executed` (`:305`) + `write_under_novice_tier_is_blocked_and_creates_no_file` (`:352`). | **B** (extends this file with the Promoted-scope-denial-on-Write tests rung 1 left); **C/D/E** (same harness pattern — new test files per rung following this archetype) |
| `crates/runtime-sandbox/src/protocol.rs` | `SandboxRequest = ValidateArtifact | Shutdown` (`:29`). **No exec variant.** Confirms the sandbox has no command-execution path; rung 1's scope lock holds for B–E. | **All stages** (the scope-lock confirmation; if a stage cannot proceed without sandbox exec, STOP and surface — separate ADR-class rung) |
| `docs/cluster-pattern.md` | The close-gate discipline every stage runs under. §1 five-step cycle; §2 zero-propagation rule; §4 acceptance-assertion rule; §5 mutation gate (incl. Stryker spec for v1.0+ TS work + the cargo-mutants `--in-place` Windows mitigation Stage B's opening docs commit appends). | **All stages** + **V** (the 5th `assembled_execution` pass is V running the rung's assembled path) |
| `docs/build-prompts/retrospectives/M08.7.A-retrospective.md` | The shipped rung-1 retro + IRL addendum (2026-05-29). 254 lines. Records the assembled-test-drives-the-real-loop pattern, the §1.3-A/B ground-at-red findings (`config.tools` populated via `builtin_tool_defs`; enforcer grants loaded via `grant_framework_capabilities`), the cargo-mutants `--in-place` Windows finding, the IRL-observed close (real Anthropic model quoted Cargo.toml), the Decisions for the next stage (4 items the docs commit applies). | **B** (mandatory `<read_first>`; the Decisions become the opening docs commit) |
| `docs/tech-debt.md` | TD-033 (defense-in-depth — executor gates file_access scope, not execution-time tool-auth — v1.0 hardening; **rung 2 must NOT re-open**). TD-034 (no agent output visible in running app — mitigated by `RUST_LOG=debug`; full fix → M08.7b live-graph execution view). TD-035 (relative paths resolve against app CWD ≠ repo root — forward-looking; any later rung that accepts relative paths defines what they resolve against). | **B/C/D/E** (the scope-out ledger — read; do not re-open) |
| `examples/aria/` + `examples/ralph/` | The reference frameworks (loaded + rendered + saved at M08.6). NOT consumed by M08.7a (fixtures are hand-authored minimal frameworks per rung — keeps a break diagnosable). | **M08.7c rung N** (the integration test — ARIA runs the v0.1 subset) |
| `docs/MVP-v0.1.md` §M8 + `agent-runtime-spec.md` §0b / §2 / §4 (gaps) / §6 (Framework JSON Loader) / §8.security (L1/L2a/L4) + §13 (privacy — no telemetry) | The spec sections M08.7a implements against. | All stages (V later checks against them) |
| `docs/coverage-policy.md` (+ CLAUDE.md §5/§6, `codecov.yml`) | The four-mirror coverage source-of-truth. M08.7.A added no new exclusion (the executor is path-agnostic + `tempfile`-tested + ≥95 PASSING in package gate). | **All stages** (if a stage surfaces an OS-call holdout, the v1.8 four-mirror sync applies + the closeout `<coverage_policy_reconciliation>` verifies it — M08.7a is designed to avoid one) + **closeout** (`<coverage_policy_reconciliation>`) |

---

## Stage A — Built-in tool executor (rung 1) — **SHIPPED reference**

> **Status:** Closed. Branch: `claude/m08.7-execution-engine`. Commits `f7f9b7e..f9b02bf` (8 commits: red + impl + 2 follow-up tests + retro + debug-log + 2 TDs + IRL addendum). IRL-observed close 2026-05-29 — real Anthropic model invoked `Read` → executor read `Cargo.toml` off disk under `file_access` scope → model quoted contents back. Mutation gate `cargo mutants --in-place`: 8 mutants, 1 caught, 7 unviable, **0 missed**. This stage section documents the as-built archetype B–E mirror; live authoring opens at Stage B.

### A.1 Problem Statement

A built-in `ProviderEvent::ToolUse` reaches `drive_stream` (`agent_sdk.rs:412`), is offered to `try_mcp_dispatch` which returns `Ok(None)` (not an MCP tool, `agent_sdk.rs:607`), and falls through to `pipeline.next_event` which emits `ToolInvoked` and stops. **Nothing executes the tool; nothing feeds a `tool_result` back; the multi-turn loop sees empty `feedback.dispatched` and breaks** (`agent_sdk.rs:261`). The agent cannot do work with a built-in tool — the M08.6-IRL central finding.

### A.2 Files to Change (as-built)

| File | Change |
|---|---|
| `crates/runtime-main/src/sdk/builtin_tools.rs` | **NEW** (195 lines). The executor + `is_builtin_tool` + `builtin_tool_defs` + `execute_builtin` + `BuiltinExecError`. |
| `crates/runtime-main/src/sdk/agent_sdk.rs` | +208 lines (the `drive_stream` built-in branch + the `log_event_debug` IRL observability unblock + the multi-turn feedback re-stream path). |
| `crates/runtime-main/src/sdk/mod.rs` | +6 lines (export the `builtin_tools` module). |
| `crates/runtime-main/src/builder/tester.rs` | +28 lines (`test_agent_config` advertises built-in `ToolDef`s via `builtin_tool_defs(allowed_tools)` — the §1.3-A closure). |
| `crates/runtime-main/src/framework_loader/{mod,capability_map}.rs` | +3 / +20 lines (the §1.3-B enforcer-grant-loading closure: `grant_framework_capabilities` is the new pub fn that loads an agent's `file_access` into the enforcer). |
| `crates/runtime-main/tests/builtin_tool_execution.rs` | **NEW** (701 lines). The assembled regression + unit contract — drives the REAL `run_test_session_with → run_agent` loop against a `tempfile` workspace fixture; ONLY the provider is stubbed. |
| `crates/runtime-main/tests/mcp_dispatch_runloop.rs` | +12 lines (migration — adjusted to coexist with the built-in branch). |
| `crates/runtime-main/tests/sdk_capability_integration.rs` | +35 lines (migration — denied-path collisions resolved). |
| `docs/build-prompts/retrospectives/M08.7.A-retrospective.md` | **NEW** (254 lines). |
| `docs/tech-debt.md` | +68 lines (TD-033 + TD-034 + TD-035). |

Diffstat verified against `origin/main...origin/claude/m08.7-execution-engine`: **11 files, +1504/−26**.

### A.3 Detailed Changes (as-built archetype)

The shipped surfaces — every later rung mirrors at least one:

- **The executor module** (`builtin_tools.rs`). `&Path`-parameterised per the path-agnostic archetype (CLAUDE.md §9). `execute_builtin(name, input, agent, enforcer, …)` builds a `CapabilityDeclaration` for the op (Read = `Path`-scoped against `file_access.read`; Write = `Path`-scoped against `file_access.write`), runs through `CapabilityEnforcer::check`, executes `std::fs::read_to_string` / `write` in-process, returns `ToolResultContent::Text`. `tempfile`-testable. `is_builtin_tool("Read"|"Write") → true`; `is_builtin_tool("Bash") → false` (scope-lock — Bash is out of M08.7a). `builtin_tool_defs(allowed_tools) → Vec<ToolDef>` advertises only the names in the agent's `allowed_tools` (the file-access check is the operative authorization boundary; TD-033 captures the defense-in-depth gap for v1.0+).
- **The `drive_stream` built-in branch** (`agent_sdk.rs:412`). Inserted BETWEEN `try_mcp_dispatch` (the MCP branch) and `pipeline.next_event` (emit-only). A `ToolUse` naming an in-process built-in routes to `execute_builtin`; its result joins `feedback.dispatched` (the `TurnFeedback` / `DispatchedTool` shapes, `:80-97`) — the **same** feedback contract MCP uses, so the multi-turn loop (`:256-294`) re-streams to the model on the next turn. Not-a-built-in falls through to `pipeline.next_event` unchanged. One construction-reachability wire — `inputs_reachable="true"` at impl.
- **`test_agent_config` populates `config.tools`** (`tester.rs:332`). Pre-M08.7.A this was empty for built-ins; the §1.3-A ground-step found it; impl wired `config.tools.extend(builtin_tool_defs(&agent.allowed_tools))` so the model receives the built-in `ToolDef`s and emits the `ToolUse`. **Archetype for rung 3** — a `LoadSkill` `ToolDef` is advertised the same way if `LoadSkill` is in `allowed_tools`.
- **Enforcer grants loaded from `file_access`** (`framework_loader/mod.rs` + `capability_map.rs`). Pre-M08.7.A, `run_test_session_with` built `CapabilityEnforcer::new()` and the fresh enforcer denied with `NoDeclarations` for any tool. The §1.3-B ground-step found it; impl added `grant_framework_capabilities(&mut enforcer, &fw)` — the production grant pipeline that loads each agent's `file_access` (and the other capability blocks) into the enforcer. **Archetype for rung 2** — the same grant pipeline carries Write scopes; rung 2 verifies the Promoted-tier Write-out-of-scope denial through this real grant path.
- **`log_event_debug`** (`agent_sdk.rs::emit`). The IRL observability unblock (TD-034 minimal mitigation). Logs each `AgentEvent`'s salient payload at `debug`; `RUST_LOG=debug` makes a run watchable in the log. Full UI surfacing routes to M08.7b's live-graph execution view.
- **The assembled regression** (`tests/builtin_tool_execution.rs`). 701 lines. Header (lines 4-17) is the cluster-gate contract statement: "The ONLY stub is the provider (no live Anthropic — CLAUDE.md §10); the executor, the capability enforcer (with grants loaded from the framework's `file_access` — the §1.3-B closure), the filesystem read, and the multi-turn feedback are all real." Assertion archetype (rule 11 / gotcha #66): "the load-bearing assertion is NOT 'a `ToolInvoked` event fired' — it is that the agent's NEXT provider turn RECEIVES the file contents as a `tool_result` (observed on the real `AgentConfig` the loop built) AND the final text quotes `ladder-rung-1`." **Archetype every later rung's assembled test mirrors** (rung 2: assert the file does/does-not exist on disk; rung 3: assert the agent's reply is in all caps; rung 4: assert the session is suspended + recoverable; rung 5: assert no further provider turns are issued).

### A.4 Tests (as-shipped)

The 701-line `tests/builtin_tool_execution.rs` ships **15 tests** across assembled + unit:

- Assembled (drive `run_test_session_with`): `read_feeds_file_contents_back_and_agent_quotes_them` (the BDD §1.4 contract); `read_outside_file_access_scope_is_denied_and_not_executed` (the partial rung-2 coverage rung 2 extends); `write_under_novice_tier_is_blocked_and_creates_no_file` (the partial rung-2 coverage rung 2 extends); `read_of_a_missing_file_in_scope_feeds_an_error_result_back` (the missing-file path).
- Unit (executor): `is_builtin_tool_recognizes_read_and_write_only`; `execute_read_inside_scope_returns_file_content`; `execute_read_outside_scope_is_denied_no_matching_grant`; `execute_read_with_no_grants_is_denied_no_declarations`; `execute_write_inside_scope_promoted_writes_file`; `execute_write_under_novice_is_tier_forbidden_and_writes_no_file`; `execute_missing_path_is_an_op_error`; `execute_write_missing_content_is_an_op_error`; `execute_empty_path_is_an_op_error`; `execute_non_builtin_tool_name_is_an_op_error`.
- Advertisement contract: `builtin_tool_defs_advertises_only_builtins`; `builtin_tool_def_shape_is_keyed_per_name_read_vs_write` (added in follow-up commit `95f3052` to kill the surviving `:165 ==/!=` mutant — the rung-1 mutation gate close).
- Grant-loading: `grant_framework_capabilities_loads_file_access_into_enforcer` (proves the §1.3-B closure is live — `file_access` round-trips through the production pipeline into the enforcer).

#### A.4.X Acceptance criteria — **MET, IRL-observed**

The §1.4 BDD: a real Anthropic model invoked `Read` → executor read `Cargo.toml` → model quoted contents back. The full event chain (`tool invoked` → `tool result` carrying the file content → `agent stream text` quoting it → `agent complete`) was watchable in the `RUST_LOG=debug` log. Cluster closed — assembled run observed, not hypothesized.

### A.5 CLI Prompt — N/A (closed)

Stage A's CLI prompt was the `<work_stage_prompt id="M08.7.A">` block consumed by the build CLI. It is preserved in the git history (the pre-rewrite phase doc at `f15f916` / `28e5614` / `7fd657c`); not reproduced here because Stage A is closed. The archetype it embodies (cluster-gate close + strict-TDD two-commit + ground_at_red + assembled-test-drives-real-path + mutation-gate + IRL-required) is the template Stages B–E mirror.

### A.6 Commit message — as-shipped

`a1eac90 feat(runtime): M08.7.A — built-in tool executor (Read/Write run + feed back)` + follow-up commits (`6f9cbbc test(M08.7.A): cover the built-in executor Op-error path (follow-up additive)`; `95f3052 test(M08.7.A): kill builtin_tool_def ==/!= mutant`; `0db6ace feat(runtime): M08.7.A — debug-log agent events for IRL observability`; `631bbb0 docs(tech-debt): TD-034`; `f9b02bf docs(retro,tech-debt): M08.7.A IRL-observed close + TD-035 path finding`).

---

<!-- ============================================================ -->
<!-- LIVE AUTHORING QUEUE — Stages B, C, D, E to be filled         -->
<!-- in subsequent rewrite pieces (2–5).                            -->
<!-- ============================================================ -->

## Stage B — Capability enforcement on a live tool (rung 2)

> _Authored in Piece 2 of the rewrite — full X.1–X.6 incl. CLI prompt._

## Stage C — Skill load (rung 3) — `LoadSkill` handler

> _Authored in Piece 3 of the rewrite — full X.1–X.6 incl. CLI prompt + ADR-0027 (skill injection model)._

## Stage D — Gap detection (rung 4) — `request_capability` wired into the run loop

> _Authored in Piece 4 of the rewrite — full X.1–X.6 incl. CLI prompt._

## Stage E — Budget at the run loop (rung 5)

> _Authored in Piece 5 of the rewrite — full X.1–X.6 incl. CLI prompt._

## M08.7b — Multi-agent + plan (rungs 6–7) · M08.7c — Hooks/rails, MCP, ARIA (rungs 8–9, N)

> _Authored in Piece 6 — structured-not-detailed forward section; each sub-phase's per-rung X.1–X.6 is authored at sub-phase entry against the prior sub-phase's evidence._

## Stage V — Five-pass verifier (Inventory + Wire + Behavior + Multi-call + v1.9 `assembled_execution`)

> _Authored in Piece 6 — incl. the V.5 CLI prompt._

## Closeout — M08.7a summary + immutable gap-analysis + simplify_pass + coverage reconciliation + the M08.7b/c handoff

> _Authored in Piece 6._

## Summary Table

> _Authored in Piece 6._

## Verification Checklist

> _Authored in Piece 6._
