# M08.8 — Design Workbench + Gap-Resume

> **The milestone where execution becomes app-observable and the runtime becomes human-operable.** M08.7a made the single-agent engine *execute* (rungs 1–5, observed in the assembled engine + per-rung IRL); the app still can't *show* it. M08.8 builds the front-to-back workbench — new-load → run → **watch** → gap → **Grant/Decline** → resume → complete — to `DESIGN.md`. Third beat of the arc: **paints** (pre-M08.7) → **executes** (M08.7) → **observable + operable** (M08.8).

---

## Background — the grounded state at entry

**What runs but isn't visible.** The execution-status ledger (`docs/execution-status.md`) records rungs 1–5 as `executes — observed` **at the engine granularity** (the assembled `run_test_session_with` evals E-01..E-05 + per-rung IRL). The assembled *app* was not the observation surface for any of them:

- **Execution is log-only.** Tool calls / agent text / completions surface through `log_event_debug` at `RUST_LOG=debug`, never on the live graph in the running app. That is **TD-034** (agent output not visible in the app).
- **Budget enforces but is invisible.** Rung 5 halts a run at the cap, but `BudgetHeaderBar` reads the *live* `useGraphStore` while a Tester run replays into the *scoped* `useTestGraphStore` (ADR-0019 isolation) — so the header is structurally blind to a Tester run's spend; and `log_event_debug` drops every `Budget*` event. The rung-5 in-app observe was explicitly deferred here.
- **The gap suspends but dead-ends.** Rung 4 suspends cleanly and records a recoverable snapshot, but there is no resolution surface — the human cannot Grant/Install/Decline and resume. ADR-0029 (Accepted) pulled resolve-and-resume into v0.1; its **engine resume** + **resolution UI** land here.
- **The Builder carries 32 IRL findings** (`docs/M08.6-irl-findings.md`), 5🔴 of which routed to this milestone (#12 clear-canvas, #17 save-from-scratch, #19 tier-UI, #22 budget-cap-persistence, #32 save-companions) plus the 🟡/🟢 UX clusters.

**The unifying insight.** `DESIGN.md`'s nine interaction principles **each trace to a Builder finding** (principle 1 → the no-feedback cluster #1/#14/#21 incl. the dead "Save Cap"; principle 5 → recoverability #11/#12/#13; principle 8 → the "Promote to Promoted" mislabel #20; the component-modal rule → #23/#24). **Building the app *to `DESIGN.md`* IS how the findings get resolved** — the visual layer and the findings-fix are one body of work, not two. So the findings are not a back-half dump; each rides the stage that owns its surface.

**The standard this milestone is held to (rule 11 + ADR-0021).** A UI surface "works" only when the **real Tauri app** has been driven through `tauri-driver` and the behavior observed — never Playwright-mock-green, never code-read. Every stage closes on the real-app IRL. The execution-status ledger gains an **"observed in app"** bar (distinct from M08.7's engine-observe) that M08.8 stages flip.

**Inputs (read at each stage's entry):**

- `DESIGN.md` — the committed "Light Instrument" visual layer (#117): the nine interaction principles, the node-kind two-axis model, the component specs (the live-graph execution view is the centerpiece), modals/panels/badges/toasts behavior rules, AA-contrast tokens.
- `docs/adr/0029-gap-resolve-and-resume.md` (Accepted) — the full gap loop: engine resume (reload snapshot → deliver resolved `tool_result` → continue; **idempotent rebuild §1b — no re-execution of completed calls — is the load-bearing assertion**) + the **Grant / Install / Decline** governed-elevation UI.
- `docs/adr/0021-*` — the `tauri-driver` real-app E2E gate (the merge-blocking close bar).
- `docs/tech-debt.md` — **TD-034** (agent output in app), **TD-036** (tier never wired into the run loop — Tester/smoke always Novice). **TD-037** (canvas authors no skill bodies) **stays M09 Generators** — out of M08.8 scope.
- `docs/M08.6-irl-findings.md` — the 32 findings + clusters; the 5🔴 routed here.
- `docs/execution-status.md` — the paints→executes ledger; stages add the "observed in app" status.

---

## Scope

**In scope (v0.1 §0d holds):** the app surfaces that make the *already-shipped* engine observable + operable — the live-graph execution view, the budget surface, the gap-resolution UI + the engine resume, tier-in-the-run-loop, and the Builder-findings UX build-out — all to `DESIGN.md`, all real-app IRL'd.

**Out of scope:** new engine primitives (sub-agents / plan / hooks / MCP-fix / composition = **M08.9**); canvas skill-body authoring (**TD-037 = M09 Generators**); the mentor (**M09**, deprioritized). No new execution capability is built here — M08.8 *exposes and operates* what M08.7 built; the only new *engine* work is ADR-0029's gap **resume** (the operability half of an already-Accepted decision).

**Locks:** built to `DESIGN.md` (a surface diverging from it is a Stage-D design-review finding); real-app `tauri-driver` IRL is the close bar (ADR-0021); no schema change unless a stage proves one (ADR per §11); findings distributed by surface affinity, not dumped.

---

## Scope correction (the reset behind this staging)

The first B–E draft was loose and shipped a defect M08.8.B's build caught at ground-at-red: it invented a **"dead budget modal"** by misattributing IRL **#23** (MCP `npx`-spawn 🔴, *backend*) + **#24** (MCP Add-modal "Canc" truncation 🟢) — neither is budget (DESIGN.md ties both to the **MCP** modal) — and carried a stale "*add `BudgetState` to `useTestGraphStore`*" hint the shared `createGraphStore` factory already satisfies. The corrected staging routes by **evidence** (#23 → M08.9, #24 → B's `Modal`, #21 → C's `Toast`) and is authored to **M07/M08 depth** — every stage pins the real `file:line`/store-slot/Tauri-command surface before pseudocode.

**Why the visual foundation (B) comes first.** A's Stage-D review surfaced three DESIGN.md divergences — dark canvas, no three-pane shell, dark node cards — with **no owner**, because the first staging applied design *per surface*. DESIGN.md is a **system**: a three-tier token layer + shared `Toast`/`Modal`/shell primitives the component specs assume. Building it **once, first** means C–F build *into* the real shell (natively styled, the primitives present), not retrofit — and re-homes A's Output rail from the stop-gap `.graph-layout` (320px) into the spec'd **360px right rail**. (Web-grounded: a primitive→semantic→component token architecture as CSS custom properties, theme-switchable at the semantic layer — [DTCG](https://www.designtokens.org/); toasts `role=status`/polite with no interactive children — [Sara Soueidan](https://www.sarasoueidan.com/blog/accessible-notifications-with-aria-live-regions-part-1/).)

## Staging (visual foundation → features; each closes on the real-app IRL)

| Stage | Deliverable (forensic) | DESIGN.md / findings |
|---|---|---|
| **A** ✓ | execution-view **data plane** — `stream_text`→rail, tool `{input,output}`, two-axis node data, over both stores | TD-034 (shipped) |
| **B** | **Light Instrument visual foundation** — DTCG 3-tier token rebuild (`styles.css:9` dark `:root` → light), 52/232/360 three-pane shell, dotted light canvas + node cards (`surface-0` / 4px kind bar / glyph chip / status dot), IBM Plex; **+ the missing `Toast` (`role=status`/polite, ~4.2–5s, no interactive children) + `Modal` (z-300, 86vh scroll, untruncated — closes #24) primitives**; run-banner + transport; re-home A's rail → 360px right rail | §Colors/Typography/Layout/Components; #3/#18/#24 |
| **B.fix** | **Complete the Light Instrument restyle** — every app surface (`.budget-bar`/`.builder-palette__item`/`.agent-node`/`.capability-badge`/`.approval-panel`/`.inspector-panel`/Tester/Settings @ `styles.css` 1049–2300) restyled to its `workbench.css` spec (hairline borders · restrained radii · whisper elevation · the mono/tabular register · segmented controls); **+ the 2 missing mockup surfaces** (Tester `.metric` cards, the validation err-card) **+ IBM Plex woff2 + TesterModal→Modal (closes TD-043)**. The entire design, surface-by-surface; no feature logic | §Colors/Typography/Layout/Elevation/Components; the B-IRL "looks bad" + TD-043 |
| **C** | **Budget, visible** — parameterize `BudgetHeaderBar:60` (live↔scoped — the *only* real gap; ramp/badges already exist), paint `budget_*`, cap control via B's `Toast` (kills #21), reconcile the **two** cap controls (`BudgetHeaderBar` inline form + `SettingsPanel::BudgetControl`); Tester full-screen via B's `Modal` | budget bar; #21; *no "budget modal"* |
| **D** | **Gap resolve-and-resume** (ADR-0029) — engine resume (idempotent §1b rebuild) + Grant/Install/Decline panel; **+ audit `recovery::resume`/`recovery_lifecycle.rs` for the B.fix2 fabricated-fixture disease (TD-045)** before reusing it; **mutation BLOCKING** | gap node → banner → resolution; TD-045 audit |
| **E** | **Tier in the run loop** (TD-036; the `tester.rs:481 set_tier` seam exists, the Tauri command never threads it) + tier-UI truth; **mutation BLOCKING**; likely an ADR-0019 amendment | #19/#20; principles 2/8 |
| **F** | **UX hardening + save-path 🔴** — validation/error surface (plain-English + show-raw disclosure), recoverability (clear/delete/undo), `save_framework` companions #32 + from-scratch #17 + cap-persist #22, Tester-results metric cards, badges, panels | #1/#5/#11–17/#22/#32; principles 3/5/6/7 |
| **V** + closeout | five-pass **real-app** verifier + Stage-D aggregate; summary + immutable gap-analysis + simplify + coverage + ADR flips | — |

**Routed out by evidence:** #23 MCP `npx`-spawn, #28 `BUILTIN_TOOLS` set, #31 ARIA narrowing → **M08.9** (engine); TD-037 canvas skill bodies → **M09**.

Each stage section below is authored to **M07/M08 depth** — X.1 Problem → X.2 Files → X.3 Detailed Changes (real `file:line`/types) → X.4 Tests → X.5 CLI Prompt → X.6 Commit — with the v1.8 `<wire_signature_audit>` (pin Tauri params + store-slot types before pseudocode) + `<construction_reachability_check>`. All stages are authored to this depth (A shipped; B–F + B.fix marked `(rewritten)`) and ready to drop in order.

---

## Process (per stage)

1. **Authored at entry** against the live renderer + the `DESIGN.md` component spec (§8 phase-doc pre-flight — cross-machine state checked).
2. **Cluster-gate close discipline** (`docs/cluster-pattern.md`): acceptance-first (BDD) → strict-TDD two-commit → machine gates + mutation gate (on any run-loop/enforcer touch, e.g. Stage C resume, Stage D tier) → **RUN the real app + the maintainer IRL** → triage-in-place (zero-propagation).
3. **Built to `DESIGN.md` + a Stage-D design-review** — the first design-review run of the project (the scaffolding in ORCHESTRATOR open-threads); a surface diverging from `DESIGN.md` is a design-review finding, not shipped.
4. **The close flips the execution-status "observed in app" status** for the surface — on the real-app IRL observation, never tests-green (rule 11).
5. **E2E is `tauri-driver` against the built binary** (ADR-0021; the merge-blocking `e2e-tauri-driver` job). Playwright-renderer is structurally blind to the Tauri shell — it does not close a stage.

---

## Verification checklist (before the M08.8 PR pushes)

- [ ] Every UI stage closed on the **real Tauri app driven via `tauri-driver`** + the maintainer IRL watch — not Playwright-mock-green, not code-read (rule 11 / ADR-0021).
- [ ] The execution-status ledger carries an **"observed in app"** status for each surface M08.8 exposed (execution view, budget, gap-resolution, tier), each citing its real-app E2E + the IRL date.
- [ ] Stage C's resume asserts the **idempotent rebuild** (no re-execution of completed tool calls on reload — ADR-0029 / §1b) in an assembled test.
- [ ] Every surface built matches `DESIGN.md` (the Stage-D design-review signed off; divergences are findings, fixed or waived).
- [ ] The 5🔴 routed here (#12/#17/#19/#22/#32) closed-observed in the real app; the 🟡/🟢 clusters resolved or re-tiered with evidence (zero-propagation — nothing silently dropped).
- [ ] Strict v1.8 two-commit TDD held on every code stage; the mutation gate ran on Stage C (resume) + Stage D (tier) run-loop/enforcer touches.
- [ ] Scope locks held — no new engine primitive (those are M08.9); TD-037 untouched (M09); no schema change unless a stage filed an ADR.
- [ ] The post-M08.8 sequencing is stated in the closeout: **M08.9** (sub-agents → plan → hooks → MCP-fix → composition) → **M09 mentor** (deprioritized; begins only once the front-to-back loop is operable, which M08.8 delivers).

---

## Summary table

| Stage | Goal | Close bar |
|---|---|---|
| A ✓ | execution-view data plane (TD-034) | shipped — rail / payloads / two-axis nodes observed in-app |
| B | Light Instrument visual foundation (tokens + 3-pane shell + canvas/nodes + Toast + Modal + transport) | real-app IRL: the app matches the mockup; Toast/Modal usable; rail in the 360px right rail |
| B.fix | Complete the Light Instrument restyle — every surface to its `workbench.css` spec + metric cards + err-card + Plex woff2 + TesterModal→Modal (TD-043) | real-app IRL: "it looks like the mockup, **fully**" — Stage-D review surface-by-surface |
| C | Budget visible (live↔scoped reconcile + cap Toast + paint budget_*) | real-app IRL: a capped run shows the halt + spend; the cap toasts |
| D | Gap resolve-and-resume (ADR-0029) — engine resume + Grant/Install/Decline | real-app IRL: gap → Grant → resumes → completes; idempotent-rebuild asserted; mutation BLOCKING |
| E | Tier in the run loop (TD-036) + tier-UI truth | real-app IRL: a Promoted run enforces Promoted; the label is true; mutation BLOCKING |
| F | UX hardening + save-path 🔴 | real-app IRL: recoverability + feedback + #17/#22/#32 closed |
| V | five-pass real-app verifier + Stage-D aggregate | 🔴 blocks; 🟡 zero-propagation; 🟢 tech-debt |
| Closeout | summary + immutable gap-analysis + simplify + coverage + ADR flips | three-artifact review; CI green |

**Ending:** a **complete, human-operable, no-AI front-to-back loop** — new-load → run → watch → gap → resolve → resume → complete — that *looks like the mockup* and is IRL-verified on the real Tauri app. v0.1's working core; M08.9 adds the remaining engine primitives, M09 the generators + mentor.

---

## Stage A — Live-graph execution view (TD-034) — the observability foundation

### A.1 Problem statement

A real run's agent output is **invisible in the app** (TD-034, maintainer-confirmed 2026-05-31). Agent events already reach the live graph — `src/lib/ipc.ts:641` `listen<AgentEvent>('agent_event', …)` → `graphStore.applyEvent` (`App.tsx:143`); the backend emits each event on the `agent_event` Tauri channel (`src-tauri/src/commands.rs:297/584` `app.emit("agent_event", &event)`) — and the events **paint nodes**. But the agent's *output* is unwatchable:

- `stream_text` is a **`graphStore` no-op** ("feeds the inspector, not the [graph]" — `graphStore.ts:1574,1734`) that renders **nowhere**. The agent's reply text never appears in the app.
- Tool nodes store `toolName` (`graphStore.ts:902,949`) but **no input/result payload**, and nodes aren't clickable — so a `ToolInvoked(Read)` / `ToolResult` node exposes nothing (you can't see what was read or returned).

So a run paints shapes a human can't read; the only observe channel is `RUST_LOG`. M08.8.A makes execution observable **in-app**, to DESIGN.md's Inspector/Output rail — without a debug log. It is the foundation B–E are watched through.

### A.2 Files to change

- **A new Output/Inspector rail** — DESIGN.md's 360px right-rail "Inspector / Output" (`DESIGN.md` Layout & spacing): renders the streamed agent text live + the selected node's payload, **IBM Plex Mono** for machine output (DESIGN.md Typography "instrument register").
- `src/lib/graphStore.ts` + `src/lib/builderStore.ts` (the scoped test-graph store) — surface `stream_text` (currently no-op) into an **output buffer** the rail reads; **retain tool `{input, output}` payloads** on nodes for inspection.
- `src/components/builder/TesterGraphPane.tsx` (the primary candidate-run watch surface) + `src/components/GraphCanvas.tsx` (the live session) — node click → selection feeding the Inspector; node render to the DESIGN.md two-axis kind/status model.
- `tests/e2e-tauri/execution_view.e2e.ts` (new — the real-app close) + a vitest/Playwright unit (the rail renders given a mocked event stream).
- **No schema change** — the `AgentEvent` union already carries `text` / tool `input` / `output_preview`.

### A.3 Detailed changes

1. `stream_text` → append `event.text` to a per-session **output buffer** in the store; the Output rail renders it live as the agent's reply (DESIGN principle 1 — visible output; mono register).
2. Tool nodes **retain `{input, output}`**; clicking a node selects it; the Inspector renders the selected node's payload (the `Read` input path + the file contents that fed back) — closing the "nodes not clickable / no payload" half of TD-034.
3. Nodes render per DESIGN.md **two-axis** model (kind: accent bar + glyph chip `A/T/S/H/‖`; status: ring/dot idle→active→complete/error/gap) to the extent a single-agent built-in run surfaces them (Agent + Tool; active→complete).
4. Wired for **both** the Tester scoped graph (`useTestGraphStore`) and the live `useGraphStore` — the same rail/inspector component over either store.

### A.4 Acceptance (BDD — the close contract)

```gherkin
Feature: A run is observable in the running app
  Scenario: watch a built-in-tool run
    Given a framework whose agent reads a file and quotes it
    When I run it in the app (Tester)
    Then the agent's streamed reply text is visible in the Output rail (not only RUST_LOG)
    And clicking the Read tool node shows its input path + the file contents it returned
    And the Tool node renders teal (kind) and green (complete status) per DESIGN.md
```

Asserted on the **real Tauri app via `tauri-driver`** (`execution_view.e2e.ts`) + the maintainer IRL watch — NOT Playwright-mock-green, NOT code-read (rule 11 / ADR-0021).

### A.5 construction_reachability_check

Dead wires today (both `inputs_reachable="false"` on `main`): `stream_text` is dropped at `graphStore.ts:1734` → the Output rail has nothing to render; tool nodes carry no payload → a click exposes nothing. Stage A inverts both to `"true"` (text buffered + rendered; payloads retained + inspectable). The build greps `case 'stream_text'` pre-A (no-op) vs post-A (appends to the output buffer the rail reads) and confirms the inversion.

### A.6 Close gate

- **Strict v1.8 two-commit TDD** — red: the e2e + unit asserting output-visible fail for the right reason; impl without touching the test files (`git diff <red>..<impl>` over test paths EMPTY).
- **Machine gates** — `prettier --check` / `eslint` / `tsc --noEmit` / `vitest` (≥80% `src/`) + **`npm run test:e2e:tauri`** (the real-app close, ADR-0021; the merge-blocking `e2e-tauri-driver` job).
- **Mutation gate** — advisory (renderer observability is not execution-wiring / a §6 critical module per `cluster-pattern.md` §5).
- **The close flips execution-status's new "observed in app" status** for the execution-view surface — on the maintainer IRL watch, not tests-green (rule 11).
- **Stage-D design-review** — the Output rail + node render match DESIGN.md (the Inspector/Output rail, the node-kind two-axis model, the mono register, AA contrast). A divergence is a design-review finding.

### A.7 CLI prompt

```xml
<work_stage_prompt id="M08.8.A">
  <context>
    M08.8 Stage A — the live-graph execution view (TD-034). M08.7a made the
    single-agent engine EXECUTE (rungs 1–5, observed at engine granularity);
    the app can't SHOW it — a real run's agent output is visible only via
    RUST_LOG (TD-034, maintainer-confirmed). This stage surfaces agent output
    IN the app per DESIGN.md, the foundation B–E are watched through. It is a
    RENDERER stage (TS/React) — the events already arrive; the gap is purely
    surfacing.

    Grounded reality: agent_event reaches graphStore.applyEvent (ipc.ts:641 →
    App.tsx:143; backend emits at commands.rs:297/584) and paints nodes, BUT
    stream_text is a graphStore no-op (graphStore.ts:1734 — renders nowhere)
    and tool nodes carry toolName without input/result payload and aren't
    clickable. So a run paints shapes a human can't read.

    Cluster-gate close discipline (docs/cluster-pattern.md): acceptance-first
    (the A.4 BDD) → strict-TDD two-commit → machine gates → RUN the real app
    + maintainer IRL → triage-in-place (zero-propagation, NOTHING to M08.9).
    The close bar is the REAL Tauri app driven via tauri-driver + the IRL
    watch (rule 11 / ADR-0021) — Playwright-mock-green does NOT close this.
  </context>

  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Background + Stage A — A.1 problem, A.2 files, A.3 changes, A.4 BDD, A.5 reachability, A.6 close gate)</file>
    <file>DESIGN.md (the 360px right-rail "Inspector / Output" in Layout & spacing; the node-kind two-axis model — kind bar/glyph + status ring/dot; Typography "instrument register" mono for machine output; the interaction principles, esp. #1 feedback + #6 no-silent-discard)</file>
    <file>docs/tech-debt.md TD-034 (the gap this stage closes — its "Recommended approach (M08.7b)" IS this stage)</file>
    <file>docs/adr/0021-* (the tauri-driver real-app E2E gate — the merge-blocking close)</file>
    <file>docs/cluster-pattern.md (§1 close gate, §2 zero-propagation, §4 acceptance-assertion-on-side-effect)</file>
    <file>src/lib/graphStore.ts (applyEvent — the stream_text no-op @1734; tool_invoked/tool_result @873/973; the node data shape) + src/lib/builderStore.ts (useTestGraphStore — the scoped Tester graph)</file>
    <file>src/components/builder/TesterGraphPane.tsx + src/components/builder/TesterModal.tsx (the Tester run-watch path — replays the trace into useTestGraphStore) + src/components/GraphCanvas.tsx (the live graph)</file>
    <file>src/lib/ipc.ts:641 (the agent_event listen) + src/App.tsx:143 (applyEvent wiring) — the live event flow (do NOT change the wire; surface what it already delivers)</file>
    <file>STAGE-PROMPT-PROTOCOL.md (the work-stage schema; strict tdd_discipline) + CLAUDE.md §5/§6 (frontend gates) + §8 (surface-don't-commit)</file>
  </read_first>

  <deliverable>
    Make a real run observable in-app: (1) stream_text rendered live as agent
    text in a DESIGN.md Inspector/Output rail (mono register); (2) tool nodes
    retain {input, output} + are clickable → the Inspector shows the selected
    node's payload; (3) nodes render the DESIGN.md kind/status two-axis model;
    (4) works over BOTH useTestGraphStore (Tester) and useGraphStore (live).
    No schema change (the AgentEvent union already carries the payloads).
  </deliverable>

  <tdd_discipline strict="true">
    Two commits: (1) the failing tests — the execution_view.e2e.ts real-app
    assertions (agent text visible; node-click shows payload) + a vitest unit
    (the rail renders given a mocked event stream) — commit as test(M08.8.A);
    confirm they fail for the right reason (the rail/inspector don't exist /
    render nothing). (2) the impl WITHOUT touching the test files; git diff
    <red>..<impl> over tests/** + **/*.test.* EMPTY. Net-new additive tests +
    mechanical test-file formatting → a separate labelled follow-up commit.
  </tdd_discipline>

  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="A.5"/>

  <execution_steps>
    <implement>
      Per CLAUDE.md §6 frontend gate ordering: prettier --check '**/*.{ts,tsx,..}'
      → eslint . → tsc --noEmit → npm run test (vitest, ≥80% src/) →
      npm run test:e2e (Playwright renderer unit) → npm run test:e2e:tauri
      (the REAL Tauri app — the close, ADR-0021). Build the drone/sandbox
      siblings before tauri dev if a clean wiped them (gotcha #86).
    </implement>
  </execution_steps>

  <close_gate>
    <real_app_irl>The maintainer runs a framework in the app and WATCHES: the
    agent's reply text appears in the Output rail (not only RUST_LOG); clicking
    the Read node shows its input path + the returned file contents; the Tool
    node is teal+green. This IRL watch flips the execution-status "observed in
    app" status for the execution view (rule 11) — orchestration provides the
    walkthrough.</real_app_irl>
    <design_review>The rail + node render match DESIGN.md (Inspector/Output
    rail, node-kind two-axis, mono register, AA) — the first Stage-D review.</design_review>
    <mutation_gate blocking="false">Advisory — renderer observability, not
    execution-wiring / a §6 critical module.</mutation_gate>
    <cumulative_regression>execution_view.e2e.ts joins the append-on-close
    real-app regression suite.</cumulative_regression>
  </close_gate>

  <approval_surface>
    <item>cross-machine state: git log --oneline main..HEAD + the red→impl diff over test paths (EMPTY)</item>
    <item>the machine-gate results (prettier/eslint/tsc/vitest + e2e + e2e:tauri) — each pass/fail with numbers</item>
    <item>the real-app IRL walkthrough script for the maintainer (what to run, what to watch) — orchestration runs it before the execution-status flip</item>
    <item>the Stage-D design-review note (DESIGN.md conformance; divergences as findings)</item>
    <item>explicit: "M08.8.A is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>Renderer-only: do NOT change the agent_event wire (ipc.ts:641 / App.tsx:143 / the backend emit) — surface what it already delivers.</lock>
    <lock>No schema change — the AgentEvent union already carries text / tool input / output_preview.</lock>
    <lock>If surfacing requires a backend or IPC change beyond rendering, STOP and surface — do not silently expand the cluster.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>State what RAN on the REAL Tauri app (tauri-driver) vs only Playwright/vitest + the observed result; the maintainer IRL watch that flipped execution-status "observed in app" for the execution view; that the close was the real-app run, not tests-green (rule 11).</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage A"/>
</work_stage_prompt>
```

---

## Stage B — Light Instrument visual foundation (the design system) — `(rewritten)`

> The enabling stage: the token system + three-pane shell + the `Toast` / `Modal` primitives every later surface inherits. **No feature logic** — a pure visual-system port + two missing primitives. Largest renderer-only stage; mutation gate advisory. **Source of truth is the design export `docs/design/workbench-mockup/` — Stage B ports it, it does not invent.**

### B.1 Problem statement

The app renders the **dark M03 baseline**, not the committed Light Instrument system: `src/styles.css:9` `:root` defines `--node-bg:#15192a` / `--node-fg:#e6e6e6` + per-kind dark colors, and the canvas/nodes/panels are styled against them. A's Stage-D review found three DESIGN.md divergences **with no owner** — dark canvas, no three-pane shell, dark node cards — because the first staging applied design *per surface*. DESIGN.md is a **system**, and its source of truth is `docs/design/workbench-mockup/`: **`tokens.css`** (75 W3C-DTCG tokens — the exact Light-Instrument values), `workbench.css` (the component CSS), and the per-screen `.jsx` (`app.jsx` shell, `components.jsx` primitives, `livegraph.jsx`, `tester.jsx`, `settings.jsx`). Two primitives the specs *assume* don't exist: a reusable **`Toast`** (only `HITLToast`, HITL-specific) and a reusable **`Modal`** (each modal hand-rolls overlay/z-index — #24 lived in one). Stage B ports the system **once**, so C–F build *into* the real shell — and re-homes A's Output rail from the stop-gap `.graph-layout` (320px, `styles.css:100`) into the spec'd **360px right rail**.

### B.2 Files to change

**`<wire_signature_audit>` — pin before pseudocode:** token source = `tokens.css` (`:root` 16–110 + `[data-accent|density|direction]` hooks 117–138); shell = `app.jsx::Shell` (`.topbar` brand + `role=tablist` tabs + tier/key chips → `.workspace`, in `<ToastProvider>`); live shell = `App.tsx` (`BudgetHeaderBar` + `<h1>` + `ViewSwitch` + `SettingsPanel` + `RuntimeLayout|BuilderShell`, `.graph-layout` holding `GraphCanvas` + `InspectorPanel`).

- `src/styles.css` — **replace the dark `:root` (the line-9 block) with the ported `tokens.css` three-tier system** + the `[data-accent|density|direction]` hooks + base `html/body` + the `.t-*`/`.mono`/`.tnum` utilities + the instrument scrollbars; repaint `.graph-layout` / `.graph-canvas` / `.inspector-panel` + every `.*-node` to the tokens. IBM Plex via the `@import` (offline fallback — B.3.6).
- `src/components/Toast.tsx` + `src/lib/toastStore.ts` (NEW) — `ToastProvider` + `useToast().push({kind,title,message})`; container `role="status" aria-live="polite"`, bottom-right stack, left status border, icon+title+plain body, auto-dismiss 4.2s (6s if long), dismissible, **no interactive children**. Ports `components.jsx`'s toast.
- `src/components/Modal.tsx` (NEW) — `<Modal open onClose title size?>`: overlay **z-300** over a blurred scrim, `--r-lg`/`--e3`, **`max-height:86vh` scroll**, Esc/scrim/× close, focus-trap+restore, **untruncated labels**. `MCPServerAddModal` migrates onto it (**closes #24**); `TesterModal` uses `size="full"`.
- `src/components/AppShell.tsx` (NEW) + `src/App.tsx` — the **52px top chrome** (brand · `ViewSwitch` tabs · tier chip · key chip) + the **three-pane workspace** (232px left · fluid center canvas+transport · **360px right Inspector/Output**). Retire the bare `<h1>` ("M03 live graph" — IRL #20/TD-009). Re-mount `InspectorPanel` in the right rail.
- `src/components/RunBanner.tsx` + `src/components/Transport.tsx` (NEW) — run banner + transport (play/scrub/speed + HITL/gap/incident ticks); the scrub re-drives the **existing** `agent_event` replay (`invokeReplaySession`, `App.tsx:134`) — no new persistence. Ports `livegraph.jsx`.
- `src/components/nodes/*` + `GraphCanvas` — to the DESIGN.md card (`--surface-0`, 4px `--kind-*` bar, glyph chip, mono sub-line, `--st-*` dot; selected = `--focus-ring`).
- **Reference (adapt, don't re-invent):** `docs/design/workbench-mockup/{tokens.css, workbench.css, app.jsx, components.jsx, livegraph.jsx, settings.jsx, tester.jsx}`.

### B.3 Detailed changes

**B.3.1 Token port (three-tier, DTCG).** Replace the `styles.css:9` dark `:root` with the `tokens.css` `:root` ported: **primitive** (`--accent*`, `--app-bg`/`--canvas-bg`/`--surface-0..3`, `--border*`, `--text-0..3`, `--ok|warn|error|info`×fill/text/bg/border, type scale, `--s1..s12`, `--r-*`, `--e0..e3`, `--focus-ring`, `--t-*`) → **semantic** (`--kind-*` × color/text/bg, `--st-*` aliasing primitives) → **component**. Keep the variable *names* DESIGN.md uses (rename-free adoption). The `[data-accent|density|direction]` hooks land on `<html>` as the theming seam (the Tweaks panel itself is later). (Web: primitive→semantic→component CSS custom properties, theme-switch at the semantic layer — [DTCG](https://www.designtokens.org/).)

**B.3.2 Three-pane shell.** `AppShell`: `grid-template-columns: 232px 1fr 360px` under a 52px topbar (DESIGN.md §Layout + `app.jsx`). Topbar = brand + `ViewSwitch` as `role=tablist` + the **tier chip** (`currentTier`) + **key chip** (`hasKey`) — both "state visible" (principle 2). Left rail = palette / task+results per view; center = dotted `--canvas-bg` + transport; right rail = Inspector/Output (A's rail, re-homed). Bare `<h1>` removed.

**B.3.3 `Toast`.** `ToastProvider` queue; `useToast().push(...)`. Container `role="status" aria-live="polite"`, fixed bottom-right, each a `--surface-0` card with a 3px left `--{kind}` border, icon, `t-strong` title, `t-small` body, auto-dismiss (4.2s; 6s if `message.length>80`), × dismiss. **No buttons/links inside** (a11y — [Soueidan](https://www.sarasoueidan.com/blog/accessible-notifications-with-aria-live-regions-part-1/), [MDN status](https://developer.mozilla.org/en-US/docs/Web/Accessibility/ARIA/Reference/Roles/status_role)). AA on every tint. C consumes it (cap-saved); F (validate/save).

**B.3.4 `Modal`.** `role="dialog" aria-modal aria-labelledby`, overlay z-300 over a `backdrop-filter:blur` scrim, `--r-lg`/`--e3`, header(title+×)/scroll-body(`86vh`)/optional footer with **full untruncated labels**. Esc+scrim+× → `onClose`; focus traps + returns. `MCPServerAddModal` migrates ("Canc"→"Cancel" — **closes #24**); `TesterModal` `size="full"` (the 2026-06-03 full-screen finding).

**B.3.5 Run banner + transport.** Over the canvas: a run banner (state pill + elapsed, mono tabular) + a transport (play/pause, scrubber over the event timeline, speed, HITL/gap/incident ticks). The scrubber re-drives the *existing* replay (`invokeReplaySession` re-emits `agent_event`s the stores already reduce); no new persistence, no schema change. Idle collapses the transport. Ports `livegraph.jsx`.

**B.3.6 Node cards + fonts.** Each `.*-node` → the DESIGN.md card (`--surface-0`, `--r-md`, `--e1`, 4px `--kind-*` bar, mono glyph chip, name `t-body`/600, mono sub-line, `--st-*` dot/ring; selected `--focus-ring`; running tool = flowing edge). IBM Plex via the `tokens.css` `@import`; **offline note** — the Tauri WebView may block the Google-Fonts fetch, so bundle the Plex `woff2` + a local `@font-face` if the IRL shows the fallback font (confirm at red).

### B.4 Tests

- **vitest (≥80% `src/`):** `Toast` — `role=status`/`aria-live=polite`, renders the message, auto-dismisses (`vi.useFakeTimers`), × dismisses, **no focusable descendants**; `Modal` — Esc/scrim/× each fire `onClose`, focus trapped+restored, body scrolls at 86vh, labels untruncated; `AppShell` — three panes render, Inspector in the right rail, tier/key chips reflect state; a token smoke (a node reads `--kind-tool`).
- **Playwright (renderer):** the shell + a node render to the light tokens; a toast appears+auto-dismisses; a modal opens+Esc-closes.
- **tauri-driver (the close, ADR-0021):** `visual_foundation.e2e.ts` on the **real app** — light canvas; three-pane shell present; a node is a light card with its kind bar; the MCP Add modal shows untruncated "Cancel" (#24); a pushed toast appears bottom-right + auto-dismisses; the Output rail is in the 360px right rail.

### B.5 construction_reachability_check

`inputs_reachable="false"` today: (1) `styles.css:9` `:root` is dark → every surface paints dark; (2) no `Toast` export (`grep -r 'role="status"' src` → none) → "every save/apply toasts" has nothing to call; (3) no generic `Modal` → each modal hand-rolls z-index (#24 lived there); (4) `InspectorPanel` mounts in `.graph-layout` (320px), not the 360px rail. B inverts each to `"true"` — verify by grep (`--surface-0` in the node CSS; `role="status"` in `Toast.tsx`; `z-index: 300` in `Modal.tsx`; `InspectorPanel` mounted in `AppShell`'s right rail).

### B.6 Close gate

Strict v1.8 two-commit TDD (red: the vitest a11y/Modal/shell tests + `visual_foundation.e2e.ts` fail right-reason — no `Toast`/`Modal`, dark `:root`; impl untouching tests). Frontend gates (`prettier`/`eslint`/`tsc`/`vitest` ≥80) + `test:e2e` + **`test:e2e:tauri`** (the close). Mutation **advisory** (pure renderer/CSS). **Stage-D design-review is heavy** — the shipped app vs `Agent Runtime Workbench.html` / `tokens.css`; divergences are findings. The maintainer IRL ("does it look like the mockup?") is the design-conformance gate. **B ships no execution behavior → flips no execution-status row** (it's the surface C–F's flips render through).

### B.7 CLI prompt
```xml
<work_stage_prompt id="M08.8.B">
  <context>
    M08.8 Stage B — the Light Instrument visual foundation. The app ships the
    dark M03 baseline (styles.css:9 :root), not DESIGN.md. Port the design
    system ONCE so C–F build into the real shell. Source of truth:
    docs/design/workbench-mockup/ (tokens.css = 75 DTCG tokens; workbench.css;
    app.jsx/components.jsx/livegraph.jsx). Build the two missing primitives —
    a reusable Toast (role=status/polite, NO interactive children) + Modal
    (z-300, 86vh, untruncated labels — closes #24) — and re-home A's Output
    rail into the 360px right rail. Renderer/CSS ONLY — no feature logic, no
    run-loop/enforcer/persistence change, no schema change. Close = the REAL
    Tauri app via tauri-driver + the maintainer "does it look like the
    mockup?" watch (rule 11 / ADR-0021), NOT Playwright-mock-green.
    Zero-propagation.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage B B.1–B.6 + the spine's DESIGN.md→stage map + the §Scope-correction)</file>
    <file>docs/design/workbench-mockup/tokens.css (port the :root + the data-accent|density|direction hooks — the VALUES) + workbench.css (component CSS) + app.jsx (Shell: topbar + 3-pane workspace + ToastProvider) + components.jsx (Toast/Modal/node markup) + livegraph.jsx (run banner + transport)</file>
    <file>DESIGN.md (§Colors/Typography/Layout/Elevation + §Components — Toast/Modal/Inspector/Canvas specs + the do/don't list)</file>
    <file>src/styles.css (the dark :root @9 + .graph-layout/.inspector-panel/.*-node to repaint) + src/App.tsx (restructure the shell) + src/components/InspectorPanel.tsx (A's rail — re-home) + src/components/MCPServerAddModal.tsx (migrate onto Modal — #24)</file>
    <file>a11y: toasts role=status/polite + no interactive children (sarasoueidan.com aria-live part 1; MDN status role); modal focus-trap + Esc</file>
    <file>docs/cluster-pattern.md (§1/§2/§4) + docs/adr/0021-* (tauri-driver close) + CLAUDE.md §5/§6 (frontend gates)</file>
  </read_first>
  <deliverable>Port tokens.css into styles.css:9 (3-tier system + theming hooks + utilities); the 52/232/360 three-pane shell (AppShell, retiring the bare h1) with A's rail in the 360px right rail; the reusable Toast + Modal primitives (MCPServerAddModal migrates onto Modal closing #24; TesterModal size=full); run-banner + transport over the existing replay; node cards/canvas/panels repainted to the tokens. No feature logic, no schema change.</deliverable>
  <tdd_discipline strict="true">Two commits: red — vitest Toast(a11y/auto-dismiss/no-focusables)/Modal(Esc/scrim/focus-trap/86vh)/AppShell(3 panes, rail in right rail) + visual_foundation.e2e.ts (light canvas, 3-pane, light card, untruncated Cancel, toast appears+dismisses) fail right-reason (no Toast/Modal, dark :root); impl untouching tests (git diff <red>..<impl> over test paths EMPTY). Additive tests separate.</tdd_discipline>
  <wire_signature_audit>Pin before pseudocode: tokens.css :root (16–110) + hooks (117–138); app.jsx::Shell; App.tsx's current composition; InspectorPanel's mount; MCPServerAddModal's overlay. Author token names to MATCH DESIGN.md (rename-free).</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="B.5"/>
  <execution_steps><implement>CLAUDE.md §6 frontend ordering: prettier → eslint → tsc → vitest (≥80 src/) → test:e2e → test:e2e:tauri (the close). If the Plex web-font fails in the WebView (fallback font on screen), bundle the woff2 + a local @font-face (note in the retro).</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer launches the app: light canvas; three-pane shell; nodes are light cards with kind bars; the MCP Add modal shows "Cancel" untruncated; a toast appears bottom-right + auto-dismisses; the Output rail sits in the 360px right rail — "it looks like the mockup." (B ships no execution behavior — flips no execution-status row; this is the DESIGN-conformance gate.)</real_app_irl>
    <design_review>Heavy — shipped app vs Agent Runtime Workbench.html / tokens.css; divergences are findings.</design_review>
    <mutation_gate blocking="false">Advisory — pure renderer/CSS.</mutation_gate>
    <cumulative_regression>visual_foundation.e2e.ts joins the real-app suite; the Toast/Modal vitest joins the unit suite.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY)</item>
    <item>gate results (frontend + e2e + e2e:tauri)</item>
    <item>the Stage-D design-review note (shipped vs the mockup; divergences) + screenshots</item>
    <item>the real-app IRL ("does it look like the mockup")</item>
    <item>explicit: "M08.8.B is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>Renderer/CSS only — no feature logic, no run-loop/enforcer/persistence change, no schema change.</lock>
    <lock>Port the token VALUES from tokens.css; do not invent hex. Keep the DESIGN.md variable names (rename-free).</lock>
    <lock>The transport re-drives the EXISTING replay (invokeReplaySession); do not add a new persistence/replay channel.</lock>
    <lock>Build Toast/Modal as primitives consumed by C–F; do not inline a one-off.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>What ported cleanly from tokens.css/workbench.css vs needed adaptation; the Plex-font offline outcome; the Stage-D divergences (shipped vs mockup); that the close was the real-app "looks like the mockup" watch (rule 11), and that B ships no execution behavior so no execution-status row flips.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage B"/>
</work_stage_prompt>
```

### B.8 Commit message
```
feat(M08.8.B): Light Instrument visual foundation — tokens, shell, Toast, Modal

Port the committed design system (docs/design/workbench-mockup/tokens.css)
into styles.css; build the 52/232/360 three-pane shell + the reusable Toast
(role=status/polite) and Modal (z-300, 86vh, untruncated — closes #24)
primitives; re-home the Output rail into the 360px right rail; repaint the
canvas/nodes/panels to the light tokens; add the run-banner + transport over
the existing replay. Renderer/CSS only — no feature logic, no schema change.

Closes #24 (MCP-modal "Canc" truncation, via the Modal primitive).
```

---

## Stage B.fix — Complete the Light Instrument restyle (the entire design) `(rewritten)`

> **B recolored; it did not restyle.** `styles.css:1–12` states the approach — the legacy `--node-*` names remapped onto the light tokens (@150–162) "so the existing node/panel CSS repaints to light rename-free." Right palette, M03–M08 structure. The app's own classes (`.budget-bar`, `.builder-palette__item`, `.agent-node`, `.capability-badge`, `.approval-panel`, `.inspector-panel`…) keep their old spacing/borders/radii; the instrument texture is shallow (`var(--font-mono)` on 8 selectors vs the mockup's ~20+, `tabular-nums` 3×, one segmented control). **B.fix restyles every app surface to its `docs/design/workbench-mockup/workbench.css` counterpart**, builds the two mockup surfaces the app lacks (Tester `.metric` cards; the validation err-card), bundles IBM Plex, and migrates `TesterModal` onto `Modal` (closes **TD-043**) — so the app matches `Agent Runtime Workbench.html` **surface-by-surface, before C**. Renderer/CSS + the missing surfaces' markup; **no feature logic** — C–F wire behavior into the styled surfaces. The entire design, delivered; nothing deferred. Mutation advisory; **the Stage-D design review IS the gate.**

### B.fix.1 Problem statement

Stage B's IRL surfaced it: *"works, but doesn't look like the mockup."* The cause, observed in `src/styles.css` (2786 lines): B ported the **tokens** (the DTCG three-tier `:root`, byte-faithful — confirmed) and built the **shell** (`.app-shell`/`.topbar`/`.brand__mark`/`.view-switch` @2408–2448) + the **node two-axis model** (@509–1022), but the rest of the component CSS was **recolored, not restyled to the mockup's specs**:

- the instrument register is thin — `var(--font-mono)` on 8 selectors (the mockup sets mono on every machine value: node sub-lines, IDs, the clock, budget/metric numbers, `kv` values, source chips, JSON — ~20+); `tabular-nums` 3× (mockup: all counts/budgets/durations); one segmented control (mockup: view-switch + transport speed + settings + palette tabs);
- the existing components carry M03–M08 spacing/borders/radii, not the `workbench.css` specs: `ApprovalPanel` @1049, `BudgetHeaderBar` @1159, `GapPanel` @1262, `CapabilityBadge` @1342, the MCP modal/rows @1373, the Builder shell/palette @1676, the Inspector + JSON binding @1940, the Tester @2101, the Settings panel @2300;
- two mockup surfaces have **no app markup**: the Tester-results **metric cards** (`.metric` — Result/Verify/Tokens/Spend, mono-tabular) and the dedicated **validation err-card** (plain cause + `→` fix + "Show raw" disclosure);
- `TesterModal` is CSS-full-screen (@2101), not on the `Modal` primitive (**TD-043**);
- IBM Plex loads via the `@import` @ `styles.css:12` — WebView2 may block the Google-Fonts fetch (the B-IRL font question), so the instrument typography isn't guaranteed.

The "Light Instrument" feel `DESIGN.md` defines — *hairline borders do the structure, elevation only where it floats*, restrained radii (8/5/3/12), the mono/tabular register, uppercase tracked micro-labels, segmented controls — is only partially present. **The design is not delivered until every surface matches the mockup.**

### B.fix.2 Files to change

**`<wire_signature_audit>` — pin before pseudocode.** Target = `docs/design/workbench-mockup/workbench.css` (the component specs, 368 lines) + `DESIGN.md` §Colors/Typography/Layout/Elevation/Components + the do/don't list. Current = `src/styles.css` @ the anchors below + the matching `src/components/*`. **The app's class names stay** (rename-free, like B's token remap); each surface adopts its `workbench.css` counterpart's **spec** (spacing/border/radius/elevation/type/mono), not its name.

- **`src/styles.css`** — restyle each surface to its `workbench.css` spec (the B.fix.3 map). Anchors: topbar/brand @2414/2424, `view-switch` @1680, nodes @509–1022, edges @1023, `ApprovalPanel` @1049, `BudgetHeaderBar` @1159, `GapPanel` @1262, `CapabilityBadge` @1342, MCP @1373, Builder @1676, Inspector/JSON @1940, Tester @2101, Settings @2300. Apply the **global discipline** (B.fix.3 foot) across all.
- **Fonts** — bundle `IBM Plex Sans` + `IBM Plex Mono` `woff2` into `src/assets/fonts/` + a local `@font-face`, ending reliance on the `styles.css:12` Google `@import` (keep it as an online fallback or drop it per the IRL result).
- **`src/components/`** — the **NEW** mockup surfaces: a `MetricCard`/Tester-results grid (`.metric` — Result/Verify/Tokens/Spend, styled + stub values; F wires data) and a `ValidationCard`/err-surface (plain cause + `→` fix + a "Show raw error" `<details>`; F wires the real validator). **`TesterModal` migrates onto `Modal`** (`size="full"` — closes TD-043; rewrite the `tester-modal__*` tests in the red phase). Touch `AppShell`, the node components, the Tester only as the restyle/markup needs.
- **Reference (adapt to the app's classes, don't rename):** `workbench.css` + `app.jsx`/`components.jsx`/`livegraph.jsx`/`tester.jsx`/`settings.jsx` + `Agent Runtime Workbench.html`.

### B.fix.3 Detailed changes — the per-surface restyle map

| App surface (`styles.css` anchor) | → `workbench.css` spec it adopts |
|---|---|
| Topbar/brand @2414/2424 · `view-switch` @1680 | `.topbar` (52px, `surface-0`, `border-bottom`, `e1`) · `.brand` (26px **gradient** mark + name + `ver` mono-micro) · `.tabs` **segmented well** (`surface-2`, 3px pad, selected = `surface-0`+`e1`+`accent-on-light`) · `.tier-chip` + key chip **pills** (border, `surface-1`, swatch dot) |
| Nodes @509–1022 (agent/tool/skill/hook/hitl/gap/mcp/plan/task/verify/framework) | `.node` (`surface-0`, `r-md`, `e1`, 4px `::before` kind bar, 22px **mono glyph chip**, name 13/600, **mono** sub-line, 9px status dot) + the full status axis (`s-active` pulse+breathe, `s-gap` pulse, complete/error/violation/blocked/loaded) + `node__io` chip + `data-nodediff`/`data-direction` variants |
| Edges @1023 | `.edge` kind-differentiated (agent neutral 2px · tool teal 1.75 **flow** · skill violet dashed · hook pink 1.5 · hitl amber 2.5) + port dots |
| `ApprovalPanel` @1049 · `GapPanel` @1262 | `.panel` (`surface-0`, `border`, `r-md`, `e1`, disclosure head + chevron) + `.btn` actions; gap entries on `kind-gap-bg`/border |
| `BudgetHeaderBar` @1159 | `.budget` (track `surface-3`, fill `ok`→`warn`→`error`, **mono tabular** nums) — styled here; **C wires live** |
| `CapabilityBadge` @1342 | `.badge` (pill, label/glyph, AA tint families) + `.src` **mono** source chips (`builtin`/`mcp`/`generated`) |
| MCP modal/rows @1373 | the `Modal` primitive (already #24) + `.panel`/row styling; tier-eval as a `.badge` |
| Builder shell/palette @1676 | `.rail-left` (232, `surface-0`, `border-right`) · `.palette__item` (draggable, 4px kind bar, **mono** glyph + hint) · palette tabs **segmented** |
| Inspector/JSON @1940 | `.rail-right` (360) · `.kv` grid (**uppercase tracked** keys, **mono** values) · `.insp-head`/glyph · `.jsonview` syntax tokens |
| Tester @2101 | migrate onto `Modal` (`size="full"`, **TD-043**) + the **NEW** `.metric` cards (Result/Verify/Tokens/Spend, 22px **mono tabular**) + the run-watch |
| Settings @2300 | `.settings`/`.set-row` (hairline-divided) · `.seg` **segmented** controls |
| Transport/RunBanner (B's components) | `.transport` (36px play, `.scrub` track/fill/ticks/head, **tabular** clock, segmented speed) · `.run-banner` suspended/running/done |
| Output rail / stream (A's rail) | `.rail-tabs` (accent underline) · `.stream`/`.entry` transcript (chips + connector lines + reply boxes + **mono** code) |
| **NEW** | `.err-card` validation surface (`err-plain` cause + `err-fix` + `err-raw` disclosure) |

**Global discipline (apply across every surface — this is "the feel"):**

- **Hairline borders do the structure** (1px `--border` #d6deea on rails/topbar/panels/cards); **elevation only where it floats** — `e1` resting cards/nodes, `e2` hover/popover/run-banner/toast, `e3` modals. No heavy fills/shadows for structure.
- **Restrained radii:** cards/nodes `r-md` 8 · buttons/inputs `r-sm` 5 · chips/badges `r-xs` 3 · panels/modals `r-lg` 12 · pills 999.
- **The mono/tabular instrument register:** `IBM Plex Mono` + `font-variant-numeric: tabular-nums` on **every** machine value (IDs, counts, budgets, durations, `$`, token counts, `kv` values, metric values, JSON, source chips, the clock). Uppercase letter-tracked (`~0.06em`) micro-labels on `kv` keys + section labels.
- **Segmented controls** (view-switch, transport speed, settings, palette tabs): grey well (`surface-2`, 3px pad, `r-md`), selected = `surface-0` + `e1` + `accent-on-light`.
- **One accent** (electric blue); `accent-on-light` #1d4ed8 for any accent-colored text (never raw `accent`).
- **Buttons:** `.btn` primary (accent fill, #fff) · ghost · danger; 7×13 pad, `r-sm`, `border-strong`.

### B.fix.4 Tests

- **vitest (≥80% `src/`):** the new `MetricCard`/`ValidationCard` render to spec (mono-tabular values; the "Show raw" `<details>` toggles); `TesterModal` is a `Modal` instance (TD-043 — `role=dialog`, `size=full`, Esc/scrim close); restyle smoke (a node reads `--kind-*` + the 4px bar; the topbar tabs are a segmented well; a `kv` value is mono).
- **Playwright (renderer):** the restyled surfaces render to the light instrument; the metric cards + err-card appear.
- **tauri-driver (the close):** `design_conformance.e2e.ts` on the **real app** — the mono register on machine values, segmented tabs, hairline-bordered panels, the metric cards, the validation card's raw-disclosure, the IBM-Plex font computed (not the fallback), `TesterModal` as a full Modal.

### B.fix.5 construction_reachability_check

`inputs_reachable="false"`: the existing component CSS is **recolored, not restyled** (markers: `var(--font-mono)` 8× — should be ~20+; `tabular-nums` 3× — should cover all counts; one segmented control — should be 4; the @1049/1159/1262/1342/1373/1676/1940/2101/2300 surfaces carry M03–M08 specs); no `.metric`/err-card markup; `TesterModal` hand-rolled @2101; Plex via a blockable `@import` @12. B.fix inverts each — verify by grep (`var(--font-mono)` ≥ ~20; `tabular-nums` on budget/metric/clock; a local `@font-face`; `MetricCard`/`ValidationCard` exports; `TesterModal` rendering `<Modal`).

### B.fix.6 Close gate

Strict v1.8 two-commit TDD (red: the `MetricCard`/`ValidationCard`/`TesterModal`-as-Modal vitest + `design_conformance.e2e.ts` fail right-reason — surfaces unstyled/missing; impl untouching tests; the `tester-modal__*` test rewrite rides the **red** commit). Frontend gates + `test:e2e` + **`test:e2e:tauri`**. Mutation **advisory** (renderer/CSS). **The Stage-D design review is THE gate** — the shipped app vs `Agent Runtime Workbench.html`, **surface-by-surface**; every divergence is a finding. The maintainer IRL — *"it looks like the mockup, fully"* — closes it (rule 11 / ADR-0021). **B.fix ships no execution behavior → flips no execution-status row.**

### B.fix.7 CLI prompt
```xml
<work_stage_prompt id="M08.8.B.fix">
  <context>
    M08.8 Stage B.fix — complete the Light Instrument restyle, the ENTIRE
    design, before C. B recolored the app's M03–M08 components to the light
    tokens but did NOT restyle them to the mockup's component specs
    (styles.css:1–12 — "repaints to light rename-free" via the token remap
    @150–162). Right palette, M03 structure: the instrument register is thin
    (var(--font-mono) 8× vs ~20+; tabular-nums 3×; one segmented control) and
    the existing components (ApprovalPanel @1049, BudgetHeaderBar @1159,
    GapPanel @1262, CapabilityBadge @1342, MCP modal @1373, Builder palette
    @1676, Inspector @1940, Tester @2101, Settings @2300) carry old
    spacing/borders/radii. Restyle EVERY surface to its workbench.css spec
    (keep the app's class names, rename-free); build the 2 missing mockup
    surfaces (Tester .metric cards, the validation err-card); bundle IBM Plex
    woff2 (the @import @12 is WebView2-blockable); migrate TesterModal onto
    Modal (closes TD-043). Renderer/CSS + the missing markup — NO feature
    logic, no run-loop/enforcer/persistence/schema change; C–F wire behavior
    into the styled surfaces. Close = the REAL Tauri app + the maintainer
    "it looks like the mockup, FULLY" watch (rule 11 / ADR-0021) + a
    surface-by-surface Stage-D review. Zero-propagation.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage B.fix — B.fix.1–B.fix.6, esp. the B.fix.3 per-surface map + the global discipline)</file>
    <file>docs/design/workbench-mockup/workbench.css (the 368-line component specs — the TARGET) + Agent Runtime Workbench.html (the rendered reference) + components.jsx/livegraph.jsx/tester.jsx/settings.jsx</file>
    <file>DESIGN.md (§Colors/Typography/Layout/Elevation/Components + the do/don't list — "hairline borders do the structure, elevation only where it floats")</file>
    <file>src/styles.css (the recolored-not-restyled component CSS @ the B.fix.2 anchors) + src/components (AppShell, the node components, the Tester, MCPServerAddModal/Modal)</file>
    <file>docs/tech-debt.md TD-043 (TesterModal→Modal — closed here) + docs/cluster-pattern.md (§1/§2/§4) + docs/adr/0021-* (tauri-driver close) + CLAUDE.md §5/§6</file>
  </read_first>
  <deliverable>Every app surface restyled to its workbench.css spec (hairline borders, restrained radii 8/5/3/12, whisper elevation, the mono/tabular register on all machine values, uppercase tracked micro-labels, segmented controls, the .btn/.badge/.panel/.node/.kv/.budget/.palette__item specs); the 2 NEW mockup surfaces (Tester .metric cards Result/Verify/Tokens/Spend mono-tabular; the validation err-card plain-cause + → fix + Show-raw disclosure); IBM Plex Sans+Mono woff2 bundled + a local @font-face; TesterModal migrated onto Modal (size=full, closes TD-043). The app matches Agent Runtime Workbench.html surface-by-surface. No feature logic, no schema change.</deliverable>
  <tdd_discipline strict="true">Two commits: red — vitest (MetricCard/ValidationCard render-to-spec; TesterModal IS a Modal — role=dialog/size=full/Esc-close; restyle smoke: segmented tabs, mono kv value, 4px node kind bar) + design_conformance.e2e.ts (mono register, segmented tabs, hairline panels, metric cards, raw-disclosure, Plex computed, TesterModal-as-Modal) fail right-reason (surfaces unstyled/missing); impl untouching tests; the tester-modal__* test rewrite rides the RED commit (TD-043). git diff &lt;red&gt;..&lt;impl&gt; over test paths EMPTY. Additive tests separate.</tdd_discipline>
  <wire_signature_audit>Pin before pseudocode: the TARGET = workbench.css component specs (per B.fix.3); the CURRENT = src/styles.css @ topbar 2414, view-switch 1680, nodes 509–1022, ApprovalPanel 1049, BudgetHeaderBar 1159, GapPanel 1262, CapabilityBadge 1342, MCP 1373, Builder 1676, Inspector 1940, Tester 2101, Settings 2300. Keep the app's class names (rename-free, like B's token remap); adopt the SPEC, not the name.</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="B.fix.5"/>
  <execution_steps><implement>CLAUDE.md §6 frontend ordering: prettier → eslint → tsc → vitest (≥80 src/) → test:e2e → test:e2e:tauri (the close). Bundle the Plex woff2 + local @font-face (the @import @12 is WebView2-blockable — confirm the computed font in the real app).</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer launches the app: every surface matches the mockup — segmented topbar tabs, hairline-bordered panels, restrained radii, whisper elevation, the mono/tabular register on all machine values, IBM Plex on screen, the Tester metric cards, the validation card with its Show-raw disclosure, TesterModal as a full Modal. "It looks like the mockup, fully." (B.fix ships no execution behavior — flips no execution-status row; this is THE design-conformance gate.)</real_app_irl>
    <design_review>THE gate — shipped app vs Agent Runtime Workbench.html, surface-by-surface; every divergence is a finding, fixed or waived.</design_review>
    <mutation_gate blocking="false">Advisory — renderer/CSS.</mutation_gate>
    <cumulative_regression>design_conformance.e2e.ts joins the real-app suite; the MetricCard/ValidationCard/TesterModal-as-Modal vitest joins the unit suite.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY)</item>
    <item>gate results (frontend + e2e + e2e:tauri)</item>
    <item>the surface-by-surface Stage-D review (shipped vs the mockup) + screenshots of every surface</item>
    <item>the real-app IRL ("it looks like the mockup, fully") + the Plex-font computed-style confirmation</item>
    <item>explicit: "M08.8.B.fix is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>Renderer/CSS + the 2 missing surfaces' markup only — no feature logic, no run-loop/enforcer/persistence change, no schema change. C–F wire behavior into the styled surfaces.</lock>
    <lock>Keep the app's class names (rename-free); adopt the workbench.css SPEC, not its names. Port values from the mockup; invent no hex.</lock>
    <lock>The metric cards + err-card are STYLED with stub/static content here; C/F wire their real data.</lock>
    <lock>Bundle the Plex woff2 (do not ship relying on the blockable @import).</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>Which surfaces needed the deepest restyle vs were close; the Plex-woff2 outcome; the surface-by-surface design-review divergences (shipped vs mockup); TD-043 closed; that the close was the real-app "looks like the mockup, fully" watch (rule 11) and B.fix flips no execution-status row.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage B.fix"/>
</work_stage_prompt>
```

### B.fix.8 Commit message
```
feat(M08.8.B.fix): complete the Light Instrument restyle — every surface to the mockup

B recolored the M03–M08 components to the light tokens but did not restyle
them to the mockup's specs (styles.css:1–12 — token remap, rename-free).
Restyle every app surface to its workbench.css counterpart (hairline
borders, restrained radii, whisper elevation, the mono/tabular instrument
register, segmented controls, the .btn/.badge/.panel/.node/.kv/.budget/
.palette__item specs); build the 2 missing mockup surfaces (Tester metric
cards, the validation err-card); bundle IBM Plex Sans+Mono woff2 + a local
@font-face; migrate TesterModal onto Modal. The app matches
Agent Runtime Workbench.html surface-by-surface. Renderer/CSS + the missing
markup only — C–F wire behavior into the styled surfaces.

Closes TD-043 (TesterModal → Modal primitive).
```

---

## Stage C — Budget, visible `(rewritten)`

> Renderer-only; rides B's chrome (the budget bar in the 52px topbar) + B's `Toast`. **There is no "budget modal"** — that was the misattribution (#23 = MCP `npx`-spawn → M08.9; #24 = MCP Add-modal → closed by B's `Modal`). Mutation gate advisory: the budget *threshold* engine is M08.7.E, untouched.

### C.1 Problem statement
Budget enforces at the run loop (M08.7.E — `record_spend` → thresholds → `Budget*` events) but is **invisible for a Tester run**. The forensic gap is one line: `BudgetHeaderBar` reads `useGraphStore((s)=>s.budget)` **hardcoded** (`BudgetHeaderBar.tsx:60`), while a Tester run replays into the **scoped** `useTestGraphStore` (`builderStore.ts:444`) — so the chrome bar never lights for a candidate run. The ramp + status badges already exist (`BudgetHeaderBar` `colorClass` + the exceeded/suspended/downshift badges); both stores already carry `budget`/`globalBudgetCap`/the four `budget_*` reducers (the shared `createGraphStore` factory, `graphStore.ts:1858` — the old "add `BudgetState`" hint was stale). Two further findings: the cap control gives **no feedback** on save (`SettingsPanel::BudgetControl` "Save cap" → `set_global_budget`, no toast — **#21**), and the cap is controlled in **two** places — `SettingsPanel::BudgetControl` *and* a duplicate inline form in `BudgetHeaderBar` (lines 140–177).

### C.2 Files to change
**`<wire_signature_audit>` — pin before pseudocode:** `BudgetHeaderBar.tsx:60` (`useGraphStore((s)=>s.budget)`) + its inline cap form (140–177); `SettingsPanel.tsx::BudgetControl:114` (`globalBudgetCap` / `invokeSetGlobalBudget`, no feedback); `useTestGraphStore = createGraphStore()` (builderStore.ts:444, already carries `budget`); B's `Toast` signature (`useToast().push`); the budget bar mounts in B's 52px chrome.
- `src/components/BudgetHeaderBar.tsx` — **parameterize with a `store` prop** (default `useGraphStore`; mirrors A's `InspectorPanel`), so a scoped instance reads the active run's store; **remove the duplicate inline cap form** (the cap lives once, in Settings).
- `src/components/AppShell.tsx` (B) — the chrome `BudgetHeaderBar` (live) sits in the topbar; the Tester (B's full-screen `Modal`) mounts `<BudgetHeaderBar store={useTestGraphStore} />` beside the scoped graph.
- `src/components/SettingsPanel.tsx::BudgetControl` — on a successful save, fire **B's `Toast`** (`push({kind:'ok',title:'Budget cap saved',message:'$X/day'})`) — closes **#21**; keep `invokeSetGlobalBudget` + the `globalBudgetCap` mirror. The single cap control.
- `tests/e2e-tauri/budget_view.e2e.ts` (NEW) + a vitest unit on the parameterized store read.

### C.3 Detailed changes
1. **Parameterize the bar.** `BudgetHeaderBar({ store = useGraphStore })`; every `useGraphStore(...)` read becomes `store(...)`. The chrome instance uses the default (live); the Tester instance passes `store={useTestGraphStore}`. The ramp/badges/exceeded-banner are unchanged — they already render from `BudgetState`. This is the structural fix for the live↔scoped split (the exact shape A used for `InspectorPanel`).
2. **Cap once, with feedback.** Remove `BudgetHeaderBar`'s inline cap form; `SettingsPanel::BudgetControl` is the sole cap control; on a successful `set_global_budget` it pushes B's `ok` `Toast` (kills **#21**). The bar's click opens Settings rather than its own form.
3. **Paint the events.** Already wired — the four `budget_*` reducers drive `BudgetState` in *both* stores; once the bar reads the active store (step 1), warn→amber, downshift→orange, suspended/exceeded→red + the halt banner render for a Tester run. **No reducer / engine / schema change.**

### C.4 Acceptance (BDD)
```gherkin
Feature: Budget is visible for a real run in the app
  Scenario: a tiny-cap Tester run halts, observably
    Given a candidate framework with a tiny session_usd_cap, run in the Tester
    When it crosses the cap
    Then the Tester's budget bar shows the spend + the exceeded/halt state (not only RUST_LOG)
  Scenario: saving the cap confirms
    When I save the daily cap in Settings
    Then a success toast confirms it (closes #21) — no silent save
```
Real-app `tauri-driver` IRL + the maintainer watch (rule 11 / ADR-0021).

### C.5 construction_reachability_check
`inputs_reachable="false"`: `BudgetHeaderBar:60` reads `useGraphStore` hardcoded → a Tester run (`useTestGraphStore`) never lights the bar; `BudgetControl`'s save emits no UI signal → #21. C inverts both (the `store` prop + the scoped mount; the save `Toast`). Verify by grep: `store={useTestGraphStore}` at the Tester mount; the `useToast().push` in `BudgetControl`.

### C.6 Close gate
Strict v1.8 two-commit TDD; frontend gates + `test:e2e:tauri` (a tiny-cap Tester run lights the scoped bar + halts; the cap toasts); mutation **advisory** (the threshold engine is M08.7.E, untouched — C is renderer surfacing); the IRL watch flips execution-status's **"observed in app" for budget** (citing `budget_view.e2e.ts` + the date); Stage-D review (the bar ramp + toast match `tokens.css`).

### C.7 CLI prompt
```xml
<work_stage_prompt id="M08.8.C">
  <context>
    M08.8 Stage C — make budget visible for a real run (closes the M08.7
    rung-5 in-app-observe deferral). Budget enforces (M08.7.E) but the chrome
    bar reads useGraphStore HARDCODED (BudgetHeaderBar:60) while a Tester run
    replays into the scoped useTestGraphStore (builderStore.ts:444) — so a
    candidate run never lights it. The ramp/badges + both stores' budget
    reducers ALREADY exist (the shared createGraphStore factory). The only
    gaps: parameterize the bar (the A InspectorPanel shape) + a scoped Tester
    mount, and give the cap save feedback via B's Toast (#21); reconcile the
    two cap controls. Renderer ONLY — the budget threshold engine (M08.7.E)
    + the budget_* reducers are untouched; no schema change. NOT a "budget
    modal" (a misattribution — #23 = MCP npx → M08.9, #24 = MCP modal →
    closed by B's Modal). Rides B's chrome + Toast + Modal. Close = real-app
    tauri-driver IRL (rule 11 / ADR-0021). Zero-propagation; nothing to M08.9.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage C C.1–C.6) + Stage A (the InspectorPanel store-prop pattern) + Stage B (the Toast + chrome + Modal it rides)</file>
    <file>src/components/BudgetHeaderBar.tsx (reads useGraphStore @60; the duplicate inline cap form @140–177; the ramp/badges to keep) + src/components/SettingsPanel.tsx (BudgetControl @114 — invokeSetGlobalBudget, no feedback = #21)</file>
    <file>src/lib/graphStore.ts (the budget_* reducers @1383–1428; the budget/globalBudgetCap slots; createGraphStore @1858) + src/lib/builderStore.ts (useTestGraphStore = createGraphStore() @444 — already carries budget)</file>
    <file>DESIGN.md (the budget bar ramp + status family; principle 1 feedback) + docs/design/workbench-mockup/tokens.css (the --warn/--error/--ok ramp values)</file>
    <file>docs/cluster-pattern.md (§1/§2/§4) + docs/adr/0021-* + CLAUDE.md §5/§6 (frontend gates)</file>
  </read_first>
  <deliverable>BudgetHeaderBar takes a store prop (default useGraphStore); a scoped instance mounts in the Tester so a candidate run's budget lights the bar (the live↔scoped fix); the cap save (SettingsPanel::BudgetControl) fires B's Toast (closes #21); the duplicate inline cap form is removed (cap lives once). No reducer/engine/schema change.</deliverable>
  <tdd_discipline strict="true">Two commits: red (budget_view.e2e.ts: a tiny-cap Tester run lights the scoped bar + halts; the cap save toasts — + a vitest unit on the parameterized store read — fail right-reason: the Tester bar is blind; the save is silent) → impl untouching tests (diff over test paths EMPTY). Additive tests separate.</tdd_discipline>
  <wire_signature_audit>Pin: BudgetHeaderBar:60 (useGraphStore read) + the inline form 140–177; SettingsPanel::BudgetControl:114; useTestGraphStore @444; B's Toast signature; B's chrome mount. Mirror A's InspectorPanel store-prop exactly — do not fork the component.</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="C.5"/>
  <execution_steps><implement>CLAUDE.md §6 frontend ordering: prettier → eslint → tsc → vitest (≥80 src/) → test:e2e → test:e2e:tauri (the close).</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer runs a tiny-cap framework IN THE TESTER + watches the scoped budget bar show the spend + the halt; saves the cap in Settings + sees the toast. Flips execution-status "observed in app" for budget.</real_app_irl>
    <design_review>The bar ramp + the toast match tokens.css.</design_review>
    <mutation_gate blocking="false">Advisory — renderer surfacing; the M08.7.E threshold engine is untouched.</mutation_gate>
    <cumulative_regression>budget_view.e2e.ts joins the real-app suite.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY)</item>
    <item>gate results (frontend + e2e + e2e:tauri)</item>
    <item>the real-app IRL walkthrough (a Tester tiny-cap run + the cap toast)</item>
    <item>Stage-D design-review note</item>
    <item>explicit: "M08.8.C is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>Renderer only — the budget threshold engine (M08.7.E record_spend/thresholds) + the budget_* reducers are untouched; no schema change.</lock>
    <lock>Parameterize BudgetHeaderBar exactly as A parameterized InspectorPanel (a store prop, default useGraphStore) — do not fork the component.</lock>
    <lock>NO "budget modal" — #23 (MCP npx) is M08.9, #24 (MCP modal) is closed by B's Modal; do not build a budget modal.</lock>
    <lock>The cap lives in ONE control (SettingsPanel::BudgetControl) — remove the duplicate; do not add a third.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>The live↔scoped parameterize approach (vs A's InspectorPanel); the cap-control reconcile (which control was removed); the real-app IRL of a Tester tiny-cap run lighting the bar + the cap toast; that the close was the real-app watch (rule 11), threshold engine untouched.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage C"/>
</work_stage_prompt>
```

### C.8 Commit message
```
feat(M08.8.C): budget visible — parameterize BudgetHeaderBar (live↔scoped) + cap Toast

The chrome budget bar read useGraphStore hardcoded (BudgetHeaderBar:60), so
a Tester run (scoped useTestGraphStore) never lit it. Parameterize the bar
with a store prop (the A InspectorPanel shape) + mount a scoped instance in
the Tester; the cap save (SettingsPanel::BudgetControl) fires B's Toast
(closes #21); remove the duplicate inline cap form. Renderer only — the
M08.7.E threshold engine + the budget_* reducers are untouched; no schema
change.

Closes #21 (Save-cap no feedback).
```

---

## Stage D — Gap resolve-and-resume (ADR-0029), front-to-back `(rewritten)`

> The signature primitive's loop, app-observable end-to-end. **Reuses the existing recovery substrate** — `crates/runtime-main/src/recovery/{mod.rs, resume.rs}` already rebuilds a run from the snapshot **without re-invoking tools** (gotcha #15 = the §1b idempotency), and `agent_sdk.rs:378` already suspends-and-records the gap. D adds the *resolution action* + the resumed *delivery*. Touches the run loop + the capability enforcer → **mutation gate BLOCKING**. May split D1 (engine) / D2 (UI) at entry.

### D.1 Problem statement
Rung 4 (M08.7.D) suspends a `request_capability` gap cleanly and records it (recoverable §1b): `agent_sdk.rs:378` breaks the loop on `feedback.gap_suspended`, and the in-code comment (`agent_sdk.rs:121–126`) already names this "the same resolve-and-resume pattern … folded into ADR-0029." But it **dead-ends** — no resolution surface, and no caller delivers a resolved `tool_result` to resume. ADR-0029 (Accepted) pulled the resume into v0.1: the human Grant/Install/Decline → the runtime **reloads from the snapshot** (idempotent rebuild — *no re-execution of completed tool calls*, already the contract of `recovery/resume.rs` + gotcha #15) → delivers the resolved `tool_result` → the agent's loop resumes. D builds the **resolution action + resumed delivery** (reusing `recovery::resume`) and the **Grant/Install/Decline UI**.

**Precondition — audit the rebuild substrate (B.fix2 fallout).** The UI reload-reconstruct (`replay.rs` / `smoke.e2e.ts` test 6) is **RESOLVED at M08.8.B.fix2** — but its root cause was a *fabricated-fixture* bug: a ~170-line hand-rolled translator (`replay.rs::translate_agent`) read invented `payload.event` field-names against a serialized-`AgentEvent` signal log, tautology-tested against a shape that never existed → **always broken vs real signals** (TD-044; **TD-045** sweeps for siblings). D reuses a **different** rebuild — `recovery::resume` (the engine snapshot rebuild, *not* `replay.rs`) — so reload-reconstruct does **not** prove it. **D's precondition is the TD-045 audit applied to its own substrate — and the orch cross-check already found the gap:** `recovery_lifecycle.rs` is a *real* flow test (real drone IPC, real `recover_session`, asserts task→pending + the no-re-invoke §1b invariant) **but its snapshot is a placeholder** — `state_json: json!({"checkpoint":1})` that `recover_session` **never deserializes** (the test only asserts `snapshot_id.is_some()`). So `recovery::resume`'s **real-snapshot-state reconstruction** — exactly what D's resume-and-deliver leans on — is **untested**. D must extend `recovery_lifecycle` to a **real serialized snapshot state** before reusing `recovery::resume`; inheriting the placeholder-tested path is the `replay.rs` disease one level deeper.

### D.2 Files to change
**`<wire_signature_audit>` — pin before pseudocode:** the suspend wire `agent_sdk.rs:378` (`gap_suspended` loop-break) + the flag `:112` + the ADR-0029 comment `:121–126`; `handle_request_capability` (`agent_sdk.rs:51` / `request_capability.rs` — emits the gap); `narrow(parent, proposed)` `:313`; the **existing** recovery `crates/runtime-main/src/recovery/{mod.rs, resume.rs}` (snapshot rebuild, tools-not-re-invoked, gotcha #15) + `crates/runtime-drone/src/snapshot.rs`; `HitlSeam` `:63`; the gap node + `tokens.css --kind-gap #ea580c` + B's `Modal`.
- **Engine resume** — `agent_sdk.rs` + a `resolve_gap` Tauri command that: (a) reloads the suspended run via **`recovery::resume`** (the §1b rebuild — *no tool re-invocation*); (b) on Grant/Install, updates the enforcer grant set, re-narrowed via `narrow()`; (c) delivers the resolved (granted) `tool_result` into the pending `request_capability`; (d) re-enters `run_agent`'s turn loop. On Decline, delivers a denied `tool_result`.
- **Resolution UI** — the gap node (`--kind-gap`, pulsing `--st-gap`) → a resolution panel (B's `Modal` / the 360px rail) with **Grant / Install / Decline** (governed elevation, no AI); the run banner reads "suspended — awaiting resolution"; on resolve, the run resumes live.
- `tests/` — the assembled idempotent-rebuild test (**extends `recovery_lifecycle.rs`** — resume + deliver runs zero completed tools again; **first audit it for the B.fix2 fabricated-fixture disease — assert REAL serialized snapshot shapes, not invented ones**, TD-045) + `tests/e2e-tauri/gap_resolve.e2e.ts`. (UI reload-reconstruct / `smoke.e2e.ts` test 6 is RESOLVED at B.fix2 — a different `replay.rs` path.)

### D.3 Detailed changes
1. **Reuse the rebuild.** `recovery/resume.rs` already reconstructs the run from the snapshot chain with tools NOT re-invoked (gotcha #15) — the §1b idempotency is *existing*. D's resume calls it, then performs the *new* step: deliver the resolved `tool_result` for the pending `request_capability` + re-enter the loop. **The load-bearing assertion = the rebuild + delivery runs zero completed tools again.**
2. **Govern the grant.** Grant → `enforcer.grant(agent, capability)` re-narrowed via `narrow(parent_grants, proposed)` (`:313`); Install (MCP gap) → install+connect, then grant; Decline → denied `tool_result`. The human is the authorizing principal — **no AI** (ADR-0029's governed re-authorization).
3. **Surface it.** The gap node flips to `--kind-gap`/`--st-gap`; the resolution panel mounts; the transport/banner shows suspended→resumed.

### D.4 Acceptance (BDD)
```gherkin
Feature: A suspended gap resolves and the run resumes
  Scenario: Grant → resume → complete
    Given a run suspended on request_capability for an unheld tool (agent_sdk.rs:378)
    When the human clicks Grant in the resolution panel
    Then the session reloads via recovery::resume WITHOUT re-running completed tool calls (§1b)
    And the pending request_capability receives a granted tool_result
    And the agent's loop resumes and the run completes
  Scenario: Decline → clean end
    When the human clicks Decline
    Then the pending call receives a denied tool_result and the run ends/continues cleanly
```
The idempotent-rebuild leg is asserted in an assembled Rust test (extending `recovery_lifecycle.rs`); the full loop is real-app `tauri-driver` IRL + the maintainer watch.

**Precondition (B.fix2 fallout):** UI reload-reconstruct (`replay.rs` / `smoke.e2e.ts` test 6) is **RESOLVED at B.fix2** — but D reuses `recovery::resume` (a *different*, engine path). Before reusing it, **audit `recovery_lifecycle.rs` for the same fabricated-fixture disease** (assert REAL serialized snapshot shapes, not invented — TD-045). Don't inherit a tautology-tested rebuild.

### D.5 construction_reachability_check
`inputs_reachable="false"`: `agent_sdk.rs:378` suspends + records but **no caller resumes** with a resolved `tool_result`; the gap node has no resolution affordance. D inverts both — the `resolve_gap` command calls `recovery::resume` + delivers + re-enters (a production caller); the panel mounts. Verify: the `resolve_gap` Tauri command; `recovery::resume` called from it; the resolved `tool_result` delivered into the pending call; the panel grep.

### D.6 Close gate
Strict v1.8 two-commit TDD; **mutation gate BLOCKING** on the resume + enforcer-grant/re-narrow + `tool_result`-delivery wiring (a surviving viable mutant blocks close or is justified inline); the assembled idempotent-rebuild test (zero completed tools re-run, reusing `recovery::resume`); machine gates + `test:e2e:tauri`; the IRL watch flips **"observed in app" for the gap loop**; Stage-D review (the gap node + panel match `tokens.css`). **ADR check:** ADR-0029 governs; a new ADR only if the resume reveals a snapshot/grant decision beyond it (§11).

### D.7 CLI prompt
```xml
<work_stage_prompt id="M08.8.D">
  <context>
    M08.8 Stage D — gap resolve-and-resume (ADR-0029), front-to-back: the
    resolution action + the resumed delivery + the Grant/Install/Decline UI.
    Rung 4 (M08.7.D) shipped the SUSPEND half — agent_sdk.rs:378 breaks the
    loop on gap_suspended + records the snapshot (recoverable §1b). KEY: the
    rebuild already exists — crates/runtime-main/src/recovery/{mod.rs,
    resume.rs} reconstructs a run from the snapshot WITHOUT re-invoking tools
    (gotcha #15 = the §1b idempotency). D REUSES recovery::resume + adds the
    new step: deliver the resolved tool_result into the pending
    request_capability + re-enter the loop; Grant re-narrows via narrow().
    The idempotent-rebuild (no completed tool re-runs) is the load-bearing
    assertion. Touches the run loop + the capability enforcer → MUTATION GATE
    BLOCKING. Surface the capability/IPC plan FIRST (Hard Rule 8). May split
    D1 (engine) / D2 (UI). Close = real-app tauri-driver IRL (rule 11 /
    ADR-0021). Zero-propagation; nothing to M08.9.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage D D.1–D.6)</file>
    <file>docs/adr/0029-gap-resolve-and-resume.md (THE contract — suspend→resolve→resume; durable checkpoint-resume + idempotent rebuild + governed human re-authorization)</file>
    <file>crates/runtime-main/src/recovery/{mod.rs, resume.rs} (the EXISTING snapshot rebuild — tools NOT re-invoked, gotcha #15) + crates/runtime-main/tests/recovery_lifecycle.rs (the existing recovery test to extend) + crates/runtime-drone/src/snapshot.rs</file>
    <file>crates/runtime-main/tests/recovery_lifecycle.rs — D's PRECONDITION: audit it for the B.fix2 fabricated-fixture disease (assert REAL serialized snapshot shapes, not invented field-names — TD-045) BEFORE reusing recovery::resume. The UI reload-reconstruct (replay.rs / smoke.e2e.ts test 6) is RESOLVED at B.fix2 — a different path; do not re-litigate it.</file>
    <file>crates/runtime-main/src/sdk/agent_sdk.rs (the rung-4 suspend @378 gap_suspended loop-break + the flag @112/@121-126 comment; handle_request_capability @51; narrow @313; HitlSeam @63; run_agent's loop re-entry)</file>
    <file>agent-runtime-spec.md §1b (recovery rebuilds, does NOT re-execute), §4/§4b (gap flow), §8.security (narrow())</file>
    <file>DESIGN.md (the gap node --kind-gap #ea580c pulsing; the governed-elevation resolution panel) + docs/design/workbench-mockup/livegraph.jsx (the gap/resolution surface) + B's Modal + the gap node component</file>
    <file>docs/cluster-pattern.md (§1/§2/§4/§5 mutation gate) + docs/adr/0021-* + CLAUDE.md §5/§6/§8 (Hard Rule 8 — surface the capability/IPC plan first)</file>
  </read_first>
  <deliverable>A resolve_gap Tauri command that reloads the suspended run via recovery::resume (the §1b rebuild — no tool re-invocation), on Grant/Install updates the enforcer grant (re-narrowed via narrow()), delivers the resolved tool_result into the pending request_capability, and re-enters run_agent's loop; Decline delivers a denied tool_result. The Grant/Install/Decline resolution panel (gap node → B's Modal/the rail, governed, no AI). The idempotent-rebuild (no completed tools re-run) is the load-bearing assertion.</deliverable>
  <tdd_discipline strict="true">Two commits: red (the assembled idempotent-rebuild test — extends recovery_lifecycle.rs: resume + deliver runs zero completed tools again — + gap_resolve.e2e.ts: gap→Grant→resume→complete + Decline — fail right-reason: no resolve caller, no panel) → impl untouching tests (diff over test paths EMPTY).</tdd_discipline>
  <wire_signature_audit>Pin: agent_sdk.rs:378 (the loop-break) + :112/:121-126 (the flag + the ADR-0029 comment); recovery/resume.rs's resume signature; handle_request_capability; narrow @313; the pending-call delivery point in run_agent; B's Modal + the gap node.</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="D.5"/>
  <execution_steps><implement>Rust gates (fmt → clippy --fix → clippy -D warnings → test → llvm-cov; runtime-main ≥95) + frontend gates + test:e2e:tauri. Surface the capability/IPC plan BEFORE coding (Hard Rule 8).</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer drives gap → Grant → watches the run RESUME (not restart — the same nodes, continued) → complete; + Decline → clean end. Flips "observed in app" for the gap loop.</real_app_irl>
    <mutation_gate blocking="true">On the resume + enforcer-grant/re-narrow + tool_result-delivery wiring — a surviving viable mutant blocks close or is justified inline.</mutation_gate>
    <design_review>Gap node + resolution panel match DESIGN.md/tokens.css.</design_review>
    <cumulative_regression>The idempotent-rebuild test + gap_resolve.e2e.ts join the suites.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>the capability/IPC plan (surfaced before coding)</item>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY)</item>
    <item>the idempotent-rebuild assertion result (zero completed tools re-run) + the BLOCKING mutation-gate result</item>
    <item>the recovery_lifecycle.rs fabricated-fixture audit result (TD-045 — does it assert REAL serialized snapshot shapes?) before reusing recovery::resume</item>
    <item>gate results + the real-app IRL walkthrough (gap→Grant→resume→complete + Decline)</item>
    <item>Stage-D design-review note + any ADR filed</item>
    <item>explicit: "M08.8.D is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>REUSE recovery::resume — do not write a second snapshot-rebuild path; the §1b idempotency (tools not re-invoked, gotcha #15) is existing.</lock>
    <lock>Audit recovery_lifecycle.rs for the B.fix2 fabricated-fixture disease (assert REAL serialized snapshot shapes, not invented field-names — TD-045) BEFORE reusing recovery::resume; do not inherit a tautology-tested rebuild. (UI reload-reconstruct / replay.rs / smoke.e2e.ts test 6 is RESOLVED at B.fix2 — a different path.)</lock>
    <lock>The idempotent rebuild MUST NOT re-execute completed tool calls — the load-bearing correctness constraint.</lock>
    <lock>Grant re-narrows via the existing narrow() — never widen beyond the parent's grants. The human is the authorizing principal; no AI mediates.</lock>
    <lock>Surface the capability/IPC plan BEFORE coding (Hard Rule 8). If D exceeds one cluster, split D1 (engine) / D2 (UI) and surface.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>The idempotent-rebuild assertion result (zero completed tools re-run, reusing recovery::resume); the BLOCKING mutation-gate result on the resume/grant/delivery wiring; what RAN real-app (gap→Grant→resume→complete + Decline); any ADR filed; rule 11.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage D"/>
</work_stage_prompt>
```

### D.8 Commit message
```
feat(M08.8.D): gap resolve-and-resume — resolve_gap reuses recovery::resume

Rung 4 suspended the gap (agent_sdk.rs:378) but it dead-ended. Add the
resume half (ADR-0029): a resolve_gap command reloads the run via the
EXISTING recovery::resume (the §1b rebuild — tools not re-invoked, gotcha
#15), on Grant updates the enforcer grant (re-narrowed via narrow()),
delivers the resolved tool_result into the pending request_capability, and
re-enters run_agent's loop; Decline delivers a denied tool_result. The
Grant/Install/Decline resolution panel (gap node -> Modal, governed, no AI).
The idempotent rebuild (zero completed tools re-run) is the load-bearing
assertion. Mutation gate BLOCKING on the resume/grant/delivery wiring.
```

---

## Stage E — Tier in the run loop (TD-036) + tier-UI truth `(rewritten)`

> Touches the capability enforcer (the tier gate) — **mutation gate BLOCKING**. Likely **files an ADR** (the Tester's always-Novice vs user-tier model amends ADR-0019).

### E.1 Problem statement
No production path calls `set_tier` (TD-036): both run-loop entries build `CapabilityEnforcer::new()` (defaults Novice, `enforcer.rs:64`) and never set the user's tier, so **every** real-app run executes at Novice — the Promoted scope-gate is unreachable in-app. The seam exists (`tester.rs:481 enforcer.set_tier(tier)` via `run_test_session_with_tier`); the Tauri command never threads the tracked tier. This is also the **root of #19**: `SettingsPanel::TierControl` displays `currentTier` (`SettingsPanel.tsx:42`, from the live store) but the run enforces Novice regardless — **UI-tier ≠ enforced-tier**. **#20**: the label is `"Promote to Promoted"` when Novice (`:49`) — principle 8 wants a truthful label.

### E.2 Files to change
**`<wire_signature_audit>` — pin before pseudocode:** `tester.rs:481` (`enforcer.set_tier(tier)` via `run_test_session_with_tier`); `crates/runtime-main/src/capability/enforcer.rs` (`set_tier`, the Novice default `:64`, the L4 tier gate); `src-tauri/src/commands.rs` (`test_framework` + `run_smoke_session` entries; `CurrentTierState` / `get_current_tier` / `request_tier_transition`); `SettingsPanel.tsx::TierControl:42` (reads `currentTier`) / `:49` (the label).
- `src-tauri/src/commands.rs` — `test_framework` + `run_smoke_session` read `CurrentTierState` (or `tier.json`) and pass the tier into `run_test_session_with_tier` (already plumbed) + an analogous smoke tier param; re-apply on a mid-session transition.
- `src/components/SettingsPanel.tsx::TierControl` — the displayed tier is now the **enforced** tier (principle 2, closes #19's desync); relabel (`"Promote"` not `"Promote to Promoted"` — principle 8, #20).
- A new ADR amending ADR-0019's Tester tier model (always-Novice-sandbox → user-tier) if confirmed a product decision (surface the plan first — Hard Rule 8).

### E.3 Acceptance (BDD)
```gherkin
Feature: Runs gate on the user's tier
  Scenario: a Promoted user's run enforces Promoted
    Given the user is Promoted
    When a framework runs a Promoted-allowed action (a built-in Write in scope)
    Then it succeeds (not Novice-tier-denied), and an out-of-scope Write shows the scope-gate CapabilityViolation
  Scenario: the tier label is true
    Then the control reads the real current tier and never says "Promote to Promoted" when already Promoted
```
Real-app `tauri-driver` IRL — pairs with A/C (the Promoted scope-gate `CapabilityViolation` is now observable in-app).

### E.4 construction_reachability_check
`inputs_reachable="false"`: `set_tier` has no production caller; the Tauri run commands never thread the tracked tier → every run is Novice (#19 desync). E wires the tracked tier into the run-loop enforcer (a production `set_tier` caller). Verify: `set_tier` called from `test_framework` / `run_smoke_session` with the tracked tier; `TierControl` shows the enforced tier + the truthful label.

### E.5 Close gate
Strict v1.8 two-commit TDD; **mutation gate BLOCKING** on the tier→enforcer wiring; machine gates + `test:e2e:tauri`; the IRL watch (a Promoted run enforces Promoted) flips **"observed in app" for tier**; Stage-D design-review (the tier control matches principles 2/8); **the ADR-0019 amendment filed + flipped Accepted** if the Tester tier model changed.

### E.6 CLI prompt
```xml
<work_stage_prompt id="M08.8.E">
  <context>
    M08.8 Stage E — wire the user's tier into the run loop (TD-036) + fix the
    tier UI (#19 desync, #20 mislabel). No production path calls set_tier;
    every real-app run is pinned to Novice (enforcer.rs:64), so the Promoted
    scope-gate is unreachable in-app — and that IS #19's root (the UI shows
    currentTier, the run enforces Novice). The seam exists (tester.rs:481 via
    run_test_session_with_tier); the Tauri command must thread the tracked
    tier. Touches the capability enforcer → MUTATION GATE BLOCKING. Likely
    files an ADR (amends ADR-0019's Tester always-Novice model — surface the
    plan first per Hard Rule 8). Close = real-app tauri-driver IRL (rule 11 /
    ADR-0021). Zero-propagation; nothing to M08.9.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage E — E.1–E.5)</file>
    <file>docs/tech-debt.md TD-036 (the gap + the recommended wire) + TD-034 (pairs — A/C make the scope-gate violation observable)</file>
    <file>crates/runtime-main/src/builder/tester.rs (run_test_session_with_tier @ the set_tier seam @481) + crates/runtime-main/src/capability/enforcer.rs (set_tier; the Novice default @64; the L4 tier gate)</file>
    <file>src-tauri/src/commands.rs (test_framework + run_smoke_session — the entries to thread tier; CurrentTierState / get_current_tier / request_tier_transition) + src/components/SettingsPanel.tsx (TierControl @42 reads currentTier; the label @49 — #19/#20)</file>
    <file>docs/adr/0019-* (the Tester isolated-session tier model this may amend) + DESIGN.md (principles 2 state-visible + 8 labels-true)</file>
    <file>docs/cluster-pattern.md (§5 mutation gate) + docs/adr/0021-* + CLAUDE.md §5/§6/§8</file>
  </read_first>
  <deliverable>The Tauri run commands read the tracked/persisted tier and set_tier on the run-loop enforcer before dispatch (Tester + smoke), re-applied on transition; the tier UI shows the real (enforced) tier + a truthful label. ADR-0019 amendment if the Tester tier model changes.</deliverable>
  <tdd_discipline strict="true">Two commits: red (a Promoted-run-enforces-Promoted assembled/e2e test + the label test fail right-reason: the run is Novice; the label says "Promote to Promoted") → impl untouching tests (diff over test paths EMPTY).</tdd_discipline>
  <wire_signature_audit>Pin: tester.rs:481 (set_tier seam); enforcer.rs:64 (Novice default) + set_tier signature; the test_framework/run_smoke_session entries + CurrentTierState; SettingsPanel::TierControl:42/:49.</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="E.4"/>
  <execution_steps><implement>Rust gates (runtime-main ≥95) + frontend gates + test:e2e:tauri. Surface the tier-model ADR plan BEFORE coding (Hard Rule 8).</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer (Promoted) runs a Promoted-allowed action + watches it succeed (not Novice-denied); the label reads true. Flips "observed in app" for tier.</real_app_irl>
    <mutation_gate blocking="true">On the tier→enforcer wiring.</mutation_gate>
    <design_review>Tier control matches DESIGN.md (principles 2/8).</design_review>
    <cumulative_regression>The tier-enforcement e2e joins the suite.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>the tier-model ADR plan (surfaced before coding) + the ADR if filed</item>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY)</item>
    <item>the mutation-gate result (blocking) + gate results + the IRL walkthrough</item>
    <item>Stage-D design-review note</item>
    <item>explicit: "M08.8.E is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>Tier wiring only — set_tier from the tracked/persisted tier; NEVER widen capability beyond the user's actual tier.</lock>
    <lock>Surface the ADR-0019 tier-model amendment plan BEFORE coding (Hard Rule 8).</lock>
    <lock>No other capability-enforcement change.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>The BLOCKING mutation-gate result on the tier→enforcer wiring; the ADR filed (if any); the Promoted-enforced real-app IRL; that #19's root was UI-tier≠enforced-tier; rule 11.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage E"/>
</work_stage_prompt>
```

### E.7 Commit message
```
feat(M08.8.E): tier in the run loop — thread the tracked tier into the enforcer

No production path called set_tier (TD-036): every real-app run was pinned
to Novice (enforcer.rs:64), so the Promoted scope-gate was unreachable AND
the UI tier desynced from the enforced tier (#19). Thread the tracked tier
(CurrentTierState) into run_test_session_with_tier + run_smoke_session
(set_tier before dispatch, re-applied on transition); the tier control shows
the enforced tier + a truthful label (#20 — no "Promote to Promoted").
Mutation gate BLOCKING on the tier->enforcer wiring; ADR-0019 amendment if
the Tester tier model changes.
```

---

## Stage F — UX hardening + save-path 🔴 (the DESIGN.md build-out) `(rewritten)`

> The remaining 🔴/🟡/🟢 Builder findings as a build-to-DESIGN.md pass, **riding B's `Toast`/`Modal` + the validation/error-surface, Tester-results, badge, and panel specs DESIGN.md §Components defines.** May split F1 (save-path 🔴 — Rust persistence) / F2 (UX — renderer) at entry. Mutation advisory.

### F.1 Problem statement
The 🔴 save-path regressions (#17 `save_framework` hard-fails from-scratch; #22 budget cap doesn't persist across restart; **#32 `save_framework` drops tools/skills companion files** — the MVP §M8 round-trip regression that escaped M08.6.V, gotcha #66) + the cross-cutting UX findings, each a DESIGN.md violation: recoverability (#12 clear-canvas, #11/#13 delete/undo — principle 5), feedback (#1 key-save, #14 validate — principle 1, via B's `Toast`), plain-language validation (#5/#15 — principles 6/7, the DESIGN.md validation/error surface: plain cause + `→` fix + "Show raw error" disclosure), config defects (#2–9), edit-discard guard (#16 — principle 6), contrast (#3 — AA tokens from B), progressive disclosure (#18 — panels). DESIGN.md also specs **Tester-results metric cards** (Result/Verify/Tokens/Spend, mono tabular) + **Badges** (validation/capability/source) — not yet built.

### F.2 Files to change
**`<wire_signature_audit>`:** `crates/runtime-main/src/builder/persist.rs::save_framework` (the companion-write path #32 — it writes `framework.json` but the companion `.md` write is incomplete/untested, the M08.6 escape); the byte-stable round-trip tests (assert only the json hash); the renderer canvas / Inspector / validation surfaces; B's `Toast`/`Modal`/badge tokens.
- `crates/runtime-main/src/builder/persist.rs` — **#32**: write the companion `.md` files on save (+ a test asserting companions exist **on disk**, not only the `framework.json` hash — the gotcha #66 fix); **#17**: from-scratch save succeeds; **#22**: persist the budget cap across restart (a `globalBudgetCap` persistence file, like `tier.json`).
- `src/` renderer (to DESIGN.md §Components) — clear-canvas + delete/undo (#11/#12/#13, principle 5); save/validate **`Toast`** feedback (#1/#14); the **validation/error surface** (per-node plain-English cause + `→` fix + "Show raw error" disclosure holding the literal serde/typify string — #5/#15); config-field defects (#2–9); edit-discard warn/cancel via B's `Modal` (#16); the **Tester-results metric cards** + **Badges**; contrast (#3, B's AA tokens); panel disclosure (#18).

### F.3 Acceptance (BDD)
```gherkin
Feature: The workbench is recoverable, gives feedback, and round-trips
  Scenario: save writes companions (round-trip)
    When I save a framework with tool/skill bodies and reload it
    Then the agents/tools/skills .md companions are on disk and reload identical (MVP §M8 #8)
  Scenario: recoverability + feedback + plain-language errors
    Then the canvas has clear + per-node delete + undo; every save/validate confirms with a Toast;
    And an invalid node shows a plain-English cause + fix (not a raw serde string), with a "Show raw error" disclosure
```
The save-path round-trip is an assembled Rust test (companions **on disk**); the UX is real-app `tauri-driver` IRL.

### F.4 construction_reachability_check
`inputs_reachable="false"`: #32 — `save_framework` writes `framework.json` but the companion-write path is incomplete/untested (the M08.6 escape; the round-trip test asserts only the json hash); the recoverability/feedback/validation-surface/Tester-cards/badges don't exist. F wires + tests companion writes (assert files on disk) + adds the renderer surfaces (consuming B's `Toast`/`Modal`/badges).

### F.5 Close gate
Strict v1.8 two-commit TDD (the save-path test asserts the **SIDE EFFECT — companions on disk** — per cluster-pattern §4 / gotcha #66; it must fail on `main` (companions absent) and pass after); machine gates + `test:e2e:tauri`; mutation **advisory** (persistence + renderer); the IRL watch; Stage-D review against the do/don't list + the validation/Tester-card/badge specs. **The 5🔴 routed here (#12/#17/#22/#32) closed-observed; #19/#20 are E.**

### F.6 CLI prompt
```xml
<work_stage_prompt id="M08.8.F">
  <context>
    M08.8 Stage F — UX hardening + the save-path 🔴, a build-to-DESIGN.md pass
    riding B's Toast/Modal + the DESIGN.md §Components surfaces (validation/
    error surface, Tester-results cards, badges, panels). The 🔴: #32
    save_framework drops companion files (the MVP §M8 round-trip regression
    that escaped M08.6.V — gotcha #66 class), #17 save-from-scratch hard-fails,
    #22 budget cap doesn't persist. The cross-cutting UX findings each map to a
    DESIGN.md principle. May split F1 (save-path Rust) / F2 (UX renderer).
    Close = real-app tauri-driver IRL + the save-path assembled round-trip
    asserting COMPANIONS ON DISK (not the json hash — the §4 rule). The 5🔴
    (#12/#17/#22/#32) close-observed. Zero-propagation; nothing to M08.9.
  </context>
  <read_first>
    <file>docs/build-prompts/M08.8-design-workbench.md (Stage F — F.1–F.5) + Stage B (the Toast/Modal/badge tokens it rides)</file>
    <file>docs/M08.6-irl-findings.md (the 🔴 #17/#22/#32 + the 🟡/🟢 clusters: Builder-UX #11–13/25/26/29/30, no-feedback #1/14, validator-opacity #5/15, config #2–9, edit-discard #16, contrast #3, disclosure #18)</file>
    <file>DESIGN.md (§Components — the validation/error surface, Tester-results cards, Badges, Panels specs; the interaction principles + do/don't list) + docs/gotchas.md #66 (tests-pass-contract-fails — the #32 escape) + docs/design/workbench-mockup/{tester.jsx, components.jsx}</file>
    <file>crates/runtime-main/src/builder/persist.rs (save_framework — the companion-write path #32; from-scratch #17) + the byte-stable round-trip tests (which asserted only the json hash) + the tier.json persistence pattern (for the #22 cap-persist file)</file>
    <file>src/components (the canvas / Inspector / SettingsPanel surfaces) + docs/cluster-pattern.md (§4 assert the side effect) + CLAUDE.md §5/§6</file>
  </read_first>
  <deliverable>save_framework writes + tests companions ON DISK (#32) + from-scratch save (#17) + cap persistence (#22); the canvas/Inspector gain recoverability (clear/delete/undo), Toast feedback (#1/#14), the plain-language validation/error surface (#5/#15) with a "Show raw error" disclosure, edit-discard guard (#16, Modal), the Tester-results metric cards + Badges, contrast (#3) + disclosure (#18) — all to DESIGN.md.</deliverable>
  <tdd_discipline strict="true">Two commits: red (the round-trip test asserting companions ON DISK + the UX e2e fail right-reason) → impl untouching tests. The #32 test MUST fail on main (companions absent) and pass after — the gotcha #66 fix.</tdd_discipline>
  <wire_signature_audit>Pin: save_framework's current write path (json only); the round-trip test's hash-only assertion; the tier.json persistence pattern (for #22); the canvas/Inspector/validation surfaces; B's Toast/Modal/badge signatures + the validation/Tester-card specs in DESIGN.md.</wire_signature_audit>
  <construction_reachability_check ref="docs/build-prompts/M08.8-design-workbench.md" section="F.4"/>
  <execution_steps><implement>Rust gates (the persist round-trip; runtime-main ≥95) + frontend gates + test:e2e:tauri.</implement></execution_steps>
  <close_gate>
    <real_app_irl>Maintainer saves+reloads a framework with bodies (companions survive on disk) + exercises clear/delete/undo + sees save/validate Toasts + sees a plain-English validation error with a "Show raw" disclosure + the Tester-results cards. Closes the 5🔴 routed here, observed.</real_app_irl>
    <design_review>The surfaces match DESIGN.md's §Components (validation/error surface, Tester cards, badges, panels) + the do/don't list.</design_review>
    <mutation_gate blocking="false">Advisory — persistence + renderer.</mutation_gate>
    <cumulative_regression>The companion round-trip test + the UX e2e join the suites.</cumulative_regression>
  </close_gate>
  <approval_surface>
    <item>cross-machine state + the red→impl diff over test paths (EMPTY); the #32 test red-on-main proof</item>
    <item>gate results + the real-app IRL walkthrough; the 5🔴 disposition (closed-observed)</item>
    <item>Stage-D design-review note (vs §Components)</item>
    <item>explicit: "M08.8.F is ready. I will not commit until you approve."</item>
  </approval_surface>
  <scope_locks>
    <lock>The save-path test asserts the SIDE EFFECT (companions on disk), not the framework.json hash (§4 / gotcha #66).</lock>
    <lock>The UX findings build to DESIGN.md §Components — no scope beyond the named findings (#1–#18 + #17/#22/#32); reuse B's Toast/Modal/badges, do not re-roll.</lock>
    <lock>May split F1 (save-path Rust) / F2 (UX renderer) — surface the split if so.</lock>
  </scope_locks>
  <gates milestone="M08"/>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/RETROSPECTIVE-TEMPLATE.md">
    <special_log>The #32 red-on-main proof (companions absent on main, present after); the 5🔴 closed-observed in the real app; the validation/error surface + Tester cards vs DESIGN.md §Components; rule 11.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Stage F"/>
</work_stage_prompt>
```

### F.7 Commit message
```
feat(M08.8.F): UX hardening + save-path red — companions on disk (#32), recoverability, plain-language errors

#32 save_framework dropped tool/skill companion files (the MVP §M8 round-trip
regression that escaped M08.6.V — the round-trip test asserted only the json
hash, gotcha #66): write + test companions ON DISK. #17 from-scratch save;
#22 cap persistence. Renderer (to DESIGN.md §Components): clear/delete/undo
recoverability, Toast feedback (#1/#14), the plain-language validation/error
surface with a "Show raw error" disclosure (#5/#15), edit-discard guard (#16),
Tester-results metric cards, badges, contrast (#3), panel disclosure (#18).
```

---

## Stage V — Five-pass verifier (real-app `assembled_execution`) + Stage-D design-review

### V.1 Problem statement
Run the fresh-context five-pass verifier against A–F. M08.8's headline is **observable-in-app**, so V's central duty is to **drive the real Tauri app** (`tauri-driver`, ADR-0021) and observe each surface — not read tests. The M08.6.V escape (Sound 0🔴 while the app was never run) is exactly what this prevents.

### V.2 Scope to verify
| Stage | Observable-in-app check (the 5th pass drives the real app) |
|---|---|
| A | A real run's agent text renders in the Output rail + a tool node click shows its input/result payload (shipped) |
| B | The app matches the mockup — light canvas + 3-pane shell (52/232/360) + light node cards; a `Toast` appears + auto-dismisses; the MCP modal shows untruncated "Cancel" (#24); the rail is in the 360px right rail |
| C | A tiny-cap **Tester** run lights the **scoped** budget bar + halts (not only RUST_LOG); saving the cap toasts (#21) |
| D | gap → Grant → the run **resumes** (not restarts; no re-exec of completed calls — reuses `recovery::resume`) → completes; Decline → clean end |
| E | a Promoted user's run **enforces Promoted** (a Promoted-allowed action succeeds; the label reads true — #19/#20) |
| F | save → reload round-trips with **companions on disk** (#32); clear/delete/undo + `Toast` feedback + the plain-language validation surface work |

### V.5 CLI prompt
```xml
<verifier_stage_prompt id="M08.8.V">
  <context>
    M08.8 Stage V — the fresh-context, FIVE-pass verifier (ADR-0008 + the
    v1.9 5th assembled_execution pass). M08.8 made the engine OBSERVABLE +
    the runtime OPERABLE in the app. Run with empty session memory: you have
    NOT seen the M08.8.A–F retrospectives, the M08.8 summary, or any
    gap-analysis beyond M08.7's. The clear-and-paste session is the bias guard.

    V's CENTRAL DUTY here is the OBSERVABLE-IN-APP check: M08.8's whole point
    is that a run is watchable in the running app — so the 5th pass DRIVES THE
    REAL TAURI APP (npm run test:e2e:tauri, ADR-0021), not the engine, and
    OBSERVES each surface. A "Sound" that did not drive the real app is
    FORBIDDEN (rule 11 — your own verdict is grounded at "the real app showed
    it"). Findings: 🔴 block → D.fix (max 2 iter); 🟡 zero-propagation (NEVER
    carry to M08.9); 🟢 docs/tech-debt.md.
  </context>
  <read_first>
    <file>STAGE-PROMPT-PROTOCOL.md §14 + docs/cluster-pattern.md (§3 the 5th pass, §4 assert-the-side-effect) + docs/adr/0008-* + docs/adr/0021-* (tauri-driver)</file>
    <file>docs/build-prompts/M08.8-design-workbench.md (Background + Stages A–F + V.2 — but NOT any *-retrospective.md the doc references)</file>
    <file>docs/adr/0029-gap-resolve-and-resume.md (Stage D's contract — the idempotent rebuild) + DESIGN.md + docs/design/workbench-mockup/ (the design-review aggregate target)</file>
    <file>docs/execution-status.md (the "observed in app" rows A–F flip) + docs/tech-debt.md TD-034/036 + docs/M08.6-irl-findings.md (the 🔴 F closed)</file>
    <file>tests/e2e-tauri/ (the real-app E2E suite A–F added) + the renderer surfaces</file>
    <file>docs/build-prompts/STAGE-V-VERIFIER-PROMPT-TEMPLATE.md + VERIFIER-RETROSPECTIVE-TEMPLATE.md</file>
  </read_first>
  <scope_to_verify ref="docs/build-prompts/M08.8-design-workbench.md" section="V.2 Scope to verify"/>
  <verification_passes>
    <pass name="inventory">Each A–F deliverable exists + matches its X.2/X.3. Missing→🔴; stub→🟡.</pass>
    <pass name="wire">The 5-step data-path trace (gotcha #66) per surface — esp. D's resume (a PRODUCTION caller reloads the snapshot via recovery::resume + re-narrows + delivers the resolved tool_result) + E's set_tier production caller (test_framework/run_smoke_session thread the tracked tier). Break at step 4 → 🔴.</pass>
    <pass name="behavior">Each test asserts the observable side effect, not the event/structure alone (§4): agent text rendered (A); the app matches the mockup + a Toast dismisses + #24 untruncated (B); spend shown + run halted in the scoped bar (C); run RESUMED not restarted (D); Promoted enforced (E); companions on disk + recoverability (F). Event/structure-only → 🔴.</pass>
    <pass name="multi_call_invariants">A second run re-renders cleanly (A); a re-resolved gap doesn't double-deliver (D); the idempotent rebuild does NOT re-execute completed calls (D — load-bearing).</pass>
    <pass name="assembled_execution">THE 5th PASS — DRIVE THE REAL TAURI APP (npm run test:e2e:tauri) and OBSERVE per the V.2 table. A surface whose real-app E2E you did NOT run → that verdict is a HYPOTHESIS, labeled (rule 11), pass INCOMPLETE — say so. Report N/M (no key → state it; never 0/0-as-pass).</pass>
  </verification_passes>
  <design_review>Aggregate Stage-D review: A–F conform to DESIGN.md / docs/design/workbench-mockup (the token system + 3-pane shell, node-kinds two-axis, Inspector/Output rail, budget ramp, gap panel, tier labels, validation surface + Tester cards, the do/don't list, AA). Divergences → findings.</design_review>
  <findings_format ref="docs/build-prompts/retrospectives/VERIFIER-RETROSPECTIVE-TEMPLATE.md" section="Findings"/>
  <merge_gate red_blocks="true" dfix_iteration_cap="2" waiver_path="docs/adr/NNNN-waiver-M08.8-finding-N.md"/>
  <gates milestone="M08"/>
  <approval_surface>
    <item>the five-pass findings table (🔴/🟡/🟢 + file:line + the rule each tests)</item>
    <item>the 5th-pass result per surface — RAN-real-app vs read; observed behavior; N/M</item>
    <item>which verdicts are grounded-by-real-app-execution vs hypothesis (rule 11)</item>
    <item>the design-review aggregate (DESIGN.md / mockup conformance across A–F)</item>
    <item>the merge-gate disposition + the verifier retrospective</item>
    <item>explicit: "M08.8.V is complete. Findings surfaced for routing; I will not commit until you approve."</item>
  </approval_surface>
  <retrospective_requirements ref="docs/build-prompts/retrospectives/VERIFIER-RETROSPECTIVE-TEMPLATE.md">
    <special_log>State, per A–F surface, whether you DROVE THE REAL TAURI APP (the 5th pass) or only read its test — and the observed result; N/M for the e2e:tauri run (or "not run", never 0/0-as-pass); which verdicts are grounded-by-real-app-execution vs hypothesis (rule 11); the design-review aggregate's DESIGN.md/mockup divergences.</special_log>
  </retrospective_requirements>
  <commit_protocol ref="CLAUDE.md" section="8. PR + commit workflow (CRITICAL — read carefully)"/>
  <commit_message ref="docs/build-prompts/M08.8-design-workbench.md" section="Closeout — V commits its retrospective per ADR-0008"/>
</verifier_stage_prompt>
```

---

## Closeout

Per CLAUDE.md §19 (summary) + §20 (immutable gap-analysis), build-side (it aggregates the A–F + V retros + runs simplify_pass + coverage):

1. **`M08.8-summary.md`** — aggregate the A–F + V retros; 3-axis scoring; time-box; the decisions to apply before M08.9; the verdict. Drafts the PR description.
2. **The immutable `docs/gap-analysis.md` M08.8 entry** (§20, append-only) — the six sections + the established extras. Load-bearing carry-forward: **the 32 M08.6-IRL findings with OBSERVED dispositions** (which closed-observed in the real app at A–F, which re-tiered), and **each execution-status "observed in app" row** cited to its real-app E2E + IRL date.
3. **`<simplify_pass>`** over `M08.8.A..HEAD` (proposal; deferrals → tech-debt).
4. **`<coverage_policy_reconciliation>`** — any gate change (Stage C/D Rust touches) syncs the four mirrors + a §C entry.
5. **ADR flips** — any ADR filed at C (snapshot/grant) or D (ADR-0019 tier amendment) `Proposed → Accepted`.
6. **Post-M08.8 sequencing** stated: **M08.9** (sub-agents → plan → hooks → MCP-fix → composition) → **M09 mentor** (begins only once the front-to-back loop is operable — which M08.8 delivers).

Three-artifact review (code diff + retros/summary + gap-analysis), surfaced together; CI green → ADRs flipped → merge.
