# M08.5 IRL Re-verify — Session Handoff (2026-05-23)

Paste this back when resuming. Self-contained — Claude doesn't need to re-read
full history.

---

## Where we are

- **M08.5 E.fix committed locally on build machine** as `2226d78` on
  `claude/m08.5-irl-fix`. NOT pushed. E.fix marked M06.5 🔴-1 as RESOLVED
  pending IRL re-verify; the re-verify proved that assumption WRONG.
- **Build machine repo path**: `C:\agent-runtime` (Windows, app working dir).
- **Orchestration session** (this session) is on
  `claude/review-agent-runtime-yktaL`. Latest orchestration commit:
  `fd93286` — `docs/post-m08.6-critical-review.md`.
- **Latest on `main`** when this paused: `ce54539` (PR #98 merged —
  docs-only CI skip).
- Cycle PRs to date for M08.5: A.fix landed via main; E.fix not yet pushed.

## The 3 🔴 M08.5 was scoped to close — all PASS on IRL

- **🔴-1 palette drag**: PASS — node instantiates + draggable on Builder Canvas
- **🔴-2 Tester root agent name**: PASS — Tester scoped-graph node shows
  `lead-orchestrator` (the candidate framework's root agent role), NOT
  "smoke". `null` in VDR is the known-carried M08.V 🟡 #1.
- **🔴-3 MCP modal buttons**: PASS — all three buttons (Test / Cancel / Add)
  respond. Backend gets invoked. (Test and Add then hit a SEPARATE Windows
  spawn bug — see 🔴 NEW #6 below — but that's not a 🔴-3 regression.)

## NEW findings the IRL re-verify surfaced

### 🔴 candidates (4 new + 1 still-open)

- **🔴 NEW #4 — Tier state UI/backend desync.** UI shows "Current tier: novice"
  but backend treats user as Promoted (MCP modal opens, which is gated on
  Promoted). Click "Promote to Promoted" → backend logs
  `previous=Promoted target=Promoted; idempotent no-op`. Desync persists
  across `npm run tauri dev` restart.
- **🔴 NEW #5 — Builder Canvas state not persisted across app restart.**
  User's edited framework reverted to an old/different state after Window-A
  restart. No "saved canvas state" load on app start.
- **🔴 NEW #6 — MCP Add/Test buttons enter infinite loop on Windows.**
  Backend gets invoked (button click works — 🔴-3 fix proven) but spawning
  the MCP subprocess fails with Windows OS error: "The filename, directory
  name, or volume label syntax is incorrect." Repeats indefinitely in
  Window A. No UI surface for the failure. No row written to either DB.
  Likely `npx` path resolution or argument quoting in `runtime-mcp` on
  Windows.
- **🔴 NEW #7 — Schema drift between `session.sqlite` and stray `mcp.sqlite`.**
  Both DBs have `mcp_servers` tables with DIFFERENT status CHECK constraints:
  - `session.sqlite`: `status IN ('connected', 'disconnected', 'health_pending', 'error')`
  - stray `mcp.sqlite`: `status IN ('configured', 'connected', 'errored', 'disabled', 'failed')`
  Two SQLite DBs on the same machine with incompatible schemas for the same
  table name — exactly the schema-drift class the architectural audit warned
  about.
- **🔴 STILL OPEN — M06.5 🔴-1 MCP-registry mis-resolve.** The stray
  `mcp.sqlite` file exists at
  `C:\Users\kknip\AppData\Local\dev.aria-runtime.app\mcp.sqlite` (4096 bytes,
  dated 5/17/2026), contains a COMPLETE session-DB schema (sessions,
  signals, snapshots, vdr, plans, tasks, mcp_servers, etc. — not just an
  mcp-only DB), and has an `fs-test` row from 5/17 prior testing.
  `session.sqlite` mcp_servers is empty. The MCP registry is writing to the
  WRONG DB. The E.fix amend planned to mark this RESOLVED — it must stay
  OPEN.

### 🟡 candidates (11 new)

- 🟡 — `wdio.conf.ts:31` hardcodes a stale `src-tauri/target/release/`
  path that doesn't match the workspace layout (Cargo workspace members
  share workspace-root `target/`). Real fix: update `APP_BIN_PATH` in
  `wdio.conf.ts` to use `target/release/agent-runtime.exe`. Workaround
  documented at `docs/build-machine-tauri-driver-setup.md` Phase 3.5
  (junction). Folds into Revision 1's protocol commit when M08.5 ships.

- 🟡 — The `e2e-tauri-driver` harness assumes the Tauri app can find
  sibling subprocess binaries (`runtime-drone.exe`, `runtime-sandbox.exe`)
  in `target/release/` next to `agent-runtime.exe`. `npx tauri build`
  only builds the Tauri package itself; the sibling binaries must be
  built separately via `cargo build --release -p runtime-drone -p runtime-sandbox`.
  CI does this in two named steps (A.fix follow-up #3 + #4); local setup
  guide now documents this in Phase 3 (was missing → app crashed at
  startup with "drone IPC unavailable: spawn drone subprocess: ... file
  not found"). Real fix: have `wdio.conf.ts:onPrepare()` build all three
  binaries via cargo before spawning tauri-driver, or document in
  `Cargo.toml` workspace metadata.

- 🟡 — WebDriver Actions API multi-step drag (B.fix's regression test)
  does NOT synthesize past Edge 148's dragstart threshold on local Windows
  even though it passes on CI (older Edge). Test fails locally with
  `[data-testid="builder-tool-node-Read"] still not existing after 5000ms`;
  manual IRL verifies the drag works in the running app. CI is the
  verifier of record for this regression test. Real fix: retry the
  WebDriver Actions sequence with larger intermediate pointerMove, or
  use the `dnd` action variant if available in webdriverio v9. Logged as
  a separate 🟡 because it's a NEW symptom (Edge auto-update broke local
  parity with CI).

(original 8 below)

- 🟡 — Promote button label doesn't update when at target tier ("Promote to
  Promoted" reads as nonsense when already Promoted). Should hide, switch to
  "Demote", or display "Promoted (active)".
- 🟡 — Settings panel is an always-visible large horizontal bar — no
  progressive disclosure / no collapse.
- 🟡 — Builder Canvas generates agents with INVALID id format (observed:
  `demo-agent@1.0.0`). Schema agent.v1.json requires `^[a-z][a-z0-9-]*$` —
  no `@`, no `.`.
- 🟡 — Builder Canvas omits required `capabilities` block when creating
  agents. Tester then rejects with opaque
  `data did not match any variant of untagged enum FrameworkAgentsItem`.
- 🟡 — Configure modal on Canvas lacks Save button AND lacks a toast/hint
  pointing to Inspector → Save. User has no signal where to save.
- 🟡 — Inspector → Save dialog: no preloaded filename, no extension hint;
  defaults to a previous-session directory unrelated to current framework.
- 🟡 — `session_root_agent` has no UI surface in the Builder; user must hand-edit JSON tab.
- 🟡 — Test plan `docs/M08-irl-test-plan.md` has stale column name:
  `transport_type` → should be `transport`.

### 🟢 elevation

- 🟢 elevation of TD-009 (stale "M03 live graph" `<h1>` label): broaden to
  "strip ALL M0N references from UI" + "add proper small-type version
  display in usual chrome location."

### 🟢 re-confirmed standing ask

- UI modernization: progressive disclosure + expand/collapse for ALL modals
  (already in carry-forward; this IRL pass re-confirmed scope).

## Decisions finalized (2026-05-23)

Both pending decisions resolved during this session. No outstanding
protocol-level decisions blocking M08.5 ship.

### Routing the new 🔴 — Option C (split)

- **M08.5.5** new short fix-cycle (3 stages): 🔴 #6 (MCP Add/Test
  infinite-loop on Windows) + M06.5 🔴-1 (stray mcp.sqlite + schema
  drift). One cluster — MCP resilience on Windows.
- **M08.6 Stage A intake**: 🔴 #4 (tier UI/backend desync) + 🔴 #5
  (Canvas state not persisted) + 🔴 #7 (DB schema drift implication) +
  11 🟡 + 🟢 elevation. State-sync + Builder validation + UI
  modernization.

### DESIGN.md + Stage D protocol — Option A (3-brain stack, post-M08.6)

- DESIGN.md at repo root — open standard (Google Stitch + Anthropic).
  Orchestration protocol reads it; build machine reads it when
  generating UI code.
- Claude Design (web product at claude.ai/design/) — does the visual
  designing. Not me. Invoked occasionally per milestone or when UI
  modernization is needed.
- Stage D Design Review — new orchestration stage at each M0N closeout,
  separate from Stage V (V = contract fidelity; D = UX quality).
- Introduce **post-M08.6 ship**. Don't add the third brain mid-cycle.

## Master plan (numbered)

1. M08.5 E.fix amend + push + CI + PR (build machine doing now)
2. M08.5 PR merge
3. M08.5.5 phase doc author (orchestration)
4. M08.5.5 build cycle (build machine)
5. M08.5.5 PR merge
6. M08.6 phase doc revisions — absorb 🔴 #4/#5/#7 + 11 🟡 + 🟢
   elevation into Stage A intake
7. M08.6 build cycle (full milestone with Stage V + closeout)
8. M08.6 PR merge
9. Post-M08.6 IRL re-verify
10. Claude Design web → generate DESIGN.md → commit at repo root
11. Stage D protocol scaffolding — templates + ledger + CLAUDE.md
    updates
12. First formal Stage D run (retroactively on M08.6)

## M08.5.5 stage split (proposed — finalize before authoring)

**Pattern-consistent with M06.5 / M07.5 / M08.5 (3-stage X.5 fix cycle).**

- **M08.5.5 Stage A.fix — harness hardening + smoke-test env-var fix
  + gitleaks gate**. Wires the harness fixes surfaced during M08.5 IRL
  pass that aren't M08.5 scope:
  - `wdio.conf.ts:31` workspace path fix (drafted on review branch,
    commit `0239804`).
  - `wdio.conf.ts` `.env.local` loader for local API key support
    (drafted on review branch, commit `0239804`).
  - Smoke-test env-var fix — see "Smoke-test env-var fix approach"
    section below for the two options + recommendation.
  - `gotchas.md` additions (5+ new gotchas from M08.5 IRL pass +
    install).
  - **gitleaks secret-scan integration** (one-time gate):
    - `lefthook.yml` — add a `pre-commit` block running
      `gitleaks git --staged --no-banner` so accidental secret stages
      block locally.
    - `.github/workflows/ci.yml` — add a `gitleaks` job (using the
      `gitleaks/gitleaks-action@v2` step with `fetch-depth: 0` so the
      full history scans) gated by `detect-cargo` (skip on docs-only
      changes per the existing path filter).
    - `docs/build-machine-tauri-driver-setup.md` — add Phase 0.5 noting
      gitleaks is required locally for the pre-commit hook (`apt install
      gitleaks` on Linux; `winget install gitleaks` on Windows; or the
      release binary).
    - **Why this matters**: scanned the full git history at M08.5 IRL
      pass (324 commits, all branches) + working tree — 0 leaks
      confirmed. This gate keeps the repo at 0 going forward. Strict-TDD
      stage scope: assembled regression is a CI run on a deliberate
      no-op commit confirming gitleaks job ran + reported 0 leaks; on a
      seed-secret commit (test branch, not pushed) gitleaks fails the
      CI job loud and clear. Verifies the gate is live, not silently
      broken.

- **M08.5.5 Stage B.fix — 🔴 #6 MCP Add/Test Windows spawn**. Root cause
  diagnosis (live-DOM + log analysis enabled by tauri-driver installed
  in M08.5 IRL): likely `npx` path resolution or argument quoting in
  `runtime-mcp` spawn on Windows. Strict-TDD: assembled regression test
  driving the real-app modal Add button → assert row appears in
  `session.sqlite` within timeout; fails on `main` (infinite loop); green
  on fix. Fix is in `runtime-mcp` (or its Windows-spawn helper).

- **M08.5.5 Stage C.fix — M06.5 🔴-1 stray mcp.sqlite + schema drift**.
  Two interlocking sub-fixes:
  - MCP registry path resolution: confirm it writes to `session.sqlite`
    (the `$REG` canonical DB), NEVER creates a stray `mcp.sqlite`.
    Assembled regression: Add server → query both DBs → assert row in
    `session.sqlite`, no row in stray file, no stray file created.
  - Schema reconciliation: align `mcp_servers` status CHECK constraint
    in `session.sqlite` migration to match the newer schema in the
    stray DB (or vice versa per the spec § MCP server status). One
    canonical schema. Migration test passes.

## Smoke-test env-var fix approach (Option A vs Option B)

Surfaced during the tauri-driver install: smoke test #2 in
`tests/e2e-tauri/smoke.e2e.ts` hardcodes a placeholder API key
(`sk-ant-test-1234567890123456`) and saves it to OS keychain via the
SetupPanel UI. Tests 3-6 then make REAL Anthropic API calls and use the
keychain entry → auth fail with the placeholder.

**Option A — change test code**: smoke test #2 reads
`process.env.ANTHROPIC_API_KEY` when set, falls back to the placeholder
when unset. Tests 3-6 then use the env var via keychain. One-file edit
(`smoke.e2e.ts`), low blast radius. Slight fragility: tests are now
env-dependent.

**Option B — change app code** (recommended): Tauri shell command
that reads the API key checks `ANTHROPIC_API_KEY` env var FIRST, falls
back to keychain when env var unset. Standard pattern (env var as
override). One change in `src-tauri/src/commands.rs` (the key-resolver
command). Tests stay unchanged; both local + CI work cleanly.

**Lands in M08.5.5 Stage A.fix.**

## Pending: gotchas.md additions (M08.5.5 Stage A.fix)

Five new gotchas to add to `docs/gotchas.md` (numbers continue from the
current highest):

1. tauri-driver doesn't support `--version` flag — use `--help`.
2. Build-machine tauri-driver install: Edge auto-updates can invalidate
   msedgedriver; re-download per setup-guide Phase 2 when "version
   mismatch" errors appear.
3. `wdio.conf.ts` stale workspace path — release binary at
   workspace-root `target/release/`, NOT `src-tauri/target/release/`
   (fixed by the M08.5.5.A wdio.conf.ts update).
4. `npx tauri build` doesn't build sibling subprocess binaries
   (`runtime-drone`, `runtime-sandbox`) — must `cargo build --release
   -p runtime-drone -p runtime-sandbox` separately.
5. WebDriver Actions API multi-step drag fails on Edge 148+ even when
   the underlying `dragDropEnabled: false` fix works — CI's older Edge
   is the verifier of record.



## Machine context (paths used during the IRL pass)

- Build machine: `C:\agent-runtime` (Windows, PowerShell admin)
- `$APPDATA_DIR` = `C:\Users\kknip\AppData\Local\dev.aria-runtime.app`
- `$REG` = `$APPDATA_DIR\session.sqlite` (canonical)
- Stray DB = `$APPDATA_DIR\mcp.sqlite` (the bug)
- `$SCRATCH` was `C:\Users\kknip\AppData\Local\Temp\m08irl-1318332649` (one-shot for the session)

## User preferences carried in this session

- Brevity — single paragraphs, no walls.
- Choose best practice over simple/cheap on architectural calls.
- Verify on web before guessing.
- Never commit without explicit approval; never push to main.
- Refused ANTHROPIC_TEST_KEY secret (HITL IRL test path).
- "I'm not managing the CI" — minimal CI surface.
- UI modernization is a v1.0+ priority ("cool goes a long way for
  acceptance").

## Open architectural discussion (NEW — added mid-install)

User raised a separate architectural concern during the tauri-driver
install: "design quality" is an unconsidered lens in the protocol. M08
IRL surfaced 8+ UX findings that no Stage V / no test would catch
because contract-fidelity ≠ design-quality. User asked about a `DESIGN.md`
+ Stage D Design Review.

**Web research confirmed** (2026): `DESIGN.md` is the emerging open
standard (Google Stitch + Anthropic). Format: YAML frontmatter design
tokens (W3C DTCG-compatible) + markdown prose. Canonical sections:
Overview, Colors, Typography, Layout, Elevation, Shapes, Components,
Do's and Don'ts. Lives at repo root, like README.md / CLAUDE.md.

**Claude Design** is Anthropic Labs' web product at `claude.ai/design/` —
reads codebase → generates DESIGN.md + UI mockups → hands off to Claude
Code via a portable SKILL.md. Powered by Claude Opus 4.7.

**Three-brain stack discussed**:
- Orchestration (planning, retros, protocol) — this session's role
- Build (code, tests, gates) — separate Windows session
- Claude Design (visual iteration) — web product, occasional per-milestone

**Two options surfaced** (user noted they'd come back to this post-tauri-driver-install):
- A. Three brains (full Anthropic stack — best design quality, complexity cost). Includes Claude Design + Stage D protocol stage.
- B. Two brains (DESIGN.md authored by orchestration, Stage D internal — simpler, loses rich visual iteration).

User leaning A but only post-M08.6 (don't add third brain mid-cycle).

**Pending protocol-doc draft when this resumes**: DESIGN.md starter for
this repo + Stage D (Design Review) protocol additions:
- `STAGE-DESIGN-REVIEW-PROMPT-TEMPLATE.md`
- `DESIGN-REVIEW-RETROSPECTIVE-TEMPLATE.md`
- `docs/design-quality.md` (append-only ledger, analog to coverage-policy.md)
- CLAUDE.md §19 retro protocol update
- STAGE-PROMPT-PROTOCOL.md new `<design_review_stage_prompt>` schema

Sources cited in chat: Google open-sourcing DESIGN.md (blog.google);
google-labs-code/design.md repo; Anthropic Claude Design announcement;
DataCamp Claude Design overview; VoltAgent awesome-claude-design.

## Before M9 (Generators) — starter-kit methodology reconciliation

The maintainer has a parallel project, **build-framework-starter-kit**
(`github.com/kknipe2k/build-framework-starter-kit`), shipping next week
to internal work users. It is a tier-calibrated (Lite / Standard /
Full) build methodology that has REVERSE-ENGINEERED much of the
runtime's own methodology into a portable, stack-agnostic starter:
phase docs + XML stage prompts, schema validator, Stage V Verifier,
orchestrator/build split + ORCHESTRATOR.md, append-only gap-analysis
(CI-enforced at Full), 3-axis retrospectives + threshold gates, ADRs
with Proposed→Accepted protocol, X.5 fix cycles, cross-session-
blindness protection.

**Decision finalized 2026-05-23**: do NOT entangle the two before
M08.5.5 / M08.6 ship. Reasons:

- The starter-kit ships independently to internal users next week;
  needs to be standalone + stable.
- The runtime's master plan (M08.5 → M08.5.5 → M08.6 → DESIGN.md /
  Stage D / M9) is already long. Adding methodology reconciliation
  mid-cycle would slow both.
- Internal-user feedback on the starter-kit (which tiers people pick,
  where calibration interview fails, what overhead they tolerate) is
  higher-signal than the runtime's solo-developer evidence. Wait for
  that data before reconciling.

**Cheap-now action**: maintain a delta log on each side so reconciliation
is mechanical (not archaeology) when it lands. Five-minute weekly
maintenance per side.

- `docs/methodology-delta.md` in the runtime (this repo) — captures
  "starter-kit has X (link); runtime has Y; reconcile at M9+" per
  diverged pattern.
- `RUNTIME-DELTA.md` in the starter-kit — symmetric, captures
  "runtime has Y; starter-kit has X; reconcile at M9+".

**Reconciliation gate**: before authoring the M9 (Generators) phase
doc, the orchestration session reads the two delta logs end-to-end and
incorporates the merged methodology. M9's Generators are the LLM that
builds frameworks; the methodology those generated frameworks inherit
is the reconciled merge of both sides' best practices.

**Starter-kit patterns the runtime should consider adopting** (current
read of the README, 2026-05-23):

- Tier calibration (Lite / Standard / Full). Runtime is implicitly
  Full-tier hardcoded — no Lite path. M9's generated frameworks need
  the tier dial.
- Expertise dial (Novice / Familiar / Experienced). Calibration
  interview at bootstrap.
- SessionStart hook for auto-loading read-first (vs runtime's current
  honor-system reading; the M04 PR #53 false-premise rewrite was
  partly a read-first enforcement failure).
- Stage R (refactor health check) — proposal, parallel to Stage V but
  for code maintainability. Triggered by `docs/tech-debt.md`
  accumulation rate.
- Operating modes (greenfield / bug_fix / audit / research_publish)
  — proposal. Runtime's current cycle types (X.5 fix cycle, full
  M0N milestone, IRL re-verify) are de facto operating modes; the
  starter-kit's formalization gives vocabulary + structure.

**Runtime patterns the starter-kit should consider adopting** (for the
symmetric `RUNTIME-DELTA.md`):

- DESIGN.md + Stage D Design Review (post-M08.6) — the starter-kit
  doesn't have a design-quality lens yet; Stage V is contract-fidelity,
  Stage R is maintainability, Stage D is UX quality. Three lenses,
  three fresh-context stages.
- Strict-TDD v1.8 two-commit pattern (red commit + impl commit + diff-
  EMPTY invariant) — the runtime's strongest TDD enforcement to date.
- Real-app regression gate (ADR-0021 tauri-driver) — the structural
  response to "tests pass but the assembled app is broken." Applies
  to any Tauri/Electron/web framework the starter-kit might target.
- 12-step master plan / cross-machine state stamps — the orchestration
  pattern the starter-kit's ORCHESTRATOR.md already has but the runtime
  has more IRL evidence on (M04 PR #53 precedent).

**Action item for orchestration**: create the runtime-side delta log
(`docs/methodology-delta.md`) as a separate small commit in the
post-M08.5.5-ship window (between steps 5 and 6 of the master plan).
Symmetric to the starter-kit's `RUNTIME-DELTA.md` which the maintainer
adds on the starter-kit side.

## Resume protocol

When the user feeds this doc back:

1. Read this file end-to-end (it's <2 minutes).
2. Confirm: "Resumed. Open decisions are: (a) routing the new 🔴 —
   Option A, B, or C? Recommendation: C — split. (b) DESIGN.md + Stage D
   protocol decision — Option A (3 brains, post-M08.6) or B (2 brains,
   now or later)?"
3. Wait for both decisions.
4. Once routing decided, draft the E.fix amend block (verbatim
   observations to paste into `docs/M08-irl-findings.md`) for the user
   to relay to the build machine.
5. Once DESIGN.md route decided, draft the protocol changes (or just
   DESIGN.md if Option B internal) per the route.
6. After build machine amends + pushes, proceed per the routing decision.
