# Common Gotchas (lessons learned)

The spec is large and the project covers a lot of ground. These are traps that have already bitten the work or are predictable based on the design. CLAUDE.md §15 references this file as the source.

1. **Tool ≠ Skill ≠ Agent.** Three distinct concepts (§0b). Tools are called. Skills are loaded into context. Agents are spawned. Don't conflate them; the schemas, file formats, and runtime mechanics are different.
2. **Capability narrowing on Agent→Agent edges.** A child agent's `allowed_tools` and `allowed_skills` cannot exceed the parent's. The Builder Canvas (Phase 9) enforces this automatically; manual JSON editing must respect it.
3. **v0.1 ships STANDARD mode hardcoded.** No mode router (§3b). The framework JSON's `modes` field still exists in schema but is not evaluated at runtime in v0.1.
4. **v0.1 ships `fresh_context_per_task` only.** The continuous loop policy (Ralph-style) is in the schema but not implemented. `examples/ralph/framework.json` exists but won't run on v0.1.
5. **v0.1 ships Novice + Promoted tiers only.** No Operator tier. Promoted is blocked from auto-accepting `shell: true` and `network: ["*"]` artifacts even though the tier's general behavior is auto-accept-when-validated.
6. **v0.1 is single-session.** §1c Multi-session is v1.0. Do not write multi-session code paths in v0.1; they create surface area without benefit.
7. **v0.1 is Windows-only.** Not because Tauri is Windows-only — Tauri is cross-platform — but because we test only on Windows in v0.1, and macOS/Linux ports come at v1.0. CI still runs on all three OSes to catch drift early.
8. **No telemetry, ever.** No analytics, no crash reporter, no "anonymous metrics," no phone-home. Per §13 of spec. Adding any requires an ADR with public dashboard plan + opt-in mechanism.
9. **Anthropic API is hit directly.** No `@anthropic-ai/sdk` dep, no `anthropic-rs`. `reqwest` + `eventsource-stream` only. The API surface is small and stable; direct HTTP keeps the dependency surface minimal.
10. **Tauri allowlist is the security boundary.** The renderer has no Node API. Anything the renderer needs from Rust goes through a typed `#[tauri::command]`. Don't widen the allowlist without considering capability implications.
11. **Drone ≠ Main ≠ Sandbox.** Three Rust processes. Drone owns SQLite + snapshots + recovery (per session). Main owns the agent loop, MCP, providers, framework loader, capability enforcer. Sandbox is per-artifact, OS-isolated, used for L3 validation. Don't blur these.
12. **IPC is two layers.** Renderer↔Main is Tauri typed IPC. Main↔Drone is framed JSON over Unix socket / named pipe. Different mechanisms with different semantics. Don't try to use Tauri IPC for drone communication.
13. **SQLite WAL pragmas matter.** `PRAGMA journal_mode = WAL`, `PRAGMA synchronous = NORMAL`, `PRAGMA busy_timeout = 5000`, `PRAGMA foreign_keys = ON`. Set them in this order at every connection open. Missing busy_timeout causes flaky tests under contention.
14. **Snapshots are append-only.** Drone never updates a snapshot row; new snapshot = new row. State_hash deduplication happens at read time, not write time.
15. **Resume rebuilds history, doesn't re-execute.** Tool calls in flight at crash time are flagged `tool_call_uncertain` and surfaced to the user. Don't replay tool calls automatically.
16. **`request_capability` is a meta-tool.** It's auto-injected into every agent's tool list. When the model calls it, the runtime translates to `tool_missing` or `skill_missing` and routes through gap flow. Agents can decline `skill_load_requested` events but typically comply.
17. **Mode-variant skills filter sections.** A `skill.md` with `mode_variants: { LITE: { include_sections: ["quick"] }, ... }` has its body filtered by section header at load time. The full markdown is on disk; the model sees only the slice for the active mode.
18. **JSONLogic for triggers.** Programmatic skill triggers use a JSONLogic-style expression language. Operators allowed in v0.1: `var`, `==`, `!=`, `<`, `<=`, `>`, `>=`, `and`, `or`, `not`, `in`, `+`, `-`, `*`, `/`. Adding operators requires extending the evaluator; do not silently extend.
19. **Capability declarations are mandatory for generated artifacts.** Hand-authored artifacts can omit the `capabilities` block, but they default to Operator-tier-only loading. Generated artifacts (Phase 8) must declare capabilities; the validator rejects missing blocks.
20. **DCO sign-off is mandatory.** `git commit -s`. Without it, the commit is rejected by the CI hook (once configured at M1+).
21. **Common clippy pedantic+nursery traps in this project.** Compound entry covering recurring patterns from M01.B onward — appeared in every M02 stage retrospective:
    - `redundant_pub_crate` — use plain `pub` in private modules, not `pub(crate)`.
    - `derive_partial_eq_without_eq` — `serde_json::Value` containment requires `#[allow]` with one-line rationale; manual `Eq` is wrong because Value contains floats.
    - `unused_async` — cross-platform `cfg` variants where one branch awaits and another doesn't trip this; suppress per-fn when both variants must share signature.
    - `default_trait_access` — use `HashMap::default()` over `Default::default()`; explicit type avoids the lint.
    - `match_wildcard_for_single_variants` — bind explicitly (`Foo::Bar(_)`) rather than `_` when only one variant remains.
    - `cast_precision_loss` / `suboptimal_flops` — pin numeric ops to exact types (`f64`, `u64`) and document the precision claim inline.
    - `struct_excessive_bools` — collapse 3+ `bool` fields into a typed flag enum or struct.
    - `missing_const_for_fn` — `const fn` opt-in for pure constructors; clippy nags otherwise.
    - `unnecessary_literal_bound` / `doc_markdown` — backtick code identifiers in doc comments; align literal slice bounds.
    All five M02 stages tripped at least one of these; consolidating here so M03+ stages don't rediscover. (Source: M02-summary.md "Friction patterns that recurred".)
22. **Subprocess-spawning integration tests must derive paths from `std::env::current_exe()`.** Never hard-code `target/debug/<binary>` literals — `cargo llvm-cov --workspace` uses a distinct target dir, and the test binary's location depends on whether you ran `cargo test` (debug) or `cargo llvm-cov` (instrumented). Archetype: `crates/runtime-main/tests/drone_ipc_loopback.rs::drone_binary`. The pre-M02 `crates/runtime-drone/tests/integration*.rs` fixtures still use literals and need retrofitting before M03. (Source: M02.D Decisions.)
23. **Tauri 2.x E2E uses `tauri-driver` + WebdriverIO, not Playwright `_electron`.** Playwright's `_electron` API is Electron-specific and won't drive a Tauri 2.x WebView2 / WebKitGTK window. Per official docs (https://v2.tauri.app/develop/tests/webdriver/), only Linux + Windows are supported (macOS unsupported). M02 Stage E ships renderer-level Playwright against the Vite dev server with `@tauri-apps/api` mocked at the module level; full desktop-shell E2E is M03 carry-forward. (Source: M02.E friction r1.)
24. **ESLint 9 default is flat-config (`eslint.config.js`).** The legacy `.eslintrc.cjs` requires `ESLINT_USE_FLAT_CONFIG=false` to load, which most CI environments don't set. New projects should use `eslint.config.js` directly. (Source: M02.E friction r3.)
25. **Vite project root holds `index.html`.** The convention is `<root>/index.html` (not `<root>/src/index.html`); the script tag references `/src/main.tsx` (absolute path under root). Move the file or set `root: 'src'` in `vite.config.ts` if your layout differs. (Source: M02.E friction r9.)
26. **`serde(tag = "type")` requires struct-shape variants, not newtype-wrapped primitives.** `Provider(String)` errors at runtime because the tag-form serializer can't add `{ "type": "Provider" }` to a String; `Provider { message: String }` works because struct variants accept inline-tagging. Pattern: any error/event enum surfaced over Tauri IPC or HTTP wire format must use struct-shape variants. (Source: M02.E friction r7.)
27. **Vitest + React Testing Library: re-query DOM refs after each await.** Capturing a button's DOM ref before an awaited re-render leaves it stale even when `waitFor(toBeEnabled())` passes — `expect(button).toBeEnabled()` succeeds, then `userEvent.click(button)` fires on the stale ref. Sync on a visible-in-DOM indicator (`findByLabelText(...)` for the result, then re-query the button) before re-querying dependent UI. Archetype: `tests/unit/App.test.tsx::save_key_then_run_smoke_renders_event_list`. (Source: M02.E friction r8.)
28. **Bound test-fixture streams.** `futures::stream::repeat(...)` against a stateful pipeline (in-memory accumulator, channel, cancellation token) will OOM the harness — M02.D measured ~34 GB allocated before the test runner aborted. Use `take(N)` or `iter([...]).chain(pending())` to bound the stream while still triggering whatever timeout/cancellation behavior you're testing. Archetype: `tests/sdk_cancellation.rs::drops_mid_text_burst_no_panic`. (Source: M02.D friction r1.)
29. **`keyring` 3.x ships NO platform backend by default.** Bare `keyring = "3.6"` compiles fine but uses a stub that silently succeeds on writes and returns `NoEntry` on reads. The renderer sees "Save key ✓" then `setup_required` on read and the gap is invisible without log output. Always opt into platform features explicitly. Workspace pattern (M02 carry-forward fix): `keyring = { version = "3.6", default-features = false, features = ["apple-native", "windows-native", "sync-secret-service"] }`. Verify the dep tree includes `windows-sys` (Windows) / `core-foundation-sys` (macOS) / `secret-service` (Linux) — if those are absent from `Cargo.lock`, the stub backend is in use. Triggered the M02.E live-test "[object Object]" smoke-test failure. (Source: M02 Stage E live debugging, post-merge.)
30. **Tauri renderer's `catch(e)` gets a plain object when Rust throws a `serde(tag = "type")` enum.** `e instanceof Error` returns `false`; `String(e)` yields `"[object Object]"`; the actual error is hidden in `e.message` (or `e.type` for unit variants). Always unwrap structured errors via a typed helper — see `src/lib/ipc.ts::unwrapCmdError` for the canonical pattern (Error instance → graceful object → `e.type`/`e.message` → fallback). And ALWAYS `console.error(label, e)` before user-facing dispatch so DevTools shows the full structured object even when the renderer collapses it to a string. Pairs with #29: without the console log, the keyring-stub failure surfaced as "[object Object]" in the UI and zero diagnostic signal anywhere. (Source: M02 Stage E live debugging, post-merge.)
31. **Tauri main process binary needs `tracing_subscriber::fmt::init()` in `main()`.** `tracing::info!` / `tracing::error!` calls compile fine without it but emit to a null sink. Symptoms: `cargo run` / `npm run tauri dev` shows zero output from inside Tauri commands even when they error. Fix: add `init_tracing()` at the top of `main()` calling `tracing_subscriber::fmt::EnvFilter`-configured init. Default level should be `info` globally + `debug` for project crates; honor `RUST_LOG` env override. Per spec §13.5 Dev Logging. (Source: M02 Stage E live debugging, post-merge.)

32. **Hand-authored cross-stack integration examples in build prompts.** When a build prompt contains a code example for a cross-stack integration (Tauri WebDriver capabilities, OS-keychain backend feature flags, MCP JSON-RPC framing, OAuth flows, ESLint flat-config shape, etc.) authored from docs/memory rather than copied verbatim from a known-working upstream reference, the build agent will ship it verbatim and CI will fail with a setup-shaped error (capability mismatch, "no binary at...", silent stub backend, "Failed to match capabilities"). Bit M02 (PR #45 — keyring 3.x stub backend silently passing writes; gotcha #29) and M03 (PR #47 — tauri-driver capabilities, three iterations: 'edge'/'webkit2gtk' → 'wry' → omit). Mitigation: any cross-stack integration example in a build prompt MUST (a) be quoted verbatim from a working upstream example with a commit SHA referenced in a comment, OR (b) carry an explicit "verify against upstream reference X before shipping" guard naming the file/repo/issue to check. The discriminator is whether the example crosses a stack boundary (Rust ↔ TS, Rust ↔ OS, runtime ↔ third-party protocol) — pure-Rust and pure-React/TS examples don't trigger this rule. (Source: M02 PR #45 + M03 PR #47 hotfix iterations; STAGE-PROMPT-PROTOCOL.md §10 + §13.)

33. **snake_case schema discipline.** Schemas in `schemas/*.v1.json` use snake_case for all field names (`tokens_in`, `agent_id`, `duration_ms`); generated Rust types preserve snake_case via serde rename derives, and TS types are likewise generated with snake_case. Authoring stage prompts in camelCase before schema regeneration locks the convention is the recurring failure mode — sample bodies need to match the lock. Recurred across M03.A, M03.B, M03.D. Mitigation: regenerate types BEFORE writing any sample bodies, then reference the generated types as the contract. (Source: M03 Stage A–D retros; M03-summary.md "Friction patterns that recurred".)

34. **Prettier and `cargo fmt` are the first `verify_gates` step, not the last.** Each multi-file authoring stage's first round-1 friction was 1–9 files tripping prettier formatting. Run `prettier --write` (TS) / `cargo fmt --all` (Rust) BEFORE any downstream lint to catch mechanical formatting churn early — clippy/eslint findings on formatting-mangled code are noise that disappears once fmt runs. Recurred M03.B, M03.C, M03.D, M03.E. Mitigation: bake `fmt --write` into stage prompt `<execution_steps>` immediately after `implement` and before any lint or test. (Source: M03-summary.md "Friction patterns that recurred".)

35. **React Flow + happy-dom emits unavoidable `act()` warnings.** React Flow uses `useLayoutEffect` for size measurements; happy-dom doesn't fully simulate the browser layout pipeline, so React schedules updates outside RTL's `act()` boundary. Tests still pass and gates stay green; the warnings come from React Flow internals (`MarkerDefinitions`, `EdgeRendererComponent`, `Pane`, `ZoomPane`). Don't try to wrap in `act()` — it doesn't work; the source is below user code. Mitigation: ignore for components using React Flow under happy-dom; if signal-to-noise becomes a problem, filter via console-error mocks scoped to those components. (Source: M03.B + M03.C retros.)

36. **For renderer components without yet-defined event variants, pass populated state directly into `<NodeComponent>` rather than dispatching events through the store.** M03 shipped GapNode, HITLNode, PlanNode, TaskNode, VerifyNode, HookNode renderers before their corresponding event variants exist (M04+/M05 wires them). Testing them via `applyEvent('plan_created', {...})` would force inventing event shapes pre-spec; testing via `<PlanNode data={syntheticPlanState} />` locks the renderer's contract without spec churn. Pattern locked M03.C; durable through M03.D. (Source: M03.C–D retros; M03-summary.md.)

37. **Don't add explicit type assertions after TypeScript narrows discriminated unions.** After `node.type === 'plan'`, TS already narrows `node` to the `PlanNode` branch — adding `as PlanNode` is dead weight and trips `@typescript-eslint/no-unnecessary-type-assertion`. Surfaced as eslint errors in M03.B and M03.D. Mitigation: write code that lets the narrow do its work; if TS doesn't narrow as expected, the discriminant tag or interface shape is wrong, not the missing assertion. (Source: M03-summary.md.)

38. **WebdriverIO v9 changed `await $('selector')` semantics — chainables don't extend `PromiseLike`.** The v8 pattern `const el = await $('button'); await el.click();` silently breaks under v9 because `ChainablePromiseElement` no longer satisfies `PromiseLike`. Call methods directly on `$()` and `$$()` chainables without intermediate await: `await $('button').click()`. Add `import type {} from 'webdriverio'` as a side-effect import to bring in `WebdriverIO.Browser` augmentations. Bit M03.F with three round-trips during typing setup. (Source: M03.F retro; M03-summary.md.)

39. **`npm audit` failures from transitive deps fix via `package.json` `overrides`, not by swapping the dependent.** When a new devDep brings in a patched-but-old transitive that triggers `npm audit --audit-level=high`, force the patched version via `overrides.<package>: "^<patched-version>"` rather than dropping the dependent or pinning the dev dep to an older release. M03.F example: `overrides.serialize-javascript: "^7.0.5"` resolved 3 high-severity findings from `@wdio/mocha-framework`'s transitive tree without touching the framework itself. (Source: M03.F retro.)

40. **The standard eslint override glob `**/*.config.{ts,js}` matches `vite.config.ts` but NOT `wdio.conf.ts`.** Tauri 2.x WebDriver convention names the config file `wdio.conf.ts` (per upstream docs), and the `.conf.ts` extension falls outside the `.config.ts` glob. Configure an explicit named override (`'wdio.conf.ts'`) plus `parserOptions.projectService: false` for that file; otherwise `projectService` cannot find it in tsconfig includes and eslint errors with "file not in tsconfig". (Source: M03.F retro.)

41. **Phase doc claims about codebase reality must be grep-verified at authoring time, not derived from spec/retros alone.** When a milestone Phase doc names file paths, function names, struct names, IPC command variants, integration points, or any other claim about the codebase's current state, every claim must be verified via `grep` / `Test-Path` / file inspection at *authoring time* — before the doc ships. Trusting `agent-runtime-spec.md` text or `docs/gap-analysis.md` carry-forward chains as proxies for codebase state is a named failure mode: spec describes intent; gap-analysis is append-only and items "carry forward" but don't auto-close when work happens incidentally; both can drift from current code reality. Bit M04 (Phase doc claimed `event_translation.rs`, `prompt_template.rs`, `WriteSignal` IPC variant, `vdr.rs` in `runtime-main`, `RevertToSnapshot` as new — none matched reality; 8 of 11 plan/task events already existed; structurally misaligned across Stages B/D/F). Different from #32: that gotcha is about *third-party* examples (Tauri docs, etc.); this gotcha is about claims about *your own* codebase. Mitigation: at authoring time, for every X.2 file path and every X.3 symbol/function/struct/path claim, run the verification command and confirm reality before shipping the Phase doc. The "Pre-existing legacy file inventory" section in `docs/build-prompts/TEMPLATE.md` is *verified*, not *descriptive* — see TEMPLATE.md addition + STAGE-PROMPT-PROTOCOL.md §10 v1.3 hardening rule. (Source: M04.A2 friction event Sev-4 + M04 codebase audit post-A2.)

42. **Origin is a partial view of project state when CLAUDE.md §8 forbids per-stage pushes.** Any orchestration session reading `origin` alone may infer "stage X unexecuted" when in fact the build machine has the work locally. Different discriminator from #41: #41 is *grep-verify-claims* (verify against the codebase); this rule is *verify against THE RIGHT codebase* — origin is one view, the build machine's local milestone branch is another, and they diverge whenever §8's "no push between stages" rule is in effect. Mitigation: phase-doc edits > 50 lines require the user to paste `git log --oneline main..HEAD` from the build machine for the active milestone branch BEFORE the edit; if the output contains commits with retrospectives in `docs/build-prompts/retrospectives/M[NN].<X>-retrospective.md`, those stages are DONE regardless of what origin shows. Stronger backstop: every stage end surface includes git log + retrospective file listing as a standard item (CLAUDE.md §19 rule 7) — when the user pastes the surface to any downstream orchestration session, that session sees real project state, not origin's partial view. Bit M04 (PR #53 rewrote the M04 phase doc against "A1+A2 unexecuted per origin" while the build machine had `c5fe035` + `2b0e8d2` with full retros; 4+ hours of work merged on a false premise; reverted via PR #54). (Source: M04 PR #53 → revert PR #54; CLAUDE.md §8 phase-doc-edit pre-flight + §19 rule 7 + STAGE-PROMPT-PROTOCOL.md §10 v1.4 hardening.)

43. **typify 0.6.2 panics on root-`oneOf` with validated inline strings.** Validated string subschemas (`{ "type": "string", "minLength": 1 }`) in event-variant `oneOf` or top-level `oneOf` schemas trip typify's name-derivation at `type_entry.rs:511` when no `title` is available — the variant generator can't name the newtype. Extract validated strings to `$defs/<Name>` with a `title` so typify can name the validation newtype. Recurred M04.A1 (`error.v1.json` `message`), M04.B (`ApprovedBy` enum), M04.D (`HookCategoryRef` + `OnFailureRef` + `RailPolicy`). (Source: M04.A1 friction Sev 3.)

44. **New generated TS file → both `.prettierignore` AND `eslint.config.js` `ignores`.** Any new file at `src/types/*.ts` produced by `cargo xtask regenerate-types` must be added to BOTH ignore lists — match the `agent_event.ts` + `error.ts` + `plan.ts` + `task.ts` + `hitl.ts` + `budget.ts` precedent. Easy to miss without reading the existing ignore lists; prettier's path-dependent ignore behavior produces opaque CI failures (`prettier --file-info <path>` is the canonical debug). (Source: M04.A1 retro.)

45. **Wire-equal-but-Rust-shape-different schema migration playbook.** When a typify-generated type replaces a hand-rolled type, the JSON wire format stays the same but Rust callsite shape differs (e.g., tuple-variant `CmdError::Provider(ErrorMessage)` vs struct-variant `CmdError::Provider { message: String }`). Migration pattern: add inherent helper constructors + `Display` + `std::error::Error` impls in a separate module within the same crate (orphan rule satisfied). Place `From<ForeignType>` impls in the crate that owns the source type. ~17 callsites for a 5-variant enum is typical. Forward-applicable to M05+/M06 schema reshapes. (Source: M04.A2 friction Sev 3 + `crates/runtime-core/src/cmd_error_ext.rs`.)

46. **`futures::stream::unfold` panics on poll-after-Ready(None).** Streams built on `futures::stream::unfold` panic with "Unfold must not be polled after it returned `Poll::Ready(None)`" when drained-then-polled. When draining via `.next()` until None, track termination state explicitly — do not poll again on the same stream. Forward-applicable to any test draining drone-IPC `events()` stream or other unfold-based streams. (Source: M04.A2 friction Sev 2.)

47. **Windows file lock on subprocess binaries persists briefly after SIGKILL.** Even with `tokio::process::Command::kill_on_drop(true)`, Windows takes a few seconds to release the file lock after a child process exits. If `cargo test` reports `Access is denied (os error 5)` on a `runtime-*` binary, run `Stop-Process -Name <bin> -Force` from PowerShell and retry. Forward-applicable to any test that spawns runtime-drone or future runtime-* subprocesses. (Source: M04.A2 friction Sev 2.)

48. **clippy `too_long_first_doc_paragraph` fires on module `//!` docs with 4+ line first paragraphs.** Keep the first paragraph short (one sentence, ≤80 chars) followed by a section heading or blank line before continuation. The lint reports without source spans, making it hard to localize — use `cargo clippy --message-format=json | grep file_name` to find the offending module. OR suppress at module level: `#![allow(clippy::too_long_first_doc_paragraph, reason = "...")]`. Recurred M04.B, M04.D, M04.E, M04.F. (Source: M04.B + M04.F friction Sev 2.)

49. **Per-package coverage gates need package-internal unit tests, not just integration tests.** `cargo llvm-cov --package <crate> --fail-under-lines 95` counts only tests living in the same package's `tests/` directory or `#[cfg(test)] mod tests`. Safety primitives exercised primarily via cross-package integration tests (e.g., `runtime-main` tests calling `runtime-drone` via subprocess IPC) need package-internal unit tests too — otherwise per-package coverage drops below the gate even though the wire path is fully exercised. Pattern: write the integration test for the wire path; write package-internal unit tests for the same logic to satisfy the per-package gate. Forward-applicable to M05 (capability enforcer), M06 (MCP client). (Source: M04.B friction Sev 3.)

50. **`snapshot::write` uses second-granularity timestamps; tests asserting latest-by-timestamp need explicit timestamps.** Production `snapshot::write` truncates timestamps to seconds. Tests that assert latest-by-timestamp ordering between rapid back-to-back writes must seed explicit timestamps via raw INSERT (or sleep 1+ second between writes — slow). Forward-applicable to any test of latest-row ordering with timestamp keys. (Source: M04.B friction Sev 1.)

51. **Schema-derived enums match `$defs` values exactly; pre-flight grep before authoring sample bodies.** Generated enums like `ApprovedBy = 'user' | 'auto'` come from `schemas/*.v1.json` `$defs`. Phase doc sample bodies or test fixtures using enum-member literals must match the actual schema values (e.g., `'human'` is NOT assignable to `ApprovedBy`). Pre-flight grep the schema `$defs` before authoring sample bodies. Recurred M04.C (`ApprovedBy`) and M04.F (audit-baseline event names). (Source: M04.C friction Sev 1.)

52. **GraphCanvas-style layout memos must cache positions-by-ID, not full node arrays.** A `useMemo([nodes.length, edges.length])` cached layout silently drops data updates that don't change array length — node-status transitions (e.g., `awaiting_approval → in_progress`) keep array length constant but change the data each node carries. Fix: cache positions in a `Map<string, {x, y}>` keyed by node ID, then merge fresh nodes from the store with positions on every render. M04.C plan FSM transitions surfaced the latent M03.D bug. Forward-applicable to any stage adding visually-driven node-data updates. (Source: M04.C friction Sev 3.)

53. **Vite dep-optimizer cold-start re-optimizes on first request to new code paths within a Playwright spec.** When adding a new component reachable from a NEW spec file (no other spec exercises its imports), the spec's first `page.goto` pays a Vite dep-optimization pass on top of the smoke baseline (~30–60s). Bump the describe-block timeout to match `webServer.timeout` (90s) via `test.describe.configure({ timeout: 90_000 })` until the dep is added to `vite.config.ts` `optimizeDeps.include` if hot. Surfaced M04.C `plan_approval.spec.ts`. (Source: M04.C friction Sev 3 timing.)

54. **Renderer-level Playwright state injection via `window.__graphStore` affordance.** Module mocking across the `@tauri-apps/api` ESM boundary doesn't work in Playwright (only in Vitest's `vi.mock`). Tests that drive renderer state without a live Tauri runtime should expose the Zustand store on `window.__graphStore` (App.tsx unconditional affordance — the store carries no secrets) and use `page.evaluate(() => window.__graphStore.getState().applyEvent(...))` to inject state. Click→invoke linkage stays in Vitest; state-driven surface tests stay in Playwright. Pairs with the `e2e-tauri-driver`-disabled-status carry-forward. (Source: M04.C retro.)

55. **Rustdoc intra-doc links resolve at module-end scope; lint anchors at doc-location.** Rustdoc resolves `` [`HitlPolicy`] ``, `` [`Desktop::with_dispatcher`] ``, and similar intra-doc links at the END of the module's declaration scope, but the lint that fires on unresolved links is anchored at the doc-location (BEFORE the submodule / cross-module item is declared). Use plain backticks for submodule + cross-module + cross-crate refs in module-level `//!` docs to avoid `cargo doc -D missing_docs` errors. Recurred M04.D (`hooks/mod.rs`), M04.E (`hitl/*`), M04.F (`budget/mod.rs` + `recovery/mod.rs`). (Source: M04.D + E + F friction Sev 1–2.)

56. **Windows-local `cargo llvm-cov` may flake on subprocess-spawning tests under default parallelism.** Instrumented child binaries take longer to bind named pipes than the test's default retry deadline; CI Linux is faster and not affected. Mitigations: `cargo llvm-cov ... -- --test-threads=1` (serializes) + `--skip plan_recovery --skip drone_reconnect --skip recovery_lifecycle` (filters out the worst offenders). Pairs with gotcha #22 (subprocess test path derivation). (Source: M04.D + E + F friction Sev 3.)

57. **Top-level `$ref` in JSON Schema breaks `json-schema-to-typescript`.** The tool errors at `parseNonLiteral` and requires a concrete `type: object` (or similar) at the schema root. typify (Rust side) supports both forms; TS codegen requires concrete-type-at-root. Inline the root definition's fields at the schema root and keep `$defs` for shared sub-types. Recurred M04.E (`hitl.v1.json` initial draft); M04.F (`budget.v1.json`) followed the corrected pattern from the start. (Source: M04.E friction Sev 2.)

58. **Vitest fake-timers + `@testing-library/user-event` don't compose cleanly.** When `vi.useFakeTimers()` is active, `userEvent.setup({ advanceTimers: vi.advanceTimersByTime }).click(...)` may time out at the test's default 5s budget — the user-event library's internal `wait` calls don't reliably progress fake timers. Use `fireEvent.click(...)` + `await waitFor(...)` directly for click-driven assertions when fake timers are needed elsewhere in the same test file; isolate fake timers to timer-specific tests. (Source: M04.E HITLToast friction Sev 2.)

59. **Component-internal pure helpers: export via `_testing` named export.** Pure helper functions inside React component modules often need direct unit-test coverage but should not pollute the public component API. Pattern: `export const _testing = { helperFn };` at the component module's end; tests import the named export. The `_`-prefix marks the export as internal-test-only and is recognized by the eslint `no-unused-vars` allowed-unused-prefix rule. Proven across M04.E (HITLPanel/Modal/Toast) + M04.F (BudgetHeaderBar/RecoveryDialog/UncertaintyPrompt). (Source: M04.E retro.)

60. **`fireEvent.submit(form)` for `type='submit'` button in vitest, not `fireEvent.click(button)`.** jsdom does not propagate `click` on a `type='submit'` button to the form's `onSubmit` handler. Use `fireEvent.submit(form)` to drive the form submit cycle in vitest. `userEvent.click` DOES propagate (it dispatches the full click→submit chain) but is slower in test runs. (Source: M04.F BudgetHeaderBar friction Sev 2.)

61. **`cargo test --skip <prefix>` is substring match, not file-name filter.** When integration tests in `tests/<file>.rs` share a test-function-name PREFIX with lib unit tests in `#[cfg(test)] mod tests`, `--skip <prefix>` filters BOTH sets — package-internal coverage drops on lib unit tests too. Rename integration tests to have a distinct prefix (e.g., `recovery_lifecycle_*` for integration tests when lib tests are `recover_session_*`). (Source: M04.F friction Sev 2.)

62. **`#[allow(<lint>, reason = "...")]` always includes a 1-sentence rationale.** When suppressing a clippy lint, always pass the `reason` parameter with a 1-sentence justification. Examples: `#[allow(clippy::cast_precision_loss, reason = "u64 token counts up to 2^53 are exact in f64")]`, `#[allow(clippy::too_many_lines, reason = "integration test with 5 phases")]`. Pairs with the `#![allow(clippy::too_long_first_doc_paragraph, reason = "...")]` module-level pattern (gotcha #48). Forward-applicable to any cast-heavy or lint-noisy module. (Source: M04.F retro.)

63. **React form-submit handlers can capture stale closures when reading `useState` inside `async`.** When a form's `onSubmit` reads from `useState` state inside an async function, the closure captures state at render time. If the user changes input then submits in the same render, the handler may see stale state. Defensive pattern: pass the current input value explicitly into the handler from the form's submit event (e.g., `onSubmit={(e) => { e.preventDefault(); void saveGlobal(globalCap); }}`) rather than relying on the closure. (Source: M04.F BudgetHeaderBar settings friction Sev 2.)

64. **`cargo clippy --fix --allow-dirty` as the SECOND mechanical first-pass alongside `cargo fmt --all`.** Corollary to gotcha #34: clippy's `--fix --allow-dirty` auto-resolves many lints from gotcha #21's cluster (redundant_pub_crate, default_trait_access, missing_const_for_fn, etc.) without manual edits. Run both as the first verify_gates step: `cargo fmt --all && cargo clippy --fix --allow-dirty --workspace --all-targets`. The remaining clippy findings after fmt+fix are real lint debt requiring judgment. (Source: M04.D corollary to #34.)

65. **Pin `rust-toolchain.toml` to an explicit version; floating `stable` causes CI-vs-local skew.** Floating `channel = "stable"` means local build agent + CI fetch whatever stable is current at their respective run times. When CI updates to a new stable ahead of the build machine (or vice versa), new rustfmt heuristics and clippy lints fire on existing code that passed pre-bump. Recurred M04 PR #58 across multiple hotfixes (rustfmt drift, then `clippy::case_sensitive_file_extension_comparisons` on Rust 1.95). Mitigation: pin to an explicit version (e.g., `channel = "1.95.0"`); bump deliberately when ready to handle new lints. Industry-standard practice for Rust projects per https://rust-lang.github.io/rustup/overrides.html. (Source: M04 PR #58 hotfix commits `4214edc` + `78ddbad`.)

66. **Tests-pass-but-contract-fails is a distinct bug class from tests-fail.** A unit test that calls `tokenScale(50000) → 1.5` is GREEN but tells you nothing about whether `AgentNode` renders a node at scale 1.5 — the test never asserts the DOM transform. A unit test that confirms `colorClass('warn') → 'budget-bar__bar--warn'` is GREEN but tells you nothing about whether the bar is actually amber — the CSS rule for that class might not exist. The pattern: implementation-tests verify "the code does what I wrote"; contract-tests verify "the code does what the spec said." Five M04 bugs exhibited this exactly. Mitigation: Stage V verifier (ADR-0008) catches this class structurally via the Behavior pass (run the thing + observe). For unit tests specifically, prefer behavior assertions that mirror user-observable outcomes: assert DOM computed-style (not just className); assert two sequential API calls work (not just one); assert visual differs between two inputs (not just that the formula returns expected numbers). (Source: M04 IRL test findings ×5; meta-gotcha graduated alongside ADR-0008.)

67. **Component rendered in DOM ≠ CSS for the component exists.** A React component sets a className like `budget-bar__bar--warn`; the JSX renders correctly; tests verifying the className output pass — but `styles.css` has zero rules for that class. The component renders without any color styling; the user sees no visual change. The DOM-level test that catches this: `const root = render(<X/>); expect(getComputedStyle(root).backgroundColor).not.toBe('rgba(0, 0, 0, 0)')`. OR a `readFileSync(stylesPath).includes('.budget-bar__bar--warn')` static check. Recurred M04.F BudgetHeaderBar (5 status classes missing). (Source: M04 IRL BUD-01 + PR #64 regression test `every_status_class_has_a_corresponding_CSS_rule_in_styles_css`.)

68. **Component reads wrong field from projection — schema field unused while a similarly-named field is read.** `AgentNodeData` has `tokensIn`, `tokensOut`, AND `tokensTotal`. The store's `applyEvent` writes `tokensTotal` from `agent_complete.tokens_total`. The renderer destructures `tokensIn + tokensOut` (which are NOT populated from that event). Static analysis passes — both field names are valid. Unit test asserting `tokenScale(50000) → 1.5` passes — the math function is fine. Only a DOM-render with two different `tokensTotal` values catches the wrong-field bug. Mitigation: when a projector and consumer touch the same data shape, verify the consumer reads ALL the fields the projector writes (Stage V Wire pass step 5). Recurred M04 AgentNode. (Source: M04 IRL LG-03 + PR #64 regression test `reads_tokensTotal_for_visual_scale_not_tokensIn_plus_tokensOut`.)

69. **IPC primitives need multi-call invariant tests, not just single-call coverage.** A `query_session_db` call that succeeds once tells you the wire works — but doesn't tell you the second call works. M04's drone IPC `take_event_stream` moved the reader into a per-call stream that was dropped at return; first call succeeded, every subsequent call surfaced `"event stream ended without response"`. Coverage at ≥95% on the IPC module didn't catch it because every line was exercised by single-call tests. Mitigation: for every IPC method / Tauri command / shared-state API, add a sequential-call test asserting BOTH calls succeed with distinct expected outcomes. Stage V Multi-call pass enforces this structurally. (Source: M04 IRL SQL-02 + PR #64 regression test `query_session_db_succeeds_twice_in_sequence`.)

70. **Viewport / window-size assumptions in CSS don't show up in unit tests.** `main { max-width: 720px }` was a sensible default for the M02 pre-graph layout but became hostile to M03's graph canvas at typical Tauri window sizes (≥1280px). No unit test would catch this — Vitest+jsdom doesn't simulate window width. Mitigation: for any layout-level CSS rule (margins, max-widths, viewport-relative sizing), verify in real Tauri window OR Playwright at multiple viewport configs OR via Stage V Behavior pass with explicit window-size sweeps. (Source: M04 IRL — "View screen too small — needs to fill width of window".)

71. **Schema field "missing" can render as blank string, not "missing" indicator.** When a schema-generated event type has no `title` field but the projection unconditionally writes `title: ''`, downstream components render the empty string. `truncate('', 30) === ''` — no compile error, no test failure, just blank label. M04 TaskNodes from `task_started` events showed up "untitled" in the inspector + canvas. Mitigation: components rendering a label MUST fall back to a non-empty placeholder when the source field is absent or empty (`title || \`task ${id.slice(0,8)}\``). For projections where the schema legitimately has no display field, the renderer's fallback is the contract — not the projection's default empty-string. (Source: M04 IRL LG-02 + PR #64 fix in `TaskNode.tsx`.)

72. **Tokio `io::duplex` EOF propagation requires dropping the WRITER half on the producing side; keeping the READER half alive doesn't propagate EOF either direction.** When testing reader-side `next()` returns `None` after the peer closes, drop the peer's WRITER half (`b_wr`) — dropping the peer's READER half (`b_rd`) does NOT cause `a_rd.next().await` to return `None` reliably while `_peer_rd` is held. Bit me in PR #64's `next_event_returns_consecutive_events_without_consuming_reader` test (hung CI for 1 hour). Mitigation: in any duplex-based test asserting EOF on one side, drop the peer's writer half explicitly AND don't bind the peer's reader half (or drop it before the EOF assertion). For EOF behavior more broadly, prefer `tokio::test(start_paused = true)` + paused-time machinery (the existing `*_stream_close_*_not_hang` tests in `client.rs`) so a missed EOF surfaces as a deterministic timeout rather than a hang. (Source: M04 IRL fix PR #64 commit `c3c3004`.)

73. **typify `oneOf`-generated enums with non-`Copy` variant payloads don't derive `PartialEq`/`Eq`.** Generated `oneOf`-based enums whose variants wrap a newtype that itself doesn't derive `Eq` (because the wrapped value contains a float, a `serde_json::Value`, or another non-`Eq` payload) silently lose the auto-derived `PartialEq` / `Eq` impls. Symptom: a typify-generated `SandboxRequest::Check(CapabilityDeclaration)` compiles fine but `request_a == request_b` fails to compile with "no implementation for `SandboxRequest == SandboxRequest`". M05.B first observed it on `CapabilityScope`; M05.C1 confirmed recurrence on `SandboxRequest` (which wraps `CapabilityDeclaration::scope`). Forward-applicable to any future wire format nesting typify-derived `oneOf` types whose variants wrap non-`Copy` newtypes. Mitigation: compare via serde round-trip (re-serialize the deserialized value and check string equality) rather than `==`; or hand-write `PartialEq` for the small subset of variants that actually need equality comparison in tests. Do NOT attempt to derive `Eq` manually on the typify-generated type — the next `cargo xtask regenerate-types` run will overwrite the manual derive. (Source: M05.B + C1 retros; M05-summary.md "Friction patterns that recurred"; v1.6 graduation from "reserved" #73.)

74. **`cfg(target_os = ...)` blocks not native to the build machine require docs-driven API derivation — read `docs.rs` source, do NOT guess the API.** CI is the first compile for cross-platform `cfg`-gated code; the local `cargo check` on the build machine compiles the *other* platform's branch (the empty / cfg-disabled side). No type-system feedback fires locally for the gated code. The first compile happens when the PR pushes and CI's matrix runner enters that branch. Mitigation: when authoring `cfg(target_os = "linux")` or `cfg(windows)` code that uses a non-trivial third-party API, treat `docs.rs/<crate>/<version>/` as the authoritative source-of-truth — fetch the actual function signatures, return types, and trait bounds before writing the call. Verbatim cite from docs.rs in the implementation surface so the PR review can match what was read. Cross-compile setup (e.g. `cross` or platform-specific Docker containers) is the structural fix but adds toolchain complexity; for a single-purpose isolation crate like `runtime-sandbox`, the CI-first-compile loop is acceptable IFF the docs-driven derivation is rigorous. Watch for: method renames between major versions (`new` vs `new_filter`), return-type changes from a bare variant to a wrapper type (`AccessFs` vs `BitFlags<AccessFs>`), and `const fn` variants of non-const methods (`intersection` vs `intersection_c`) — all three bit M05.C2 simultaneously. (Source: M05.C2 PR #70 CI failure — `landlock 0.4.4` + `libseccomp 0.3.0` API drift surfaced only on Linux CI; fixed in the M05.C2 post-merge hotfix.)

75. **Zustand v5 derived-array selectors require `useShallow` from `zustand/react/shallow`.** When a renderer component selects a derived array (filter / map / find over a slice of store state) without wrapping the selector with `useShallow`, Zustand v5's default referential-equality check returns a NEW array reference on every store update — even when the array's contents are identical — triggering an infinite re-render loop and `Maximum update depth exceeded` console errors. Pattern: `const gaps = useGraphStore((s) => s.nodes.filter(n => n.type === 'gap'))` is the wrong shape; `import { useShallow } from 'zustand/react/shallow'; const gaps = useGraphStore(useShallow((s) => s.nodes.filter(n => n.type === 'gap')))` is the right one. Bit M05.F (CapabilityBadge + GapPanel tests). Forward-applicable to any future renderer stage selecting derived arrays from the store. Mitigation: pair every `filter` / `map` / `find` (or other array-producing operator) inside a `useStore(...)` callback with `useShallow`. The v1.6 `<zustand_selector_audit>` slot encodes this at phase-doc-authoring time. (Source: M05.F retro; M05-summary.md.) **Clarification (M08.6.E):** this applies to DERIVED arrays/objects — a `filter`/`map`/`find` producing a NEW reference each call. Selecting a *stable reference already in the store* — e.g. `useBuilderStore((s) => s.framework)` returning the framework object itself — does NOT need `useShallow` (the reference only changes when that slot is actually replaced).

76. **`windows-sys` feature flags gate function bindings by ALL parameter type modules, not just the function's home module.** When using `windows-sys` (the no-std Windows FFI crate) to bind a Windows API function, the feature flags required to compile the binding are the union of the feature flags for the function's home module AND every module that defines a type in the function's signature. Example: `CreateJobObjectW` lives in `Win32_System_JobObjects` but its signature uses `SECURITY_ATTRIBUTES` from `Win32_Security` — the `Cargo.toml` features list must include BOTH `Win32_System_JobObjects` AND `Win32_Security`, even though the call site only mentions the function name. Symptom: cryptic "unresolved import" / "type not found" errors that mention a module the call site never references. Mitigation: when binding a new windows-sys function, read its signature on `docs.rs/windows-sys/<version>/` and add ALL the modules its parameter types reference to the `Cargo.toml` features list before writing the call. Pair with WEBCHECK discipline (gotcha #32) for any unfamiliar Windows API. The v1.6 `<dependency_audit_check>` extension's `<feature_interdependency>` child encodes this at phase-doc-authoring time. (Source: M05.C2 retro — `CreateJobObjectW` needed `Win32_Security` for `SECURITY_ATTRIBUTES`; M05-summary.md.)

77. **`tokio::io::duplex` buffer must be smaller than the payload to surface write-failure error branches.** When testing peer-write-failure error mapping in a codec (e.g., the codec's "what happens when the peer drops the writer half mid-response" branch), `tokio::io::duplex(2048)` (the typical 2KB default) absorbs typical response payloads — the underlying-stream error never surfaces because the codec's response fits entirely in the duplex's in-memory buffer before the peer-side error materializes. Symptom: a test named `handle_connection_returns_false_when_response_write_fails` passes its assertions but coverage data shows the write-failure branch is unhit. Mitigation: reduce the duplex buffer to ≤8 bytes (smaller than the smallest valid response) so writes block immediately and the peer-drop error propagates through the codec's error path. Forward-applicable to any future codec-test pattern: buffer capacity MUST be smaller than the largest payload that would otherwise mask the error. (Source: M05.C2 retro — uncovered branch in `handle_connection_returns_false_when_response_write_fails` until duplex was sized to 8 bytes; M05-summary.md.)

78. **`json-schema-to-typescript` cross-schema `$ref` relative-path resolver fails — use mirror string descriptions per the M04.D `CapabilityKindRef` archetype.** When an event-variant field references a `$def` in a sibling schema (e.g., `schemas/event.v1.json`'s `agent_spawned.narrowed_from` referencing `schemas/capability.v1.json#/$defs/CapabilityDeclaration`), `json-schema-to-typescript`'s resolver fails on the relative path even though `typify` (Rust side) handles it cleanly. Symptom: TS-side codegen errors at the cross-schema boundary; Rust side compiles fine — asymmetric failure mode. Pattern (M04.D precedent): mirror the typed structure as a stringified description in the consuming schema (e.g., `"kind:resource:scope:side_effect_class"`), extract the validated string to a `$def` with a title per gotcha #43, and parse on the rare consumer that needs structured access. Bit M06.A (`narrowed_from`). Forward-applicable to any future cross-schema field reference. Mitigation: at schema-authoring time, prefer mirror-string descriptions to cross-schema `$ref` for the TS-side asymmetry; reach for cross-schema `$ref` only when both Rust + TS codegen confirm acceptance. (Source: M06.A retro Product axis schema deviation; M04.D `CapabilityKindRef` mirror precedent.)

79. **Self-deactivating `#[expect(clippy::unused_async, reason = "...")]` for TDD red-phase stub async methods — prefer `expect` over `allow`.** When writing TDD red-phase stub async methods that will become real `await`-using methods in the green phase, use `#[expect(clippy::unused_async, reason = "M0X.Y green phase awaits ...")]` instead of `#[allow(clippy::unused_async)]`. The `expect` attribute is self-deactivating: once green-phase impl adds real awaits, the lint stops applying and `expect` itself warns "this lint expectation is unfulfilled" — `cargo clippy --fix` then removes the now-unnecessary attribute automatically. Net effect: zero manual cleanup; the green-phase commit's clippy-fix step (canonical gate-ordering step 2) does it. Forward-applicable to ALL TDD red-phase work where a stub currently violates a lint that it will not violate after impl. Known limitations (per rust-clippy issues #12998 / #13466 / #14704 / #16488): the self-fire does NOT trigger when the async fn is in a trait, is passed as a value, or in some test-context configurations — in those cases `expect` produces a permanent "unfulfilled" warning instead of self-cleaning. Diagnostic: an `unfulfilled lint expectation` warning that persists after green-phase impl means the lint never fired; fall back to `allow` for that specific case. Works cleanly for standalone async fn bodies that will gain `.await` in green phase (the common TDD-stub case). (Source: M06.C retro surprise-events log + user-directed web research on rust-clippy expect-attribute foot-guns.)

80. **Non-idempotent migrations (`ALTER` / `RENAME` / `DROP`) make `init_idempotent` tests meaningful for the first time — verify the migration runner's version-skip is the SOLE idempotency guarantor before landing one.** While every migration is `CREATE TABLE IF NOT EXISTS` (the pre-M06.C drone state), re-init is idempotent at the SQL layer regardless of the runner's `_migrations` version-skip — the `init_idempotent` test "passes" trivially (a temporarily-vacuous test; gotcha #66 family). The first non-idempotent migration (M06.C's 002: `RENAME COLUMN` + `ADD COLUMN`) makes the version-skip LOAD-BEARING: re-running 002 raw would error ("duplicate column" / "no such column"); only the runner's "skip migrations whose version ≤ current" logic prevents it. Pattern: before any non-idempotent migration lands, (a) confirm the migration runner's version-skip is the only thing standing between re-init and a hard error, and (b) add a test that specifically asserts re-init does NOT re-apply the non-idempotent migration (not just "re-init doesn't crash"). Applies forward to M07+ schemas (registry imports, framework caches, any `ALTER`/`RENAME`/`DROP`). Pairs with the "latent-meaningful test" discipline — a red-phase trivially-passing test that becomes contract-exercising post-impl must have its meaningfulness verified in the green phase, not assumed. (Source: M06.C retro — `init_idempotent` meaningfulness confirmed post-002 per user direction.)

81. **`cargo llvm-cov` merges `.profraw` across runs — run `cargo llvm-cov clean` before a gate measurement if any prior `llvm-cov` invocation ran this session.** `cargo llvm-cov` accumulates profile data across invocations within a session; a prior run (ESPECIALLY a failing one that exited mid-suite) leaves stale `.profraw` that merges into the next measurement and produces phantom numbers. Symptom: a coverage gate reports a drop that no code change explains (M06.C: false 92.07% reported for `runtime-mcp`; true value 96.64% after a clean re-measure). Mitigation: `cargo llvm-cov clean --workspace` before the authoritative gate measurement whenever a prior `llvm-cov` ran this session (failing runs especially). One line; ~10 min saved per occurrence. Now encoded in CLAUDE.md §6 canonical gate-ordering (step 4 preamble). Forward-applicable to every coverage gate run in a multi-run session. (Source: M06.C retro — ~10 min chasing a phantom coverage drop from stale merged profile data.)

82. **`<phase_doc_inventory_audit verified="true">` proves a symbol EXISTS — not that it is reachable / correctly-shaped / ADR-current / exercised in the assembled app.** M06 hit this gap at four layers: cross-crate-dep construction (D — symbol exists but no shell ctor inputs reach it), renderer/IPC wire shape (E — `currentMcpServers` was `Record<…>` not the phase-doc-assumed `Map`+`.tools`; `mcp_test_connection` took `{name}` not `{config}` — five drifts), Wire-trace-vs-ADR currency (V Decision 6 — trace #6 assumed `McpClient` drives re-resolution; ADR-0010 had moved it to `McpDispatcher`), and assembled-exercise (M06.5 — the isolated component passed its unit test while the assembled composition was broken, AND the phase-doc root-cause diagnosis was itself incomplete). Existence ≠ reachability ≠ shape ≠ ADR-currency ≠ assembled-exercise. Mitigation: at phase-doc-authoring time use the v1.8 slots — `<construction_reachability_check>` (trace the construction graph for every "inject X into Y"), `<wire_signature_audit>` + `<phase_doc_inventory_audit shape=…>` (pin IPC params + store-slot types to the actual wire), `<wire_trace_vs_adr_reconcile>` (reconcile every Wire trace against accepted ADRs) — and at acceptance time, the CLAUDE.md §6 assembled-app-regression mandate (the regression test exercises the real composition; the phase-doc root cause is a falsifiable hypothesis it must disprove, not a premise). Pairs with gotcha #41 (grep-verify codebase claims) + #66 (tests-pass-but-contract-fails) — this is the authoring-time generalization. (Source: M06 gap-analysis lines 1897/1901 + M06.V Decisions 6/7 + M06.5-summary "To Cycle 2"; graduated at M06.6 protocol v1.8.)

83. **`tauri-driver` has no `--version` flag — use `--help` to verify the install.** Running `tauri-driver --version` returns `Error: unused arguments left: ["--version"]`, which itself proves the binary is on PATH and executable but produces confusing output that reads like an install failure. The setup guide (`docs/build-machine-tauri-driver-setup.md` Phase 1.1) uses `tauri-driver --help` for the clean check — the binary prints its usage banner and exits 0. (Source: M08.5 IRL build-machine install pass 2026-05-23; M08.5.5 Stage A.fix gotcha capture.)

84. **Edge auto-update silently invalidates `msedgedriver` — re-download on `Microsoft Edge WebDriver only supports Microsoft Edge version XYZ` errors.** Windows Edge updates itself in the background; `msedgedriver` does not. When the WebDriver's `session not created: ... only supports Microsoft Edge version XYZ` error fires, the driver lags behind the browser. Re-download from <https://developer.microsoft.com/en-us/microsoft-edge/tools/webdriver/> per setup guide Phase 2. Plan a quarterly re-install regardless to stay ahead of the drift; the Linux `webkit2gtk-driver` does not have this issue (apt-managed). (Source: M08.5 IRL install pass; M08.5.5 Stage A.fix.)

85. **`wdio.conf.ts` binary path is workspace-root `target/release/agent-runtime.exe`, NOT `src-tauri/target/release/...`.** The `src-tauri` crate is a Cargo workspace member (root `Cargo.toml` `members = [.., "src-tauri"]`), so `cargo` / `tauri build` emits the binary to the workspace-root `target/` directory, not to a crate-local one. The stale `src-tauri/target/release/` path the pre-M08.5 `wdio.conf.ts` carried worked on CI by accident (GitHub runners may follow a different code path) but failed on local Windows installs — the M08.5 IRL pass required a Windows junction workaround to bridge the path mismatch. Fixed at M08.5.A and re-explained inline in `wdio.conf.ts:38` for posterity. (Source: M03 PR #47 → M08.5.A.fix workspace-path fix; M08.5.5 Stage A.fix gotcha capture.)

86. **`npx tauri build --no-bundle` builds the Tauri package only — `runtime-drone` + `runtime-sandbox` sibling subprocesses need a separate `cargo build --release`.** The Tauri app spawns `runtime-drone.exe` and `runtime-sandbox.exe` from the `agent-runtime.exe` sibling directory at startup. `npx tauri build` only compiles the Tauri-package crate; the siblings must be built explicitly via `cargo build --release -p runtime-drone -p runtime-sandbox`. Without them the app crashes immediately with `drone IPC unavailable: spawn drone subprocess: ... file not found`. CI does both in two named steps; the M08.5.5 Stage A.fix `wdio.conf.ts::onPrepare()` hook now runs all three binary builds via one cargo invocation before spawning tauri-driver, so the local harness is idempotent + complete in one shot. (Source: M08.5 IRL build-machine install pass; M08.5.5 Stage A.fix harness fix.)

87. **WebDriver Actions API multi-step pointer drag stopped synthesizing HTML5 `dragstart` on Chromium 148+ — replace with JS event dispatch.** Pre-Chromium-148 the W3C WebDriver Actions API's multi-step pointer drag (`pointerDown` → small `pointerMove` past the ~5px threshold → duration-paced second `pointerMove` to target → `pointerUp`) synthesized HTML5 `dragstart` on chromium-driver-backed automation (msedgedriver / chromedriver). Chromium 148+ tightened the dragstart synthesis threshold; the same sequence no longer trips it, and tests that depend on HTML5 DnD silently break with `[data-testid] still not existing after 5000ms` (the test never finds the dropped node because dragstart never fired). Replace the W3C Actions API drag with JavaScript event dispatch via `browser.executeScript` per the `ePages-de/chromedriver-html5-dragdrop` upstream pattern (gotcha #32) — `new DragEvent(type, { dataTransfer: new DataTransfer(), clientX, clientY, bubbles, cancelable })` for each of `dragstart` / `dragover` / `drop` / `dragend`, threaded with one `DataTransfer` instance so `setData`/`getData` survive across the chain. The mechanism bypasses the WebDriver layer entirely; the dispatch sequence is documented in `tests/e2e-tauri/builder_drag.e2e.ts`. (Source: M08.5 IRL 🟡 #11; M08.5.5 Stage A.fix mechanism revival.)

88. **`browser.execute<R, A extends any[]>(...)` in WebdriverIO v9 is a DUAL-generic signature — a single-generic call fails `TS2558`.** Typing the return of `browser.execute(...)` in an e2e-tauri (wdio v9) test: the signature is `execute<ReturnValue, InnerArguments extends any[]>(script, ...args)`. `execute<R>(...)` errors `TS2558: Expected 2 type arguments, but got 1`; the correct no-arg-script form is `execute<R, []>(...)`. (Source: M08.6.D retro.)

89. **`npm run test:e2e:tauri` is a CI-only gate in practice — the per-stage local `verify_gates` does not run it.** The tauri-driver harness needs `npx tauri build --no-bundle` + `cargo build --release -p runtime-drone -p runtime-sandbox` + a `tauri-driver` launch — a multi-minute, locally-state-dependent setup. CI Linux + Windows are the structural verifiers (ADR-0021); a stage's local "all gates green" surface explicitly cites CI as the verifier for the `e2e-tauri-driver` job, and the `tests/e2e-tauri/` regression ships with the stage. The strict-TDD invariant (test file identical red→impl) holds regardless. (Source: M08.6.D retro.)

90. **`HashMap` iteration breaks byte-stable serialization — canonicalize through `serde_json::Value` before pretty-print.** A typify-generated type with `HashMap<K,V>` fields serialized via `serde_json::to_string_pretty(&value)` produces non-deterministic key order (HashMap's randomized iteration), so a save→load→save round-trip is NOT byte-stable. Fix: `let canonical = serde_json::to_value(&value)?; serde_json::to_string_pretty(&canonical)?` — `serde_json::Map` is `BTreeMap`-backed (the workspace does not enable `preserve_order`), so every nested object's keys serialize alphabetically. The byte-stable write pattern for any `framework.json`-class deterministic-output file; the resulting alphabetical field order is a deliberate, documented change. (Source: M08.6.C `save_framework` re-split.)

91. **An additive item-source with de-duplication needs a test that asserts the de-dup DISCRIMINATOR, not just presence.** When a list draws from N sources (e.g. the Palette: built-ins + installed + loaded-framework) and de-dups by a key, a test that only asserts "the item appears" is a tautology — it passes whether or not de-dup ran and whether the right source won. Assert the surviving item's source/discriminator attribute (which source won the collision) + `count == 1`, so the test fails if de-dup is absent or picks the wrong precedence. (Source: M08.6.E Palette loaded-framework source.)

92. **`cargo-mutants` on Windows: the scratch-copy build fails with `os error 1314` on a non-admin shell — use `--in-place` (or an elevated / Developer-Mode shell).** By default `cargo-mutants` copies the whole workspace tree into a temporary scratch directory and builds each mutant there (`mutants.rs`: "the entire workspace tree is copied to the temporary directory unless `--in-place` is used"). On a non-elevated Windows shell that copy step trips `os error 1314` — "A required privilege is not held by the client", the Windows symbolic-link-creation privilege (`SeCreateSymbolicLinkPrivilege`), which is admin / Developer-Mode-gated. Symptom: the mutation gate cannot build its first mutant; it reads like a cargo-mutants install failure but the binary is fine. Mitigation: run `cargo mutants --in-place` (mutates the source tree directly, no scratch-copy — it dirties the tree during the run, so use a clean working copy), or launch the gate from an elevated shell / with Windows Developer Mode on. Supersedes the M08.7.A-retro note that cargo-mutants "may be unavailable locally" — it is available; this is the concrete fix. Forward-applicable to every cluster's mutation gate on Windows (the CLAUDE.md §5 / cluster-pattern §5 blocking gate on execution + critical modules). (Source: M08.7.A rung-1 build — empirically hit `os error 1314` on the sandboxed-copy build, `--in-place` worked; cargo-mutants docs <https://mutants.rs/how-it-works.html>.)

93. **`cargo test` is fail-fast ACROSS test binaries — use `--no-fail-fast` to see a change's full blast radius.** `cargo test` runs each test target (the unit binary, every integration-test file, doc tests) as a separate process and stops after the first target that fails by default — so a behavior change that breaks tests in several integration files shows you only the first one. (Within a single binary libtest still reports all of that binary's failures; it is the cross-binary stop that hides the rest.) Mitigation: run `cargo test --workspace --no-fail-fast` when assessing the blast radius of a change (red-phase, or triaging a regression) so every affected target reports at once — then fix from the complete list instead of re-running N times. (Source: M08.7.A rung-1 build.)

94. **Translator / projector / IPC-mirror tests must be PRODUCER-DRIVEN (`serde_json::to_value(&real_event)`), never hand-built `json!` — a hand-built fixture pins the test to an INVENTED shape, so the real path can be 100% broken while the suite is green.** The M08.8.B.fix2 root cause: `replay.rs::translate_agent` read `payload.get("event")` while the real persisted signal is a serialized `AgentEvent` tagged by `.type`; its unit tests fabricated `{event:"spawned"}` — a shape the producer never emits — so the translator was *always broken against real signals* yet the suite passed (a §5-tautology / gotcha #66 instance at the **data-contract layer**). The TD-045 sweep proved the class recurs: `vdr.rs:98` reads a phantom `passed` field (TD-046 — `VerifyPassed`/`VerifyFailed` carry no `passed`; pass/fail is the variant tag → records every verify "fail", untested, latent until a verify producer lands); `recovery_lifecycle.rs`, `plan_projector`, the `vdr` decision arm, and the `ipc.ts` TS mirrors (TD-011) all hand-build fixtures (accurate-today but un-cross-checked → drift). Mitigation: any code that consumes a serialized type by field-name (a `payload_json`→typed translator, a signal projector, a snapshot deser, a hand-mirrored TS interface) gets a test that constructs its input by **serializing the real producer type** (`serde_json::to_value(&AgentEvent::Verify{..})`) or drives the real producer — so a field rename/retag breaks the *test*, not silently the production path. Prefer deserializing the whole serde type (`from_value::<AgentEvent>`) over hand-matching field-names (the B.fix2 fix collapsed ~170 lines of field-matching → ~10 lines of `from_value`). Pairs with gotcha #66 (tests-pass-contract-fails) + #41 (grep-verify) — this is the data-contract-layer instance, where the producer's serde shape *is* the contract. (Source: M08.8.B.fix2 `replay.rs` root cause + the TD-045 audit / TD-046.)

